---
title: EventWaitHandle
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- threading [.NET Framework], EventWaitHandle class
- EventWaitHandle class
- event wait handles [.NET Framework]
- threading [.NET Framework], cross-process synchronization
ms.assetid: 11ee0b38-d663-4617-b793-35eb6c64e9fc
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 86376919d7456167c58ada4145d213b39e050e33
ms.sourcegitcommit: 3ab9254890a52a50762995fa6d7d77a00348db7e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/20/2018
ms.locfileid: "46489113"
---
# <a name="eventwaithandle"></a><span data-ttu-id="b1586-102">EventWaitHandle</span><span class="sxs-lookup"><span data-stu-id="b1586-102">EventWaitHandle</span></span>
<span data-ttu-id="b1586-103"><xref:System.Threading.EventWaitHandle> Sinyal ve sinyalleri için bekleyen birbirleri ile iletişim kurmak için iş parçacığı sınıfı sağlar.</span><span class="sxs-lookup"><span data-stu-id="b1586-103">The <xref:System.Threading.EventWaitHandle> class allows threads to communicate with each other by signaling and by waiting for signals.</span></span> <span data-ttu-id="b1586-104">Olay bekleme tanıtıcıları (yalnızca olaylar olarak da bilinir) bir veya daha fazla bekleyen iş parçacıklarının serbest bırakmak için işareti bekleme tanıtıcıları ' dir.</span><span class="sxs-lookup"><span data-stu-id="b1586-104">Event wait handles (also referred to simply as events) are wait handles that can be signaled in order to release one or more waiting threads.</span></span> <span data-ttu-id="b1586-105">Bu sinyal sonra bir olay bekleme tanıtıcısı el ile veya otomatik olarak sıfırlanır.</span><span class="sxs-lookup"><span data-stu-id="b1586-105">After it is signaled, an event wait handle is reset either manually or automatically.</span></span> <span data-ttu-id="b1586-106"><xref:System.Threading.EventWaitHandle> Sınıfı ya da bir yerel olay bekleme tanıtıcısı (yerel olay) temsil edebilir veya adlandırılmış sistem olay işleyici (veya sistem olayın, tüm işlemler için görünür olarak adlandırılır) bekleyin.</span><span class="sxs-lookup"><span data-stu-id="b1586-106">The <xref:System.Threading.EventWaitHandle> class can represent either a local event wait handle (local event) or a named system event wait handle (named event or system event, visible to all processes).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="b1586-107">Olay bekleme tanıtıcıları .NET olmayan [olayları](../events/index.md).</span><span class="sxs-lookup"><span data-stu-id="b1586-107">Event wait handles are not .NET [events](../events/index.md).</span></span> <span data-ttu-id="b1586-108">Temsilciler ya da olay işleyicileri kullanılan vardır.</span><span class="sxs-lookup"><span data-stu-id="b1586-108">There are no delegates or event handlers involved.</span></span> <span data-ttu-id="b1586-109">"Olay" sözcük olduğundan, bunlar geleneksel için işletim sistemi olaylar olarak adlandırılmıştır ve bekleyen iş parçacığı bekleme tanıtıcısı sinyal eylemi belirttiğinden tanımlamak için bir olayın oluştuğunu kullanılır.</span><span class="sxs-lookup"><span data-stu-id="b1586-109">The word "event" is used to describe them because they have traditionally been referred to as operating-system events, and because the act of signaling the wait handle indicates to waiting threads that an event has occurred.</span></span>  
  
 <span data-ttu-id="b1586-110">Sistem tarafından korunan eşitleme nesneleri hem yerel hem de adlandırılmış olay bekleme tanıtıcıları kullanan <xref:Microsoft.Win32.SafeHandles.SafeWaitHandle> sarmalayıcıları kaynakların serbest bırakıldığından emin olmak için.</span><span class="sxs-lookup"><span data-stu-id="b1586-110">Both local and named event wait handles use system synchronization objects, which are protected by <xref:Microsoft.Win32.SafeHandles.SafeWaitHandle> wrappers to ensure that the resources are released.</span></span> <span data-ttu-id="b1586-111">Kullanabileceğiniz <xref:System.Threading.WaitHandle.Dispose%2A> ne zaman, nesneyi kullanmayı bitirdikten hemen ücretsiz kaynaklar için yöntem.</span><span class="sxs-lookup"><span data-stu-id="b1586-111">You can use the <xref:System.Threading.WaitHandle.Dispose%2A> method to free the resources immediately when you have finished using the object.</span></span>  
  
## <a name="event-wait-handles-that-reset-automatically"></a><span data-ttu-id="b1586-112">Otomatik sıfırlama olay bekleme tanıtıcıları</span><span class="sxs-lookup"><span data-stu-id="b1586-112">Event Wait Handles That Reset Automatically</span></span>  
 <span data-ttu-id="b1586-113">Otomatik sıfırlama olay belirterek oluşturma <xref:System.Threading.EventResetMode.AutoReset?displayProperty=nameWithType> oluşturduğunuzda <xref:System.Threading.EventWaitHandle> nesne.</span><span class="sxs-lookup"><span data-stu-id="b1586-113">You create an automatic reset event by specifying <xref:System.Threading.EventResetMode.AutoReset?displayProperty=nameWithType> when you create the <xref:System.Threading.EventWaitHandle> object.</span></span> <span data-ttu-id="b1586-114">Adından da anlaşılacağı gibi bu eşitleme olay tek bir bekleyen iş parçacığı bırakılıyor sonra sinyal olduğunda otomatik olarak sıfırlar.</span><span class="sxs-lookup"><span data-stu-id="b1586-114">As its name implies, this synchronization event resets automatically when signaled, after releasing a single waiting thread.</span></span> <span data-ttu-id="b1586-115">Çağırarak olayın sinyal kendi <xref:System.Threading.EventWaitHandle.Set%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b1586-115">Signal the event by calling its <xref:System.Threading.EventWaitHandle.Set%2A> method.</span></span>  
  
 <span data-ttu-id="b1586-116">Otomatik sıfırlama olaylar genellikle bir kerede tek bir iş parçacığı için özel bir kaynağa erişim sağlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="b1586-116">Automatic reset events are usually used to provide exclusive access to a resource for a single thread at a time.</span></span> <span data-ttu-id="b1586-117">Bir iş parçacığı çağrı yaparak kaynak isteklerini <xref:System.Threading.WaitHandle.WaitOne%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b1586-117">A thread requests the resource by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span> <span data-ttu-id="b1586-118">Başka bir iş parçacığı bekleme tanıtıcısı bulunduran ise, yöntem döndürür `true` ve çağıran iş parçacığını kaynak denetime sahiptir.</span><span class="sxs-lookup"><span data-stu-id="b1586-118">If no other thread is holding the wait handle, the method returns `true` and the calling thread has control of the resource.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="b1586-119">Tüm eşitleme mekanizmaları gibi ile korunan bir kaynağa erişmeden önce tüm kod yolları üzerinde uygun bekleme tanıtıcısı bekleyin emin olmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="b1586-119">As with all synchronization mechanisms, you must ensure that all code paths wait on the appropriate wait handle before accessing a protected resource.</span></span> <span data-ttu-id="b1586-120">İş parçacığı eşitleme ortaktır.</span><span class="sxs-lookup"><span data-stu-id="b1586-120">Thread synchronization is cooperative.</span></span>  
  
 <span data-ttu-id="b1586-121">İş parçacığı beklenirken otomatik sıfırlama olaya sinyal vermediyse, bir iş parçacığı üzerinde bekleyin girişiminde bulunmadan sinyal kalır.</span><span class="sxs-lookup"><span data-stu-id="b1586-121">If an automatic reset event is signaled when no threads are waiting, it remains signaled until a thread attempts to wait on it.</span></span> <span data-ttu-id="b1586-122">Olay iş parçacığını serbest bırakır ve hemen sıfırlar, sonraki iş parçacığı engelleme.</span><span class="sxs-lookup"><span data-stu-id="b1586-122">The event releases the thread and immediately resets, blocking subsequent threads.</span></span>  
  
## <a name="event-wait-handles-that-reset-manually"></a><span data-ttu-id="b1586-123">El ile Sıfırlanan olay bekleme tanıtıcıları</span><span class="sxs-lookup"><span data-stu-id="b1586-123">Event Wait Handles That Reset Manually</span></span>  
 <span data-ttu-id="b1586-124">Belirterek elle sıfırlama olayı oluşturma <xref:System.Threading.EventResetMode.ManualReset?displayProperty=nameWithType> oluşturduğunuzda <xref:System.Threading.EventWaitHandle> nesne.</span><span class="sxs-lookup"><span data-stu-id="b1586-124">You create a manual reset event by specifying <xref:System.Threading.EventResetMode.ManualReset?displayProperty=nameWithType> when you create the <xref:System.Threading.EventWaitHandle> object.</span></span> <span data-ttu-id="b1586-125">Adından da anlaşılacağı gibi sinyal sonra bu eşitleme olay el ile sıfırlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="b1586-125">As its name implies, this synchronization event must be reset manually after it has been signaled.</span></span> <span data-ttu-id="b1586-126">Bu, çağırarak sıfırlanana kadar kendi <xref:System.Threading.EventWaitHandle.Reset%2A> yöntemi, olay tutamacı bekleyin iş parçacığı devam hemen engellemeden.</span><span class="sxs-lookup"><span data-stu-id="b1586-126">Until it is reset, by calling its <xref:System.Threading.EventWaitHandle.Reset%2A> method, threads that wait on the event handle proceed immediately without blocking.</span></span>  
  
 <span data-ttu-id="b1586-127">El ile bir corral, ağ geçidi gibi olay davranır sıfırlayın.</span><span class="sxs-lookup"><span data-stu-id="b1586-127">A manual reset event acts like the gate of a corral.</span></span> <span data-ttu-id="b1586-128">Olaya sinyal gönderilmedi, içinde bir corral atları gibi iş parçacığı üzerinde bekleyin engelleyin.</span><span class="sxs-lookup"><span data-stu-id="b1586-128">When the event is not signaled, threads that wait on it block, like horses in a corral.</span></span> <span data-ttu-id="b1586-129">Ne zaman olayın sinyal, çağırarak kendi <xref:System.Threading.EventWaitHandle.Set%2A> yöntemi, tüm bekleyen iş parçacığı devam etmek ücretsiz.</span><span class="sxs-lookup"><span data-stu-id="b1586-129">When the event is signaled, by calling its <xref:System.Threading.EventWaitHandle.Set%2A> method, all waiting threads are free to proceed.</span></span> <span data-ttu-id="b1586-130">Olay kadar sinyal kalır, <xref:System.Threading.EventWaitHandle.Reset%2A> yöntemi çağrılır.</span><span class="sxs-lookup"><span data-stu-id="b1586-130">The event remains signaled until its <xref:System.Threading.EventWaitHandle.Reset%2A> method is called.</span></span> <span data-ttu-id="b1586-131">Bu elle sıfırlama olayı bir iş parçacığı bir görev bitene kadar beklemeniz gereken iş parçacıklarını tutmak için ideal bir yol sağlar.</span><span class="sxs-lookup"><span data-stu-id="b1586-131">This makes the manual reset event an ideal way to hold up threads that need to wait until one thread finishes a task.</span></span>  
  
 <span data-ttu-id="b1586-132">Bir corral bırakarak atları gibi yayımlanmış iş parçacığı işletim sistemi tarafından zamanlanması ve yürütme devam etmek için zaman alır.</span><span class="sxs-lookup"><span data-stu-id="b1586-132">Like horses leaving a corral, it takes time for the released threads to be scheduled by the operating system and to resume execution.</span></span> <span data-ttu-id="b1586-133">Varsa <xref:System.Threading.EventWaitHandle.Reset%2A> yöntemi, tüm iş parçacıklarının yürütülmesine devam ettirildi önce çağrılır, diğer iş parçacıkları yine engelleyin.</span><span class="sxs-lookup"><span data-stu-id="b1586-133">If the <xref:System.Threading.EventWaitHandle.Reset%2A> method is called before all the threads have resumed execution, the remaining threads once again block.</span></span> <span data-ttu-id="b1586-134">Hangi iş parçacıklarını sürdürme ve hangi iş parçacıkları blok rastgele etkenlere bağlıdır, bu iş parçacığı sayısını sistemdeki yük gibi bekleyen Zamanlayıcı ve benzeri için.</span><span class="sxs-lookup"><span data-stu-id="b1586-134">Which threads resume and which threads block depends on random factors like the load on the system, the number of threads waiting for the scheduler, and so on.</span></span> <span data-ttu-id="b1586-135">Olay sinyalini verir iş parçacığı en yaygın kullanım desenini olduğu sinyali sonra sona ererse, bir sorun değildir.</span><span class="sxs-lookup"><span data-stu-id="b1586-135">This is not a problem if the thread that signals the event ends after signaling, which is the most common usage pattern.</span></span> <span data-ttu-id="b1586-136">Tüm bekleyen iş parçacığı devam ettirildi kadar tüm yeni göreve başlamak için bir olay iş parçacığı devam ettirildi bekleme sinyal iş parçacığı istiyorsanız engellemelidir.</span><span class="sxs-lookup"><span data-stu-id="b1586-136">If you want the thread that signaled the event to begin a new task after all the waiting threads have resumed, you must block it until all the waiting threads have resumed.</span></span> <span data-ttu-id="b1586-137">Aksi takdirde bir yarış durumu olması ve kodunuzu davranışını tahmin edilemez.</span><span class="sxs-lookup"><span data-stu-id="b1586-137">Otherwise, you have a race condition, and the behavior of your code is unpredictable.</span></span>  
  
## <a name="features-common-to-automatic-and-manual-events"></a><span data-ttu-id="b1586-138">Otomatik ve el ile olaylar ortak özellikleri</span><span class="sxs-lookup"><span data-stu-id="b1586-138">Features Common to Automatic and Manual Events</span></span>  
 <span data-ttu-id="b1586-139">Genellikle, bir veya daha fazla iş parçacığı engelleme üzerinde bir <xref:System.Threading.EventWaitHandle> engeli bir iş parçacığı çağrı kadar <xref:System.Threading.EventWaitHandle.Set%2A> bekleyen iş parçacıklarının (durumunda otomatik sıfırlama olayları) birini veya tümünü serbest yöntemi (olaylar olması durumunda el ile sıfırlama).</span><span class="sxs-lookup"><span data-stu-id="b1586-139">Typically, one or more threads block on an <xref:System.Threading.EventWaitHandle> until an unblocked thread calls the <xref:System.Threading.EventWaitHandle.Set%2A> method, which releases one of the waiting threads (in the case of automatic reset events) or all of them (in the case of manual reset events).</span></span> <span data-ttu-id="b1586-140">Bir iş parçacığı sinyal verebilirsiniz bir <xref:System.Threading.EventWaitHandle> ve ardından bunun üzerinde bir atomik işlem olarak statik çağırarak engelle <xref:System.Threading.WaitHandle.SignalAndWait%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b1586-140">A thread can signal an <xref:System.Threading.EventWaitHandle> and then block on it, as an atomic operation, by calling the static <xref:System.Threading.WaitHandle.SignalAndWait%2A?displayProperty=nameWithType> method.</span></span>  
  
 <span data-ttu-id="b1586-141"><xref:System.Threading.EventWaitHandle> nesneleri ile statik kullanılabilir <xref:System.Threading.WaitHandle.WaitAll%2A?displayProperty=nameWithType> ve <xref:System.Threading.WaitHandle.WaitAny%2A?displayProperty=nameWithType> yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="b1586-141"><xref:System.Threading.EventWaitHandle> objects can be used with the static <xref:System.Threading.WaitHandle.WaitAll%2A?displayProperty=nameWithType> and <xref:System.Threading.WaitHandle.WaitAny%2A?displayProperty=nameWithType> methods.</span></span> <span data-ttu-id="b1586-142">Çünkü <xref:System.Threading.EventWaitHandle> ve <xref:System.Threading.Mutex> sınıflarının her ikisi de türetilen <xref:System.Threading.WaitHandle>, bu yöntemlerle hem sınıflarını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b1586-142">Because the <xref:System.Threading.EventWaitHandle> and <xref:System.Threading.Mutex> classes both derive from <xref:System.Threading.WaitHandle>, you can use both classes with these methods.</span></span>  
  
### <a name="named-events"></a><span data-ttu-id="b1586-143">Adlandırılmış olayları</span><span class="sxs-lookup"><span data-stu-id="b1586-143">Named Events</span></span>  
 <span data-ttu-id="b1586-144">Windows işletim sistemi olay bekleme tanıtıcıları adları olmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="b1586-144">The Windows operating system allows event wait handles to have names.</span></span> <span data-ttu-id="b1586-145">Sistem genelinde bir adlandırılmış olayıdır.</span><span class="sxs-lookup"><span data-stu-id="b1586-145">A named event is system wide.</span></span> <span data-ttu-id="b1586-146">Diğer bir deyişle, adlandırılmış olayı oluşturulduktan sonra tüm iş parçacıklarının tüm işlemler için görünür.</span><span class="sxs-lookup"><span data-stu-id="b1586-146">That is, once the named event is created, it is visible to all threads in all processes.</span></span> <span data-ttu-id="b1586-147">Bu nedenle, adlandırılmış olaylar, işlemler ve bunun yanı sıra iş parçacıkları etkinlikleri eşitlemek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="b1586-147">Thus, named events can be used to synchronize the activities of processes as well as threads.</span></span>  
  
 <span data-ttu-id="b1586-148">Oluşturabileceğiniz bir <xref:System.Threading.EventWaitHandle> belirten bir olay adı oluşturucular birini kullanarak bir adlandırılmış sistem olayı temsil eden nesne.</span><span class="sxs-lookup"><span data-stu-id="b1586-148">You can create an <xref:System.Threading.EventWaitHandle> object that represents a named system event by using one of the constructors that specifies an event name.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="b1586-149">Adlandırılmış olaylar Sistem genelinde olduğundan, birden çok mümkün <xref:System.Threading.EventWaitHandle> adlı olay aynı temsil eden nesneleri.</span><span class="sxs-lookup"><span data-stu-id="b1586-149">Because named events are system wide, it is possible to have multiple <xref:System.Threading.EventWaitHandle> objects that represent the same named event.</span></span> <span data-ttu-id="b1586-150">Bir oluşturucu çağırmak her zaman veya <xref:System.Threading.EventWaitHandle.OpenExisting%2A> yöntemi, yeni bir <xref:System.Threading.EventWaitHandle> nesnesi oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="b1586-150">Each time you call a constructor, or the <xref:System.Threading.EventWaitHandle.OpenExisting%2A> method, a new <xref:System.Threading.EventWaitHandle> object is created.</span></span> <span data-ttu-id="b1586-151">Tekrar tekrar aynı adı belirterek aynı adlandırılmış olayı temsil eden birden çok nesneleri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="b1586-151">Specifying the same name repeatedly creates multiple objects that represent the same named event.</span></span>  
  
 <span data-ttu-id="b1586-152">Olayları adlı kullanırken dikkatli olmanız önerilir.</span><span class="sxs-lookup"><span data-stu-id="b1586-152">Caution is advised in using named events.</span></span> <span data-ttu-id="b1586-153">Aynı adı kullanan başka bir işlem, iş parçacıkları sistem genelinde olduklarından beklenmedik bir şekilde engelleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b1586-153">Because they are system wide, another process that uses the same name can block your threads unexpectedly.</span></span> <span data-ttu-id="b1586-154">Kötü amaçlı kod aynı bilgisayarda çalıştırıldığında bu bir hizmet reddi saldırısı temel olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b1586-154">Malicious code executing on the same computer could use this as the basis of a denial-of-service attack.</span></span>  
  
 <span data-ttu-id="b1586-155">Erişim denetimi güvenliği korumak için kullanmak bir <xref:System.Threading.EventWaitHandle> belirten bir kurucu kullanarak adlandırılmış bir olay tercihen temsil eden nesne bir <xref:System.Security.AccessControl.EventWaitHandleSecurity> nesne.</span><span class="sxs-lookup"><span data-stu-id="b1586-155">Use access control security to protect an <xref:System.Threading.EventWaitHandle> object that represents a named event, preferably by using a constructor that specifies an <xref:System.Security.AccessControl.EventWaitHandleSecurity> object.</span></span> <span data-ttu-id="b1586-156">Erişim denetimi güvenlik kullanarak uygulayabilirsiniz <xref:System.Threading.EventWaitHandle.SetAccessControl%2A> yöntemi, ancak bu bir güvenlik açığı korumalı zaman arasında olay bekleme tanıtıcısı oluşturulduğu zaman penceresi bırakır.</span><span class="sxs-lookup"><span data-stu-id="b1586-156">You can also apply access control security using the <xref:System.Threading.EventWaitHandle.SetAccessControl%2A> method, but this leaves a window of vulnerability between the time the event wait handle is created and the time it is protected.</span></span> <span data-ttu-id="b1586-157">Olayları koruma erişim denetimi ile güvenlik kötü amaçlı saldırıların önlenmesine yardımcı olur, ancak yanlışlıkla ad çakışması sorunu çözmez.</span><span class="sxs-lookup"><span data-stu-id="b1586-157">Protecting events with access control security helps prevent malicious attacks, but it does not solve the problem of unintentional name collisions.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="b1586-158">Farklı <xref:System.Threading.EventWaitHandle> türetilen sınıflar, sınıf <xref:System.Threading.AutoResetEvent> ve <xref:System.Threading.ManualResetEvent> temsil yalnızca yerel tutamaçlarını bekleyebileceği.</span><span class="sxs-lookup"><span data-stu-id="b1586-158">Unlike the <xref:System.Threading.EventWaitHandle> class, the derived classes <xref:System.Threading.AutoResetEvent> and <xref:System.Threading.ManualResetEvent> can represent only local wait handles.</span></span> <span data-ttu-id="b1586-159">Bunlar, adlandırılmış sistem olaylarını temsil edemez.</span><span class="sxs-lookup"><span data-stu-id="b1586-159">They cannot represent named system events.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b1586-160">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="b1586-160">See also</span></span>

- <xref:System.Threading.EventWaitHandle>  
- <xref:System.Threading.WaitHandle>  
- <xref:System.Threading.AutoResetEvent>  
- <xref:System.Threading.ManualResetEvent>  
- [<span data-ttu-id="b1586-161">EventWaitHandle, AutoResetEvent, CountdownEvent, ManualResetEvent</span><span class="sxs-lookup"><span data-stu-id="b1586-161">EventWaitHandle, AutoResetEvent, CountdownEvent, ManualResetEvent</span></span>](../../../docs/standard/threading/eventwaithandle-autoresetevent-countdownevent-manualresetevent.md)
