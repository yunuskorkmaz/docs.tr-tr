---
title: "Normal İfade Seçenekleri"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords:
- regular expressions, options
- constructs, options
- .NET Framework regular expressions, options
- inline option constructs
- options parameter
ms.assetid: c82dc689-7e82-4767-a18d-cd24ce5f05e9
caps.latest.revision: "27"
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.openlocfilehash: 7bc068cc248e1ca8e1d3c64eaa4132682721e035
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/18/2017
---
# <a name="regular-expression-options"></a><span data-ttu-id="fba6b-102">Normal İfade Seçenekleri</span><span class="sxs-lookup"><span data-stu-id="fba6b-102">Regular Expression Options</span></span>
<a name="Top"></a><span data-ttu-id="fba6b-103">Varsayılan olarak, herhangi bir normal ifade deseni değişmez değer karakter ile giriş dizesi karşılaştırması büyük küçük harfe duyarlı olduğundan, normal ifade deseni boşluk değişmez değer boşluk karakterleri ve yakalama gruplarında normal bir ifade olarak yorumlanır açıkça örtük olarak da adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-103">By default, the comparison of an input string with any literal characters in a regular expression pattern is case sensitive, white space in a regular expression pattern is interpreted as literal white-space characters, and capturing groups in a regular expression are named implicitly as well as explicitly.</span></span> <span data-ttu-id="fba6b-104">Normal ifade seçenekleri belirterek bu ve diğer birkaç varsayılan normal ifade davranışının yönlerini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="fba6b-104">You can modify these and several other aspects of default regular expression behavior by specifying regular expression options.</span></span> <span data-ttu-id="fba6b-105">Aşağıdaki tabloda listelenen, bu seçenekleri normal ifade deseni bir parçası olarak dahil edilen satır içi olabilir ya da için sağlanabilir bir <xref:System.Text.RegularExpressions.Regex?displayProperty=nameWithType> sınıfı oluşturucusu veya statik desen eşleştirme yöntemi olarak bir <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> numaralandırma değeri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-105">These options, which are listed in the following table, can be included inline as part of the regular expression pattern, or they can be supplied to a <xref:System.Text.RegularExpressions.Regex?displayProperty=nameWithType> class constructor or static pattern matching method as a <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> enumeration value.</span></span>  
  
|<span data-ttu-id="fba6b-106">RegexOptions üyesi</span><span class="sxs-lookup"><span data-stu-id="fba6b-106">RegexOptions member</span></span>|<span data-ttu-id="fba6b-107">Satır içi karakter</span><span class="sxs-lookup"><span data-stu-id="fba6b-107">Inline character</span></span>|<span data-ttu-id="fba6b-108">Efekt</span><span class="sxs-lookup"><span data-stu-id="fba6b-108">Effect</span></span>|  
|-------------------------|----------------------|------------|  
|<xref:System.Text.RegularExpressions.RegexOptions.None>|<span data-ttu-id="fba6b-109">Yok</span><span class="sxs-lookup"><span data-stu-id="fba6b-109">Not available</span></span>|<span data-ttu-id="fba6b-110">Varsayılan davranışını kullanın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-110">Use default behavior.</span></span> <span data-ttu-id="fba6b-111">Daha fazla bilgi için bkz: [varsayılan seçenekleri](#Default).</span><span class="sxs-lookup"><span data-stu-id="fba6b-111">For more information, see [Default Options](#Default).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase>|`i`|<span data-ttu-id="fba6b-112">Büyük küçük harf duyarlı eşleme kullanın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-112">Use case-insensitive matching.</span></span> <span data-ttu-id="fba6b-113">Daha fazla bilgi için bkz: [Case-Insensitive eşleşen](#Case).</span><span class="sxs-lookup"><span data-stu-id="fba6b-113">For more information, see [Case-Insensitive Matching](#Case).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.Multiline>|`m`|<span data-ttu-id="fba6b-114">Çok satırlı moda kullanın, burada `^` ve `$` başlangıcını ve bitişini her satırın (yerine, başlangıç ve bitiş Giriş dizesinin) eşleşmesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-114">Use multiline mode, where `^` and `$` match the beginning and end of each line (instead of the beginning and end of the input string).</span></span> <span data-ttu-id="fba6b-115">Daha fazla bilgi için bkz: [çok satırlı modu](#Multiline).</span><span class="sxs-lookup"><span data-stu-id="fba6b-115">For more information, see [Multiline Mode](#Multiline).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.Singleline>|`s`|<span data-ttu-id="fba6b-116">Her karakter nokta (.) eşleştiği tek satırlı modu kullan (her karakteri dışındaki yerine `\n`).</span><span class="sxs-lookup"><span data-stu-id="fba6b-116">Use single-line mode, where the period (.) matches every character (instead of every character except `\n`).</span></span> <span data-ttu-id="fba6b-117">Daha fazla bilgi için bkz: [Singleline modu](#Singleline).</span><span class="sxs-lookup"><span data-stu-id="fba6b-117">For more information, see [Singleline Mode](#Singleline).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture>|`n`|<span data-ttu-id="fba6b-118">Adsız grupları yakalamayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-118">Do not capture unnamed groups.</span></span> <span data-ttu-id="fba6b-119">Yalnızca geçerli yakalamaları açıkça adlı veya numaralı grupları formun `(?<` *adı* `>` *alt*`)`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-119">The only valid captures are explicitly named or numbered groups of the form `(?<`*name*`>` *subexpression*`)`.</span></span> <span data-ttu-id="fba6b-120">Daha fazla bilgi için bkz: [yalnızca açık yakalar](#Explicit).</span><span class="sxs-lookup"><span data-stu-id="fba6b-120">For more information, see [Explicit Captures Only](#Explicit).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.Compiled>|<span data-ttu-id="fba6b-121">Yok</span><span class="sxs-lookup"><span data-stu-id="fba6b-121">Not available</span></span>|<span data-ttu-id="fba6b-122">Normal ifade bir derleme derleyin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-122">Compile the regular expression to an assembly.</span></span> <span data-ttu-id="fba6b-123">Daha fazla bilgi için bkz: [derlenmiş normal ifadeler](#Compiled).</span><span class="sxs-lookup"><span data-stu-id="fba6b-123">For more information, see [Compiled Regular Expressions](#Compiled).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace>|`x`|<span data-ttu-id="fba6b-124">Atlanmayan boşluk düzeni dışlayın ve sonra sayı işareti açıklamaları etkinleştir (`#`).</span><span class="sxs-lookup"><span data-stu-id="fba6b-124">Exclude unescaped white space from the pattern, and enable comments after a number sign (`#`).</span></span> <span data-ttu-id="fba6b-125">Daha fazla bilgi için bkz: [yoksay boşluk](#Whitespace).</span><span class="sxs-lookup"><span data-stu-id="fba6b-125">For more information, see [Ignore Whitespace](#Whitespace).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.RightToLeft>|<span data-ttu-id="fba6b-126">Yok</span><span class="sxs-lookup"><span data-stu-id="fba6b-126">Not available</span></span>|<span data-ttu-id="fba6b-127">Arama yönünü değiştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-127">Change the search direction.</span></span> <span data-ttu-id="fba6b-128">Arama sağdan sola yerine soldan sağa taşır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-128">Search moves from right to left instead of from left to right.</span></span> <span data-ttu-id="fba6b-129">Daha fazla bilgi için bkz: [sağdan sola modu](#RightToLeft).</span><span class="sxs-lookup"><span data-stu-id="fba6b-129">For more information, see [Right-to-Left Mode](#RightToLeft).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.ECMAScript>|<span data-ttu-id="fba6b-130">Yok</span><span class="sxs-lookup"><span data-stu-id="fba6b-130">Not available</span></span>|<span data-ttu-id="fba6b-131">İfadenin ECMAScript uyumlu davranışı etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-131">Enable ECMAScript-compliant behavior for the expression.</span></span> <span data-ttu-id="fba6b-132">Daha fazla bilgi için bkz: [ECMAScript eşleşen davranış](#ECMAScript).</span><span class="sxs-lookup"><span data-stu-id="fba6b-132">For more information, see [ECMAScript Matching Behavior](#ECMAScript).</span></span>|  
|<xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant>|<span data-ttu-id="fba6b-133">Yok</span><span class="sxs-lookup"><span data-stu-id="fba6b-133">Not available</span></span>|<span data-ttu-id="fba6b-134">Kültürel dil farklılıkları yoksay.</span><span class="sxs-lookup"><span data-stu-id="fba6b-134">Ignore cultural differences in language.</span></span> <span data-ttu-id="fba6b-135">Daha fazla bilgi için bkz: [sabit kültür kullanarak karşılaştırma](#Invariant).</span><span class="sxs-lookup"><span data-stu-id="fba6b-135">For more information, see [Comparison Using the Invariant Culture](#Invariant).</span></span>|  
  
## <a name="specifying-the-options"></a><span data-ttu-id="fba6b-136">Seçeneklerini belirtme</span><span class="sxs-lookup"><span data-stu-id="fba6b-136">Specifying the Options</span></span>  
 <span data-ttu-id="fba6b-137">Normal ifadeler için seçeneklerini üç yöntemden birini belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="fba6b-137">You can specify options for regular expressions in one of three ways:</span></span>  
  
-   <span data-ttu-id="fba6b-138">İçinde `options` parametresinin bir <xref:System.Text.RegularExpressions.Regex?displayProperty=nameWithType> sınıfı oluşturucusu veya statik (`Shared` Visual Basic'te) desen eşleştirme yöntemi gibi <xref:System.Text.RegularExpressions.Regex.%23ctor%28System.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> veya <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-138">In the `options` parameter of a <xref:System.Text.RegularExpressions.Regex?displayProperty=nameWithType> class constructor or static (`Shared` in Visual Basic) pattern-matching method, such as <xref:System.Text.RegularExpressions.Regex.%23ctor%28System.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> or <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType>.</span></span> <span data-ttu-id="fba6b-139">`options` Parametresi birleşimidir bit düzeyinde OR <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> değerleri numaralandırılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-139">The `options` parameter is a bitwise OR combination of <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> enumerated values.</span></span>  
  
     <span data-ttu-id="fba6b-140">Zaman seçenekleri sağlanır için bir <xref:System.Text.RegularExpressions.Regex> kullanarak örnek `options` parametresi bir sınıf oluşturucu, seçeneklerdir atanan <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> özelliği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-140">When options are supplied to a <xref:System.Text.RegularExpressions.Regex> instance by using the `options` parameter of a class constructor, the options are are assigned to the <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="fba6b-141">Ancak, <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> özelliği, normal ifade deseni satır içi seçeneklerinde yansıtacak değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-141">However, the <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> property does not reflect inline options in the regular expression pattern itself.</span></span>  
  
     <span data-ttu-id="fba6b-142">Aşağıdaki örnek, bir gösterim sağlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-142">The following example provides an illustration.</span></span> <span data-ttu-id="fba6b-143">Kullandığı `options` parametresinin <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> yöntemi büyük küçük harf duyarsız eşleşen etkinleştirmek ve "d" harfiyle başlayan sözcükleri tanımlarken düzeni boşluk yoksay.</span><span class="sxs-lookup"><span data-stu-id="fba6b-143">It uses the `options` parameter of the <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method to enable case-insensitive matching and to ignore pattern white space when identifying words that begin with the letter "d".</span></span>  
  
     [!code-csharp[Conceptual.Regex.Language.Options#6](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/example1.cs#6)]
     [!code-vb[Conceptual.Regex.Language.Options#6](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/example1.vb#6)]  
  
-   <span data-ttu-id="fba6b-144">Satır içi bir normal ifade deseni sözdizimiyle seçeneklerinde uygulayarak `(?imnsx-imnsx)`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-144">By applying inline options in a regular expression pattern with the syntax `(?imnsx-imnsx)`.</span></span> <span data-ttu-id="fba6b-145">Seçenek desen seçeneği seçeneği başka bir satır içi seçenek tarafından tanımsız noktasına veya ucunu bir desen tanımlanan noktasından uygulanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-145">The option applies to the pattern from the point that the option is defined to either the end of the pattern or to the point at which the option is undefined by another inline option.</span></span> <span data-ttu-id="fba6b-146">Unutmayın <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> özelliği bir <xref:System.Text.RegularExpressions.Regex> örneği, bu satır içi seçenekleri yansıtmak değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-146">Note that the <xref:System.Text.RegularExpressions.RegexOptions?displayProperty=nameWithType> property of a <xref:System.Text.RegularExpressions.Regex> instance does not reflect these inline options.</span></span> <span data-ttu-id="fba6b-147">Daha fazla bilgi için bkz: [çeşitli yapıları](../../../docs/standard/base-types/miscellaneous-constructs-in-regular-expressions.md) konu.</span><span class="sxs-lookup"><span data-stu-id="fba6b-147">For more information, see the [Miscellaneous Constructs](../../../docs/standard/base-types/miscellaneous-constructs-in-regular-expressions.md) topic.</span></span>  
  
     <span data-ttu-id="fba6b-148">Aşağıdaki örnek, bir gösterim sağlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-148">The following example provides an illustration.</span></span> <span data-ttu-id="fba6b-149">Büyük küçük harf duyarsız eşleşen etkinleştirmek ve "d" harfiyle başlayan sözcükleri tanımlarken düzeni boşluk yoksaymak için satır içi seçeneklerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-149">It uses inline options to enable case-insensitive matching and to ignore pattern white space when identifying words that begin with the letter "d".</span></span>  
  
     [!code-csharp[Conceptual.Regex.Language.Options#7](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/example1.cs#7)]
     [!code-vb[Conceptual.Regex.Language.Options#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/example1.vb#7)]  
  
-   <span data-ttu-id="fba6b-150">Normal ifade deseni sözdizimiyle içinde belirli bir gruplandırma satır içi seçeneklerinde uygulayarak oluşturmak `(?imnsx-imnsx:` *alt*`)`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-150">By applying inline options in a particular grouping construct in a regular expression pattern with the syntax `(?imnsx-imnsx:`*subexpression*`)`.</span></span> <span data-ttu-id="fba6b-151">Bir seçenek kümesini kümesi kapanmadan önce hiçbir oturum; eksi bir seçenek kümesini önce kümesi devre dışı bırakır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-151">No sign before a set of options turns the set on; a minus sign before a set of options turns the set off.</span></span> <span data-ttu-id="fba6b-152">(`?` seçenekleri etkin veya devre dışı bırakılan gereklidir dil yapısı 's sözdizimi, sabit bir parçasıdır.) Seçeneği, yalnızca bu grup için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-152">(`?` is a fixed part of the language construct's syntax that is required whether options are enabled or disabled.) The option applies only to that group.</span></span> <span data-ttu-id="fba6b-153">Daha fazla bilgi için bkz: [gruplandırma yapıları](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="fba6b-153">For more information, see [Grouping Constructs](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span></span>  
  
     <span data-ttu-id="fba6b-154">Aşağıdaki örnek, bir gösterim sağlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-154">The following example provides an illustration.</span></span> <span data-ttu-id="fba6b-155">Büyük küçük harf duyarsız eşleşen etkinleştirmek ve "d" harfiyle başlayan sözcükleri tanımlarken düzeni boşluk yoksaymak için satır içi seçenekleri bir gruplama yapısı kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-155">It uses inline options in a grouping construct to enable case-insensitive matching and to ignore pattern white space when identifying words that begin with the letter "d".</span></span>  
  
     [!code-csharp[Conceptual.Regex.Language.Options#8](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/example1.cs#8)]
     [!code-vb[Conceptual.Regex.Language.Options#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/example1.vb#8)]  
  
 <span data-ttu-id="fba6b-156">Belirtilen satır içi, eksi işareti seçeneklerini olması durumunda (`-`) önce bir seçenek veya seçenek kümesi bu seçenekleri devre dışı bırakır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-156">If options are specified inline, a minus sign (`-`) before an option or set of options turns off those options.</span></span> <span data-ttu-id="fba6b-157">Örneğin, satır içi oluşturmak `(?ix-ms)` açar <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> ve <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> seçenekleri ve kapanmadan <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> ve <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> seçenekleri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-157">For example, the inline construct `(?ix-ms)` turns on the <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> and <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> options and turns off the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> and <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> options.</span></span> <span data-ttu-id="fba6b-158">Tüm normal ifade seçenekleri varsayılan olarak kapalıdır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-158">All regular expression options are turned off by default.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-159">Normal ifade seçenekleri belirtilmişse `options` seçeneklerle Oluşturucusu veya yöntem çağrısı çakışma parametresi belirtilen satır içi bir normal ifade deseni, satır içi seçenekleri kullanılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-159">If the regular expression options specified in the `options` parameter of a constructor or method call conflict with the options specified inline in a regular expression pattern, the inline options are used.</span></span>  
  
 <span data-ttu-id="fba6b-160">Aşağıdaki beş normal ifade seçenekleri hem seçenekleri parametre ve satır içi ayarlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="fba6b-160">The following five regular expression options can be set both with the options parameter and inline:</span></span>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType>  
  
 <span data-ttu-id="fba6b-161">Aşağıdaki beş normal ifade seçenekleri kullanılarak ayarlanabilir `options` parametresi satır içi ayarlanamaz, ancak:</span><span class="sxs-lookup"><span data-stu-id="fba6b-161">The following five regular expression options can be set using the `options` parameter but cannot be set inline:</span></span>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType>  
  
-   <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType>  
  
## <a name="determining-the-options"></a><span data-ttu-id="fba6b-162">Seçeneklerini belirleme</span><span class="sxs-lookup"><span data-stu-id="fba6b-162">Determining the Options</span></span>  
 <span data-ttu-id="fba6b-163">Hangi seçeneklerin için sağlanan belirleyebilirsiniz bir <xref:System.Text.RegularExpressions.Regex> nesne salt okunur değeri alarak örneğinin başlatılmasından zaman <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> özelliği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-163">You can determine which options were provided to a <xref:System.Text.RegularExpressions.Regex> object when it was instantiated by retrieving the value of the read-only <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="fba6b-164">Bu özellik tarafından oluşturulan derlenmiş bir normal ifade için tanımlanan seçeneklerini belirlemek için özellikle yararlıdır <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-164">This property is particularly useful for determining the options that are defined for a compiled regular expression created by the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> method.</span></span>  
  
 <span data-ttu-id="fba6b-165">Dışında herhangi bir seçenek varlığını sınamak için <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>, değeriyle ve işlemi <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> özelliği ve <xref:System.Text.RegularExpressions.RegexOptions> içinde ilgi değeri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-165">To test for the presence of any option except <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>, perform an AND operation with the value of the <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> property and the <xref:System.Text.RegularExpressions.RegexOptions> value in which you are interested.</span></span> <span data-ttu-id="fba6b-166">Sonuç eşittir kapanmadığını <xref:System.Text.RegularExpressions.RegexOptions> değeri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-166">Then test whether the result equals that <xref:System.Text.RegularExpressions.RegexOptions> value.</span></span> <span data-ttu-id="fba6b-167">Aşağıdaki örnek testleri olup olmadığını <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> seçeneği ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-167">The following example tests whether the <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> option has been set.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#19](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/determine1.cs#19)]
 [!code-vb[Conceptual.Regex.Language.Options#19](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/determine1.vb#19)]  
  
 <span data-ttu-id="fba6b-168">Sınamak için <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>, belirlemek olup olmadığını değerini <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> özelliği eşittir <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>aşağıdaki örnekte gösterildiği gibi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-168">To test for <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>, determine whether the value of the <xref:System.Text.RegularExpressions.Regex.Options%2A?displayProperty=nameWithType> property is equal to <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType>, as the following example illustrates.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#20](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/determine1.cs#20)]
 [!code-vb[Conceptual.Regex.Language.Options#20](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/determine1.vb#20)]  
  
 <span data-ttu-id="fba6b-169">Aşağıdaki bölümlerde .NET normal ifadede tarafından desteklenen seçenekleri listelenmiştir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-169">The following sections list the options supported by regular expression in .NET.</span></span>  
  
<a name="Default"></a>   
## <a name="default-options"></a><span data-ttu-id="fba6b-170">Varsayılan seçenekleri</span><span class="sxs-lookup"><span data-stu-id="fba6b-170">Default Options</span></span>  
 <span data-ttu-id="fba6b-171"><xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> Seçeneği hiçbir seçenek belirtilen ve normal ifade altyapısı kendi varsayılan davranışı kullanacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-171">The <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> option indicates that no options have been specified, and the regular expression engine uses its default behavior.</span></span> <span data-ttu-id="fba6b-172">Buna aşağıdakiler dahildir:</span><span class="sxs-lookup"><span data-stu-id="fba6b-172">This includes the following:</span></span>  
  
-   <span data-ttu-id="fba6b-173">Bir kurallı yerine olarak ECMAScript normal ifade deseni yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-173">The pattern is interpreted as a canonical rather than an ECMAScript regular expression.</span></span>  
  
-   <span data-ttu-id="fba6b-174">Giriş dizisinde soldan sağa normal ifade deseni eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-174">The regular expression pattern is matched in the input string from left to right.</span></span>  
  
-   <span data-ttu-id="fba6b-175">Karşılaştırma büyük/küçük harfe duyarlıdır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-175">Comparisons are case-sensitive.</span></span>  
  
-   <span data-ttu-id="fba6b-176">`^` Ve `$` dil öğeleri eşleşen başlangıcını ve bitişini giriş dizesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-176">The `^` and `$` language elements match the beginning and end of the input string.</span></span>  
  
-   <span data-ttu-id="fba6b-177">`.` Language öğesi eşleşen her karakteri dışındaki `\n`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-177">The `.` language element matches every character except `\n`.</span></span>  
  
-   <span data-ttu-id="fba6b-178">Normal ifade deseni herhangi bir beyaz alanı bir değişmez değer boşluk karakteri yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-178">Any white space in a regular expression pattern is interpreted as a literal space character.</span></span>  
  
-   <span data-ttu-id="fba6b-179">Geçerli kültür kurallarına giriş dizesi desen karşılaştırma olduğunda kullanılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-179">The conventions of the current culture are used when comparing the pattern to the input string.</span></span>  
  
-   <span data-ttu-id="fba6b-180">Normal ifade deseni yakalama gruplarında örtük olarak açık.</span><span class="sxs-lookup"><span data-stu-id="fba6b-180">Capturing groups in the regular expression pattern are implicit as well as explicit.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-181"><xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> Seçeneği eşdeğer bir satır vardır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-181">The <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> option has no inline equivalent.</span></span> <span data-ttu-id="fba6b-182">Normal ifade seçenekleri uygulanan satır içi olduğunda, varsayılan davranışı belirli bir seçenek kapatma tarafından bir seçenek olarak seçeneği temelinde geri yüklendi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-182">When regular expression options are applied inline, the default behavior is restored on an option-by-option basis, by turning a particular option off.</span></span> <span data-ttu-id="fba6b-183">Örneğin, `(?i)` büyük küçük harf duyarsız karşılaştırma üzerine kapatır ve `(?-i)` varsayılan büyük küçük harfe duyarlı karşılaştırma geri yükler.</span><span class="sxs-lookup"><span data-stu-id="fba6b-183">For example, `(?i)` turns on case-insensitive comparison, and `(?-i)` restores the default case-sensitive comparison.</span></span>  
  
 <span data-ttu-id="fba6b-184">Çünkü <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> seçeneği normal ifade Altyapısı'nın varsayılan davranışını temsil eder, bu nadiren açıkça bir yöntem çağrısı belirtilir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-184">Because the <xref:System.Text.RegularExpressions.RegexOptions.None?displayProperty=nameWithType> option represents the default behavior of the regular expression engine, it is rarely explicitly specified in a method call.</span></span> <span data-ttu-id="fba6b-185">Oluşturucunun ya da statik desen eşleştirme yöntemi olmadan bir `options` parametresi yerine çağrılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-185">A constructor or static pattern-matching method without an `options` parameter is called instead.</span></span>  
  
 [<span data-ttu-id="fba6b-186">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-186">Back to Top</span></span>](#Top)  
  
<a name="Case"></a>   
## <a name="case-insensitive-matching"></a><span data-ttu-id="fba6b-187">Büyük küçük harf duyarsız eşleştirme</span><span class="sxs-lookup"><span data-stu-id="fba6b-187">Case-Insensitive Matching</span></span>  
 <span data-ttu-id="fba6b-188"><xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase> Seçeneğini veya `i` satır içi seçenek sağlar eşleştirme büyük küçük harfe duyarsızdır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-188">The <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase> option, or the `i` inline option, provides case-insensitive matching.</span></span> <span data-ttu-id="fba6b-189">Varsayılan olarak, geçerli kültürü büyük/küçük harf kuralları kullanılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-189">By default, the casing conventions of the current culture are used.</span></span>  
  
 <span data-ttu-id="fba6b-190">Aşağıdaki örnekte bir normal ifade deseni tanımlayan `\bthe\w*\b`, "" ile başlayan tüm sözcükleri eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-190">The following example defines a regular expression pattern, `\bthe\w*\b`, that matches all words starting with "the".</span></span> <span data-ttu-id="fba6b-191">İlk çağrı çünkü <xref:System.Text.RegularExpressions.Regex.Match%2A> yöntemi kullanan varsayılan büyük küçük harfe duyarlı karşılaştırma, "" cümlesi başlar dizeyi eşlenemiyor çıkış gösterir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-191">Because the first call to the <xref:System.Text.RegularExpressions.Regex.Match%2A> method uses the default case-sensitive comparison, the output indicates that the string "The" that begins the sentence is not matched.</span></span> <span data-ttu-id="fba6b-192">Eşleşen zaman <xref:System.Text.RegularExpressions.Regex.Match%2A> kümesine seçeneklerle yöntemi çağrıldığında <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-192">It is matched when the <xref:System.Text.RegularExpressions.Regex.Match%2A> method is called with options set to <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase>.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/case1.cs#1)]
 [!code-vb[Conceptual.Regex.Language.Options#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/case1.vb#1)]  
  
 <span data-ttu-id="fba6b-193">Aşağıdaki örnek yerine satır içi seçeneklerini kullanmayı önceki örnekten normal ifade deseni değiştirir `options` parametresi büyük küçük harf duyarsız karşılaştırma sağlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-193">The following example modifies the regular expression pattern from the previous example to use inline options instead of the `options` parameter to provide case-insensitive comparison.</span></span> <span data-ttu-id="fba6b-194">İlk desen dizesindeki "t" harf yalnızca uygulandığı bir gruplandırma yapısında büyük küçük harf duyarsız seçeneği tanımlar. "".</span><span class="sxs-lookup"><span data-stu-id="fba6b-194">The first pattern defines the case-insensitive option in a grouping construct that applies only to the letter "t" in the string "the".</span></span> <span data-ttu-id="fba6b-195">Seçenek yapı düzeni başında oluştuğundan, ikinci desen büyük küçük harf duyarsız seçeneği tüm normal ifadeye uygular.</span><span class="sxs-lookup"><span data-stu-id="fba6b-195">Because the option construct occurs at the beginning of the pattern, the second pattern applies the case-insensitive option to the entire regular expression.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#2](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/case2.cs#2)]
 [!code-vb[Conceptual.Regex.Language.Options#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/case2.vb#2)]  
  
 [<span data-ttu-id="fba6b-196">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-196">Back to Top</span></span>](#Top)  
  
<a name="Multiline"></a>   
## <a name="multiline-mode"></a><span data-ttu-id="fba6b-197">Çok satırlı moda</span><span class="sxs-lookup"><span data-stu-id="fba6b-197">Multiline Mode</span></span>  
 <span data-ttu-id="fba6b-198"><xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> Seçeneğini veya `m` satır içi seçenek, çok satırlı oluşan giriş dizesi işlemek normal ifade altyapısı sağlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-198">The <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option, or the `m` inline option, enables the regular expression engine to handle an input string that consists of multiple lines.</span></span> <span data-ttu-id="fba6b-199">Yorumu değiştirir `^` ve `$` dil öğeleri başlangıcını ve bitişini başlangıcını ve bitişini Giriş dizesinin yerine bir çizginin eşleşmesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-199">It changes the interpretation of the `^` and `$` language elements so that they match the beginning and end of a line, instead of the beginning and end of the input string.</span></span>  
  
 <span data-ttu-id="fba6b-200">Varsayılan olarak, `$` yalnızca giriş dizesi sonu ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-200">By default, `$` matches only the end of the input string.</span></span> <span data-ttu-id="fba6b-201">Belirtirseniz <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> seçeneği, yeni satır karakteri eşleşen (`\n`) veya giriş dize sonu.</span><span class="sxs-lookup"><span data-stu-id="fba6b-201">If you specify the <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> option, it matches either the newline character (`\n`) or the end of the input string.</span></span> <span data-ttu-id="fba6b-202">Bu ancak, satır başı/karakter bileşimi satır besleme eşleşmiyor.</span><span class="sxs-lookup"><span data-stu-id="fba6b-202">It does not, however, match the carriage return/line feed character combination.</span></span> <span data-ttu-id="fba6b-203">Başarıyla ayarlarla eşleşecek şekilde alt kullanmak `\r?$` yerine yalnızca `$`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-203">To successfully match them, use the subexpression `\r?$` instead of just `$`.</span></span>  
  
 <span data-ttu-id="fba6b-204">Aşağıdaki örnek bowlers adları ve puanlarını ayıklar ve eklenmektedir bir <xref:System.Collections.Generic.SortedList%602> azalan düzende sıralar koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="fba6b-204">The following example extracts bowlers' names and scores and adds them to a <xref:System.Collections.Generic.SortedList%602> collection that sorts them in descending order.</span></span> <span data-ttu-id="fba6b-205"><xref:System.Text.RegularExpressions.Regex.Matches%2A> Yöntemi iki kez çağrılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-205">The <xref:System.Text.RegularExpressions.Regex.Matches%2A> method is called twice.</span></span> <span data-ttu-id="fba6b-206">İlk yöntem çağrısında normal ifade olan `^(\w+)\s(\d+)$` ve hiçbir seçeneklerini ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-206">In the first method call, the regular expression is `^(\w+)\s(\d+)$` and no options are set.</span></span> <span data-ttu-id="fba6b-207">Normal ifade altyapısı başlangıcını ve bitişini Giriş dizesinin birlikte giriş düzeni eşleştiğinden çıkış gösterildiği gibi herhangi bir eşleşme bulunamadı.</span><span class="sxs-lookup"><span data-stu-id="fba6b-207">As the output shows, because the regular expression engine cannot match the input pattern along with the beginning and end of the input string, no matches are found.</span></span> <span data-ttu-id="fba6b-208">Normal ifade değiştirilir ikinci yöntem çağrısında `^(\w+)\s(\d+)\r?$` ve seçeneklerini ayarlamak <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-208">In the second method call, the regular expression is changed to `^(\w+)\s(\d+)\r?$` and the options are set to <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType>.</span></span> <span data-ttu-id="fba6b-209">Çıktı gösterildiği gibi ad ve puanlarını başarıyla eşleştirilir ve notların azalan sırada görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-209">As the output shows, the names and scores are successfully matched, and the scores are displayed in descending order.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#3](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/multiline1.cs#3)]
 [!code-vb[Conceptual.Regex.Language.Options#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/multiline1.vb#3)]  
  
 <span data-ttu-id="fba6b-210">Normal ifade deseni `^(\w+)\s(\d+)\r*$` aşağıdaki tabloda gösterildiği gibi tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-210">The regular expression pattern `^(\w+)\s(\d+)\r*$` is defined as shown in the following table.</span></span>  
  
|<span data-ttu-id="fba6b-211">Desen</span><span class="sxs-lookup"><span data-stu-id="fba6b-211">Pattern</span></span>|<span data-ttu-id="fba6b-212">Açıklama</span><span class="sxs-lookup"><span data-stu-id="fba6b-212">Description</span></span>|  
|-------------|-----------------|  
|`^`|<span data-ttu-id="fba6b-213">Satırın başlangıcına başlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-213">Begin at the start of the line.</span></span>|  
|`(\w+)`|<span data-ttu-id="fba6b-214">Bir veya daha fazla sözcük karakteri eşleştir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-214">Match one or more word characters.</span></span> <span data-ttu-id="fba6b-215">Bu ilk yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-215">This is the first capturing group.</span></span>|  
|`\s`|<span data-ttu-id="fba6b-216">Bir boşluk karakteri ile eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-216">Match a white-space character.</span></span>|  
|`(\d+)`|<span data-ttu-id="fba6b-217">Bir veya daha fazla ondalık basamağı eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-217">Match one or more decimal digits.</span></span> <span data-ttu-id="fba6b-218">Bu ikinci yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-218">This is the second capturing group.</span></span>|  
|`\r?`|<span data-ttu-id="fba6b-219">Sıfır veya bir satır başı karakteri eşleşmesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-219">Match zero or one carriage return character.</span></span>|  
|`$`|<span data-ttu-id="fba6b-220">Satırın sonuna sonlandırın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-220">End at the end of the line.</span></span>|  
  
 <span data-ttu-id="fba6b-221">Satır içi seçeneğini kullanan dışında aşağıdaki örnekte önceki birine eşdeğerdir `(?m)` çok satırlı seçeneğini ayarlamak için.</span><span class="sxs-lookup"><span data-stu-id="fba6b-221">The following example is equivalent to the previous one, except that it uses the inline option `(?m)` to set the multiline option.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#4](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/multiline2.cs#4)]
 [!code-vb[Conceptual.Regex.Language.Options#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/multiline2.vb#4)]  
  
 [<span data-ttu-id="fba6b-222">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-222">Back to Top</span></span>](#Top)  
  
<a name="Singleline"></a>   
## <a name="single-line-mode"></a><span data-ttu-id="fba6b-223">Tek satırlı modu</span><span class="sxs-lookup"><span data-stu-id="fba6b-223">Single-line Mode</span></span>  
 <span data-ttu-id="fba6b-224"><xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> Seçeneğini veya `s` satır içi seçenek, normal ifade altyapısı tarafından giriş dizesini tek bir satırı oluşuyorsa gibi davran neden olur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-224">The <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> option, or the `s` inline option, causes the regular expression engine to treat the input string as if it consists of a single line.</span></span> <span data-ttu-id="fba6b-225">Bunu süresi davranışını değiştirerek yapar (`.`) yerine yeni satır karakteri dışındaki her karakter eşleştirme her karakter, BT'nin eşleşecek şekilde language öğesi `\n` veya \u000A.</span><span class="sxs-lookup"><span data-stu-id="fba6b-225">It does this by changing the behavior of the period (`.`) language element so that it matches every character, instead of matching every character except for the newline character `\n` or \u000A.</span></span>  
  
 <span data-ttu-id="fba6b-226">Aşağıdaki örnek gösterilmektedir nasıl davranışını `.` language öğesi değişiklikleri kullandığınızda <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-226">The following example illustrates how the behavior of the `.` language element changes when you use the <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="fba6b-227">Normal ifade `^.+` dizenin başında başlar ve her bir karakterle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-227">The regular expression `^.+` starts at the beginning of the string and matches every character.</span></span> <span data-ttu-id="fba6b-228">Varsayılan olarak, ilk satırın sonuna eşleşme sonlandırır; Normal ifade deseni ile eşleşen satır başı karakteri `\r` veya \u000D, ancak eşleşmiyor `\n`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-228">By default, the match ends at the end of the first line; the regular expression pattern matches the carriage return character, `\r` or \u000D, but it does not match `\n`.</span></span> <span data-ttu-id="fba6b-229">Çünkü <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> seçeneği yorumlar tek bir satır olarak tüm giriş dizesi, giriş dizesi içinde her karakterle eşleşir dahil olmak üzere `\n`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-229">Because the <xref:System.Text.RegularExpressions.RegexOptions.Singleline?displayProperty=nameWithType> option interprets the entire input string as a single line, it matches every character in the input string, including `\n`.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.CharacterClasses#5](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.characterclasses/cs/any2.cs#5)]
 [!code-vb[Conceptual.Regex.Language.CharacterClasses#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.characterclasses/vb/any2.vb#5)]  
  
 <span data-ttu-id="fba6b-230">Satır içi seçeneğini kullanan dışında aşağıdaki örnekte önceki birine eşdeğerdir `(?s)` tek satırlı modunu etkinleştirmek için.</span><span class="sxs-lookup"><span data-stu-id="fba6b-230">The following example is equivalent to the previous one, except that it uses the inline option `(?s)` to enable single-line mode.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#5](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/singleline1.cs#5)]
 [!code-vb[Conceptual.Regex.Language.Options#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/singleline1.vb#5)]  
  
 [<span data-ttu-id="fba6b-231">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-231">Back to Top</span></span>](#Top)  
  
<a name="Explicit"></a>   
## <a name="explicit-captures-only"></a><span data-ttu-id="fba6b-232">Yalnızca açık yakalar</span><span class="sxs-lookup"><span data-stu-id="fba6b-232">Explicit Captures Only</span></span>  
 <span data-ttu-id="fba6b-233">Varsayılan olarak, yakalama gruplarını normal ifade deseni parantezlerde kullanımını tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-233">By default, capturing groups are defined by the use of parentheses in the regular expression pattern.</span></span> <span data-ttu-id="fba6b-234">Adlandırılmış gruplar tarafından sayı veya bir ad atanmış `(?<` *adı*`>`*alt* `)` dil seçeneği ise adlandırılmamış grupları dizini tarafından erişilebilir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-234">Named groups are assigned a name or number by the `(?<`*name*`>`*subexpression*`)` language option, whereas unnamed groups are accessible by index.</span></span> <span data-ttu-id="fba6b-235">İçinde <xref:System.Text.RegularExpressions.GroupCollection> nesnesi, adlandırılmamış grupları adlandırılmış gruplar koyun.</span><span class="sxs-lookup"><span data-stu-id="fba6b-235">In the <xref:System.Text.RegularExpressions.GroupCollection> object, unnamed groups precede named groups.</span></span>  
  
 <span data-ttu-id="fba6b-236">Gruplandırma yapıları genellikle yalnızca nicelik birden çok dil öğelerine uygulamak için kullanılır ve hiçbir ilgilendiğiniz yakalanan alt dizeler.</span><span class="sxs-lookup"><span data-stu-id="fba6b-236">Grouping constructs are often used only to apply quantifiers to multiple language elements, and the captured substrings are of no interest.</span></span> <span data-ttu-id="fba6b-237">Örneğin, aşağıdaki normal ifade:</span><span class="sxs-lookup"><span data-stu-id="fba6b-237">For example, if the following regular expression:</span></span>  
  
```  
\b\(?((\w+),?\s?)+[\.!?]\)?  
```  
  
 <span data-ttu-id="fba6b-238">yalnızca süresi, ünlem veya soru işareti bir belgeden yalnızca elde edilen cümlesi ile biter cümleleri ayıklamak için tasarlanmıştır (tarafından gösterilen <xref:System.Text.RegularExpressions.Match> nesnesi) ilginizi çekecektir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-238">is intended only to extract sentences that end with a period, exclamation point, or question mark from a document, only the resulting sentence (which is represented by the <xref:System.Text.RegularExpressions.Match> object) is of interest.</span></span> <span data-ttu-id="fba6b-239">Koleksiyon içindeki ayrı sözcükleri değildir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-239">The individual words in the collection are not.</span></span>  
  
 <span data-ttu-id="fba6b-240">Daha sonra kullanılmayan grupları yakalama olabilir pahalı, normal ifade altyapısı hem de doldurmanız gerekir çünkü <xref:System.Text.RegularExpressions.GroupCollection> ve <xref:System.Text.RegularExpressions.CaptureCollection> koleksiyon nesnesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-240">Capturing groups that are not subsequently used can be expensive, because the regular expression engine must populate both the <xref:System.Text.RegularExpressions.GroupCollection> and <xref:System.Text.RegularExpressions.CaptureCollection> collection objects.</span></span> <span data-ttu-id="fba6b-241">Alternatif olarak, ya da kullanabilirsiniz <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> seçeneği veya `n` yalnızca geçerli yakalamaları açıkça adlı veya numaralı tarafından atanan grupları olduğunu belirtmek için satır içi seçeneği `(?<` *adı* `>` *alt* `)` oluşturun.</span><span class="sxs-lookup"><span data-stu-id="fba6b-241">As an alternative, you can use either the <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> option or the `n` inline option to specify that the only valid captures are explicitly named or numbered groups that are designated by the `(?<`*name*`>` *subexpression*`)` construct.</span></span>  
  
 <span data-ttu-id="fba6b-242">Aşağıdaki örnek tarafından döndürülen eşleşme hakkında bilgi görüntüler `\b\(?((\w+),?\s?)+[\.!?]\)?` normal ifade deseni <xref:System.Text.RegularExpressions.Regex.Match%2A> olan ve olmayan yöntemi çağrıldığında <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-242">The following example displays information about the matches returned by the `\b\(?((\w+),?\s?)+[\.!?]\)?` regular expression pattern when the <xref:System.Text.RegularExpressions.Regex.Match%2A> method is called with and without the <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="fba6b-243">İlk yöntem çıktısı olarak gösterir çağrısı, normal ifade altyapısı tam doldurur <xref:System.Text.RegularExpressions.GroupCollection> ve <xref:System.Text.RegularExpressions.CaptureCollection> yakalanan alt dizeler hakkında bilgi içeren koleksiyon nesnesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-243">As the output from the first method call shows, the regular expression engine fully populates the <xref:System.Text.RegularExpressions.GroupCollection> and <xref:System.Text.RegularExpressions.CaptureCollection> collection objects with information about captured substrings.</span></span> <span data-ttu-id="fba6b-244">İkinci yöntem ile çağrıldığı için `options` kümesine <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType>, grupları bilgi yakalamaz.</span><span class="sxs-lookup"><span data-stu-id="fba6b-244">Because the second method is called with `options` set to <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType>, it does not capture information on groups.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#9](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/explicit1.cs#9)]
 [!code-vb[Conceptual.Regex.Language.Options#9](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/explicit1.vb#9)]  
  
 <span data-ttu-id="fba6b-245">Normal ifade deseni`\b\(?((?>\w+),?\s?)+[\.!?]\)?` aşağıdaki tabloda gösterildiği gibi tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-245">The regular expression pattern`\b\(?((?>\w+),?\s?)+[\.!?]\)?` is defined as shown in the following table.</span></span>  
  
|<span data-ttu-id="fba6b-246">Desen</span><span class="sxs-lookup"><span data-stu-id="fba6b-246">Pattern</span></span>|<span data-ttu-id="fba6b-247">Açıklama</span><span class="sxs-lookup"><span data-stu-id="fba6b-247">Description</span></span>|  
|-------------|-----------------|  
|`\b`|<span data-ttu-id="fba6b-248">Word sınırında başlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-248">Begin at a word boundary.</span></span>|  
|`\(?`|<span data-ttu-id="fba6b-249">Eşleşen sıfır veya bir oluşumu açma ayracı ("(").</span><span class="sxs-lookup"><span data-stu-id="fba6b-249">Match zero or one occurrences of the opening parenthesis ("(").</span></span>|  
|`(?>\w+),?`|<span data-ttu-id="fba6b-250">Sıfır veya bir virgül koyarak ve ardından bir veya daha fazla word karakterlerle eşleşen.</span><span class="sxs-lookup"><span data-stu-id="fba6b-250">Match one or more word characters, followed by zero or one commas.</span></span> <span data-ttu-id="fba6b-251">Sözcük karakterlerini eşleştirirken geri izlemeyi değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-251">Do not backtrack when matching word characters.</span></span>|  
|`\s?`|<span data-ttu-id="fba6b-252">Sıfır veya bir beyaz boşluk karakterini eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-252">Match zero or one white-space characters.</span></span>|  
|`((\w+),?\s?)+`|<span data-ttu-id="fba6b-253">Bir veya daha fazla word karakter, sıfır veya bir virgül ve sıfır birleşimi eşleşmiyor veya bir veya birden çok kez bir boşluk karakterleri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-253">Match the combination of one or more word characters, zero or one commas, and zero or one white-space characters one or more times.</span></span>|  
|`[\.!?]\)?`|<span data-ttu-id="fba6b-254">Sıfır veya bir ayraç tarafından (") ve ardından üç noktalama simge hiçbiriyle").</span><span class="sxs-lookup"><span data-stu-id="fba6b-254">Match any of the three punctuation symbols, followed by zero or one closing parentheses (")").</span></span>|  
  
 <span data-ttu-id="fba6b-255">Aynı zamanda `(?n)` otomatik yakalamaları gizlemek için satır içi öğenin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-255">You can also use the `(?n)` inline element to suppress automatic captures.</span></span> <span data-ttu-id="fba6b-256">Aşağıdaki örnek kullanmak için önceki normal ifade deseni değiştirir `(?n)` satır içi öğenin yerine <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-256">The following example modifies the previous regular expression pattern to use the `(?n)` inline element instead of the <xref:System.Text.RegularExpressions.RegexOptions.ExplicitCapture?displayProperty=nameWithType> option.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#10](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/explicit2.cs#10)]
 [!code-vb[Conceptual.Regex.Language.Options#10](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/explicit2.vb#10)]  
  
 <span data-ttu-id="fba6b-257">Son olarak, satır içi Grup öğesi kullanabilirsiniz `(?n:)` grubu grubu temelinde otomatik yakalamaları gizlemek için.</span><span class="sxs-lookup"><span data-stu-id="fba6b-257">Finally, you can use the inline group element `(?n:)` to suppress automatic captures on a group-by-group basis.</span></span> <span data-ttu-id="fba6b-258">Aşağıdaki örnek dış Grup adlandırılmamış yakalamaları gizlemek için önceki düzeni değiştirdiği `((?>\w+),?\s?)`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-258">The following example modifies the previous pattern to suppress unnamed captures in the outer group, `((?>\w+),?\s?)`.</span></span> <span data-ttu-id="fba6b-259">Bu bastırır adlandırılmamış Not iç grubunda yakalar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-259">Note that this suppresses unnamed captures in the inner group as well.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#11](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/explicit3.cs#11)]
 [!code-vb[Conceptual.Regex.Language.Options#11](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/explicit3.vb#11)]  
  
 [<span data-ttu-id="fba6b-260">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-260">Back to Top</span></span>](#Top)  
  
<a name="Compiled"></a>   
## <a name="compiled-regular-expressions"></a><span data-ttu-id="fba6b-261">Derlenmiş normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="fba6b-261">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="fba6b-262">Varsayılan olarak, .NET normal ifadelerde yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-262">By default, regular expressions in .NET are interpreted.</span></span> <span data-ttu-id="fba6b-263">Zaman bir <xref:System.Text.RegularExpressions.Regex> nesnesidir oluşturulmuş veya statik <xref:System.Text.RegularExpressions.Regex> yöntemi çağrıldığında, normal ifade deseni özel işlem kodları kümesine ayrıştırılır ve bir yorumlayıcı normal ifade çalıştırmak için bu işlem kodları kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-263">When a <xref:System.Text.RegularExpressions.Regex> object is instantiated or a static <xref:System.Text.RegularExpressions.Regex> method is called, the regular expression pattern is parsed into a set of custom opcodes, and an interpreter uses these opcodes to run the regular expression.</span></span> <span data-ttu-id="fba6b-264">Bu bir kolaylığını içerir: çalışma zamanı performans ödün verme pahasına normal ifade altyapısı başlatma maliyetini en aza indirilir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-264">This involves a tradeoff: The cost of initializing the regular expression engine is minimized at the expense of run-time performance.</span></span>  
  
 <span data-ttu-id="fba6b-265">Kullanabileceğiniz yerine yorumlanan normal ifadeler kullanarak derlenmiş <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-265">You can use compiled instead of interpreted regular expressions by using the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="fba6b-266">Bu durumda, bir desen normal ifade altyapısı geçirildiğinde, bu işlem kodları kümesine ayrıştırılır ve doğrudan ortak dil çalışma zamanı için geçirilebilen Microsoft Ara dile (MSIL) dönüştürülür.</span><span class="sxs-lookup"><span data-stu-id="fba6b-266">In this case, when a pattern is passed to the regular expression engine, it is parsed into a set of opcodes and then converted to Microsoft intermediate language (MSIL), which can be passed directly to the common language runtime.</span></span> <span data-ttu-id="fba6b-267">Derlenmiş normal ifadeler başlatma saati ödün verme pahasına çalışma zamanı performansı en üst düzeye çıkarın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-267">Compiled regular expressions maximize run-time performance at the expense of initialization time.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-268">Normal bir ifade yalnızca sağlayarak derlenebilir <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> değeri `options` parametresinin bir <xref:System.Text.RegularExpressions.Regex> sınıf oluşturucu veya statik bir desen eşleştirme yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-268">A regular expression can be compiled only by supplying the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> value to the `options` parameter of a <xref:System.Text.RegularExpressions.Regex> class constructor or a static pattern-matching method.</span></span> <span data-ttu-id="fba6b-269">Satır içi seçenek olarak kullanılabilir değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-269">It is not available as an inline option.</span></span>  
  
 <span data-ttu-id="fba6b-270">Hem statik çağrılarında derlenmiş normal ifadeler ve örneği normal ifadeler kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="fba6b-270">You can use compiled regular expressions in calls to both static and instance regular expressions.</span></span> <span data-ttu-id="fba6b-271">Statik normal ifadelerde <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> seçeneği geçirilen `options` desen eşleştirme normal ifade yönteminin parametresi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-271">In static regular expressions, the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option is passed to the `options` parameter of the regular expression pattern-matching method.</span></span> <span data-ttu-id="fba6b-272">İçin geçirilen örneği normal ifadelerde `options` parametresinin <xref:System.Text.RegularExpressions.Regex> sınıfı oluşturucusu.</span><span class="sxs-lookup"><span data-stu-id="fba6b-272">In instance regular expressions, it is passed to the `options` parameter of the <xref:System.Text.RegularExpressions.Regex> class constructor.</span></span> <span data-ttu-id="fba6b-273">Her iki durumda da, Gelişmiş performansla sonuçlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-273">In both cases, it results in enhanced performance.</span></span>  
  
 <span data-ttu-id="fba6b-274">Ancak, bu geliştirme performans, yalnızca aşağıdaki koşullarda oluşur:</span><span class="sxs-lookup"><span data-stu-id="fba6b-274">However, this improvement in performance occurs only under the following conditions:</span></span>  
  
-   <span data-ttu-id="fba6b-275">A <xref:System.Text.RegularExpressions.Regex> belirli olağan bir ifadeyi temsil eden nesne birden fazla çağrı normal ifade desen eşleştirme yöntemleri için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-275">A <xref:System.Text.RegularExpressions.Regex> object that represents a particular regular expression is used in multiple calls to regular expression pattern-matching methods.</span></span>  
  
-   <span data-ttu-id="fba6b-276"><xref:System.Text.RegularExpressions.Regex> Nesnesi tekrar kullanılabilmesi için kapsam dışına gitmek için izin yok.</span><span class="sxs-lookup"><span data-stu-id="fba6b-276">The <xref:System.Text.RegularExpressions.Regex> object is not allowed to go out of scope, so it can be reused.</span></span>  
  
-   <span data-ttu-id="fba6b-277">Statik bir normal ifade birden fazla çağrı normal ifade desen eşleştirme yöntemleri için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-277">A static regular expression is used in multiple calls to regular expression pattern-matching methods.</span></span> <span data-ttu-id="fba6b-278">(Performans geliştirmesi statik yöntem çağrılarında kullanılan normal ifadeleri normal ifade altyapısı tarafından önbelleğe alındığı için mümkündür.)</span><span class="sxs-lookup"><span data-stu-id="fba6b-278">(The performance improvement is possible because regular expressions used in static method calls are cached by the regular expression engine.)</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-279"><xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> Seçeneği ilgisi olmayan <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> önceden tanımlanmış derlenmiş normal ifadeler içeren bir özel amaçlı derleme oluşturur yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-279">The <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option is unrelated to the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> method, which creates a special-purpose assembly that contains predefined compiled regular expressions.</span></span>  
  
 [<span data-ttu-id="fba6b-280">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-280">Back to Top</span></span>](#Top)  
  
<a name="Whitespace"></a>   
## <a name="ignore-white-space"></a><span data-ttu-id="fba6b-281">Beyaz alan yoksay</span><span class="sxs-lookup"><span data-stu-id="fba6b-281">Ignore White Space</span></span>  
 <span data-ttu-id="fba6b-282">Varsayılan olarak, bir normal ifade deseni boşluk önemlidir; Giriş dizisinde bir boşluk karakteri eşleştirmek için normal ifade altyapısı zorlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-282">By default, white space in a regular expression pattern is significant; it forces the regular expression engine to match a white-space character in the input string.</span></span> <span data-ttu-id="fba6b-283">Bu nedenle, normal ifade "`\b\w+\s`"ve"`\b\w+` " kabaca eşdeğer normal ifadeler.</span><span class="sxs-lookup"><span data-stu-id="fba6b-283">Because of this, the regular expression "`\b\w+\s`" and "`\b\w+` " are roughly equivalent regular expressions.</span></span> <span data-ttu-id="fba6b-284">Ayrıca, numara işareti (#) bir normal ifade deseni karşılaşıldığında, eşleştirilmesini harf karakter olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-284">In addition, when the number sign (#) is encountered in a regular expression pattern, it is interpreted as a literal character to be matched.</span></span>  
  
 <span data-ttu-id="fba6b-285"><xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> Seçeneğini veya `x` satır içi seçenek, bu varsayılan davranışı aşağıdaki gibi değişir:</span><span class="sxs-lookup"><span data-stu-id="fba6b-285">The <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> option, or the `x` inline option, changes this default behavior as follows:</span></span>  
  
-   <span data-ttu-id="fba6b-286">Atlanmayan boşluk normal ifade deseni göz ardı edilir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-286">Unescaped white space in the regular expression pattern is ignored.</span></span> <span data-ttu-id="fba6b-287">Normal ifade deseni bir parçası olması için boşluk karakterleri kaçış uygulanmalıdır (örneğin, `\s` veya "`\` ").</span><span class="sxs-lookup"><span data-stu-id="fba6b-287">To be part of a regular expression pattern, white-space characters must be escaped (for example, as `\s` or "`\` ").</span></span>  
  
-   <span data-ttu-id="fba6b-288">Sayı işareti (#) açıklama başlangıcını yerine harf karakter olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-288">The number sign (#) is interpreted as the beginning of a comment, rather than as a literal character.</span></span> <span data-ttu-id="fba6b-289">Tüm metin dizesi sonuna # karakter normal ifade deseni olarak bir açıklama olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-289">All text in the regular expression pattern from the # character to the end of the string is interpreted as a comment.</span></span>  
  
 <span data-ttu-id="fba6b-290">Kullansanız bile ancak, aşağıdaki durumlarda boşluk karakterlerini normal bir ifade, göz ardı olmayan <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> seçeneği:</span><span class="sxs-lookup"><span data-stu-id="fba6b-290">However, in the following cases, white space characters in a regular expression aren't ignored, even if you use the <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> option:</span></span>  
  
-   <span data-ttu-id="fba6b-291">Boşluk karakteri sınıfı içinde her zaman tam anlamıyla yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-291">White space within a character class is always interpreted literally.</span></span> <span data-ttu-id="fba6b-292">Örneğin, normal ifade deseni `[ .,;:]` herhangi tek boşluk karakterini, nokta, virgül, noktalı virgül veya iki nokta üst üste ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-292">For example, the regular expression pattern `[ .,;:]` matches any single white-space character, period, comma, semicolon, or colon.</span></span>  
  
-   <span data-ttu-id="fba6b-293">Beyaz alan kullanılamaz köşeli parantez içindeki bir niceleyici içinde gibi `{`  *n*  `}`, `{`  *n*  `,}`ve `{`  *n*  `,` *m*`}`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-293">White space isn't allowed within a bracketed quantifier, such as `{`*n*`}`, `{`*n*`,}`, and `{`*n*`,`*m*`}`.</span></span> <span data-ttu-id="fba6b-294">Örneğin, normal ifade deseni `\d{1. 3}` bir boşluk karakteri içerdiğinden bir ile üç basamak basamaklarının herhangi bir dizisini eşleştirmek başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-294">For example, the regular expression pattern `\d{1. 3}` fails to match any sequences of digits from one to three digits because it contains a white-space character.</span></span>  
  
-   <span data-ttu-id="fba6b-295">Beyaz alan language öğesi tanıtan bir karakter dizisi içinde izin verilmiyor.</span><span class="sxs-lookup"><span data-stu-id="fba6b-295">White space isn't allowed within a character sequence that introduces a language element.</span></span> <span data-ttu-id="fba6b-296">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="fba6b-296">For example:</span></span>  
  
    -   <span data-ttu-id="fba6b-297">Language öğesi `(?:` *alt* `)` Yakalama yapmayan bir grubu temsil eder ve `(?:` öğesi kısmı, alanları katıştırılmış olamaz.</span><span class="sxs-lookup"><span data-stu-id="fba6b-297">The language element `(?:`*subexpression*`)` represents a noncapturing group, and the `(?:` portion of the element can't have embedded spaces.</span></span> <span data-ttu-id="fba6b-298">Desen `(? :` *alt* `)` oluşturur bir <xref:System.ArgumentException> çalışma zamanında normal ifade altyapısı düzeni ve düzeni ayrıştıramadığından `( ?:` *alt*  `)` eşleşecek şekilde başarısız *alt*.</span><span class="sxs-lookup"><span data-stu-id="fba6b-298">The pattern `(? :`*subexpression*`)` throws an <xref:System.ArgumentException> at run time because the regular expression engine can't parse the pattern, and the pattern `( ?:`*subexpression*`)` fails to match *subexpression*.</span></span>  
  
    -   <span data-ttu-id="fba6b-299">Language öğesi `\p{` *adı*`}`, Unicode kategorisini temsil eder veya bloğu adlı içinde katıştırılmış boşluklar içeremez `\p{` öğesi kısmı.</span><span class="sxs-lookup"><span data-stu-id="fba6b-299">The language element `\p{`*name*`}`, which represents a Unicode category or named block, can't include embedded spaces in the `\p{` portion of the element.</span></span> <span data-ttu-id="fba6b-300">Bir boşluk içeriyorsa öğesi oluşturduğunda bir <xref:System.ArgumentException> çalışma zamanında.</span><span class="sxs-lookup"><span data-stu-id="fba6b-300">If you do include a white space, the element throws an <xref:System.ArgumentException> at run time.</span></span>  
  
 <span data-ttu-id="fba6b-301">Bu seçeneğin etkinleştirilmesi, genellikle ayrıştırılamıyor ve anlaşılması zor olan normal ifadeler basitleştirmeye yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-301">Enabling this option helps simplify regular expressions that are often difficult to parse and to understand.</span></span> <span data-ttu-id="fba6b-302">Okunabilirliğini artırır ve normal bir ifade belge mümkün kılar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-302">It improves readability, and makes it possible to document a regular expression.</span></span>  
  
 <span data-ttu-id="fba6b-303">Aşağıdaki örnek, aşağıdaki normal ifade deseni tanımlar:</span><span class="sxs-lookup"><span data-stu-id="fba6b-303">The following example defines the following regular expression pattern:</span></span>  
  
 `\b \(? ( (?>\w+) ,?\s? )+  [\.!?] \)? # Matches an entire sentence.`  
  
 <span data-ttu-id="fba6b-304">Bu desen tanımlanan örnekle benzer [yalnızca açık yakalar](#Explicit) kullanır ancak bu, bölüm <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> düzeni boşluk gözardı etme seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-304">This pattern is similar to the pattern defined in the [Explicit Captures Only](#Explicit) section, except that it uses the <xref:System.Text.RegularExpressions.RegexOptions.IgnorePatternWhitespace?displayProperty=nameWithType> option to ignore pattern white space.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#12](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/whitespace1.cs#12)]
 [!code-vb[Conceptual.Regex.Language.Options#12](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/whitespace1.vb#12)]  
  
 <span data-ttu-id="fba6b-305">Aşağıdaki örnek satır içi seçeneğini kullanır `(?x)` düzeni boşluk yoksaymak için.</span><span class="sxs-lookup"><span data-stu-id="fba6b-305">The following example uses the inline option `(?x)` to ignore pattern white space.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#13](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/whitespace2.cs#13)]
 [!code-vb[Conceptual.Regex.Language.Options#13](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/whitespace2.vb#13)]  
  
 [<span data-ttu-id="fba6b-306">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-306">Back to Top</span></span>](#Top)  
  
<a name="RightToLeft"></a>   
## <a name="right-to-left-mode"></a><span data-ttu-id="fba6b-307">Sağdan sola modu</span><span class="sxs-lookup"><span data-stu-id="fba6b-307">Right-to-Left Mode</span></span>  
 <span data-ttu-id="fba6b-308">Varsayılan olarak, normal ifade altyapısı soldan sağa arar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-308">By default, the regular expression engine searches from left to right.</span></span> <span data-ttu-id="fba6b-309">Kullanarak arama yönünü ters çevirebilirsiniz <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-309">You can reverse the search direction by using the <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> option.</span></span> <span data-ttu-id="fba6b-310">Arama dizesi son karakter konumunda otomatik olarak başlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-310">The search automatically begins at the last character position of the string.</span></span> <span data-ttu-id="fba6b-311">Başlangıç içeren desen eşleştirme yöntemleri parametresi gibi konumlandırmak için <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.Int32%29?displayProperty=nameWithType>, konumdan başlayarak olduğunu arama olduğu başlamak için en sağdaki karakterin dizini.</span><span class="sxs-lookup"><span data-stu-id="fba6b-311">For pattern-matching methods that include a starting position parameter, such as <xref:System.Text.RegularExpressions.Regex.Match%28System.String%2CSystem.Int32%29?displayProperty=nameWithType>, the starting position is the index of the rightmost character position at which the search is to begin.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-312">Sağdan sola desen modu kullanılabilir yalnızca sağlayarak <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> değeri `options` parametresinin bir <xref:System.Text.RegularExpressions.Regex> sınıf oluşturucu veya statik desen eşleştirme yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-312">Right-to-left pattern mode is available only by supplying the <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> value to the `options` parameter of a <xref:System.Text.RegularExpressions.Regex> class constructor or static pattern-matching method.</span></span> <span data-ttu-id="fba6b-313">Satır içi seçenek olarak kullanılabilir değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-313">It is not available as an inline option.</span></span>  
  
 <span data-ttu-id="fba6b-314"><xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> Seçeneği yalnızca arama yönünü değiştirir; sağdan sola normal ifade deseni yorumlama değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-314">The <xref:System.Text.RegularExpressions.RegexOptions.RightToLeft?displayProperty=nameWithType> option changes the search direction only; it does not interpret the regular expression pattern from right to left.</span></span> <span data-ttu-id="fba6b-315">Örneğin, normal ifade `\bb\w+\s` , "b" harfiyle başlayan ve sonrasında bir boşluk karakteri sözcükler eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-315">For example, the regular expression `\bb\w+\s` matches words that begin with the letter "b" and are followed by a white-space character.</span></span> <span data-ttu-id="fba6b-316">Aşağıdaki örnekte, giriş dizesi bir veya daha fazla "b" karakter içeren üç sözcük birleşiminden oluşur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-316">In the following example, the input string consists of three words that include one or more "b" characters.</span></span> <span data-ttu-id="fba6b-317">İlk word "b" ile başlar, "b" ve üçüncü ikinci biter word ortasında iki "b" karakter içerir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-317">The first word begins with "b", the second ends with "b", and the third includes two "b" characters in the middle of the word.</span></span> <span data-ttu-id="fba6b-318">Örneğin çıktısını gösterildiği gibi yalnızca ilk sözcük normal ifade deseni eşleştirir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-318">As the output from the example shows, only the first word matches the regular expression pattern.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#17](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/righttoleft1.cs#17)]
 [!code-vb[Conceptual.Regex.Language.Options#17](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/righttoleft1.vb#17)]  
  
 <span data-ttu-id="fba6b-319">Ayrıca ileri yönlü onaylama ( `(?=` *alt* `)` language öğesi) ve geriye ilerleme onaylama ( `(?<=` *alt* `)`language öğesi) yönü değiştirmeyin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-319">Also note that the lookahead assertion (the `(?=`*subexpression*`)` language element) and the lookbehind assertion (the `(?<=`*subexpression*`)` language element) do not change direction.</span></span> <span data-ttu-id="fba6b-320">İleri yönlü onaylar sağa bakın; geriye ilerleme onaylar sola arayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-320">The lookahead assertions look to the right; the lookbehind assertions look to the left.</span></span> <span data-ttu-id="fba6b-321">Örneğin, normal ifade `(?<=\d{1,2}\s)\w+,?\s\d{4}` geriye ilerleme onaylama işlemi için bir ay tıklayınca bir tarihi test etmek için kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-321">For example, the regular expression `(?<=\d{1,2}\s)\w+,?\s\d{4}` uses the lookbehind assertion to test for a date that precedes a month name.</span></span> <span data-ttu-id="fba6b-322">Ay ve yıl sonra normal ifadeyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-322">The regular expression then matches the month and the year.</span></span> <span data-ttu-id="fba6b-323">İleri yönlü ve geriye ilerleme assertsions hakkında daha fazla bilgi için bkz: [gruplandırma yapıları](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="fba6b-323">For information on lookahead and lookbehind assertsions, see [Grouping Constructs](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#18](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/righttoleft2.cs#18)]
 [!code-vb[Conceptual.Regex.Language.Options#18](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/righttoleft2.vb#18)]  
  
 <span data-ttu-id="fba6b-324">Normal ifade deseni aşağıdaki tabloda gösterildiği gibi tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-324">The regular expression pattern is defined as shown in the following table.</span></span>  
  
|<span data-ttu-id="fba6b-325">Desen</span><span class="sxs-lookup"><span data-stu-id="fba6b-325">Pattern</span></span>|<span data-ttu-id="fba6b-326">Açıklama</span><span class="sxs-lookup"><span data-stu-id="fba6b-326">Description</span></span>|  
|-------------|-----------------|  
|`(?<=\d{1,2}\s)`|<span data-ttu-id="fba6b-327">Eşleşme başına bir boşluk bırakarak bir veya iki ondalık basamak gelmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-327">The beginning of the match must be preceded by one or two decimal digits followed by a space.</span></span>|  
|`\w+`|<span data-ttu-id="fba6b-328">Bir veya daha fazla sözcük karakteri eşleştir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-328">Match one or more word characters.</span></span>|  
|`,?`|<span data-ttu-id="fba6b-329">Sıfır veya bir virgül karakteri eşleştirmek.</span><span class="sxs-lookup"><span data-stu-id="fba6b-329">Match zero or one comma characters.</span></span>|  
|`\s`|<span data-ttu-id="fba6b-330">Bir boşluk karakteri ile eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="fba6b-330">Match a white-space character.</span></span>|  
|`\d{4}`|<span data-ttu-id="fba6b-331">Dört ondalık basamak eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-331">Match four decimal digits.</span></span>|  
  
 [<span data-ttu-id="fba6b-332">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-332">Back to Top</span></span>](#Top)  
  
<a name="ECMAScript"></a>   
## <a name="ecmascript-matching-behavior"></a><span data-ttu-id="fba6b-333">ECMAScript eşleşen davranışı</span><span class="sxs-lookup"><span data-stu-id="fba6b-333">ECMAScript Matching Behavior</span></span>  
 <span data-ttu-id="fba6b-334">Varsayılan olarak, normal ifade altyapısı kurallı davranışı bir normal ifade ile eşleşen metin girişi için kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-334">By default, the regular expression engine uses canonical behavior when matching a regular expression pattern to input text.</span></span> <span data-ttu-id="fba6b-335">Ancak belirterek davranışı eşleşen ECMAScript kullanılacak normal ifade altyapısı söyleyebilirsiniz <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-335">However, you can instruct the regular expression engine to use ECMAScript matching behavior by specifying the <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> option.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-336">ECMAScript uyumlu davranış kullanılabilir yalnızca sağlayarak <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> değeri `options` parametresinin bir <xref:System.Text.RegularExpressions.Regex> sınıf oluşturucu veya statik desen eşleştirme yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-336">ECMAScript-compliant behavior is available only by supplying the <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> value to the `options` parameter of a <xref:System.Text.RegularExpressions.Regex> class constructor or static pattern-matching method.</span></span> <span data-ttu-id="fba6b-337">Satır içi seçenek olarak kullanılabilir değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-337">It is not available as an inline option.</span></span>  
  
 <span data-ttu-id="fba6b-338"><xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> Seçeneği, yalnızca ile birleştirilebilir <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> ve <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> seçenekleri.</span><span class="sxs-lookup"><span data-stu-id="fba6b-338">The <xref:System.Text.RegularExpressions.RegexOptions.ECMAScript?displayProperty=nameWithType> option can be combined only with the <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> and <xref:System.Text.RegularExpressions.RegexOptions.Multiline?displayProperty=nameWithType> options.</span></span> <span data-ttu-id="fba6b-339">Diğer bir seçenek normal ifadede kullanımını sonuçlanıyor bir <xref:System.ArgumentOutOfRangeException>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-339">The use of any other option in a regular expression results in an <xref:System.ArgumentOutOfRangeException>.</span></span>  
  
 <span data-ttu-id="fba6b-340">Üç alana ECMAScript ve kurallı normal ifadeler davranışını farklıdır: karakter sınıfı sözdizimi, kendine başvuran grupları yakalama ve yeniden başvuru yorumlama karşı sekizli.</span><span class="sxs-lookup"><span data-stu-id="fba6b-340">The behavior of ECMAScript and canonical regular expressions differs in three areas: character class syntax, self-referencing capturing groups, and octal versus backreference interpretation.</span></span>  
  
-   <span data-ttu-id="fba6b-341">Karakter sınıfı sözdizimi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-341">Character class syntax.</span></span> <span data-ttu-id="fba6b-342">ECMAScript'in almadığı kurallı normal ifadeler Unicode desteği olduğundan daha kısıtlı bir söz dizimi ECMAScript karakter sınıfları sahip ve bazı karakter sınıfı dil öğeleri farklı bir anlama sahip.</span><span class="sxs-lookup"><span data-stu-id="fba6b-342">Because canonical regular expressions support Unicode whereas ECMAScript does not, character classes in ECMAScript have a more limited syntax, and some character class language elements have a different meaning.</span></span> <span data-ttu-id="fba6b-343">Örneğin, ECMAScript dil öğeleri Unicode kategori veya blok öğeleri gibi desteklemiyor `\p` ve `\P`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-343">For example, ECMAScript does not support language elements such as the Unicode category or block elements `\p` and `\P`.</span></span> <span data-ttu-id="fba6b-344">Benzer şekilde, `\w` adıyla eşleşen bir sözcük karakteriyle öğesi eşdeğerdir `[a-zA-Z_0-9]` ECMAScript kullanırken sınıfı karakter ve `[\p{Ll}\p{Lu}\p{Lt}\p{Lo}\p{Nd}\p{Pc}\p{Lm}]` kurallı davranışı kullanırken.</span><span class="sxs-lookup"><span data-stu-id="fba6b-344">Similarly, the `\w` element, which matches a word character, is equivalent to the `[a-zA-Z_0-9]` character class when using ECMAScript and `[\p{Ll}\p{Lu}\p{Lt}\p{Lo}\p{Nd}\p{Pc}\p{Lm}]` when using canonical behavior.</span></span> <span data-ttu-id="fba6b-345">Daha fazla bilgi için bkz: [karakter sınıfları](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="fba6b-345">For more information, see [Character Classes](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).</span></span>  
  
     <span data-ttu-id="fba6b-346">Aşağıdaki örnek, kurallı arasındaki farkı gösterir ve ECMAScript desen eşleştirme.</span><span class="sxs-lookup"><span data-stu-id="fba6b-346">The following example illustrates the difference between canonical and ECMAScript pattern matching.</span></span> <span data-ttu-id="fba6b-347">Normal bir ifade tanımlar `\b(\w+\s*)+`, boşluk karakterleri tarafından izlenen sözcükler eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-347">It defines a regular expression, `\b(\w+\s*)+`, that matches words followed by white-space characters.</span></span> <span data-ttu-id="fba6b-348">İki dizeyi, Latin karakter kümesi kullanan bir ve Kiril karakter kümesi kullanan diğer giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-348">The input consists of two strings, one that uses the Latin character set and the other that uses the Cyrillic character set.</span></span> <span data-ttu-id="fba6b-349">Çıktı gösterildiği çağrısı <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> ECMAScript eşleşen kullandığı yöntem başarısız Kiril sözcükleri eşleştirmeye kurallı eşleşen kullandığı yöntem çağrısı bu sözcükleri eşleşmiyor ise.</span><span class="sxs-lookup"><span data-stu-id="fba6b-349">As the output shows, the call to the <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method that uses ECMAScript matching fails to match the Cyrillic words, whereas the method call that uses canonical matching does match these words.</span></span>  
  
     [!code-csharp[Conceptual.Regex.Language.Options#16](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/ecmascript1.cs#16)]
     [!code-vb[Conceptual.Regex.Language.Options#16](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/ecmascript1.vb#16)]  
  
-   <span data-ttu-id="fba6b-350">Yakalama kendi kendine başvuran gruplandırır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-350">Self-referencing capturing groups.</span></span> <span data-ttu-id="fba6b-351">Yeniden başvuru kendisine bir normal ifade yakalama sınıfla her bir yakalama yineleme güncelleştirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-351">A regular expression capture class with a backreference to itself must be updated with each capture iteration.</span></span> <span data-ttu-id="fba6b-352">Aşağıdaki örnekte gösterildiği gibi bu özellik, normal ifade sağlar. `((a+)(\1) ?)+` "aa aaaa aaaaaa" giriş dizesi ECMAScript kullanırken, ancak kurallı eşleşen kullanmadığınızda eşleşecek şekilde.</span><span class="sxs-lookup"><span data-stu-id="fba6b-352">As the following example shows, this feature enables the regular expression `((a+)(\1) ?)+` to match the input string " aa aaaa aaaaaa " when using ECMAScript, but not when using canonical matching.</span></span>  
  
     [!code-csharp[Conceptual.Regex.Language.Options#21](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/ecmascript2.cs#21)]
     [!code-vb[Conceptual.Regex.Language.Options#21](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/ecmascript2.vb#21)]  
  
     <span data-ttu-id="fba6b-353">Normal ifade aşağıdaki tabloda gösterildiği gibi tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-353">The regular expression is defined as shown in the following table.</span></span>  
  
    |<span data-ttu-id="fba6b-354">Desen</span><span class="sxs-lookup"><span data-stu-id="fba6b-354">Pattern</span></span>|<span data-ttu-id="fba6b-355">Açıklama</span><span class="sxs-lookup"><span data-stu-id="fba6b-355">Description</span></span>|  
    |-------------|-----------------|  
    |<span data-ttu-id="fba6b-356">(bir +)</span><span class="sxs-lookup"><span data-stu-id="fba6b-356">(a+)</span></span>|<span data-ttu-id="fba6b-357">Harf "a" bir veya birden çok kez eşleşir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-357">Match the letter "a" one or more times.</span></span> <span data-ttu-id="fba6b-358">Bu ikinci yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-358">This is the second capturing group.</span></span>|  
    |<span data-ttu-id="fba6b-359">(\1)</span><span class="sxs-lookup"><span data-stu-id="fba6b-359">(\1)</span></span>|<span data-ttu-id="fba6b-360">İlk yakalama grubu tarafından yakalanan alt dize eşleşiyor.</span><span class="sxs-lookup"><span data-stu-id="fba6b-360">Match the substring captured by the first capturing group.</span></span> <span data-ttu-id="fba6b-361">Bu, üçüncü yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-361">This is the third capturing group.</span></span>|  
    |<span data-ttu-id="fba6b-362">?</span><span class="sxs-lookup"><span data-stu-id="fba6b-362">?</span></span>|<span data-ttu-id="fba6b-363">Sıfır veya bir boşluk karakterleri eşleşmesi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-363">Match zero or one space characters.</span></span>|  
    |<span data-ttu-id="fba6b-364">((a+)(\1)?) +</span><span class="sxs-lookup"><span data-stu-id="fba6b-364">((a+)(\1) ?)+</span></span>|<span data-ttu-id="fba6b-365">Sıfır veya bir boşluk bırakarak ilk yakalama grubuyla eşleşen bir dize tarafından izlenen bir veya daha fazla "a" karakter desen eşleştirme bir veya birden çok kez karakter.</span><span class="sxs-lookup"><span data-stu-id="fba6b-365">Match the pattern of one or more "a" characters followed by a string that matches the first capturing group followed by zero or one space characters one or more times.</span></span> <span data-ttu-id="fba6b-366">Bu ilk yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="fba6b-366">This is the first capturing group.</span></span>|  
  
-   <span data-ttu-id="fba6b-367">Yeniden başvurular sekizli çıkışları arasındaki belirsizlikleri çözünürlüğü.</span><span class="sxs-lookup"><span data-stu-id="fba6b-367">Resolution of ambiguities between octal escapes and backreferences.</span></span> <span data-ttu-id="fba6b-368">Aşağıdaki tabloda farklar özetlenmektedir kurallı tarafından yeniden başvuru yorumunu ve ECMAScript normal ifadeler karşı sekizli.</span><span class="sxs-lookup"><span data-stu-id="fba6b-368">The following table summarizes the differences in octal versus backreference interpretation by canonical and ECMAScript regular expressions.</span></span>  
  
    |<span data-ttu-id="fba6b-369">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="fba6b-369">Regular expression</span></span>|<span data-ttu-id="fba6b-370">Kurallı davranışı</span><span class="sxs-lookup"><span data-stu-id="fba6b-370">Canonical behavior</span></span>|<span data-ttu-id="fba6b-371">ECMAScript davranışı</span><span class="sxs-lookup"><span data-stu-id="fba6b-371">ECMAScript behavior</span></span>|  
    |------------------------|------------------------|-------------------------|  
    |<span data-ttu-id="fba6b-372">`\0`0-2 sekizli basamak ile izlenen</span><span class="sxs-lookup"><span data-stu-id="fba6b-372">`\0` followed by 0 to 2 octal digits</span></span>|<span data-ttu-id="fba6b-373">Bir sekizli yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-373">Interpret as an octal.</span></span> <span data-ttu-id="fba6b-374">Örneğin, `\044` her zaman bir sekizli değeri olarak yorumlanır ve "$" anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-374">For example, `\044` is always interpreted as an octal value and means "$".</span></span>|<span data-ttu-id="fba6b-375">Aynı davranışı.</span><span class="sxs-lookup"><span data-stu-id="fba6b-375">Same behavior.</span></span>|  
    |<span data-ttu-id="fba6b-376">`\`bir sayı 1'den 9, hiçbir ek ondalık basamak ile izlenen ve ardından,</span><span class="sxs-lookup"><span data-stu-id="fba6b-376">`\` followed by a digit from 1 to 9, followed by no additional decimal digits,</span></span>|<span data-ttu-id="fba6b-377">Yeniden başvuru yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-377">Interpret as a backreference.</span></span> <span data-ttu-id="fba6b-378">Örneğin, `\9` Grup yakalama dokuzuncu yok olsa bile her zaman yeniden başvuru 9, anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-378">For example, `\9` always means backreference 9, even if a ninth capturing group does not exist.</span></span> <span data-ttu-id="fba6b-379">Yakalama grubu yok, normal ifade ayrıştırıcısının döndürürse bir <xref:System.ArgumentException>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-379">If the capturing group does not exist, the regular expression parser throws an <xref:System.ArgumentException>.</span></span>|<span data-ttu-id="fba6b-380">Tek bir ondalık basamak yakalama grubu varsa, bu sayı için yeniden başvuru.</span><span class="sxs-lookup"><span data-stu-id="fba6b-380">If a single decimal digit capturing group exists, backreference to that digit.</span></span> <span data-ttu-id="fba6b-381">Aksi halde değer sabit değer olarak algılar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-381">Otherwise, interpret the value as a literal.</span></span>|  
    |<span data-ttu-id="fba6b-382">`\`Ek ondalık basamak ile izlenen 9 ' 1'den bir rakam ile izlenen</span><span class="sxs-lookup"><span data-stu-id="fba6b-382">`\` followed by a digit from 1 to 9, followed by additional decimal digits</span></span>|<span data-ttu-id="fba6b-383">Basamaklı bir ondalık değer olarak algılar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-383">Interpret the digits as a decimal value.</span></span> <span data-ttu-id="fba6b-384">Bu yakalama Grup zaten varsa, ifade bir yeniden başvuru olarak yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-384">If that capturing group exists, interpret the expression as a backreference.</span></span><br /><br /> <span data-ttu-id="fba6b-385">Aksi takdirde, baştaki sekizli basamak sekizli 377 kadar yorumlar; diğer bir deyişle, yalnızca düşük 8 bit değeri göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="fba6b-385">Otherwise, interpret the leading octal digits up to octal 377; that is, consider only the low 8 bits of the value.</span></span> <span data-ttu-id="fba6b-386">Kalan basamaklar değişmez değerler yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-386">Interpret the remaining digits as literals.</span></span> <span data-ttu-id="fba6b-387">Örneğin, ifadede `\3000`, Grup 300 yakalama varsa, 300 yeniden başvuru yorumlanan; Grup 300 yakalama mevcut değilse, 0 ile izlenen sekizli 300 olarak yorumlar.</span><span class="sxs-lookup"><span data-stu-id="fba6b-387">For example, in the expression `\3000`, if capturing group 300 exists, interpret as backreference 300; if capturing group 300 does not exist, interpret as octal 300 followed by 0.</span></span>|<span data-ttu-id="fba6b-388">Yeniden başvuru yakalamaya başvurabilir ondalık bir değer mümkün olduğunca çok basamak dönüştürerek yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-388">Interpret as a backreference by converting as many digits as possible to a decimal value that can refer to a capture.</span></span> <span data-ttu-id="fba6b-389">Sekizlik 377 kadar başında sekizli basamak kullanarak hiç basamak dönüştürülebilir ise bir sekizli yorumlar; kalan basamaklar değişmez değerler yorumlayın.</span><span class="sxs-lookup"><span data-stu-id="fba6b-389">If no digits can be converted, interpret as an octal by using the leading octal digits up to octal 377; interpret the remaining digits as literals.</span></span>|  
  
 [<span data-ttu-id="fba6b-390">Başa dön</span><span class="sxs-lookup"><span data-stu-id="fba6b-390">Back to Top</span></span>](#Top)  
  
<a name="Invariant"></a>   
## <a name="comparison-using-the-invariant-culture"></a><span data-ttu-id="fba6b-391">Sabit kültür kullanarak karşılaştırma</span><span class="sxs-lookup"><span data-stu-id="fba6b-391">Comparison Using the Invariant Culture</span></span>  
 <span data-ttu-id="fba6b-392">Normal ifade altyapısı duyarlı karşılaştırmalar gerçekleştirdiğinde varsayılan olarak, bu geçerli kültürü büyük/küçük harf kuralları eşdeğeri büyük ve küçük harfleri belirlemek için kullanır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-392">By default, when the regular expression engine performs case-insensitive comparisons, it uses the casing conventions of the current culture to determine equivalent uppercase and lowercase characters.</span></span>  
  
 <span data-ttu-id="fba6b-393">Ancak, bu özellikle parolalar, dosyaları veya URL'leri gibi sistem kaynakların adları için kullanıcı girişi karşılaştırılırken karşılaştırmaları, bazı türleri için istenmeyen bir davranıştır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-393">However, this behavior is undesirable for some types of comparisons, particularly when comparing user input to the names of system resources, such as passwords, files, or URLs.</span></span> <span data-ttu-id="fba6b-394">Aşağıdaki örnek gibi senaryo gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-394">The following example illustrates such as scenario.</span></span> <span data-ttu-id="fba6b-395">Kod, URL başında ile herhangi bir kaynağa erişimi engellemek için yöneliktir **FILE://**.</span><span class="sxs-lookup"><span data-stu-id="fba6b-395">The code is intended to block access to any resource whose URL is prefaced with **FILE://**.</span></span> <span data-ttu-id="fba6b-396">Normal ifade kullanarak normal ifade dizesini küçük harf olarak eşleşen çalışır `$FILE://`.</span><span class="sxs-lookup"><span data-stu-id="fba6b-396">The regular expression attempts a case-insensitive match with the string by using the regular expression `$FILE://`.</span></span> <span data-ttu-id="fba6b-397">Geçerli sistem kültürü tr-TR (Türkçe-Türkiye) olduğunda, ancak "Yeni" büyük denk değil "i".</span><span class="sxs-lookup"><span data-stu-id="fba6b-397">However, when the current system culture is tr-TR (Turkish-Turkey), "I" is not the uppercase equivalent of "i".</span></span> <span data-ttu-id="fba6b-398">Sonuç olarak, çağrısı <xref:System.Text.RegularExpressions.Regex.IsMatch%2A?displayProperty=nameWithType> yöntemi döndürür `false`, ve dosyaya erişimi verilir.</span><span class="sxs-lookup"><span data-stu-id="fba6b-398">As a result, the call to the <xref:System.Text.RegularExpressions.Regex.IsMatch%2A?displayProperty=nameWithType> method returns `false`, and access to the file is allowed.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#14](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/culture1.cs#14)]
 [!code-vb[Conceptual.Regex.Language.Options#14](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/culture1.vb#14)]  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-399">Dize karşılaştırmaları büyük küçük harfe duyarlı olmayan ve sabit kültür kullanma hakkında daha fazla bilgi için bkz: [kullanarak dizeleri için en iyi uygulamaları](../../../docs/standard/base-types/best-practices-strings.md).</span><span class="sxs-lookup"><span data-stu-id="fba6b-399">For more information about string comparisons that are case-sensitive and that use the invariant culture, see [Best Practices for Using Strings](../../../docs/standard/base-types/best-practices-strings.md).</span></span>  
  
 <span data-ttu-id="fba6b-400">Geçerli kültür duyarlı karşılaştırmaları kullanmak yerine, belirtebilirsiniz <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType> kültürel dil farklılıkları yoksay ve sabit kültür kurallarını kullanmasını seçeneği.</span><span class="sxs-lookup"><span data-stu-id="fba6b-400">Instead of using the case-insensitive comparisons of the current culture, you can specify the <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType> option to ignore cultural differences in language and to use the conventions of the invariant culture.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="fba6b-401">Sabit kültür kullanarak karşılaştırma kullanılabilir yalnızca sağlayarak <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType> değeri `options` parametresinin bir <xref:System.Text.RegularExpressions.Regex> sınıf oluşturucu veya statik desen eşleştirme yöntemi.</span><span class="sxs-lookup"><span data-stu-id="fba6b-401">Comparison using the invariant culture is available only by supplying the <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType> value to the `options` parameter of a <xref:System.Text.RegularExpressions.Regex> class constructor or static pattern-matching method.</span></span> <span data-ttu-id="fba6b-402">Satır içi seçenek olarak kullanılabilir değil.</span><span class="sxs-lookup"><span data-stu-id="fba6b-402">It is not available as an inline option.</span></span>  
  
 <span data-ttu-id="fba6b-403">Aşağıdaki örnek, önceki örnekte, hariç eşdeğerdir statik <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> dahil seçeneklerle yöntemi çağrıldığında <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="fba6b-403">The following example is identical to the previous example, except that the static <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.RegexOptions%29?displayProperty=nameWithType> method is called with options that include <xref:System.Text.RegularExpressions.RegexOptions.CultureInvariant?displayProperty=nameWithType>.</span></span> <span data-ttu-id="fba6b-404">Geçerli kültür Türkçe (Türkiye) bile ayarlandığında, normal ifade "Dosyası" ve "dosyası" ve blok erişim dosya kaynağı başarıyla eşleşen mümkün altyapısıdır.</span><span class="sxs-lookup"><span data-stu-id="fba6b-404">Even when the current culture is set to Turkish (Turkey), the regular expression engine is able to successfully match "FILE" and "file" and block access to the file resource.</span></span>  
  
 [!code-csharp[Conceptual.Regex.Language.Options#15](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.regex.language.options/cs/culture1.cs#15)]
 [!code-vb[Conceptual.Regex.Language.Options#15](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.regex.language.options/vb/culture1.vb#15)]  
  
## <a name="see-also"></a><span data-ttu-id="fba6b-405">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="fba6b-405">See Also</span></span>  
 [<span data-ttu-id="fba6b-406">Normal ifade dili - hızlı başvuru</span><span class="sxs-lookup"><span data-stu-id="fba6b-406">Regular Expression Language - Quick Reference</span></span>](../../../docs/standard/base-types/regular-expression-language-quick-reference.md)
