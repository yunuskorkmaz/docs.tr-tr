---
title: .NET normal Ifadelerinde karakter kaçışları
description: .NET normal ifadelerinde özel karakterler ve kaçış karakterleri hakkında bilgi edinin.
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- unescaped characters
- replacement patterns
- characters, escapes
- regular expressions, character escapes
- escape characters
- .NET Framework regular expressions, character escapes
- constructs, character escapes
ms.assetid: f49cc9cc-db7d-4058-8b8a-422bc08b29b0
author: rpetrusha
ms.author: ronpet
ms.custom: seodec18
ms.openlocfilehash: 248d434f7aad56d84d952fa27cf49f3d370f4a1c
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69934826"
---
# <a name="character-escapes-in-regular-expressions"></a><span data-ttu-id="654c0-103">Normal İfadelerdeki Karakter Çıkışları</span><span class="sxs-lookup"><span data-stu-id="654c0-103">Character Escapes in Regular Expressions</span></span>
<span data-ttu-id="654c0-104">Normal ifadede ters\\eğik çizgi () aşağıdakilerden birini belirtir:</span><span class="sxs-lookup"><span data-stu-id="654c0-104">The backslash (\\) in a regular expression indicates one of the following:</span></span>  
  
- <span data-ttu-id="654c0-105">Aşağıdaki bölümde gösterildiği gibi, bu karakteri izleyen karakter özel bir karakterdir.</span><span class="sxs-lookup"><span data-stu-id="654c0-105">The character that follows it is a special character, as shown in the table in the following section.</span></span> <span data-ttu-id="654c0-106">Örneğin, `\b` bir normal ifade eşleşmesinin bir sözcük sınırında başlaması gerektiğini belirten bir bağlantıdır, `\t` bir sekmeyi temsil eder ve `\x020` bir alanı temsil eder.</span><span class="sxs-lookup"><span data-stu-id="654c0-106">For example, `\b` is an anchor that indicates that a regular expression match should begin on a word boundary, `\t` represents a tab, and `\x020` represents a space.</span></span>  
  
- <span data-ttu-id="654c0-107">Aksi halde, kaçışsız bir dil yapısı olarak yorumlanabilecek bir karakter, tam olarak yorumlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="654c0-107">A character that otherwise would be interpreted as an unescaped language construct should be interpreted literally.</span></span> <span data-ttu-id="654c0-108">Örneğin, bir küme ayracı (`{`) bir nicelik tanımının tanımını başlatır, ancak bir ters eğik çizgiden sonra bir ayraç (`\{`), normal ifade altyapısının küme ayracı ile eşleşmesi gerektiğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="654c0-108">For example, a brace (`{`) begins the definition of a quantifier, but a backslash followed by a brace (`\{`) indicates that the regular expression engine should match the brace.</span></span> <span data-ttu-id="654c0-109">Benzer şekilde, tek bir ters eğik çizgi, kaçan dil yapısının başlangıcını işaret ediyor, ancak`\\`iki ters eğik çizgi (), normal ifade altyapısının ters eğik çizgiyle eşleşmesi gerektiğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="654c0-109">Similarly, a single backslash marks the beginning of an escaped language construct, but two backslashes (`\\`) indicate that the regular expression engine should match the backslash.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="654c0-110">Karakter kaçışları normal ifade desenlerinde tanınır ancak değiştirme desenlerinde kabul edilmez.</span><span class="sxs-lookup"><span data-stu-id="654c0-110">Character escapes are recognized in regular expression patterns but not in replacement patterns.</span></span>  
  
## <a name="character-escapes-in-net"></a><span data-ttu-id="654c0-111">.NET 'teki karakter kaçışları</span><span class="sxs-lookup"><span data-stu-id="654c0-111">Character Escapes in .NET</span></span>  
 <span data-ttu-id="654c0-112">Aşağıdaki tabloda, .NET 'teki normal ifadeler tarafından desteklenen karakter çıkışları listelenmektedir.</span><span class="sxs-lookup"><span data-stu-id="654c0-112">The following table lists the character escapes supported by regular expressions in .NET.</span></span>  
  
|<span data-ttu-id="654c0-113">Karakter veya sıra</span><span class="sxs-lookup"><span data-stu-id="654c0-113">Character or sequence</span></span>|<span data-ttu-id="654c0-114">Açıklama</span><span class="sxs-lookup"><span data-stu-id="654c0-114">Description</span></span>|  
|---------------------------|-----------------|  
|<span data-ttu-id="654c0-115">Aşağıdakiler hariç tüm karakterler:</span><span class="sxs-lookup"><span data-stu-id="654c0-115">All characters except for the following:</span></span><br /><br /> <span data-ttu-id="654c0-116">biçimindeki telefon numarasıdır.</span><span class="sxs-lookup"><span data-stu-id="654c0-116">.</span></span> <span data-ttu-id="654c0-117">$ ^ { [ ( &#124; ) \* + ?</span><span class="sxs-lookup"><span data-stu-id="654c0-117">$ ^ { [ ( &#124; ) \* + ?</span></span> \ |<span data-ttu-id="654c0-118">**Karakter veya dizi** sütununda listelenenler dışındaki karakterlerin normal ifadelerde özel bir anlamı yoktur; Bunlar kendileriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-118">Characters other than those listed in the **Character or sequence** column have no special meaning in regular expressions; they match themselves.</span></span><br /><br /> <span data-ttu-id="654c0-119">**Karakter veya dizi** sütununa dahil edilen karakterler özel normal ifade dili öğeleridir.</span><span class="sxs-lookup"><span data-stu-id="654c0-119">The characters included in the **Character or sequence** column are special regular expression language elements.</span></span> <span data-ttu-id="654c0-120">Bunları düzenli bir ifadede eşleştirmek için, bu, kaçış veya [pozitif bir karakter grubuna](../../../docs/standard/base-types/character-classes-in-regular-expressions.md)dahil edilmeleri gerekir.</span><span class="sxs-lookup"><span data-stu-id="654c0-120">To match them in a regular expression, they must be escaped or included in a [positive character group](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).</span></span> <span data-ttu-id="654c0-121">Örneğin, normal ifade `\$\d+` veya `[$]\d+` "$1200" ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-121">For example, the regular expression `\$\d+` or `[$]\d+` matches "$1200".</span></span>|  
|`\a`|<span data-ttu-id="654c0-122">Bir Bell (alarm) karakteriyle `\u0007`eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-122">Matches a bell (alarm) character, `\u0007`.</span></span>|  
|`\b`|<span data-ttu-id="654c0-123">Bir `[` *character_group* `\u0008`karakter sınıfında, geri al, ile eşleşir.`]`</span><span class="sxs-lookup"><span data-stu-id="654c0-123">In a `[`*character_group*`]` character class, matches a backspace, `\u0008`.</span></span>  <span data-ttu-id="654c0-124">(Bkz. [karakter sınıfları](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).) Bir karakter sınıfı dışında, `\b` bir sözcük sınırı ile eşleşen bir bağlantıdır.</span><span class="sxs-lookup"><span data-stu-id="654c0-124">(See [Character Classes](../../../docs/standard/base-types/character-classes-in-regular-expressions.md).) Outside a character class, `\b` is an anchor that matches a word boundary.</span></span> <span data-ttu-id="654c0-125">(Bkz [](../../../docs/standard/base-types/anchors-in-regular-expressions.md). Tutturucuların.)</span><span class="sxs-lookup"><span data-stu-id="654c0-125">(See [Anchors](../../../docs/standard/base-types/anchors-in-regular-expressions.md).)</span></span>|  
|`\t`|<span data-ttu-id="654c0-126">Bir sekmeden eşleşir, `\u0009`.</span><span class="sxs-lookup"><span data-stu-id="654c0-126">Matches a tab, `\u0009`.</span></span>|  
|`\r`|<span data-ttu-id="654c0-127">Bir satır dönüşüyle eşleşir `\u000D`,.</span><span class="sxs-lookup"><span data-stu-id="654c0-127">Matches a carriage return, `\u000D`.</span></span> <span data-ttu-id="654c0-128">Yeni satır karakteri ile eşdeğer olmadığına unutmayın. `\n` `\r`</span><span class="sxs-lookup"><span data-stu-id="654c0-128">Note that `\r` is not equivalent to the newline character, `\n`.</span></span>|  
|`\v`|<span data-ttu-id="654c0-129">Dikey bir sekme `\u000B`ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-129">Matches a vertical tab, `\u000B`.</span></span>|  
|`\f`|<span data-ttu-id="654c0-130">Bir form akışı `\u000C`ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-130">Matches a form feed, `\u000C`.</span></span>|  
|`\n`|<span data-ttu-id="654c0-131">Yeni bir satırla eşleşir, `\u000A`.</span><span class="sxs-lookup"><span data-stu-id="654c0-131">Matches a new line, `\u000A`.</span></span>|  
|`\e`|<span data-ttu-id="654c0-132">Bir kaçış ile eşleşir `\u001B`,.</span><span class="sxs-lookup"><span data-stu-id="654c0-132">Matches an escape, `\u001B`.</span></span>|  
|<span data-ttu-id="654c0-133">`\`*nnn*| Bir ASCII karakteriyle eşleşir, burada *nnn* sekizli karakter kodunu temsil eden iki veya üç sayıdan oluşur.</span><span class="sxs-lookup"><span data-stu-id="654c0-133">`\` *nnn*|Matches an ASCII character, where *nnn* consists of two or three digits that represent the octal character code.</span></span> <span data-ttu-id="654c0-134">Örneğin, `\040` bir boşluk karakterini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="654c0-134">For example, `\040` represents a space character.</span></span> <span data-ttu-id="654c0-135">Bu yapı yalnızca bir basamak varsa (örneğin, `\2`) veya bir yakalama grubu sayısına karşılık geliyorsa, bir geri başvuru olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="654c0-135">This construct is interpreted as a backreference if it has only one digit (for example, `\2`) or if it corresponds to the number of a capturing group.</span></span> <span data-ttu-id="654c0-136">(Bkz. [Backreference yapıları](../../../docs/standard/base-types/backreference-constructs-in-regular-expressions.md).)</span><span class="sxs-lookup"><span data-stu-id="654c0-136">(See [Backreference Constructs](../../../docs/standard/base-types/backreference-constructs-in-regular-expressions.md).)</span></span>|  
|<span data-ttu-id="654c0-137">`\x`*nn*</span><span class="sxs-lookup"><span data-stu-id="654c0-137">`\x` *nn*</span></span>|<span data-ttu-id="654c0-138">*Nn* 'in iki basamaklı bir onaltılık karakter kodu olduğu ASCII karakteriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-138">Matches an ASCII character, where *nn* is a two-digit hexadecimal character code.</span></span>|  
|<span data-ttu-id="654c0-139">`\c` *X*</span><span class="sxs-lookup"><span data-stu-id="654c0-139">`\c` *X*</span></span>|<span data-ttu-id="654c0-140">Bir ASCII denetim karakteriyle eşleşir, burada X, denetim karakterinin harfidir.</span><span class="sxs-lookup"><span data-stu-id="654c0-140">Matches an ASCII control character, where X is the letter of the control character.</span></span> <span data-ttu-id="654c0-141">Örneğin, CTRL `\cC` -C ' dir.</span><span class="sxs-lookup"><span data-stu-id="654c0-141">For example, `\cC` is CTRL-C.</span></span>|  
|<span data-ttu-id="654c0-142">`\u`*nnnn*</span><span class="sxs-lookup"><span data-stu-id="654c0-142">`\u` *nnnn*</span></span>|<span data-ttu-id="654c0-143">Değeri *nnnn* onaltılık olan bir UTF-16 kod birimiyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-143">Matches a UTF-16 code unit whose value is *nnnn* hexadecimal.</span></span> <span data-ttu-id="654c0-144">**Not:**  Unicode belirtmek için kullanılan Perl 5 karakter kaçış, .NET tarafından desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="654c0-144">**Note:**  The Perl 5 character escape that is used to specify Unicode is not supported by .NET.</span></span> <span data-ttu-id="654c0-145">Perl 5 karakter `\x{`kaçış biçimi *####* `…}`, *####* burada`…` bir dizi onaltılık basamak olur.</span><span class="sxs-lookup"><span data-stu-id="654c0-145">The Perl 5 character escape has the form `\x{`*####*`…}`, where *####*`…` is a series of hexadecimal digits.</span></span> <span data-ttu-id="654c0-146">Bunun yerine `\u` *nnnn*kullanın.</span><span class="sxs-lookup"><span data-stu-id="654c0-146">Instead, use `\u`*nnnn*.</span></span>|  
|<span data-ttu-id="654c0-147">`\`| Bunun ardından, kaçış karakteri olarak tanınmayan bir karakter gelmesi durumunda, bu karakterle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="654c0-147">`\`|When followed by a character that is not recognized as an escaped character, matches that character.</span></span> <span data-ttu-id="654c0-148">Örneğin, `\*` bir yıldız işareti (\*) ile eşleşir ve ile `\x2A`aynıdır.</span><span class="sxs-lookup"><span data-stu-id="654c0-148">For example, `\*` matches an asterisk (\*) and is the same as `\x2A`.</span></span>|  
  
## <a name="an-example"></a><span data-ttu-id="654c0-149">Bir Örnek</span><span class="sxs-lookup"><span data-stu-id="654c0-149">An Example</span></span>  
 <span data-ttu-id="654c0-150">Aşağıdaki örnek, bir normal ifadede karakter kaçışları kullanımını gösterir.</span><span class="sxs-lookup"><span data-stu-id="654c0-150">The following example illustrates the use of character escapes in a regular expression.</span></span> <span data-ttu-id="654c0-151">Dünyanın en büyük şehirlerinin adlarını ve 2009 içindeki popülasyonlarını içeren bir dizeyi ayrıştırır.</span><span class="sxs-lookup"><span data-stu-id="654c0-151">It parses a string that contains the names of the world's largest cities and their populations in 2009.</span></span> <span data-ttu-id="654c0-152">Her şehir adı, bir sekme (`\t`) veya dikey çubuk (&#124; veya `\u007c`) tarafından popülasyondan ayrılır.</span><span class="sxs-lookup"><span data-stu-id="654c0-152">Each city name is separated from its population by a tab (`\t`) or a vertical bar (&#124; or `\u007c`).</span></span> <span data-ttu-id="654c0-153">Bireysel şehirler ve bunların popülasyonu bir satır başı ve satır besleme ile birbirinden ayrılır.</span><span class="sxs-lookup"><span data-stu-id="654c0-153">Individual cities and their populations are separated from each other by a carriage return and line feed.</span></span>  
  
 [!code-csharp[RegularExpressions.Language.Escapes#1](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.escapes/cs/escape1.cs#1)]
 [!code-vb[RegularExpressions.Language.Escapes#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.escapes/vb/escape1.vb#1)]  
  
 <span data-ttu-id="654c0-154">Normal ifade `\G(.+)[\t|\u007c](.+)\r?\n` aşağıdaki tabloda gösterildiği gibi yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="654c0-154">The regular expression `\G(.+)[\t|\u007c](.+)\r?\n` is interpreted as shown in the following table.</span></span>  
  
|<span data-ttu-id="654c0-155">Desen</span><span class="sxs-lookup"><span data-stu-id="654c0-155">Pattern</span></span>|<span data-ttu-id="654c0-156">Açıklama</span><span class="sxs-lookup"><span data-stu-id="654c0-156">Description</span></span>|  
|-------------|-----------------|  
|`\G`|<span data-ttu-id="654c0-157">En son eşleşmenin sona erdiği eşleşmeyi Başlat.</span><span class="sxs-lookup"><span data-stu-id="654c0-157">Begin the match where the last match ended.</span></span>|  
|`(.+)`|<span data-ttu-id="654c0-158">Karakterleri bir veya daha fazla kez eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="654c0-158">Match any character one or more times.</span></span> <span data-ttu-id="654c0-159">Bu ilk yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="654c0-159">This is the first capturing group.</span></span>|  
|`[\t\u007c]`|<span data-ttu-id="654c0-160">Bir sekmeden (`\t`) veya dikey çubukla (&#124;) eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="654c0-160">Match a tab (`\t`) or a vertical bar (&#124;).</span></span>|  
|`(.+)`|<span data-ttu-id="654c0-161">Karakterleri bir veya daha fazla kez eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="654c0-161">Match any character one or more times.</span></span> <span data-ttu-id="654c0-162">Bu ikinci yakalama grubudur.</span><span class="sxs-lookup"><span data-stu-id="654c0-162">This is the second capturing group.</span></span>|  
|`\r?\n`|<span data-ttu-id="654c0-163">Bir satır başı ve ardından yeni bir satır gelen sıfır veya bir oluşumunu eşleştirin.</span><span class="sxs-lookup"><span data-stu-id="654c0-163">Match zero or one occurrence of a carriage return followed by a new line.</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="654c0-164">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="654c0-164">See also</span></span>

- [<span data-ttu-id="654c0-165">Normal İfade Dili - Hızlı Başvuru</span><span class="sxs-lookup"><span data-stu-id="654c0-165">Regular Expression Language - Quick Reference</span></span>](../../../docs/standard/base-types/regular-expression-language-quick-reference.md)
