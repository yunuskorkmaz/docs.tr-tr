---
title: Normal İfadelerde Derleme ve Yeniden Kullanma
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
ms.openlocfilehash: b89f7f88233ecdab25ba2a74647aafeb4d8b74af
ms.sourcegitcommit: 59e36e65ac81cdd094a5a84617625b2a0ff3506e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/27/2020
ms.locfileid: "80344186"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="8df88-102">Normal İfadelerde Derleme ve Yeniden Kullanma</span><span class="sxs-lookup"><span data-stu-id="8df88-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="8df88-103">Normal ifade altyapısının ifadeleri nasıl derlediğini anlayarak ve normal ifadelerin önbelleğe nasıl kullanıldığını anlayarak normal ifadeleri yaygın olarak kullanan uygulamaların performansını en iyi duruma getirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="8df88-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="8df88-104">Bu konu hem derleme hem de önbelleğe alma konur.</span><span class="sxs-lookup"><span data-stu-id="8df88-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="8df88-105">Derlenmiş Düzenli İfadeler</span><span class="sxs-lookup"><span data-stu-id="8df88-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="8df88-106">Varsayılan olarak, normal ifade altyapısı bir iç yönerge dizisine normal bir ifade derler (bunlar Microsoft ara dilinden veya MSIL'den farklı olan üst düzey kodlardır).</span><span class="sxs-lookup"><span data-stu-id="8df88-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="8df88-107">Motor düzenli bir ifade yürütürken, iç kodları yorumlar.</span><span class="sxs-lookup"><span data-stu-id="8df88-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="8df88-108">Bir <xref:System.Text.RegularExpressions.Regex> nesne <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> seçeneğiyle oluşturulmuşsa, üst düzey normal ifade iç yönergeleri yerine normal ifadeyi açık MSIL koduna derler.</span><span class="sxs-lookup"><span data-stu-id="8df88-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="8df88-109">Bu sağlar. NET'in tam zamanında (JIT) derleyicisi daha yüksek performans için ifadeyi yerel makine koduna dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="8df88-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  <span data-ttu-id="8df88-110">Nesneyi <xref:System.Text.RegularExpressions.Regex> oluşturmanın maliyeti daha yüksek olabilir, ancak onunla eşleşen performans maliyeti çok daha küçük olması muhtemeldir.</span><span class="sxs-lookup"><span data-stu-id="8df88-110">The cost of constructing the <xref:System.Text.RegularExpressions.Regex> object may be higher, but the cost of performing matches with it is likely to be much smaller.</span></span>

 <span data-ttu-id="8df88-111">Bir alternatif önceden derlenmiş düzenli ifadeler kullanmaktır.</span><span class="sxs-lookup"><span data-stu-id="8df88-111">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="8df88-112">Tüm ifadelerinizi <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> yöntemi kullanarak yeniden kullanılabilir bir DLL'ye kopyalayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="8df88-112">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="8df88-113">Bu, derlenmiş düzenli ifadelerin hızından yararlanırken çalışma zamanında derleme gereksinimini önler.</span><span class="sxs-lookup"><span data-stu-id="8df88-113">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="8df88-114">Normal İfadeler Önbelleği</span><span class="sxs-lookup"><span data-stu-id="8df88-114">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="8df88-115">Performansı artırmak için, normal ifade altyapısı derlenmiş düzenli ifadelerin uygulama çapında önbelleğini tutar.</span><span class="sxs-lookup"><span data-stu-id="8df88-115">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="8df88-116">Önbellek, yalnızca statik yöntem çağrılarında kullanılan düzenli ifade desenleri depolar.</span><span class="sxs-lookup"><span data-stu-id="8df88-116">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="8df88-117">(Örnek yöntemlerine verilen normal ifade desenleri önbelleğe alınmaz.) Bu, bir ifadenin her kullanıldığında üst düzey bayt koduna reparse gereksinimini önler.</span><span class="sxs-lookup"><span data-stu-id="8df88-117">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="8df88-118">Önbelleğe alınan en fazla normal ifade sayısı `static` (`Shared` Visual Basic'teki) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> özelliğinin değerine göre belirlenir.</span><span class="sxs-lookup"><span data-stu-id="8df88-118">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="8df88-119">Varsayılan olarak, normal ifade altyapısı 15'e kadar derlenmiş normal ifadeleri önbelleğe alır.</span><span class="sxs-lookup"><span data-stu-id="8df88-119">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="8df88-120">Derlenen normal ifadelerin sayısı önbellek boyutunu aşıyorsa, en son kullanılan normal ifade atılır ve yeni normal ifade önbelleğe çıkar.</span><span class="sxs-lookup"><span data-stu-id="8df88-120">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="8df88-121">Uygulamanız normal ifadeleri aşağıdaki iki şekilde yeniden kullanabilir:</span><span class="sxs-lookup"><span data-stu-id="8df88-121">Your application can reuse regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="8df88-122">Normal ifadeyi tanımlamak <xref:System.Text.RegularExpressions.Regex> için nesnenin statik bir yöntem kullanarak.</span><span class="sxs-lookup"><span data-stu-id="8df88-122">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="8df88-123">Başka bir statik yöntem çağrısı yla zaten tanımlanmış olan normal bir ifade deseni kullanıyorsanız, normal ifade altyapısı önbellekten almaya çalışır.</span><span class="sxs-lookup"><span data-stu-id="8df88-123">If you're using a regular expression pattern that has already been defined by another static method call, the regular expression engine will try to retrieve it from the cache.</span></span> <span data-ttu-id="8df88-124">Önbellekte kullanılamıyorsa, motor normal ifadeyi derler ve önbelleğe ekler.</span><span class="sxs-lookup"><span data-stu-id="8df88-124">If it's not available in the cache, the engine will compile the regular expression and add it to the cache.</span></span>
  
- <span data-ttu-id="8df88-125">Varolan <xref:System.Text.RegularExpressions.Regex> bir nesneyi normal ifade deseni gerekli olduğu sürece yeniden kullanarak.</span><span class="sxs-lookup"><span data-stu-id="8df88-125">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="8df88-126">Çünkü nesne anlık ve düzenli ifade derleme yükü, oluşturma ve hızla <xref:System.Text.RegularExpressions.Regex> çok sayıda nesne yok çok pahalı bir süreçtir.</span><span class="sxs-lookup"><span data-stu-id="8df88-126">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="8df88-127">Çok sayıda farklı normal ifade kullanan uygulamalar da, statik `Regex` yöntemlere yapılan çağrıları kullanarak ve büyük olasılıkla normal ifade önbelleğinin boyutunu artırarak performansı en iyi duruma getirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="8df88-127">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="8df88-128">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="8df88-128">See also</span></span>

- [<span data-ttu-id="8df88-129">.NET Düzenli İfadeler</span><span class="sxs-lookup"><span data-stu-id="8df88-129">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
