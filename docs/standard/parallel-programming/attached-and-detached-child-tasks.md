---
title: "Eklenen ve Ayrılan Alt Görevler"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords: tasks, child tasks
ms.assetid: c95788bf-90a6-4e96-b7bc-58e36a228cc5
caps.latest.revision: "21"
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.openlocfilehash: c1a0c664dffc2986d4d6985fd2b71cd8055bf2c9
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/21/2017
---
# <a name="attached-and-detached-child-tasks"></a><span data-ttu-id="b882a-102">Eklenen ve Ayrılan Alt Görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-102">Attached and Detached Child Tasks</span></span>
<span data-ttu-id="b882a-103">A *alt görev* (veya *iç içe geçmiş görev*) olan bir <xref:System.Threading.Tasks.Task?displayProperty=nameWithType> kullanıcı temsilci olarak bilinen başka bir görev oluşturulan örnek *üst görev*.</span><span class="sxs-lookup"><span data-stu-id="b882a-103">A *child task* (or *nested task*) is a <xref:System.Threading.Tasks.Task?displayProperty=nameWithType> instance that is created in the user delegate of another task, which is known as the *parent task*.</span></span> <span data-ttu-id="b882a-104">Bir alt görev ya da iliştirilemez ya da.</span><span class="sxs-lookup"><span data-stu-id="b882a-104">A child task can be either detached or attached.</span></span> <span data-ttu-id="b882a-105">A *ayrılmış alt görev* üst bağımsız olarak yürüten bir görevdir.</span><span class="sxs-lookup"><span data-stu-id="b882a-105">A *detached child task* is a task that executes independently of its parent.</span></span> <span data-ttu-id="b882a-106">Bir *alt görev bağlı* ile oluşturulan iç içe geçmiş bir görevi <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> , üst değil açıkça veya varsayılan olarak engelliyor, bağlı olmak seçeneği.</span><span class="sxs-lookup"><span data-stu-id="b882a-106">An *attached child task* is a nested task that is created with the <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> option whose parent does not explicitly or by default prohibit it from being attached.</span></span> <span data-ttu-id="b882a-107">Bir görev herhangi bir sayıda eklenen ve ayrılan alt görevler, yalnızca sistem kaynaklarının yetersizliği sınırlı oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="b882a-107">A task may create any number of attached and detached child tasks, limited only by system resources.</span></span>  
  
 <span data-ttu-id="b882a-108">Aşağıdaki tabloda iki tür alt görevleri arasındaki temel farklar listelenmektedir.</span><span class="sxs-lookup"><span data-stu-id="b882a-108">The following table lists the basic differences between the two kinds of child tasks.</span></span>  
  
|<span data-ttu-id="b882a-109">Kategori</span><span class="sxs-lookup"><span data-stu-id="b882a-109">Category</span></span>|<span data-ttu-id="b882a-110">Ayrılan alt görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-110">Detached child tasks</span></span>|<span data-ttu-id="b882a-111">Ekli alt görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-111">Attached child tasks</span></span>|  
|--------------|--------------------------|--------------------------|  
|<span data-ttu-id="b882a-112">Üst alt görevin tamamlanmasını bekler.</span><span class="sxs-lookup"><span data-stu-id="b882a-112">Parent waits for child tasks to complete.</span></span>|<span data-ttu-id="b882a-113">Hayır</span><span class="sxs-lookup"><span data-stu-id="b882a-113">No</span></span>|<span data-ttu-id="b882a-114">Evet</span><span class="sxs-lookup"><span data-stu-id="b882a-114">Yes</span></span>|  
|<span data-ttu-id="b882a-115">Üst alt görevler tarafından oluşturulan özel durumları yayar.</span><span class="sxs-lookup"><span data-stu-id="b882a-115">Parent propagates exceptions thrown by child tasks.</span></span>|<span data-ttu-id="b882a-116">Hayır</span><span class="sxs-lookup"><span data-stu-id="b882a-116">No</span></span>|<span data-ttu-id="b882a-117">Evet</span><span class="sxs-lookup"><span data-stu-id="b882a-117">Yes</span></span>|  
|<span data-ttu-id="b882a-118">Üst durumunu alt durumuna bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="b882a-118">Status of parent depends on status of child.</span></span>|<span data-ttu-id="b882a-119">Hayır</span><span class="sxs-lookup"><span data-stu-id="b882a-119">No</span></span>|<span data-ttu-id="b882a-120">Evet</span><span class="sxs-lookup"><span data-stu-id="b882a-120">Yes</span></span>|  
  
 <span data-ttu-id="b882a-121">Diğer görevler ile bunların ilişkileri daha az karmaşık olduğundan çoğu senaryoda, ayrılan alt görevler kullanmanızı öneririz.</span><span class="sxs-lookup"><span data-stu-id="b882a-121">In most scenarios, we recommend that you use detached child tasks, because their relationships with other tasks are less complex.</span></span> <span data-ttu-id="b882a-122">Diğer bir deyişle neden üst görevleri içinde oluşturulan görevler, varsayılan olarak ayrılır ve açıkça belirtmelisiniz <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> ekli alt görev oluşturmak için seçeneği.</span><span class="sxs-lookup"><span data-stu-id="b882a-122">That is why tasks created inside parent tasks are detached by default, and you must explicitly specify the <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> option to create an attached child task.</span></span>  
  
## <a name="detached-child-tasks"></a><span data-ttu-id="b882a-123">Ayrılan alt görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-123">Detached child tasks</span></span>  
 <span data-ttu-id="b882a-124">Alt görevin üst göreve göre oluşturulur, ancak varsayılan olarak, üst görevini bağımsızdır.</span><span class="sxs-lookup"><span data-stu-id="b882a-124">Although a child task is created by a parent task, by default it is independent of the parent task.</span></span> <span data-ttu-id="b882a-125">Aşağıdaki örnekte, bir basit alt görevin üst göreve oluşturur.</span><span class="sxs-lookup"><span data-stu-id="b882a-125">In the following example, a parent task creates one simple child task.</span></span> <span data-ttu-id="b882a-126">Kod örneği birden çok kez çalıştırırsanız, örneğin çıktısını gösterilen farklı ve ayrıca çıktı değişebilir, her zaman kod çalıştırmadan fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b882a-126">If you run the example code multiple times, you may notice that the output from the example differs from that shown, and also that the output may change each time you run the code.</span></span> <span data-ttu-id="b882a-127">Bu durum, üst görev ve alt görevler diğer bağımsız olarak execute kaynaklanır; alt ayrılmış bir görevdir.</span><span class="sxs-lookup"><span data-stu-id="b882a-127">This occurs because the parent task and child tasks execute independently of each other; the child is a detached task.</span></span> <span data-ttu-id="b882a-128">Örnek tamamlamak, yalnızca üst görevin tamamlanmasını bekler ve alt görev değil yürütür veya tam konsol uygulaması önce sona erer.</span><span class="sxs-lookup"><span data-stu-id="b882a-128">The example waits only for the parent task to complete, and the child task may not execute or complete before the console app terminates.</span></span>  
  
 [!code-csharp[TPL_ChildTasks#1](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_childtasks/cs/nested1.cs#1)]
 [!code-vb[TPL_ChildTasks#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_childtasks/vb/nested1.vb#1)]  
  
 <span data-ttu-id="b882a-129">Alt görev tarafından temsil edilen varsa bir <xref:System.Threading.Tasks.Task%601> nesne yerine bir <xref:System.Threading.Tasks.Task> nesne olun üst görev erişerek tamamlamak alt için bekleyeceği <xref:System.Threading.Tasks.Task%601.Result%2A?displayProperty=nameWithType> ayrılmış alt görevin olsa bile alt özelliği.</span><span class="sxs-lookup"><span data-stu-id="b882a-129">If the child task is represented by a <xref:System.Threading.Tasks.Task%601> object rather than a <xref:System.Threading.Tasks.Task> object, you can ensure that the parent task will wait for the child to complete by accessing the <xref:System.Threading.Tasks.Task%601.Result%2A?displayProperty=nameWithType> property of the child even if it is a detached child task.</span></span> <span data-ttu-id="b882a-130"><xref:System.Threading.Tasks.Task%601.Result%2A> Özelliği, aşağıdaki örnekte gösterildiği gibi görevi tamamlanana kadar engeller.</span><span class="sxs-lookup"><span data-stu-id="b882a-130">The <xref:System.Threading.Tasks.Task%601.Result%2A> property blocks until its task completes, as the following example shows.</span></span>  
  
 [!code-csharp[TPL_ChildTasks#4](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_childtasks/cs/childtasks.cs#4)]
 [!code-vb[TPL_ChildTasks#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_childtasks/vb/tpl_childtasks.vb#4)]  
  
## <a name="attached-child-tasks"></a><span data-ttu-id="b882a-131">Ekli alt görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-131">Attached child tasks</span></span>  
 <span data-ttu-id="b882a-132">Ayrılan alt görevler, bağlı alt görevler yakından üst öğesi ile eşitlenir.</span><span class="sxs-lookup"><span data-stu-id="b882a-132">Unlike detached child tasks, attached child tasks are closely synchronized with the parent.</span></span> <span data-ttu-id="b882a-133">Kullanarak bir ekli alt görevi için önceki örnekte ayrılmış alt görev değiştirebilirsiniz <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> aşağıdaki örnekte gösterildiği gibi görev oluşturma deyiminde seçeneği.</span><span class="sxs-lookup"><span data-stu-id="b882a-133">You can change the detached child task in the previous example to an attached child task by using the <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> option in the task creation statement, as shown in the following example.</span></span> <span data-ttu-id="b882a-134">Bu kodda ekli alt görevin üst önce tamamlanır.</span><span class="sxs-lookup"><span data-stu-id="b882a-134">In this code, the attached child task completes before its parent.</span></span> <span data-ttu-id="b882a-135">Sonuç olarak, örneğin çıktısını kodu çalıştırmak her zaman aynıdır.</span><span class="sxs-lookup"><span data-stu-id="b882a-135">As a result, the output from the example is the same each time you run the code.</span></span>  
  
 [!code-csharp[TPL_ChildTasks#2](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_childtasks/cs/child1.cs#2)]
 [!code-vb[TPL_ChildTasks#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_childtasks/vb/child1.vb#2)]  
  
 <span data-ttu-id="b882a-136">Ekli alt görevler sıkı bir şekilde eşitlenmiş grafiklerini zaman uyumsuz işlemler oluşturmak için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b882a-136">You can use attached child tasks to create tightly synchronized graphs of asynchronous operations.</span></span>  
  
 <span data-ttu-id="b882a-137">Ancak, yalnızca üst ekli alt görevler yasaklamaz varsa alt görevin üst göreve ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b882a-137">However, a child task can attach to its parent only if its parent does not prohibit attached child tasks.</span></span> <span data-ttu-id="b882a-138">Üst görevleri açıkça engelleyebilir alt görevler için belirterek eklenmesini <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> üst görevin sınıfı oluşturucusu seçeneğinde veya <xref:System.Threading.Tasks.TaskFactory.StartNew%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b882a-138">Parent tasks can explicitly prevent child tasks from attaching to them by specifying the <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> option in the parent task's class constructor or the <xref:System.Threading.Tasks.TaskFactory.StartNew%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="b882a-139">Üst görevleri örtük olarak çağrılarak oluşturulan varsa bunları eklenmesini alt görevler engellemek <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b882a-139">Parent tasks implicitly prevent child tasks from attaching to them if they are created by calling the <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="b882a-140">Aşağıdaki örnek bunu göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="b882a-140">The following example illustrates this.</span></span> <span data-ttu-id="b882a-141">Üst görev çağırarak oluşturulur ancak bu önceki örneğe benzer <xref:System.Threading.Tasks.Task.Run%28System.Action%29?displayProperty=nameWithType> yöntemi yerine <xref:System.Threading.Tasks.TaskFactory.StartNew%28System.Action%29?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b882a-141">It is identical to the previous example, except that the parent task is created by calling the <xref:System.Threading.Tasks.Task.Run%28System.Action%29?displayProperty=nameWithType> method rather than the <xref:System.Threading.Tasks.TaskFactory.StartNew%28System.Action%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="b882a-142">Alt görevin üst göreve eklemek mümkün olmadığı için örneğin çıktısını tahmin edilemez.</span><span class="sxs-lookup"><span data-stu-id="b882a-142">Because the child task is not able to attach to its parent, the output from the example is unpredictable.</span></span> <span data-ttu-id="b882a-143">Varsayılan görev oluşturma seçenekleri çünkü <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> aşırı dahil <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType>, bu örnekte, "alt görevler ayrılmış" bölümündeki ilk örnek işlevsel olarak eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="b882a-143">Because the default task creation options for the <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> overloads include <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType>, this example is functionally equivalent to the first example in the "Detached child tasks" section.</span></span>  
  
 [!code-csharp[TPL_ChildTasks#3](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_childtasks/cs/child1a.cs#3)]
 [!code-vb[TPL_ChildTasks#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_childtasks/vb/child1a.vb#3)]  
  
## <a name="exceptions-in-child-tasks"></a><span data-ttu-id="b882a-144">Alt görevler özel durumları</span><span class="sxs-lookup"><span data-stu-id="b882a-144">Exceptions in child tasks</span></span>  
 <span data-ttu-id="b882a-145">Ayrılmış alt görevin bir özel durum oluşturursa, bu özel durum gözlenen veya doğrudan üst görevinde olduğu gibi iç içe olmayan görev işlenmiş.</span><span class="sxs-lookup"><span data-stu-id="b882a-145">If a detached child task throws an exception, that exception must be observed or handled directly in the parent task just as with any non-nested task.</span></span> <span data-ttu-id="b882a-146">Ekli alt görev bir özel durum oluşturursa, özel durum otomatik olarak üst göreve ve geri bekler veya görevin erişmeye çalışan iş parçacığı yayılır <xref:System.Threading.Tasks.Task%601.Result%2A?displayProperty=nameWithType> özelliği.</span><span class="sxs-lookup"><span data-stu-id="b882a-146">If an attached child task throws an exception, the exception is automatically propagated to the parent task and back to the thread that waits or tries to access the task's <xref:System.Threading.Tasks.Task%601.Result%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="b882a-147">Bu nedenle, bağlı alt görevler kullanarak, tüm özel durumları çağrısında yalnızca bir noktada işleyebilir <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> çağıran iş parçacığı üzerinde.</span><span class="sxs-lookup"><span data-stu-id="b882a-147">Therefore, by using attached child tasks, you can handle all exceptions at just one point in the call to <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> on the calling thread.</span></span> <span data-ttu-id="b882a-148">Daha fazla bilgi için bkz: [özel durum işleme](../../../docs/standard/parallel-programming/exception-handling-task-parallel-library.md).</span><span class="sxs-lookup"><span data-stu-id="b882a-148">For more information, see [Exception Handling](../../../docs/standard/parallel-programming/exception-handling-task-parallel-library.md).</span></span>  
  
## <a name="cancellation-and-child-tasks"></a><span data-ttu-id="b882a-149">İptal etme ve alt görevler</span><span class="sxs-lookup"><span data-stu-id="b882a-149">Cancellation and child tasks</span></span>  
 <span data-ttu-id="b882a-150">Görev iptali işbirlikçi ' dir.</span><span class="sxs-lookup"><span data-stu-id="b882a-150">Task cancellation is cooperative.</span></span> <span data-ttu-id="b882a-151">Diğer bir deyişle, iptal edilebilen olması için her bağlı veya ayrılmış alt görev iptal belirteci durumunu izlemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="b882a-151">That is, to be cancelable, every attached or detached child task must monitor the status of the cancellation token.</span></span> <span data-ttu-id="b882a-152">Bir iptal isteğini kullanarak bir üst ve tüm alt öğelerini iptal etmek istiyorsanız, aynı belirteci tüm görevler bağımsız değişken olarak geçirmek ve her görev her görev isteğine yanıt mantığını sağlayın.</span><span class="sxs-lookup"><span data-stu-id="b882a-152">If you want to cancel a parent and all its children by using one cancellation request, you pass the same token as an argument to all tasks and provide in each task the logic to respond to the request in each task.</span></span> <span data-ttu-id="b882a-153">Daha fazla bilgi için bkz: [görev iptali](../../../docs/standard/parallel-programming/task-cancellation.md) ve [nasıl yapılır: bir görevi ve ait alt öğelerini iptal etme](../../../docs/standard/parallel-programming/how-to-cancel-a-task-and-its-children.md).</span><span class="sxs-lookup"><span data-stu-id="b882a-153">For more information, see [Task Cancellation](../../../docs/standard/parallel-programming/task-cancellation.md) and [How to: Cancel a Task and Its Children](../../../docs/standard/parallel-programming/how-to-cancel-a-task-and-its-children.md).</span></span>  
  
### <a name="when-the-parent-cancels"></a><span data-ttu-id="b882a-154">Ne zaman üst iptal eder</span><span class="sxs-lookup"><span data-stu-id="b882a-154">When the parent cancels</span></span>  
 <span data-ttu-id="b882a-155">Alt görevini başlatmadan önce bir üst kendisini iptal ederse alt hiçbir zaman başlatır.</span><span class="sxs-lookup"><span data-stu-id="b882a-155">If a parent cancels itself before its child task is started, the child never starts.</span></span> <span data-ttu-id="b882a-156">Bir üst kendisi, alt görevini zaten başladıktan sonra iptal ederse, kendi iptal mantığı olmadıkça alt tamamlanıncaya kadar çalışır.</span><span class="sxs-lookup"><span data-stu-id="b882a-156">If a parent cancels itself after its child task has already started, the child runs to completion unless it has its own cancellation logic.</span></span> <span data-ttu-id="b882a-157">Daha fazla bilgi için bkz: [görev iptali](../../../docs/standard/parallel-programming/task-cancellation.md).</span><span class="sxs-lookup"><span data-stu-id="b882a-157">For more information, see [Task Cancellation](../../../docs/standard/parallel-programming/task-cancellation.md).</span></span>  
  
### <a name="when-a-detached-child-task-cancels"></a><span data-ttu-id="b882a-158">Ayrılmış alt görevin ne zaman iptal eder</span><span class="sxs-lookup"><span data-stu-id="b882a-158">When a detached child task cancels</span></span>  
 <span data-ttu-id="b882a-159">Özel durum zararsız iş Birliği iptal değerlendirildiğinden ayrılmış alt görevin kendisi üst geçildi aynı belirteci kullanarak iptal eder ve üst alt görevin tamamlanmasını bekle değil, hiçbir özel durum yayılır.</span><span class="sxs-lookup"><span data-stu-id="b882a-159">If a detached child task cancels itself by using the same token that was passed to the parent, and the parent does not wait for the child task, no exception is propagated, because the exception is treated as benign cooperation cancellation.</span></span> <span data-ttu-id="b882a-160">Bu davranış, herhangi bir üst düzey görev aynıdır.</span><span class="sxs-lookup"><span data-stu-id="b882a-160">This behavior is the same as that of any top-level task.</span></span>  
  
### <a name="when-an-attached-child-task-cancels"></a><span data-ttu-id="b882a-161">Ne zaman bir ekli alt görev iptal eder</span><span class="sxs-lookup"><span data-stu-id="b882a-161">When an attached child task cancels</span></span>  
 <span data-ttu-id="b882a-162">Ekli alt görevin kendisi onun üst görevine geçirildi aynı belirteci kullanarak iptal ettiğinde bir <xref:System.Threading.Tasks.TaskCanceledException> katılma iş parçacığı içinde yayılır bir <xref:System.AggregateException>.</span><span class="sxs-lookup"><span data-stu-id="b882a-162">When an attached child task cancels itself by using the same token that was passed to its parent task, a <xref:System.Threading.Tasks.TaskCanceledException> is propagated to the joining thread inside an <xref:System.AggregateException>.</span></span> <span data-ttu-id="b882a-163">Böylece yukarı ekli alt görevler bir grafik yayılır tüm hataya neden olan özel durumları yanı sıra tüm zararsız özel durumları işlemek için üst görev beklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="b882a-163">You must wait for the parent task so that you can handle all benign exceptions in addition to all faulting exceptions that are propagated up through a graph of attached child tasks.</span></span>  
  
 <span data-ttu-id="b882a-164">Daha fazla bilgi için bkz: [özel durum işleme](../../../docs/standard/parallel-programming/exception-handling-task-parallel-library.md).</span><span class="sxs-lookup"><span data-stu-id="b882a-164">For more information, see [Exception Handling](../../../docs/standard/parallel-programming/exception-handling-task-parallel-library.md).</span></span>  
  
## <a name="preventing-a-child-task-from-attaching-to-its-parent"></a><span data-ttu-id="b882a-165">Alt görevin üst göreve eklenmesini önleme</span><span class="sxs-lookup"><span data-stu-id="b882a-165">Preventing a child task from attaching to its parent</span></span>  
 <span data-ttu-id="b882a-166">Bir alt görevi tarafından oluşturulan işlenmeyen bir özel durum üst göreve yayılır.</span><span class="sxs-lookup"><span data-stu-id="b882a-166">An unhandled exception that is thrown by a child task is propagated to the parent task.</span></span> <span data-ttu-id="b882a-167">Bu davranış, görevleri ağacının çapraz geçiş yapan yerine bir kök görevden tüm alt görev özel durumlarını izlemek için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b882a-167">You can use this behavior to observe all child task exceptions from one root task instead of traversing a tree of tasks.</span></span> <span data-ttu-id="b882a-168">Ancak, bir üst görev diğer kodundan ek beklemiyor olduğunda özel durum yayma sorunlu olabilir.</span><span class="sxs-lookup"><span data-stu-id="b882a-168">However, exception propagation can be problematic when a parent task does not expect attachment from other code.</span></span> <span data-ttu-id="b882a-169">Örneğin, bir üçüncü taraf kitaplığı bileşenden çağıran uygulamasının göz önünde bulundurun bir <xref:System.Threading.Tasks.Task> nesnesi.</span><span class="sxs-lookup"><span data-stu-id="b882a-169">For example, consider an app that calls a third-party library component from a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="b882a-170">Üçüncü taraf kitaplık bileşeni de oluşturursa bir <xref:System.Threading.Tasks.Task> nesne ve belirten <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> üst göreve eklemek için üst alt görevde meydana gelen işlenmeyen özel durumlar yayılması.</span><span class="sxs-lookup"><span data-stu-id="b882a-170">If the third-party library component also creates a <xref:System.Threading.Tasks.Task> object and specifies <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> to attach it to the parent task, any unhandled exceptions that occur in the child task propagate to the parent.</span></span> <span data-ttu-id="b882a-171">Bu ana uygulama beklenmeyen davranışlara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="b882a-171">This could lead to unexpected behavior in the main app.</span></span>  
  
 <span data-ttu-id="b882a-172">Bir alt görev kendi üst göreve eklenmesini önlemek için şunu belirtin <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> seçeneği üst oluşturduğunuzda <xref:System.Threading.Tasks.Task> veya <xref:System.Threading.Tasks.Task%601> nesnesi.</span><span class="sxs-lookup"><span data-stu-id="b882a-172">To prevent a child task from attaching to its parent task, specify the <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> option when you create the parent <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> object.</span></span> <span data-ttu-id="b882a-173">Ne zaman kendi üst eklemek bir görev çalışır ve üst belirtir <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> seçeneği, alt görevin üst öğeye eklemeniz mümkün olmaz ve yalnızca yürütecek gibi <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> seçeneği belirtilmedi.</span><span class="sxs-lookup"><span data-stu-id="b882a-173">When a task tries to attach to its parent and the parent specifies the <xref:System.Threading.Tasks.TaskCreationOptions.DenyChildAttach?displayProperty=nameWithType> option, the child task will not be able to attach to a parent and will execute just as if the <xref:System.Threading.Tasks.TaskCreationOptions.AttachedToParent?displayProperty=nameWithType> option was not specified.</span></span>  
  
 <span data-ttu-id="b882a-174">Alt görevin alt görev zamanında bitirdiğinizde değil, üst göreve eklenmesini önlemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b882a-174">You might also want to prevent a child task from attaching to its parent when the child task does not finish in a timely manner.</span></span> <span data-ttu-id="b882a-175">Tüm alt görevler tamamlanana kadar üst göreve tamamlanmıyor olduğundan, uzun süre çalışan alt görevin kötü gerçekleştirmek genel uygulama neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="b882a-175">Because a parent task does not finish until all child tasks finish, a long-running child task can cause the overall app to perform poorly.</span></span> <span data-ttu-id="b882a-176">Bir görev kendi üst göreve eklenmesini engelleyerek uygulama performansını gösteren örnek için bkz: [nasıl yapılır: alt görevin üst göreve Attaching gelen önleme](../../../docs/standard/parallel-programming/how-to-prevent-a-child-task-from-attaching-to-its-parent.md).</span><span class="sxs-lookup"><span data-stu-id="b882a-176">For an example that shows how to improve app performance by preventing a task from attaching to its parent task, see [How to: Prevent a Child Task from Attaching to its Parent](../../../docs/standard/parallel-programming/how-to-prevent-a-child-task-from-attaching-to-its-parent.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b882a-177">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="b882a-177">See Also</span></span>  
 [<span data-ttu-id="b882a-178">Paralel Programlama</span><span class="sxs-lookup"><span data-stu-id="b882a-178">Parallel Programming</span></span>](../../../docs/standard/parallel-programming/index.md)  
 [<span data-ttu-id="b882a-179">Veri Paralelliği</span><span class="sxs-lookup"><span data-stu-id="b882a-179">Data Parallelism</span></span>](../../../docs/standard/parallel-programming/data-parallelism-task-parallel-library.md)
