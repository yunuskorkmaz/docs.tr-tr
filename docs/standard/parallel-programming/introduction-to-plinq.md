---
title: "PLINQ'e Giriş"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
caps.latest.revision: 
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.workload:
- dotnet
- dotnetcore
ms.openlocfilehash: 6ee718737885618322c3623a80b0b091bbfc729d
ms.sourcegitcommit: 3a96c706e4dbb4667bf3bf37edac9e1666646f93
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/27/2018
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="a0b75-102">PLINQ'e Giriş</span><span class="sxs-lookup"><span data-stu-id="a0b75-102">Introduction to PLINQ</span></span>
## <a name="what-is-a-parallel-query"></a><span data-ttu-id="a0b75-103">Paralel Sorgu nedir?</span><span class="sxs-lookup"><span data-stu-id="a0b75-103">What is a Parallel Query?</span></span>  
 <span data-ttu-id="a0b75-104">Dil ile tümleşik sorgu (LINQ) içinde sunulmuştur [!INCLUDE[net_v35_long](../../../includes/net-v35-long-md.md)].</span><span class="sxs-lookup"><span data-stu-id="a0b75-104">Language-Integrated Query (LINQ) was introduced in the [!INCLUDE[net_v35_long](../../../includes/net-v35-long-md.md)].</span></span>  <span data-ttu-id="a0b75-105">Herhangi bir sorgulama için birleşik bir modeli özellikleri <xref:System.Collections.IEnumerable?displayProperty=nameWithType> veya <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> bir tür kullanımı uyumlu şekilde veri kaynağı.</span><span class="sxs-lookup"><span data-stu-id="a0b75-105">It features a unified model for querying any <xref:System.Collections.IEnumerable?displayProperty=nameWithType> or <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> data source in a type-safe manner.</span></span> <span data-ttu-id="a0b75-106">Nesnelere LINQ bellek içi koleksiyonları karşı gibi çalıştırmak LINQ sorgularını addır <xref:System.Collections.Generic.List%601> ve dizi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-106">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="a0b75-107">Bu makalede LINQ ilgili temel bilgilere sahip olduğunuzu varsayar.</span><span class="sxs-lookup"><span data-stu-id="a0b75-107">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="a0b75-108">Daha fazla bilgi için bkz: [LINQ (dil ile tümleşik sorgu)](http://msdn.microsoft.com/library/a73c4aec-5d15-4e98-b962-1274021ea93d).</span><span class="sxs-lookup"><span data-stu-id="a0b75-108">For more information, see [LINQ (Language-Integrated Query)](http://msdn.microsoft.com/library/a73c4aec-5d15-4e98-b962-1274021ea93d).</span></span>  
  
 <span data-ttu-id="a0b75-109">Paralel LINQ (PLINQ) bir paralel LINQ düzeni uygulamasıdır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-109">Parallel LINQ (PLINQ) is a parallel implementation of the LINQ pattern.</span></span> <span data-ttu-id="a0b75-110">Birçok yolu PLINQ sorgusunda olmayan paralel LINQ to nesneleri sorgusunda benzer.</span><span class="sxs-lookup"><span data-stu-id="a0b75-110">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="a0b75-111">PLINQ sorguları, olduğu gibi sıralı [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] sorguları, çalışan tüm bellek içi üzerinde <xref:System.Collections.IEnumerable> veya <xref:System.Collections.Generic.IEnumerable%601> veri kaynağı ve bunlar değil başlamak sorgu numaralandırılan kadar yürütme anlamına gelir yürütme ertelenmiş.</span><span class="sxs-lookup"><span data-stu-id="a0b75-111">PLINQ queries, just like sequential [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="a0b75-112">PLINQ sistemdeki tüm işlemcilerin tam kullanımını yapma girişiminde birincil farktır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-112">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="a0b75-113">Veri kaynağı kesimler halinde bölümlendirme ve birden çok işlemci üzerinde paralel ayrı çalışan iş parçacığı üzerinde her kesiminde sorgu yürütülürken tarafından bunu yapar.</span><span class="sxs-lookup"><span data-stu-id="a0b75-113">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="a0b75-114">Çoğu durumda, sorgu önemli ölçüde daha hızlı çalışır, Paralel yürütme anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-114">In many cases, parallel execution means that the query runs significantly faster.</span></span>  
  
 <span data-ttu-id="a0b75-115">Paralel yürütme, sorguları, ekleyerek genellikle yalnızca belirli türdeki için eski kod üzerinden, önemli performans geliştirmeleri PLINQ gerçekleştirebilirsiniz <xref:System.Linq.ParallelEnumerable.AsParallel%2A> sorgu veri kaynağı işlemi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-115">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="a0b75-116">Ancak, paralellik kendi karmaşıklık getirebilir ve tüm sorgu işlemleri PLINQ'te daha hızlı çalışır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-116">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="a0b75-117">Aslında, paralelleştirme gerçekte bazı sorgular yavaşlatır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-117">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="a0b75-118">Bu nedenle, sıralama gibi sorunlar paralel sorgular nasıl etkileyeceğini anlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-118">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="a0b75-119">Daha fazla bilgi için bkz: [Plınq'te hızlandırmayı anlama](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-119">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="a0b75-120">Bu belge lambda ifadeleri temsilciler PLINQ'te tanımlamak için kullanır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-120">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="a0b75-121">C# veya Visual Basic'deki lambda ifadeleri alışık değilseniz bkz [PLINQ ve TPL'deki Lambda ifadeleri](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-121">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span></span>  
  
 <span data-ttu-id="a0b75-122">Bu makalenin sonraki bölümlerinde ana PLINQ sınıfları genel bir bakış sağlar ve PLINQ sorgu oluşturma açıklanır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-122">The remainder of this article gives an overview of the main PLINQ classes, and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="a0b75-123">Her bölüm daha ayrıntılı bilgi ve kod örnekleri bağlantılar içerir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-123">Each section contains links to more detailed information and code examples.</span></span>  
  
## <a name="the-parallelenumerable-class"></a><span data-ttu-id="a0b75-124">ParallelEnumerable Sınıfı</span><span class="sxs-lookup"><span data-stu-id="a0b75-124">The ParallelEnumerable Class</span></span>  
 <span data-ttu-id="a0b75-125"><xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> Sınıfı neredeyse tüm PLINQ'in işlevselliğini kullanıma sunar.</span><span class="sxs-lookup"><span data-stu-id="a0b75-125">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span>  <span data-ttu-id="a0b75-126">Bu ve diğer <xref:System.Linq?displayProperty=nameWithType> ad alanı türleri System.Core.dll bütünleştirilmiş koda derlenmiş.</span><span class="sxs-lookup"><span data-stu-id="a0b75-126">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="a0b75-127">Visual Studio'da varsayılan C# ve Visual Basic projeleri derleme başvurusu hem ad içeri aktarın.</span><span class="sxs-lookup"><span data-stu-id="a0b75-127">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>  
  
 <span data-ttu-id="a0b75-128"><xref:System.Linq.ParallelEnumerable> her biri paralel hale çalışmaz ancak nesnelere LINQ destekleyen tüm standart sorgu işleçlerinin uygulamaları içerir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-128"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="a0b75-129">Alışık değilseniz [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)], bkz: [Lınq'ye](http://msdn.microsoft.com/library/24dddf19-12a0-4707-a4bc-eba4fa7f219e).</span><span class="sxs-lookup"><span data-stu-id="a0b75-129">If you are not familiar with [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)], see [Introduction to LINQ](http://msdn.microsoft.com/library/24dddf19-12a0-4707-a4bc-eba4fa7f219e).</span></span>  
  
 <span data-ttu-id="a0b75-130">Standart sorgu işleçleri yanı sıra <xref:System.Linq.ParallelEnumerable> sınıfı Paralel yürütme belirli davranışları sağlayan yöntemler kümesi içerir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-130">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="a0b75-131">Bu PLINQ özgü yöntemleri aşağıdaki tabloda listelenmiştir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-131">These PLINQ-specific methods are listed in the following table.</span></span>  
  
|<span data-ttu-id="a0b75-132">ParallelEnumerable işleci</span><span class="sxs-lookup"><span data-stu-id="a0b75-132">ParallelEnumerable Operator</span></span>|<span data-ttu-id="a0b75-133">Açıklama</span><span class="sxs-lookup"><span data-stu-id="a0b75-133">Description</span></span>|  
|---------------------------------|-----------------|  
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="a0b75-134">PLINQ için giriş noktası.</span><span class="sxs-lookup"><span data-stu-id="a0b75-134">The entry point for PLINQ.</span></span> <span data-ttu-id="a0b75-135">Mümkünse sorgunun kalan bölümüyle paralel birkaç ölçeklendirin olduğunu, belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-135">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="a0b75-136">Sorgunun kalan bölümüyle ardışık olarak olmayan paralel LINQ Sorgu olarak çalıştırılması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-136">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="a0b75-137">PLINQ sorgu veya sıralama, örneğin (Order By Vlsual Basic'te) orderby yan tümcesinin kullandığı değiştirilene kadar kalan için kaynak sıradaki sıralama korumak belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-137">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Vlsual Basic) clause.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="a0b75-138">PLINQ sorgu geri kalanı için kaynak dizi sıralaması korumak için gerekli olmadığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-138">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="a0b75-139">PLINQ düzenli aralıklarla sağlanan iptal belirteci durumunu izlemek ve istenirse yürütme iptal belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-139">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="a0b75-140">En fazla PLINQ sorgu paralel hale kullanması gereken işlemci sayısını belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-140">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="a0b75-141">Mümkünse, nasıl PLINQ gerektiği hakkında bir ipucu sağlar Süren iş parçacığı üzerinde yalnızca bir sıralı uygulamasına geri paralel sonuçları birleştirir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-141">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="a0b75-142">Sıralı olarak çalıştırmak için varsayılan davranış bile olacaktır zaman PLINQ sorgu paralel hale olup olmadığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-142">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="a0b75-143">Sağlayan, sorgu sonuçları yineleme aksine ilk geri tüketici iş parçacığı için birleştirme olmadan paralel olarak işlenecek sonuçları birden çok iş parçacıklı numaralandırma yöntemi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-143">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|  
|<span data-ttu-id="a0b75-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> Aşırı yükleme</span><span class="sxs-lookup"><span data-stu-id="a0b75-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="a0b75-145">PLINQ için benzersizdir ve iş parçacığı yerel bölümleri artı tüm bölümleri sonuçlarını birleştirmek için bir son toplama işlevi ara toplama sağlayan bir aşırı.</span><span class="sxs-lookup"><span data-stu-id="a0b75-145">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|  
  
## <a name="the-opt-in-model"></a><span data-ttu-id="a0b75-146">Abone Olma Modeli</span><span class="sxs-lookup"><span data-stu-id="a0b75-146">The Opt-in Model</span></span>  
 <span data-ttu-id="a0b75-147">Bir sorgu yazdığınızda, PLINQ için çağırarak kabul <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> genişletme yöntemi aşağıdaki örnekte gösterildiği gibi veri kaynağı üzerinde.</span><span class="sxs-lookup"><span data-stu-id="a0b75-147">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>  
  
 [!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
 [!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]  
  
 <span data-ttu-id="a0b75-148"><xref:System.Linq.ParallelEnumerable.AsParallel%2A> Genişletme yöntemi bağlar sonraki sorgu işleçleri bu durumda, `where` ve `select`, <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> uygulamaları.</span><span class="sxs-lookup"><span data-stu-id="a0b75-148">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>  
  
## <a name="execution-modes"></a><span data-ttu-id="a0b75-149">Yürütme Modları</span><span class="sxs-lookup"><span data-stu-id="a0b75-149">Execution Modes</span></span>  
 <span data-ttu-id="a0b75-150">Varsayılan olarak, PLINQ koruyucu.</span><span class="sxs-lookup"><span data-stu-id="a0b75-150">By default, PLINQ is conservative.</span></span> <span data-ttu-id="a0b75-151">Çalışma zamanında PLINQ altyapı sorgu genel yapısını analiz eder.</span><span class="sxs-lookup"><span data-stu-id="a0b75-151">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="a0b75-152">Sorgu speedups paralelleştirme tarafından elde etmek üzere olasılığı varsa, kaynak sırası, eşzamanlı olarak çalışan görevler içine PLINQ bölümler.</span><span class="sxs-lookup"><span data-stu-id="a0b75-152">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="a0b75-153">Bir sorgu paralel hale güvenli değilse, PLINQ yalnızca sorgu sırayla çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-153">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="a0b75-154">PLINQ olası pahalı bir paralel algoritması veya pahalı olmayan bir sıralı algoritma arasında bir seçim varsa, varsayılan olarak sıralı algoritmasını seçer.</span><span class="sxs-lookup"><span data-stu-id="a0b75-154">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="a0b75-155">Kullanabileceğiniz <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> yöntemi ve <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> paralel algoritmasını seçmek için PLINQ istemek üzere numaralandırması.</span><span class="sxs-lookup"><span data-stu-id="a0b75-155">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="a0b75-156">Test etme ve belirli bir sorgu paralel olarak hızlı yürütür ölçüm bildiğiniz durumlarda kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-156">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="a0b75-157">Daha fazla bilgi için bkz: [nasıl yapılır: PLINQ'te yürütme modunu belirtme](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-157">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>  
  
## <a name="degree-of-parallelism"></a><span data-ttu-id="a0b75-158">Paralellik Derecesi</span><span class="sxs-lookup"><span data-stu-id="a0b75-158">Degree of Parallelism</span></span>  
 <span data-ttu-id="a0b75-159">Varsayılan olarak, PLINQ tüm işlemcilerin ana bilgisayara kullanır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-159">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="a0b75-160">Belirtilen bir işlemci sayısı birden fazla kullanarak PLINQ söyleyebilirsiniz <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-160">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="a0b75-161">Bilgisayarda çalışan başka işlemler belirli bir miktarda CPU süresi aldığınızdan emin olmak istediğinizde kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-161">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="a0b75-162">Aşağıdaki kod parçacığında en fazla iki işlemci kullanılarak sorgu sınırlar.</span><span class="sxs-lookup"><span data-stu-id="a0b75-162">The following snippet limits the query to utilizing a maximum of two processors.</span></span>  
  
 [!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
 [!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]  
  
 <span data-ttu-id="a0b75-163">Bir sorgu önemli miktarda dosya g/ç gibi işlem bağlı iş yeri gerçekleştiriyor durumlarda çekirdek sayısından büyük paralellik derecesini makinede belirtmek faydalı olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-163">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>  
  
## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="a0b75-164">Sıralı ve Sırasız Paralel Sorgular</span><span class="sxs-lookup"><span data-stu-id="a0b75-164">Ordered Versus Unordered Parallel Queries</span></span>  
 <span data-ttu-id="a0b75-165">Bazı sorgular, sorgu işleci kaynak dizi sıralaması korumak sonuçlara yol gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-165">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="a0b75-166">PLINQ sağlar <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> bu amaçla işleci.</span><span class="sxs-lookup"><span data-stu-id="a0b75-166">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="a0b75-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> farklıdır <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span><span class="sxs-lookup"><span data-stu-id="a0b75-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="a0b75-168">Bir <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> dizisi hala paralel olarak işlenir, ancak sonuçlarını arabelleğe ve sıralanır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-168">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="a0b75-169">Sıra koruma genellikle ek iş içerdiğinden bir <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> dizisi işlenmesi varsayılan daha yavaş <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> dizisi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-169">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="a0b75-170">Belirli bir sıralı paralel işlem işlemi sıralı bir sürümünü daha hızlı olup olmadığını birçok faktöre bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-170">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>  
  
 <span data-ttu-id="a0b75-171">Aşağıdaki kod örneğinde, sıra koruma için kabul gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-171">The following code example shows how to opt in to order preservation.</span></span>  
  
 [!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
 [!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]  
  
 <span data-ttu-id="a0b75-172">Daha fazla bilgi için bkz: [plınq'te sıra koruma](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-172">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>  
  
## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="a0b75-173">Paralel vs. Sıralı sorguları</span><span class="sxs-lookup"><span data-stu-id="a0b75-173">Parallel vs. Sequential Queries</span></span>  
 <span data-ttu-id="a0b75-174">Bazı işlemler, veri kaynağını sıralı bir şekilde teslim edilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-174">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="a0b75-175"><xref:System.Linq.ParallelEnumerable> Sorgu işleçleri, gerekli olduğunda sıralı moduna otomatik olarak geri döndürüyoruz.</span><span class="sxs-lookup"><span data-stu-id="a0b75-175">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="a0b75-176">Kullanıcı tanımlı sorgu işleçleri ve sıralı yürütme gerektiren kullanıcı temsilcileri için PLINQ sağlar <xref:System.Linq.ParallelEnumerable.AsSequential%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-176">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="a0b75-177">Kullandığınızda <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, sorgudaki tüm sonraki işleçleri kadar sırayla yürütülen <xref:System.Linq.ParallelEnumerable.AsParallel%2A> yeniden adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-177">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="a0b75-178">Daha fazla bilgi için bkz: [nasıl yapılır: birleştirmek paralel ve sıralı LINQ sorgularını](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-178">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>  
  
## <a name="options-for-merging-query-results"></a><span data-ttu-id="a0b75-179">Sorgu Sonuçlarını Birleştirme Seçenekleri</span><span class="sxs-lookup"><span data-stu-id="a0b75-179">Options for Merging Query Results</span></span>  
 <span data-ttu-id="a0b75-180">Tüketimi için ana iş parçacığı üzerine geri bir PLINQ sorgusu paralel olarak yürütüldüğünde, her iş parçacığı kendi sonuçlarından birleştirilmelidir bir `foreach` döngü (`For Each` içinde [!INCLUDE[vbprvb](../../../includes/vbprvb-md.md)]), veya bir liste veya dizi ekleme.</span><span class="sxs-lookup"><span data-stu-id="a0b75-180">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in [!INCLUDE[vbprvb](../../../includes/vbprvb-md.md)]), or insertion into a list or array.</span></span> <span data-ttu-id="a0b75-181">Bazı durumlarda, örneğin birleştirme işlemi, belirli bir tür belirtmek için sonuçlar daha hızlı oluşturan başlamak için faydalı olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-181">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="a0b75-182">Bu amaçla PLINQ destekleyen <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> yöntemi ve <xref:System.Linq.ParallelMergeOptions> numaralandırması.</span><span class="sxs-lookup"><span data-stu-id="a0b75-182">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="a0b75-183">Daha fazla bilgi için bkz: [plınq'te Birleştirme Seçenekleri](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-183">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>  
  
## <a name="the-forall-operator"></a><span data-ttu-id="a0b75-184">ForAll İşleci</span><span class="sxs-lookup"><span data-stu-id="a0b75-184">The ForAll Operator</span></span>  
 <span data-ttu-id="a0b75-185">Sıralı olarak [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] sorguları, yürütme sorgu numaralandırılan kadar ertelenmiş ya da bir `foreach` (`For Each` içinde [!INCLUDE[vbprvb](../../../includes/vbprvb-md.md)]) döngü veya göre çağrılırken bir yöntem gibi <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , veya <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span><span class="sxs-lookup"><span data-stu-id="a0b75-185">In sequential [!INCLUDE[vbteclinq](../../../includes/vbteclinq-md.md)] queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in [!INCLUDE[vbprvb](../../../includes/vbprvb-md.md)]) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="a0b75-186">De kullanabilirsiniz, PLINQ'te `foreach` sorguyu yürütmek ve sonuçlarını yinelemek için.</span><span class="sxs-lookup"><span data-stu-id="a0b75-186">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="a0b75-187">Ancak, `foreach` kendisini paralel olarak çalışmaz ve bu nedenle, tüm Paralel Görevler çıktısını geri döngü çalıştığı akışına birleştirilemez gerektirir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-187">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="a0b75-188">PLINQ'te, kullandığınız `foreach` zaman son sorgu sonuçlarını sıralama korumanız gerekir ve ayrıca her işlediğiniz sonuçları, çağrılırken örneğin seri bir şekilde `Console.WriteLine` her öğe için.</span><span class="sxs-lookup"><span data-stu-id="a0b75-188">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="a0b75-189">İçin daha hızlı sorgu yürütme sıra koruma gerekli olmadığında ve sonuçları işlenmesini kendisini paralel birkaç ölçeklendirin kullanacağınız <xref:System.Linq.ParallelEnumerable.ForAll%2A> yöntemi bir PLINQ sorgusu yürütülemedi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-189">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="a0b75-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A> Bu son birleştirme adım gerçekleştirmez.</span><span class="sxs-lookup"><span data-stu-id="a0b75-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="a0b75-191">Aşağıdaki kod örneğinde nasıl kullanılacağını gösterir <xref:System.Linq.ParallelEnumerable.ForAll%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="a0b75-191">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="a0b75-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> aynı anda tüm öğeleri kaldırmak çalışırken olmadan ekleme birden çok iş parçacığı için optimize edilmiş olduğundan burada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>  
  
 [!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
 [!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]  
  
 <span data-ttu-id="a0b75-193">Aşağıdaki çizimde arasındaki farkı gösterir `foreach` ve <xref:System.Linq.ParallelEnumerable.ForAll%2A> sorgu yürütme sabittir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-193">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>  
  
 <span data-ttu-id="a0b75-194">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="a0b75-194">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>  
  
## <a name="cancellation"></a><span data-ttu-id="a0b75-195">İptal Etme</span><span class="sxs-lookup"><span data-stu-id="a0b75-195">Cancellation</span></span>  
 <span data-ttu-id="a0b75-196">PLINQ iptal türler ile tümleştirildiğinde [!INCLUDE[net_v40_short](../../../includes/net-v40-short-md.md)].</span><span class="sxs-lookup"><span data-stu-id="a0b75-196">PLINQ is integrated with the cancellation types in [!INCLUDE[net_v40_short](../../../includes/net-v40-short-md.md)].</span></span> <span data-ttu-id="a0b75-197">(Daha fazla bilgi için bkz: [yönetilen iş parçacıklarında iptal](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Bu nedenle, nesneler sorgulara sıralı LINQ PLINQ sorguları iptal edilebilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-197">(For more information, see [Cancellation in Managed Threads](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="a0b75-198">İptal edilebilen bir PLINQ sorgusu oluşturmak için kullanmak <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> sorgu işlecinin ve sağlayan bir <xref:System.Threading.CancellationToken> örneği bağımsız değişkeni olarak.</span><span class="sxs-lookup"><span data-stu-id="a0b75-198">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="a0b75-199">Zaman <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> belirteç özelliği PLINQ true olarak ayarlanmış, dikkat, tüm iş parçacıklarında işlemeyi durdur ve throw bir <xref:System.OperationCanceledException>.</span><span class="sxs-lookup"><span data-stu-id="a0b75-199">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>  
  
 <span data-ttu-id="a0b75-200">PLINQ sorgusunu iptal belirteci ayarlandıktan sonra bazı öğeler işlemeye devam edebilir mümkündür.</span><span class="sxs-lookup"><span data-stu-id="a0b75-200">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>  
  
 <span data-ttu-id="a0b75-201">Büyük yanıtlama hızı için uzun süre çalışan kullanıcı temsilcileri içinde iptal isteklerini yanıtlayabilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-201">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="a0b75-202">Daha fazla bilgi için bkz: [nasıl yapılır: PLINQ sorgusunu iptal](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-202">For more information, see [How to: Cancel a PLINQ Query](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span></span>  
  
## <a name="exceptions"></a><span data-ttu-id="a0b75-203">Özel Durumlar</span><span class="sxs-lookup"><span data-stu-id="a0b75-203">Exceptions</span></span>  
 <span data-ttu-id="a0b75-204">Bir PLINQ sorgusu yürütüldüğünde, birden çok farklı iş parçacığı tarafından eşzamanlı olarak özel durumlar.</span><span class="sxs-lookup"><span data-stu-id="a0b75-204">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="a0b75-205">Ayrıca, farklı bir iş parçacığında özel durum oluşturdu kodu daha özel durumu işlemek üzere kod olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-205">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="a0b75-206">PLINQ kullanan <xref:System.AggregateException> bir sorgu tarafından oluşturulan tüm özel durumları kapsülleyen yazın ve bu özel durumlar çağıran iş parçacığı dön'ı sıralama.</span><span class="sxs-lookup"><span data-stu-id="a0b75-206">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="a0b75-207">Çağıran iş parçacığı üzerinde yalnızca bir try-catch bloğu gereklidir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-207">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="a0b75-208">Ancak, tüm içinde kapsüllenmiş özel durumların aracılığıyla yineleyebilirsiniz <xref:System.AggregateException> ve güvenli bir şekilde kurtarabilir herhangi catch.</span><span class="sxs-lookup"><span data-stu-id="a0b75-208">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="a0b75-209">Nadir durumlarda bazı özel durumlar olarak kaydırılan değil durum oluşturulabilir bir <xref:System.AggregateException>, ve <xref:System.Threading.ThreadAbortException>s ayrıca değil sarılır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-209">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>  
  
 <span data-ttu-id="a0b75-210">Özel durum oluşturulduktan sonra bazı öğeleri işlemek bir sorgu devam edebilir mümkündür ne zaman özel durumlar oluşturan geri katılma akışa Kabarcık izin verilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-210">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>  
  
 <span data-ttu-id="a0b75-211">Daha fazla bilgi için bkz: [nasıl yapılır: PLINQ sorgusunda özel durumları işlemek](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-211">For more information, see [How to: Handle Exceptions in a PLINQ Query](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span></span>  
  
## <a name="custom-partitioners"></a><span data-ttu-id="a0b75-212">Özel Bölümleyiciler</span><span class="sxs-lookup"><span data-stu-id="a0b75-212">Custom Partitioners</span></span>  
 <span data-ttu-id="a0b75-213">Bazı durumlarda, bazı karakteristiğini kaynak verileri, yararlanan özel bir bölümleyici yazarak sorgu performansını iyileştirebilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-213">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="a0b75-214">Sorgu, özel bölümleyici sorgulanan numaralandırılabilir nesnesidir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-214">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>  
  
 [!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
 [!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]  
  
 <span data-ttu-id="a0b75-215">(Veri dinamik olarak o bölümler için Yük Dengeleme için çalışma zamanı sırasında atanabilir. rağmen) PLINQ bölümleri sabit sayıda destekler.</span><span class="sxs-lookup"><span data-stu-id="a0b75-215">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="a0b75-216"><xref:System.Threading.Tasks.Parallel.For%2A> ve <xref:System.Threading.Tasks.Parallel.ForEach%2A> yalnızca dinamik bölümlendirme, bölümler değişiklik sayısı çalışma zamanında anlamına destekler.</span><span class="sxs-lookup"><span data-stu-id="a0b75-216"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="a0b75-217">Daha fazla bilgi için bkz: [PLINQ ve TPL için özel Bölümleyiciler](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-217">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>  
  
## <a name="measuring-plinq-performance"></a><span data-ttu-id="a0b75-218">PLINQ Performansını Ölçme</span><span class="sxs-lookup"><span data-stu-id="a0b75-218">Measuring PLINQ Performance</span></span>  
 <span data-ttu-id="a0b75-219">Çoğu durumda, bir sorgu paralel birkaç ölçeklendirin, ancak elde edilen performans avantajı paralel sorgulamasını oluşturma yükünü ağır.</span><span class="sxs-lookup"><span data-stu-id="a0b75-219">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="a0b75-220">Bir sorgu kadar hesaplama yapmıyorsa veya veri kaynağı küçükse, bir PLINQ sorgusu sıralı LINQ to nesneleri sorgusunda daha yavaş olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0b75-220">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="a0b75-221">İşleme engelleri bulun ve sorgunuzu paralel veya sıralı olarak çalışıp çalışmadığını belirlemek için çeşitli sorguların performansını karşılaştırmak için Visual Studio Team Server'da paralel Performans Çözümleyicisi'ni kullanın.</span><span class="sxs-lookup"><span data-stu-id="a0b75-221">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="a0b75-222">Daha fazla bilgi için bkz: [eşzamanlılık görselleştiricisi](/visualstudio/profiling/concurrency-visualizer) ve [nasıl yapılır: PLINQ sorgu performansını ölçü](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span><span class="sxs-lookup"><span data-stu-id="a0b75-222">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a0b75-223">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="a0b75-223">See Also</span></span>  
 [<span data-ttu-id="a0b75-224">Paralel LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="a0b75-224">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)  
 [<span data-ttu-id="a0b75-225">PLINQ'te Hızlandırmayı Anlama</span><span class="sxs-lookup"><span data-stu-id="a0b75-225">Understanding Speedup in PLINQ</span></span>](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)
