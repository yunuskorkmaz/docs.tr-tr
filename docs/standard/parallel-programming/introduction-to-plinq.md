---
title: PLINQ'e Giriş
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
ms.openlocfilehash: e9ef72c2691a4dbb9c68202b29e0f5c77dcdaa74
ms.sourcegitcommit: 961ec21c22d2f1d55c9cc8a7edf2ade1d1fd92e3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/02/2020
ms.locfileid: "80588178"
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="ea484-102">PLINQ'e Giriş</span><span class="sxs-lookup"><span data-stu-id="ea484-102">Introduction to PLINQ</span></span>

<span data-ttu-id="ea484-103">Paralel LINQ (PLINQ) [Dil-Tümleşik Sorgu (LINQ)](../../csharp/programming-guide/concepts/linq/index.md) deseninin paralel bir uygulamasıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-103">Parallel LINQ (PLINQ) is a parallel implementation of the [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md) pattern.</span></span> <span data-ttu-id="ea484-104">PLINQ, ad alanı için uzantı yöntemleri olarak LINQ standart sorgu işleçlerinin tam kümesini <xref:System.Linq> uygular ve paralel işlemler için ek işleçlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ea484-104">PLINQ implements the full set of LINQ standard query operators as extension methods for the <xref:System.Linq> namespace and has additional operators for parallel operations.</span></span> <span data-ttu-id="ea484-105">PLINQ, LINQ sözdiziminin basitliğini ve okunabilirliğini paralel programlamanın gücüyle birleştirir.</span><span class="sxs-lookup"><span data-stu-id="ea484-105">PLINQ combines the simplicity and readability of LINQ syntax with the power of parallel programming.</span></span>

> [!TIP]
> <span data-ttu-id="ea484-106">LINQ'ya aşina değilseniz, herhangi bir tanımlanabilir veri kaynağını tür güvenli bir şekilde sorgulamak için birleşik bir model sunar.</span><span class="sxs-lookup"><span data-stu-id="ea484-106">If you're not familiar with LINQ, it features a unified model for querying any enumerable data source in a type-safe manner.</span></span> <span data-ttu-id="ea484-107">LinQ to Objects, bellek içi koleksiyonlar ve diziler gibi <xref:System.Collections.Generic.List%601> karşı çalıştırılabilen LINQ sorgularının adıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-107">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="ea484-108">Bu makalede, LINQ temel bir anlayışa sahip olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="ea484-108">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="ea484-109">Daha fazla bilgi için [bkz.](../../csharp/programming-guide/concepts/linq/index.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-109">For more information, see [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md).</span></span>

## <a name="what-is-a-parallel-query"></a><span data-ttu-id="ea484-110">Paralel sorgu nedir?</span><span class="sxs-lookup"><span data-stu-id="ea484-110">What is a Parallel query?</span></span>

<span data-ttu-id="ea484-111">Birçok yönden bir PLINQ sorgusu, Nesneler sorgusuna paralel olmayan bir LINQ sorgusuna benzer.</span><span class="sxs-lookup"><span data-stu-id="ea484-111">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="ea484-112">PLINQ sorguları, sıralı LINQ sorguları gibi, <xref:System.Collections.IEnumerable> herhangi <xref:System.Collections.Generic.IEnumerable%601> bir bellek veya veri kaynağında çalışır ve yürütmeyi erteler, bu da sorgu numaralandırılAna kadar yürütmeye başlamadıkları anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="ea484-112">PLINQ queries, just like sequential LINQ queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="ea484-113">Birincil fark, PLINQ sistemdeki tüm işlemcileri tam olarak kullanmaya çalışır.</span><span class="sxs-lookup"><span data-stu-id="ea484-113">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="ea484-114">Bunu, veri kaynağını bölümlere ayırarak ve sonra her segmentteki sorguyu birden çok işlemciye paralel olarak ayrı bir alt iş parçacığı üzerinde gerçekleştirerek yapar.</span><span class="sxs-lookup"><span data-stu-id="ea484-114">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="ea484-115">Çoğu durumda, paralel yürütme sorgunun önemli ölçüde daha hızlı çalıştığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="ea484-115">In many cases, parallel execution means that the query runs significantly faster.</span></span>

<span data-ttu-id="ea484-116">Paralel yürütme sayesinde PLINQ, genellikle yalnızca sorgu işlemini <xref:System.Linq.ParallelEnumerable.AsParallel%2A> veri kaynağına ekleyerek, belirli sorgu türleri için eski kod üzerinde önemli performans iyileştirmeleri elde edebilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-116">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="ea484-117">Ancak, paralellik kendi karmaşıklıklarını tanıtabilir ve tüm sorgu işlemleri PLINQ'da daha hızlı çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="ea484-117">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="ea484-118">Aslında, paralelleştirme aslında bazı sorguları yavaşlatıyor.</span><span class="sxs-lookup"><span data-stu-id="ea484-118">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="ea484-119">Bu nedenle, sipariş verme gibi sorunların paralel sorguları nasıl etkilediğini anlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="ea484-119">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="ea484-120">Daha fazla bilgi için [PLINQ'da Çabuk'u Anlama'ya](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-120">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>

> [!NOTE]
> <span data-ttu-id="ea484-121">Bu dokümantasyon PLINQ'daki delegeleri tanımlamak için lambda ifadelerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="ea484-121">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="ea484-122">C# veya Visual Basic'teki lambda ifadelerine aşina değilseniz, [PLINQ ve TPL'deki Lambda İfadeleri'ne](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-122">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span></span>

<span data-ttu-id="ea484-123">Bu makalenin geri kalanı ana PLINQ sınıfları genel bir bakış verir ve PLINQ sorguları oluşturmak için nasıl tartışır.</span><span class="sxs-lookup"><span data-stu-id="ea484-123">The remainder of this article gives an overview of the main PLINQ classes and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="ea484-124">Her bölüm daha ayrıntılı bilgi ve kod örnekleri bağlantılar içerir.</span><span class="sxs-lookup"><span data-stu-id="ea484-124">Each section contains links to more detailed information and code examples.</span></span>

## <a name="the-parallelenumerable-class"></a><span data-ttu-id="ea484-125">ParallelEnumerable Sınıfı</span><span class="sxs-lookup"><span data-stu-id="ea484-125">The ParallelEnumerable Class</span></span>

<span data-ttu-id="ea484-126">Sınıf <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> PLINQ işlevselliğinin hemen hemen tüm ortaya çıkarır.</span><span class="sxs-lookup"><span data-stu-id="ea484-126">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span> <span data-ttu-id="ea484-127">Bu ve <xref:System.Linq?displayProperty=nameWithType> diğer ad alanı türleri System.Core.dll derlemesine derlenir.</span><span class="sxs-lookup"><span data-stu-id="ea484-127">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="ea484-128">Visual Studio'daki varsayılan C# ve Visual Basic projeleri hem derlemeye başvurur hem de ad alanını alır.</span><span class="sxs-lookup"><span data-stu-id="ea484-128">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>

<span data-ttu-id="ea484-129"><xref:System.Linq.ParallelEnumerable>LINQ to Objects'in desteklediği tüm standart sorgu işleçlerinin uygulamalarını içerir, ancak her birini paralelleştirmeye çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="ea484-129"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="ea484-130">LINQ'ya aşina değilseniz, [LINQ'ya Giriş (C#)](../../csharp/programming-guide/concepts/linq/index.md) ve [LINQ'a Giriş (Visual Basic) (Visual Basic) 'ye](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-130">If you are not familiar with LINQ, see [Introduction to LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) and [Introduction to LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span></span>

<span data-ttu-id="ea484-131"><xref:System.Linq.ParallelEnumerable> Standart sorgu işleçlerine ek olarak, sınıf paralel yürütmeye özgü davranışları etkinleştiren bir yöntem kümesi içerir.</span><span class="sxs-lookup"><span data-stu-id="ea484-131">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="ea484-132">Bu PLINQ özel yöntemler aşağıdaki tabloda listelenmiştir.</span><span class="sxs-lookup"><span data-stu-id="ea484-132">These PLINQ-specific methods are listed in the following table.</span></span>

|<span data-ttu-id="ea484-133">ParallelUmerable Operatörü</span><span class="sxs-lookup"><span data-stu-id="ea484-133">ParallelEnumerable Operator</span></span>|<span data-ttu-id="ea484-134">Açıklama</span><span class="sxs-lookup"><span data-stu-id="ea484-134">Description</span></span>|
|---------------------------------|-----------------|
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="ea484-135">PLINQ için giriş noktası.</span><span class="sxs-lookup"><span data-stu-id="ea484-135">The entry point for PLINQ.</span></span> <span data-ttu-id="ea484-136">Mümkünse, sorgunun geri kalanının paralelleştirilmesi gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-136">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="ea484-137">Sorgunun geri kalanının paralel olmayan bir LINQ sorgusu olarak sırayla çalıştırılması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-137">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="ea484-138">PLINQ'un, örneğin bir orderby (Visual Basic'te Sırayla) yan tümcesi kullanarak, örneğin sipariş değiştirilene kadar veya sorgunun geri kalanı için kaynak sıranın sırasını koruması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-138">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Visual Basic) clause.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="ea484-139">Sorgunun geri kalanı için PLINQ kaynak sırasını korumak için gerekli olmadığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-139">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="ea484-140">PLINQ'nin, verilen iptal jetonunun durumunu periyodik olarak izlemesi ve talep edilmesi halinde yürütmeyi iptal etmesi gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-140">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="ea484-141">PLINQ'un sorguyu paralelleştirmek için kullanması gereken maksimum işlemci sayısını belirtir.</span><span class="sxs-lookup"><span data-stu-id="ea484-141">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="ea484-142">PLINQ'un, mümkünse paralel sonuçları tüketen iş parçacığıüzerinde tek bir sırayla nasıl birleştirmesi gerektiği hakkında bir ipucu sağlar.</span><span class="sxs-lookup"><span data-stu-id="ea484-142">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="ea484-143">PlinQ'un sorguyu paralelleştirip paralelleştirmesi gerektiğini belirtir, varsayılan davranış sırayla çalıştırmak için olsa bile.</span><span class="sxs-lookup"><span data-stu-id="ea484-143">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="ea484-144">Sorgunun sonuçları üzerinde yineleme aksine, ilk tüketici iş parçacığı geri birleştirme den sonuçları paralel olarak işlenmesini sağlayan çok iş parçacığı numaralandırma yöntemi.</span><span class="sxs-lookup"><span data-stu-id="ea484-144">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|
|<span data-ttu-id="ea484-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A>Aşırı</span><span class="sxs-lookup"><span data-stu-id="ea484-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="ea484-146">PLINQ'ye özgü olan ve iş parçacığı yerel bölümleri üzerinde ara toplama yı sağlayan aşırı yük ve tüm bölümlerin sonuçlarını birleştirmek için son bir toplama işlevi.</span><span class="sxs-lookup"><span data-stu-id="ea484-146">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|

## <a name="the-opt-in-model"></a><span data-ttu-id="ea484-147">Abone Olma Modeli</span><span class="sxs-lookup"><span data-stu-id="ea484-147">The Opt-in Model</span></span>

<span data-ttu-id="ea484-148">Bir sorgu yazarken, aşağıdaki örnekte gösterildiği gibi, <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> veri kaynağındaki uzantı yöntemini çağırarak PLINQ'yi tercih edin.</span><span class="sxs-lookup"><span data-stu-id="ea484-148">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>

[!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
[!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]

<span data-ttu-id="ea484-149">Uzantı <xref:System.Linq.ParallelEnumerable.AsParallel%2A> yöntemi, bu durumda sonraki sorgu işleçlerini `where` ve <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> `select`uygulamaları bağlar.</span><span class="sxs-lookup"><span data-stu-id="ea484-149">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>

## <a name="execution-modes"></a><span data-ttu-id="ea484-150">Yürütme Modları</span><span class="sxs-lookup"><span data-stu-id="ea484-150">Execution Modes</span></span>

<span data-ttu-id="ea484-151">Varsayılan olarak, PLINQ muhafazakar.</span><span class="sxs-lookup"><span data-stu-id="ea484-151">By default, PLINQ is conservative.</span></span> <span data-ttu-id="ea484-152">Çalışma zamanında, PLINQ altyapısı sorgunun genel yapısını analiz eder.</span><span class="sxs-lookup"><span data-stu-id="ea484-152">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="ea484-153">Sorgu paralelleştirme ile hız lanma olasılığı yüksekse, PLINQ kaynak sırasını aynı anda çalıştırılabilen görevlere bölümlere ayırır.</span><span class="sxs-lookup"><span data-stu-id="ea484-153">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="ea484-154">Bir sorguyu paralelleştirmek güvenli değilse, PLINQ sorguyu sırayla çalıştırZ.</span><span class="sxs-lookup"><span data-stu-id="ea484-154">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="ea484-155">PLINQ potansiyel olarak pahalı bir paralel algoritma veya ucuz sıralı algoritma arasında bir seçim varsa, varsayılan olarak sıralı algoritmaseçer.</span><span class="sxs-lookup"><span data-stu-id="ea484-155">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="ea484-156">PlinQ'a paralel algoritmayı <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> seçmetalimatı vermek için yöntemi ve <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> numaralandırmayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-156">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="ea484-157">Bu, belirli bir sorgunun paralel olarak daha hızlı yürütüldettiğini sınayıp ölçerek bildiğinizde kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-157">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="ea484-158">Daha fazla bilgi için [bkz: PLINQ'da Yürütme Modunu belirtin.](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-158">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>

## <a name="degree-of-parallelism"></a><span data-ttu-id="ea484-159">Paralellik Derecesi</span><span class="sxs-lookup"><span data-stu-id="ea484-159">Degree of Parallelism</span></span>

<span data-ttu-id="ea484-160">Varsayılan olarak PLINQ, ana bilgisayardaki tüm işlemcileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ea484-160">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="ea484-161">PLINQ'a <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> yöntemi kullanarak belirli sayıdaişlemciden daha fazla kullanmamasını talimatı verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-161">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="ea484-162">Bu, bilgisayarda çalışan diğer işlemlerin belirli bir cpu süresi aldığından emin olmak istediğinizde yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-162">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="ea484-163">Aşağıdaki parçacık sorguyu en fazla iki işlemci kullanmakla sınırlar.</span><span class="sxs-lookup"><span data-stu-id="ea484-163">The following snippet limits the query to utilizing a maximum of two processors.</span></span>

[!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
[!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]

<span data-ttu-id="ea484-164">Bir sorgunun Dosya G/Ç gibi önemli miktarda bilgi işlem le yükümlü olmayan bir çalışma gerçekleştirdiği durumlarda, makinedeki çekirdek sayısından daha büyük bir paralellik derecesi belirtmek yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-164">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>

## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="ea484-165">Sıralı ve Sırasız Paralel Sorgular</span><span class="sxs-lookup"><span data-stu-id="ea484-165">Ordered Versus Unordered Parallel Queries</span></span>

<span data-ttu-id="ea484-166">Bazı sorgularda, bir sorgu işleci kaynak sıranın sırasını koruyan sonuçlar üretmelidir.</span><span class="sxs-lookup"><span data-stu-id="ea484-166">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="ea484-167">PLINQ <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> bu amaç için operatör sağlar.</span><span class="sxs-lookup"><span data-stu-id="ea484-167">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="ea484-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A>dan <xref:System.Linq.ParallelEnumerable.AsSequential%2A>farklıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="ea484-169">Bir <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sıra hala paralel olarak işlenir, ancak sonuçları arabelleğe alınır ve sıralanır.</span><span class="sxs-lookup"><span data-stu-id="ea484-169">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="ea484-170">Sipariş koruma genellikle ek çalışma <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> içerdiğinden, bir dizi varsayılan <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> diziden daha yavaş işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-170">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="ea484-171">Belirli bir sıralı paralel işlemin, işlemin sıralı bir sürümünden daha hızlı olup olmadığı birçok etkene bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="ea484-171">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>

<span data-ttu-id="ea484-172">Aşağıdaki kod örneği, koruma sırasına nasıl seçilen gösterir.</span><span class="sxs-lookup"><span data-stu-id="ea484-172">The following code example shows how to opt in to order preservation.</span></span>

[!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
[!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]

<span data-ttu-id="ea484-173">Daha fazla bilgi için [PLINQ'da Sipariş Koruma'ya](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-173">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>

## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="ea484-174">Paralel ve Sıralı Sorgular</span><span class="sxs-lookup"><span data-stu-id="ea484-174">Parallel vs. Sequential Queries</span></span>

<span data-ttu-id="ea484-175">Bazı işlemler, kaynak verilerin sıralı bir şekilde teslim edilmesini gerektirir.</span><span class="sxs-lookup"><span data-stu-id="ea484-175">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="ea484-176">Sorgu <xref:System.Linq.ParallelEnumerable> işleçleri gerektiğinde otomatik olarak sıralı moda geri döner.</span><span class="sxs-lookup"><span data-stu-id="ea484-176">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="ea484-177">Kullanıcı tanımlı sorgu işleçleri ve sıralı yürütme gerektiren <xref:System.Linq.ParallelEnumerable.AsSequential%2A> kullanıcı temsilcileri için PLINQ yöntemi sağlar.</span><span class="sxs-lookup"><span data-stu-id="ea484-177">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="ea484-178">Kullandığınızda, <xref:System.Linq.ParallelEnumerable.AsSequential%2A>sorgudaki sonraki tüm işleçler <xref:System.Linq.ParallelEnumerable.AsParallel%2A> yeniden çağrılana kadar sırayla yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ea484-178">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="ea484-179">Daha fazla bilgi için [bkz: Paralel ve Sıralı LINQ Sorgularını Birleştir.](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-179">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>

## <a name="options-for-merging-query-results"></a><span data-ttu-id="ea484-180">Sorgu Sonuçlarını Birleştirme Seçenekleri</span><span class="sxs-lookup"><span data-stu-id="ea484-180">Options for Merging Query Results</span></span>

<span data-ttu-id="ea484-181">Bir PLINQ sorgusu paralel olarak yürütüldüğünde, her alt iş parçacığının sonuçları bir `foreach` döngü (Visual`For Each` Basic'te) veya bir liste veya diziye eklemek tarafından tüketim için ana iş parçacığına geri birleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="ea484-181">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in Visual Basic), or insertion into a list or array.</span></span> <span data-ttu-id="ea484-182">Bazı durumlarda, sonuçları daha hızlı üretmeye başlamak için belirli bir tür birleştirme işlemi belirtmek yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-182">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="ea484-183">Bu amaçla PLINQ <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> yöntemi ve numaralandırmayı <xref:System.Linq.ParallelMergeOptions> destekler.</span><span class="sxs-lookup"><span data-stu-id="ea484-183">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="ea484-184">Daha fazla bilgi için [PLINQ'daki Seçenekleri Birleştir'e](../../../docs/standard/parallel-programming/merge-options-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-184">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>

## <a name="the-forall-operator"></a><span data-ttu-id="ea484-185">ForAll İşleci</span><span class="sxs-lookup"><span data-stu-id="ea484-185">The ForAll Operator</span></span>

<span data-ttu-id="ea484-186">Ardışık LINQ sorgularında, yürütme sorgu `foreach` (Visual`For Each` Basic' de) veya <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> veya <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span><span class="sxs-lookup"><span data-stu-id="ea484-186">In sequential LINQ queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in Visual Basic) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="ea484-187">PLINQ'de, sorguyu `foreach` yürütmek ve sonuçları doğrulamak için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-187">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="ea484-188">Ancak, `foreach` kendisi paralel olarak çalışmaz ve bu nedenle, tüm paralel görevlerden çıktı döngü çalışan iş parçacığı geri birleştirilmelidir gerektirir.</span><span class="sxs-lookup"><span data-stu-id="ea484-188">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="ea484-189">PLINQ'de, sorgu `foreach` sonuçlarının son sırasını korumanız gerektiğinde ve sonuçları seri bir şekilde işlediğinizde, örneğin her `Console.WriteLine` öğe için arama yaparken kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-189">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="ea484-190">Sipariş koruma gerekli olmadığında ve sonuçların işlenmesi paralelleştirilebildiği nde daha hızlı <xref:System.Linq.ParallelEnumerable.ForAll%2A> sorgu yürütmesi için, plinq sorgusu yürütmek için yöntemi kullanın.</span><span class="sxs-lookup"><span data-stu-id="ea484-190">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="ea484-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A>bu son birleştirme adımLarını gerçekleştirmez.</span><span class="sxs-lookup"><span data-stu-id="ea484-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="ea484-192">Aşağıdaki kod örneği yöntemin <xref:System.Linq.ParallelEnumerable.ForAll%2A> nasıl kullanılacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="ea484-192">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="ea484-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>herhangi bir öğeyi kaldırmaya çalışmadan aynı anda ekleyerek birden çok iş parçacığı için optimize edilmiş olduğundan burada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ea484-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>

[!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
[!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]

<span data-ttu-id="ea484-194">Aşağıdaki resimde sorgu yürütme `foreach` <xref:System.Linq.ParallelEnumerable.ForAll%2A> ile ilgili ve arasındaki farkı gösterir.</span><span class="sxs-lookup"><span data-stu-id="ea484-194">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>

<span data-ttu-id="ea484-195">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="ea484-195">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>

## <a name="cancellation"></a><span data-ttu-id="ea484-196">İptal</span><span class="sxs-lookup"><span data-stu-id="ea484-196">Cancellation</span></span>

<span data-ttu-id="ea484-197">PLINQ,.NET Framework 4'teki iptal türleri ile entegre edilmiştir.</span><span class="sxs-lookup"><span data-stu-id="ea484-197">PLINQ is integrated with the cancellation types in .NET Framework 4.</span></span> <span data-ttu-id="ea484-198">(Daha fazla bilgi için yönetilen [iş parçacıklarında İptal](../../../docs/standard/threading/cancellation-in-managed-threads.md)konusuna bakın.) Bu nedenle, nesnelere sıralı LINQ'dan farklı olarak PLINQ sorguları iptal edilebilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-198">(For more information, see [Cancellation in Managed Threads](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="ea484-199">İptal edilebilir bir PLINQ sorgusu <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> oluşturmak için, sorguda <xref:System.Threading.CancellationToken> işleci kullanın ve bağımsız değişken olarak bir örnek sağlayın.</span><span class="sxs-lookup"><span data-stu-id="ea484-199">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="ea484-200">Belirteç üzerindeki <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> özellik doğru ayarlandığında, PLINQ bunu fark edecek, tüm iş <xref:System.OperationCanceledException>parçacıkları üzerinde işlemeyi durduracak ve bir .</span><span class="sxs-lookup"><span data-stu-id="ea484-200">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="ea484-201">İptal belirteci ayarlandıktan sonra bir PLINQ sorgusunun bazı öğeleri işlemeye devam etmesi mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ea484-201">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>

<span data-ttu-id="ea484-202">Daha fazla yanıt verme için, uzun süredir devam eden kullanıcı temsilcilerindeki iptal isteklerine de yanıt verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-202">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="ea484-203">Daha fazla bilgi için [bkz: PLINQ Sorgusuiptal edin.](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-203">For more information, see [How to: Cancel a PLINQ Query](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span></span>

## <a name="exceptions"></a><span data-ttu-id="ea484-204">Özel Durumlar</span><span class="sxs-lookup"><span data-stu-id="ea484-204">Exceptions</span></span>

<span data-ttu-id="ea484-205">Bir PLINQ sorgusu yürütüldüğünde, aynı anda farklı iş parçacığından birden çok özel durum atılabilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-205">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="ea484-206">Ayrıca, özel durumu işlemek için kod özel durum attı kod farklı bir iş parçacığı üzerinde olabilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-206">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="ea484-207"><xref:System.AggregateException> PLINQ, sorgu tarafından atılan tüm özel durumları kapsüllemek ve bu özel durumları arama iş parçacığına geri döndürmek için türü kullanır.</span><span class="sxs-lookup"><span data-stu-id="ea484-207">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="ea484-208">Arama iş parçacığında yalnızca bir try-catch bloğu gereklidir.</span><span class="sxs-lookup"><span data-stu-id="ea484-208">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="ea484-209">Ancak, kapsüllenmiş tüm özel durumlar aracılığıyla yineleyebilir <xref:System.AggregateException> ve güvenli bir şekilde kurtarabilirsiniz herhangi bir yakalamak.</span><span class="sxs-lookup"><span data-stu-id="ea484-209">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="ea484-210">Nadir durumlarda, bazı özel durumlar bir <xref:System.AggregateException>sarılmış değil atılabilir <xref:System.Threading.ThreadAbortException>ve s de sarılmış değildir.</span><span class="sxs-lookup"><span data-stu-id="ea484-210">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>

<span data-ttu-id="ea484-211">Özel durumların birleştirme iş parçacığına geri dönmesine izin verildiğinde, özel durum yükseltildikten sonra bir sorgunun bazı öğeleri işlemeye devam etmesi mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ea484-211">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>

<span data-ttu-id="ea484-212">Daha fazla bilgi için [bkz: PLINQ Sorgusunda Özel Durumları Nasıl İşleyin.](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-212">For more information, see [How to: Handle Exceptions in a PLINQ Query](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span></span>

## <a name="custom-partitioners"></a><span data-ttu-id="ea484-213">Özel Bölümleyiciler</span><span class="sxs-lookup"><span data-stu-id="ea484-213">Custom Partitioners</span></span>

<span data-ttu-id="ea484-214">Bazı durumlarda, kaynak verilerin bazı özelliklerinden yararlanan özel bir bölümleyici yazarak sorgu performansını artırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-214">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="ea484-215">Sorguda, özel bölümleyicinin kendisi sorgulanacak sayısal nesnedir.</span><span class="sxs-lookup"><span data-stu-id="ea484-215">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>

[!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
[!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]

<span data-ttu-id="ea484-216">PLINQ sabit sayıda bölümü destekler (ancak yük dengeleme için çalışma süresi boyunca veriler dinamik olarak bu bölümlere yeniden atanabilir.)</span><span class="sxs-lookup"><span data-stu-id="ea484-216">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="ea484-217"><xref:System.Threading.Tasks.Parallel.For%2A>ve <xref:System.Threading.Tasks.Parallel.ForEach%2A> yalnızca dinamik bölümleme desteği, bu da bölüm sayısının çalışma zamanında değiştiği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="ea484-217"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="ea484-218">Daha fazla bilgi [için PLINQ ve TPL için Özel Bölümleyiciler'e](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="ea484-218">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>

## <a name="measuring-plinq-performance"></a><span data-ttu-id="ea484-219">PLINQ Performansını Ölçme</span><span class="sxs-lookup"><span data-stu-id="ea484-219">Measuring PLINQ Performance</span></span>

<span data-ttu-id="ea484-220">Çoğu durumda, bir sorgu paralelolabilir, ancak paralel sorgu ayarlama yükü kazanılan performans avantajından daha ağır basar.</span><span class="sxs-lookup"><span data-stu-id="ea484-220">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="ea484-221">Bir sorgu çok hesaplama gerçekleştirmezse veya veri kaynağı küçükse, PLINQ sorgusu Nesneler için sıralı BIR LINQ sorgusundan daha yavaş olabilir.</span><span class="sxs-lookup"><span data-stu-id="ea484-221">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="ea484-222">Visual Studio Team Server'daki Paralel Performans Çözümleyicisini çeşitli sorguların performansını karşılaştırmak, işlem darboğazlarını bulmak ve sorgunuzun paralel mi sırayla mı çalıştığını belirlemek için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ea484-222">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="ea484-223">Daha fazla bilgi için [Eşzamanlılık Görselleştiricisi](/visualstudio/profiling/concurrency-visualizer) ve [Nasıl Yapılacağını görün: PLINQ Sorgu Performansını Ölçün.](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md)</span><span class="sxs-lookup"><span data-stu-id="ea484-223">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="ea484-224">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="ea484-224">See also</span></span>

- [<span data-ttu-id="ea484-225">Paralel LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="ea484-225">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/introduction-to-plinq.md)
- [<span data-ttu-id="ea484-226">PLINQ'te Hızlandırmayı Anlama</span><span class="sxs-lookup"><span data-stu-id="ea484-226">Understanding Speedup in PLINQ</span></span>](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)
