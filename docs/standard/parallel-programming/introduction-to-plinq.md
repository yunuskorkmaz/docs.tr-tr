---
title: PLINQ'e Giriş
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
ms.openlocfilehash: ed1b2df57c118a0ebb6b5ffa4326b3e2eac81dec
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2020
ms.locfileid: "75632369"
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="1e8be-102">PLINQ'e Giriş</span><span class="sxs-lookup"><span data-stu-id="1e8be-102">Introduction to PLINQ</span></span>

## <a name="what-is-a-parallel-query"></a><span data-ttu-id="1e8be-103">Paralel Sorgu nedir?</span><span class="sxs-lookup"><span data-stu-id="1e8be-103">What is a Parallel Query?</span></span>

<span data-ttu-id="1e8be-104">Dil-Tümleşik Sorgu (LINQ) .NET Framework 3.5'te tanıtıldı.</span><span class="sxs-lookup"><span data-stu-id="1e8be-104">Language-Integrated Query (LINQ) was introduced in the .NET Framework 3.5.</span></span> <span data-ttu-id="1e8be-105">Herhangi <xref:System.Collections.IEnumerable?displayProperty=nameWithType> bir veri <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> kaynağını tür güvenli bir şekilde sorgulamak için birleşik bir model sunar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-105">It features a unified model for querying any <xref:System.Collections.IEnumerable?displayProperty=nameWithType> or <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> data source in a type-safe manner.</span></span> <span data-ttu-id="1e8be-106">LinQ to Objects, bellek içi koleksiyonlar ve diziler gibi <xref:System.Collections.Generic.List%601> karşı çalıştırılabilen LINQ sorgularının adıdır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-106">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="1e8be-107">Bu makalede, LINQ temel bir anlayışa sahip olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-107">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="1e8be-108">Daha fazla bilgi için bkz: [Dil-Tümleşik Sorgu (LINQ) - C#](../../csharp/programming-guide/concepts/linq/index.md) veya [Dil-Tümleşik Sorgu (LINQ) - Visual Basic](../../visual-basic/programming-guide/concepts/linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="1e8be-108">For more information, see [Language-Integrated Query (LINQ) - C#](../../csharp/programming-guide/concepts/linq/index.md) or [Language-Integrated Query (LINQ) - Visual Basic](../../visual-basic/programming-guide/concepts/linq/index.md).</span></span>

<span data-ttu-id="1e8be-109">Paralel LINQ (PLINQ) LINQ deseni paralel bir uygulamadır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-109">Parallel LINQ (PLINQ) is a parallel implementation of the LINQ pattern.</span></span> <span data-ttu-id="1e8be-110">Birçok yönden bir PLINQ sorgusu, Nesneler sorgusuna paralel olmayan bir LINQ sorgusuna benzer.</span><span class="sxs-lookup"><span data-stu-id="1e8be-110">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="1e8be-111">PLINQ sorguları, sıralı LINQ sorguları gibi, <xref:System.Collections.IEnumerable> herhangi <xref:System.Collections.Generic.IEnumerable%601> bir bellek veya veri kaynağında çalışır ve yürütmeyi erteler, bu da sorgu numaralandırılAna kadar yürütmeye başlamadıkları anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-111">PLINQ queries, just like sequential LINQ queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="1e8be-112">Birincil fark, PLINQ sistemdeki tüm işlemcileri tam olarak kullanmaya çalışır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-112">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="1e8be-113">Bunu, veri kaynağını bölümlere ayırarak ve sonra her segmentteki sorguyu birden çok işlemciye paralel olarak ayrı bir alt iş parçacığı üzerinde gerçekleştirerek yapar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-113">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="1e8be-114">Çoğu durumda, paralel yürütme sorgunun önemli ölçüde daha hızlı çalıştığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-114">In many cases, parallel execution means that the query runs significantly faster.</span></span>

<span data-ttu-id="1e8be-115">Paralel yürütme sayesinde PLINQ, genellikle yalnızca sorgu işlemini <xref:System.Linq.ParallelEnumerable.AsParallel%2A> veri kaynağına ekleyerek, belirli sorgu türleri için eski kod üzerinde önemli performans iyileştirmeleri elde edebilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-115">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="1e8be-116">Ancak, paralellik kendi karmaşıklıklarını tanıtabilir ve tüm sorgu işlemleri PLINQ'da daha hızlı çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-116">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="1e8be-117">Aslında, paralelleştirme aslında bazı sorguları yavaşlatıyor.</span><span class="sxs-lookup"><span data-stu-id="1e8be-117">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="1e8be-118">Bu nedenle, sipariş verme gibi sorunların paralel sorguları nasıl etkilediğini anlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-118">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="1e8be-119">Daha fazla bilgi için [PLINQ'da Çabuk'u Anlama'ya](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-119">For more information, see [Understanding Speedup in PLINQ](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md).</span></span>

> [!NOTE]
> <span data-ttu-id="1e8be-120">Bu dokümantasyon PLINQ'daki delegeleri tanımlamak için lambda ifadelerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-120">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="1e8be-121">C# veya Visual Basic'teki lambda ifadelerine aşina değilseniz, [PLINQ ve TPL'deki Lambda İfadeleri'ne](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-121">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](../../../docs/standard/parallel-programming/lambda-expressions-in-plinq-and-tpl.md).</span></span>

<span data-ttu-id="1e8be-122">Bu makalenin geri kalanı ana PLINQ sınıflarına genel bir bakış verir ve PLINQ sorgularının nasıl oluşturulup oluşturulabildiğini tartışır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-122">The remainder of this article gives an overview of the main PLINQ classes, and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="1e8be-123">Her bölüm daha ayrıntılı bilgi ve kod örnekleri bağlantılar içerir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-123">Each section contains links to more detailed information and code examples.</span></span>

## <a name="the-parallelenumerable-class"></a><span data-ttu-id="1e8be-124">ParallelEnumerable Sınıfı</span><span class="sxs-lookup"><span data-stu-id="1e8be-124">The ParallelEnumerable Class</span></span>

<span data-ttu-id="1e8be-125">Sınıf <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> PLINQ işlevselliğinin hemen hemen tüm ortaya çıkarır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-125">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span> <span data-ttu-id="1e8be-126">Bu ve <xref:System.Linq?displayProperty=nameWithType> diğer ad alanı türleri System.Core.dll derlemesine derlenir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-126">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="1e8be-127">Visual Studio'daki varsayılan C# ve Visual Basic projeleri hem derlemeye başvurur hem de ad alanını alır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-127">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>

<span data-ttu-id="1e8be-128"><xref:System.Linq.ParallelEnumerable>LINQ to Objects'in desteklediği tüm standart sorgu işleçlerinin uygulamalarını içerir, ancak her birini paralelleştirmeye çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-128"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="1e8be-129">LINQ'ya aşina değilseniz, [LINQ'ya Giriş (C#)](../../csharp/programming-guide/concepts/linq/index.md) ve [LINQ'a Giriş (Visual Basic) (Visual Basic) 'ye](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-129">If you are not familiar with LINQ, see [Introduction to LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) and [Introduction to LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span></span>

<span data-ttu-id="1e8be-130"><xref:System.Linq.ParallelEnumerable> Standart sorgu işleçlerine ek olarak, sınıf paralel yürütmeye özgü davranışları etkinleştiren bir yöntem kümesi içerir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-130">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="1e8be-131">Bu PLINQ özel yöntemler aşağıdaki tabloda listelenmiştir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-131">These PLINQ-specific methods are listed in the following table.</span></span>

|<span data-ttu-id="1e8be-132">ParallelUmerable Operatörü</span><span class="sxs-lookup"><span data-stu-id="1e8be-132">ParallelEnumerable Operator</span></span>|<span data-ttu-id="1e8be-133">Açıklama</span><span class="sxs-lookup"><span data-stu-id="1e8be-133">Description</span></span>|
|---------------------------------|-----------------|
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="1e8be-134">PLINQ için giriş noktası.</span><span class="sxs-lookup"><span data-stu-id="1e8be-134">The entry point for PLINQ.</span></span> <span data-ttu-id="1e8be-135">Mümkünse, sorgunun geri kalanının paralelleştirilmesi gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-135">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="1e8be-136">Sorgunun geri kalanının paralel olmayan bir LINQ sorgusu olarak sırayla çalıştırılması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-136">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="1e8be-137">PLINQ'un, örneğin bir orderby (Visual Basic'te Sırayla) yan tümcesi kullanarak, örneğin sipariş değiştirilene kadar veya sorgunun geri kalanı için kaynak sıranın sırasını koruması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-137">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Visual Basic) clause.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="1e8be-138">Sorgunun geri kalanı için PLINQ kaynak sırasını korumak için gerekli olmadığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-138">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="1e8be-139">PLINQ'nin, verilen iptal jetonunun durumunu periyodik olarak izlemesi ve talep edilmesi halinde yürütmeyi iptal etmesi gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-139">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="1e8be-140">PLINQ'un sorguyu paralelleştirmek için kullanması gereken maksimum işlemci sayısını belirtir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-140">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="1e8be-141">PLINQ'un, mümkünse paralel sonuçları tüketen iş parçacığıüzerinde tek bir sırayla nasıl birleştirmesi gerektiği hakkında bir ipucu sağlar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-141">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="1e8be-142">PlinQ'un sorguyu paralelleştirip paralelleştirmesi gerektiğini belirtir, varsayılan davranış sırayla çalıştırmak için olsa bile.</span><span class="sxs-lookup"><span data-stu-id="1e8be-142">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="1e8be-143">Sorgunun sonuçları üzerinde yineleme aksine, ilk tüketici iş parçacığı geri birleştirme den sonuçları paralel olarak işlenmesini sağlayan çok iş parçacığı numaralandırma yöntemi.</span><span class="sxs-lookup"><span data-stu-id="1e8be-143">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|
|<span data-ttu-id="1e8be-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A>Aşırı</span><span class="sxs-lookup"><span data-stu-id="1e8be-144"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="1e8be-145">PLINQ'ye özgü olan ve iş parçacığı yerel bölümleri üzerinde ara toplama yı sağlayan aşırı yük ve tüm bölümlerin sonuçlarını birleştirmek için son bir toplama işlevi.</span><span class="sxs-lookup"><span data-stu-id="1e8be-145">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|

## <a name="the-opt-in-model"></a><span data-ttu-id="1e8be-146">Abone Olma Modeli</span><span class="sxs-lookup"><span data-stu-id="1e8be-146">The Opt-in Model</span></span>

<span data-ttu-id="1e8be-147">Bir sorgu yazarken, aşağıdaki örnekte gösterildiği gibi, <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> veri kaynağındaki uzantı yöntemini çağırarak PLINQ'yi tercih edin.</span><span class="sxs-lookup"><span data-stu-id="1e8be-147">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>

[!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
[!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]

<span data-ttu-id="1e8be-148">Uzantı <xref:System.Linq.ParallelEnumerable.AsParallel%2A> yöntemi, bu durumda sonraki sorgu işleçlerini `where` ve <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> `select`uygulamaları bağlar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-148">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>

## <a name="execution-modes"></a><span data-ttu-id="1e8be-149">Yürütme Modları</span><span class="sxs-lookup"><span data-stu-id="1e8be-149">Execution Modes</span></span>

<span data-ttu-id="1e8be-150">Varsayılan olarak, PLINQ muhafazakar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-150">By default, PLINQ is conservative.</span></span> <span data-ttu-id="1e8be-151">Çalışma zamanında, PLINQ altyapısı sorgunun genel yapısını analiz eder.</span><span class="sxs-lookup"><span data-stu-id="1e8be-151">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="1e8be-152">Sorgu paralelleştirme ile hız lanma olasılığı yüksekse, PLINQ kaynak sırasını aynı anda çalıştırılabilen görevlere bölümlere ayırır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-152">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="1e8be-153">Bir sorguyu paralelleştirmek güvenli değilse, PLINQ sorguyu sırayla çalıştırZ.</span><span class="sxs-lookup"><span data-stu-id="1e8be-153">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="1e8be-154">PLINQ potansiyel olarak pahalı bir paralel algoritma veya ucuz sıralı algoritma arasında bir seçim varsa, varsayılan olarak sıralı algoritmaseçer.</span><span class="sxs-lookup"><span data-stu-id="1e8be-154">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="1e8be-155">PlinQ'a paralel algoritmayı <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> seçmetalimatı vermek için yöntemi ve <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> numaralandırmayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-155">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="1e8be-156">Bu, belirli bir sorgunun paralel olarak daha hızlı yürütüldettiğini sınayıp ölçerek bildiğinizde kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-156">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="1e8be-157">Daha fazla bilgi için [bkz: PLINQ'da Yürütme Modunu belirtin.](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md)</span><span class="sxs-lookup"><span data-stu-id="1e8be-157">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>

## <a name="degree-of-parallelism"></a><span data-ttu-id="1e8be-158">Paralellik Derecesi</span><span class="sxs-lookup"><span data-stu-id="1e8be-158">Degree of Parallelism</span></span>

<span data-ttu-id="1e8be-159">Varsayılan olarak PLINQ, ana bilgisayardaki tüm işlemcileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-159">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="1e8be-160">PLINQ'a <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> yöntemi kullanarak belirli sayıdaişlemciden daha fazla kullanmamasını talimatı verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-160">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="1e8be-161">Bu, bilgisayarda çalışan diğer işlemlerin belirli bir cpu süresi aldığından emin olmak istediğinizde yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-161">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="1e8be-162">Aşağıdaki parçacık sorguyu en fazla iki işlemci kullanmakla sınırlar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-162">The following snippet limits the query to utilizing a maximum of two processors.</span></span>

[!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
[!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]

<span data-ttu-id="1e8be-163">Bir sorgunun Dosya G/Ç gibi önemli miktarda bilgi işlem le yükümlü olmayan bir çalışma gerçekleştirdiği durumlarda, makinedeki çekirdek sayısından daha büyük bir paralellik derecesi belirtmek yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-163">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>

## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="1e8be-164">Sıralı ve Sırasız Paralel Sorgular</span><span class="sxs-lookup"><span data-stu-id="1e8be-164">Ordered Versus Unordered Parallel Queries</span></span>

<span data-ttu-id="1e8be-165">Bazı sorgularda, bir sorgu işleci kaynak sıranın sırasını koruyan sonuçlar üretmelidir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-165">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="1e8be-166">PLINQ <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> bu amaç için operatör sağlar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-166">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="1e8be-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A>dan <xref:System.Linq.ParallelEnumerable.AsSequential%2A>farklıdır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-167"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="1e8be-168">Bir <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sıra hala paralel olarak işlenir, ancak sonuçları arabelleğe alınır ve sıralanır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-168">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="1e8be-169">Sipariş koruma genellikle ek çalışma <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> içerdiğinden, bir dizi varsayılan <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> diziden daha yavaş işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-169">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="1e8be-170">Belirli bir sıralı paralel işlemin, işlemin sıralı bir sürümünden daha hızlı olup olmadığı birçok etkene bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-170">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>

<span data-ttu-id="1e8be-171">Aşağıdaki kod örneği, koruma sırasına nasıl seçilen gösterir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-171">The following code example shows how to opt in to order preservation.</span></span>

[!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
[!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]

<span data-ttu-id="1e8be-172">Daha fazla bilgi için [PLINQ'da Sipariş Koruma'ya](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-172">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>

## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="1e8be-173">Paralel ve Sıralı Sorgular</span><span class="sxs-lookup"><span data-stu-id="1e8be-173">Parallel vs. Sequential Queries</span></span>

<span data-ttu-id="1e8be-174">Bazı işlemler, kaynak verilerin sıralı bir şekilde teslim edilmesini gerektirir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-174">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="1e8be-175">Sorgu <xref:System.Linq.ParallelEnumerable> işleçleri gerektiğinde otomatik olarak sıralı moda geri döner.</span><span class="sxs-lookup"><span data-stu-id="1e8be-175">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="1e8be-176">Kullanıcı tanımlı sorgu işleçleri ve sıralı yürütme gerektiren <xref:System.Linq.ParallelEnumerable.AsSequential%2A> kullanıcı temsilcileri için PLINQ yöntemi sağlar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-176">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="1e8be-177">Kullandığınızda, <xref:System.Linq.ParallelEnumerable.AsSequential%2A>sorgudaki sonraki tüm işleçler <xref:System.Linq.ParallelEnumerable.AsParallel%2A> yeniden çağrılana kadar sırayla yürütülür.</span><span class="sxs-lookup"><span data-stu-id="1e8be-177">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="1e8be-178">Daha fazla bilgi için [bkz: Paralel ve Sıralı LINQ Sorgularını Birleştir.](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md)</span><span class="sxs-lookup"><span data-stu-id="1e8be-178">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](../../../docs/standard/parallel-programming/how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>

## <a name="options-for-merging-query-results"></a><span data-ttu-id="1e8be-179">Sorgu Sonuçlarını Birleştirme Seçenekleri</span><span class="sxs-lookup"><span data-stu-id="1e8be-179">Options for Merging Query Results</span></span>

<span data-ttu-id="1e8be-180">Bir PLINQ sorgusu paralel olarak yürütüldüğünde, her alt iş parçacığının sonuçları bir `foreach` döngü (Visual`For Each` Basic'te) veya bir liste veya diziye eklemek tarafından tüketim için ana iş parçacığına geri birleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-180">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in Visual Basic), or insertion into a list or array.</span></span> <span data-ttu-id="1e8be-181">Bazı durumlarda, sonuçları daha hızlı üretmeye başlamak için belirli bir tür birleştirme işlemi belirtmek yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-181">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="1e8be-182">Bu amaçla PLINQ <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> yöntemi ve numaralandırmayı <xref:System.Linq.ParallelMergeOptions> destekler.</span><span class="sxs-lookup"><span data-stu-id="1e8be-182">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="1e8be-183">Daha fazla bilgi için [PLINQ'daki Seçenekleri Birleştir'e](../../../docs/standard/parallel-programming/merge-options-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-183">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>

## <a name="the-forall-operator"></a><span data-ttu-id="1e8be-184">ForAll İşleci</span><span class="sxs-lookup"><span data-stu-id="1e8be-184">The ForAll Operator</span></span>

<span data-ttu-id="1e8be-185">Ardışık LINQ sorgularında, yürütme sorgu `foreach` (Visual`For Each` Basic' de) veya <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> veya <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span><span class="sxs-lookup"><span data-stu-id="1e8be-185">In sequential LINQ queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in Visual Basic) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="1e8be-186">PLINQ'de, sorguyu `foreach` yürütmek ve sonuçları doğrulamak için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-186">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="1e8be-187">Ancak, `foreach` kendisi paralel olarak çalışmaz ve bu nedenle, tüm paralel görevlerden çıktı döngü çalışan iş parçacığı geri birleştirilmelidir gerektirir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-187">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="1e8be-188">PLINQ'de, sorgu `foreach` sonuçlarının son sırasını korumanız gerektiğinde ve sonuçları seri bir şekilde işlediğinizde, örneğin her `Console.WriteLine` öğe için arama yaparken kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-188">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="1e8be-189">Sipariş koruma gerekli olmadığında ve sonuçların işlenmesi paralelleştirilebildiği nde daha hızlı <xref:System.Linq.ParallelEnumerable.ForAll%2A> sorgu yürütmesi için, plinq sorgusu yürütmek için yöntemi kullanın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-189">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="1e8be-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A>bu son birleştirme adımLarını gerçekleştirmez.</span><span class="sxs-lookup"><span data-stu-id="1e8be-190"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="1e8be-191">Aşağıdaki kod örneği yöntemin <xref:System.Linq.ParallelEnumerable.ForAll%2A> nasıl kullanılacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-191">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="1e8be-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>herhangi bir öğeyi kaldırmaya çalışmadan aynı anda ekleyerek birden çok iş parçacığı için optimize edilmiş olduğundan burada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-192"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>

[!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
[!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]

<span data-ttu-id="1e8be-193">Aşağıdaki resimde sorgu yürütme `foreach` <xref:System.Linq.ParallelEnumerable.ForAll%2A> ile ilgili ve arasındaki farkı gösterir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-193">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>

<span data-ttu-id="1e8be-194">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="1e8be-194">![ForAll vs. ForEach](../../../docs/standard/parallel-programming/media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>

## <a name="cancellation"></a><span data-ttu-id="1e8be-195">İptal</span><span class="sxs-lookup"><span data-stu-id="1e8be-195">Cancellation</span></span>

<span data-ttu-id="1e8be-196">PLINQ,.NET Framework 4'teki iptal türleri ile entegre edilmiştir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-196">PLINQ is integrated with the cancellation types in .NET Framework 4.</span></span> <span data-ttu-id="1e8be-197">(Daha fazla bilgi için yönetilen [iş parçacıklarında İptal](../../../docs/standard/threading/cancellation-in-managed-threads.md)konusuna bakın.) Bu nedenle, nesnelere sıralı LINQ'dan farklı olarak PLINQ sorguları iptal edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-197">(For more information, see [Cancellation in Managed Threads](../../../docs/standard/threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="1e8be-198">İptal edilebilir bir PLINQ sorgusu <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> oluşturmak için, sorguda <xref:System.Threading.CancellationToken> işleci kullanın ve bağımsız değişken olarak bir örnek sağlayın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-198">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="1e8be-199">Belirteç üzerindeki <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> özellik doğru ayarlandığında, PLINQ bunu fark edecek, tüm iş <xref:System.OperationCanceledException>parçacıkları üzerinde işlemeyi durduracak ve bir .</span><span class="sxs-lookup"><span data-stu-id="1e8be-199">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="1e8be-200">İptal belirteci ayarlandıktan sonra bir PLINQ sorgusunun bazı öğeleri işlemeye devam etmesi mümkündür.</span><span class="sxs-lookup"><span data-stu-id="1e8be-200">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>

<span data-ttu-id="1e8be-201">Daha fazla yanıt verme için, uzun süredir devam eden kullanıcı temsilcilerindeki iptal isteklerine de yanıt verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-201">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="1e8be-202">Daha fazla bilgi için [bkz: PLINQ Sorgusuiptal edin.](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md)</span><span class="sxs-lookup"><span data-stu-id="1e8be-202">For more information, see [How to: Cancel a PLINQ Query](../../../docs/standard/parallel-programming/how-to-cancel-a-plinq-query.md).</span></span>

## <a name="exceptions"></a><span data-ttu-id="1e8be-203">Özel durumlar</span><span class="sxs-lookup"><span data-stu-id="1e8be-203">Exceptions</span></span>

<span data-ttu-id="1e8be-204">Bir PLINQ sorgusu yürütüldüğünde, aynı anda farklı iş parçacığından birden çok özel durum atılabilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-204">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="1e8be-205">Ayrıca, özel durumu işlemek için kod özel durum attı kod farklı bir iş parçacığı üzerinde olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-205">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="1e8be-206"><xref:System.AggregateException> PLINQ, sorgu tarafından atılan tüm özel durumları kapsüllemek ve bu özel durumları arama iş parçacığına geri döndürmek için türü kullanır.</span><span class="sxs-lookup"><span data-stu-id="1e8be-206">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="1e8be-207">Arama iş parçacığında yalnızca bir try-catch bloğu gereklidir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-207">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="1e8be-208">Ancak, kapsüllenmiş tüm özel durumlar aracılığıyla yineleyebilir <xref:System.AggregateException> ve güvenli bir şekilde kurtarabilirsiniz herhangi bir yakalamak.</span><span class="sxs-lookup"><span data-stu-id="1e8be-208">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="1e8be-209">Nadir durumlarda, bazı özel durumlar bir <xref:System.AggregateException>sarılmış değil atılabilir <xref:System.Threading.ThreadAbortException>ve s de sarılmış değildir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-209">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>

<span data-ttu-id="1e8be-210">Özel durumların birleştirme iş parçacığına geri dönmesine izin verildiğinde, özel durum yükseltildikten sonra bir sorgunun bazı öğeleri işlemeye devam etmesi mümkündür.</span><span class="sxs-lookup"><span data-stu-id="1e8be-210">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>

<span data-ttu-id="1e8be-211">Daha fazla bilgi için [bkz: PLINQ Sorgusunda Özel Durumları Nasıl İşleyin.](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md)</span><span class="sxs-lookup"><span data-stu-id="1e8be-211">For more information, see [How to: Handle Exceptions in a PLINQ Query](../../../docs/standard/parallel-programming/how-to-handle-exceptions-in-a-plinq-query.md).</span></span>

## <a name="custom-partitioners"></a><span data-ttu-id="1e8be-212">Özel Bölümleyiciler</span><span class="sxs-lookup"><span data-stu-id="1e8be-212">Custom Partitioners</span></span>

<span data-ttu-id="1e8be-213">Bazı durumlarda, kaynak verilerin bazı özelliklerinden yararlanan özel bir bölümleyici yazarak sorgu performansını artırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-213">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="1e8be-214">Sorguda, özel bölümleyicinin kendisi sorgulanacak sayısal nesnedir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-214">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>

[!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
[!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]

<span data-ttu-id="1e8be-215">PLINQ sabit sayıda bölümü destekler (ancak yük dengeleme için çalışma süresi boyunca veriler dinamik olarak bu bölümlere yeniden atanabilir.)</span><span class="sxs-lookup"><span data-stu-id="1e8be-215">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="1e8be-216"><xref:System.Threading.Tasks.Parallel.For%2A>ve <xref:System.Threading.Tasks.Parallel.ForEach%2A> yalnızca dinamik bölümleme desteği, bu da bölüm sayısının çalışma zamanında değiştiği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-216"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="1e8be-217">Daha fazla bilgi [için PLINQ ve TPL için Özel Bölümleyiciler'e](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1e8be-217">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>

## <a name="measuring-plinq-performance"></a><span data-ttu-id="1e8be-218">PLINQ Performansını Ölçme</span><span class="sxs-lookup"><span data-stu-id="1e8be-218">Measuring PLINQ Performance</span></span>

<span data-ttu-id="1e8be-219">Çoğu durumda, bir sorgu paralelolabilir, ancak paralel sorgu ayarlama yükü kazanılan performans avantajından daha ağır basar.</span><span class="sxs-lookup"><span data-stu-id="1e8be-219">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="1e8be-220">Bir sorgu çok hesaplama gerçekleştirmezse veya veri kaynağı küçükse, PLINQ sorgusu Nesneler için sıralı BIR LINQ sorgusundan daha yavaş olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e8be-220">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="1e8be-221">Visual Studio Team Server'daki Paralel Performans Çözümleyicisini çeşitli sorguların performansını karşılaştırmak, işlem darboğazlarını bulmak ve sorgunuzun paralel mi sırayla mı çalıştığını belirlemek için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-221">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="1e8be-222">Daha fazla bilgi için [Eşzamanlılık Görselleştiricisi](/visualstudio/profiling/concurrency-visualizer) ve [Nasıl Yapılacağını görün: PLINQ Sorgu Performansını Ölçün.](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md)</span><span class="sxs-lookup"><span data-stu-id="1e8be-222">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](../../../docs/standard/parallel-programming/how-to-measure-plinq-query-performance.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="1e8be-223">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="1e8be-223">See also</span></span>

- [<span data-ttu-id="1e8be-224">Paralel LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="1e8be-224">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
- [<span data-ttu-id="1e8be-225">PLINQ'te Hızlandırmayı Anlama</span><span class="sxs-lookup"><span data-stu-id="1e8be-225">Understanding Speedup in PLINQ</span></span>](../../../docs/standard/parallel-programming/understanding-speedup-in-plinq.md)
