---
title: 'Nasıl yapılır: Veri Akışı Bloklarının Bağlantısını Kaldırma'
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- dataflow blocks, unlinking in TPL
- Task Parallel Library, dataflows
- TPL dataflow library, unlinking dataflow blocks
ms.assetid: 40f0208d-4618-47f7-85cf-4913d07d2d7d
ms.openlocfilehash: b49cfc9730ba154202baf15093a54ba3ce0e2a8a
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2020
ms.locfileid: "73139301"
---
# <a name="how-to-unlink-dataflow-blocks"></a><span data-ttu-id="a2cb4-102">Nasıl yapılır: Veri Akışı Bloklarının Bağlantısını Kaldırma</span><span class="sxs-lookup"><span data-stu-id="a2cb4-102">How to: Unlink Dataflow Blocks</span></span>
<span data-ttu-id="a2cb4-103">Bu belge, hedef veri akışı bloğunun kaynağından nasıl çıkarılabildiğini açıklar.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-103">This document describes how to unlink a target dataflow block from its source.</span></span>

[!INCLUDE [tpl-install-instructions](../../../includes/tpl-install-instructions.md)]

## <a name="example"></a><span data-ttu-id="a2cb4-104">Örnek</span><span class="sxs-lookup"><span data-stu-id="a2cb4-104">Example</span></span>  
 <span data-ttu-id="a2cb4-105">Aşağıdaki örnek, her <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> biri bir değeri `TrySolution` hesaplamak için yöntemi çağıran üç nesne oluşturur.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-105">The following example creates three <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objects, each of which calls the `TrySolution` method to compute a value.</span></span> <span data-ttu-id="a2cb4-106">Bu örnek, yalnızca bitirmek için `TrySolution` ilk çağrıdan sonucu gerektirir.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-106">This example requires only the result from the first call to `TrySolution` to finish.</span></span>  
  
 [!code-csharp[TPLDataflow_ReceiveAny#1](../../../samples/snippets/csharp/VS_Snippets_Misc/tpldataflow_receiveany/cs/dataflowreceiveany.cs#1)]
 [!code-vb[TPLDataflow_ReceiveAny#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpldataflow_receiveany/vb/dataflowreceiveany.vb#1)]  
  
 <span data-ttu-id="a2cb4-107">Bu örnek, tamamlayan <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> ilk nesneden değeri almak için `ReceiveFromAny(T)` yöntemi tanımlar.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-107">To receive the value from the first <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> object that finishes, this example defines the `ReceiveFromAny(T)` method.</span></span> <span data-ttu-id="a2cb4-108">Yöntem `ReceiveFromAny(T)` bir <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> dizi nesne kabul eder ve bu nesnelerin <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> her birini bir nesneye bağlar.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-108">The `ReceiveFromAny(T)` method accepts an array of <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> objects and links each of these objects to a <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="a2cb4-109">Kaynak veri <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> akışı bloğunu hedef bloğa bağlamak için metodu kullandığında, kaynak veri kullanılabilir hale geldiğinde iletileri hedefe yayır.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-109">When you use the <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method to link a source dataflow block to a target block, the source propagates messages to the target as data becomes available.</span></span> <span data-ttu-id="a2cb4-110"><xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> Sınıf yalnızca sunulduğu ilk iletiyi kabul ettiği `ReceiveFromAny(T)` için, <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> yöntem yöntemi çağırarak sonucunu üretir.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-110">Because the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> class accepts only the first message that it is offered, the `ReceiveFromAny(T)` method produces its result by calling the <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> method.</span></span> <span data-ttu-id="a2cb4-111">Bu, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesneye sunulan ilk iletiyi üretir.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-111">This produces the first message that is offered to the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="a2cb4-112">Yöntem, <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> hedef <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> `1`kaynaktan bir ileti aldıktan sonra kaynak bloğunun hedeften bağlantısını kaldırmasını söyleyen bir nesneyi bir özelliğe sahip alan aşırı yüklü bir sürüme sahiptir.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-112">The <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method has an overloaded version that takes an <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> object with a <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> property that, when it is set to `1`, instructs the source block to unlink from the target after the target receives one message from the source.</span></span> <span data-ttu-id="a2cb4-113">Nesne bir ileti aldıktan sonra kaynak ve <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesne dizisi arasındaki ilişki artık gerekli olmadığından, nesnenin kaynaklarından bağlantısını kaldırması önemlidir. <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601></span><span class="sxs-lookup"><span data-stu-id="a2cb4-113">It is important for the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object to unlink from its sources because the relationship between the array of sources and the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object is no longer required after the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object receives a message.</span></span>  
  
 <span data-ttu-id="a2cb4-114">Kalan `TrySolution` çağrıların bir değeri hesapladıktan sonra sona ermesini `TrySolution` sağlamak <xref:System.Threading.CancellationToken> için, yöntem geri döndürme `ReceiveFromAny(T)` çağrısından sonra iptal edilen bir nesne alır.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-114">To enable the remaining calls to `TrySolution` to end after one of them computes a value, the `TrySolution` method takes a <xref:System.Threading.CancellationToken> object that is canceled after the call to `ReceiveFromAny(T)` returns.</span></span> <span data-ttu-id="a2cb4-115">Bu <xref:System.Threading.SpinWait.SpinUntil%2A> <xref:System.Threading.CancellationToken> nesne iptal edildiğinde yöntem döndürür.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-115">The <xref:System.Threading.SpinWait.SpinUntil%2A> method returns when this <xref:System.Threading.CancellationToken> object is canceled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a2cb4-116">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="a2cb4-116">See also</span></span>

- [<span data-ttu-id="a2cb4-117">Veri akışı</span><span class="sxs-lookup"><span data-stu-id="a2cb4-117">Dataflow</span></span>](../../../docs/standard/parallel-programming/dataflow-task-parallel-library.md)
