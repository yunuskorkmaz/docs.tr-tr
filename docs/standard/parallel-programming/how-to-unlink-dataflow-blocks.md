---
title: 'Nasıl yapılır: Veri Akışı Bloklarının Bağlantısını Kaldırma'
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- dataflow blocks, unlinking in TPL
- Task Parallel Library, dataflows
- TPL dataflow library, unlinking dataflow blocks
ms.assetid: 40f0208d-4618-47f7-85cf-4913d07d2d7d
ms.openlocfilehash: b49cfc9730ba154202baf15093a54ba3ce0e2a8a
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/30/2019
ms.locfileid: "73139301"
---
# <a name="how-to-unlink-dataflow-blocks"></a><span data-ttu-id="f7694-102">Nasıl yapılır: Veri Akışı Bloklarının Bağlantısını Kaldırma</span><span class="sxs-lookup"><span data-stu-id="f7694-102">How to: Unlink Dataflow Blocks</span></span>
<span data-ttu-id="f7694-103">Bu belge, hedef veri akışı bloğunun kaynağından nasıl bağlantısını kaldırmayı açıklar.</span><span class="sxs-lookup"><span data-stu-id="f7694-103">This document describes how to unlink a target dataflow block from its source.</span></span>

[!INCLUDE [tpl-install-instructions](../../../includes/tpl-install-instructions.md)]

## <a name="example"></a><span data-ttu-id="f7694-104">Örnek</span><span class="sxs-lookup"><span data-stu-id="f7694-104">Example</span></span>  
 <span data-ttu-id="f7694-105">Aşağıdaki örnek, her biri bir değeri hesaplamak için `TrySolution` yöntemini çağıran üç <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> nesnesi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f7694-105">The following example creates three <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objects, each of which calls the `TrySolution` method to compute a value.</span></span> <span data-ttu-id="f7694-106">Bu örnek, `TrySolution` için yalnızca ilk çağrıdan elde edilen sonucun bitmesini gerektirir.</span><span class="sxs-lookup"><span data-stu-id="f7694-106">This example requires only the result from the first call to `TrySolution` to finish.</span></span>  
  
 [!code-csharp[TPLDataflow_ReceiveAny#1](../../../samples/snippets/csharp/VS_Snippets_Misc/tpldataflow_receiveany/cs/dataflowreceiveany.cs#1)]
 [!code-vb[TPLDataflow_ReceiveAny#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpldataflow_receiveany/vb/dataflowreceiveany.vb#1)]  
  
 <span data-ttu-id="f7694-107">Bunu izleyen ilk <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> nesnesinden değeri almak için, bu örnek `ReceiveFromAny(T)` yöntemini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="f7694-107">To receive the value from the first <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> object that finishes, this example defines the `ReceiveFromAny(T)` method.</span></span> <span data-ttu-id="f7694-108">`ReceiveFromAny(T)` yöntemi bir <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> nesneleri dizisini kabul eder ve bu nesnelerin her birini bir <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesnesine bağlar.</span><span class="sxs-lookup"><span data-stu-id="f7694-108">The `ReceiveFromAny(T)` method accepts an array of <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> objects and links each of these objects to a <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="f7694-109">Kaynak veri akışı bloğunu hedef bloğa bağlamak için <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> yöntemini kullandığınızda, kaynak, veriler kullanılabilir hale geldiğinde iletileri hedefe yayar.</span><span class="sxs-lookup"><span data-stu-id="f7694-109">When you use the <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method to link a source dataflow block to a target block, the source propagates messages to the target as data becomes available.</span></span> <span data-ttu-id="f7694-110"><xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> sınıfı yalnızca sunulan ilk iletiyi kabul ettiğinden, `ReceiveFromAny(T)` yöntemi <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> yöntemini çağırarak sonucunu üretir.</span><span class="sxs-lookup"><span data-stu-id="f7694-110">Because the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> class accepts only the first message that it is offered, the `ReceiveFromAny(T)` method produces its result by calling the <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> method.</span></span> <span data-ttu-id="f7694-111">Bu, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesnesine sunulan ilk iletiyi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f7694-111">This produces the first message that is offered to the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="f7694-112"><xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> yöntemi, `1`olarak ayarlandığında, kaynak bloğunun kaynaktan bir ileti aldıktan sonra hedefin bağlantısını kesmek için bir <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> özelliğine sahip bir <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> nesnesi alan aşırı yüklenmiş bir sürüme sahiptir.</span><span class="sxs-lookup"><span data-stu-id="f7694-112">The <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method has an overloaded version that takes an <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> object with a <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> property that, when it is set to `1`, instructs the source block to unlink from the target after the target receives one message from the source.</span></span> <span data-ttu-id="f7694-113"><xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesnesi bir ileti aldıktan sonra, kaynak dizisi ve <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesnesi arasındaki ilişki artık gerekli olmadığından, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> nesnenin kaynaklarından bağlantısı kaldırmak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="f7694-113">It is important for the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object to unlink from its sources because the relationship between the array of sources and the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object is no longer required after the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object receives a message.</span></span>  
  
 <span data-ttu-id="f7694-114">`TrySolution` için kalan çağrıların, bir değer hesapladıktan sonra sona erdirmek için, `TrySolution` yöntemi `ReceiveFromAny(T)` dönüşe yapılan çağrıdan sonra iptal edilen bir <xref:System.Threading.CancellationToken> nesnesi alır.</span><span class="sxs-lookup"><span data-stu-id="f7694-114">To enable the remaining calls to `TrySolution` to end after one of them computes a value, the `TrySolution` method takes a <xref:System.Threading.CancellationToken> object that is canceled after the call to `ReceiveFromAny(T)` returns.</span></span> <span data-ttu-id="f7694-115"><xref:System.Threading.SpinWait.SpinUntil%2A> yöntemi bu <xref:System.Threading.CancellationToken> nesnesi iptal edildiğinde döndürür.</span><span class="sxs-lookup"><span data-stu-id="f7694-115">The <xref:System.Threading.SpinWait.SpinUntil%2A> method returns when this <xref:System.Threading.CancellationToken> object is canceled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f7694-116">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="f7694-116">See also</span></span>

- [<span data-ttu-id="f7694-117">Veri akışı</span><span class="sxs-lookup"><span data-stu-id="f7694-117">Dataflow</span></span>](../../../docs/standard/parallel-programming/dataflow-task-parallel-library.md)
