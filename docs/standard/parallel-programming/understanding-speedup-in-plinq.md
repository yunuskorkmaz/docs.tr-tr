---
title: PLINQ'te Hızlandırmayı Anlama
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, performance tuning
ms.assetid: 53706c7e-397d-467a-98cd-c0d1fd63ba5e
ms.openlocfilehash: 07b5027d560a4caccc6c0a516c3f70c11df6be83
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2020
ms.locfileid: "73139912"
---
# <a name="understanding-speedup-in-plinq"></a><span data-ttu-id="cf28f-102">PLINQ'te Hızlandırmayı Anlama</span><span class="sxs-lookup"><span data-stu-id="cf28f-102">Understanding Speedup in PLINQ</span></span>
<span data-ttu-id="cf28f-103">PLINQ'nin birincil amacı, çok çekirdekli bilgisayarlarda sorgu temsilcilerini paralel olarak yürüterek LINQ to Objects sorgularının yürütülmesini hızlandırmaktır.</span><span class="sxs-lookup"><span data-stu-id="cf28f-103">The primary purpose of PLINQ is to speed up the execution of LINQ to Objects queries by executing the query delegates in parallel on multi-core computers.</span></span> <span data-ttu-id="cf28f-104">PLINQ, bir kaynak koleksiyonundaki her öğenin işlenmesi bağımsız olduğunda ve tek tek temsilciler arasında paylaşılan bir durum söz konusu olmadığında en iyi performansı gösterir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-104">PLINQ performs best when the processing of each element in a source collection is independent, with no shared state involved among the individual delegates.</span></span> <span data-ttu-id="cf28f-105">Bu tür işlemler Linq to Objects ve PLINQ'da yaygındır ve genellikle "*nefis paralel*" olarak adlandırılır, çünkü birden çok iş parçacığı üzerinde zamanlamaya kolayca ödünç verirler.</span><span class="sxs-lookup"><span data-stu-id="cf28f-105">Such operations are common in LINQ to Objects and PLINQ, and are often called "*delightfully parallel*" because they lend themselves easily to scheduling on multiple threads.</span></span> <span data-ttu-id="cf28f-106">Ancak, tüm sorgular tamamen nefis paralel işlemlerden oluşmaz; çoğu durumda, bir sorgu paralelleştirileemeyen veya paralel yürütmeyi yavaşlatan bazı işleçler içerir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-106">However, not all queries consist entirely of delightfully parallel operations; in most cases, a query involves some operators that either cannot be parallelized, or that slow down parallel execution.</span></span> <span data-ttu-id="cf28f-107">Ve tamamen nefis paralel sorguları bile, PLINQ hala veri kaynağı bölüm ve iş parçacıkları üzerinde çalışma zamanlama ve genellikle sorgu tamamlandığında sonuçları birleştirmek gerekir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-107">And even with queries that are entirely delightfully parallel, PLINQ must still partition the data source and schedule the work on the threads, and usually merge the results when the query completes.</span></span> <span data-ttu-id="cf28f-108">Tüm bu işlemler paralelleştirmenin hesaplama maliyetine eklenir; paralelleştirme eklemenin bu *maliyetlerine genel gider*denir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-108">All these operations add to the computational cost of parallelization; these costs of adding parallelization are called *overhead*.</span></span> <span data-ttu-id="cf28f-109">PLINQ sorgusunda optimum performans elde etmek için amaç, nefis paralel olan parçaları en üst düzeye çıkarmak ve ek yükü gerektiren parçaları en aza indirmektir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-109">To achieve optimum performance in a PLINQ query, the goal is to maximize the parts that are delightfully parallel and minimize the parts that require overhead.</span></span> <span data-ttu-id="cf28f-110">Bu makalede, hala doğru sonuçlar verirken mümkün olduğunca verimli PLINQ sorguları yazmanıza yardımcı olacak bilgiler sağlar.</span><span class="sxs-lookup"><span data-stu-id="cf28f-110">This article provides information that will help you write PLINQ queries that are as efficient as possible while still yielding correct results.</span></span>  
  
## <a name="factors-that-impact-plinq-query-performance"></a><span data-ttu-id="cf28f-111">PLINQ Sorgu Performansını Etkileyen Faktörler</span><span class="sxs-lookup"><span data-stu-id="cf28f-111">Factors that Impact PLINQ Query Performance</span></span>  
 <span data-ttu-id="cf28f-112">Aşağıdaki bölümlerde paralel sorgu performansını etkileyen en önemli etkenlerden bazıları listelemektedir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-112">The following sections lists some of the most important factors that impact parallel query performance.</span></span> <span data-ttu-id="cf28f-113">Bunlar, tüm durumlarda sorgu performansını tahmin etmek için tek başlarına yeterli olmayan genel ifadelerdir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-113">These are general statements that by themselves are not sufficient to predict query performance in all cases.</span></span> <span data-ttu-id="cf28f-114">Her zaman olduğu gibi, temsili yapılandırmaları ve yükleri bir dizi bilgisayarlarda belirli sorguların gerçek performansını ölçmek önemlidir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-114">As always, it is important to measure actual performance of specific queries on computers with a range of representative configurations and loads.</span></span>  
  
1. <span data-ttu-id="cf28f-115">Genel çalışmanın hesaplama maliyeti.</span><span class="sxs-lookup"><span data-stu-id="cf28f-115">Computational cost of the overall work.</span></span>  
  
     <span data-ttu-id="cf28f-116">Hız elde etmek için, bir PLINQ sorgusu nun genel yükü dengelemek için yeterli nefis paralel çalışma olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-116">To achieve speedup, a PLINQ query must have enough delightfully parallel work to offset the overhead.</span></span> <span data-ttu-id="cf28f-117">Çalışma, her temsilcinin hesaplama maliyeti, kaynak koleksiyondaki öğe sayısıyla çarpılırken ifade edilebilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-117">The work can be expressed as the computational cost of each delegate multiplied by the number of elements in the source collection.</span></span> <span data-ttu-id="cf28f-118">Bir işlemin paralelleştirilebildiği, hesaplama olarak ne kadar pahalı olursa, hızlandırma fırsatı da o kadar büyük olur.</span><span class="sxs-lookup"><span data-stu-id="cf28f-118">Assuming that an operation can be parallelized, the more computationally expensive it is, the greater the opportunity for speedup.</span></span> <span data-ttu-id="cf28f-119">Örneğin, bir işlevin yürütülmesi bir milisaniye sürerse, 1000 öğenin üzerindeki sıralı bir sorgunun bu işlemi gerçekleştirmesi bir saniye sürerken, dört çekirdekli bir bilgisayardaki paralel sorgu yalnızca 250 milisaniye sürebilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-119">For example, if a function takes one millisecond to execute, a sequential query over 1000 elements will take one second to perform that operation, whereas a parallel query on a computer with four cores might take only 250 milliseconds.</span></span> <span data-ttu-id="cf28f-120">Bu, 750 milisaniyelik bir hız verir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-120">This yields a speedup of 750 milliseconds.</span></span> <span data-ttu-id="cf28f-121">İşlevher öğe için çalıştırmak için bir saniye gerekiyorsa, o zaman hız 750 saniye olacaktır.</span><span class="sxs-lookup"><span data-stu-id="cf28f-121">If the function required one second to execute for each element, then the speedup would be 750 seconds.</span></span> <span data-ttu-id="cf28f-122">Temsilci çok pahalıysa, PLINQ kaynak koleksiyonunda yalnızca birkaç öğeyle önemli bir hız sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-122">If the delegate is very expensive, then PLINQ might offer significant speedup with only a few items in the source collection.</span></span> <span data-ttu-id="cf28f-123">Tersine, önemsiz delegeler ile küçük kaynak koleksiyonları genellikle PLINQ için iyi adaylar değildir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-123">Conversely, small source collections with trivial delegates are generally not good candidates for PLINQ.</span></span>  
  
     <span data-ttu-id="cf28f-124">Aşağıdaki örnekte, queryA muhtemelen PLINQ için iyi bir adaydır ve Select işlevinin çok fazla iş içerdiğini varsayarak.</span><span class="sxs-lookup"><span data-stu-id="cf28f-124">In the following example, queryA is probably a good candidate for PLINQ, assuming that its Select function involves a lot of work.</span></span> <span data-ttu-id="cf28f-125">queryB, Select deyiminde yeterli çalışma olmadığından ve paralelleştirmenin yükü hızın çoğunu veya tamamını dengeleyeceği için büyük olasılıkla iyi bir aday değildir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-125">queryB is probably not a good candidate because there is not enough work in the Select statement, and the overhead of parallelization will offset most or all of the speedup.</span></span>  
  
    ```vb  
    Dim queryA = From num In numberList.AsParallel()  
                 Select ExpensiveFunction(num); 'good for PLINQ  
  
    Dim queryB = From num In numberList.AsParallel()  
                 Where num Mod 2 > 0  
                 Select num; 'not as good for PLINQ  
    ```  
  
    ```csharp  
    var queryA = from num in numberList.AsParallel()  
                 select ExpensiveFunction(num); //good for PLINQ  
  
    var queryB = from num in numberList.AsParallel()  
                 where num % 2 > 0  
                 select num; //not as good for PLINQ  
    ```  
  
2. <span data-ttu-id="cf28f-126">Sistemdeki mantıksal çekirdek sayısı (paralellik derecesi).</span><span class="sxs-lookup"><span data-stu-id="cf28f-126">The number of logical cores on the system (degree of parallelism).</span></span>  
  
     <span data-ttu-id="cf28f-127">Bu nokta önceki bölüme bariz bir corollary, iş daha eşzamanlı iş parçacıkları arasında bölünebilir, çünkü nefis paralel daha çekirdekli makinelerde daha hızlı çalıştırmak sorguları.</span><span class="sxs-lookup"><span data-stu-id="cf28f-127">This point is an obvious corollary to the previous section, queries that are delightfully parallel run faster on machines with more cores because the work can be divided among more concurrent threads.</span></span> <span data-ttu-id="cf28f-128">Toplam hız lanma miktarı, sorgunun genel çalışmasının yüzde kaçının paralelleştirilebilir olduğuna bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="cf28f-128">The overall amount of speedup depends on what percentage of the overall work of the query is parallelizable.</span></span> <span data-ttu-id="cf28f-129">Ancak, tüm sorguların dört çekirdekli bir bilgisayardan sekiz çekirdekli bilgisayarda iki kat daha hızlı çalışacağını düşünmeyin.</span><span class="sxs-lookup"><span data-stu-id="cf28f-129">However, do not assume that all queries will run twice as fast on an eight core computer as a four core computer.</span></span> <span data-ttu-id="cf28f-130">En iyi performans için sorguları alarken, çeşitli sayıda çekirdek içeren bilgisayarlarda gerçek sonuçları ölçmek önemlidir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-130">When tuning queries for optimal performance, it is important to measure actual results on computers with various numbers of cores.</span></span> <span data-ttu-id="cf28f-131">Bu nokta #1 noktasıile ilgilidir: daha büyük veri kümelerinin daha büyük bilgi işlem kaynaklarından yararlanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-131">This point is related to point #1: larger datasets are required to take advantage of greater computing resources.</span></span>  
  
3. <span data-ttu-id="cf28f-132">Operasyonların sayısı ve türü.</span><span class="sxs-lookup"><span data-stu-id="cf28f-132">The number and kind of operations.</span></span>  
  
     <span data-ttu-id="cf28f-133">PLINQ, kaynak dizideki öğelerin sırasını korumak için gerekli olan durumlar için AsOrdered işleci sağlar.</span><span class="sxs-lookup"><span data-stu-id="cf28f-133">PLINQ provides the AsOrdered operator for situations in which it is necessary to maintain the order of elements in the source sequence.</span></span> <span data-ttu-id="cf28f-134">Sipariş ile ilişkili bir maliyet vardır, ancak bu maliyet genellikle mütevazı.</span><span class="sxs-lookup"><span data-stu-id="cf28f-134">There is a cost associated with ordering, but this cost is usually modest.</span></span> <span data-ttu-id="cf28f-135">GroupBy ve Join işlemleri de aynı şekilde ek yükü ne durumdadır.</span><span class="sxs-lookup"><span data-stu-id="cf28f-135">GroupBy and Join operations likewise incur overhead.</span></span> <span data-ttu-id="cf28f-136">PLINQ, kaynak koleksiyonundaki öğeleri herhangi bir sırada işlemesine ve hazır olur olmaz bir sonraki işleceye geçirmesine izin verildiğinde en iyi performansı gösterir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-136">PLINQ performs best when it is allowed to process elements in the source collection in any order, and pass them to the next operator as soon as they are ready.</span></span> <span data-ttu-id="cf28f-137">Daha fazla bilgi için [PLINQ'da Sipariş Koruma'ya](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="cf28f-137">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>  
  
4. <span data-ttu-id="cf28f-138">Sorgu yürütme biçimi.</span><span class="sxs-lookup"><span data-stu-id="cf28f-138">The form of query execution.</span></span>  
  
     <span data-ttu-id="cf28f-139">Bir sorgunun sonuçlarını ToArray veya ToList'i arayarak saklıyorsanız, tüm paralel iş parçacıklarının sonuçlarıtek bir veri yapısında birleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-139">If you are storing the results of a query by calling ToArray or ToList, then the results from all parallel threads must be merged into the single data structure.</span></span> <span data-ttu-id="cf28f-140">Bu kaçınılmaz bir hesaplama maliyeti içerir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-140">This involves an unavoidable computational cost.</span></span> <span data-ttu-id="cf28f-141">Aynı şekilde, sonuçları foreach (Visual Basic'teki her biri için) döngü kullanarak yinelerseniz, alt iş parçacığının sonuçlarının da sayısaliş iş parçacığına serileştirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-141">Likewise, if you iterate the results by using a foreach (For Each in Visual Basic) loop, the results from the worker threads need to be serialized onto the enumerator thread.</span></span> <span data-ttu-id="cf28f-142">Ancak, her iş parçacığının sonucuna göre bazı eylem gerçekleştirmek istiyorsanız, birden çok iş parçacığı üzerinde bu işi gerçekleştirmek için ForAll yöntemini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cf28f-142">But if you just want to perform some action based on the result from each thread, you can use the ForAll method to perform this work on multiple threads.</span></span>  
  
5. <span data-ttu-id="cf28f-143">Birleştirme seçeneklerinin türü.</span><span class="sxs-lookup"><span data-stu-id="cf28f-143">The type of merge options.</span></span>  
  
     <span data-ttu-id="cf28f-144">PLINQ, çıktısını arabelleğe alacak şekilde yapılandırılabilir ve tüm sonuç kümesi üretildikten sonra parçalar halinde veya tek seferde üretilebilir veya tek tek sonuçları üretildikçe aktarabilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-144">PLINQ can be configured to either buffer its output, and produce it in chunks or all at once after the entire result set is produced, or else to stream individual results as they are produced.</span></span> <span data-ttu-id="cf28f-145">Eski sonuçlar genel yürütme süresini azaltmış ve ikincisi verim öğeleri arasındaki gecikme süresinin azalmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="cf28f-145">The former results in decreased overall execution time and the latter results in decreased latency between yielded elements.</span></span>  <span data-ttu-id="cf28f-146">Birleştirme seçenekleri her zaman genel sorgu performansı üzerinde önemli bir etkiye sahip olmasa da, kullanıcının sonuçları görmek için ne kadar beklemesi gerektiğini denetlediği için algılanan performansı etkileyebilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-146">While the merge options do not always have a major impact on overall query performance, they can impact perceived performance because they control how long a user must wait to see results.</span></span> <span data-ttu-id="cf28f-147">Daha fazla bilgi için [PLINQ'daki Seçenekleri Birleştir'e](../../../docs/standard/parallel-programming/merge-options-in-plinq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="cf28f-147">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>  
  
6. <span data-ttu-id="cf28f-148">Bir tür bölümleme.</span><span class="sxs-lookup"><span data-stu-id="cf28f-148">The kind of partitioning.</span></span>  
  
     <span data-ttu-id="cf28f-149">Bazı durumlarda, dizine eklenebilir kaynak koleksiyonu üzerinde plinq sorgusu dengesiz bir iş yükü neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-149">In some cases, a PLINQ query over an indexable source collection may result in an unbalanced work load.</span></span> <span data-ttu-id="cf28f-150">Bu durumda, özel bir bölümleyici oluşturarak sorgu performansını artırmak mümkün olabilir.</span><span class="sxs-lookup"><span data-stu-id="cf28f-150">When this occurs, you might be able to increase the query performance by creating a custom partitioner.</span></span> <span data-ttu-id="cf28f-151">Daha fazla bilgi [için PLINQ ve TPL için Özel Bölümleyiciler'e](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="cf28f-151">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>  
  
## <a name="when-plinq-chooses-sequential-mode"></a><span data-ttu-id="cf28f-152">PLINQ Sıralı Modu Seçtiğinde</span><span class="sxs-lookup"><span data-stu-id="cf28f-152">When PLINQ Chooses Sequential Mode</span></span>  
 <span data-ttu-id="cf28f-153">PLINQ her zaman sorgunun sırayla çalıştırılacaya kadar en az bir sorguyu yürütmeyi dener.</span><span class="sxs-lookup"><span data-stu-id="cf28f-153">PLINQ will always attempt to execute a query at least as fast as the query would run sequentially.</span></span> <span data-ttu-id="cf28f-154">PLINQ, kullanıcı temsilcilerinin hesaplama açısından ne kadar pahalı olduğuna veya giriş kaynağının ne kadar büyük olduğuna bakmasa da, belirli sorgu "şekilleri" arar.</span><span class="sxs-lookup"><span data-stu-id="cf28f-154">Although PLINQ does not look at how computationally expensive the user delegates are, or how big the input source is, it does look for certain query "shapes."</span></span> <span data-ttu-id="cf28f-155">Özellikle, sorgu işleçleri veya genellikle paralel modda daha yavaş yürütmek için bir sorgu neden işleçleri kombinasyonları arar.</span><span class="sxs-lookup"><span data-stu-id="cf28f-155">Specifically, it looks for query operators or combinations of operators that typically cause a query to execute more slowly in parallel mode.</span></span> <span data-ttu-id="cf28f-156">Bu şekilleri bulduğunda, PLINQ varsayılan olarak sıralı moda geri düşer.</span><span class="sxs-lookup"><span data-stu-id="cf28f-156">When it finds such shapes, PLINQ by default falls back to sequential mode.</span></span>  
  
 <span data-ttu-id="cf28f-157">Ancak, belirli bir sorgunun performansını ölçtükten sonra, gerçekte paralel modda daha hızlı çalıştığını belirleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cf28f-157">However, after measuring a specific query's performance, you may determine that it actually runs faster in parallel mode.</span></span> <span data-ttu-id="cf28f-158">Bu gibi durumlarda, <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> plinq <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> sorguparalelleştirmek için talimat yöntemi ile bayrağı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cf28f-158">In such cases you can use the <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> flag via the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method to instruct PLINQ to parallelize the query.</span></span> <span data-ttu-id="cf28f-159">Daha fazla bilgi için [bkz: PLINQ'da Yürütme Modunu belirtin.](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md)</span><span class="sxs-lookup"><span data-stu-id="cf28f-159">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>  
  
 <span data-ttu-id="cf28f-160">Aşağıdaki liste plinq varsayılan olarak sıralı modda yürütecek sorgu şekilleri açıklanır:</span><span class="sxs-lookup"><span data-stu-id="cf28f-160">The following list describes the query shapes that PLINQ by default will execute in sequential mode:</span></span>  
  
- <span data-ttu-id="cf28f-161">Özgün endeksleri kaldıran veya yeniden düzenleyen bir sipariş veya filtreleme işlecinden sonra Select, indexdWhere, indexdSelectMany veya ElementAt yan tümcesi içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cf28f-161">Queries that contain a Select, indexed Where, indexed SelectMany, or ElementAt clause after an ordering or filtering operator that has removed or rearranged original indices.</span></span>  
  
- <span data-ttu-id="cf28f-162">Take, TakeWhile, Skip, SkipWhile işleci ve kaynak dizisindeki endekslerin özgün sırada olmadığı sorgular.</span><span class="sxs-lookup"><span data-stu-id="cf28f-162">Queries that contain a Take, TakeWhile, Skip, SkipWhile operator and where indices in the source sequence are not in the original order.</span></span>  
  
- <span data-ttu-id="cf28f-163">Veri kaynaklarından biri özgün olarak sıralanmış bir diziye sahip değilse ve diğer veri kaynağı dizinlenebilir (örneğin, bir dizi veya IList(T)) yoksa Zip veya SequenceEquals içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cf28f-163">Queries that contain Zip or SequenceEquals, unless one of the data sources has an originally ordered index and the other data source is indexable (i.e. an array or IList(T)).</span></span>  
  
- <span data-ttu-id="cf28f-164">Dizine eklenebilir veri kaynaklarına uygulanmadığı sürece Concat içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cf28f-164">Queries that contain Concat, unless it is applied to indexable data sources.</span></span>  
  
- <span data-ttu-id="cf28f-165">Dizinlenebilir bir veri kaynağına uygulanmadığı sürece Ters içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cf28f-165">Queries that contain Reverse, unless applied to an indexable data source.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cf28f-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="cf28f-166">See also</span></span>

- [<span data-ttu-id="cf28f-167">Paralel LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="cf28f-167">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
