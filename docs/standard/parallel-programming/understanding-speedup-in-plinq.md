---
title: PLINQ'te Hızlandırmayı Anlama
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, performance tuning
ms.assetid: 53706c7e-397d-467a-98cd-c0d1fd63ba5e
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: bc36c926ba81de8a59ff3af69719bec6b7370efc
ms.sourcegitcommit: 3ab9254890a52a50762995fa6d7d77a00348db7e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/19/2018
ms.locfileid: "46326325"
---
# <a name="understanding-speedup-in-plinq"></a><span data-ttu-id="cde45-102">PLINQ'te Hızlandırmayı Anlama</span><span class="sxs-lookup"><span data-stu-id="cde45-102">Understanding Speedup in PLINQ</span></span>
<span data-ttu-id="cde45-103">PLINQ birincil amacı, çok çekirdekli bilgisayarlarda paralel sorgu temsilcileri yürüterek Plınq sorgularının LINQ yürütülmesini hızlandırmak sağlamaktır.</span><span class="sxs-lookup"><span data-stu-id="cde45-103">The primary purpose of PLINQ is to speed up the execution of LINQ to Objects queries by executing the query delegates in parallel on multi-core computers.</span></span> <span data-ttu-id="cde45-104">Kaynak koleksiyondaki her öğe işlenmesini bağımsız olarak, paylaşılan durumu olmadan arasında bireysel temsilciler dahil olduğunda PLINQ en iyi şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="cde45-104">PLINQ performs best when the processing of each element in a source collection is independent, with no shared state involved among the individual delegates.</span></span> <span data-ttu-id="cde45-105">Bu işlemler LINQ nesneleri ve PLINQ için ortak olan ve genellikle denir "*delightfully paralel*" çünkü bunlar kendilerini kolayca birden çok iş parçacığında zamanlamaya uygun.</span><span class="sxs-lookup"><span data-stu-id="cde45-105">Such operations are common in LINQ to Objects and PLINQ, and are often called "*delightfully parallel*" because they lend themselves easily to scheduling on multiple threads.</span></span> <span data-ttu-id="cde45-106">Ancak, tüm sorguları tamamen delightfully paralel işlemleri oluşur; Çoğu durumda, bir sorgu, ya da paralelleştirildi olamaz veya paralel yürütme yavaş, bazı işleçler içerir.</span><span class="sxs-lookup"><span data-stu-id="cde45-106">However, not all queries consist entirely of delightfully parallel operations; in most cases, a query involves some operators that either cannot be parallelized, or that slow down parallel execution.</span></span> <span data-ttu-id="cde45-107">Ve tamamen delightfully paralel bile sorgular ile PLINQ gerekir yine de veri kaynağının bölüm ve iş parçacıkları üzerinde iş zamanlama ve sorgu tamamlandığında genellikle sonuçları birleştirin.</span><span class="sxs-lookup"><span data-stu-id="cde45-107">And even with queries that are entirely delightfully parallel, PLINQ must still partition the data source and schedule the work on the threads, and usually merge the results when the query completes.</span></span> <span data-ttu-id="cde45-108">Tüm bu işlemler, paralelleştirme hesaplama maliyetine ekleyin; Bu maliyetler paralelleştirme ekleme adlı *yükü*.</span><span class="sxs-lookup"><span data-stu-id="cde45-108">All these operations add to the computational cost of parallelization; these costs of adding parallelization are called *overhead*.</span></span> <span data-ttu-id="cde45-109">PLINQ sorgusunda en iyi performansı elde etmek için delightfully paralel bölümleri en üst düzeye çıkarmak ve getirdiği ek yüke gerek bölümleri en aza indirmek için hedeftir.</span><span class="sxs-lookup"><span data-stu-id="cde45-109">To achieve optimum performance in a PLINQ query, the goal is to maximize the parts that are delightfully parallel and minimize the parts that require overhead.</span></span> <span data-ttu-id="cde45-110">Bu makalede, mümkün olduğunca etkili doğru sonuçları hala sonuçlanmıyor sırasında PLINQ sorguları yazmanıza yardımcı olacak bilgiler sağlar.</span><span class="sxs-lookup"><span data-stu-id="cde45-110">This article provides information that will help you write PLINQ queries that are as efficient as possible while still yielding correct results.</span></span>  
  
## <a name="factors-that-impact-plinq-query-performance"></a><span data-ttu-id="cde45-111">PLINQ sorgu performansını etkileyen faktörler</span><span class="sxs-lookup"><span data-stu-id="cde45-111">Factors that Impact PLINQ Query Performance</span></span>  
 <span data-ttu-id="cde45-112">Aşağıdaki bölümlerde listelenmiştir bazı en önemli olan faktör, paralel sorgu performansı etkileyebilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-112">The following sections lists some of the most important factors that impact parallel query performance.</span></span> <span data-ttu-id="cde45-113">Tüm durumlarda sorgu performansını tahmin etmek yeterli değil, kendileri tarafından genel deyimleri şunlardır.</span><span class="sxs-lookup"><span data-stu-id="cde45-113">These are general statements that by themselves are not sufficient to predict query performance in all cases.</span></span> <span data-ttu-id="cde45-114">Her zaman olduğu gibi bir dizi temsili yapılandırmaları ve yükü ile gerçek bilgisayarlarda belirli sorguların performansını ölçmek önemlidir.</span><span class="sxs-lookup"><span data-stu-id="cde45-114">As always, it is important to measure actual performance of specific queries on computers with a range of representative configurations and loads.</span></span>  
  
1.  <span data-ttu-id="cde45-115">Genel İş hesaplama maliyeti.</span><span class="sxs-lookup"><span data-stu-id="cde45-115">Computational cost of the overall work.</span></span>  
  
     <span data-ttu-id="cde45-116">Hızlandırmayı elde etmek için yeterli delightfully paralel iş yükünü dengelemek için bir PLINQ sorgusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="cde45-116">To achieve speedup, a PLINQ query must have enough delightfully parallel work to offset the overhead.</span></span> <span data-ttu-id="cde45-117">İş hesaplama maliyeti kaynak koleksiyondaki öğe sayısını çarpılmasıyla her temsilci olarak ifade edilebilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-117">The work can be expressed as the computational cost of each delegate multiplied by the number of elements in the source collection.</span></span> <span data-ttu-id="cde45-118">Bir işlem, daha fazla hesaplama açısından paralelleştirilebilir varsayarak, pahalıdır, büyük hızlandırmayı fırsat tanır.</span><span class="sxs-lookup"><span data-stu-id="cde45-118">Assuming that an operation can be parallelized, the more computationally expensive it is, the greater the opportunity for speedup.</span></span> <span data-ttu-id="cde45-119">Örneğin, bir işlevi yürütmek için bir milisaniyeden kısa alırsa, paralel sorgu ise dört çekirdek içeren bir bilgisayarda 1000'den öğeleri bu işlemi gerçekleştirmek için bir saniye sürecek sıralı bir sorgu yalnızca 250 milisaniye alabilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-119">For example, if a function takes one millisecond to execute, a sequential query over 1000 elements will take one second to perform that operation, whereas a parallel query on a computer with four cores might take only 250 milliseconds.</span></span> <span data-ttu-id="cde45-120">Bu, bir hızlandırmayı 750 milisaniye olarak verir.</span><span class="sxs-lookup"><span data-stu-id="cde45-120">This yields a speedup of 750 milliseconds.</span></span> <span data-ttu-id="cde45-121">Ardından işlev her öğe için bir saniyede gerekirse hızlandırmayı 750 saniye olacaktır.</span><span class="sxs-lookup"><span data-stu-id="cde45-121">If the function required one second to execute for each element, then the speedup would be 750 seconds.</span></span> <span data-ttu-id="cde45-122">Ardından temsilci çok pahalı ise, PLINQ kaynak koleksiyondaki yalnızca birkaç öğeleri ile önemli hızlandırmayı sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-122">If the delegate is very expensive, then PLINQ might offer significant speedup with only a few items in the source collection.</span></span> <span data-ttu-id="cde45-123">Buna karşılık, küçük kaynak koleksiyonlarla Önemsiz temsilcileri genellikle PLINQ için iyi adaylar değildir.</span><span class="sxs-lookup"><span data-stu-id="cde45-123">Conversely, small source collections with trivial delegates are generally not good candidates for PLINQ.</span></span>  
  
     <span data-ttu-id="cde45-124">Aşağıdaki örnekte, queryA büyük olasılıkla, Select işlevi birçok iş içerdiğini varsayarak, PLINQ için iyi bir aday olan.</span><span class="sxs-lookup"><span data-stu-id="cde45-124">In the following example, queryA is probably a good candidate for PLINQ, assuming that its Select function involves a lot of work.</span></span> <span data-ttu-id="cde45-125">queryB iyi bir aday paralelleştirme yükü çoğunu veya tümünü hızlandırmayı uzaklığı ve Select deyiminde yeterli iş olmadığından büyük olasılıkla değil.</span><span class="sxs-lookup"><span data-stu-id="cde45-125">queryB is probably not a good candidate because there is not enough work in the Select statement, and the overhead of parallelization will offset most or all of the speedup.</span></span>  
  
    ```vb  
    Dim queryA = From num In numberList.AsParallel()  
                 Select ExpensiveFunction(num); 'good for PLINQ  
  
    Dim queryB = From num In numberList.AsParallel()  
                 Where num Mod 2 > 0  
                 Select num; 'not as good for PLINQ  
    ```  
  
    ```csharp  
    var queryA = from num in numberList.AsParallel()  
                 select ExpensiveFunction(num); //good for PLINQ  
  
    var queryB = from num in numberList.AsParallel()  
                 where num % 2 > 0  
                 select num; //not as good for PLINQ  
    ```  
  
2.  <span data-ttu-id="cde45-126">' % S'sistemde (çalıştırılır. paralellik derecesini) mantıksal çekirdek sayısı.</span><span class="sxs-lookup"><span data-stu-id="cde45-126">The number of logical cores on the system (degree of parallelism).</span></span>  
  
     <span data-ttu-id="cde45-127">Bu noktaya belirgin bir corollary önceki bölüme, daha fazla eşzamanlı iş parçacıkları arasında iş ayrılabilir çünkü delightfully paralel sorgular makinelerde ile daha fazla çekirdek daha hızlı çalışır.</span><span class="sxs-lookup"><span data-stu-id="cde45-127">This point is an obvious corollary to the previous section, queries that are delightfully parallel run faster on machines with more cores because the work can be divided among more concurrent threads.</span></span> <span data-ttu-id="cde45-128">Toplam miktarı hızlandırmayı sorgunun genel iş yüzdesini paralelleştirilebilir olduğuna bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="cde45-128">The overall amount of speedup depends on what percentage of the overall work of the query is parallelizable.</span></span> <span data-ttu-id="cde45-129">Ancak, tüm sorgular kat çalıştırılır varsaymayın dört ana bilgisayarla sekiz çekirdeği bilgisayarında hızlı.</span><span class="sxs-lookup"><span data-stu-id="cde45-129">However, do not assume that all queries will run twice as fast on an eight core computer as a four core computer.</span></span> <span data-ttu-id="cde45-130">Sorgular için en iyi performans ayarlama, çeşitli sayıda çekirdek olan bilgisayarlarda gerçek sonuçlar ölçmek önemlidir.</span><span class="sxs-lookup"><span data-stu-id="cde45-130">When tuning queries for optimal performance, it is important to measure actual results on computers with various numbers of cores.</span></span> <span data-ttu-id="cde45-131">Bu noktaya #1 işaret edecek şekilde ilişkilidir: büyük veri kümeleri, daha büyük bilgi işlem kaynakları yararlanmak için gereklidir.</span><span class="sxs-lookup"><span data-stu-id="cde45-131">This point is related to point #1: larger datasets are required to take advantage of greater computing resources.</span></span>  
  
3.  <span data-ttu-id="cde45-132">Sayısı ve işlemleri bir tür.</span><span class="sxs-lookup"><span data-stu-id="cde45-132">The number and kind of operations.</span></span>  
  
     <span data-ttu-id="cde45-133">PLINQ kaynak dizisindeki öğelerin sırasını korumak için gerekli olduğu durumları metodu AsOrdered işleci sağlar.</span><span class="sxs-lookup"><span data-stu-id="cde45-133">PLINQ provides the AsOrdered operator for situations in which it is necessary to maintain the order of elements in the source sequence.</span></span> <span data-ttu-id="cde45-134">Sıralama ile ilişkilendirilmiş bir maliyet yoktur, ancak bu maliyet genellikle daha düşüktür.</span><span class="sxs-lookup"><span data-stu-id="cde45-134">There is a cost associated with ordering, but this cost is usually modest.</span></span> <span data-ttu-id="cde45-135">GroupBy ve birleştirme işlemlerinin benzer şekilde zahmetine.</span><span class="sxs-lookup"><span data-stu-id="cde45-135">GroupBy and Join operations likewise incur overhead.</span></span> <span data-ttu-id="cde45-136">Herhangi bir sırada kaynak koleksiyondaki öğe işleme ve hazır olduğunda hemen sonra geçirileceğini sonraki işlecine izin verilmediği halde PLINQ en iyi şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="cde45-136">PLINQ performs best when it is allowed to process elements in the source collection in any order, and pass them to the next operator as soon as they are ready.</span></span> <span data-ttu-id="cde45-137">Daha fazla bilgi için [plınq'te sıra koruma](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="cde45-137">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>  
  
4.  <span data-ttu-id="cde45-138">Sorgu yürütme formu.</span><span class="sxs-lookup"><span data-stu-id="cde45-138">The form of query execution.</span></span>  
  
     <span data-ttu-id="cde45-139">ToArray veya ToList çağırarak bir sorgunun sonuçlarını depoluyorsanız tüm paralel iş parçacıkları sonuçlardan tek bir veri yapısına birleştirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="cde45-139">If you are storing the results of a query by calling ToArray or ToList, then the results from all parallel threads must be merged into the single data structure.</span></span> <span data-ttu-id="cde45-140">Bu, bir kaçınılmaz hesaplama maliyetini kapsar.</span><span class="sxs-lookup"><span data-stu-id="cde45-140">This involves an unavoidable computational cost.</span></span> <span data-ttu-id="cde45-141">Benzer şekilde, bir (For Each Visual Basic'te) foreach döngüsü kullanarak sonuçları yineleme, çalışan iş parçacıkları sonuçlardan Numaralandırıcı parçacığına serileştirilecek gerekir.</span><span class="sxs-lookup"><span data-stu-id="cde45-141">Likewise, if you iterate the results by using a foreach (For Each in Visual Basic) loop, the results from the worker threads need to be serialized onto the enumerator thread.</span></span> <span data-ttu-id="cde45-142">Ancak, yalnızca her bir iş parçacığı sonucuna dayalı bazı eylemler gerçekleştirmek istiyorsanız, birden çok iş parçacığı üzerinde bu işi gerçekleştirmek için ForAll yöntemini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cde45-142">But if you just want to perform some action based on the result from each thread, you can use the ForAll method to perform this work on multiple threads.</span></span>  
  
5.  <span data-ttu-id="cde45-143">Birleştirme seçeneklerini türü.</span><span class="sxs-lookup"><span data-stu-id="cde45-143">The type of merge options.</span></span>  
  
     <span data-ttu-id="cde45-144">PLINQ çıktısını arabelleğe ve bunlar üretilen gibi sonuç kümesinin tamamı, veya başka stream tek sonuçları üretilen sonra öbekleri veya tümünü tek seferde, üretmek için yapılandırılabilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-144">PLINQ can be configured to either buffer its output, and produce it in chunks or all at once after the entire result set is produced, or else to stream individual results as they are produced.</span></span> <span data-ttu-id="cde45-145">Önceki sonuç azalan toplam yürütme süresi ve ikinci sonucunu veriyor öğeler arasında düşük gecikme olur.</span><span class="sxs-lookup"><span data-stu-id="cde45-145">The former result is decreased overall execution time and the latter results in decreased latency between yielded elements.</span></span>  <span data-ttu-id="cde45-146">Birleştirme seçeneklerini her zaman önemli bir etkiye genel sorgu performansı üzerindeki değil olsa da, bunlar ne bir kullanıcı denetimi çünkü bunlar algılanan performansını etkileyebilir sonuçlarını görmek için beklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="cde45-146">While the merge options do not always have a major impact on overall query performance, they can impact perceived performance because they control how long a user must wait to see results.</span></span> <span data-ttu-id="cde45-147">Daha fazla bilgi için [plınq'te Birleştirme Seçenekleri](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="cde45-147">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>  
  
6.  <span data-ttu-id="cde45-148">Bölümleme türü.</span><span class="sxs-lookup"><span data-stu-id="cde45-148">The kind of partitioning.</span></span>  
  
     <span data-ttu-id="cde45-149">Bazı durumlarda, dizinlenebilir kaynak koleksiyonu üzerinden bir PLINQ sorgusu bir dengesiz iş yüküne neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-149">In some cases, a PLINQ query over an indexable source collection may result in an unbalanced work load.</span></span> <span data-ttu-id="cde45-150">Bu durumda, özel bir bölümleyici oluşturarak sorgu performansını artırmanın mümkün olabilir.</span><span class="sxs-lookup"><span data-stu-id="cde45-150">When this occurs, you might be able to increase the query performance by creating a custom partitioner.</span></span> <span data-ttu-id="cde45-151">Daha fazla bilgi için [PLINQ ve TPL için özel Bölümleyiciler](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="cde45-151">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>  
  
## <a name="when-plinq-chooses-sequential-mode"></a><span data-ttu-id="cde45-152">PLINQ sıralı modu ne zaman seçer</span><span class="sxs-lookup"><span data-stu-id="cde45-152">When PLINQ Chooses Sequential Mode</span></span>  
 <span data-ttu-id="cde45-153">PLINQ sorgusu sıralı olarak çalışır en az kadar hızlı bir sorgu yürütme her zaman dener.</span><span class="sxs-lookup"><span data-stu-id="cde45-153">PLINQ will always attempt to execute a query at least as fast as the query would run sequentially.</span></span> <span data-ttu-id="cde45-154">PLINQ ne hesaplama açısından aramaz ancak kullanıcı temsilcileri pahalıdır veya nasıl büyük giriş kaynağı olan belirli sorgu "şekiller." arayın</span><span class="sxs-lookup"><span data-stu-id="cde45-154">Although PLINQ does not look at how computationally expensive the user delegates are, or how big the input source is, it does look for certain query "shapes."</span></span> <span data-ttu-id="cde45-155">Özellikle, sorgu işleçleri veya genellikle bir sorgu paralel modunda daha yavaş çalışmasına neden işleçleri birleşimleri için arar.</span><span class="sxs-lookup"><span data-stu-id="cde45-155">Specifically, it looks for query operators or combinations of operators that typically cause a query to execute more slowly in parallel mode.</span></span> <span data-ttu-id="cde45-156">Tür şekilleri bulduğunda, varsayılan olarak PLINQ sıralı moduna geri döner.</span><span class="sxs-lookup"><span data-stu-id="cde45-156">When it finds such shapes, PLINQ by default falls back to sequential mode.</span></span>  
  
 <span data-ttu-id="cde45-157">Ancak, belirli bir sorgu performansını ölçme sonra bunun gerçekten daha hızlı bir şekilde paralel modunda çalıştığından belirlemek.</span><span class="sxs-lookup"><span data-stu-id="cde45-157">However, after measuring a specific query's performance, you may determine that it actually runs faster in parallel mode.</span></span> <span data-ttu-id="cde45-158">Bu gibi durumlarda kullanabilirsiniz <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> aracılığıyla bayrak <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> PLINQ'nun sorguyu paralelleştirmek için istemek için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="cde45-158">In such cases you can use the <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> flag via the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method to instruct PLINQ to parallelize the query.</span></span> <span data-ttu-id="cde45-159">Daha fazla bilgi için [nasıl yapılır: PLINQ'te yürütme modunu belirtme](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="cde45-159">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>  
  
 <span data-ttu-id="cde45-160">Aşağıdaki listede, varsayılan olarak PLINQ sıralı modunda yürütülür sorgu şekiller açıklanmaktadır:</span><span class="sxs-lookup"><span data-stu-id="cde45-160">The following list describes the query shapes that PLINQ by default will execute in sequential mode:</span></span>  
  
-   <span data-ttu-id="cde45-161">Bir Select içeren sorgular dizini, dizinlenmiş SelectMany veya kaldırıldı veya özgün dizinleri yeniden düzenlenmiş olan bir sıralama veya filtreleme işleci sonra ElementAt yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="cde45-161">Queries that contain a Select, indexed Where, indexed SelectMany, or ElementAt clause after an ordering or filtering operator that has removed or rearranged original indices.</span></span>  
  
-   <span data-ttu-id="cde45-162">SkipWhile işleci bir Al ' TakeWhile, Atla içeren ve dizinleri kaynak dizisindeki özgün sırayla olmadığı sorgular.</span><span class="sxs-lookup"><span data-stu-id="cde45-162">Queries that contain a Take, TakeWhile, Skip, SkipWhile operator and where indices in the source sequence are not in the original order.</span></span>  
  
-   <span data-ttu-id="cde45-163">ZIP veya SequenceEquals, veri kaynaklarından biri özgün sıralı bir dizine sahip ve diğer veri kaynağı dizinlenebilir sürece içeren sorgular (yani bir dizi veya IList(T)).</span><span class="sxs-lookup"><span data-stu-id="cde45-163">Queries that contain Zip or SequenceEquals, unless one of the data sources has an originally ordered index and the other data source is indexable (i.e. an array or IList(T)).</span></span>  
  
-   <span data-ttu-id="cde45-164">Concat, dizinlenebilir veri kaynaklarına uygulanır sürece içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cde45-164">Queries that contain Concat, unless it is applied to indexable data sources.</span></span>  
  
-   <span data-ttu-id="cde45-165">Tersine, bir dizine veri kaynağına uygulanan sürece içeren sorgular.</span><span class="sxs-lookup"><span data-stu-id="cde45-165">Queries that contain Reverse, unless applied to an indexable data source.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cde45-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="cde45-166">See also</span></span>

- [<span data-ttu-id="cde45-167">Paralel LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="cde45-167">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
