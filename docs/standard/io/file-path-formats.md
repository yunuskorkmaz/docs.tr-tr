---
title: Windows sistemlerinde dosya yolu biçimleri
ms.date: 06/06/2019
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- I/O, long paths
- long paths
- path formats, Windows
ms.openlocfilehash: 808c92e906a0bf6f8fdc368396d6d240573de501
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/30/2019
ms.locfileid: "73120783"
---
# <a name="file-path-formats-on-windows-systems"></a><span data-ttu-id="634c4-102">Windows sistemlerinde dosya yolu biçimleri</span><span class="sxs-lookup"><span data-stu-id="634c4-102">File path formats on Windows systems</span></span>

<span data-ttu-id="634c4-103"><xref:System.IO> ad alanındaki birçok türün üyeleri, bir dosya sistemi kaynağına mutlak veya göreli bir yol belirtmenize olanak tanıyan bir `path` parametresi içerir.</span><span class="sxs-lookup"><span data-stu-id="634c4-103">Members of many of the types in the <xref:System.IO> namespace include a `path` parameter that lets you specify an absolute or relative path to a file system resource.</span></span> <span data-ttu-id="634c4-104">Bu yol daha sonra [Windows dosya sistemi API 'lerine](/windows/desktop/fileio/file-systems)geçirilir.</span><span class="sxs-lookup"><span data-stu-id="634c4-104">This path is then passed to [Windows file system APIs](/windows/desktop/fileio/file-systems).</span></span> <span data-ttu-id="634c4-105">Bu konuda, Windows sistemlerinde kullanabileceğiniz dosya yollarının biçimleri ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="634c4-105">This topic discusses the formats for file paths that you can use on Windows systems.</span></span>

## <a name="traditional-dos-paths"></a><span data-ttu-id="634c4-106">Geleneksel DOS yolları</span><span class="sxs-lookup"><span data-stu-id="634c4-106">Traditional DOS paths</span></span>

<span data-ttu-id="634c4-107">Standart bir DOS yolu üç bileşenden oluşabilir:</span><span class="sxs-lookup"><span data-stu-id="634c4-107">A standard DOS path can consist of three components:</span></span>

- <span data-ttu-id="634c4-108">Birim veya sürücü harfi, ardından birim ayırıcısı (`:`).</span><span class="sxs-lookup"><span data-stu-id="634c4-108">A volume or drive letter followed by the volume separator (`:`).</span></span>
- <span data-ttu-id="634c4-109">Bir dizin adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-109">A directory name.</span></span> <span data-ttu-id="634c4-110">[Dizin ayırıcı karakteri](<xref:System.IO.Path.DirectorySeparatorChar>) , iç içe geçmiş Dizin hiyerarşisindeki alt dizinleri ayırır.</span><span class="sxs-lookup"><span data-stu-id="634c4-110">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="634c4-111">İsteğe bağlı bir dosya adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-111">An optional filename.</span></span> <span data-ttu-id="634c4-112">[Dizin ayırıcı karakteri](<xref:System.IO.Path.DirectorySeparatorChar>) dosya yolunu ve dosya adını ayırır.</span><span class="sxs-lookup"><span data-stu-id="634c4-112">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="634c4-113">Üç bileşen varsa, yol mutlak olur.</span><span class="sxs-lookup"><span data-stu-id="634c4-113">If all three components are present, the path is absolute.</span></span> <span data-ttu-id="634c4-114">Birim veya sürücü harfi belirtilmemişse ve dizin adı [dizin ayırıcı karakteriyle](<xref:System.IO.Path.DirectorySeparatorChar>)başlıyorsa, yol geçerli sürücünün kökünden görelidir.</span><span class="sxs-lookup"><span data-stu-id="634c4-114">If no volume or drive letter is specified and the directory name begins with the [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>), the path is relative from the root of the current drive.</span></span> <span data-ttu-id="634c4-115">Aksi takdirde, yol geçerli dizine göre değişir.</span><span class="sxs-lookup"><span data-stu-id="634c4-115">Otherwise, the path is relative to the current directory.</span></span> <span data-ttu-id="634c4-116">Aşağıdaki tabloda bazı olası dizin ve dosya yolları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="634c4-116">The following table shows some possible directory and file paths.</span></span>

|<span data-ttu-id="634c4-117">Yol</span><span class="sxs-lookup"><span data-stu-id="634c4-117">Path</span></span>  |<span data-ttu-id="634c4-118">Açıklama</span><span class="sxs-lookup"><span data-stu-id="634c4-118">Description</span></span>  |
| -- | -- |
| `C:\Documents\Newsletters\Summer2018.pdf` | <span data-ttu-id="634c4-119">C sürücüsünün kökünden mutlak bir dosya yolu:</span><span class="sxs-lookup"><span data-stu-id="634c4-119">An absolute file path from the root of drive C:</span></span> |
| `\Program Files\Custom Utilities\StringFinder.exe` | <span data-ttu-id="634c4-120">Geçerli sürücünün kökünden mutlak bir yol.</span><span class="sxs-lookup"><span data-stu-id="634c4-120">An absolute path from the root of the current drive.</span></span> |
| `2018\January.xlsx` | <span data-ttu-id="634c4-121">Geçerli dizinin bir alt dizinindeki bir dosyanın göreli yolu.</span><span class="sxs-lookup"><span data-stu-id="634c4-121">A relative path to a file in a subdirectory of the current directory.</span></span> |
| `..\Publications\TravelBrochure.pdf` | <span data-ttu-id="634c4-122">Geçerli dizinin eşi olan dizindeki dosyanın göreli yolu.</span><span class="sxs-lookup"><span data-stu-id="634c4-122">A relative path to file in a directory that is a peer of the current directory.</span></span> |
| `C:\Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="634c4-123">C sürücüsünün kökünden bir dosyanın mutlak yolu:</span><span class="sxs-lookup"><span data-stu-id="634c4-123">An absolute path to a file from the root of drive C:</span></span> |
| `C:Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="634c4-124">C: sürücüsünün geçerli dizininden göreli bir yol.</span><span class="sxs-lookup"><span data-stu-id="634c4-124">A relative path from the current directory of the C: drive.</span></span> |

> [!IMPORTANT]
> <span data-ttu-id="634c4-125">Son iki yol arasındaki farkı dikkate alın.</span><span class="sxs-lookup"><span data-stu-id="634c4-125">Note the difference between the last two paths.</span></span> <span data-ttu-id="634c4-126">Her ikisi de isteğe bağlı birim belirticisini (C: her iki durumda) belirtir, ancak ilki belirtilen birimin köküyle başlar, ikincisi ise değildir.</span><span class="sxs-lookup"><span data-stu-id="634c4-126">Both specify the optional volume specifier (C: in both cases), but the first begins with the root of the specified volume, whereas the second does not.</span></span> <span data-ttu-id="634c4-127">Sonuç olarak, ilki C sürücüsünün kök dizinindeki mutlak bir yoldur, ikincisi ise C: sürücüsündeki geçerli dizinden göreli bir yoldur.</span><span class="sxs-lookup"><span data-stu-id="634c4-127">As result, the first is an absolute path from the root directory of drive C:, whereas the second is a relative path from the current directory of drive C:.</span></span> <span data-ttu-id="634c4-128">İlki Windows dosya yollarını içeren yaygın bir hata kaynağı olduğunda ikinci formun kullanılması.</span><span class="sxs-lookup"><span data-stu-id="634c4-128">Use of the second form when the first is intended is a common source of bugs that involve Windows file paths.</span></span>

<span data-ttu-id="634c4-129">Bir dosya yolunun tam nitelikli olup olmadığını (yani, yolun geçerli dizinden bağımsız olduğunu ve geçerli dizin değiştiğinde değişmediğini) <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> yöntemini çağırarak belirleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-129">You can determine whether a file path is fully qualified (that is, it the path is independent of the current directory and does not change when the current directory changes) by calling the <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> method.</span></span> <span data-ttu-id="634c4-130">Böyle bir yolun göreli Dizin segmentlerini (`.` ve `..`) içerebildiğini ve çözümlenen yolun her zaman aynı konuma işaret ettiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="634c4-130">Note that such a path can include relative directory segments (`.` and `..`) and still be fully qualified if the resolved path always points to the same location.</span></span>

<span data-ttu-id="634c4-131">Aşağıdaki örnek mutlak ve göreli yollar arasındaki farkı gösterir.</span><span class="sxs-lookup"><span data-stu-id="634c4-131">The following example illustrates the difference between absolute and relative paths.</span></span> <span data-ttu-id="634c4-132">Dizin \ saat 2018\ ' un var olduğunu ve D:\ için herhangi bir seçkin Dizin belirlemediğinizi varsayar. komut isteminden örneği çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="634c4-132">It assumes that the directory D:\FY2018\ exists, and that you haven't set any curent directory for D:\ from the command prompt before running the example.</span></span>

[!code-csharp[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/cs/paths.cs)]
[!code-vb[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/vb/paths.vb)]

## <a name="unc-paths"></a><span data-ttu-id="634c4-133">UNC yolları</span><span class="sxs-lookup"><span data-stu-id="634c4-133">UNC paths</span></span>

<span data-ttu-id="634c4-134">Ağ kaynaklarına erişmek için kullanılan evrensel adlandırma kuralı (UNC) yolları aşağıdaki biçimdedir:</span><span class="sxs-lookup"><span data-stu-id="634c4-134">Universal naming convention (UNC) paths, which are used to access network resources, have the following format:</span></span>

- <span data-ttu-id="634c4-135">\\\\tarafından önceden ortaya çıkacak sunucu veya ana bilgisayar adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-135">A server or host name, which is prefaced by \\\\.</span></span> <span data-ttu-id="634c4-136">Sunucu adı bir NetBIOS makine adı veya IP/FQDN adresi (IPv4 ve V6 desteklenir) olabilir.</span><span class="sxs-lookup"><span data-stu-id="634c4-136">The server name can be a NetBIOS machine name or an IP/FQDN address (IPv4 as well as v6 are supported).</span></span>
- <span data-ttu-id="634c4-137">\\tarafından ana bilgisayar adından ayrılan bir paylaşma adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-137">A share name, which is separated from the host name by \\.</span></span> <span data-ttu-id="634c4-138">Birlikte, sunucu ve paylaşımın adı birimi oluşturun.</span><span class="sxs-lookup"><span data-stu-id="634c4-138">Together, the server and share name make up the volume.</span></span>
- <span data-ttu-id="634c4-139">Bir dizin adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-139">A directory name.</span></span> <span data-ttu-id="634c4-140">[Dizin ayırıcı karakteri](<xref:System.IO.Path.DirectorySeparatorChar>) , iç içe geçmiş Dizin hiyerarşisindeki alt dizinleri ayırır.</span><span class="sxs-lookup"><span data-stu-id="634c4-140">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="634c4-141">İsteğe bağlı bir dosya adı.</span><span class="sxs-lookup"><span data-stu-id="634c4-141">An optional filename.</span></span> <span data-ttu-id="634c4-142">[Dizin ayırıcı karakteri](<xref:System.IO.Path.DirectorySeparatorChar>) dosya yolunu ve dosya adını ayırır.</span><span class="sxs-lookup"><span data-stu-id="634c4-142">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="634c4-143">Aşağıda, UNC yollarının bazı örnekleri verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="634c4-143">The following are some examples of UNC paths:</span></span>

|<span data-ttu-id="634c4-144">Yol</span><span class="sxs-lookup"><span data-stu-id="634c4-144">Path</span></span>  |<span data-ttu-id="634c4-145">Açıklama</span><span class="sxs-lookup"><span data-stu-id="634c4-145">Description</span></span>  |
| -- | -- |
| `\\system07\C$\` | <span data-ttu-id="634c4-146">`system07`üzerindeki C: sürücüsünün kök dizini.</span><span class="sxs-lookup"><span data-stu-id="634c4-146">The root directory of the C: drive on `system07`.</span></span> |
| `\\Server2\Share\Test\Foo.txt` | <span data-ttu-id="634c4-147">\\\\\\test dizinindeki foo. txt dosyası.</span><span class="sxs-lookup"><span data-stu-id="634c4-147">The Foo.txt file in the Test directory of the \\\\Server2\\Share volume.</span></span>|

<span data-ttu-id="634c4-148">UNC yollarının her zaman tam olarak nitelenmiş olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="634c4-148">UNC paths must always be fully qualified.</span></span> <span data-ttu-id="634c4-149">Bunlara göreli Dizin kesimleri (`.` ve `..`) dahil edebilirler, ancak bunların tam nitelikli bir yolun parçası olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="634c4-149">They can include relative directory segments (`.` and `..`), but these must be part of a fully qualified path.</span></span> <span data-ttu-id="634c4-150">Yalnızca bir UNC yolunu bir sürücü harfine eşleyerek göreli yolları kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-150">You can use relative paths only by mapping a UNC path to a drive letter.</span></span>

## <a name="dos-device-paths"></a><span data-ttu-id="634c4-151">DOS cihaz yolları</span><span class="sxs-lookup"><span data-stu-id="634c4-151">DOS device paths</span></span>

<span data-ttu-id="634c4-152">Windows işletim sistemi, dosyalar dahil olmak üzere tüm kaynaklara işaret eden birleştirilmiş bir nesne modeline sahiptir.</span><span class="sxs-lookup"><span data-stu-id="634c4-152">The Windows operating system has a unified object model that points to all resources, including files.</span></span> <span data-ttu-id="634c4-153">Bu nesne yollarına konsol penceresinden erişilebilir ve eski DOS ve UNC yollarının eşlendiği özel bir sembolik bağlantı klasörü aracılığıyla Win32 katmanına açıktır.</span><span class="sxs-lookup"><span data-stu-id="634c4-153">These object paths are accessible from the console window and are exposed to the Win32 layer through a special folder of symbolic links that legacy DOS and UNC paths are mapped to.</span></span> <span data-ttu-id="634c4-154">Bu özel klasöre, aşağıdakilerden biri olan DOS cihaz yolu sözdizimi kullanılarak erişilir:</span><span class="sxs-lookup"><span data-stu-id="634c4-154">This special folder is accessed via the DOS device path syntax, which is one of:</span></span>

`\\.\C:\Test\Foo.txt`  
`\\?\C:\Test\Foo.txt`

<span data-ttu-id="634c4-155">Bir sürücüyü sürücü harfine göre tanımlamaya ek olarak, birim GUID 'sini kullanarak bir birimi belirleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-155">In addition to identifying a drive by its drive letter, you can identify a volume by using its volume GUID.</span></span> <span data-ttu-id="634c4-156">Bu formu alır:</span><span class="sxs-lookup"><span data-stu-id="634c4-156">This takes the form:</span></span>

`\\.\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`
`\\?\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`

> [!NOTE]
> <span data-ttu-id="634c4-157">DOS cihaz yolu sözdizimi, .NET Core 1,1 ve .NET Framework 4.6.2 ile başlayan Windows üzerinde çalışan .NET uygulamalarında desteklenir.</span><span class="sxs-lookup"><span data-stu-id="634c4-157">DOS device path syntax is supported on .NET implementations running on Windows starting with .NET Core 1.1 and .NET Framework 4.6.2.</span></span>

<span data-ttu-id="634c4-158">DOS cihaz yolu aşağıdaki bileşenlerden oluşur:</span><span class="sxs-lookup"><span data-stu-id="634c4-158">The DOS device path consists of the following components:</span></span>

- <span data-ttu-id="634c4-159">Yolu bir DOS cihaz yolu olarak tanımlayan cihaz yolu Belirleyicisi (`\\.\` veya `\\?\`).</span><span class="sxs-lookup"><span data-stu-id="634c4-159">The device path specifier (`\\.\` or `\\?\`), which identifies the path as a DOS device path.</span></span>

   > [!NOTE]
   > <span data-ttu-id="634c4-160">`\\?\` tüm .NET Core sürümlerinde .NET Framework ve 4.6.2 sürümünden itibaren desteklenir.</span><span class="sxs-lookup"><span data-stu-id="634c4-160">The `\\?\` is supported in all versions of .NET Core and in the .NET Framework starting with version 4.6.2.</span></span>

- <span data-ttu-id="634c4-161">"Gerçek" cihaz nesnesine (bir sürücü adı veya birim GUID 'SI olması durumunda {b75e2c83-0000-0000-0000-602f00000000} birimi) sembolik bir bağlantı.</span><span class="sxs-lookup"><span data-stu-id="634c4-161">A symbolic link to the "real" device object (C: in the case of a drive name, or Volume{b75e2c83-0000-0000-0000-602f00000000} in the case of a volume GUID).</span></span>

   <span data-ttu-id="634c4-162">Cihaz yolu belirticisinden sonra DOS cihaz yolunun ilk segmenti birimi veya sürücüyü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-162">The first segment of the DOS device path after the device path specifier identifies the volume or drive.</span></span> <span data-ttu-id="634c4-163">(Örneğin, `\\?\C:\` ve `\\.\BootPartition\`.)</span><span class="sxs-lookup"><span data-stu-id="634c4-163">(For example, `\\?\C:\` and `\\.\BootPartition\`.)</span></span>

   <span data-ttu-id="634c4-164">UNCs 'Ler için, `UNC`olarak adlandırılan belirli bir bağlantı vardır.</span><span class="sxs-lookup"><span data-stu-id="634c4-164">There is a specific link for UNCs that is called, not surprisingly, `UNC`.</span></span> <span data-ttu-id="634c4-165">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="634c4-165">For example:</span></span>

  `\\.\UNC\Server\Share\Test\Foo.txt`  
  `\\?\UNC\Server\Share\Test\Foo.txt`

    <span data-ttu-id="634c4-166">Cihaz UNCs için, sunucu/paylaşma bölümü birimi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="634c4-166">For device UNCs, the server/share portion forms the volume.</span></span> <span data-ttu-id="634c4-167">Örneğin, `\\?\server1\e:\utilities\\filecomparer\`, sunucu/paylaşma bölümü server1\utilities.</span><span class="sxs-lookup"><span data-stu-id="634c4-167">For example, in `\\?\server1\e:\utilities\\filecomparer\`, the server/share portion is server1\utilities.</span></span> <span data-ttu-id="634c4-168">Göreli Dizin kesimlerine sahip <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> gibi bir yöntemi çağırırken bu çok önemlidir; birimin ötesinde gezinmek hiçbir şekilde mümkün değildir.</span><span class="sxs-lookup"><span data-stu-id="634c4-168">This is significant when calling a method such as <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> with relative directory segments; it is never possible to navigate past the volume.</span></span> 

<span data-ttu-id="634c4-169">DOS cihaz yolları tanımına göre tam olarak nitelenir.</span><span class="sxs-lookup"><span data-stu-id="634c4-169">DOS device paths are fully qualified by definition.</span></span> <span data-ttu-id="634c4-170">Göreli Dizin kesimlerine (`.` ve `..`) izin verilmez.</span><span class="sxs-lookup"><span data-stu-id="634c4-170">Relative directory segments (`.` and `..`) are not allowed.</span></span> <span data-ttu-id="634c4-171">Geçerli dizinler, kullanımlarına hiçbir şekilde girmez.</span><span class="sxs-lookup"><span data-stu-id="634c4-171">Current directories never enter into their usage.</span></span>

## <a name="example-ways-to-refer-to-the-same-file"></a><span data-ttu-id="634c4-172">Örnek: aynı dosyaya başvurmanın yolları</span><span class="sxs-lookup"><span data-stu-id="634c4-172">Example: Ways to refer to the same file</span></span>

<span data-ttu-id="634c4-173">Aşağıdaki örnek, <xref:System.IO> ad alanında API 'Leri kullanırken bir dosyaya başvurmak için kullanabileceğiniz bazı yolları göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="634c4-173">The following example illustrates some of the ways in which you can refer to a file when using the APIs in the <xref:System.IO> namespace.</span></span> <span data-ttu-id="634c4-174">Örnek, bir <xref:System.IO.FileInfo> nesnesini örnekleyen ve dosyanın dosya adını ve uzunluğunu göstermek için <xref:System.IO.FileInfo.Name> ve <xref:System.IO.FileInfo.Length> özelliklerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-174">The example instantiates a <xref:System.IO.FileInfo> object and uses its <xref:System.IO.FileInfo.Name> and <xref:System.IO.FileInfo.Length> properties to display the filename and the length of the file.</span></span>

[!code-csharp[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/cs/file-refs.cs)]
[!code-vb[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/vb/file-refs.vb)]

## <a name="path-normalization"></a><span data-ttu-id="634c4-175">Yol normalleştirme</span><span class="sxs-lookup"><span data-stu-id="634c4-175">Path normalization</span></span>

<span data-ttu-id="634c4-176">Windows API 'Lerine geçirilen neredeyse tüm yollar normalleştirilir.</span><span class="sxs-lookup"><span data-stu-id="634c4-176">Almost all paths passed to Windows APIs are normalized.</span></span> <span data-ttu-id="634c4-177">Normalleştirme sırasında, Windows aşağıdaki adımları gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="634c4-177">During normalization, Windows performs the following steps:</span></span>

- <span data-ttu-id="634c4-178">Yolu tanımlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-178">Identifies the path.</span></span>
- <span data-ttu-id="634c4-179">Geçerli dizini kısmen nitelenmiş (göreli) yollara uygular.</span><span class="sxs-lookup"><span data-stu-id="634c4-179">Applies the current directory to partially qualified (relative) paths.</span></span>
- <span data-ttu-id="634c4-180">Canonicalizes bileşeni ve Dizin ayırıcıları.</span><span class="sxs-lookup"><span data-stu-id="634c4-180">Canonicalizes component and directory separators.</span></span>
- <span data-ttu-id="634c4-181">Göreli Dizin bileşenlerini değerlendirir (geçerli dizin için`.` ve üst dizin için `..`).</span><span class="sxs-lookup"><span data-stu-id="634c4-181">Evaluates relative directory components (`.` for the current directory and `..` for the parent directory).</span></span>
- <span data-ttu-id="634c4-182">Belirli karakterleri kırpar.</span><span class="sxs-lookup"><span data-stu-id="634c4-182">Trims certain characters.</span></span>

<span data-ttu-id="634c4-183">Bu normalleştirme örtük bir şekilde gerçekleşir, ancak [GetFullPathName () işlevine](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea)yapılan çağrıyı sarmalayan <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> yöntemini çağırarak bunu açıkça yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-183">This normalization happens implicitly, but you can do it explicitly by calling the <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> method, which wraps a call to the  [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span> <span data-ttu-id="634c4-184">Ayrıca, P/Invoke kullanarak doğrudan Windows [GetFullPathName () işlevini](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-184">You can also call the Windows [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) directly using P/Invoke.</span></span>

### <a name="identifying-the-path"></a><span data-ttu-id="634c4-185">Yolu tanımlama</span><span class="sxs-lookup"><span data-stu-id="634c4-185">Identifying the path</span></span>

<span data-ttu-id="634c4-186">Yol normalleştirmede ilk adım yolun türünü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-186">The first step in path normalization is identifying the type of path.</span></span> <span data-ttu-id="634c4-187">Yollar birkaç kategoriden birinde yer almalıdır:</span><span class="sxs-lookup"><span data-stu-id="634c4-187">Paths fall into one of a few categories:</span></span>

- <span data-ttu-id="634c4-188">Bunlar cihaz yollardır; diğer bir deyişle, iki ayırıcıya ve soru işareti ya da nokta (`\\?` ya da `\\.`) ile başlarlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-188">They are device paths; that is, they begin with two separators and a question mark or period (`\\?` or `\\.`).</span></span>
- <span data-ttu-id="634c4-189">Bunlar UNC yollardır; diğer bir deyişle, bir soru işareti veya dönem olmadan iki ayırıcıya başlarlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-189">They are UNC paths; that is, they begin with two separators without a question mark or period.</span></span> 
- <span data-ttu-id="634c4-190">Bunlar tamamen nitelikli DOS yollarıdır; diğer bir deyişle, bir sürücü harfi, bir birim ayırıcısı ve bir bileşen ayırıcısı (`C:\`) ile başlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-190">They are fully qualified DOS paths; that is, they begin with a drive letter, a volume separator, and a component separator (`C:\`).</span></span>
- <span data-ttu-id="634c4-191">Eski bir cihaz (`CON`, `LPT1`) tasarlarlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-191">They designate a legacy device (`CON`, `LPT1`).</span></span>
- <span data-ttu-id="634c4-192">Bunlar geçerli sürücünün köküne görelidir; diğer bir deyişle, tek bir bileşen ayırıcısı (`\`) ile başlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-192">They are relative to the root of the current drive; that is, they begin with a single component separator (`\`).</span></span>
- <span data-ttu-id="634c4-193">Bunlar, belirtilen sürücünün geçerli dizinine göre belirlenir; diğer bir deyişle, bir sürücü harfi, bir birim ayırıcısı ve bileşen ayırıcısı (`C:`) ile başlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-193">They are relative to the current directory of a specified drive; that is, they begin with a drive letter, a volume separator, and no component separator (`C:`).</span></span>
- <span data-ttu-id="634c4-194">Bunlar geçerli dizine göre değişir; diğer bir deyişle, başka bir şeyle (`temp\testfile.txt`) başlarlar.</span><span class="sxs-lookup"><span data-stu-id="634c4-194">They are relative to the current directory; that is, they begin with anything else (`temp\testfile.txt`).</span></span>

<span data-ttu-id="634c4-195">Yolun türü, geçerli bir dizinin bir biçimde uygulanıp uygulanmadığı belirler.</span><span class="sxs-lookup"><span data-stu-id="634c4-195">The type of the path determines whether or not a current directory is applied in some way.</span></span> <span data-ttu-id="634c4-196">Ayrıca, yolun "kökünün" ne olduğunu da belirler.</span><span class="sxs-lookup"><span data-stu-id="634c4-196">It also determines what the "root" of the path is.</span></span>

### <a name="handling-legacy-devices"></a><span data-ttu-id="634c4-197">Eski cihazları işleme</span><span class="sxs-lookup"><span data-stu-id="634c4-197">Handling legacy devices</span></span>

<span data-ttu-id="634c4-198">Yol, `CON`, `COM1`veya `LPT1`gibi eski bir DOS cihazından, ön bekleyen `\\.\` tarafından bir cihaz yoluna dönüştürülür ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="634c4-198">If the path is a legacy DOS device such as `CON`, `COM1`, or `LPT1`, it is converted into a device path by prepending `\\.\` and returned.</span></span> 

<span data-ttu-id="634c4-199">Eski cihaz adı ile başlayan bir yol <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> yöntemi tarafından her zaman eski bir cihaz olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-199">A path that begins with a legacy device name is always interpreted as a legacy device by the <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="634c4-200">Örneğin, `CON.TXT` için DOS cihaz yolu `\\.\CON`ve `COM1.TXT\file1.txt` DOS cihaz yolu `\\.\COM1`.</span><span class="sxs-lookup"><span data-stu-id="634c4-200">For example, the DOS device path for `CON.TXT` is `\\.\CON`, and the DOS device path for `COM1.TXT\file1.txt` is `\\.\COM1`.</span></span>

### <a name="applying-the-current-directory"></a><span data-ttu-id="634c4-201">Geçerli dizin uygulanıyor</span><span class="sxs-lookup"><span data-stu-id="634c4-201">Applying the current directory</span></span>

<span data-ttu-id="634c4-202">Bir yol tam nitelikli değilse, Windows geçerli dizini bu dizine uygular.</span><span class="sxs-lookup"><span data-stu-id="634c4-202">If a path isn't fully qualified, Windows applies the current directory to it.</span></span> <span data-ttu-id="634c4-203">UNCs ve cihaz yollarına geçerli dizin uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="634c4-203">UNCs and device paths do not have the current directory applied.</span></span> <span data-ttu-id="634c4-204">, Ayırıcısı C:\\olan tam bir sürücü yapmaz.</span><span class="sxs-lookup"><span data-stu-id="634c4-204">Neither does a full drive with separator C:\\.</span></span>

<span data-ttu-id="634c4-205">Yol tek bir bileşen ayırıcısıyla başlıyorsa, geçerli dizindeki sürücü uygulanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-205">If the path starts with a single component separator, the drive from the current directory is applied.</span></span> <span data-ttu-id="634c4-206">Örneğin, dosya yolu `\utilities` ve geçerli dizin `C:\temp\`, normalleştirme `C:\utilities`üretir.</span><span class="sxs-lookup"><span data-stu-id="634c4-206">For example, if the file path is `\utilities` and the current directory is `C:\temp\`, normalization produces `C:\utilities`.</span></span>

<span data-ttu-id="634c4-207">Yol bir sürücü harfi, birim ayırıcısı ve bileşen ayırıcısı ile başlıyorsa, belirtilen sürücü için komut kabuğundan ayarlanan son geçerli dizin uygulanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-207">If the path starts with a drive letter, volume separator, and no component separator, the last current directory set from the command shell for the specified drive is applied.</span></span> <span data-ttu-id="634c4-208">Son geçerli dizin ayarlanmamışsa, tek başına sürücü uygulanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-208">If the last current directory was not set, the drive alone is applied.</span></span> <span data-ttu-id="634c4-209">Örneğin, dosya yolu `D:sources`, geçerli dizin `C:\Documents\`ve D: sürücüsündeki son geçerli dizin `D:\sources\`, sonuç ise `D:\sources\sources`.</span><span class="sxs-lookup"><span data-stu-id="634c4-209">For example, if the file path is `D:sources`, the current directory is `C:\Documents\`, and the last current directory on drive D: was `D:\sources\`, the result is `D:\sources\sources`.</span></span> <span data-ttu-id="634c4-210">Bu "sürücü göreli" yolları, yaygın olarak kullanılan bir program ve betik mantığı hataları kaynağıdır.</span><span class="sxs-lookup"><span data-stu-id="634c4-210">These "drive relative" paths are a common source of program and script logic errors.</span></span> <span data-ttu-id="634c4-211">Bir harf ile başlayan bir yolun ve iki nokta üst üste doğru olmadığını varsayarsak</span><span class="sxs-lookup"><span data-stu-id="634c4-211">Assuming that a path beginning with a letter and a colon isn't relative is obviously not correct.</span></span>

<span data-ttu-id="634c4-212">Yol, ayırıcı dışında bir şeyle başlıyorsa, geçerli sürücü ve geçerli dizin uygulanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-212">If the path starts with something other than a separator, the current drive and current directory are applied.</span></span> <span data-ttu-id="634c4-213">Örneğin, yol `filecompare` ve geçerli dizin `C:\utilities\`, sonuç `C:\utilities\filecompare\`olur.</span><span class="sxs-lookup"><span data-stu-id="634c4-213">For example, if the path is `filecompare` and the current directory is `C:\utilities\`, the result is `C:\utilities\filecompare\`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="634c4-214">Geçerli dizin işlem başına bir ayar olduğundan, göreli yollar çok iş parçacıklı uygulamalarda (yani, çoğu uygulama) tehlikelidir.</span><span class="sxs-lookup"><span data-stu-id="634c4-214">Relative paths are dangerous in multithreaded applications (that is, most applications) because the current directory is a per-process setting.</span></span> <span data-ttu-id="634c4-215">Herhangi bir iş parçacığı herhangi bir zamanda geçerli dizini değiştirebilir.</span><span class="sxs-lookup"><span data-stu-id="634c4-215">Any thread can change the current directory at any time.</span></span> <span data-ttu-id="634c4-216">.NET Core 2,1 ' den itibaren, bir göreli yol ve kendisine karşı çözümlemek istediğiniz temel yol (geçerli dizin) için bir mutlak yol almak üzere <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> yöntemini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="634c4-216">Starting with .NET Core 2.1, you can call the <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> method to get an absolute path from a relative path and the base path (the current directory) that you want to resolve it against.</span></span> 

### <a name="canonicalizing-separators"></a><span data-ttu-id="634c4-217">Standart hale getirme ayırıcılar</span><span class="sxs-lookup"><span data-stu-id="634c4-217">Canonicalizing separators</span></span>

<span data-ttu-id="634c4-218">Tüm eğik çizgiler (`/`), standart Windows ayırıcısına dönüştürülür, ters eğik çizgi (`\`).</span><span class="sxs-lookup"><span data-stu-id="634c4-218">All forward slashes (`/`) are converted into the standard Windows separator, the back slash (`\`).</span></span> <span data-ttu-id="634c4-219">Varsa, ilk iki eğik çizgiyi izleyen eğik çizgi serileri tek eğik çizgi olarak daraltılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-219">If they are present, a series of slashes that follow the first two slashes are collapsed into a single slash.</span></span>

### <a name="evaluating-relative-components"></a><span data-ttu-id="634c4-220">Göreli bileşenleri değerlendirme</span><span class="sxs-lookup"><span data-stu-id="634c4-220">Evaluating relative components</span></span>

<span data-ttu-id="634c4-221">Yol işlendiğinde, tek veya çift dönemden (`.` veya `..`) oluşan tüm bileşenler veya segmentler değerlendirilir:</span><span class="sxs-lookup"><span data-stu-id="634c4-221">As the path is processed, any components or segments that are composed of a single or a double period (`.` or `..`) are evaluated:</span></span> 

- <span data-ttu-id="634c4-222">Tek bir dönem için geçerli kesim, geçerli dizine başvurduğundan kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-222">For a single period, the current segment is removed, since it refers to the current directory.</span></span>

- <span data-ttu-id="634c4-223">Çift dönem için, çift nokta üst dizine başvurduğundan, geçerli segment ve üst kesim kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-223">For a double period, the current segment and the parent segment are removed, since the double period refers to the parent directory.</span></span>

   <span data-ttu-id="634c4-224">Üst dizinler yalnızca yolun kökünü geçmemişse kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-224">Parent directories are only removed if they aren't past the root of the path.</span></span> <span data-ttu-id="634c4-225">Yolun kökü, yol türüne bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="634c4-225">The root of the path depends on the type of path.</span></span> <span data-ttu-id="634c4-226">DOS yolları için sürücü (`C:\`), UNCs için sunucu/paylaşma (`\\Server\Share`) ve cihaz yolları için cihaz yolu ön eki (`\\?\` veya `\\.\`).</span><span class="sxs-lookup"><span data-stu-id="634c4-226">It is the drive (`C:\`) for DOS paths, the server/share for UNCs (`\\Server\Share`), and the device path prefix for device paths (`\\?\` or `\\.\`).</span></span>

### <a name="trimming-characters"></a><span data-ttu-id="634c4-227">Karakterleri kırpma</span><span class="sxs-lookup"><span data-stu-id="634c4-227">Trimming characters</span></span>

<span data-ttu-id="634c4-228">Daha önce kaldırılan ayırıcıların ve göreli parçaların çalıştırılmalarının yanı sıra, normalleştirme sırasında bazı ek karakterler kaldırılır:</span><span class="sxs-lookup"><span data-stu-id="634c4-228">Along with the runs of separators and relative segments removed earlier, some additional characters are removed during normalization:</span></span>

- <span data-ttu-id="634c4-229">Bir segment tek bir dönemde sona ererse bu süre kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-229">If a segment ends in a single period, that period is removed.</span></span> <span data-ttu-id="634c4-230">(Tek veya çift dönemin bir segmenti, önceki adımda normalleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="634c4-230">(A segment of a single or double period is normalized in the previous step.</span></span> <span data-ttu-id="634c4-231">Üç veya daha fazla dönemin bir segmenti normalleştirilmez ve aslında geçerli bir dosya/dizin adıdır.)</span><span class="sxs-lookup"><span data-stu-id="634c4-231">A segment of three or more periods is not normalized and is actually a valid file/directory name.)</span></span>

- <span data-ttu-id="634c4-232">Yol bir ayırıcıda sonlanmazsa, tüm sondaki noktalar ve boşluklar (U + 0020) kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="634c4-232">If the path doesn't end in a separator, all trailing periods and spaces (U+0020) are removed.</span></span> <span data-ttu-id="634c4-233">Son segment yalnızca tek veya çift bir nokta ise, yukarıdaki ilgili bileşenler kuralının altına girer.</span><span class="sxs-lookup"><span data-stu-id="634c4-233">If the last segment is simply a single or double period, it falls under the relative components rule above.</span></span> 

   <span data-ttu-id="634c4-234">Bu kural, boşluktan sonra sondaki bir ayırıcı ekleyerek sonunda boşluk olan bir dizin adı oluşturabileceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="634c4-234">This rule means that you can create a directory name with a trailing space by adding a trailing separator after the space.</span></span>  

   > [!IMPORTANT]
   > <span data-ttu-id="634c4-235">Sonunda boşluk olan bir dizin veya dosya **adı oluşturmanız gerekir** .</span><span class="sxs-lookup"><span data-stu-id="634c4-235">You should **never** create a directory or filename with a trailing space.</span></span> <span data-ttu-id="634c4-236">Sondaki boşluklar, bir dizine erişmek zor veya olanaksız hale gelir ve uygulamalar genellikle boşluklar içeren dizinleri veya dosyaları işlemeye çalışırken başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="634c4-236">Trailing spaces can make it difficult or impossible to access a directory, and applications commonly fail when attempting to handle directories or files whose names include trailing spaces.</span></span>

## <a name="skipping-normalization"></a><span data-ttu-id="634c4-237">Normalleştirme atlanıyor</span><span class="sxs-lookup"><span data-stu-id="634c4-237">Skipping normalization</span></span>

<span data-ttu-id="634c4-238">Normalde, bir Windows API 'sine geçirilen tüm yol, [GetFullPathName işlevine](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) ve normalleştirilmesine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="634c4-238">Normally, any path passed to a Windows API is (effectively) passed to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) and normalized.</span></span> <span data-ttu-id="634c4-239">Önemli bir özel durum vardır: bir dönem yerine soru işaretiyle başlayan bir cihaz yolu.</span><span class="sxs-lookup"><span data-stu-id="634c4-239">There is one important exception: a device path that begins with a question mark instead of a period.</span></span> <span data-ttu-id="634c4-240">Yol `\\?\` ile tamamen başlıyorsa (kurallı ters eğik çizginin kullanımını aklınızda bulunur), normalleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="634c4-240">Unless the path starts exactly with `\\?\` (note the use of the canonical backslash), it is normalized.</span></span>

<span data-ttu-id="634c4-241">Neden normalleştirmeyi atlamak istiyorsunuz?</span><span class="sxs-lookup"><span data-stu-id="634c4-241">Why would you want to skip normalization?</span></span> <span data-ttu-id="634c4-242">Üç önemli neden vardır:</span><span class="sxs-lookup"><span data-stu-id="634c4-242">There are three major reasons:</span></span>

1. <span data-ttu-id="634c4-243">Normalde kullanılamayan ancak geçerli olan yollara erişim sağlamak için.</span><span class="sxs-lookup"><span data-stu-id="634c4-243">To get access to paths that are normally unavailable but are legal.</span></span> <span data-ttu-id="634c4-244">`hidden.`adlı bir dosya veya dizin, örneğin, başka bir şekilde erişim imkansızdır.</span><span class="sxs-lookup"><span data-stu-id="634c4-244">A file or directory called `hidden.`, for example, is impossible to access in any other way.</span></span> 

1. <span data-ttu-id="634c4-245">Zaten normalleştirdiyseniz normalleştirmeyi atlayarak performansı artırmak için.</span><span class="sxs-lookup"><span data-stu-id="634c4-245">To improve performance by skipping normalization if you've already normalized.</span></span>

1. <span data-ttu-id="634c4-246">Yalnızca .NET Framework, 259 karakterden daha büyük yollara izin vermek üzere yol uzunluğu denetimini `MAX_PATH` atlamak için.</span><span class="sxs-lookup"><span data-stu-id="634c4-246">On the .NET Framework only, to skip the `MAX_PATH` check for path length to allow for paths that are greater than 259 characters.</span></span> <span data-ttu-id="634c4-247">Çoğu API, bazı özel durumlarla birlikte buna izin verir.</span><span class="sxs-lookup"><span data-stu-id="634c4-247">Most APIs allow this, with some exceptions.</span></span>

> [!NOTE]
> <span data-ttu-id="634c4-248">.NET Core, uzun yolları örtük olarak işler ve `MAX_PATH` denetimi gerçekleştirmez.</span><span class="sxs-lookup"><span data-stu-id="634c4-248">.NET Core handles long paths implicitly and does not perform a `MAX_PATH` check.</span></span> <span data-ttu-id="634c4-249">`MAX_PATH` denetimi yalnızca .NET Framework uygulanır.</span><span class="sxs-lookup"><span data-stu-id="634c4-249">The `MAX_PATH` check applies only to the .NET Framework.</span></span>

<span data-ttu-id="634c4-250">Normalleştirme atlama ve en yüksek yol denetimleri, iki cihaz yolu sözdizimleri arasındaki tek farktır; Aksi halde özdeş.</span><span class="sxs-lookup"><span data-stu-id="634c4-250">Skipping normalization and max path checks is the only difference between the two device path syntaxes; they are otherwise identical.</span></span> <span data-ttu-id="634c4-251">"Normal" uygulamalar için zor olan yolları kolayca oluşturabildiklerinden, normalleştirmeyi atlama konusunda dikkatli olun.</span><span class="sxs-lookup"><span data-stu-id="634c4-251">Be careful with skipping normalization, since you can easily create paths that are difficult for "normal" applications to deal with.</span></span>

<span data-ttu-id="634c4-252">`\\?\` ile başlayan yollar, bunları [GetFullPathName işlevine](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea)açıkça geçirdiğinizde normalleştirmeye devam eder.</span><span class="sxs-lookup"><span data-stu-id="634c4-252">Paths that start with `\\?\` are still normalized if you explicitly pass them to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span>

<span data-ttu-id="634c4-253">`\\?\`olmadan [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) 'e `MAX_PATH` karakterden daha fazla yol kullanabileceğinizi unutmayın.</span><span class="sxs-lookup"><span data-stu-id="634c4-253">Note that you can paths of more than `MAX_PATH` characters to [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) without `\\?\`.</span></span> <span data-ttu-id="634c4-254">Windows 'un işleyebileceği en büyük dize boyutuna kadar rastgele uzunluk yollarını destekler.</span><span class="sxs-lookup"><span data-stu-id="634c4-254">It supports arbitrary length paths up to the maximum string size that Windows can handle.</span></span>

## <a name="case-and-the-windows-file-system"></a><span data-ttu-id="634c4-255">Büyük/küçük harf ve Windows dosya sistemi</span><span class="sxs-lookup"><span data-stu-id="634c4-255">Case and the Windows file system</span></span>

<span data-ttu-id="634c4-256">Windows dosya sisteminin, Windows olmayan kullanıcılar ve geliştiricilerin kafa karıştırıcı olduğunu fark eden, yol ve dizin adlarının büyük/küçük harfe duyarlı olduğunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="634c4-256">A peculiarity of the Windows file system that non-Windows users and developers find confusing is that path and directory names are case-insensitive.</span></span> <span data-ttu-id="634c4-257">Diğer bir deyişle, dizin ve dosya adları, oluşturulduklarında kullanılan dizelerin büyük küçük harflerini yansıtır.</span><span class="sxs-lookup"><span data-stu-id="634c4-257">That is, directory and file names reflect the casing of the strings used when they are created.</span></span> <span data-ttu-id="634c4-258">Örneğin, yöntem çağrısı</span><span class="sxs-lookup"><span data-stu-id="634c4-258">For example, the method call</span></span>

```csharp
Directory.Create("TeStDiReCtOrY");
```

```vb
Directory.Create("TeStDiReCtOrY")
```

<span data-ttu-id="634c4-259">TeStDiReCtOrY adlı bir dizin oluşturur.</span><span class="sxs-lookup"><span data-stu-id="634c4-259">creates a directory named TeStDiReCtOrY.</span></span> <span data-ttu-id="634c4-260">Bir dizini veya dosyayı durumunu değiştirmek için yeniden adlandırırsanız, dizin veya dosya adı, yeniden adlandırdığınızda kullanılan dize durumunu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="634c4-260">If you rename a directory or file to change its case, the directory or file name reflects the case of the string used when you rename it.</span></span> <span data-ttu-id="634c4-261">Örneğin, aşağıdaki kod test. txt adlı dosyayı test. txt öğesine yeniden adlandırır:</span><span class="sxs-lookup"><span data-stu-id="634c4-261">For example, the following code renames a file named test.txt to Test.txt:</span></span>

[!code-csharp[case-and-renaming](~/samples/snippets/standard/io/file-names/cs/rename.cs)]
[!code-vb[case-and-renaming](~/samples/snippets/standard/io/file-names/vb/rename.vb)]

<span data-ttu-id="634c4-262">Bununla birlikte, dizin ve dosya adı karşılaştırmaları büyük/küçük harfe duyarlıdır.</span><span class="sxs-lookup"><span data-stu-id="634c4-262">However, directory and file name comparisons are case-insensitive.</span></span> <span data-ttu-id="634c4-263">"Test. txt" adlı bir dosyayı arıyorsanız, .NET dosya sistemi API 'Leri karşılaştırmayla ilgili büyük/küçük harf durumunu yoksayar.</span><span class="sxs-lookup"><span data-stu-id="634c4-263">If you search for a file named "test.txt", .NET file system APIs ignore case in the comparison.</span></span> <span data-ttu-id="634c4-264">Test. txt, TEST. TXT, test. TXT ve büyük ve küçük harflerin diğer birleşimleri "test. txt" ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="634c4-264">Test.txt, TEST.TXT, test.TXT, and any other combination of upper- and lowercase letters will match "test.txt".</span></span>
