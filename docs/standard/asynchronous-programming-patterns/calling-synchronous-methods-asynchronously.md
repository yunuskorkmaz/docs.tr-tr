---
title: Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
helpviewer_keywords:
- asynchronous programming, delegates
- asynchronous delegates
- AsyncWaitHandle property
- callback methods
- calling synchronous methods in asynchronous manner
- WaitHandle class, code examples
- asynchronous programming, status polling
- polling asynchronous operation status
- delegates [.NET Framework], asynchronous
- synchronous calling in asynchronous manner
- waiting for asynchronous calls
- status information [.NET Framework], asynchronous operations
ms.assetid: 41972034-92ed-450a-9664-ab93fcc6f1fb
ms.openlocfilehash: 06df584f0120fbd4978e18647854a3ee844a2095
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2020
ms.locfileid: "73105123"
---
# <a name="calling-synchronous-methods-asynchronously"></a><span data-ttu-id="061db-102">Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma</span><span class="sxs-lookup"><span data-stu-id="061db-102">Calling Synchronous Methods Asynchronously</span></span>

<span data-ttu-id="061db-103">.NET Framework, herhangi bir yöntemi eşzamanlı olarak aramanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="061db-103">The .NET Framework enables you to call any method asynchronously.</span></span> <span data-ttu-id="061db-104">Bunu yapmak için aramak istediğiniz yöntemle aynı imzaya sahip bir temsilci tanımlarsınız; ortak dil çalışma süresi otomatik `BeginInvoke` `EndInvoke` olarak tanımlar ve uygun imzaları ile bu temsilci için yöntemler.</span><span class="sxs-lookup"><span data-stu-id="061db-104">To do this you define a delegate with the same signature as the method you want to call; the common language runtime automatically defines `BeginInvoke` and `EndInvoke` methods for this delegate, with the appropriate signatures.</span></span>

> [!NOTE]
> <span data-ttu-id="061db-105">Eşzamanlı temsilci çağrıları, özellikle `BeginInvoke` .net `EndInvoke` Compact Framework'de desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="061db-105">Asynchronous delegate calls, specifically the `BeginInvoke` and `EndInvoke` methods, are not supported in the .NET Compact Framework.</span></span>

<span data-ttu-id="061db-106">Yöntem, `BeginInvoke` eşzamanlı çağrıyı başlatır.</span><span class="sxs-lookup"><span data-stu-id="061db-106">The `BeginInvoke` method initiates the asynchronous call.</span></span> <span data-ttu-id="061db-107">Eş senkronize olarak yürütmek istediğiniz yöntemle aynı parametrelere ve iki ek isteğe bağlı parametreye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="061db-107">It has the same parameters as the method that you want to execute asynchronously, plus two additional optional parameters.</span></span> <span data-ttu-id="061db-108">İlk parametre, <xref:System.AsyncCallback> eşyoknöz çağrı tamamlandığında çağrılacak bir yönteme başvuran bir temsilcidir.</span><span class="sxs-lookup"><span data-stu-id="061db-108">The first parameter is an <xref:System.AsyncCallback> delegate that references a method to be called when the asynchronous call completes.</span></span> <span data-ttu-id="061db-109">İkinci parametre, bilgileri geri arama yöntemine aktaran kullanıcı tanımlı bir nesnedir.</span><span class="sxs-lookup"><span data-stu-id="061db-109">The second parameter is a user-defined object that passes information into the callback method.</span></span> <span data-ttu-id="061db-110">`BeginInvoke`hemen döner ve eşzamanlı aramanın tamamlanmasını beklemez.</span><span class="sxs-lookup"><span data-stu-id="061db-110">`BeginInvoke` returns immediately and does not wait for the asynchronous call to complete.</span></span> <span data-ttu-id="061db-111">`BeginInvoke`eşzamanlı <xref:System.IAsyncResult>aramanın ilerlemesini izlemek için kullanılabilecek bir , döndürür.</span><span class="sxs-lookup"><span data-stu-id="061db-111">`BeginInvoke` returns an <xref:System.IAsyncResult>, which can be used to monitor the progress of the asynchronous call.</span></span>

<span data-ttu-id="061db-112">Yöntem, `EndInvoke` eşzamanlı aramanın sonuçlarını alır.</span><span class="sxs-lookup"><span data-stu-id="061db-112">The `EndInvoke` method retrieves the results of the asynchronous call.</span></span> <span data-ttu-id="061db-113">Sonra her zaman `BeginInvoke`çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="061db-113">It can be called any time after `BeginInvoke`.</span></span> <span data-ttu-id="061db-114">Eşkron arama tamamlanmadıysa, `EndInvoke` arama iş parçacığı tamamlanana kadar engeller.</span><span class="sxs-lookup"><span data-stu-id="061db-114">If the asynchronous call has not completed, `EndInvoke` blocks the calling thread until it completes.</span></span> <span data-ttu-id="061db-115">`EndInvoke` `out` Parametreleri ve `ref` parametreleri`<Out>` `ByRef` (ve `ByRef` Visual Basic) eşzamanlı olarak yürütmek istediğiniz yöntemin yanı <xref:System.IAsyncResult> sıra `BeginInvoke`döndürülen yöntemi içerir.</span><span class="sxs-lookup"><span data-stu-id="061db-115">The parameters of `EndInvoke` include the `out` and `ref` parameters (`<Out>` `ByRef` and `ByRef` in Visual Basic) of the method that you want to execute asynchronously, plus the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span>

> [!NOTE]
> <span data-ttu-id="061db-116">Visual Studio'daki `BeginInvoke` `EndInvoke`IntelliSense özelliği,</span><span class="sxs-lookup"><span data-stu-id="061db-116">The IntelliSense feature in Visual Studio displays the parameters of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="061db-117">Visual Studio veya benzer bir araç kullanmıyorsanız veya Visual Studio ile C# kullanıyorsanız, bu yöntemler için tanımlanan parametrelerin açıklaması için [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="061db-117">If you're not using Visual Studio or a similar tool, or if you're using C# with Visual Studio, see [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) for a description of the parameters defined for these methods.</span></span>

<span data-ttu-id="061db-118">Bu konudaki kod örnekleri, eşkron `BeginInvoke` `EndInvoke` arama yapmak ve kullanmak için dört yaygın yol gösterir.</span><span class="sxs-lookup"><span data-stu-id="061db-118">The code examples in this topic demonstrate four common ways to use `BeginInvoke` and `EndInvoke` to make asynchronous calls.</span></span> <span data-ttu-id="061db-119">Aradıktan `BeginInvoke` sonra aşağıdakileri yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="061db-119">After calling `BeginInvoke` you can do the following:</span></span>

- <span data-ttu-id="061db-120">Biraz iş yapın `EndInvoke` ve arama tamamlanana kadar engellemeyi arayın.</span><span class="sxs-lookup"><span data-stu-id="061db-120">Do some work and then call `EndInvoke` to block until the call completes.</span></span>

- <span data-ttu-id="061db-121"><xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> Özelliği <xref:System.Threading.WaitHandle> kullanarak edinin, sinyal <xref:System.Threading.WaitHandle.WaitOne%2A> verilene kadar <xref:System.Threading.WaitHandle> yürütmeyi engellemek için `EndInvoke`yöntemini kullanın ve sonra arayın.</span><span class="sxs-lookup"><span data-stu-id="061db-121">Obtain a <xref:System.Threading.WaitHandle> using the <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> property, use its <xref:System.Threading.WaitHandle.WaitOne%2A> method to block execution until the <xref:System.Threading.WaitHandle> is signaled, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="061db-122">Eşzamanlı <xref:System.IAsyncResult> aramanın `BeginInvoke` ne zaman tamamlandığını belirlemek için döndürülen `EndInvoke`tarafından anket ve ardından arayın.</span><span class="sxs-lookup"><span data-stu-id="061db-122">Poll the <xref:System.IAsyncResult> returned by `BeginInvoke` to determine when the asynchronous call has completed, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="061db-123">Geri arama yöntemi için bir `BeginInvoke`temsilciyi ' ye geçirin.</span><span class="sxs-lookup"><span data-stu-id="061db-123">Pass a delegate for a callback method to `BeginInvoke`.</span></span> <span data-ttu-id="061db-124">Yöntem, eşkron <xref:System.Threading.ThreadPool> çağrı tamamlandığında bir iş parçacığı üzerinde yürütülür.</span><span class="sxs-lookup"><span data-stu-id="061db-124">The method is executed on a <xref:System.Threading.ThreadPool> thread when the asynchronous call completes.</span></span> <span data-ttu-id="061db-125">Geri arama yöntemi `EndInvoke`çağırır.</span><span class="sxs-lookup"><span data-stu-id="061db-125">The callback method calls `EndInvoke`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="061db-126">Hangi tekniği kullanırsanız kullanın, `EndInvoke` her zaman eşzamanlı aramanızı tamamlamak için arayın.</span><span class="sxs-lookup"><span data-stu-id="061db-126">No matter which technique you use, always call `EndInvoke` to complete your asynchronous call.</span></span>

## <a name="defining-the-test-method-and-asynchronous-delegate"></a><span data-ttu-id="061db-127">Test Yöntemini ve Eşzamanlı Temsilciyi Tanımlama</span><span class="sxs-lookup"><span data-stu-id="061db-127">Defining the Test Method and Asynchronous Delegate</span></span>
 <span data-ttu-id="061db-128">İzleyen kod örnekleri, `TestMethod`aynı uzun süren yöntemi eş zamanlı olarak adlandırmanın çeşitli yollarını gösterir.</span><span class="sxs-lookup"><span data-stu-id="061db-128">The code examples that follow demonstrate various ways of calling the same long-running method, `TestMethod`, asynchronously.</span></span> <span data-ttu-id="061db-129">Yöntem, `TestMethod` işleme başladığını, birkaç saniye uyuduğuve sonra sona erdiğini göstermek için bir konsol iletisi görüntüler.</span><span class="sxs-lookup"><span data-stu-id="061db-129">The `TestMethod` method displays a console message to show that it has begun processing, sleeps for a few seconds, and then ends.</span></span> <span data-ttu-id="061db-130">`TestMethod`bu `out` parametrelerin imzalara nasıl eklenmediğini gösteren bir `BeginInvoke` `EndInvoke`parametreye sahiptir ve .</span><span class="sxs-lookup"><span data-stu-id="061db-130">`TestMethod` has an `out` parameter to demonstrate the way such parameters are added to the signatures of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="061db-131">Parametreleri `ref` benzer şekilde işleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-131">You can handle `ref` parameters similarly.</span></span>

 <span data-ttu-id="061db-132">Aşağıdaki kod örneği, eşsenkronize olarak `AsyncMethodCaller` aramak `TestMethod` için kullanılabilecek tanımını `TestMethod` ve adlandırılmış temsilciyi gösterir.</span><span class="sxs-lookup"><span data-stu-id="061db-132">The following code example shows the definition of `TestMethod` and the delegate named `AsyncMethodCaller` that can be used to call `TestMethod` asynchronously.</span></span> <span data-ttu-id="061db-133">Kod örneklerini derlemek için, `TestMethod` temsilcinin tanımlarını `AsyncMethodCaller` ve temsilciyi eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="061db-133">To compile the code examples, you must include the definitions for `TestMethod` and the `AsyncMethodCaller` delegate.</span></span>

 [!code-cpp[AsyncDelegateExamples#1](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/TestMethod.cpp#1)]
 [!code-csharp[AsyncDelegateExamples#1](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/TestMethod.cs#1)]
 [!code-vb[AsyncDelegateExamples#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/TestMethod.vb#1)]

## <a name="waiting-for-an-asynchronous-call-with-endinvoke"></a><span data-ttu-id="061db-134">EndInvoke ile Eşzamanlı Arama Bekleniyor</span><span class="sxs-lookup"><span data-stu-id="061db-134">Waiting for an Asynchronous Call with EndInvoke</span></span>
 <span data-ttu-id="061db-135">Bir yöntemi eş senkronize olarak yürütmenin en basit yolu, temsilcinin `BeginInvoke` yöntemini çağırarak yöntemi yürütmeye başlamak, ana iş `EndInvoke` parçacığı üzerinde bazı işler yapmak ve ardından temsilcinin yöntemini çağırmaktır.</span><span class="sxs-lookup"><span data-stu-id="061db-135">The simplest way to execute a method asynchronously is to start executing the method by calling the delegate's `BeginInvoke` method, do some work on the main thread, and then call the delegate's `EndInvoke` method.</span></span> <span data-ttu-id="061db-136">`EndInvoke`eşyokkron çağrı tamamlanana kadar geri dönmediği için arama iş parçacığı engelleyebilir.</span><span class="sxs-lookup"><span data-stu-id="061db-136">`EndInvoke` might block the calling thread because it does not return until the asynchronous call completes.</span></span> <span data-ttu-id="061db-137">Bu, dosya veya ağ işlemleri ile kullanmak için iyi bir tekniktir.</span><span class="sxs-lookup"><span data-stu-id="061db-137">This is a good technique to use with file or network operations.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="061db-138">Engelleyebileceğinden, `EndInvoke` kullanıcı arabirimine hizmet veren iş parçacıklarından asla aramamalısınız.</span><span class="sxs-lookup"><span data-stu-id="061db-138">Because `EndInvoke` might block, you should never call it from threads that service the user interface.</span></span>

 [!code-cpp[AsyncDelegateExamples#2](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/EndInvoke.cpp#2)]
 [!code-csharp[AsyncDelegateExamples#2](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/EndInvoke.cs#2)]
 [!code-vb[AsyncDelegateExamples#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/EndInvoke.vb#2)]

## <a name="waiting-for-an-asynchronous-call-with-waithandle"></a><span data-ttu-id="061db-139">WaitHandle ile Eşzamanlı Arama Bekleniyor</span><span class="sxs-lookup"><span data-stu-id="061db-139">Waiting for an Asynchronous Call with WaitHandle</span></span>
 <span data-ttu-id="061db-140">Tarafından <xref:System.IAsyncResult> döndürülen <xref:System.Threading.WaitHandle> <xref:System.IAsyncResult.AsyncWaitHandle%2A> özelliğini kullanarak a `BeginInvoke`elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-140">You can obtain a <xref:System.Threading.WaitHandle> by using the <xref:System.IAsyncResult.AsyncWaitHandle%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span> <span data-ttu-id="061db-141">Eşkron <xref:System.Threading.WaitHandle> arama tamamlandığında sinyal verilir ve <xref:System.Threading.WaitHandle.WaitOne%2A> yöntemi arayarak bunu bekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-141">The <xref:System.Threading.WaitHandle> is signaled when the asynchronous call completes, and you can wait for it by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span>

 <span data-ttu-id="061db-142">Bir <xref:System.Threading.WaitHandle>, eşsenkronize arama tamamlanmadan önce veya sonra, ancak sonuçları almak `EndInvoke` için aramadan önce ek işleme gerçekleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-142">If you use a <xref:System.Threading.WaitHandle>, you can perform additional processing before or after the asynchronous call completes, but before calling `EndInvoke` to retrieve the results.</span></span>

> [!NOTE]
> <span data-ttu-id="061db-143">Arama yaptığınızda bekleme tutamacı otomatik `EndInvoke`olarak kapatılmaz.</span><span class="sxs-lookup"><span data-stu-id="061db-143">The wait handle is not closed automatically when you call `EndInvoke`.</span></span> <span data-ttu-id="061db-144">Bekleme tanıtıcısına yapılan tüm başvuruları serbest ederseniz, çöp toplama bekleme tutamacını geri aldığında sistem kaynakları serbest bırakılır.</span><span class="sxs-lookup"><span data-stu-id="061db-144">If you release all references to the wait handle, system resources are freed when garbage collection reclaims the wait handle.</span></span> <span data-ttu-id="061db-145">Bekleme tutamacını kullanmayı bitirir bitirmez sistem kaynaklarını serbest hale getirmek <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> için, yöntemi arayarak sistemden kurtulun.</span><span class="sxs-lookup"><span data-stu-id="061db-145">To free the system resources as soon as you are finished using the wait handle, dispose of it by calling the <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="061db-146">Tek kullanımlık nesneler açıkça imha edildiğinde çöp toplama daha verimli çalışır.</span><span class="sxs-lookup"><span data-stu-id="061db-146">Garbage collection works more efficiently when disposable objects are explicitly disposed.</span></span>

 [!code-cpp[AsyncDelegateExamples#3](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/waithandle.cpp#3)]
 [!code-csharp[AsyncDelegateExamples#3](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/waithandle.cs#3)]
 [!code-vb[AsyncDelegateExamples#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/WaitHandle.vb#3)]

## <a name="polling-for-asynchronous-call-completion"></a><span data-ttu-id="061db-147">Eşzamanlı Çağrı Tamamlama Için Yoklama</span><span class="sxs-lookup"><span data-stu-id="061db-147">Polling for Asynchronous Call Completion</span></span>
 <span data-ttu-id="061db-148">Eşzamanlı aramanın <xref:System.IAsyncResult.IsCompleted%2A> ne <xref:System.IAsyncResult> zaman `BeginInvoke` tamamlaşabileceğini keşfetmek için döndürülenin özelliğini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-148">You can use the <xref:System.IAsyncResult.IsCompleted%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke` to discover when the asynchronous call completes.</span></span> <span data-ttu-id="061db-149">Bunu, kullanıcı arabirimine hizmet veren bir iş parçacığından asynchronous aramayaparken yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-149">You might do this when making the asynchronous call from a thread that services the user interface.</span></span> <span data-ttu-id="061db-150">Tamamlanma için yoklama, eşkron çağrı bir <xref:System.Threading.ThreadPool> iş parçacığı üzerinde yürütülürken arama iş parçacığıyürütmeye devam etmesine olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="061db-150">Polling for completion allows the calling thread to continue executing while the asynchronous call executes on a <xref:System.Threading.ThreadPool> thread.</span></span>

 [!code-cpp[AsyncDelegateExamples#4](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/polling.cpp#4)]
 [!code-csharp[AsyncDelegateExamples#4](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/polling.cs#4)]
 [!code-vb[AsyncDelegateExamples#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/polling.vb#4)]

## <a name="executing-a-callback-method-when-an-asynchronous-call-completes"></a><span data-ttu-id="061db-151">Bir Eşzamanlı Arama Tamamlandığında Geri Arama Yöntemini Yürütme</span><span class="sxs-lookup"><span data-stu-id="061db-151">Executing a Callback Method When an Asynchronous Call Completes</span></span>
 <span data-ttu-id="061db-152">Eşzamanlı aramayı başlatan iş parçacığının sonuçları işleyen iş parçacığı olması gerekmiyorsa, arama tamamlandığında bir geri arama yöntemi yürütebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-152">If the thread that initiates the asynchronous call does not need to be the thread that processes the results, you can execute a callback method when the call completes.</span></span> <span data-ttu-id="061db-153">Geri arama yöntemi bir <xref:System.Threading.ThreadPool> iş parçacığı üzerinde yürütülür.</span><span class="sxs-lookup"><span data-stu-id="061db-153">The callback method is executed on a <xref:System.Threading.ThreadPool> thread.</span></span>

 <span data-ttu-id="061db-154">Geri arama yöntemini kullanmak için `BeginInvoke` <xref:System.AsyncCallback> geri arama yöntemini temsil eden bir temsilciyi geçmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="061db-154">To use a callback method, you must pass `BeginInvoke` an <xref:System.AsyncCallback> delegate that represents the callback method.</span></span> <span data-ttu-id="061db-155">Geri arama yöntemi tarafından kullanılacak bilgileri içeren bir nesneyi de geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-155">You can also pass an object that contains information to be used by the callback method.</span></span> <span data-ttu-id="061db-156">Geri arama yönteminde, geri <xref:System.IAsyncResult>arama yönteminin tek parametresi olan , <xref:System.Runtime.Remoting.Messaging.AsyncResult> bir nesneye döküm yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="061db-156">In the callback method, you can cast the <xref:System.IAsyncResult>, which is the only parameter of the callback method, to an <xref:System.Runtime.Remoting.Messaging.AsyncResult> object.</span></span> <span data-ttu-id="061db-157">Daha sonra aramayı <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> başlatmak için kullanılan temsilciyi almak için özelliği kullanabilirsiniz, böylece arayabilirsiniz. `EndInvoke`</span><span class="sxs-lookup"><span data-stu-id="061db-157">You can then use the <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> property to get the delegate that was used to initiate the call so that you can call `EndInvoke`.</span></span>

 <span data-ttu-id="061db-158">Örnekteki notlar:</span><span class="sxs-lookup"><span data-stu-id="061db-158">Notes on the example:</span></span>

- <span data-ttu-id="061db-159">`threadId` Parametresi `TestMethod` `out` bir parametredir`<Out>` `ByRef` (Visual Basic'te), bu nedenle giriş `TestMethod`değeri .</span><span class="sxs-lookup"><span data-stu-id="061db-159">The `threadId` parameter of `TestMethod` is an `out` parameter ([`<Out>` `ByRef` in Visual Basic), so its input value is never used by `TestMethod`.</span></span> <span data-ttu-id="061db-160">`BeginInvoke` Sahte bir değişken çağrıya aktarılır.</span><span class="sxs-lookup"><span data-stu-id="061db-160">A dummy variable is passed to the `BeginInvoke` call.</span></span> <span data-ttu-id="061db-161">`threadId` `ref` Parametre bir parametre olsaydı`ByRef` (Visual Basic'te), değişkenin sınıf düzeyinde bir alan olması gerekir, böylece her ikisine `BeginInvoke` de ve `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="061db-161">If the `threadId` parameter were a `ref` parameter (`ByRef` in Visual Basic), the variable would have to be a class-level field so that it could be passed to both `BeginInvoke` and `EndInvoke`.</span></span>

- <span data-ttu-id="061db-162">Aktarılan durum bilgileri, `BeginInvoke` geri arama yönteminin bir çıktı iletisini biçimlendirmek için kullandığı biçim dizesidir.</span><span class="sxs-lookup"><span data-stu-id="061db-162">The state information that is passed to `BeginInvoke` is a format string, which the callback method uses to format an output message.</span></span> <span data-ttu-id="061db-163">Tür <xref:System.Object>olarak geçirildiği için, durum bilgilerinin kullanılmadan önce uygun türüne aktarılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="061db-163">Because it is passed as type <xref:System.Object>, the state information must be cast to its proper type before it can be used.</span></span>

- <span data-ttu-id="061db-164">Geri arama bir <xref:System.Threading.ThreadPool> iş parçacığı üzerinde yapılır.</span><span class="sxs-lookup"><span data-stu-id="061db-164">The callback is made on a <xref:System.Threading.ThreadPool> thread.</span></span> <span data-ttu-id="061db-165"><xref:System.Threading.ThreadPool>iş parçacıkları, ana iş parçacığı sona erdiğinde uygulamayı çalışır durumda tutmayan arka plan iş parçacıklarıdır, bu nedenle örneğin ana iş parçacığının geri aramanın bitmesi için yeterince uzun süre uyuması gerekmektedir.</span><span class="sxs-lookup"><span data-stu-id="061db-165"><xref:System.Threading.ThreadPool> threads are background threads, which do not keep the application running if the main thread ends, so the main thread of the example has to sleep long enough for the callback to finish.</span></span>

 [!code-cpp[AsyncDelegateExamples#5](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/callback.cpp#5)]
 [!code-csharp[AsyncDelegateExamples#5](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/callback.cs#5)]
 [!code-vb[AsyncDelegateExamples#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/callback.vb#5)]

## <a name="see-also"></a><span data-ttu-id="061db-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="061db-166">See also</span></span>

- <xref:System.Delegate>
- [<span data-ttu-id="061db-167">Olay Tabanlı Zaman Uyumsuz Desen (EAP)</span><span class="sxs-lookup"><span data-stu-id="061db-167">Event-based Asynchronous Pattern (EAP)</span></span>](../../../docs/standard/asynchronous-programming-patterns/event-based-asynchronous-pattern-eap.md)
