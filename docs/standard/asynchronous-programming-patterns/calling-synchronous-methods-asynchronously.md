---
title: Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
helpviewer_keywords:
- asynchronous programming, delegates
- asynchronous delegates
- AsyncWaitHandle property
- callback methods
- calling synchronous methods in asynchronous manner
- WaitHandle class, code examples
- asynchronous programming, status polling
- polling asynchronous operation status
- delegates [.NET Framework], asynchronous
- synchronous calling in asynchronous manner
- waiting for asynchronous calls
- status information [.NET Framework], asynchronous operations
ms.assetid: 41972034-92ed-450a-9664-ab93fcc6f1fb
ms.openlocfilehash: 06df584f0120fbd4978e18647854a3ee844a2095
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/30/2019
ms.locfileid: "73105123"
---
# <a name="calling-synchronous-methods-asynchronously"></a><span data-ttu-id="201b3-102">Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma</span><span class="sxs-lookup"><span data-stu-id="201b3-102">Calling Synchronous Methods Asynchronously</span></span>

<span data-ttu-id="201b3-103">.NET Framework, zaman uyumsuz olarak herhangi bir yöntemi çağırmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="201b3-103">The .NET Framework enables you to call any method asynchronously.</span></span> <span data-ttu-id="201b3-104">Bunu yapmak için, çağırmak istediğiniz yöntemle aynı imzaya sahip bir temsilci tanımlayın; ortak dil çalışma zamanı, bu temsilci için uygun imzalara sahip `BeginInvoke` ve `EndInvoke` yöntemlerini otomatik olarak tanımlar.</span><span class="sxs-lookup"><span data-stu-id="201b3-104">To do this you define a delegate with the same signature as the method you want to call; the common language runtime automatically defines `BeginInvoke` and `EndInvoke` methods for this delegate, with the appropriate signatures.</span></span>

> [!NOTE]
> <span data-ttu-id="201b3-105">Zaman uyumsuz temsilci çağrıları, özellikle `BeginInvoke` ve `EndInvoke` yöntemleri .NET Compact Framework desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="201b3-105">Asynchronous delegate calls, specifically the `BeginInvoke` and `EndInvoke` methods, are not supported in the .NET Compact Framework.</span></span>

<span data-ttu-id="201b3-106">`BeginInvoke` yöntemi, zaman uyumsuz çağrıyı başlatır.</span><span class="sxs-lookup"><span data-stu-id="201b3-106">The `BeginInvoke` method initiates the asynchronous call.</span></span> <span data-ttu-id="201b3-107">Zaman uyumsuz olarak yürütmek istediğiniz yöntemle aynı parametrelere ve ek olarak iki isteğe bağlı parametreye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="201b3-107">It has the same parameters as the method that you want to execute asynchronously, plus two additional optional parameters.</span></span> <span data-ttu-id="201b3-108">İlk parametre, zaman uyumsuz arama tamamlandığında çağrılacak bir yönteme başvuran bir <xref:System.AsyncCallback> temsilcisidir.</span><span class="sxs-lookup"><span data-stu-id="201b3-108">The first parameter is an <xref:System.AsyncCallback> delegate that references a method to be called when the asynchronous call completes.</span></span> <span data-ttu-id="201b3-109">İkinci parametre, geri çağırma yöntemine bilgi geçiren Kullanıcı tanımlı bir nesnedir.</span><span class="sxs-lookup"><span data-stu-id="201b3-109">The second parameter is a user-defined object that passes information into the callback method.</span></span> <span data-ttu-id="201b3-110">`BeginInvoke` hemen döndürülür ve zaman uyumsuz çağrının tamamlanmasını beklemez.</span><span class="sxs-lookup"><span data-stu-id="201b3-110">`BeginInvoke` returns immediately and does not wait for the asynchronous call to complete.</span></span> <span data-ttu-id="201b3-111">`BeginInvoke`, zaman uyumsuz çağrının ilerlemesini izlemek için kullanılabilen bir <xref:System.IAsyncResult>döndürür.</span><span class="sxs-lookup"><span data-stu-id="201b3-111">`BeginInvoke` returns an <xref:System.IAsyncResult>, which can be used to monitor the progress of the asynchronous call.</span></span>

<span data-ttu-id="201b3-112">`EndInvoke` yöntemi, zaman uyumsuz çağrının sonuçlarını alır.</span><span class="sxs-lookup"><span data-stu-id="201b3-112">The `EndInvoke` method retrieves the results of the asynchronous call.</span></span> <span data-ttu-id="201b3-113">`BeginInvoke`sonra herhangi bir zaman çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="201b3-113">It can be called any time after `BeginInvoke`.</span></span> <span data-ttu-id="201b3-114">Zaman uyumsuz çağrı tamamlanmazsa `EndInvoke`, çağıran iş parçacığını tamamlanana kadar engeller.</span><span class="sxs-lookup"><span data-stu-id="201b3-114">If the asynchronous call has not completed, `EndInvoke` blocks the calling thread until it completes.</span></span> <span data-ttu-id="201b3-115">`EndInvoke` parametreler, zaman uyumsuz olarak yürütmek istediğiniz yöntemin yanı sıra `ByRef` tarafından döndürülen `ByRef` `out` ve `ref` parametreleri (Visual Basic '`<Out>` <xref:System.IAsyncResult> ve `BeginInvoke`) içerir.</span><span class="sxs-lookup"><span data-stu-id="201b3-115">The parameters of `EndInvoke` include the `out` and `ref` parameters (`<Out>` `ByRef` and `ByRef` in Visual Basic) of the method that you want to execute asynchronously, plus the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span>

> [!NOTE]
> <span data-ttu-id="201b3-116">Visual Studio 'da IntelliSense özelliği `BeginInvoke` ve `EndInvoke`parametrelerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="201b3-116">The IntelliSense feature in Visual Studio displays the parameters of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="201b3-117">Visual Studio veya benzer bir araç kullanmıyorsanız veya Visual Studio ile kullanıyorsanız C# , bu yöntemler için tanımlanan parametrelerin açıklaması Için [zaman uyumsuz programlama modeli (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="201b3-117">If you're not using Visual Studio or a similar tool, or if you're using C# with Visual Studio, see [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) for a description of the parameters defined for these methods.</span></span>

<span data-ttu-id="201b3-118">Bu konudaki kod örnekleri, zaman uyumsuz çağrılar yapmak için `BeginInvoke` ve `EndInvoke` kullanmanın dört ortak yolunu göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="201b3-118">The code examples in this topic demonstrate four common ways to use `BeginInvoke` and `EndInvoke` to make asynchronous calls.</span></span> <span data-ttu-id="201b3-119">`BeginInvoke` çağrıldıktan sonra şunları yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="201b3-119">After calling `BeginInvoke` you can do the following:</span></span>

- <span data-ttu-id="201b3-120">Biraz iş yapın ve ardından çağrı tamamlanana kadar engellemek için `EndInvoke` çağırın.</span><span class="sxs-lookup"><span data-stu-id="201b3-120">Do some work and then call `EndInvoke` to block until the call completes.</span></span>

- <span data-ttu-id="201b3-121"><xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> özelliğini kullanarak bir <xref:System.Threading.WaitHandle> edinin, <xref:System.Threading.WaitHandle> sinyallene kadar yürütmeyi engellemek için <xref:System.Threading.WaitHandle.WaitOne%2A> metodunu kullanın ve ardından `EndInvoke`çağırın.</span><span class="sxs-lookup"><span data-stu-id="201b3-121">Obtain a <xref:System.Threading.WaitHandle> using the <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> property, use its <xref:System.Threading.WaitHandle.WaitOne%2A> method to block execution until the <xref:System.Threading.WaitHandle> is signaled, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="201b3-122">Zaman uyumsuz çağrının ne zaman tamamlandığını belirleyip `BeginInvoke` tarafından döndürülen <xref:System.IAsyncResult> yoklayın ve sonra `EndInvoke`çağırır.</span><span class="sxs-lookup"><span data-stu-id="201b3-122">Poll the <xref:System.IAsyncResult> returned by `BeginInvoke` to determine when the asynchronous call has completed, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="201b3-123">`BeginInvoke`için bir geri çağırma yöntemi için temsilci geçirin.</span><span class="sxs-lookup"><span data-stu-id="201b3-123">Pass a delegate for a callback method to `BeginInvoke`.</span></span> <span data-ttu-id="201b3-124">Yöntemi, zaman uyumsuz arama tamamlandığında bir <xref:System.Threading.ThreadPool> iş parçacığında yürütülür.</span><span class="sxs-lookup"><span data-stu-id="201b3-124">The method is executed on a <xref:System.Threading.ThreadPool> thread when the asynchronous call completes.</span></span> <span data-ttu-id="201b3-125">Geri arama yöntemi `EndInvoke`çağırır.</span><span class="sxs-lookup"><span data-stu-id="201b3-125">The callback method calls `EndInvoke`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="201b3-126">Hangi tekniği kullanırsanız kullanın, zaman uyumsuz çağrın her zaman `EndInvoke` çağırın.</span><span class="sxs-lookup"><span data-stu-id="201b3-126">No matter which technique you use, always call `EndInvoke` to complete your asynchronous call.</span></span>

## <a name="defining-the-test-method-and-asynchronous-delegate"></a><span data-ttu-id="201b3-127">Test yöntemini ve zaman uyumsuz temsilciyi tanımlama</span><span class="sxs-lookup"><span data-stu-id="201b3-127">Defining the Test Method and Asynchronous Delegate</span></span>
 <span data-ttu-id="201b3-128">Aşağıdaki kod örnekleri, aynı uzun süre çalışan yöntemi `TestMethod`, zaman uyumsuz olarak çağırmanın çeşitli yollarını gösterir.</span><span class="sxs-lookup"><span data-stu-id="201b3-128">The code examples that follow demonstrate various ways of calling the same long-running method, `TestMethod`, asynchronously.</span></span> <span data-ttu-id="201b3-129">`TestMethod` yöntemi, işleme başladığını, birkaç saniye uyku moduna geçeceğini ve sonra sona erdiğini göstermek için bir konsol iletisi görüntüler.</span><span class="sxs-lookup"><span data-stu-id="201b3-129">The `TestMethod` method displays a console message to show that it has begun processing, sleeps for a few seconds, and then ends.</span></span> <span data-ttu-id="201b3-130">`TestMethod`, bu tür parametrelerin `BeginInvoke` ve `EndInvoke`imzalara Eklenme şeklini göstermek için bir `out` parametresine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="201b3-130">`TestMethod` has an `out` parameter to demonstrate the way such parameters are added to the signatures of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="201b3-131">`ref` parametrelerini benzer şekilde işleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-131">You can handle `ref` parameters similarly.</span></span>

 <span data-ttu-id="201b3-132">Aşağıdaki kod örneği `TestMethod` tanımını ve zaman uyumsuz `TestMethod` çağırmak için kullanılabilecek `AsyncMethodCaller` adlı temsilciyi gösterir.</span><span class="sxs-lookup"><span data-stu-id="201b3-132">The following code example shows the definition of `TestMethod` and the delegate named `AsyncMethodCaller` that can be used to call `TestMethod` asynchronously.</span></span> <span data-ttu-id="201b3-133">Kod örneklerini derlemek için `TestMethod` tanımlarını ve `AsyncMethodCaller` temsilciyi dahil etmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="201b3-133">To compile the code examples, you must include the definitions for `TestMethod` and the `AsyncMethodCaller` delegate.</span></span>

 [!code-cpp[AsyncDelegateExamples#1](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/TestMethod.cpp#1)]
 [!code-csharp[AsyncDelegateExamples#1](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/TestMethod.cs#1)]
 [!code-vb[AsyncDelegateExamples#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/TestMethod.vb#1)]

## <a name="waiting-for-an-asynchronous-call-with-endinvoke"></a><span data-ttu-id="201b3-134">EndInvoke ile zaman uyumsuz bir çağrı bekleniyor</span><span class="sxs-lookup"><span data-stu-id="201b3-134">Waiting for an Asynchronous Call with EndInvoke</span></span>
 <span data-ttu-id="201b3-135">Bir yöntemi zaman uyumsuz olarak yürütmenin en kolay yolu, temsilcinin `BeginInvoke` metodunu çağırarak, ana iş parçacığında bazı işler yaparak ve sonra temsilcinin `EndInvoke` yöntemini çağırarak yöntemi yürütmeye başlamadır.</span><span class="sxs-lookup"><span data-stu-id="201b3-135">The simplest way to execute a method asynchronously is to start executing the method by calling the delegate's `BeginInvoke` method, do some work on the main thread, and then call the delegate's `EndInvoke` method.</span></span> <span data-ttu-id="201b3-136">`EndInvoke`, zaman uyumsuz çağrı tamamlanana kadar döndürülmediği için çağıran iş parçacığını engelleyebilir.</span><span class="sxs-lookup"><span data-stu-id="201b3-136">`EndInvoke` might block the calling thread because it does not return until the asynchronous call completes.</span></span> <span data-ttu-id="201b3-137">Bu, dosya veya ağ işlemleriyle birlikte kullanmak için iyi bir tekniktir.</span><span class="sxs-lookup"><span data-stu-id="201b3-137">This is a good technique to use with file or network operations.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="201b3-138">`EndInvoke` engelleyebileceğinden, bunu Kullanıcı arabirimine hizmet veren iş parçacıklarından asla Çağırmamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="201b3-138">Because `EndInvoke` might block, you should never call it from threads that service the user interface.</span></span>

 [!code-cpp[AsyncDelegateExamples#2](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/EndInvoke.cpp#2)]
 [!code-csharp[AsyncDelegateExamples#2](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/EndInvoke.cs#2)]
 [!code-vb[AsyncDelegateExamples#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/EndInvoke.vb#2)]

## <a name="waiting-for-an-asynchronous-call-with-waithandle"></a><span data-ttu-id="201b3-139">WaitHandle ile zaman uyumsuz bir çağrı bekleniyor</span><span class="sxs-lookup"><span data-stu-id="201b3-139">Waiting for an Asynchronous Call with WaitHandle</span></span>
 <span data-ttu-id="201b3-140">`BeginInvoke`tarafından döndürülen <xref:System.IAsyncResult> <xref:System.IAsyncResult.AsyncWaitHandle%2A> özelliğini kullanarak bir <xref:System.Threading.WaitHandle> elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-140">You can obtain a <xref:System.Threading.WaitHandle> by using the <xref:System.IAsyncResult.AsyncWaitHandle%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span> <span data-ttu-id="201b3-141">Zaman uyumsuz çağrı tamamlandığında <xref:System.Threading.WaitHandle> sinyallidir ve <xref:System.Threading.WaitHandle.WaitOne%2A> metodunu çağırarak bunu bekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-141">The <xref:System.Threading.WaitHandle> is signaled when the asynchronous call completes, and you can wait for it by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span>

 <span data-ttu-id="201b3-142"><xref:System.Threading.WaitHandle>kullanıyorsanız, zaman uyumsuz çağrı tamamlanmadan önce veya sonra, ancak sonuçları almak için `EndInvoke` çağrılmadan önce ek işlem yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-142">If you use a <xref:System.Threading.WaitHandle>, you can perform additional processing before or after the asynchronous call completes, but before calling `EndInvoke` to retrieve the results.</span></span>

> [!NOTE]
> <span data-ttu-id="201b3-143">`EndInvoke`çağırdığınızda bekleme tutamacı otomatik olarak kapanmaz.</span><span class="sxs-lookup"><span data-stu-id="201b3-143">The wait handle is not closed automatically when you call `EndInvoke`.</span></span> <span data-ttu-id="201b3-144">Bekleme tanıtıcısına yapılan tüm başvuruları serbest bırakırsanız, çöp toplama geri kazanır bekleme tutamacını, sistem kaynakları serbest bırakılır.</span><span class="sxs-lookup"><span data-stu-id="201b3-144">If you release all references to the wait handle, system resources are freed when garbage collection reclaims the wait handle.</span></span> <span data-ttu-id="201b3-145">Bekleme tutamacını kullanmayı tamamladıktan hemen sonra sistem kaynaklarını serbest bırakmak için <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> yöntemini çağırarak onu atın.</span><span class="sxs-lookup"><span data-stu-id="201b3-145">To free the system resources as soon as you are finished using the wait handle, dispose of it by calling the <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="201b3-146">Çöp toplama, atılabilir nesneleri açıkça atıldığı zaman daha verimli bir şekilde çalışabilir.</span><span class="sxs-lookup"><span data-stu-id="201b3-146">Garbage collection works more efficiently when disposable objects are explicitly disposed.</span></span>

 [!code-cpp[AsyncDelegateExamples#3](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/waithandle.cpp#3)]
 [!code-csharp[AsyncDelegateExamples#3](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/waithandle.cs#3)]
 [!code-vb[AsyncDelegateExamples#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/WaitHandle.vb#3)]

## <a name="polling-for-asynchronous-call-completion"></a><span data-ttu-id="201b3-147">Zaman uyumsuz çağrı tamamlama yoklaması</span><span class="sxs-lookup"><span data-stu-id="201b3-147">Polling for Asynchronous Call Completion</span></span>
 <span data-ttu-id="201b3-148">Zaman uyumsuz çağrının ne zaman tamamlandığını saptamak için `BeginInvoke` tarafından döndürülen <xref:System.IAsyncResult> <xref:System.IAsyncResult.IsCompleted%2A> özelliğini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-148">You can use the <xref:System.IAsyncResult.IsCompleted%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke` to discover when the asynchronous call completes.</span></span> <span data-ttu-id="201b3-149">Bu işlem, Kullanıcı arabirimine hizmet veren bir iş parçacığından zaman uyumsuz çağrı yaparken yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-149">You might do this when making the asynchronous call from a thread that services the user interface.</span></span> <span data-ttu-id="201b3-150">Tamamlanma yoklaması, zaman uyumsuz çağrı bir <xref:System.Threading.ThreadPool> iş parçacığında yürütüldüğünde, çağıran iş parçacığının yürütmeye devam etmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="201b3-150">Polling for completion allows the calling thread to continue executing while the asynchronous call executes on a <xref:System.Threading.ThreadPool> thread.</span></span>

 [!code-cpp[AsyncDelegateExamples#4](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/polling.cpp#4)]
 [!code-csharp[AsyncDelegateExamples#4](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/polling.cs#4)]
 [!code-vb[AsyncDelegateExamples#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/polling.vb#4)]

## <a name="executing-a-callback-method-when-an-asynchronous-call-completes"></a><span data-ttu-id="201b3-151">Zaman uyumsuz bir arama tamamlandığında bir geri çağırma yöntemi yürütülüyor</span><span class="sxs-lookup"><span data-stu-id="201b3-151">Executing a Callback Method When an Asynchronous Call Completes</span></span>
 <span data-ttu-id="201b3-152">Zaman uyumsuz çağrıyı başlatan iş parçacığının sonuçları işleyen iş parçacığı olması gerekmiyorsa, çağrı tamamlandığında bir geri çağırma yöntemi çalıştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-152">If the thread that initiates the asynchronous call does not need to be the thread that processes the results, you can execute a callback method when the call completes.</span></span> <span data-ttu-id="201b3-153">Geri çağırma yöntemi bir <xref:System.Threading.ThreadPool> iş parçacığında yürütülür.</span><span class="sxs-lookup"><span data-stu-id="201b3-153">The callback method is executed on a <xref:System.Threading.ThreadPool> thread.</span></span>

 <span data-ttu-id="201b3-154">Bir geri çağırma yöntemi kullanmak için, geri çağırma yöntemini temsil eden bir <xref:System.AsyncCallback> temsilcisini `BeginInvoke` geçirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="201b3-154">To use a callback method, you must pass `BeginInvoke` an <xref:System.AsyncCallback> delegate that represents the callback method.</span></span> <span data-ttu-id="201b3-155">Geri çağırma yöntemi tarafından kullanılacak bilgileri içeren bir nesnesi de geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-155">You can also pass an object that contains information to be used by the callback method.</span></span> <span data-ttu-id="201b3-156">Geri çağırma yönteminde, geri çağırma yönteminin tek parametresi olan <xref:System.IAsyncResult><xref:System.Runtime.Remoting.Messaging.AsyncResult> nesnesine dönüştürebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-156">In the callback method, you can cast the <xref:System.IAsyncResult>, which is the only parameter of the callback method, to an <xref:System.Runtime.Remoting.Messaging.AsyncResult> object.</span></span> <span data-ttu-id="201b3-157">Daha sonra, `EndInvoke`çağırabilmeniz için çağrıyı başlatmak üzere kullanılan temsilciyi almak için <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> özelliğini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="201b3-157">You can then use the <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> property to get the delegate that was used to initiate the call so that you can call `EndInvoke`.</span></span>

 <span data-ttu-id="201b3-158">Örnekteki Not:</span><span class="sxs-lookup"><span data-stu-id="201b3-158">Notes on the example:</span></span>

- <span data-ttu-id="201b3-159">`TestMethod` `threadId` parametresi bir `out` parametresidir (`ByRef``<Out>` Visual Basic), bu nedenle giriş değeri hiçbir şekilde `TestMethod`tarafından kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="201b3-159">The `threadId` parameter of `TestMethod` is an `out` parameter ([`<Out>` `ByRef` in Visual Basic), so its input value is never used by `TestMethod`.</span></span> <span data-ttu-id="201b3-160">`BeginInvoke` çağrısına bir kukla değişken geçirilir.</span><span class="sxs-lookup"><span data-stu-id="201b3-160">A dummy variable is passed to the `BeginInvoke` call.</span></span> <span data-ttu-id="201b3-161">`threadId` parametresi bir `ref` parametresi (`ByRef` Visual Basic) ise, bu değişkenin hem `BeginInvoke` hem de `EndInvoke`geçirilecek bir sınıf düzeyi alan olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="201b3-161">If the `threadId` parameter were a `ref` parameter (`ByRef` in Visual Basic), the variable would have to be a class-level field so that it could be passed to both `BeginInvoke` and `EndInvoke`.</span></span>

- <span data-ttu-id="201b3-162">`BeginInvoke` geçirilen durum bilgileri, geri çağırma yönteminin bir çıktı iletisini biçimlendirmek için kullandığı bir biçim dizesidir.</span><span class="sxs-lookup"><span data-stu-id="201b3-162">The state information that is passed to `BeginInvoke` is a format string, which the callback method uses to format an output message.</span></span> <span data-ttu-id="201b3-163"><xref:System.Object>türü olarak geçirildiğinden, kullanılmadan önce durum bilgilerinin uygun türe dönüştürülmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="201b3-163">Because it is passed as type <xref:System.Object>, the state information must be cast to its proper type before it can be used.</span></span>

- <span data-ttu-id="201b3-164">Geri çağırma <xref:System.Threading.ThreadPool> bir iş parçacığında yapılır.</span><span class="sxs-lookup"><span data-stu-id="201b3-164">The callback is made on a <xref:System.Threading.ThreadPool> thread.</span></span> <span data-ttu-id="201b3-165"><xref:System.Threading.ThreadPool> iş parçacıkları, ana iş parçacığı sona erdiğinde uygulamayı çalışır durumda tutmayan arka plan iş parçacığıdır. bu nedenle, örneğin ana iş parçacığı geri aramanın tamamlanabilmesi için yeterince uzun süre uykuya geçecek.</span><span class="sxs-lookup"><span data-stu-id="201b3-165"><xref:System.Threading.ThreadPool> threads are background threads, which do not keep the application running if the main thread ends, so the main thread of the example has to sleep long enough for the callback to finish.</span></span>

 [!code-cpp[AsyncDelegateExamples#5](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/callback.cpp#5)]
 [!code-csharp[AsyncDelegateExamples#5](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/callback.cs#5)]
 [!code-vb[AsyncDelegateExamples#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/callback.vb#5)]

## <a name="see-also"></a><span data-ttu-id="201b3-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="201b3-166">See also</span></span>

- <xref:System.Delegate>
- [<span data-ttu-id="201b3-167">Olay Tabanlı Zaman Uyumsuz Desen (EAP)</span><span class="sxs-lookup"><span data-stu-id="201b3-167">Event-based Asynchronous Pattern (EAP)</span></span>](../../../docs/standard/asynchronous-programming-patterns/event-based-asynchronous-pattern-eap.md)
