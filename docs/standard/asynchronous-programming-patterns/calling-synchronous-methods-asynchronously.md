---
title: Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
helpviewer_keywords:
- asynchronous programming, delegates
- asynchronous delegates
- AsyncWaitHandle property
- callback methods
- calling synchronous methods in asynchronous manner
- WaitHandle class, code examples
- asynchronous programming, status polling
- polling asynchronous operation status
- delegates [.NET Framework], asynchronous
- synchronous calling in asynchronous manner
- waiting for asynchronous calls
- status information [.NET Framework], asynchronous operations
ms.assetid: 41972034-92ed-450a-9664-ab93fcc6f1fb
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 371e958aca87c922c902d8efd945d94d611672d9
ms.sourcegitcommit: ad99773e5e45068ce03b99518008397e1299e0d1
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/23/2018
ms.locfileid: "46702887"
---
# <a name="calling-synchronous-methods-asynchronously"></a><span data-ttu-id="f0944-102">Zaman Uyumlu Metotları Zaman Uyumsuz Olarak Çağırma</span><span class="sxs-lookup"><span data-stu-id="f0944-102">Calling Synchronous Methods Asynchronously</span></span>

<span data-ttu-id="f0944-103">.NET Framework zaman uyumsuz olarak herhangi bir yöntemi çağırmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="f0944-103">The .NET Framework enables you to call any method asynchronously.</span></span> <span data-ttu-id="f0944-104">Bunu yapmak için aynı imzaya sahip bir temsilci yöntemi çağırmak istediğinizde olarak tanımlayın. Ortak dil çalışma zamanı otomatik olarak tanımlar `BeginInvoke` ve `EndInvoke` uygun imzalara sahip bu temsilci yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="f0944-104">To do this you define a delegate with the same signature as the method you want to call; the common language runtime automatically defines `BeginInvoke` and `EndInvoke` methods for this delegate, with the appropriate signatures.</span></span>

> [!NOTE]
> <span data-ttu-id="f0944-105">Zaman uyumsuz temsilcisini çağırır, özellikle `BeginInvoke` ve `EndInvoke` yöntemler .NET Compact Framework desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="f0944-105">Asynchronous delegate calls, specifically the `BeginInvoke` and `EndInvoke` methods, are not supported in the .NET Compact Framework.</span></span>

<span data-ttu-id="f0944-106">`BeginInvoke` Yöntemi zaman uyumsuz çağrı başlatır.</span><span class="sxs-lookup"><span data-stu-id="f0944-106">The `BeginInvoke` method initiates the asynchronous call.</span></span> <span data-ttu-id="f0944-107">Bu yöntemin zaman uyumsuz olarak yürütmek istediğiniz aynı parametreleri yanı sıra, iki ek isteğe bağlı parametreye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="f0944-107">It has the same parameters as the method that you want to execute asynchronously, plus two additional optional parameters.</span></span> <span data-ttu-id="f0944-108">İlk parametre bir <xref:System.AsyncCallback> zaman uyumsuz çağrısı tamamlandığında çağrılacak bir yönteme başvuran temsilci.</span><span class="sxs-lookup"><span data-stu-id="f0944-108">The first parameter is an <xref:System.AsyncCallback> delegate that references a method to be called when the asynchronous call completes.</span></span> <span data-ttu-id="f0944-109">İkinci parametre bilgileri geri çağırma yöntemi içinde oluşturulmuş bir kullanıcı tanımlı bir nesnedir.</span><span class="sxs-lookup"><span data-stu-id="f0944-109">The second parameter is a user-defined object that passes information into the callback method.</span></span> <span data-ttu-id="f0944-110">`BeginInvoke` hemen döner ve zaman uyumsuz çağrının tamamlanmasını beklemez.</span><span class="sxs-lookup"><span data-stu-id="f0944-110">`BeginInvoke` returns immediately and does not wait for the asynchronous call to complete.</span></span> <span data-ttu-id="f0944-111">`BeginInvoke` döndürür bir <xref:System.IAsyncResult>, zaman uyumsuz çağrı ilerlemesini izlemek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="f0944-111">`BeginInvoke` returns an <xref:System.IAsyncResult>, which can be used to monitor the progress of the asynchronous call.</span></span>

<span data-ttu-id="f0944-112">`EndInvoke` Yöntemi zaman uyumsuz çağrının sonucunu alır.</span><span class="sxs-lookup"><span data-stu-id="f0944-112">The `EndInvoke` method retrieves the results of the asynchronous call.</span></span> <span data-ttu-id="f0944-113">Sonra her zaman çağrılabilir `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-113">It can be called any time after `BeginInvoke`.</span></span> <span data-ttu-id="f0944-114">Zaman uyumsuz çağrı tamamlanmazsa `EndInvoke` işlem tamamlanana kadar çağıran iş parçacığını engeller.</span><span class="sxs-lookup"><span data-stu-id="f0944-114">If the asynchronous call has not completed, `EndInvoke` blocks the calling thread until it completes.</span></span> <span data-ttu-id="f0944-115">Parametreleri `EndInvoke` dahil `out` ve `ref` parametreleri (`<Out>` `ByRef` ve `ByRef` Visual Basic'te) zaman uyumsuz olarak yürütmek istediğiniz yöntemin artı <xref:System.IAsyncResult> tarafından döndürülen `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-115">The parameters of `EndInvoke` include the `out` and `ref` parameters (`<Out>` `ByRef` and `ByRef` in Visual Basic) of the method that you want to execute asynchronously, plus the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span>

> [!NOTE]
> <span data-ttu-id="f0944-116">Visual Studio IntelliSense özelliği parametrelerini görüntüler `BeginInvoke` ve `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-116">The IntelliSense feature in Visual Studio displays the parameters of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="f0944-117">Visual Studio veya benzer bir araç kullanmıyorsanız veya C# ile Visual Studio kullanıyorsanız, bkz: [zaman uyumsuz programlama modeli (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) bu yöntemler için tanımlanan parametrelerin bir açıklaması.</span><span class="sxs-lookup"><span data-stu-id="f0944-117">If you're not using Visual Studio or a similar tool, or if you're using C# with Visual Studio, see [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) for a description of the parameters defined for these methods.</span></span>

<span data-ttu-id="f0944-118">Bu konudaki kod örnekleri kullanmak için dört yaygın yolları göstermek `BeginInvoke` ve `EndInvoke` zaman uyumsuz çağrıları yapma.</span><span class="sxs-lookup"><span data-stu-id="f0944-118">The code examples in this topic demonstrate four common ways to use `BeginInvoke` and `EndInvoke` to make asynchronous calls.</span></span> <span data-ttu-id="f0944-119">Arama sonra `BeginInvoke` aşağıdakileri yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f0944-119">After calling `BeginInvoke` you can do the following:</span></span>

-   <span data-ttu-id="f0944-120">Bazı iş yapmak ve sonra çağrı `EndInvoke` çağrı tamamlanana kadar bloğu için.</span><span class="sxs-lookup"><span data-stu-id="f0944-120">Do some work and then call `EndInvoke` to block until the call completes.</span></span>

-   <span data-ttu-id="f0944-121">Elde bir <xref:System.Threading.WaitHandle> kullanarak <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> özelliğini kullanın, <xref:System.Threading.WaitHandle.WaitOne%2A> kadar blok yürütme yöntemine <xref:System.Threading.WaitHandle> işareti verilen ve sonra çağrı `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-121">Obtain a <xref:System.Threading.WaitHandle> using the <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> property, use its <xref:System.Threading.WaitHandle.WaitOne%2A> method to block execution until the <xref:System.Threading.WaitHandle> is signaled, and then call `EndInvoke`.</span></span>

-   <span data-ttu-id="f0944-122">Yoklama <xref:System.IAsyncResult> tarafından döndürülen `BeginInvoke` zaman uyumsuz çağrı tamamlanıp tamamlanmadığını belirlemek ve ardından çağırmak için `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-122">Poll the <xref:System.IAsyncResult> returned by `BeginInvoke` to determine when the asynchronous call has completed, and then call `EndInvoke`.</span></span>

-   <span data-ttu-id="f0944-123">Bir temsilci için bir geri çağırma yöntemine geçirmek `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-123">Pass a delegate for a callback method to `BeginInvoke`.</span></span> <span data-ttu-id="f0944-124">Yöntem üzerinde yürütülen bir <xref:System.Threading.ThreadPool> zaman uyumsuz çağrı tamamlandığında iş parçacığı.</span><span class="sxs-lookup"><span data-stu-id="f0944-124">The method is executed on a <xref:System.Threading.ThreadPool> thread when the asynchronous call completes.</span></span> <span data-ttu-id="f0944-125">Geri çağırma yöntemi çağrıları `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-125">The callback method calls `EndInvoke`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="f0944-126">Hangi yöntem ne olursa olsun kullanın, her zaman çağrı `EndInvoke` , zaman uyumsuz çağrı tamamlanması.</span><span class="sxs-lookup"><span data-stu-id="f0944-126">No matter which technique you use, always call `EndInvoke` to complete your asynchronous call.</span></span>

## <a name="defining-the-test-method-and-asynchronous-delegate"></a><span data-ttu-id="f0944-127">Test yöntemi ve zaman uyumsuz temsilci tanımlama</span><span class="sxs-lookup"><span data-stu-id="f0944-127">Defining the Test Method and Asynchronous Delegate</span></span>
 <span data-ttu-id="f0944-128">Aşağıdaki kod örnekleri aynı uzun süreli metot çağrısının çeşitli yolları göstermeye `TestMethod`, zaman uyumsuz olarak.</span><span class="sxs-lookup"><span data-stu-id="f0944-128">The code examples that follow demonstrate various ways of calling the same long-running method, `TestMethod`, asynchronously.</span></span> <span data-ttu-id="f0944-129">`TestMethod` Yöntemi bu işlem, birkaç saniye için uyku başlattı ve sonra sona erer göstermek için bir konsol iletisi görüntüler.</span><span class="sxs-lookup"><span data-stu-id="f0944-129">The `TestMethod` method displays a console message to show that it has begun processing, sleeps for a few seconds, and then ends.</span></span> <span data-ttu-id="f0944-130">`TestMethod` sahip bir `out` imzalarını için tür parametreleri eklendi yol göstermek için parametre `BeginInvoke` ve `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-130">`TestMethod` has an `out` parameter to demonstrate the way such parameters are added to the signatures of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="f0944-131">İşleyebilirsiniz `ref` parametreleri benzer.</span><span class="sxs-lookup"><span data-stu-id="f0944-131">You can handle `ref` parameters similarly.</span></span>

 <span data-ttu-id="f0944-132">Aşağıdaki kod örneği tanımı gösterilmektedir `TestMethod` ve adlı temsilcinin `AsyncMethodCaller` çağırmak için kullanılabilecek `TestMethod` zaman uyumsuz olarak.</span><span class="sxs-lookup"><span data-stu-id="f0944-132">The following code example shows the definition of `TestMethod` and the delegate named `AsyncMethodCaller` that can be used to call `TestMethod` asynchronously.</span></span> <span data-ttu-id="f0944-133">Kod örnekleri derlemek için tanımları içerir `TestMethod` ve `AsyncMethodCaller` temsilci.</span><span class="sxs-lookup"><span data-stu-id="f0944-133">To compile the code examples, you must include the definitions for `TestMethod` and the `AsyncMethodCaller` delegate.</span></span>

 [!code-cpp[AsyncDelegateExamples#1](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/TestMethod.cpp#1)]
 [!code-csharp[AsyncDelegateExamples#1](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/TestMethod.cs#1)]
 [!code-vb[AsyncDelegateExamples#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/TestMethod.vb#1)]

## <a name="waiting-for-an-asynchronous-call-with-endinvoke"></a><span data-ttu-id="f0944-134">EndInvoke ile zaman uyumsuz bir çağrı bekleniyor</span><span class="sxs-lookup"><span data-stu-id="f0944-134">Waiting for an Asynchronous Call with EndInvoke</span></span>
 <span data-ttu-id="f0944-135">Temsilcinin çağırarak yöntemi yürütmeye başlamak için bir yöntem zaman uyumsuz olarak yürütmek için en kolay yolu olan `BeginInvoke` yöntemi, bazı ana iş parçacığı üzerinde çalışır ve temsilcinin çağırma `EndInvoke` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f0944-135">The simplest way to execute a method asynchronously is to start executing the method by calling the delegate's `BeginInvoke` method, do some work on the main thread, and then call the delegate's `EndInvoke` method.</span></span> <span data-ttu-id="f0944-136">`EndInvoke` zaman uyumsuz çağrı tamamlanana kadar döndürmeyen çünkü çağıran iş parçacığını engelleyebilir.</span><span class="sxs-lookup"><span data-stu-id="f0944-136">`EndInvoke` might block the calling thread because it does not return until the asynchronous call completes.</span></span> <span data-ttu-id="f0944-137">Bu dosya veya ağ işlemleriyle kullanmak iyi bir tekniktir.</span><span class="sxs-lookup"><span data-stu-id="f0944-137">This is a good technique to use with file or network operations.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="f0944-138">Çünkü `EndInvoke` engelleyebilir, hiçbir zaman bu hizmet kullanıcı arabirimi iş parçacığından çağırmalısınız.</span><span class="sxs-lookup"><span data-stu-id="f0944-138">Because `EndInvoke` might block, you should never call it from threads that service the user interface.</span></span>

 [!code-cpp[AsyncDelegateExamples#2](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/EndInvoke.cpp#2)]
 [!code-csharp[AsyncDelegateExamples#2](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/EndInvoke.cs#2)]
 [!code-vb[AsyncDelegateExamples#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/EndInvoke.vb#2)]

## <a name="waiting-for-an-asynchronous-call-with-waithandle"></a><span data-ttu-id="f0944-139">WaitHandle ile zaman uyumsuz bir çağrı bekleniyor</span><span class="sxs-lookup"><span data-stu-id="f0944-139">Waiting for an Asynchronous Call with WaitHandle</span></span>
 <span data-ttu-id="f0944-140">Alabilirsiniz bir <xref:System.Threading.WaitHandle> kullanarak <xref:System.IAsyncResult.AsyncWaitHandle%2A> özelliği <xref:System.IAsyncResult> tarafından döndürülen `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-140">You can obtain a <xref:System.Threading.WaitHandle> by using the <xref:System.IAsyncResult.AsyncWaitHandle%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span> <span data-ttu-id="f0944-141"><xref:System.Threading.WaitHandle> Zaman uyumsuz çağrı tamamlanana ve bunun için çağırarak bekleyin sinyal <xref:System.Threading.WaitHandle.WaitOne%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f0944-141">The <xref:System.Threading.WaitHandle> is signaled when the asynchronous call completes, and you can wait for it by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span>

 <span data-ttu-id="f0944-142">Kullanıyorsanız bir <xref:System.Threading.WaitHandle>, önce veya zaman uyumsuz çağrı tamamlandıktan sonra ek işleme gerçekleştirebilirsiniz ancak çağırmadan önce `EndInvoke` sonuçlarını almak için.</span><span class="sxs-lookup"><span data-stu-id="f0944-142">If you use a <xref:System.Threading.WaitHandle>, you can perform additional processing before or after the asynchronous call completes, but before calling `EndInvoke` to retrieve the results.</span></span>

> [!NOTE]
> <span data-ttu-id="f0944-143">Çağırdığınızda bekleme tanıtıcısı otomatik olarak kapatılmamış `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-143">The wait handle is not closed automatically when you call `EndInvoke`.</span></span> <span data-ttu-id="f0944-144">Bekleme tanıtıcısı tüm başvurularını serbest bırakırsanız, çöp toplama bekleme tanıtıcısı kazandığında sistem kaynaklarının kurtulurlar.</span><span class="sxs-lookup"><span data-stu-id="f0944-144">If you release all references to the wait handle, system resources are freed when garbage collection reclaims the wait handle.</span></span> <span data-ttu-id="f0944-145">Bekleme tanıtıcısı kullanarak tamamladıktan hemen sonra sistem kaynaklarını serbest için bunu çağırarak dispose <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="f0944-145">To free the system resources as soon as you are finished using the wait handle, dispose of it by calling the <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="f0944-146">Atılabilir bir nesne açıkça elden olduğunda çöp toplamanın daha verimli bir şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="f0944-146">Garbage collection works more efficiently when disposable objects are explicitly disposed.</span></span>

 [!code-cpp[AsyncDelegateExamples#3](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/waithandle.cpp#3)]
 [!code-csharp[AsyncDelegateExamples#3](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/waithandle.cs#3)]
 [!code-vb[AsyncDelegateExamples#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/WaitHandle.vb#3)]

## <a name="polling-for-asynchronous-call-completion"></a><span data-ttu-id="f0944-147">Zaman uyumsuz çağrı tamamlanması için yoklama</span><span class="sxs-lookup"><span data-stu-id="f0944-147">Polling for Asynchronous Call Completion</span></span>
 <span data-ttu-id="f0944-148">Kullanabileceğiniz <xref:System.IAsyncResult.IsCompleted%2A> özelliği <xref:System.IAsyncResult> tarafından döndürülen `BeginInvoke` zaman uyumsuz çağrı tamamlandığında bulunacak.</span><span class="sxs-lookup"><span data-stu-id="f0944-148">You can use the <xref:System.IAsyncResult.IsCompleted%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke` to discover when the asynchronous call completes.</span></span> <span data-ttu-id="f0944-149">Bir iş parçacığından zaman uyumsuz çağrı yapmak için kullanıcı arabirimi Hizmetleri, bunu yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0944-149">You might do this when making the asynchronous call from a thread that services the user interface.</span></span> <span data-ttu-id="f0944-150">Zaman uyumsuz çağrı üzerinde çalışırken yürütmeye devam çağıran iş parçacığını tamamlama sağlar yoklama bir <xref:System.Threading.ThreadPool> iş parçacığı.</span><span class="sxs-lookup"><span data-stu-id="f0944-150">Polling for completion allows the calling thread to continue executing while the asynchronous call executes on a <xref:System.Threading.ThreadPool> thread.</span></span>

 [!code-cpp[AsyncDelegateExamples#4](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/polling.cpp#4)]
 [!code-csharp[AsyncDelegateExamples#4](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/polling.cs#4)]
 [!code-vb[AsyncDelegateExamples#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/polling.vb#4)]

## <a name="executing-a-callback-method-when-an-asynchronous-call-completes"></a><span data-ttu-id="f0944-151">Zaman uyumsuz bir çağrı tamamlandığında bir geri çağırma metodu yürütülüyor</span><span class="sxs-lookup"><span data-stu-id="f0944-151">Executing a Callback Method When an Asynchronous Call Completes</span></span>
 <span data-ttu-id="f0944-152">Zaman uyumsuz çağrı başlatır iş parçacığı sonuçlarını işleyen iş parçacığı olması gerekmez, bir geri çağırma yöntemi çağrısı tamamlandığında yürütebilir.</span><span class="sxs-lookup"><span data-stu-id="f0944-152">If the thread that initiates the asynchronous call does not need to be the thread that processes the results, you can execute a callback method when the call completes.</span></span> <span data-ttu-id="f0944-153">Geri çağırma yöntemi, üzerinde yürütülen bir <xref:System.Threading.ThreadPool> iş parçacığı.</span><span class="sxs-lookup"><span data-stu-id="f0944-153">The callback method is executed on a <xref:System.Threading.ThreadPool> thread.</span></span>

 <span data-ttu-id="f0944-154">Bir geri çağırma yöntemi kullanmak için geçmelidir `BeginInvoke` bir <xref:System.AsyncCallback> geri çağırma yöntemi temsil eden bir temsilci.</span><span class="sxs-lookup"><span data-stu-id="f0944-154">To use a callback method, you must pass `BeginInvoke` an <xref:System.AsyncCallback> delegate that represents the callback method.</span></span> <span data-ttu-id="f0944-155">Ayrıca geri çağırma yöntemi tarafından kullanılacak bilgileri içeren bir nesne geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0944-155">You can also pass an object that contains information to be used by the callback method.</span></span> <span data-ttu-id="f0944-156">Geri çağırma yöntemi, verdiğiniz <xref:System.IAsyncResult>, için geri çağırma yöntemini tek parametre olduğu bir <xref:System.Runtime.Remoting.Messaging.AsyncResult> nesne.</span><span class="sxs-lookup"><span data-stu-id="f0944-156">In the callback method, you can cast the <xref:System.IAsyncResult>, which is the only parameter of the callback method, to an <xref:System.Runtime.Remoting.Messaging.AsyncResult> object.</span></span> <span data-ttu-id="f0944-157">Ardından <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> çağırabilirsiniz, aramayı başlatmak için kullanılan temsilci almak için özellik `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-157">You can then use the <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> property to get the delegate that was used to initiate the call so that you can call `EndInvoke`.</span></span>

 <span data-ttu-id="f0944-158">Bu örnek ile ilgili notlar:</span><span class="sxs-lookup"><span data-stu-id="f0944-158">Notes on the example:</span></span>

-   <span data-ttu-id="f0944-159">`threadId` Parametresinin `TestMethod` olduğu bir `out` parametre ([`<Out>` `ByRef` Visual Basic'te), giriş değeri tarafından hiç kullanılmamış `TestMethod`.</span><span class="sxs-lookup"><span data-stu-id="f0944-159">The `threadId` parameter of `TestMethod` is an `out` parameter ([`<Out>` `ByRef` in Visual Basic), so its input value is never used by `TestMethod`.</span></span> <span data-ttu-id="f0944-160">İşlevsiz bir değişken geçirilir `BeginInvoke` çağırın.</span><span class="sxs-lookup"><span data-stu-id="f0944-160">A dummy variable is passed to the `BeginInvoke` call.</span></span> <span data-ttu-id="f0944-161">Varsa `threadId` parametresi olan bir `ref` parametre (`ByRef` Visual Basic'te), değişkeni bir sınıf seviyesi alanını böylece, her iki geçirilebilir olması gerekirdi `BeginInvoke` ve `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="f0944-161">If the `threadId` parameter were a `ref` parameter (`ByRef` in Visual Basic), the variable would have to be a class-level field so that it could be passed to both `BeginInvoke` and `EndInvoke`.</span></span>

-   <span data-ttu-id="f0944-162">Geçirilen durum bilgilerini `BeginInvoke` çıkış iletisini biçimlendirmek için geri çağırma yöntemi kullanan bir biçim dizesi.</span><span class="sxs-lookup"><span data-stu-id="f0944-162">The state information that is passed to `BeginInvoke` is a format string, which the callback method uses to format an output message.</span></span> <span data-ttu-id="f0944-163">Türü olarak geçtiğinden <xref:System.Object>, kullanılmadan önce durum bilgisi, doğru türe dönüştürmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="f0944-163">Because it is passed as type <xref:System.Object>, the state information must be cast to its proper type before it can be used.</span></span>

-   <span data-ttu-id="f0944-164">Geri çağırma üzerinde yapılan bir <xref:System.Threading.ThreadPool> iş parçacığı.</span><span class="sxs-lookup"><span data-stu-id="f0944-164">The callback is made on a <xref:System.Threading.ThreadPool> thread.</span></span> <span data-ttu-id="f0944-165"><xref:System.Threading.ThreadPool> Ana iş parçacığı sona ererse, ana iş parçacığı örneğin tamamlamak yeteri kadar geri çağırma için uyku moduna sahiptir çalışan uygulama tutma arka plan iş parçacığı akışlardır.</span><span class="sxs-lookup"><span data-stu-id="f0944-165"><xref:System.Threading.ThreadPool> threads are background threads, which do not keep the application running if the main thread ends, so the main thread of the example has to sleep long enough for the callback to finish.</span></span>

 [!code-cpp[AsyncDelegateExamples#5](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/callback.cpp#5)]
 [!code-csharp[AsyncDelegateExamples#5](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/callback.cs#5)]
 [!code-vb[AsyncDelegateExamples#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/callback.vb#5)]

## <a name="see-also"></a><span data-ttu-id="f0944-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="f0944-166">See also</span></span>

- <xref:System.Delegate>
- [<span data-ttu-id="f0944-167">Olay Tabanlı Zaman Uyumsuz Desen (EAP)</span><span class="sxs-lookup"><span data-stu-id="f0944-167">Event-based Asynchronous Pattern (EAP)</span></span>](../../../docs/standard/asynchronous-programming-patterns/event-based-asynchronous-pattern-eap.md)
