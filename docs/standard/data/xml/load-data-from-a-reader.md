---
description: 'Hakkında daha fazla bilgi edinin: okuyucudan veri yükleme'
title: Okuyucudan Veri Yükleme
ms.date: 03/30/2017
ms.assetid: 7e74918c-bc72-4977-a49b-e1520a6d8f60
ms.openlocfilehash: b7f21ff96dd616fa9919d02d3a7edfd4ac8d0678
ms.sourcegitcommit: ddf7edb67715a5b9a45e3dd44536dabc153c1de0
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/06/2021
ms.locfileid: "99732091"
---
# <a name="load-data-from-a-reader"></a><span data-ttu-id="d0e9c-103">Okuyucudan Veri Yükleme</span><span class="sxs-lookup"><span data-stu-id="d0e9c-103">Load Data from a Reader</span></span>

<span data-ttu-id="d0e9c-104">Bir XML belgesi <xref:System.Xml.XmlDocument.Load%2A> yöntemi ve bir parametresi kullanılarak yüklenirse <xref:System.Xml.XmlReader> , diğer biçimlerden verileri yükleme davranışına kıyasla oluşan davranışta farklılıklar vardır.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-104">If an XML document is loaded using the <xref:System.Xml.XmlDocument.Load%2A> method and a parameter of an <xref:System.Xml.XmlReader>, there are differences in the behavior that occurs when compared to the behavior of loading data from the other formats.</span></span> <span data-ttu-id="d0e9c-105">Okuyucu ilk durumundaysa, <xref:System.Xml.XmlDocument.Load%2A> tüm içeriği okuyucudan kullanır ve okuyucudaki tüm VERILERDEN XML belge nesne modeli (DOM) oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-105">If the reader is in its initial state, <xref:System.Xml.XmlDocument.Load%2A> consumes the entire contents from the reader and builds the XML Document Object Model (DOM) from all the data in the reader.</span></span>  
  
 <span data-ttu-id="d0e9c-106">Okuyucu, belgenin herhangi bir yerinde zaten bir düğüm üzerinde konumlandırılmışsa ve ardından dönüştürme <xref:System.Xml.XmlDocument.Load%2A> yöntemine geçtiğinde, geçerli <xref:System.Xml.XmlDocument.Load%2A> bir üst etikete kadar, geçerli derinliği belleğe kapatan bitiş etiketine kadar geçerli düğümü ve tüm eşdüzey düzeylerini okumaya çalışır.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-106">If the reader is already positioned on a node somewhere in the document, and the reader is then passed to the <xref:System.Xml.XmlDocument.Load%2A> method, <xref:System.Xml.XmlDocument.Load%2A> attempts to read the current node and all of its siblings, up to the end tag that closes the current depth into memory.</span></span> <span data-ttu-id="d0e9c-107">Denenen işlemin başarısı, <xref:System.Xml.XmlDocument.Load%2A> Yük denendiğinde okuyucunun üzerinde bulunduğu düğüme bağlıdır, <xref:System.Xml.XmlDocument.Load%2A> OKUYUCUDAN XML 'nin doğru biçimlendirildiğini doğrular.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-107">The success of the attempted <xref:System.Xml.XmlDocument.Load%2A> depends on the node that the reader is on when the load is attempted, as <xref:System.Xml.XmlDocument.Load%2A> verifies that the XML from the reader is well-formed.</span></span> <span data-ttu-id="d0e9c-108">XML doğru biçimlendirilmemiş ise <xref:System.Xml.XmlDocument.Load%2A> bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-108">If the XML is not well-formed, the <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span> <span data-ttu-id="d0e9c-109">Örneğin, aşağıdaki düğüm kümesi iki kök düzeyi öğe içerir, XML doğru biçimlendirilmemiş ve <xref:System.Xml.XmlDocument.Load%2A> bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-109">For example, the following set of nodes contain two root-level elements, the XML is not well-formed, and <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span>  
  
- <span data-ttu-id="d0e9c-110">Açıklama düğümü, ardından bir öğe düğümü, ardından bir öğesi düğümü, ve ardından bir EndElement düğümü.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-110">Comment node, followed by an Element node, followed by an Element node, followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="d0e9c-111">Aşağıdaki düğüm kümesi, kök düzeyinde bir öğe olmadığından tamamlanmamış bir DOM oluşturuyor.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-111">The following set of nodes creates an incomplete DOM, because there is no root-level element.</span></span>  
  
- <span data-ttu-id="d0e9c-112">Açıklama düğümü ve ardından bir işlem yönergesi düğümü ve ardından bir EndElement düğümü gelen bir açıklama düğümü gelir.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-112">Comment node followed by a ProcessingInstruction node followed by a Comment node followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="d0e9c-113">Bu, bir özel durum oluşturmaz ve veri yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-113">This does not throw an exception, and the data is loaded.</span></span> <span data-ttu-id="d0e9c-114">Bu düğümlerin üst kısmına bir kök öğesi ekleyebilir ve hatasız kaydedilebilecek, düzgün biçimlendirilmiş bir XML oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-114">You can add a root element to the top of these nodes and create well-formed XML that can be saved without error.</span></span>  
  
 <span data-ttu-id="d0e9c-115">Okuyucu, bir belgenin kök düzeyi (örneğin, bir boşluk veya öznitelik düğümü) için geçersiz olan bir yaprak düğümde konumlandırılmışsa, bu,, kök için kullanılabilecek bir düğüme yerleştirilene kadar okumaya devam eder.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-115">If the reader is positioned on a leaf node that is invalid for the root level of a document (for example, a white space or attribute node), the reader continues to read until it is positioned on a node that can be used for the root.</span></span> <span data-ttu-id="d0e9c-116">Belge bu noktada yüklenmeye başlıyor.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-116">The document begins loading at this point.</span></span>  
  
 <span data-ttu-id="d0e9c-117">Varsayılan olarak, <xref:System.Xml.XmlDocument.Load%2A> XML 'nin belge türü tanımı (DTD) veya şema doğrulaması kullanarak geçerli olup olmadığını doğrulamaz.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-117">By default, <xref:System.Xml.XmlDocument.Load%2A> does not verify whether the XML is valid using document type definition (DTD) or schema validation.</span></span> <span data-ttu-id="d0e9c-118">Yalnızca XML 'nin düzgün biçimlendirilmiş olup olmadığını doğrular.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-118">It only verifies whether the XML is well-formed.</span></span> <span data-ttu-id="d0e9c-119">Doğrulamanın gerçekleşmesi için, <xref:System.Xml.XmlReader> sınıfını kullanarak oluşturmanız gerekir <xref:System.Xml.XmlReaderSettings> .</span><span class="sxs-lookup"><span data-stu-id="d0e9c-119">In order for validation to occur, you need to create an <xref:System.Xml.XmlReader> using the <xref:System.Xml.XmlReaderSettings> class.</span></span> <span data-ttu-id="d0e9c-120"><xref:System.Xml.XmlReader>Sınıfı, BIR DTD veya şema tanım dili (xsd) şeması kullanarak doğrulamayı zorlayabilir.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-120">The <xref:System.Xml.XmlReader> class can enforce validation using a DTD or Schema definition language (XSD) schema.</span></span> <span data-ttu-id="d0e9c-121"><xref:System.Xml.ValidationType>Sınıftaki özelliği, <xref:System.Xml.XmlReaderSettings> <xref:System.Xml.XmlReader> Örneğin doğrulamayı zorluyor olduğunu belirler.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-121">The <xref:System.Xml.ValidationType> property on the <xref:System.Xml.XmlReaderSettings> class determines whether the <xref:System.Xml.XmlReader> instance enforces validation.</span></span> <span data-ttu-id="d0e9c-122">XML verilerini doğrulama hakkında daha fazla bilgi için başvuru sayfasının açıklamalar bölümüne bakın <xref:System.Xml.XmlReader> .</span><span class="sxs-lookup"><span data-stu-id="d0e9c-122">For more information about validating XML data, see the Remarks section of the <xref:System.Xml.XmlReader> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d0e9c-123">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="d0e9c-123">See also</span></span>

- [<span data-ttu-id="d0e9c-124">XML Belge Nesne Modeli (DOM)</span><span class="sxs-lookup"><span data-stu-id="d0e9c-124">XML Document Object Model (DOM)</span></span>](xml-document-object-model-dom.md)
