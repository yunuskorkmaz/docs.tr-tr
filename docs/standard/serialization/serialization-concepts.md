---
title: Serileştirme kavramları
ms.date: 08/07/2017
ms.assetid: e1ff4740-20a1-4c76-a8ad-d857db307054
ms.openlocfilehash: 99716a6346689ac4d3201f83b0b8204cad462e8e
ms.sourcegitcommit: c7a7e1468bf0fa7f7065de951d60dfc8d5ba89f5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/14/2019
ms.locfileid: "65593338"
---
# <a name="serialization-concepts"></a><span data-ttu-id="309c9-102">Serileştirme kavramları</span><span class="sxs-lookup"><span data-stu-id="309c9-102">Serialization concepts</span></span>
<span data-ttu-id="309c9-103">Neden serileştirme kullanmak istiyor?</span><span class="sxs-lookup"><span data-stu-id="309c9-103">Why would you want to use serialization?</span></span> <span data-ttu-id="309c9-104">İki en önemli bir tam kopya daha sonraki bir aşamada yeniden oluşturulabilir için bir depolama ortamına bir nesne durumunu sürdürülmesi için ve nesne değerine göre bir uygulama etki alanından diğerine gönderilecek nedenleridir.</span><span class="sxs-lookup"><span data-stu-id="309c9-104">The two most important reasons are to persist the state of an object to a storage medium so an exact copy can be re-created at a later stage, and to send the object by value from one application domain to another.</span></span> <span data-ttu-id="309c9-105">Örneğin, serileştirme ASP.NET oturum durumu Kaydet ve Pano'ya Windows Forms nesneleri kopyalamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="309c9-105">For example, serialization is used to save session state in ASP.NET and to copy objects to the Clipboard in Windows Forms.</span></span> <span data-ttu-id="309c9-106">Bu aynı zamanda nesneleri değerine göre bir uygulama etki alanından diğerine geçmesine uzaktan iletişim tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="309c9-106">It is also used by remoting to pass objects by value from one application domain to another.</span></span>

[!INCLUDE [binary-serialization-warning](../../../includes/binary-serialization-warning.md)]

## <a name="persistent-storage"></a><span data-ttu-id="309c9-107">Kalıcı depolama</span><span class="sxs-lookup"><span data-stu-id="309c9-107">Persistent storage</span></span>
<span data-ttu-id="309c9-108">Genellikle, disk ve ardından, daha sonra bu verileri almak için bir nesne alanlarının değeri depolamak gereklidir.</span><span class="sxs-lookup"><span data-stu-id="309c9-108">It is often necessary to store the value of the fields of an object to disk and then, later, retrieve this data.</span></span> <span data-ttu-id="309c9-109">Bu seri hale getirme üzerinde bağlı olmadan elde etmek kolay olsa da bu sık hantal yaklaşımdır ve hataya ve nesneleri hiyerarşisini izlemek gerektiğinde giderek daha karmaşık hale gelir.</span><span class="sxs-lookup"><span data-stu-id="309c9-109">Although this is easy to achieve without relying on serialization, this approach is often cumbersome and error prone, and becomes progressively more complex when you need to track a hierarchy of objects.</span></span> <span data-ttu-id="309c9-110">Nesneleri binlerce içeren bir büyük iş uygulaması yazma ve Kaydet ve alanlar ve özellikler için ve her nesne için diskten geri yüklemek için kod yazmadan düşünün.</span><span class="sxs-lookup"><span data-stu-id="309c9-110">Imagine writing a large business application, that contains thousands of objects, and having to write code to save and restore the fields and properties to and from disk for each object.</span></span> <span data-ttu-id="309c9-111">Serileştirme bu amaç elde etmek için kullanışlı bir mekanizma sağlar.</span><span class="sxs-lookup"><span data-stu-id="309c9-111">Serialization provides a convenient mechanism for achieving this objective.</span></span>

<span data-ttu-id="309c9-112">Ortak dil çalışma zamanı nesneleri bellekte nasıl depolanır yöneten ve kullanarak bir otomatik serileştirme mekanizması sağlar [yansıma](../../../docs/framework/reflection-and-codedom/reflection.md).</span><span class="sxs-lookup"><span data-stu-id="309c9-112">The common language runtime manages how objects are stored in memory and provides an automated serialization mechanism by using [reflection](../../../docs/framework/reflection-and-codedom/reflection.md).</span></span> <span data-ttu-id="309c9-113">Bir nesne seri olduğunda, sınıf, derleme ve tüm veri üyeleri sınıf örneği adı için depolama yazılır.</span><span class="sxs-lookup"><span data-stu-id="309c9-113">When an object is serialized, the name of the class, the assembly, and all the data members of the class instance are written to storage.</span></span> <span data-ttu-id="309c9-114">Üye değişkenleri genellikle depolama başvurularının diğer örnekleri nesneleri.</span><span class="sxs-lookup"><span data-stu-id="309c9-114">Objects often store references to other instances in member variables.</span></span> <span data-ttu-id="309c9-115">Sınıf serileştirilmiş olduğunda, serileştirme motoruna zaten, aynı nesne birden çok kez serileştirildiği değil emin olmak için seri başvurulan nesneleri izler.</span><span class="sxs-lookup"><span data-stu-id="309c9-115">When the class is serialized, the serialization engine tracks referenced objects, already serialized, to ensure that the same object is not serialized more than once.</span></span> <span data-ttu-id="309c9-116">Serileştirme mimarisi, .NET Framework ile doğru işleyen grafikler ve döngüsel başvurulara otomatik olarak nesne sağlanır.</span><span class="sxs-lookup"><span data-stu-id="309c9-116">The serialization architecture provided with the .NET Framework correctly handles object graphs and circular references automatically.</span></span> <span data-ttu-id="309c9-117">Serileştirilmiş nesne tarafından başvurulan tüm nesneleri olarak da işaretlenmelidir nesne grafikler üzerinde yerleştirilen tek gereksinim olmasıdır `Serializable` (daha fazla bilgi için [temel serileştirme](basic-serialization.md)).</span><span class="sxs-lookup"><span data-stu-id="309c9-117">The only requirement placed on object graphs is that all objects, referenced by the serialized object, must also be marked as `Serializable` (for more information, see [Basic Serialization](basic-serialization.md)).</span></span> <span data-ttu-id="309c9-118">Bu yapılmadığı, işaretsiz nesneyi serileştirmek seri hale getirici girişiminde bulunduğunda bir özel durum.</span><span class="sxs-lookup"><span data-stu-id="309c9-118">If this is not done, an exception will be thrown when the serializer attempts to serialize the unmarked object.</span></span>

<span data-ttu-id="309c9-119">Serileştirilmiş sınıfı seri durumdan sınıf yeniden ve tüm veri üyelerinin değerlerini otomatik olarak geri yüklenir.</span><span class="sxs-lookup"><span data-stu-id="309c9-119">When the serialized class is deserialized, the class is recreated and the values of all the data members are automatically restored.</span></span>

## <a name="marshal-by-value"></a><span data-ttu-id="309c9-120">Değere göre sıralama</span><span class="sxs-lookup"><span data-stu-id="309c9-120">Marshal by value</span></span>
<span data-ttu-id="309c9-121">Nesneleri oluşturulan burada yalnızca uygulama etki alanında geçerli değil.</span><span class="sxs-lookup"><span data-stu-id="309c9-121">Objects are valid only in the application domain where they are created.</span></span> <span data-ttu-id="309c9-122">Nesne öğesinden türer sürece her türlü girişim nesne parametre olarak geçir veya bu döndürmek için sonuç olarak başarısız `MarshalByRefObject` veya olarak işaretlenmiş `Serializable`.</span><span class="sxs-lookup"><span data-stu-id="309c9-122">Any attempt to pass the object as a parameter or return it as a result will fail unless the object derives from `MarshalByRefObject` or is marked as `Serializable`.</span></span> <span data-ttu-id="309c9-123">Nesne olarak işaretlenmişse `Serializable`, otomatik olarak nesne olacak seri, bir uygulama etki alanından diğer taşınan ve ikinci uygulama etki alanındaki nesnenin tam bir kopyasını oluşturmak için seri durumdan.</span><span class="sxs-lookup"><span data-stu-id="309c9-123">If the object is marked as `Serializable`, the object will automatically be serialized, transported from the one application domain to the other, and then deserialized to produce an exact copy of the object in the second application domain.</span></span> <span data-ttu-id="309c9-124">Bu işlem genellikle hazırlanır tarafından değeri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="309c9-124">This process is typically referred to as marshal-by-value.</span></span>
 
<span data-ttu-id="309c9-125">Bir nesneyi zaman türetilir `MarshalByRefObject`, bir nesne başvurusu nesne yerine başka bir uygulama etki alanına geçirilir.</span><span class="sxs-lookup"><span data-stu-id="309c9-125">When an object derives from `MarshalByRefObject`, an object reference is passed from one application domain to another, rather than the object itself.</span></span> <span data-ttu-id="309c9-126">Türetilen bir nesne da işaretleyebilirsiniz `MarshalByRefObject` olarak `Serializable`.</span><span class="sxs-lookup"><span data-stu-id="309c9-126">You can also mark an object that derives from `MarshalByRefObject` as `Serializable`.</span></span> <span data-ttu-id="309c9-127">Bu nesne kullanıldığında uzaktan iletişim, bir vekil Seçici ile önceden yapılandırılmış seri hale getirme, sorumlu biçimlendirici (`SurrogateSelector`), seri hale getirme işlemi denetimini alır ve türetilen tüm nesneleri değiştirir `MarshalByRefObject` ile bir ara sunucu.</span><span class="sxs-lookup"><span data-stu-id="309c9-127">When this object is used with remoting, the formatter responsible for serialization, which has been preconfigured with a surrogate selector (`SurrogateSelector`), takes control of the serialization process, and replaces all objects derived from `MarshalByRefObject` with a proxy.</span></span> <span data-ttu-id="309c9-128">Olmadan `SurrogateSelector` yerinde, serileştirme mimarisi açıklanan standart serileştirme kurallara [seri hale getirme işleminde adımları](steps-in-the-serialization-process.md).</span><span class="sxs-lookup"><span data-stu-id="309c9-128">Without the `SurrogateSelector` in place, the serialization architecture follows the standard serialization rules described in [Steps in the Serialization Process](steps-in-the-serialization-process.md).</span></span>  

## <a name="related-sections"></a><span data-ttu-id="309c9-129">İlgili bölümler</span><span class="sxs-lookup"><span data-stu-id="309c9-129">Related sections</span></span>  
 [<span data-ttu-id="309c9-130">İkili Serileştirme</span><span class="sxs-lookup"><span data-stu-id="309c9-130">Binary Serialization</span></span>](../../../docs/standard/serialization/binary-serialization.md)  
 <span data-ttu-id="309c9-131">Ortak dil çalışma zamanı ile içerdiği ikili serileştirme mekanizması açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="309c9-131">Describes the binary serialization mechanism that is included with the common language runtime.</span></span>  
  
 <span data-ttu-id="309c9-132">[.NET uzaktan iletişim](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/72x4h507(v=vs.100))\\</span><span class="sxs-lookup"><span data-stu-id="309c9-132">[.NET Remoting](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/72x4h507(v=vs.100))\\</span></span>
 <span data-ttu-id="309c9-133">Çeşitli iletişimleri .NET Framework için kullanılabilecek yöntemleri Uzaktan iletişimler açıklar.</span><span class="sxs-lookup"><span data-stu-id="309c9-133">Describes the various communications methods available in the .NET Framework for remote communications.</span></span>  
  
 [<span data-ttu-id="309c9-134">XML ve SOAP Serileştirme</span><span class="sxs-lookup"><span data-stu-id="309c9-134">XML and SOAP Serialization</span></span>](../../../docs/standard/serialization/xml-and-soap-serialization.md)  
 <span data-ttu-id="309c9-135">Ortak dil çalışma zamanı ile içerdiği XML ve SOAP serileştirme mekanizması açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="309c9-135">Describes the XML and SOAP serialization mechanism that is included with the common language runtime.</span></span>
