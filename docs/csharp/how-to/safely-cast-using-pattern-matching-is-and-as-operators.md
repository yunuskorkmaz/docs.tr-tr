---
title: 'Nasıl yapılır: model eşleştirme ve ve olarak işleç kullanarak güvenli bir şekilde atama'
description: Değişkenleri farklı bir türe güvenle dönüştürmek için model eşleştirme tekniklerini kullanmayı öğrenin. Türleri güvenli bir şekilde dönüştürmek için model eşleştirmeyi ve as işleçlerini da kullanabilirsiniz.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 8d090df1338c535b11a7fd3ec32f6d1cb00b338f
ms.sourcegitcommit: 22be09204266253d45ece46f51cc6f080f2b3fd6
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/07/2019
ms.locfileid: "73739687"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="707a1-104">Nasıl yapılır: model eşleştirme ve ve olarak işleç kullanarak güvenli bir şekilde atama</span><span class="sxs-lookup"><span data-stu-id="707a1-104">How to: safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="707a1-105">Nesneler çok biçimli olduğundan, bir taban sınıf türü değişkeni türetilmiş bir [türü](../programming-guide/types/index.md)tutmak mümkündür.</span><span class="sxs-lookup"><span data-stu-id="707a1-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="707a1-106">Türetilmiş türün örnek üyelerine erişmek için değeri türetilmiş türe [dönüştürmek](../programming-guide/types/casting-and-type-conversions.md) gerekir.</span><span class="sxs-lookup"><span data-stu-id="707a1-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="707a1-107">Ancak, bir atama <xref:System.InvalidCastException>oluşturma riskini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="707a1-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="707a1-108">C#yalnızca başarılı olacağı zaman bir tür dönüştürme gerçekleştiren [model eşleştirme](../pattern-matching.md) deyimleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="707a1-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="707a1-109">C#Ayrıca, bir değerin belirli bir türde olup olmadığını test etmek için [de ve işlecini](../language-reference/operators/type-testing-and-cast.md#is-operator) [de sağlar](../language-reference/operators/type-testing-and-cast.md#as-operator) .</span><span class="sxs-lookup"><span data-stu-id="707a1-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="707a1-110">Aşağıdaki kod, eşleşen `is` ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="707a1-110">The following code demonstrates the pattern matching `is` statement.</span></span> <span data-ttu-id="707a1-111">Bir yöntem bağımsız değişkenini test eden, olası bir türetilmiş tür kümesinden biri olup olmadığını belirlemede yöntemler içerir:</span><span class="sxs-lookup"><span data-stu-id="707a1-111">It contains methods that test a method argument to determine if it is one of a possible set of derived types:</span></span>

[!code-csharp[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="707a1-112">Önceki örnekte, model eşleştirme sözdiziminin birkaç özelliği gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="707a1-112">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="707a1-113">`if (a is Mammal m)` ve `if (o is Mammal m)` deyimleri, testi bir başlatma atamasıyla birleştirir.</span><span class="sxs-lookup"><span data-stu-id="707a1-113">The `if (a is Mammal m)` and `if (o is Mammal m)` statements combine the test with an initialization assignment.</span></span> <span data-ttu-id="707a1-114">Atama yalnızca test başarılı olduğunda gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="707a1-114">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="707a1-115">Değişken `m`, yalnızca atandığı katıştırılmış `if` deyimindeki kapsamdadır.</span><span class="sxs-lookup"><span data-stu-id="707a1-115">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="707a1-116">`m` aynı yöntemde daha sonra erişemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-116">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="707a1-117">Etkileşimli pencerede deneyin.</span><span class="sxs-lookup"><span data-stu-id="707a1-117">Try it in the interactive window.</span></span>

<span data-ttu-id="707a1-118">Aşağıdaki örnek kodda gösterildiği gibi, [null yapılabilir bir değer türünün](../language-reference/builtin-types/nullable-value-types.md) bir değere sahip olup olmadığını test etmek için aynı sözdizimini de kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="707a1-118">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following sample code:</span></span>

[!code-csharp[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="707a1-119">Önceki örnek, dönüşümlerle kullanılacak olan diğer model eşleme özelliklerini gösterir.</span><span class="sxs-lookup"><span data-stu-id="707a1-119">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="707a1-120">`null` değeri için özel olarak denetleyerek null deseninin bir değişkenini test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-120">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="707a1-121">Değişkenin çalışma zamanı değeri `null`olduğunda, bir türü denetleyen `is` bir bildirim her zaman `false`döndürür.</span><span class="sxs-lookup"><span data-stu-id="707a1-121">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="707a1-122">`is` ifadesiyle eşleşen bir değer null değer `Nullable<int>``int?` türüne izin vermez, ancak başka bir değer türü için test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-122">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="707a1-123">Yukarıdaki örnekteki `is` desenleri Nullable değer türleriyle sınırlı değildir.</span><span class="sxs-lookup"><span data-stu-id="707a1-123">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="707a1-124">Bir başvuru türü değişkeninin bir `null`değere sahip olup olmadığını test etmek için bu desenleri de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-124">You also can use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="707a1-125">Yukarıdaki örnek ayrıca, değişkenin birçok farklı türden biri olabileceği bir `switch` deyiminde model eşleştirme `is` ifadesini nasıl kullanacağınızı gösterir.</span><span class="sxs-lookup"><span data-stu-id="707a1-125">The preceding sample also shows how you use the pattern matching `is` expression in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="707a1-126">Bir değişkenin verilen bir tür olup olmadığını test etmek istiyorsanız, ancak bunu yeni bir değişkene atamadıysanız, başvuru türleri ve null yapılabilir türler için `is` ve `as` işleçlerini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-126">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable types.</span></span> <span data-ttu-id="707a1-127">Aşağıdaki kod, bir değişken belirli bir türde ise test için, modelin bir parçası C# olan `is` ve `as` deyimlerinin nasıl kullanılacağını gösterir:</span><span class="sxs-lookup"><span data-stu-id="707a1-127">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="707a1-128">Bu kodu model eşleştirme koduyla karşılaştırarak görebileceğiniz gibi, model eşleştirme sözdizimi, testi ve atamayı tek bir bildirimde birleştirerek daha sağlam özellikler sağlar.</span><span class="sxs-lookup"><span data-stu-id="707a1-128">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="707a1-129">Mümkün olan her seferinde model eşleştirme sözdizimini kullanın.</span><span class="sxs-lookup"><span data-stu-id="707a1-129">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="707a1-130">Bu örnekleri, [GitHub deponuzdaki](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast)koda bakarak deneyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-130">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="707a1-131">Ya da örnekleri [zip dosyası olarak](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip)indirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="707a1-131">Or you can download the samples [as a zip file](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span></span>
