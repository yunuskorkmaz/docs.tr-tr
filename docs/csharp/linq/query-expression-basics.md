---
title: İfade sorgusu temelleri (C#'da LINQ)
description: Sorgu ifadelerle ilgili kavramları tanır
ms.date: 11/30/2016
ms.assetid: 027db1f8-346f-44d2-a16e-043fcea3a4e0
ms.openlocfilehash: 83beaa82d4b4b42ff9da5230edddd391b33a0717
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79173360"
---
# <a name="query-expression-basics"></a><span data-ttu-id="b7f03-103">Sorgu ifadesi temelleri</span><span class="sxs-lookup"><span data-stu-id="b7f03-103">Query expression basics</span></span>

<span data-ttu-id="b7f03-104">Bu makalede, C#'daki sorgu ifadelerle ilgili temel kavramlar tanıtışlar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-104">This article introduces the basic concepts related to query expressions in C#.</span></span>

## <a name="what-is-a-query-and-what-does-it-do"></a><span data-ttu-id="b7f03-105">Sorgu nedir ve ne işe yarar?</span><span class="sxs-lookup"><span data-stu-id="b7f03-105">What is a query and what does it do?</span></span>

<span data-ttu-id="b7f03-106">*Sorgu,* belirli bir veri kaynağından (veya kaynaklardan) hangi verilerin alınacağını ve döndürülen verilerin hangi şekil ve kuruluşa sahip olması gerektiğini açıklayan bir yönerge kümesidir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-106">A *query* is a set of instructions that describes what data to retrieve from a given data source (or sources) and what shape and organization the returned data should have.</span></span> <span data-ttu-id="b7f03-107">Sorgu, ürettiği sonuçlardan farklıdır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-107">A query is distinct from the results that it produces.</span></span>

<span data-ttu-id="b7f03-108">Genellikle, kaynak veriler mantıksal olarak aynı türöğelerdizisi olarak düzenlenir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-108">Generally, the source data is organized logically as a sequence of elements of the same kind.</span></span> <span data-ttu-id="b7f03-109">Örneğin, bir SQL veritabanı tablosu bir satır sırası içerir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-109">For example, a SQL database table contains a sequence of rows.</span></span> <span data-ttu-id="b7f03-110">Bir XML dosyasında, XML öğelerinin bir "sırası" vardır (ancak bunlar bir ağaç yapısında hiyerarşik olarak düzenlenir).</span><span class="sxs-lookup"><span data-stu-id="b7f03-110">In an XML file, there is a "sequence" of XML elements (although these are organized hierarchically in a tree structure).</span></span> <span data-ttu-id="b7f03-111">Bellek içi koleksiyon bir nesne dizisi içerir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-111">An in-memory collection contains a sequence of objects.</span></span>

<span data-ttu-id="b7f03-112">Bir uygulamanın bakış açısından, özgün kaynak verilerin belirli türü ve yapısı önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-112">From an application's viewpoint, the specific type and structure of the original source data is not important.</span></span> <span data-ttu-id="b7f03-113">Uygulama her zaman kaynak verileri <xref:System.Collections.Generic.IEnumerable%601> <xref:System.Linq.IQueryable%601> bir veya koleksiyon olarak görür.</span><span class="sxs-lookup"><span data-stu-id="b7f03-113">The application always sees the source data as an <xref:System.Collections.Generic.IEnumerable%601> or <xref:System.Linq.IQueryable%601> collection.</span></span> <span data-ttu-id="b7f03-114">Örneğin, LINQ'dan XML'e kadar kaynak veriler `IEnumerable` \< <xref:System.Xml.Linq.XElement>> olarak görünür hale getirilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-114">For example, in LINQ to XML, the source data is made visible as an `IEnumerable`\<<xref:System.Xml.Linq.XElement>>.</span></span>

<span data-ttu-id="b7f03-115">Bu kaynak sırası göz önüne alındığında, bir sorgu üç şeyden birini yapabilir:</span><span class="sxs-lookup"><span data-stu-id="b7f03-115">Given this source sequence, a query may do one of three things:</span></span>

- <span data-ttu-id="b7f03-116">Tek tek öğeleri değiştirmeden yeni bir dizi oluşturmak için öğelerin bir alt kümesini alın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-116">Retrieve a subset of the elements to produce a new sequence without modifying the individual elements.</span></span> <span data-ttu-id="b7f03-117">Sorgu daha sonra aşağıdaki örnekte gösterildiği gibi döndürülen sırayı çeşitli `scores` şekillerde `int[]`sıralayabilir veya gruplayabilir (varsayalım bir):</span><span class="sxs-lookup"><span data-stu-id="b7f03-117">The query may then sort or group the returned sequence in various ways, as shown in the following example (assume `scores` is an `int[]`):</span></span>

    [!code-csharp[csrefQueryExpBasics#45](~/samples/snippets/csharp/concepts/linq/query-expression-basics_1.cs)]

- <span data-ttu-id="b7f03-118">Önceki örnekte olduğu gibi bir öğe dizisi alın, ancak bunları yeni bir nesne türüne dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="b7f03-118">Retrieve a sequence of elements as in the previous example but transform them to a new type of object.</span></span> <span data-ttu-id="b7f03-119">Örneğin, bir sorgu bir veri kaynağındaki belirli müşteri kayıtlarından yalnızca son adları alabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-119">For example, a query may retrieve only the last names from certain customer records in a data source.</span></span> <span data-ttu-id="b7f03-120">Veya tam kaydı alabilir ve son sonuç sırasını oluşturmadan önce başka bir bellek nesne si veya xml veri oluşturmak için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-120">Or it may retrieve the complete record and then use it to construct another in-memory object type or even XML data before generating the final result sequence.</span></span> <span data-ttu-id="b7f03-121">Aşağıdaki örnek, bir 'den `int` `string`bir'e bir projeksiyon gösterir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-121">The following example shows a projection from an `int` to a `string`.</span></span> <span data-ttu-id="b7f03-122">Yeni türe `highScoresQuery`dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="b7f03-122">Note the new type of `highScoresQuery`.</span></span>

    [!code-csharp[csrefQueryExpBasics#46](~/samples/snippets/csharp/concepts/linq/query-expression-basics_2.cs)]

- <span data-ttu-id="b7f03-123">Kaynak veriler le ilgili tek ton değerinde aşağıdakiler gibi alın:</span><span class="sxs-lookup"><span data-stu-id="b7f03-123">Retrieve a singleton value about the source data, such as:</span></span>

  - <span data-ttu-id="b7f03-124">Belirli bir koşulla eşleşen öğelerin sayısı.</span><span class="sxs-lookup"><span data-stu-id="b7f03-124">The number of elements that match a certain condition.</span></span>

  - <span data-ttu-id="b7f03-125">En büyük veya en az değeri olan öğe.</span><span class="sxs-lookup"><span data-stu-id="b7f03-125">The element that has the greatest or least value.</span></span>

  - <span data-ttu-id="b7f03-126">Bir koşulla eşleşen ilk öğe veya belirli bir öğe kümesindeki belirli değerlerin toplamı.</span><span class="sxs-lookup"><span data-stu-id="b7f03-126">The first element that matches a condition, or the sum of particular values in a specified set of elements.</span></span> <span data-ttu-id="b7f03-127">Örneğin, aşağıdaki sorgu, sonda dizisinden `scores` 80'den büyük puan sayısını döndürür:</span><span class="sxs-lookup"><span data-stu-id="b7f03-127">For example, the following query returns the number of scores greater than 80 from the `scores` integer array:</span></span>

    [!code-csharp[csrefQueryExpBasics#47](~/samples/snippets/csharp/concepts/linq/query-expression-basics_3.cs)]

    <span data-ttu-id="b7f03-128">Önceki örnekte, yönteme çağrıdan önce sorgu ifadesi etrafında parantez `Count` kullanımına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="b7f03-128">In the previous example, note the use of parentheses around the query expression before the call to the `Count` method.</span></span> <span data-ttu-id="b7f03-129">Ayrıca somut sonucu depolamak için yeni bir değişken kullanarak bunu ifade edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-129">You can also express this by using a new variable to store the concrete result.</span></span> <span data-ttu-id="b7f03-130">Sorguyu depolayan değişkeni bir sonucu depolayan sorgudan ayrı tuttuğundan, bu teknik daha okunabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-130">This technique is more readable because it keeps the variable that stores the query separate from the query that stores a result.</span></span>

    [!code-csharp[csrefQueryExpBasics#48](~/samples/snippets/csharp/concepts/linq/query-expression-basics_4.cs)]

<span data-ttu-id="b7f03-131">Önceki örnekte, sorgu , `Count`tarafından döndürülen `Count` öğelerin sayısını belirlemek için sonuçlar üzerinde yinelemek gerekir, çünkü çağrı yürütülür `highScoresQuery`.</span><span class="sxs-lookup"><span data-stu-id="b7f03-131">In the previous example, the query is executed in the call to `Count`, because `Count` must iterate over the results in order to determine the number of elements returned by `highScoresQuery`.</span></span>

## <a name="what-is-a-query-expression"></a><span data-ttu-id="b7f03-132">Sorgu ifadesi nedir?</span><span class="sxs-lookup"><span data-stu-id="b7f03-132">What is a query expression?</span></span>

<span data-ttu-id="b7f03-133">*Sorgu ifadesi* sorgu sözdiziminde ifade edilen bir sorgudur.</span><span class="sxs-lookup"><span data-stu-id="b7f03-133">A *query expression* is a query expressed in query syntax.</span></span> <span data-ttu-id="b7f03-134">Sorgu ifadesi birinci sınıf bir dil yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-134">A query expression is a first-class language construct.</span></span> <span data-ttu-id="b7f03-135">Diğer ifadeler gibi ve C# ifadesinin geçerli olduğu herhangi bir bağlamda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-135">It is just like any other expression and can be used in any context in which a C# expression is valid.</span></span> <span data-ttu-id="b7f03-136">Sorgu ifadesi, SQL veya XQuery'ye benzer bir bildirim sözdiziminde yazılmış bir yan tümce kümesinden oluşur.</span><span class="sxs-lookup"><span data-stu-id="b7f03-136">A query expression consists of a set of clauses written in a declarative syntax similar to SQL or XQuery.</span></span> <span data-ttu-id="b7f03-137">Sırayla her yan tümce bir veya daha fazla C# ifadeleri içerir ve bu ifadeler kendilerini bir sorgu ifadesi olabilir veya bir sorgu ifadesi içerir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-137">Each clause in turn contains one or more C# expressions, and these expressions may themselves be either a query expression or contain a query expression.</span></span>

<span data-ttu-id="b7f03-138">Sorgu ifadesi [bir from](../language-reference/keywords/from-clause.md) yan tümcesi ile başlamalı ve bir [seçme](../language-reference/keywords/select-clause.md) veya [grup](../language-reference/keywords/group-clause.md) yan tümcesi ile bitmelidir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-138">A query expression must begin with a [from](../language-reference/keywords/from-clause.md) clause and must end with a [select](../language-reference/keywords/select-clause.md) or [group](../language-reference/keywords/group-clause.md) clause.</span></span> <span data-ttu-id="b7f03-139">`from` İlk yan tümce `select` ile `group` son veya yan tümce arasında, bu isteğe bağlı yan tümcelerden bir veya daha fazlasını içerebilir: [burada,](../language-reference/keywords/where-clause.md) [orderby,](../language-reference/keywords/orderby-clause.md) [join,](../language-reference/keywords/join-clause.md) [let](../language-reference/keywords/let-clause.md) ve hatta yan [tümcelerden](../language-reference/keywords/from-clause.md) ek.</span><span class="sxs-lookup"><span data-stu-id="b7f03-139">Between the first `from` clause and the last `select` or `group` clause, it can contain one or more of these optional clauses: [where](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [join](../language-reference/keywords/join-clause.md), [let](../language-reference/keywords/let-clause.md) and even additional [from](../language-reference/keywords/from-clause.md) clauses.</span></span> <span data-ttu-id="b7f03-140">Bir `join` veya `group` yan tümcenin sonucunu, aynı sorgu ifadesinde ek sorgu yan tümceleri için kaynak olarak kullanmak için de anahtar kelimeyi kullanabilirsiniz. [into](../language-reference/keywords/into.md)</span><span class="sxs-lookup"><span data-stu-id="b7f03-140">You can also use the [into](../language-reference/keywords/into.md) keyword to enable the result of a `join` or `group` clause to serve as the source for additional query clauses in the same query expression.</span></span>

### <a name="query-variable"></a><span data-ttu-id="b7f03-141">Sorgu değişkeni</span><span class="sxs-lookup"><span data-stu-id="b7f03-141">Query variable</span></span>

<span data-ttu-id="b7f03-142">LINQ'da, sorgu değişkeni, *sorgunun sonuçları* yerine *sorgu* depolayan herhangi bir değişkendir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-142">In LINQ, a query variable is any variable that stores a *query* instead of the *results* of a query.</span></span> <span data-ttu-id="b7f03-143">Daha spesifik olarak, bir sorgu değişkeni her zaman bir `foreach` deyim veya `IEnumerator.MoveNext` yöntemine doğrudan çağrı üzerinde yinelemeli öğelerin bir dizi üretecek numaralandırılabilir türüdür.</span><span class="sxs-lookup"><span data-stu-id="b7f03-143">More specifically, a query variable is always an enumerable type that will produce a sequence of elements when it is iterated over in a `foreach` statement or a direct call to its `IEnumerator.MoveNext` method.</span></span>

<span data-ttu-id="b7f03-144">Aşağıdaki kod örneği, bir veri kaynağı, bir filtreleme yan tümcesi, bir sipariş tümcesi ve kaynak öğelerinin dönüşümü olmayan basit bir sorgu ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-144">The following code example shows a simple query expression with one data source, one filtering clause, one ordering clause, and no transformation of the source elements.</span></span> <span data-ttu-id="b7f03-145">Yan `select` tümce sorguyu sona erdirer.</span><span class="sxs-lookup"><span data-stu-id="b7f03-145">The `select` clause ends the query.</span></span>

[!code-csharp[csrefQueryExpBasics#49](~/samples/snippets/csharp/concepts/linq/query-expression-basics_5.cs)]

<span data-ttu-id="b7f03-146">Önceki örnekte, `scoreQuery` bazen sadece bir sorgu olarak adlandırılan bir *sorgu değişkeni* dir. *query*</span><span class="sxs-lookup"><span data-stu-id="b7f03-146">In the previous example, `scoreQuery` is a *query variable,* which is sometimes referred to as just a *query*.</span></span> <span data-ttu-id="b7f03-147">Sorgu değişkeni, döngüde üretilen hiçbir gerçek `foreach` sonuç verisini depolar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-147">The query variable stores no actual result data, which is produced in the `foreach` loop.</span></span> <span data-ttu-id="b7f03-148">Ve `foreach` deyim yürütüldüğünde, sorgu sonuçları sorgu değişkeni `scoreQuery`üzerinden döndürülmez.</span><span class="sxs-lookup"><span data-stu-id="b7f03-148">And when the `foreach` statement executes, the query results are not returned through the query variable `scoreQuery`.</span></span> <span data-ttu-id="b7f03-149">Bunun yerine, yineleme değişkeni `testScore`üzerinden döndürülürler.</span><span class="sxs-lookup"><span data-stu-id="b7f03-149">Rather, they are returned through the iteration variable `testScore`.</span></span> <span data-ttu-id="b7f03-150">`scoreQuery` Değişken ikinci `foreach` bir döngü içinde yinelenebilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-150">The `scoreQuery` variable can be iterated in a second `foreach` loop.</span></span> <span data-ttu-id="b7f03-151">Ne o ne de veri kaynağı değiştirilmediğinden aynı sonuçları verir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-151">It will produce the same results as long as neither it nor the data source has been modified.</span></span>

<span data-ttu-id="b7f03-152">Sorgu değişkeni sorgu sözdizimi veya yöntem sözdizimi veya ikisinin bir arada ifade edilen bir sorgu saklayabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-152">A query variable may store a query that is expressed in query syntax or method syntax, or a combination of the two.</span></span> <span data-ttu-id="b7f03-153">Aşağıdaki örneklerde, `queryMajorCities` hem `queryMajorCities2` ve sorgu değişkenleri vardır:</span><span class="sxs-lookup"><span data-stu-id="b7f03-153">In the following examples, both `queryMajorCities` and `queryMajorCities2` are query variables:</span></span>

[!code-csharp[csrefQueryExpBasics#50](~/samples/snippets/csharp/concepts/linq/query-expression-basics_6.cs)]

<span data-ttu-id="b7f03-154">Diğer taraftan, aşağıdaki iki örnek, her biri bir sorguyla baş harfe getiriliyor olsa bile sorgu değişkeni olmayan değişkenleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-154">On the other hand, the following two examples show variables that are not query variables even though each is initialized with a query.</span></span> <span data-ttu-id="b7f03-155">Sonuçları depoladıkları için sorgu değişkenleri değildir:</span><span class="sxs-lookup"><span data-stu-id="b7f03-155">They are not query variables because they store results:</span></span>

[!code-csharp[csrefQueryExpBasics#51](~/samples/snippets/csharp/concepts/linq/query-expression-basics_7.cs)]

<span data-ttu-id="b7f03-156">Sorguları ifade etmenin farklı yolları hakkında daha fazla bilgi için [LINQ'da Sorgu sözdizimi ve yöntem sözdizimi'ne](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-156">For more information about the different ways to express queries, see [Query syntax and method syntax in LINQ](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span></span>

#### <a name="explicit-and-implicit-typing-of-query-variables"></a><span data-ttu-id="b7f03-157">Sorgu değişkenlerinin açık ve örtülü olarak yazması</span><span class="sxs-lookup"><span data-stu-id="b7f03-157">Explicit and implicit typing of query variables</span></span>

<span data-ttu-id="b7f03-158">Bu belge genellikle sorgu değişkeni ve [select yan tümcesi](../language-reference/keywords/select-clause.md)arasındaki tür ilişkisini göstermek için sorgu değişkeninin açık türünü sağlar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-158">This documentation usually provides the explicit type of the query variable in order to show the type relationship between the query variable and the [select clause](../language-reference/keywords/select-clause.md).</span></span> <span data-ttu-id="b7f03-159">Ancak, derleyiciye bir sorgu değişkeninin (veya başka bir yerel değişkenin) türünü derleme zamanında çıkarmasını öğretmek için [var](../language-reference/keywords/var.md) anahtar sözcüklerini de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-159">However, you can also use the [var](../language-reference/keywords/var.md) keyword to instruct the compiler to infer the type of a query variable (or any other local variable) at compile time.</span></span> <span data-ttu-id="b7f03-160">Örneğin, bu konuda daha önce gösterilen sorgu örneği de örtülü yazı kullanılarak ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="b7f03-160">For example, the query example that was shown previously in this topic can also be expressed by using implicit typing:</span></span>

[!code-csharp[csrefQueryExpBasics#52](~/samples/snippets/csharp/concepts/linq/query-expression-basics_8.cs)]

<span data-ttu-id="b7f03-161">Daha fazla bilgi için, [LINQ sorgu işlemlerinde](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md) [örtük olarak yazılan yerel değişkenler](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) ve Tür ilişkileri'ne bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-161">For more information, see [Implicitly typed local variables](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) and [Type relationships in LINQ query operations](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span></span>

### <a name="starting-a-query-expression"></a><span data-ttu-id="b7f03-162">Sorgu ifadesini başlatma</span><span class="sxs-lookup"><span data-stu-id="b7f03-162">Starting a query expression</span></span>

<span data-ttu-id="b7f03-163">Sorgu ifadesi bir `from` yan tümceyle başlamalıdır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-163">A query expression must begin with a `from` clause.</span></span> <span data-ttu-id="b7f03-164">Bir veri kaynağını bir aralık değişkeni ile birlikte belirtir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-164">It specifies a data source together with a range variable.</span></span> <span data-ttu-id="b7f03-165">Aralık değişkeni, kaynak sırası geçerken kaynak dizideki her ardışık öğeyi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="b7f03-165">The range variable represents each successive element in the source sequence as the source sequence is being traversed.</span></span> <span data-ttu-id="b7f03-166">Aralık değişkeni, veri kaynağındaki öğelerin türüne göre güçlü bir şekilde yazılır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-166">The range variable is strongly typed based on the type of elements in the data source.</span></span> <span data-ttu-id="b7f03-167">Aşağıdaki örnekte, `countries` `Country` nesnelerin bir dizi olduğundan, aralık değişkeni de `Country`.</span><span class="sxs-lookup"><span data-stu-id="b7f03-167">In the following example, because `countries` is an array of `Country` objects, the range variable is also typed as `Country`.</span></span> <span data-ttu-id="b7f03-168">Aralık değişkeni güçlü bir şekilde yazıldığı için, türün kullanılabilir üyelerine erişmek için nokta işleci'ni kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-168">Because the range variable is strongly typed, you can use the dot operator to access any available members of the type.</span></span>

[!code-csharp[csrefQueryExpBasics#53](~/samples/snippets/csharp/concepts/linq/query-expression-basics_9.cs)]

<span data-ttu-id="b7f03-169">Aralık değişkeni, sorgu yarım sütunlu veya *devam* yan tümcesi ile çıkana kadar kapsamdadır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-169">The range variable is in scope until the query is exited either with a semicolon or with a *continuation* clause.</span></span>

<span data-ttu-id="b7f03-170">Sorgu ifadesi birden `from` çok yan tümce içerebilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-170">A query expression may contain multiple `from` clauses.</span></span> <span data-ttu-id="b7f03-171">Kaynak `from` dizideki her öğe bir koleksiyon olduğunda veya bir koleksiyon içeriyorsa ek yan tümceler kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-171">Use additional `from` clauses when each element in the source sequence is itself a collection or contains a collection.</span></span> <span data-ttu-id="b7f03-172">Örneğin, `Country` her biri `City` . `Cities`</span><span class="sxs-lookup"><span data-stu-id="b7f03-172">For example, assume that you have a collection of `Country` objects, each of which contains a collection of `City` objects named `Cities`.</span></span> <span data-ttu-id="b7f03-173">Her `City` `Country`bir innesneleri sorgulamak `from` için, burada gösterildiği gibi iki yan tümce kullanın:</span><span class="sxs-lookup"><span data-stu-id="b7f03-173">To query the `City` objects in each `Country`, use two `from` clauses as shown here:</span></span>

[!code-csharp[csrefQueryExpBasics#54](~/samples/snippets/csharp/concepts/linq/query-expression-basics_10.cs)]

<span data-ttu-id="b7f03-174">Daha fazla bilgi [için, yan tümceye](../language-reference/keywords/from-clause.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-174">For more information, see [from clause](../language-reference/keywords/from-clause.md).</span></span>

### <a name="ending-a-query-expression"></a><span data-ttu-id="b7f03-175">Sorgu ifadesini sonlandırma</span><span class="sxs-lookup"><span data-stu-id="b7f03-175">Ending a query expression</span></span>

<span data-ttu-id="b7f03-176">Sorgu ifadesi bir `group` yan tümce `select` veya yan tümceyle sona ermelidir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-176">A query expression must end with either a `group` clause or a `select` clause.</span></span>

#### <a name="group-clause"></a><span data-ttu-id="b7f03-177">group tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-177">group clause</span></span>

<span data-ttu-id="b7f03-178">Belirttiğiniz `group` bir anahtar tarafından düzenlenen gruplar dizisi oluşturmak için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-178">Use the `group` clause to produce a sequence of groups organized by a key that you specify.</span></span> <span data-ttu-id="b7f03-179">Anahtar herhangi bir veri türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-179">The key can be any data type.</span></span> <span data-ttu-id="b7f03-180">Örneğin, aşağıdaki sorgu, bir veya daha fazla `Country` nesne içeren ve anahtarı `char` bir değer olan bir grup dizisi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="b7f03-180">For example, the following query creates a sequence of groups that contains one or more `Country` objects and whose key is a `char` value.</span></span>

[!code-csharp[csrefQueryExpBasics#55](~/samples/snippets/csharp/concepts/linq/query-expression-basics_11.cs)]

<span data-ttu-id="b7f03-181">Gruplandırma hakkında daha fazla bilgi için [grup yan tümcesi'ne](../language-reference/keywords/group-clause.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-181">For more information about grouping, see [group clause](../language-reference/keywords/group-clause.md).</span></span>

#### <a name="select-clause"></a><span data-ttu-id="b7f03-182">select tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-182">select clause</span></span>

<span data-ttu-id="b7f03-183">Diğer `select` tüm dizi türlerini oluşturmak için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-183">Use the `select` clause to produce all other types of sequences.</span></span> <span data-ttu-id="b7f03-184">Basit `select` bir yan tümce yalnızca veri kaynağında bulunan nesnelerle aynı türde bir nesne dizisi üretir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-184">A simple `select` clause just produces a sequence of the same type of objects as the objects that are contained in the data source.</span></span> <span data-ttu-id="b7f03-185">Bu örnekte, veri `Country` kaynağı nesneleri içerir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-185">In this example, the data source contains `Country` objects.</span></span> <span data-ttu-id="b7f03-186">Yan `orderby` tümce yalnızca öğeleri yeni bir `select` düzene sıralar ve `Country` yan tümce yeniden sıralanan nesnelerin bir dizi üretir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-186">The `orderby` clause just sorts the elements into a new order and the `select` clause produces a sequence of the reordered `Country` objects.</span></span>

[!code-csharp[csrefQueryExpBasics#56](~/samples/snippets/csharp/concepts/linq/query-expression-basics_12.cs)]

<span data-ttu-id="b7f03-187">Yan `select` tümce, kaynak verileri yeni türler dizilerine dönüştürmek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-187">The `select` clause can be used to transform source data into sequences of new types.</span></span> <span data-ttu-id="b7f03-188">Bu dönüşüm de bir *projeksiyon*olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-188">This transformation is also named a *projection*.</span></span> <span data-ttu-id="b7f03-189">Aşağıdaki örnekte, `select` yan tümce, özgün öğedeki alanların yalnızca bir alt kümesini içeren bir dizi anonim tür *de yeğler.*</span><span class="sxs-lookup"><span data-stu-id="b7f03-189">In the following example, the `select` clause *projects* a sequence of anonymous types which contains only a subset of the fields in the original element.</span></span> <span data-ttu-id="b7f03-190">Yeni nesnelerin bir nesne başharfi kullanılarak baş harflere başharfe çarptırılmış olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-190">Note that the new objects are initialized by using an object initializer.</span></span>

[!code-csharp[csrefQueryExpBasics#57](~/samples/snippets/csharp/concepts/linq/query-expression-basics_13.cs)]

<span data-ttu-id="b7f03-191">Bir `select` yan tümcenin kaynak verileri dönüştürmek için kullanılabilme yolları hakkında daha fazla bilgi için [select yan tümcesine](../language-reference/keywords/select-clause.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-191">For more information about all the ways that a `select` clause can be used to transform source data, see [select clause](../language-reference/keywords/select-clause.md).</span></span>

#### <a name="continuations-with-into"></a><span data-ttu-id="b7f03-192">"Into" ile devam eder</span><span class="sxs-lookup"><span data-stu-id="b7f03-192">Continuations with "into"</span></span>

<span data-ttu-id="b7f03-193">Bir `select` veya `group` `into` yan tümcedeki anahtar sözcüğü, sorgu yu saklayan geçici bir tanımlayıcı oluşturmak için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-193">You can use the `into` keyword in a `select` or `group` clause to create a temporary identifier that stores a query.</span></span> <span data-ttu-id="b7f03-194">Gruplandırma veya seçme işleminden sonra sorguda ek sorgu işlemleri gerçekleştirmeniz gerektiğinde bunu yapın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-194">Do this when you must perform additional query operations on a query after a grouping or select operation.</span></span> <span data-ttu-id="b7f03-195">Aşağıdaki örnekte `countries` 10 milyon aralığındanüfusa göre gruplandırılır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-195">In the following example `countries` are grouped according to population in ranges of 10 million.</span></span> <span data-ttu-id="b7f03-196">Bu gruplar oluşturulduktan sonra, ek yan tümceler bazı grupları filtreler ve ardından grupları artan sırada sırayla sıralar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-196">After these groups are created, additional clauses filter out some groups, and then to sort the groups in ascending order.</span></span> <span data-ttu-id="b7f03-197">Bu ek işlemleri gerçekleştirmek için, `countryGroup` temsil edilen devamı gereklidir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-197">To perform those additional operations, the continuation represented by `countryGroup` is required.</span></span>

[!code-csharp[csrefQueryExpBasics#58](~/samples/snippets/csharp/concepts/linq/query-expression-basics_14.cs)]

<span data-ttu-id="b7f03-198">Daha fazla bilgi için [bkz.](../language-reference/keywords/into.md)</span><span class="sxs-lookup"><span data-stu-id="b7f03-198">For more information, see [into](../language-reference/keywords/into.md).</span></span>

### <a name="filtering-ordering-and-joining"></a><span data-ttu-id="b7f03-199">Filtreleme, sıralama ve birleştirme</span><span class="sxs-lookup"><span data-stu-id="b7f03-199">Filtering, ordering, and joining</span></span>

<span data-ttu-id="b7f03-200">`from` Başlangıç yan tümcesi `select` `group` ile bitiş veya yan`where` `join`tümce arasında, diğer tüm yan tümceler (, `orderby`, , `from`, `let`) isteğe bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-200">Between the starting `from` clause, and the ending `select` or `group` clause, all other clauses (`where`, `join`, `orderby`, `from`, `let`) are optional.</span></span> <span data-ttu-id="b7f03-201">İsteğe bağlı yan tümcelerden herhangi biri sorgu gövdesinde sıfır kez veya birden çok kez kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-201">Any of the optional clauses may be used zero times or multiple times in a query body.</span></span>

#### <a name="where-clause"></a><span data-ttu-id="b7f03-202">where tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-202">where clause</span></span>

<span data-ttu-id="b7f03-203">Bir `where` veya daha fazla yüklem ifadesini temel alan kaynak verilerdeki öğeleri filtrelemek için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-203">Use the `where` clause to filter out elements from the source data based on one or more predicate expressions.</span></span> <span data-ttu-id="b7f03-204">Aşağıdaki `where` örnekteki tümcenin iki koşullu bir yüklemi vardır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-204">The `where` clause in the following example has one predicate with two conditions.</span></span>

[!code-csharp[csrefQueryExpBasics#59](~/samples/snippets/csharp/concepts/linq/query-expression-basics_15.cs)]

<span data-ttu-id="b7f03-205">Daha fazla bilgi için [bkz.](../language-reference/keywords/where-clause.md)</span><span class="sxs-lookup"><span data-stu-id="b7f03-205">For more information, see [where clause](../language-reference/keywords/where-clause.md).</span></span>

#### <a name="orderby-clause"></a><span data-ttu-id="b7f03-206">orderby tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-206">orderby clause</span></span>

<span data-ttu-id="b7f03-207">Sonuçları `orderby` artan veya azalan sırada sıralamak için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-207">Use the `orderby` clause to sort the results in either ascending or descending order.</span></span> <span data-ttu-id="b7f03-208">İkincil sıralama siparişleri de belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-208">You can also specify secondary sort orders.</span></span> <span data-ttu-id="b7f03-209">Aşağıdaki örneközelliği kullanarak `country` `Area` nesneler üzerinde birincil sıralama gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-209">The following example performs a primary sort on the `country` objects by using the `Area` property.</span></span> <span data-ttu-id="b7f03-210">Daha sonra `Population` özelliği kullanarak ikincil bir sıralama gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-210">It then performs a secondary sort by using the `Population` property.</span></span>

[!code-csharp[csrefQueryExpBasics#60](~/samples/snippets/csharp/concepts/linq/query-expression-basics_16.cs)]

<span data-ttu-id="b7f03-211">Anahtar `ascending` kelime isteğe bağlıdır; sipariş belirtilmemişse varsayılan sıralama sırasıdır.</span><span class="sxs-lookup"><span data-stu-id="b7f03-211">The `ascending` keyword is optional; it is the default sort order if no order is specified.</span></span> <span data-ttu-id="b7f03-212">Daha fazla bilgi için [orderby yan tümcesi'ne](../language-reference/keywords/orderby-clause.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-212">For more information, see [orderby clause](../language-reference/keywords/orderby-clause.md).</span></span>

#### <a name="join-clause"></a><span data-ttu-id="b7f03-213">join tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-213">join clause</span></span>

<span data-ttu-id="b7f03-214">Her `join` öğede belirtilen anahtarlar arasında eşitlik karşılaştırması dayalı bir veri kaynağıöğeleri ile ilişkilendirmek ve / veya bir veri kaynağı öğeleri birleştirmek için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-214">Use the `join` clause to associate and/or combine elements from one data source with elements from another data source based on an equality comparison between specified keys in each element.</span></span> <span data-ttu-id="b7f03-215">LINQ'da birleştirme işlemleri, öğeleri farklı türde olan nesne dizileri üzerinde gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-215">In LINQ, join operations are performed on sequences of objects whose elements are different types.</span></span> <span data-ttu-id="b7f03-216">İki diziyi birleştirdikten sonra, çıktı `select` `group` dizisinde hangi öğeyi depolayacağınızı belirtmek için bir veya deyim kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-216">After you have joined two sequences, you must use a `select` or `group` statement to specify which element to store in the output sequence.</span></span> <span data-ttu-id="b7f03-217">Ayrıca, ilişkili öğelerin her kümesindeki özellikleri çıktı dizisi için yeni bir türde birleştirmek için anonim bir tür de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-217">You can also use an anonymous type to combine properties from each set of associated elements into a new type for the output sequence.</span></span> <span data-ttu-id="b7f03-218">Aşağıdaki örnek, `prod` `Category` özelliği `categories` dize dizisindeki kategorilerden biriyle eşleşen nesneleri ilişkilendirir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-218">The following example associates `prod` objects whose `Category` property matches one of the categories in the `categories` string array.</span></span> <span data-ttu-id="b7f03-219">Herhangi `Category` bir dize `categories` yle eşleşmeyen ürünler filtrelenir. İfade, `select` özellikleri her ikisinden de `cat` alınan `prod`yeni bir tür ve .</span><span class="sxs-lookup"><span data-stu-id="b7f03-219">Products whose `Category` does not match any string in `categories` are filtered out. The `select` statement projects a new type whose properties are taken from both `cat` and `prod`.</span></span>

[!code-csharp[csrefQueryExpBasics#61](~/samples/snippets/csharp/concepts/linq/query-expression-basics_17.cs)]

<span data-ttu-id="b7f03-220">Ayrıca, `join` işlemin sonuçlarını anahtar [kelimeye kullanarak](../language-reference/keywords/into.md) geçici bir değişkene depolayarak bir grup birleştirme gerçekleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-220">You can also perform a group join by storing the results of the `join` operation into a temporary variable by using the [into](../language-reference/keywords/into.md) keyword.</span></span> <span data-ttu-id="b7f03-221">Daha fazla bilgi için [ara yan tümcesi'ne](../language-reference/keywords/join-clause.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-221">For more information, see [join clause](../language-reference/keywords/join-clause.md).</span></span>

#### <a name="let-clause"></a><span data-ttu-id="b7f03-222">let tümcesi</span><span class="sxs-lookup"><span data-stu-id="b7f03-222">let clause</span></span>

<span data-ttu-id="b7f03-223">Yöntem `let` çağrısı gibi bir ifadenin sonucunu yeni bir aralık değişkeninde depolamak için yan tümceyi kullanın.</span><span class="sxs-lookup"><span data-stu-id="b7f03-223">Use the `let` clause to store the result of an expression, such as a method call, in a new range variable.</span></span> <span data-ttu-id="b7f03-224">Aşağıdaki örnekte, aralık `firstName` değişkeni tarafından `Split`döndürülen dize dizisinin ilk öğesini depolar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-224">In the following example, the range variable `firstName` stores the first element of the array of strings that is returned by `Split`.</span></span>

[!code-csharp[csrefQueryExpBasics#62](~/samples/snippets/csharp/concepts/linq/query-expression-basics_18.cs)]

<span data-ttu-id="b7f03-225">Daha fazla bilgi için [bkz.](../language-reference/keywords/let-clause.md)</span><span class="sxs-lookup"><span data-stu-id="b7f03-225">For more information, see [let clause](../language-reference/keywords/let-clause.md).</span></span>

### <a name="subqueries-in-a-query-expression"></a><span data-ttu-id="b7f03-226">Sorgu ifadesindeki alt sorgular</span><span class="sxs-lookup"><span data-stu-id="b7f03-226">Subqueries in a query expression</span></span>

<span data-ttu-id="b7f03-227">Sorgu yan tümcesi, bazen *alt sorgu*olarak adlandırılan bir sorgu ifadesi içerebilir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-227">A query clause may itself contain a query expression, which is sometimes referred to as a *subquery*.</span></span> <span data-ttu-id="b7f03-228">Her alt sorgu, `from` ilk `from` yan tümcedeki aynı veri kaynağını mutlaka işaret etmeyen kendi yan tümcesiyle başlar.</span><span class="sxs-lookup"><span data-stu-id="b7f03-228">Each subquery starts with its own `from` clause that does not necessarily point to the same data source in the first `from` clause.</span></span> <span data-ttu-id="b7f03-229">Örneğin, aşağıdaki sorgu, bir gruplandırma işleminin sonuçlarını almak için select deyiminde kullanılan bir sorgu ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="b7f03-229">For example, the following query shows a query expression that is used in the select statement to retrieve the results of a grouping operation.</span></span>

[!code-csharp[csrefQueryExpBasics#63](~/samples/snippets/csharp/concepts/linq/query-expression-basics_19.cs)]

<span data-ttu-id="b7f03-230">Daha fazla bilgi için bkz. [Gruplandırma işleminde bir alt sorgu yap.](perform-a-subquery-on-a-grouping-operation.md)</span><span class="sxs-lookup"><span data-stu-id="b7f03-230">For more information, see [Perform a subquery on a grouping operation](perform-a-subquery-on-a-grouping-operation.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="b7f03-231">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="b7f03-231">See also</span></span>

- [<span data-ttu-id="b7f03-232">C# programlama kılavuzu</span><span class="sxs-lookup"><span data-stu-id="b7f03-232">C# programming guide</span></span>](../programming-guide/index.md)
- [<span data-ttu-id="b7f03-233">Dil ile Tümleşik Sorgu (LINQ)</span><span class="sxs-lookup"><span data-stu-id="b7f03-233">Language Integrated Query (LINQ)</span></span>](index.md)
- [<span data-ttu-id="b7f03-234">Anahtar kelimeleri sorgula (LINQ)</span><span class="sxs-lookup"><span data-stu-id="b7f03-234">Query keywords (LINQ)</span></span>](../language-reference/keywords/query-keywords.md)
- [<span data-ttu-id="b7f03-235">Standart sorgu operatörlerine genel bakış</span><span class="sxs-lookup"><span data-stu-id="b7f03-235">Standard query operators overview</span></span>](../programming-guide/concepts/linq/standard-query-operators-overview.md)
