---
title: Sorgu ifadesi temelleri (C# üzerinde LINQ)
description: Sorgu ifadeleri ilgili kavramlar tanıtılmaktadır.
ms.date: 11/30/2016
ms.assetid: 027db1f8-346f-44d2-a16e-043fcea3a4e0
ms.openlocfilehash: 68f338381e354f4944539d63ca3a3cc3500031c1
ms.sourcegitcommit: ba5c189bf44d44204a3e8838e59ec378a62d82f3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/12/2018
ms.locfileid: "44705774"
---
# <a name="query-expression-basics"></a><span data-ttu-id="d628e-103">Sorgu ifadesi temelleri</span><span class="sxs-lookup"><span data-stu-id="d628e-103">Query expression basics</span></span>

<span data-ttu-id="d628e-104">Bu makalede, C# sorgu ifadeleri ilgili temel kavramlar tanıtılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="d628e-104">This article introduces the basic concepts related to query expressions in C#.</span></span>

## <a name="what-is-a-query-and-what-does-it-do"></a><span data-ttu-id="d628e-105">Bir sorgu nedir ve ne işe yarar?</span><span class="sxs-lookup"><span data-stu-id="d628e-105">What is a query and what does it do?</span></span>

<span data-ttu-id="d628e-106">A *sorgu* döndürülen verileri hangi verilerin belirli bir veri kaynağı (veya kaynaklarından) almak ve hangi şekli ve kuruluş için olmalıdır açıklayan yönergeler kümesidir.</span><span class="sxs-lookup"><span data-stu-id="d628e-106">A *query* is a set of instructions that describes what data to retrieve from a given data source (or sources) and what shape and organization the returned data should have.</span></span> <span data-ttu-id="d628e-107">Bir sorgu ürettiği sonuçlardan farklıdır.</span><span class="sxs-lookup"><span data-stu-id="d628e-107">A query is distinct from the results that it produces.</span></span>

<span data-ttu-id="d628e-108">Genel olarak, kaynak verileri mantıksal olarak aynı türden bir öğe dizisi düzenlenmiştir.</span><span class="sxs-lookup"><span data-stu-id="d628e-108">Generally, the source data is organized logically as a sequence of elements of the same kind.</span></span> <span data-ttu-id="d628e-109">Örneğin, bir SQL veritabanı tablosunda bir dizi satır içerir.</span><span class="sxs-lookup"><span data-stu-id="d628e-109">For example, a SQL database table contains a sequence of rows.</span></span> <span data-ttu-id="d628e-110">Bir XML dosyasında yoktur bir "dizisi" XML öğesi (bunlar hiyerarşik bir ağaç yapısında düzenlenmiş rağmen).</span><span class="sxs-lookup"><span data-stu-id="d628e-110">In an XML file, there is a "sequence" of XML elements (although these are organized hierarchically in a tree structure).</span></span> <span data-ttu-id="d628e-111">Bir bellek içi koleksiyon nesneleri dizisi içerir.</span><span class="sxs-lookup"><span data-stu-id="d628e-111">An in-memory collection contains a sequence of objects.</span></span>

<span data-ttu-id="d628e-112">Bir uygulamanın bakış açısından belirli türüne ve yapısına özgün kaynak verilerin önemli olmadığından.</span><span class="sxs-lookup"><span data-stu-id="d628e-112">From an application's viewpoint, the specific type and structure of the original source data is not important.</span></span> <span data-ttu-id="d628e-113">Uygulama her zaman kaynak verisi olarak gördüğü bir <xref:System.Collections.Generic.IEnumerable%601> veya <xref:System.Linq.IQueryable%601> koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="d628e-113">The application always sees the source data as an <xref:System.Collections.Generic.IEnumerable%601> or <xref:System.Linq.IQueryable%601> collection.</span></span> <span data-ttu-id="d628e-114">Örneğin, LINQ to XML, kaynak veriler olarak görünür hale gelir bir `IEnumerable` \< <xref:System.Xml.Linq.XElement>>.</span><span class="sxs-lookup"><span data-stu-id="d628e-114">For example, in LINQ to XML, the source data is made visible as an `IEnumerable`\<<xref:System.Xml.Linq.XElement>>.</span></span>

<span data-ttu-id="d628e-115">Bu kaynak dizisi göz önünde bulundurulduğunda, bir sorgu işlemlerden birini yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="d628e-115">Given this source sequence, a query may do one of three things:</span></span>

- <span data-ttu-id="d628e-116">Tek tek öğelerine değiştirmeden yeni bir sıra oluşturmak için öğeleri kümesini alır.</span><span class="sxs-lookup"><span data-stu-id="d628e-116">Retrieve a subset of the elements to produce a new sequence without modifying the individual elements.</span></span> <span data-ttu-id="d628e-117">Sorgu sonra sıralama veya aşağıdaki örnekte gösterildiği gibi çeşitli şekillerde döndürülen dizi Grup (varsayar `scores` olduğu bir `int[]`):</span><span class="sxs-lookup"><span data-stu-id="d628e-117">The query may then sort or group the returned sequence in various ways, as shown in the following example (assume `scores` is an `int[]`):</span></span>

    [!code-csharp[csrefQueryExpBasics#45](~/samples/snippets/csharp/concepts/linq/query-expression-basics_1.cs)]

- <span data-ttu-id="d628e-118">Önceki örnekte olduğu gibi öğeleri dizisi alabilir, ancak bunları yeni bir nesne türüne dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="d628e-118">Retrieve a sequence of elements as in the previous example but transform them to a new type of object.</span></span> <span data-ttu-id="d628e-119">Örneğin, bir sorgu, bir veri kaynağındaki belirli bir müşteri kayıt yalnızca son adlarını alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d628e-119">For example, a query may retrieve only the last names from certain customer records in a data source.</span></span> <span data-ttu-id="d628e-120">Kaydın tamamına almak ve başka bir bellek içi nesne türü veya hatta kullanmak Sonuç dizisi oluşturmadan XML verileri.</span><span class="sxs-lookup"><span data-stu-id="d628e-120">Or it may retrieve the complete record and then use it to construct another in-memory object type or even XML data before generating the final result sequence.</span></span> <span data-ttu-id="d628e-121">Aşağıdaki örnek, bir projeksiyon gelen gösterir. bir `int` için bir `string`.</span><span class="sxs-lookup"><span data-stu-id="d628e-121">The following example shows a projection from an `int` to a `string`.</span></span> <span data-ttu-id="d628e-122">Yeni Not `highScoresQuery`.</span><span class="sxs-lookup"><span data-stu-id="d628e-122">Note the new type of `highScoresQuery`.</span></span>

    [!code-csharp[csrefQueryExpBasics#46](~/samples/snippets/csharp/concepts/linq/query-expression-basics_2.cs)]

- <span data-ttu-id="d628e-123">Kaynak veriler hakkında bir tekil değer gibi alın:</span><span class="sxs-lookup"><span data-stu-id="d628e-123">Retrieve a singleton value about the source data, such as:</span></span>

  - <span data-ttu-id="d628e-124">Belirli bir koşulu karşılayan bir öğe sayısı.</span><span class="sxs-lookup"><span data-stu-id="d628e-124">The number of elements that match a certain condition.</span></span>

  - <span data-ttu-id="d628e-125">En yüksek veya en düşük değere sahip öğe.</span><span class="sxs-lookup"><span data-stu-id="d628e-125">The element that has the greatest or least value.</span></span>

  - <span data-ttu-id="d628e-126">Bir koşul ya da belirtilen öğeleri kümesi içinde belirli değerlerin toplamını eşleşen ilk öğe.</span><span class="sxs-lookup"><span data-stu-id="d628e-126">The first element that matches a condition, or the sum of particular values in a specified set of elements.</span></span> <span data-ttu-id="d628e-127">Örneğin, aşağıdaki sorguyu puanları 80'den büyüktür döndürür `scores` tamsayı dizisi:</span><span class="sxs-lookup"><span data-stu-id="d628e-127">For example, the following query returns the number of scores greater than 80 from the `scores` integer array:</span></span>

    [!code-csharp[csrefQueryExpBasics#47](~/samples/snippets/csharp/concepts/linq/query-expression-basics_3.cs)]

    <span data-ttu-id="d628e-128">Önceki örnekte, parantezler çağırmadan önce sorgu ifadesinde kullanımına dikkat edin `Count` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d628e-128">In the previous example, note the use of parentheses around the query expression before the call to the `Count` method.</span></span> <span data-ttu-id="d628e-129">Bu, somut sonucu depolamak için yeni bir değişken kullanarak da koyabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d628e-129">You can also express this by using a new variable to store the concrete result.</span></span> <span data-ttu-id="d628e-130">Sorguyu depolayan değişken sorgudan bir sonuç depolar ayrı tuttuğu bu daha okunabilir bir tekniktir.</span><span class="sxs-lookup"><span data-stu-id="d628e-130">This technique is more readable because it keeps the variable that stores the query separate from the query that stores a result.</span></span>

    [!code-csharp[csrefQueryExpBasics#48](~/samples/snippets/csharp/concepts/linq/query-expression-basics_4.cs)]

<span data-ttu-id="d628e-131">Önceki örnekte, sorgu çağrısında yürütüldüğü `Count`, çünkü `Count` tarafından döndürülen öğe sayısını belirlemek için sonuçları üzerinde yinelenmelidir `highScoresQuery`.</span><span class="sxs-lookup"><span data-stu-id="d628e-131">In the previous example, the query is executed in the call to `Count`, because `Count` must iterate over the results in order to determine the number of elements returned by `highScoresQuery`.</span></span>

## <a name="what-is-a-query-expression"></a><span data-ttu-id="d628e-132">Sorgu ifadesi nedir?</span><span class="sxs-lookup"><span data-stu-id="d628e-132">What is a query expression?</span></span>

<span data-ttu-id="d628e-133">A *sorgu ifadesi* sorgu söz diziminde ifade bir sorgu verilmiştir.</span><span class="sxs-lookup"><span data-stu-id="d628e-133">A *query expression* is a query expressed in query syntax.</span></span> <span data-ttu-id="d628e-134">Birinci sınıf dil yapısı bir sorgu ifadesidir.</span><span class="sxs-lookup"><span data-stu-id="d628e-134">A query expression is a first-class language construct.</span></span> <span data-ttu-id="d628e-135">Bu diğer ifade gibi ve bir C# ifadesi geçerli olduğu her bağlamda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d628e-135">It is just like any other expression and can be used in any context in which a C# expression is valid.</span></span> <span data-ttu-id="d628e-136">Sorgu ifadesi tümceleri SQL veya XQuery benzer bir bildirim temelli söz yazılan bir dizi oluşur.</span><span class="sxs-lookup"><span data-stu-id="d628e-136">A query expression consists of a set of clauses written in a declarative syntax similar to SQL or XQuery.</span></span> <span data-ttu-id="d628e-137">Her yan tümcesi sırayla bir veya daha fazla C# ifadeleri içeriyor ve bu ifadeler kendilerini bir sorgu ifadesi veya olabilir bir sorgu ifadesi içerir.</span><span class="sxs-lookup"><span data-stu-id="d628e-137">Each clause in turn contains one or more C# expressions, and these expressions may themselves be either a query expression or contain a query expression.</span></span>

<span data-ttu-id="d628e-138">Sorgu ifadesi ile başlamalıdır bir [gelen](../language-reference/keywords/from-clause.md) yan tümcesi ve ile sona ermelidir bir [seçin](../language-reference/keywords/select-clause.md) veya [grubu](../language-reference/keywords/group-clause.md) yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-138">A query expression must begin with a [from](../language-reference/keywords/from-clause.md) clause and must end with a [select](../language-reference/keywords/select-clause.md) or [group](../language-reference/keywords/group-clause.md) clause.</span></span> <span data-ttu-id="d628e-139">İlk arasında `from` yan tümcesi ve son `select` veya `group` yan tümcesi, bir veya daha fazla isteğe bağlı bu yan tümceleri içerebilir: [burada](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [birleştirme ](../language-reference/keywords/join-clause.md), [izin](../language-reference/keywords/let-clause.md) ve hatta ek [gelen](../language-reference/keywords/from-clause.md) yan tümceleri.</span><span class="sxs-lookup"><span data-stu-id="d628e-139">Between the first `from` clause and the last `select` or `group` clause, it can contain one or more of these optional clauses: [where](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [join](../language-reference/keywords/join-clause.md), [let](../language-reference/keywords/let-clause.md) and even additional [from](../language-reference/keywords/from-clause.md) clauses.</span></span> <span data-ttu-id="d628e-140">Ayrıca [içine](../language-reference/keywords/into.md) sonucunu etkinleştirmek için anahtar sözcüğü bir `join` veya `group` ek sorgu yan tümceleri aynı sorgu ifadesinde kaynağı olarak görev yapacak yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-140">You can also use the [into](../language-reference/keywords/into.md) keyword to enable the result of a `join` or `group` clause to serve as the source for additional query clauses in the same query expression.</span></span>

### <a name="query-variable"></a><span data-ttu-id="d628e-141">Sorgu değişkeni</span><span class="sxs-lookup"><span data-stu-id="d628e-141">Query variable</span></span>

<span data-ttu-id="d628e-142">LINQ içinde bir sorgu değişkeni depolar herhangi bir değişken olduğu bir *sorgu* yerine *sonuçları* sorgu.</span><span class="sxs-lookup"><span data-stu-id="d628e-142">In LINQ, a query variable is any variable that stores a *query* instead of the *results* of a query.</span></span> <span data-ttu-id="d628e-143">Özellikle, bir sorgu değişkeni her zaman bu yinelenir, bir dizi öğe üretir bir numaralandırılabilir türüdür bir `foreach` deyimi veya doğrudan bir çağrı ile kendi `IEnumerator.MoveNext` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d628e-143">More specifically, a query variable is always an enumerable type that will produce a sequence of elements when it is iterated over in a `foreach` statement or a direct call to its `IEnumerator.MoveNext` method.</span></span>

<span data-ttu-id="d628e-144">Aşağıdaki kod örneği, bir basit bir sorgu ifadesi bir veri kaynağı ile bir filtre yan tümcesi, bir sipariş yan tümce ve kaynak öğeleri herhangi bir dönüşüm gösterir.</span><span class="sxs-lookup"><span data-stu-id="d628e-144">The following code example shows a simple query expression with one data source, one filtering clause, one ordering clause, and no transformation of the source elements.</span></span> <span data-ttu-id="d628e-145">`select` Sorgu yan tümcesi sonlandırır.</span><span class="sxs-lookup"><span data-stu-id="d628e-145">The `select` clause ends the query.</span></span>

[!code-csharp[csrefQueryExpBasics#49](~/samples/snippets/csharp/concepts/linq/query-expression-basics_5.cs)]

<span data-ttu-id="d628e-146">Önceki örnekte, `scoreQuery` olduğu bir *sorgu değişkeni* bazen adlandırılır olarak yalnızca bir *sorgu*.</span><span class="sxs-lookup"><span data-stu-id="d628e-146">In the previous example, `scoreQuery` is a *query variable,* which is sometimes referred to as just a *query*.</span></span> <span data-ttu-id="d628e-147">Sorgu değişkeni içinde oluşturulan hiçbir gerçek sonuç verileri depolayan `foreach` döngü.</span><span class="sxs-lookup"><span data-stu-id="d628e-147">The query variable stores no actual result data, which is produced in the `foreach` loop.</span></span> <span data-ttu-id="d628e-148">Ve ne zaman `foreach` deyimi yürütür, sorgu değişkeni sorgu sonuçları döndürülmez `scoreQuery`.</span><span class="sxs-lookup"><span data-stu-id="d628e-148">And when the `foreach` statement executes, the query results are not returned through the query variable `scoreQuery`.</span></span> <span data-ttu-id="d628e-149">Bunun yerine, yineleme değişkeni döndürülür `testScore`.</span><span class="sxs-lookup"><span data-stu-id="d628e-149">Rather, they are returned through the iteration variable `testScore`.</span></span> <span data-ttu-id="d628e-150">`scoreQuery` Değişkeni bir saniye içinde yinelenir `foreach` döngü.</span><span class="sxs-lookup"><span data-stu-id="d628e-150">The `scoreQuery` variable can be iterated in a second `foreach` loop.</span></span> <span data-ttu-id="d628e-151">Bu ne veri kaynağı değiştirilmiş sürece aynı sonuçları oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d628e-151">It will produce the same results as long as neither it nor the data source has been modified.</span></span>

<span data-ttu-id="d628e-152">Bir sorgu değişkeni sorgu söz dizimi veya yöntem sözdizimi veya ikisinin birleşimini ifade edilen bir sorgu depolayabiliriz.</span><span class="sxs-lookup"><span data-stu-id="d628e-152">A query variable may store a query that is expressed in query syntax or method syntax, or a combination of the two.</span></span> <span data-ttu-id="d628e-153">Aşağıdaki örneklerde, her ikisi de `queryMajorCities` ve `queryMajorCities2` sorgu değişkenleri:</span><span class="sxs-lookup"><span data-stu-id="d628e-153">In the following examples, both `queryMajorCities` and `queryMajorCities2` are query variables:</span></span>

[!code-csharp[csrefQueryExpBasics#50](~/samples/snippets/csharp/concepts/linq/query-expression-basics_6.cs)]

<span data-ttu-id="d628e-154">Öte yandan, aşağıdaki iki örnek, her bir sorgu ile başlatılır olsa bile, sorgu değişkenleri olmayan değişkenlerini gösterir.</span><span class="sxs-lookup"><span data-stu-id="d628e-154">On the other hand, the following two examples show variables that are not query variables even though each is initialized with a query.</span></span> <span data-ttu-id="d628e-155">Sonuçları depoladıkları çünkü bunlar sorgu değişkenleri değildir:</span><span class="sxs-lookup"><span data-stu-id="d628e-155">They are not query variables because they store results:</span></span>

[!code-csharp[csrefQueryExpBasics#51](~/samples/snippets/csharp/concepts/linq/query-expression-basics_7.cs)]

<span data-ttu-id="d628e-156">Hızlı sorgular için farklı yollar hakkında daha fazla bilgi için bkz: [sorgu sözdizimi ve yöntem sözdizimi LINQ](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-156">For more information about the different ways to express queries, see [Query syntax and method syntax in LINQ](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span></span>

#### <a name="explicit-and-implicit-typing-of-query-variables"></a><span data-ttu-id="d628e-157">Açık ve örtük değişkenler sorgu yazma</span><span class="sxs-lookup"><span data-stu-id="d628e-157">Explicit and implicit typing of query variables</span></span>

<span data-ttu-id="d628e-158">Bu belgeler, açık tür sorgu değişkeni sorgu değişkeninin türü ilişkisi göstermek için genellikle sağlar ve [select yan tümcesi](../language-reference/keywords/select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-158">This documentation usually provides the explicit type of the query variable in order to show the type relationship between the query variable and the [select clause](../language-reference/keywords/select-clause.md).</span></span> <span data-ttu-id="d628e-159">Ancak, ayrıca kullanabileceğiniz [var](../language-reference/keywords/var.md) anahtar sözcüğünü kullanarak derleyicinin derleme zamanında bir sorgu değişkeni (veya herhangi bir yerel değişken) türünü çıkarsamak için.</span><span class="sxs-lookup"><span data-stu-id="d628e-159">However, you can also use the [var](../language-reference/keywords/var.md) keyword to instruct the compiler to infer the type of a query variable (or any other local variable) at compile time.</span></span> <span data-ttu-id="d628e-160">Örneğin, bu konuda daha önce gösterilen sorgusu örneği de örtülü yazma'yı kullanarak ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="d628e-160">For example, the query example that was shown previously in this topic can also be expressed by using implicit typing:</span></span>

[!code-csharp[csrefQueryExpBasics#52](~/samples/snippets/csharp/concepts/linq/query-expression-basics_8.cs)]

<span data-ttu-id="d628e-161">Daha fazla bilgi için [örtülü olarak yazılan yerel değişkenler](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) ve [tür ilişkileri LINQ Sorgu işlemleri](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-161">For more information, see [Implicitly typed local variables](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) and [Type relationships in LINQ query operations](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span></span>

### <a name="starting-a-query-expression"></a><span data-ttu-id="d628e-162">Sorgu ifadesi başlatılıyor</span><span class="sxs-lookup"><span data-stu-id="d628e-162">Starting a query expression</span></span>

<span data-ttu-id="d628e-163">Sorgu ifadesi ile başlamalıdır bir `from` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-163">A query expression must begin with a `from` clause.</span></span> <span data-ttu-id="d628e-164">Bu, bir aralık değişkeni ile birlikte bir veri kaynağını belirtir.</span><span class="sxs-lookup"><span data-stu-id="d628e-164">It specifies a data source together with a range variable.</span></span> <span data-ttu-id="d628e-165">Aralık değişkeni kaynak sırası geçiş kaynak sırası art arda gelen her öğeyi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="d628e-165">The range variable represents each successive element in the source sequence as the source sequence is being traversed.</span></span> <span data-ttu-id="d628e-166">Aralık değişkeni bağlı olarak veri kaynağındaki öğelerin türü kesin.</span><span class="sxs-lookup"><span data-stu-id="d628e-166">The range variable is strongly typed based on the type of elements in the data source.</span></span> <span data-ttu-id="d628e-167">Aşağıdaki örnekte, çünkü `countries` dizisidir `Country` nesneleri olarak de Aralık değişkeninin türü `Country`.</span><span class="sxs-lookup"><span data-stu-id="d628e-167">In the following example, because `countries` is an array of `Country` objects, the range variable is also typed as `Country`.</span></span> <span data-ttu-id="d628e-168">Aralık değişkeninin türü kesin olarak belirtilmiş olduğundan, nokta işleci türü kullanılabilir tüm üyelerine erişmek için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d628e-168">Because the range variable is strongly typed, you can use the dot operator to access any available members of the type.</span></span>

[!code-csharp[csrefQueryExpBasics#53](~/samples/snippets/csharp/concepts/linq/query-expression-basics_9.cs)]

<span data-ttu-id="d628e-169">Sorgu veya noktalı virgül ile çıkıldı kadar aralık değişkeni kapsamları dahilinde olması bir *devamlılık* yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-169">The range variable is in scope until the query is exited either with a semicolon or with a *continuation* clause.</span></span>

<span data-ttu-id="d628e-170">Sorgu ifadesi birden çok içerebilir `from` yan tümceleri.</span><span class="sxs-lookup"><span data-stu-id="d628e-170">A query expression may contain multiple `from` clauses.</span></span> <span data-ttu-id="d628e-171">Ek kullanım `from` tümceleri kaynak dizideki her öğe kendisi bir koleksiyon olduğunda veya bir koleksiyonu içerir.</span><span class="sxs-lookup"><span data-stu-id="d628e-171">Use additional `from` clauses when each element in the source sequence is itself a collection or contains a collection.</span></span> <span data-ttu-id="d628e-172">Örneğin, koleksiyonu olduğunu varsayın `Country` nesneleri, her biri bir koleksiyonunu içeren `City` adlı nesneleri `Cities`.</span><span class="sxs-lookup"><span data-stu-id="d628e-172">For example, assume that you have a collection of `Country` objects, each of which contains a collection of `City` objects named `Cities`.</span></span> <span data-ttu-id="d628e-173">Sorgulanacak `City` nesneleri her `Country`, iki kullanın `from` burada gösterildiği şekilde yan tümceleri:</span><span class="sxs-lookup"><span data-stu-id="d628e-173">To query the `City` objects in each `Country`, use two `from` clauses as shown here:</span></span>

[!code-csharp[csrefQueryExpBasics#54](~/samples/snippets/csharp/concepts/linq/query-expression-basics_10.cs)]

<span data-ttu-id="d628e-174">Daha fazla bilgi için [from yan tümcesinde](../language-reference/keywords/from-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-174">For more information, see [from clause](../language-reference/keywords/from-clause.md).</span></span>

### <a name="ending-a-query-expression"></a><span data-ttu-id="d628e-175">Sorgu ifadesi bitiş</span><span class="sxs-lookup"><span data-stu-id="d628e-175">Ending a query expression</span></span>

<span data-ttu-id="d628e-176">Sorgu ifadesi ile bitmelidir bir `group` yan tümcesi veya `select` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-176">A query expression must end with either a `group` clause or a `select` clause.</span></span>

#### <a name="group-clause"></a><span data-ttu-id="d628e-177">group tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-177">group clause</span></span>

<span data-ttu-id="d628e-178">Kullanım `group` grupları dizisini üretmek için yan, belirttiğiniz bir anahtar tarafından düzenlenir.</span><span class="sxs-lookup"><span data-stu-id="d628e-178">Use the `group` clause to produce a sequence of groups organized by a key that you specify.</span></span> <span data-ttu-id="d628e-179">Herhangi bir veri türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="d628e-179">The key can be any data type.</span></span> <span data-ttu-id="d628e-180">Örneğin, bir veya daha fazla bilgi içeren bir dizi gruplarının aşağıdaki sorguyu oluşturur `Country` nesneleri ve anahtarı olan bir `char` değeri.</span><span class="sxs-lookup"><span data-stu-id="d628e-180">For example, the following query creates a sequence of groups that contains one or more `Country` objects and whose key is a `char` value.</span></span>

[!code-csharp[csrefQueryExpBasics#55](~/samples/snippets/csharp/concepts/linq/query-expression-basics_11.cs)]

<span data-ttu-id="d628e-181">Gruplama hakkında daha fazla bilgi için bkz: [group yan tümcesi](../language-reference/keywords/group-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-181">For more information about grouping, see [group clause](../language-reference/keywords/group-clause.md).</span></span>

#### <a name="select-clause"></a><span data-ttu-id="d628e-182">select tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-182">select clause</span></span>

<span data-ttu-id="d628e-183">Kullanım `select` dizileri diğer tüm türleri üretmek için yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-183">Use the `select` clause to produce all other types of sequences.</span></span> <span data-ttu-id="d628e-184">Basit bir `select` yan tümcesi yalnızca bir veri kaynağında bulunan nesneleri aynı türde nesneler dizisi üretir.</span><span class="sxs-lookup"><span data-stu-id="d628e-184">A simple `select` clause just produces a sequence of the same type of objects as the objects that are contained in the data source.</span></span> <span data-ttu-id="d628e-185">Bu örnekte, veri kaynağını içeren `Country` nesneleri.</span><span class="sxs-lookup"><span data-stu-id="d628e-185">In this example, the data source contains `Country` objects.</span></span> <span data-ttu-id="d628e-186">`orderby` Yan tümcesi yalnızca yeni bir düzen ile öğeleri sıralar ve `select` yan tümcesi yeniden düzenlenen bir dizi üretir `Country` nesneleri.</span><span class="sxs-lookup"><span data-stu-id="d628e-186">The `orderby` clause just sorts the elements into a new order and the `select` clause produces a sequence of the reordered `Country` objects.</span></span>

[!code-csharp[csrefQueryExpBasics#56](~/samples/snippets/csharp/concepts/linq/query-expression-basics_12.cs)]

<span data-ttu-id="d628e-187">`select` Yan tümcesi, kaynak verileri yeni türleri sıralarına dönüştürmek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d628e-187">The `select` clause can be used to transform source data into sequences of new types.</span></span> <span data-ttu-id="d628e-188">Bu dönüştürme de adlı bir *projeksiyon*.</span><span class="sxs-lookup"><span data-stu-id="d628e-188">This transformation is also named a *projection*.</span></span> <span data-ttu-id="d628e-189">Aşağıdaki örnekte, `select` yan tümcesi *projeleri* yalnızca özgün öğesi alanları kümesini içeren bir anonim türleri dizisi.</span><span class="sxs-lookup"><span data-stu-id="d628e-189">In the following example, the `select` clause *projects* a sequence of anonymous types which contains only a subset of the fields in the original element.</span></span> <span data-ttu-id="d628e-190">Yeni nesneler bir nesne Başlatıcı kullanarak başlatılır unutmayın.</span><span class="sxs-lookup"><span data-stu-id="d628e-190">Note that the new objects are initialized by using an object initializer.</span></span>

[!code-csharp[csrefQueryExpBasics#57](~/samples/snippets/csharp/concepts/linq/query-expression-basics_13.cs)]

<span data-ttu-id="d628e-191">Tüm yollar hakkında daha fazla bilgi için bir `select` yan tümcesi, kaynak verileri dönüştürür, görmek için kullanılabilir [select yan tümcesi](../language-reference/keywords/select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-191">For more information about all the ways that a `select` clause can be used to transform source data, see [select clause](../language-reference/keywords/select-clause.md).</span></span>

#### <a name="continuations-with-into"></a><span data-ttu-id="d628e-192">"İçinde" devamlılıklarla</span><span class="sxs-lookup"><span data-stu-id="d628e-192">Continuations with "into"</span></span>

<span data-ttu-id="d628e-193">Kullanabileceğiniz `into` anahtar sözcüğü bir `select` veya `group` yan tümcesi bir sorgu depolayan geçici bir tanımlayıcı.</span><span class="sxs-lookup"><span data-stu-id="d628e-193">You can use the `into` keyword in a `select` or `group` clause to create a temporary identifier that stores a query.</span></span> <span data-ttu-id="d628e-194">Bunu bir gruplandırma sonra bir sorgu ek sorgu işlemleri gerçekleştirmek veya işlemi seçin.</span><span class="sxs-lookup"><span data-stu-id="d628e-194">Do this when you must perform additional query operations on a query after a grouping or select operation.</span></span> <span data-ttu-id="d628e-195">Aşağıdaki örnekte `countries` 10 milyon aralığı içinde popülasyon göre gruplandırılır.</span><span class="sxs-lookup"><span data-stu-id="d628e-195">In the following example `countries` are grouped according to population in ranges of 10 million.</span></span> <span data-ttu-id="d628e-196">Sonra bu gruplara bazı Gruplar'kullanıma ve sıralama için oluşturulmuş, ek yan tümceleri filtre sipariş artan grupları.</span><span class="sxs-lookup"><span data-stu-id="d628e-196">After these groups are created, additional clauses filter out some groups, and then to sort the groups in ascending order.</span></span> <span data-ttu-id="d628e-197">Bu ek işlemleri gerçekleştirmek için devamlılık temsil ettiği `countryGroup` gereklidir.</span><span class="sxs-lookup"><span data-stu-id="d628e-197">To perform those additional operations, the continuation represented by `countryGroup` is required.</span></span>

[!code-csharp[csrefQueryExpBasics#58](~/samples/snippets/csharp/concepts/linq/query-expression-basics_14.cs)]

<span data-ttu-id="d628e-198">Daha fazla bilgi için [içine](../language-reference/keywords/into.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-198">For more information, see [into](../language-reference/keywords/into.md).</span></span>

### <a name="filtering-ordering-and-joining"></a><span data-ttu-id="d628e-199">Filtreleme, sıralama ve birleştirme</span><span class="sxs-lookup"><span data-stu-id="d628e-199">Filtering, ordering, and joining</span></span>

<span data-ttu-id="d628e-200">Başlangıç arasında `from` yan tümcesi ve bitiş `select` veya `group` yan tümcesi, diğer tüm yan tümceleri (`where`, `join`, `orderby`, `from`, `let`) isteğe bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="d628e-200">Between the starting `from` clause, and the ending `select` or `group` clause, all other clauses (`where`, `join`, `orderby`, `from`, `let`) are optional.</span></span> <span data-ttu-id="d628e-201">Herhangi bir isteğe bağlı yan tümceleri sıfır veya birden çok kez bir sorgu gövdesi içinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d628e-201">Any of the optional clauses may be used zero times or multiple times in a query body.</span></span>

#### <a name="where-clause"></a><span data-ttu-id="d628e-202">where tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-202">where clause</span></span>

<span data-ttu-id="d628e-203">Kullanım `where` veriler kaynak veri öğeleri filtrelemek için yan tümcesi bir veya daha fazla koşul ifadeleri temel.</span><span class="sxs-lookup"><span data-stu-id="d628e-203">Use the `where` clause to filter out elements from the source data based on one or more predicate expressions.</span></span> <span data-ttu-id="d628e-204">`where` Yan tümcesi aşağıdaki örnekte bir koşula sahip iki koşul vardır.</span><span class="sxs-lookup"><span data-stu-id="d628e-204">The `where` clause in the following example has one predicate with two conditions.</span></span>

[!code-csharp[csrefQueryExpBasics#59](~/samples/snippets/csharp/concepts/linq/query-expression-basics_15.cs)]

<span data-ttu-id="d628e-205">Daha fazla bilgi için [burada yan tümcesi](../language-reference/keywords/where-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-205">For more information, see [where clause](../language-reference/keywords/where-clause.md).</span></span>

#### <a name="orderby-clause"></a><span data-ttu-id="d628e-206">orderby tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-206">orderby clause</span></span>

<span data-ttu-id="d628e-207">Kullanım `orderby` sonuçları artan veya azalan düzende sıralamak için yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-207">Use the `orderby` clause to sort the results in either ascending or descending order.</span></span> <span data-ttu-id="d628e-208">İkincil sıralamalar de belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d628e-208">You can also specify secondary sort orders.</span></span> <span data-ttu-id="d628e-209">Aşağıdaki örnekte birincil sıralama gerçekleştirir `country` kullanarak nesneleri `Area` özelliği.</span><span class="sxs-lookup"><span data-stu-id="d628e-209">The following example performs a primary sort on the `country` objects by using the `Area` property.</span></span> <span data-ttu-id="d628e-210">Kullanarak ardından bir ikincil sıralama gerçekleştirir `Population` özelliği.</span><span class="sxs-lookup"><span data-stu-id="d628e-210">It then performs a secondary sort by using the `Population` property.</span></span>

[!code-csharp[csrefQueryExpBasics#60](~/samples/snippets/csharp/concepts/linq/query-expression-basics_16.cs)]

<span data-ttu-id="d628e-211">`ascending` Anahtar sözcüğü isteğe bağlı; hiçbir sırası belirtilmezse, varsayılan sıralama düzeni.</span><span class="sxs-lookup"><span data-stu-id="d628e-211">The `ascending` keyword is optional; it is the default sort order if no order is specified.</span></span> <span data-ttu-id="d628e-212">Daha fazla bilgi için [orderby yan tümcesinin](../language-reference/keywords/orderby-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-212">For more information, see [orderby clause](../language-reference/keywords/orderby-clause.md).</span></span>

#### <a name="join-clause"></a><span data-ttu-id="d628e-213">join tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-213">join clause</span></span>

<span data-ttu-id="d628e-214">Kullanım `join` her öğeyi belirtilen anahtarlarında bir eşitlik karşılaştırması temel ilişkilendirmek ve/veya başka bir veri kaynağındaki öğeleri içeren bir veri kaynağındaki öğeleri birleştirme yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-214">Use the `join` clause to associate and/or combine elements from one data source with elements from another data source based on an equality comparison between specified keys in each element.</span></span> <span data-ttu-id="d628e-215">LINQ içinde öğeleri farklı türleri olan nesneler dizisi üzerinde birleştirme işlemleri gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="d628e-215">In LINQ, join operations are performed on sequences of objects whose elements are different types.</span></span> <span data-ttu-id="d628e-216">İki sıranın katıldıktan sonra kullanmalısınız bir `select` veya `group` deyimi bir çıkış dizisinde depolamak için hangi öğesi belirtin.</span><span class="sxs-lookup"><span data-stu-id="d628e-216">After you have joined two sequences, you must use a `select` or `group` statement to specify which element to store in the output sequence.</span></span> <span data-ttu-id="d628e-217">Anonim bir tür, çıkış sırası için yeni bir türü her ilişkili öğeleri kümesi özelliklerinden birleştirin için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d628e-217">You can also use an anonymous type to combine properties from each set of associated elements into a new type for the output sequence.</span></span> <span data-ttu-id="d628e-218">Aşağıdaki örnek ilişkilendirir `prod` ayarlanmış nesneleri `Category` özelliği eşleşen kategoride birini `categories` dize dizisi.</span><span class="sxs-lookup"><span data-stu-id="d628e-218">The following example associates `prod` objects whose `Category` property matches one of the categories in the `categories` string array.</span></span> <span data-ttu-id="d628e-219">Ürünler olan `Category` herhangi bir dize eşleşmiyor `categories` filtrelenir. `select` Deyimi özellikleri hem de alınır, yeni bir tür projeleri `cat` ve `prod`.</span><span class="sxs-lookup"><span data-stu-id="d628e-219">Products whose `Category` does not match any string in `categories` are filtered out. The `select` statement projects a new type whose properties are taken from both `cat` and `prod`.</span></span>

[!code-csharp[csrefQueryExpBasics#61](~/samples/snippets/csharp/concepts/linq/query-expression-basics_17.cs)]

<span data-ttu-id="d628e-220">Grup birleştirme sonuçlarının depolayarak de gerçekleştirebilirsiniz `join` işlemi kullanarak geçici bir değişken içine [içine](../language-reference/keywords/into.md) anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="d628e-220">You can also perform a group join by storing the results of the `join` operation into a temporary variable by using the [into](../language-reference/keywords/into.md) keyword.</span></span> <span data-ttu-id="d628e-221">Daha fazla bilgi için [JOIN yan tümcesi](../language-reference/keywords/join-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-221">For more information, see [join clause](../language-reference/keywords/join-clause.md).</span></span>

#### <a name="let-clause"></a><span data-ttu-id="d628e-222">let tümcesi</span><span class="sxs-lookup"><span data-stu-id="d628e-222">let clause</span></span> 

<span data-ttu-id="d628e-223">Kullanım `let` yan gibi bir yöntem çağrısının bir ifadenin sonucu yeni bir aralık değişkeninde depolar.</span><span class="sxs-lookup"><span data-stu-id="d628e-223">Use the `let` clause to store the result of an expression, such as a method call, in a new range variable.</span></span> <span data-ttu-id="d628e-224">Aşağıdaki örnekte, aralık değişkeni `firstName` tarafından döndürülen dizenin dizinin ilk öğesi depolar `Split`.</span><span class="sxs-lookup"><span data-stu-id="d628e-224">In the following example, the range variable `firstName` stores the first element of the array of strings that is returned by `Split`.</span></span>

[!code-csharp[csrefQueryExpBasics#62](~/samples/snippets/csharp/concepts/linq/query-expression-basics_18.cs)]

<span data-ttu-id="d628e-225">Daha fazla bilgi için [let yan tümcesi](../language-reference/keywords/let-clause.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-225">For more information, see [let clause](../language-reference/keywords/let-clause.md).</span></span>

### <a name="subqueries-in-a-query-expression"></a><span data-ttu-id="d628e-226">Bir sorgu ifadesinde alt sorgular</span><span class="sxs-lookup"><span data-stu-id="d628e-226">Subqueries in a query expression</span></span>

<span data-ttu-id="d628e-227">Sorgu yan tümcesi kendisini bazen olarak adlandırılan bir sorgu ifadesi içerebilir bir *alt sorgu*.</span><span class="sxs-lookup"><span data-stu-id="d628e-227">A query clause may itself contain a query expression, which is sometimes referred to as a *subquery*.</span></span> <span data-ttu-id="d628e-228">Her alt sorgu kendi ile başlayan `from` mutlaka aynı veri kaynağına ilk göstermiyor yan tümcesi `from` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="d628e-228">Each subquery starts with its own `from` clause that does not necessarily point to the same data source in the first `from` clause.</span></span> <span data-ttu-id="d628e-229">Örneğin, aşağıdaki sorguyu bir gruplandırma işlemi sonuçlarını almak için select deyiminde kullanılan bir sorgu ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="d628e-229">For example, the following query shows a query expression that is used in the select statement to retrieve the results of a grouping operation.</span></span>

[!code-csharp[csrefQueryExpBasics#63](~/samples/snippets/csharp/concepts/linq/query-expression-basics_19.cs)]

<span data-ttu-id="d628e-230">Daha fazla bilgi için [nasıl yapılır: gruplandırma işleminde alt sorgu gerçekleştirme](perform-a-subquery-on-a-grouping-operation.md).</span><span class="sxs-lookup"><span data-stu-id="d628e-230">For more information, see [How to: perform a subquery on a grouping operation](perform-a-subquery-on-a-grouping-operation.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="d628e-231">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="d628e-231">See also</span></span>

- [<span data-ttu-id="d628e-232">C# programlama kılavuzu</span><span class="sxs-lookup"><span data-stu-id="d628e-232">C# programming guide</span></span>](../programming-guide/index.md)  
- [<span data-ttu-id="d628e-233">Dil ile Tümleşik Sorgu (LINQ)</span><span class="sxs-lookup"><span data-stu-id="d628e-233">Language Integrated Query (LINQ)</span></span>](index.md)  
- [<span data-ttu-id="d628e-234">Sorgu anahtar sözcükleri (LINQ)</span><span class="sxs-lookup"><span data-stu-id="d628e-234">Query keywords (LINQ)</span></span>](../language-reference/keywords/query-keywords.md)  
- [<span data-ttu-id="d628e-235">Standart sorgu işleçlerine genel bakış</span><span class="sxs-lookup"><span data-stu-id="d628e-235">Standard query operators overview</span></span>](../programming-guide/concepts/linq/standard-query-operators-overview.md)  