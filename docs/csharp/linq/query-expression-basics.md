---
title: Sorgu ifadesi temelleri (LINQ ın C#)
description: Sorgu ifadeleriyle ilgili kavramları tanıtır
ms.date: 11/30/2016
ms.assetid: 027db1f8-346f-44d2-a16e-043fcea3a4e0
ms.openlocfilehash: 5ebe2163df47c60c677d7ac911ce0f65529835eb
ms.sourcegitcommit: 7bc6887ab658550baa78f1520ea735838249345e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/03/2020
ms.locfileid: "75635866"
---
# <a name="query-expression-basics"></a><span data-ttu-id="57912-103">Sorgu ifadesi temelleri</span><span class="sxs-lookup"><span data-stu-id="57912-103">Query expression basics</span></span>

<span data-ttu-id="57912-104">Bu makalede, içindeki C#sorgu ifadeleriyle ilgili temel kavramlar tanıtılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="57912-104">This article introduces the basic concepts related to query expressions in C#.</span></span>

## <a name="what-is-a-query-and-what-does-it-do"></a><span data-ttu-id="57912-105">Sorgu nedir ve ne yapar?</span><span class="sxs-lookup"><span data-stu-id="57912-105">What is a query and what does it do?</span></span>

<span data-ttu-id="57912-106">*Sorgu* , belirli bir veri kaynağından (veya kaynaklardan) hangi verilerin alınması gerektiğini ve döndürülen verilerin hangi şekil ve kuruluşun olması gerektiğini açıklayan bir yönergeler kümesidir.</span><span class="sxs-lookup"><span data-stu-id="57912-106">A *query* is a set of instructions that describes what data to retrieve from a given data source (or sources) and what shape and organization the returned data should have.</span></span> <span data-ttu-id="57912-107">Bir sorgu, ürettiği sonuçlardan farklıdır.</span><span class="sxs-lookup"><span data-stu-id="57912-107">A query is distinct from the results that it produces.</span></span>

<span data-ttu-id="57912-108">Genellikle, kaynak verileri aynı türdeki öğelerin bir sırası olarak mantıksal olarak düzenlenir.</span><span class="sxs-lookup"><span data-stu-id="57912-108">Generally, the source data is organized logically as a sequence of elements of the same kind.</span></span> <span data-ttu-id="57912-109">Örneğin, bir SQL veritabanı tablosu bir dizi satır içerir.</span><span class="sxs-lookup"><span data-stu-id="57912-109">For example, a SQL database table contains a sequence of rows.</span></span> <span data-ttu-id="57912-110">XML dosyasında, XML öğelerinin bir "sırası" vardır (ancak bunlar bir ağaç yapısında hiyerarşik olarak düzenlenirler).</span><span class="sxs-lookup"><span data-stu-id="57912-110">In an XML file, there is a "sequence" of XML elements (although these are organized hierarchically in a tree structure).</span></span> <span data-ttu-id="57912-111">Bellek içi bir koleksiyon, bir dizi nesne içerir.</span><span class="sxs-lookup"><span data-stu-id="57912-111">An in-memory collection contains a sequence of objects.</span></span>

<span data-ttu-id="57912-112">Uygulamanın bakış açısından, özgün kaynak verilerinin belirli türü ve yapısı önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="57912-112">From an application's viewpoint, the specific type and structure of the original source data is not important.</span></span> <span data-ttu-id="57912-113">Uygulama, kaynak verileri her zaman bir <xref:System.Collections.Generic.IEnumerable%601> veya <xref:System.Linq.IQueryable%601> koleksiyonu olarak görür.</span><span class="sxs-lookup"><span data-stu-id="57912-113">The application always sees the source data as an <xref:System.Collections.Generic.IEnumerable%601> or <xref:System.Linq.IQueryable%601> collection.</span></span> <span data-ttu-id="57912-114">Örneğin, LINQ to XML, kaynak veriler bir `IEnumerable`\<<xref:System.Xml.Linq.XElement>> olarak görünür hale getirilir.</span><span class="sxs-lookup"><span data-stu-id="57912-114">For example, in LINQ to XML, the source data is made visible as an `IEnumerable`\<<xref:System.Xml.Linq.XElement>>.</span></span>

<span data-ttu-id="57912-115">Bu kaynak sırası verildiğinde bir sorgu üç işlemlerden birini alabilir:</span><span class="sxs-lookup"><span data-stu-id="57912-115">Given this source sequence, a query may do one of three things:</span></span>

- <span data-ttu-id="57912-116">Tek tek öğeleri değiştirmeden yeni bir sıra oluşturmak için öğelerin bir alt kümesini alın.</span><span class="sxs-lookup"><span data-stu-id="57912-116">Retrieve a subset of the elements to produce a new sequence without modifying the individual elements.</span></span> <span data-ttu-id="57912-117">Sorgu daha sonra aşağıdaki örnekte gösterildiği gibi, döndürülen sırayı çeşitli şekillerde sıralayabilir veya gruplandırabilir (`scores` bir `int[]`olduğunu varsayın):</span><span class="sxs-lookup"><span data-stu-id="57912-117">The query may then sort or group the returned sequence in various ways, as shown in the following example (assume `scores` is an `int[]`):</span></span>

    [!code-csharp[csrefQueryExpBasics#45](~/samples/snippets/csharp/concepts/linq/query-expression-basics_1.cs)]

- <span data-ttu-id="57912-118">Önceki örnekte olduğu gibi bir dizi öğe alın, ancak bunları yeni bir nesne türüne dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="57912-118">Retrieve a sequence of elements as in the previous example but transform them to a new type of object.</span></span> <span data-ttu-id="57912-119">Örneğin, bir sorgu bir veri kaynağındaki belirli müşteri kayıtlarından yalnızca soyadlarını alabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-119">For example, a query may retrieve only the last names from certain customer records in a data source.</span></span> <span data-ttu-id="57912-120">Ya da tam kaydı alabilir ve ardından, son sonuç sırasını oluşturmadan önce başka bir bellek içi nesne türü veya hatta XML verisi oluşturmak için bunu kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-120">Or it may retrieve the complete record and then use it to construct another in-memory object type or even XML data before generating the final result sequence.</span></span> <span data-ttu-id="57912-121">Aşağıdaki örnek, bir `int` `string`bir yansıtmayı gösterir.</span><span class="sxs-lookup"><span data-stu-id="57912-121">The following example shows a projection from an `int` to a `string`.</span></span> <span data-ttu-id="57912-122">`highScoresQuery`yeni türünü aklınızda yazın.</span><span class="sxs-lookup"><span data-stu-id="57912-122">Note the new type of `highScoresQuery`.</span></span>

    [!code-csharp[csrefQueryExpBasics#46](~/samples/snippets/csharp/concepts/linq/query-expression-basics_2.cs)]

- <span data-ttu-id="57912-123">Kaynak verilerle ilgili bir tekil değer alın, örneğin:</span><span class="sxs-lookup"><span data-stu-id="57912-123">Retrieve a singleton value about the source data, such as:</span></span>

  - <span data-ttu-id="57912-124">Belirli bir koşulla eşleşen öğe sayısı.</span><span class="sxs-lookup"><span data-stu-id="57912-124">The number of elements that match a certain condition.</span></span>

  - <span data-ttu-id="57912-125">En büyük veya en az değere sahip öğe.</span><span class="sxs-lookup"><span data-stu-id="57912-125">The element that has the greatest or least value.</span></span>

  - <span data-ttu-id="57912-126">Bir koşulla eşleşen ilk öğe veya belirli bir öğe kümesindeki belirli değerlerin toplamı.</span><span class="sxs-lookup"><span data-stu-id="57912-126">The first element that matches a condition, or the sum of particular values in a specified set of elements.</span></span> <span data-ttu-id="57912-127">Örneğin, aşağıdaki sorgu `scores` tamsayı dizisinden 80 ' den büyük puan sayısını döndürür:</span><span class="sxs-lookup"><span data-stu-id="57912-127">For example, the following query returns the number of scores greater than 80 from the `scores` integer array:</span></span>

    [!code-csharp[csrefQueryExpBasics#47](~/samples/snippets/csharp/concepts/linq/query-expression-basics_3.cs)]

    <span data-ttu-id="57912-128">Önceki örnekte, `Count` metoduna çağrıdan önce sorgu ifadesinin etrafında parantezler kullanımını aklınızda.</span><span class="sxs-lookup"><span data-stu-id="57912-128">In the previous example, note the use of parentheses around the query expression before the call to the `Count` method.</span></span> <span data-ttu-id="57912-129">Bunu somut sonucu depolamak için yeni bir değişken kullanarak da ifade edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-129">You can also express this by using a new variable to store the concrete result.</span></span> <span data-ttu-id="57912-130">Bu teknik daha okunabilir çünkü sorguyu depolayan değişkeni bir sonucu depolayan sorgudan ayrı tutar.</span><span class="sxs-lookup"><span data-stu-id="57912-130">This technique is more readable because it keeps the variable that stores the query separate from the query that stores a result.</span></span>

    [!code-csharp[csrefQueryExpBasics#48](~/samples/snippets/csharp/concepts/linq/query-expression-basics_4.cs)]

<span data-ttu-id="57912-131">Önceki örnekte sorgu `Count`çağrısında yürütülür, çünkü `Count` `highScoresQuery`tarafından döndürülen öğelerin sayısını belirlemede sonuçları yinelemek zorunda olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="57912-131">In the previous example, the query is executed in the call to `Count`, because `Count` must iterate over the results in order to determine the number of elements returned by `highScoresQuery`.</span></span>

## <a name="what-is-a-query-expression"></a><span data-ttu-id="57912-132">Sorgu ifadesi nedir?</span><span class="sxs-lookup"><span data-stu-id="57912-132">What is a query expression?</span></span>

<span data-ttu-id="57912-133">Sorgu *ifadesi* sorgu sözdiziminde ifade edilen bir sorgudur.</span><span class="sxs-lookup"><span data-stu-id="57912-133">A *query expression* is a query expressed in query syntax.</span></span> <span data-ttu-id="57912-134">Sorgu ifadesi birinci sınıf dil yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="57912-134">A query expression is a first-class language construct.</span></span> <span data-ttu-id="57912-135">Bu, başka bir ifade gibi olduğu gibi, bir ifadenin geçerli olduğu herhangi bir C# bağlamda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-135">It is just like any other expression and can be used in any context in which a C# expression is valid.</span></span> <span data-ttu-id="57912-136">Sorgu ifadesi SQL veya XQuery ile benzer bir bildirime dayalı sözdiziminde yazılmış bir yan tümce kümesinden oluşur.</span><span class="sxs-lookup"><span data-stu-id="57912-136">A query expression consists of a set of clauses written in a declarative syntax similar to SQL or XQuery.</span></span> <span data-ttu-id="57912-137">Sırasıyla her bir yan tümce bir veya daha C# fazla ifade içerir ve bu ifadeler bir sorgu ifadesi veya bir sorgu ifadesi olabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-137">Each clause in turn contains one or more C# expressions, and these expressions may themselves be either a query expression or contain a query expression.</span></span>

<span data-ttu-id="57912-138">Sorgu ifadesinin [from](../language-reference/keywords/from-clause.md) yan tümcesiyle başlaması ve [Select](../language-reference/keywords/select-clause.md) veya [Group](../language-reference/keywords/group-clause.md) yan tümcesiyle bitmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="57912-138">A query expression must begin with a [from](../language-reference/keywords/from-clause.md) clause and must end with a [select](../language-reference/keywords/select-clause.md) or [group](../language-reference/keywords/group-clause.md) clause.</span></span> <span data-ttu-id="57912-139">İlk `from` yan tümcesi ile son `select` veya `group` yan tümcesi arasında bu isteğe bağlı bir veya daha fazla yan tümce bulunabilir: [WHERE](../language-reference/keywords/where-clause.md), [OrderBy](../language-reference/keywords/orderby-clause.md), [JOIN](../language-reference/keywords/join-clause.md), [Let](../language-reference/keywords/let-clause.md) ve [hatta ek yan](../language-reference/keywords/from-clause.md) tümceler.</span><span class="sxs-lookup"><span data-stu-id="57912-139">Between the first `from` clause and the last `select` or `group` clause, it can contain one or more of these optional clauses: [where](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [join](../language-reference/keywords/join-clause.md), [let](../language-reference/keywords/let-clause.md) and even additional [from](../language-reference/keywords/from-clause.md) clauses.</span></span> <span data-ttu-id="57912-140">Ayrıca, aynı sorgu ifadesinde ek sorgu yan tümceleri için kaynak olarak kullanılacak bir `join` veya `group` yan tümcesinin sonucunu etkinleştirmek için [into](../language-reference/keywords/into.md) anahtar sözcüğünü kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-140">You can also use the [into](../language-reference/keywords/into.md) keyword to enable the result of a `join` or `group` clause to serve as the source for additional query clauses in the same query expression.</span></span>

### <a name="query-variable"></a><span data-ttu-id="57912-141">Sorgu değişkeni</span><span class="sxs-lookup"><span data-stu-id="57912-141">Query variable</span></span>

<span data-ttu-id="57912-142">LINQ içinde, bir sorgu değişkeni sorgu *sonuçları* yerine *sorgu* depolayan herhangi bir değişkendir.</span><span class="sxs-lookup"><span data-stu-id="57912-142">In LINQ, a query variable is any variable that stores a *query* instead of the *results* of a query.</span></span> <span data-ttu-id="57912-143">Daha belirgin bir şekilde, bir sorgu değişkeni her zaman sıralanabilir bir tür veya bir `foreach` ifadesinde tekrarlanmıştır veya `IEnumerator.MoveNext` yöntemine doğrudan çağrı yapılır.</span><span class="sxs-lookup"><span data-stu-id="57912-143">More specifically, a query variable is always an enumerable type that will produce a sequence of elements when it is iterated over in a `foreach` statement or a direct call to its `IEnumerator.MoveNext` method.</span></span>

<span data-ttu-id="57912-144">Aşağıdaki kod örneği, bir veri kaynağı, bir filtre yan tümcesi, tek bir sıralama yan tümcesi ve kaynak öğelerinin dönüştürülmesine sahip basit bir sorgu ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="57912-144">The following code example shows a simple query expression with one data source, one filtering clause, one ordering clause, and no transformation of the source elements.</span></span> <span data-ttu-id="57912-145">`select` yan tümcesi sorguyu sonlandırır.</span><span class="sxs-lookup"><span data-stu-id="57912-145">The `select` clause ends the query.</span></span>

[!code-csharp[csrefQueryExpBasics#49](~/samples/snippets/csharp/concepts/linq/query-expression-basics_5.cs)]

<span data-ttu-id="57912-146">Önceki örnekte `scoreQuery`, bazen yalnızca bir *sorgu*olarak adlandırılan bir *sorgu değişkenidir* .</span><span class="sxs-lookup"><span data-stu-id="57912-146">In the previous example, `scoreQuery` is a *query variable,* which is sometimes referred to as just a *query*.</span></span> <span data-ttu-id="57912-147">Sorgu değişkeni, `foreach` döngüsünde üretilen gerçek sonuç verilerini depolar.</span><span class="sxs-lookup"><span data-stu-id="57912-147">The query variable stores no actual result data, which is produced in the `foreach` loop.</span></span> <span data-ttu-id="57912-148">`foreach` deyimleri yürütüldüğünde sorgu sonuçları sorgu değişkeni `scoreQuery`üzerinden döndürülmez.</span><span class="sxs-lookup"><span data-stu-id="57912-148">And when the `foreach` statement executes, the query results are not returned through the query variable `scoreQuery`.</span></span> <span data-ttu-id="57912-149">Bunun yerine, `testScore`yineleme değişkeni üzerinden döndürülür.</span><span class="sxs-lookup"><span data-stu-id="57912-149">Rather, they are returned through the iteration variable `testScore`.</span></span> <span data-ttu-id="57912-150">`scoreQuery` değişkeni ikinci bir `foreach` döngüsünde yinelenebilir.</span><span class="sxs-lookup"><span data-stu-id="57912-150">The `scoreQuery` variable can be iterated in a second `foreach` loop.</span></span> <span data-ttu-id="57912-151">Ya da veri kaynağı değiştirilmedikçe aynı sonuçları üretecektir.</span><span class="sxs-lookup"><span data-stu-id="57912-151">It will produce the same results as long as neither it nor the data source has been modified.</span></span>

<span data-ttu-id="57912-152">Sorgu değişkeni sorgu söz dizimi veya yöntem sözdiziminde ifade edilen bir sorguyu veya ikisinin birleşimini saklayabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-152">A query variable may store a query that is expressed in query syntax or method syntax, or a combination of the two.</span></span> <span data-ttu-id="57912-153">Aşağıdaki örneklerde hem `queryMajorCities` hem de `queryMajorCities2` sorgu değişkenleridir:</span><span class="sxs-lookup"><span data-stu-id="57912-153">In the following examples, both `queryMajorCities` and `queryMajorCities2` are query variables:</span></span>

[!code-csharp[csrefQueryExpBasics#50](~/samples/snippets/csharp/concepts/linq/query-expression-basics_6.cs)]

<span data-ttu-id="57912-154">Diğer taraftan, aşağıdaki iki örnek, her biri bir sorguyla başlatılmış olsa da, sorgu değişkenleri olmayan değişkenleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="57912-154">On the other hand, the following two examples show variables that are not query variables even though each is initialized with a query.</span></span> <span data-ttu-id="57912-155">Sonuçları depoladıklarından sorgu değişkenleri değildir:</span><span class="sxs-lookup"><span data-stu-id="57912-155">They are not query variables because they store results:</span></span>

[!code-csharp[csrefQueryExpBasics#51](~/samples/snippets/csharp/concepts/linq/query-expression-basics_7.cs)]

<span data-ttu-id="57912-156">Sorgu sorgularının farklı yolları hakkında daha fazla bilgi için bkz. [LINQ 'Te sorgu sözdizimi ve Yöntem sözdizimi](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span><span class="sxs-lookup"><span data-stu-id="57912-156">For more information about the different ways to express queries, see [Query syntax and method syntax in LINQ](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span></span>

#### <a name="explicit-and-implicit-typing-of-query-variables"></a><span data-ttu-id="57912-157">Sorgu değişkenlerinin açık ve örtük olarak yazılması</span><span class="sxs-lookup"><span data-stu-id="57912-157">Explicit and implicit typing of query variables</span></span>

<span data-ttu-id="57912-158">Bu belge genellikle sorgu değişkeni ve [Select yan tümcesi](../language-reference/keywords/select-clause.md)arasındaki tür ilişkisini göstermek için sorgu değişkeninin açık türünü sağlar.</span><span class="sxs-lookup"><span data-stu-id="57912-158">This documentation usually provides the explicit type of the query variable in order to show the type relationship between the query variable and the [select clause](../language-reference/keywords/select-clause.md).</span></span> <span data-ttu-id="57912-159">Ancak, derleyicisine derleme zamanında bir sorgu değişkeni (veya başka bir yerel değişken) türünü çıkarmasını söylemek için [var](../language-reference/keywords/var.md) anahtar sözcüğünü de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-159">However, you can also use the [var](../language-reference/keywords/var.md) keyword to instruct the compiler to infer the type of a query variable (or any other local variable) at compile time.</span></span> <span data-ttu-id="57912-160">Örneğin, bu konuda daha önce gösterilen sorgu örneği örtük yazma kullanılarak da ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="57912-160">For example, the query example that was shown previously in this topic can also be expressed by using implicit typing:</span></span>

[!code-csharp[csrefQueryExpBasics#52](~/samples/snippets/csharp/concepts/linq/query-expression-basics_8.cs)]

<span data-ttu-id="57912-161">Daha fazla bilgi için bkz. [LINQ sorgu Işlemlerinde](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md) [örtük olarak yazılan yerel değişkenler](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) ve tür ilişkileri.</span><span class="sxs-lookup"><span data-stu-id="57912-161">For more information, see [Implicitly typed local variables](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) and [Type relationships in LINQ query operations](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span></span>

### <a name="starting-a-query-expression"></a><span data-ttu-id="57912-162">Sorgu ifadesi başlatılıyor</span><span class="sxs-lookup"><span data-stu-id="57912-162">Starting a query expression</span></span>

<span data-ttu-id="57912-163">Sorgu ifadesinin bir `from` yan tümcesiyle başlaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="57912-163">A query expression must begin with a `from` clause.</span></span> <span data-ttu-id="57912-164">Bir veri kaynağını bir Aralık değişkeniyle birlikte belirtir.</span><span class="sxs-lookup"><span data-stu-id="57912-164">It specifies a data source together with a range variable.</span></span> <span data-ttu-id="57912-165">Aralık değişkeni, kaynak sırası çapraz taşınırken kaynak dizideki birbirini izleyen her öğeyi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="57912-165">The range variable represents each successive element in the source sequence as the source sequence is being traversed.</span></span> <span data-ttu-id="57912-166">Aralık değişkeni, veri kaynağındaki öğelerin türüne göre kesin olarak yazılır.</span><span class="sxs-lookup"><span data-stu-id="57912-166">The range variable is strongly typed based on the type of elements in the data source.</span></span> <span data-ttu-id="57912-167">Aşağıdaki örnekte, `countries` bir `Country` nesneleri dizisi olduğundan, Aralık değişkeni de `Country`olarak yazılır.</span><span class="sxs-lookup"><span data-stu-id="57912-167">In the following example, because `countries` is an array of `Country` objects, the range variable is also typed as `Country`.</span></span> <span data-ttu-id="57912-168">Aralık değişkeni kesin olarak yazıldığından, türün kullanılabilir üyelerine erişmek için nokta işlecini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-168">Because the range variable is strongly typed, you can use the dot operator to access any available members of the type.</span></span>

[!code-csharp[csrefQueryExpBasics#53](~/samples/snippets/csharp/concepts/linq/query-expression-basics_9.cs)]

<span data-ttu-id="57912-169">Aralık değişkeni, sorgunun noktalı virgül veya *devamlılık* yan tümcesi ile çıkış yapılıncaya kadar kapsamdadır.</span><span class="sxs-lookup"><span data-stu-id="57912-169">The range variable is in scope until the query is exited either with a semicolon or with a *continuation* clause.</span></span>

<span data-ttu-id="57912-170">Sorgu ifadesinde birden çok `from` yan tümce bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-170">A query expression may contain multiple `from` clauses.</span></span> <span data-ttu-id="57912-171">Kaynak dizideki her öğe bir koleksiyon ya da bir koleksiyon içerdiğinde ek `from` yan tümceleri kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-171">Use additional `from` clauses when each element in the source sequence is itself a collection or contains a collection.</span></span> <span data-ttu-id="57912-172">Örneğin, her biri `Cities`adlı `City` nesnelerinin bir koleksiyonunu içeren `Country` nesneleri koleksiyonunuz olduğunu varsayalım.</span><span class="sxs-lookup"><span data-stu-id="57912-172">For example, assume that you have a collection of `Country` objects, each of which contains a collection of `City` objects named `Cities`.</span></span> <span data-ttu-id="57912-173">Her `Country``City` nesneleri sorgulamak için, burada gösterildiği gibi iki `from` yan tümcesini kullanın:</span><span class="sxs-lookup"><span data-stu-id="57912-173">To query the `City` objects in each `Country`, use two `from` clauses as shown here:</span></span>

[!code-csharp[csrefQueryExpBasics#54](~/samples/snippets/csharp/concepts/linq/query-expression-basics_10.cs)]

<span data-ttu-id="57912-174">Daha fazla bilgi için bkz. [from yan tümcesi](../language-reference/keywords/from-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-174">For more information, see [from clause](../language-reference/keywords/from-clause.md).</span></span>

### <a name="ending-a-query-expression"></a><span data-ttu-id="57912-175">Sorgu ifadesi bitiriliyor</span><span class="sxs-lookup"><span data-stu-id="57912-175">Ending a query expression</span></span>

<span data-ttu-id="57912-176">Sorgu ifadesinin bir `group` yan tümcesiyle veya bir `select` yan tümcesiyle bitmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="57912-176">A query expression must end with either a `group` clause or a `select` clause.</span></span>

#### <a name="group-clause"></a><span data-ttu-id="57912-177">group tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-177">group clause</span></span>

<span data-ttu-id="57912-178">Belirttiğiniz bir anahtarla düzenlenmiş bir grup sırası oluşturmak için `group` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-178">Use the `group` clause to produce a sequence of groups organized by a key that you specify.</span></span> <span data-ttu-id="57912-179">Anahtar herhangi bir veri türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-179">The key can be any data type.</span></span> <span data-ttu-id="57912-180">Örneğin, aşağıdaki sorgu bir veya daha fazla `Country` nesne içeren ve anahtarı `char` bir değer olan bir grup sırası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="57912-180">For example, the following query creates a sequence of groups that contains one or more `Country` objects and whose key is a `char` value.</span></span>

[!code-csharp[csrefQueryExpBasics#55](~/samples/snippets/csharp/concepts/linq/query-expression-basics_11.cs)]

<span data-ttu-id="57912-181">Gruplandırma hakkında daha fazla bilgi için bkz. [Group yan tümcesi](../language-reference/keywords/group-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-181">For more information about grouping, see [group clause](../language-reference/keywords/group-clause.md).</span></span>

#### <a name="select-clause"></a><span data-ttu-id="57912-182">select tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-182">select clause</span></span>

<span data-ttu-id="57912-183">Tüm diğer sıra türlerini oluşturmak için `select` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-183">Use the `select` clause to produce all other types of sequences.</span></span> <span data-ttu-id="57912-184">Basit bir `select` yan tümcesi, veri kaynağında bulunan nesnelerle aynı nesne türü dizisini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="57912-184">A simple `select` clause just produces a sequence of the same type of objects as the objects that are contained in the data source.</span></span> <span data-ttu-id="57912-185">Bu örnekte, veri kaynağı `Country` nesneleri içerir.</span><span class="sxs-lookup"><span data-stu-id="57912-185">In this example, the data source contains `Country` objects.</span></span> <span data-ttu-id="57912-186">`orderby` yan tümcesi yalnızca öğeleri yeni bir sıraya göre sıralar ve `select` yan tümcesi, yeniden düzenlenen `Country` nesnelerinin bir dizisini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="57912-186">The `orderby` clause just sorts the elements into a new order and the `select` clause produces a sequence of the reordered `Country` objects.</span></span>

[!code-csharp[csrefQueryExpBasics#56](~/samples/snippets/csharp/concepts/linq/query-expression-basics_12.cs)]

<span data-ttu-id="57912-187">`select` yan tümcesi, kaynak verileri yeni türdeki sıralara dönüştürmek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-187">The `select` clause can be used to transform source data into sequences of new types.</span></span> <span data-ttu-id="57912-188">Bu dönüşüme Ayrıca bir *projeksiyon*adı verilir.</span><span class="sxs-lookup"><span data-stu-id="57912-188">This transformation is also named a *projection*.</span></span> <span data-ttu-id="57912-189">Aşağıdaki örnekte, `select` yan tümcesi, özgün öğedeki alanların *yalnızca bir alt* kümesini içeren anonim türlerin bir dizisini içerir.</span><span class="sxs-lookup"><span data-stu-id="57912-189">In the following example, the `select` clause *projects* a sequence of anonymous types which contains only a subset of the fields in the original element.</span></span> <span data-ttu-id="57912-190">Yeni nesnelerin bir nesne Başlatıcısı kullanılarak başlatıldığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="57912-190">Note that the new objects are initialized by using an object initializer.</span></span>

[!code-csharp[csrefQueryExpBasics#57](~/samples/snippets/csharp/concepts/linq/query-expression-basics_13.cs)]

<span data-ttu-id="57912-191">`select` yan tümcesinin kaynak verileri dönüştürmek için kullanılabileceği tüm yollar hakkında daha fazla bilgi için bkz. [Select yan tümcesi](../language-reference/keywords/select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-191">For more information about all the ways that a `select` clause can be used to transform source data, see [select clause](../language-reference/keywords/select-clause.md).</span></span>

#### <a name="continuations-with-into"></a><span data-ttu-id="57912-192">"İnto" ile devamlılıklar</span><span class="sxs-lookup"><span data-stu-id="57912-192">Continuations with "into"</span></span>

<span data-ttu-id="57912-193">Bir sorgu depolayan geçici bir tanımlayıcı oluşturmak için bir `select` veya `group` yan tümcesindeki `into` anahtar sözcüğünü kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-193">You can use the `into` keyword in a `select` or `group` clause to create a temporary identifier that stores a query.</span></span> <span data-ttu-id="57912-194">Bir gruplandırma veya seçme işleminden sonra sorgu üzerinde ek sorgu işlemleri gerçekleştirmeniz gerektiğinde bunu yapın.</span><span class="sxs-lookup"><span data-stu-id="57912-194">Do this when you must perform additional query operations on a query after a grouping or select operation.</span></span> <span data-ttu-id="57912-195">Aşağıdaki örnekte `countries`, 10.000.000 aralıklarında popülasyona göre gruplandırılır.</span><span class="sxs-lookup"><span data-stu-id="57912-195">In the following example `countries` are grouped according to population in ranges of 10 million.</span></span> <span data-ttu-id="57912-196">Bu gruplar oluşturulduktan sonra, ek yan tümceler bazı grupları filtreleyip grupları artan düzende sıralayacak.</span><span class="sxs-lookup"><span data-stu-id="57912-196">After these groups are created, additional clauses filter out some groups, and then to sort the groups in ascending order.</span></span> <span data-ttu-id="57912-197">Bu ek işlemleri gerçekleştirmek için `countryGroup` tarafından temsil edilen devamlılık gereklidir.</span><span class="sxs-lookup"><span data-stu-id="57912-197">To perform those additional operations, the continuation represented by `countryGroup` is required.</span></span>

[!code-csharp[csrefQueryExpBasics#58](~/samples/snippets/csharp/concepts/linq/query-expression-basics_14.cs)]

<span data-ttu-id="57912-198">Daha fazla bilgi için bkz [..](../language-reference/keywords/into.md)</span><span class="sxs-lookup"><span data-stu-id="57912-198">For more information, see [into](../language-reference/keywords/into.md).</span></span>

### <a name="filtering-ordering-and-joining"></a><span data-ttu-id="57912-199">Filtreleme, sıralama ve birleştirme</span><span class="sxs-lookup"><span data-stu-id="57912-199">Filtering, ordering, and joining</span></span>

<span data-ttu-id="57912-200">Başlangıç `from` yan tümcesi ve bitiş `select` veya `group` yan tümcesi arasında, diğer tüm yan tümceler (`where`, `join`, `orderby`, `from`, `let`) isteğe bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="57912-200">Between the starting `from` clause, and the ending `select` or `group` clause, all other clauses (`where`, `join`, `orderby`, `from`, `let`) are optional.</span></span> <span data-ttu-id="57912-201">İsteğe bağlı yan tümcelerden herhangi biri, bir sorgu gövdesinde sıfır kez veya birden çok kez kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="57912-201">Any of the optional clauses may be used zero times or multiple times in a query body.</span></span>

#### <a name="where-clause"></a><span data-ttu-id="57912-202">where tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-202">where clause</span></span>

<span data-ttu-id="57912-203">Bir veya daha fazla koşul ifadesine göre kaynak verilerden öğeleri filtrelemek için `where` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-203">Use the `where` clause to filter out elements from the source data based on one or more predicate expressions.</span></span> <span data-ttu-id="57912-204">Aşağıdaki örnekteki `where` yan tümcesinin iki koşula sahip bir koşulu vardır.</span><span class="sxs-lookup"><span data-stu-id="57912-204">The `where` clause in the following example has one predicate with two conditions.</span></span>

[!code-csharp[csrefQueryExpBasics#59](~/samples/snippets/csharp/concepts/linq/query-expression-basics_15.cs)]

<span data-ttu-id="57912-205">Daha fazla bilgi için bkz. [WHERE yan tümcesi](../language-reference/keywords/where-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-205">For more information, see [where clause](../language-reference/keywords/where-clause.md).</span></span>

#### <a name="orderby-clause"></a><span data-ttu-id="57912-206">orderby tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-206">orderby clause</span></span>

<span data-ttu-id="57912-207">Sonuçları artan veya azalan sırada sıralamak için `orderby` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-207">Use the `orderby` clause to sort the results in either ascending or descending order.</span></span> <span data-ttu-id="57912-208">Ayrıca, ikincil sıralama düzenleri de belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-208">You can also specify secondary sort orders.</span></span> <span data-ttu-id="57912-209">Aşağıdaki örnek, `Area` özelliğini kullanarak `country` nesnelerinde birincil bir sıralama gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="57912-209">The following example performs a primary sort on the `country` objects by using the `Area` property.</span></span> <span data-ttu-id="57912-210">Daha sonra `Population` özelliğini kullanarak ikincil bir sıralama gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="57912-210">It then performs a secondary sort by using the `Population` property.</span></span>

[!code-csharp[csrefQueryExpBasics#60](~/samples/snippets/csharp/concepts/linq/query-expression-basics_16.cs)]

<span data-ttu-id="57912-211">`ascending` anahtar sözcüğü isteğe bağlıdır; herhangi bir sipariş belirtilmemişse varsayılan sıralama düzeni olur.</span><span class="sxs-lookup"><span data-stu-id="57912-211">The `ascending` keyword is optional; it is the default sort order if no order is specified.</span></span> <span data-ttu-id="57912-212">Daha fazla bilgi için bkz. [OrderBy tümcesi](../language-reference/keywords/orderby-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-212">For more information, see [orderby clause](../language-reference/keywords/orderby-clause.md).</span></span>

#### <a name="join-clause"></a><span data-ttu-id="57912-213">join tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-213">join clause</span></span>

<span data-ttu-id="57912-214">Her bir öğesinde belirtilen anahtarlar arasındaki eşitlik karşılaştırmasına bağlı olarak, bir veri kaynağındaki öğeleri farklı bir veri kaynağındaki öğelerle ilişkilendirmek ve/veya birleştirmek için `join` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-214">Use the `join` clause to associate and/or combine elements from one data source with elements from another data source based on an equality comparison between specified keys in each element.</span></span> <span data-ttu-id="57912-215">LINQ ' ta, öğeleri farklı türlerde olan nesne dizileri üzerinde JOIN işlemleri gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="57912-215">In LINQ, join operations are performed on sequences of objects whose elements are different types.</span></span> <span data-ttu-id="57912-216">İki dizinin birleştirildikten sonra, çıkış dizisinde hangi öğenin depolanacağını belirtmek için bir `select` veya `group` ifadesini kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="57912-216">After you have joined two sequences, you must use a `select` or `group` statement to specify which element to store in the output sequence.</span></span> <span data-ttu-id="57912-217">Ayrıca, her bir ilişkili öğe kümesinden özellikleri, çıkış sırası için yeni bir türe birleştirmek üzere anonim bir tür de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-217">You can also use an anonymous type to combine properties from each set of associated elements into a new type for the output sequence.</span></span> <span data-ttu-id="57912-218">Aşağıdaki örnek, `Category` özelliği `categories` dize dizisindeki kategorilerden biriyle eşleşen `prod` nesneleri ilişkilendirir.</span><span class="sxs-lookup"><span data-stu-id="57912-218">The following example associates `prod` objects whose `Category` property matches one of the categories in the `categories` string array.</span></span> <span data-ttu-id="57912-219">`Category` `categories` hiçbir dizeyle eşleşmeyen ürünler filtrelenmez. `select` deyimleri, özellikleri `cat` ve `prod`alınan yeni bir tür.</span><span class="sxs-lookup"><span data-stu-id="57912-219">Products whose `Category` does not match any string in `categories` are filtered out. The `select` statement projects a new type whose properties are taken from both `cat` and `prod`.</span></span>

[!code-csharp[csrefQueryExpBasics#61](~/samples/snippets/csharp/concepts/linq/query-expression-basics_17.cs)]

<span data-ttu-id="57912-220">Ayrıca, `join` işleminin sonuçlarını, [into](../language-reference/keywords/into.md) anahtar sözcüğünü kullanarak geçici bir değişkende depolayarak da bir grup birleşimi gerçekleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="57912-220">You can also perform a group join by storing the results of the `join` operation into a temporary variable by using the [into](../language-reference/keywords/into.md) keyword.</span></span> <span data-ttu-id="57912-221">Daha fazla bilgi için bkz. [JOIN yan tümcesi](../language-reference/keywords/join-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-221">For more information, see [join clause](../language-reference/keywords/join-clause.md).</span></span>

#### <a name="let-clause"></a><span data-ttu-id="57912-222">let tümcesi</span><span class="sxs-lookup"><span data-stu-id="57912-222">let clause</span></span> 

<span data-ttu-id="57912-223">Yöntem çağrısı gibi bir ifadenin sonucunu yeni bir Aralık değişkeninde depolamak için `let` yan tümcesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="57912-223">Use the `let` clause to store the result of an expression, such as a method call, in a new range variable.</span></span> <span data-ttu-id="57912-224">Aşağıdaki örnekte, Aralık değişkeni `firstName` `Split`tarafından döndürülen dizelerin dizisinin ilk öğesini depolar.</span><span class="sxs-lookup"><span data-stu-id="57912-224">In the following example, the range variable `firstName` stores the first element of the array of strings that is returned by `Split`.</span></span>

[!code-csharp[csrefQueryExpBasics#62](~/samples/snippets/csharp/concepts/linq/query-expression-basics_18.cs)]

<span data-ttu-id="57912-225">Daha fazla bilgi için bkz. [Let yan tümcesi](../language-reference/keywords/let-clause.md).</span><span class="sxs-lookup"><span data-stu-id="57912-225">For more information, see [let clause](../language-reference/keywords/let-clause.md).</span></span>

### <a name="subqueries-in-a-query-expression"></a><span data-ttu-id="57912-226">Sorgu ifadesinde alt sorgular</span><span class="sxs-lookup"><span data-stu-id="57912-226">Subqueries in a query expression</span></span>

<span data-ttu-id="57912-227">Sorgu yan tümcesi, bazen *alt sorgu*olarak adlandırılan bir sorgu ifadesi içerebilir.</span><span class="sxs-lookup"><span data-stu-id="57912-227">A query clause may itself contain a query expression, which is sometimes referred to as a *subquery*.</span></span> <span data-ttu-id="57912-228">Her alt sorgu, ilk `from` yan tümcesindeki aynı veri kaynağını işaret etmek zorunda olmayan kendi `from` yan tümcesiyle başlar.</span><span class="sxs-lookup"><span data-stu-id="57912-228">Each subquery starts with its own `from` clause that does not necessarily point to the same data source in the first `from` clause.</span></span> <span data-ttu-id="57912-229">Örneğin, aşağıdaki sorgu, Gruplandırma işleminin sonuçlarını almak için SELECT deyiminde kullanılan bir sorgu ifadesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="57912-229">For example, the following query shows a query expression that is used in the select statement to retrieve the results of a grouping operation.</span></span>

[!code-csharp[csrefQueryExpBasics#63](~/samples/snippets/csharp/concepts/linq/query-expression-basics_19.cs)]

<span data-ttu-id="57912-230">Daha fazla bilgi için bkz. [Gruplandırma işleminde alt sorgu gerçekleştirme](perform-a-subquery-on-a-grouping-operation.md).</span><span class="sxs-lookup"><span data-stu-id="57912-230">For more information, see [Perform a subquery on a grouping operation](perform-a-subquery-on-a-grouping-operation.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="57912-231">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="57912-231">See also</span></span>

- [<span data-ttu-id="57912-232">C#Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="57912-232">C# programming guide</span></span>](../programming-guide/index.md)
- [<span data-ttu-id="57912-233">Dil ile Tümleşik Sorgu (LINQ)</span><span class="sxs-lookup"><span data-stu-id="57912-233">Language Integrated Query (LINQ)</span></span>](index.md)
- [<span data-ttu-id="57912-234">Sorgu anahtar sözcükleri (LINQ)</span><span class="sxs-lookup"><span data-stu-id="57912-234">Query keywords (LINQ)</span></span>](../language-reference/keywords/query-keywords.md)
- [<span data-ttu-id="57912-235">Standart sorgu işleçlerine genel bakış</span><span class="sxs-lookup"><span data-stu-id="57912-235">Standard query operators overview</span></span>](../programming-guide/concepts/linq/standard-query-operators-overview.md)
