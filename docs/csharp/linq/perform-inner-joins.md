---
title: İç birleştirmeleri gerçekleştirin (C#'da LINQ)
description: C#'da LINQ'u kullanarak iç birleştirmeleri nasıl gerçekleştireceklerini öğrenin.
ms.date: 12/01/2016
ms.assetid: 45bceed6-f549-4114-a9b1-b44feb497742
ms.openlocfilehash: a3e8e9bd97ec630797bc48a3302b27ed45d9103e
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "61659845"
---
# <a name="perform-inner-joins"></a><span data-ttu-id="25587-103">İç birleşimler gerçekleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-103">Perform inner joins</span></span>

<span data-ttu-id="25587-104">İlişkisel veritabanı terimlerinde, *iç birleştirme,* ilk koleksiyonun her öğesinin ikinci koleksiyondaki her eşleşen öğe için bir kez göründüğü bir sonuç kümesi üretir.</span><span class="sxs-lookup"><span data-stu-id="25587-104">In relational database terms, an *inner join* produces a result set in which each element of the first collection appears one time for every matching element in the second collection.</span></span> <span data-ttu-id="25587-105">İlk koleksiyondaki bir öğeeşleşen öğe yoksa, sonuç kümesinde görünmez.</span><span class="sxs-lookup"><span data-stu-id="25587-105">If an element in the first collection has no matching elements, it does not appear in the result set.</span></span> <span data-ttu-id="25587-106">C#'daki <xref:System.Linq.Enumerable.Join%2A> `join` yan tümceyle çağrılan yöntem, bir iç birleştirme uygular.</span><span class="sxs-lookup"><span data-stu-id="25587-106">The <xref:System.Linq.Enumerable.Join%2A> method, which is called by the `join` clause in C#, implements an inner join.</span></span>

<span data-ttu-id="25587-107">Bu makalede, bir iç birleştirme dört varyasyonları gerçekleştirmek için nasıl gösterir:</span><span class="sxs-lookup"><span data-stu-id="25587-107">This article shows you how to perform four variations of an inner join:</span></span>

- <span data-ttu-id="25587-108">Basit bir anahtara dayalı olarak iki veri kaynağındaki öğeleri ilişkilendiren basit bir iç birleştirme.</span><span class="sxs-lookup"><span data-stu-id="25587-108">A simple inner join that correlates elements from two data sources based on a simple key.</span></span>

- <span data-ttu-id="25587-109">*Bileşik* anahtara dayalı iki veri kaynağındaki öğeleri ilişkilendiren bir iç birleştirme.</span><span class="sxs-lookup"><span data-stu-id="25587-109">An inner join that correlates elements from two data sources based on a *composite* key.</span></span> <span data-ttu-id="25587-110">Birden fazla değerden oluşan bir anahtar olan bileşik anahtar, birden fazla özelliği temel alan öğeleri ilişkilendirmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="25587-110">A composite key, which is a key that consists of more than one value, enables you to correlate elements based on more than one property.</span></span>

- <span data-ttu-id="25587-111">Birbirini izleyen birleştirme işlemlerinin birbirine eklendiği *birden çok birleştirme.*</span><span class="sxs-lookup"><span data-stu-id="25587-111">A *multiple join* in which successive join operations are appended to each other.</span></span>

- <span data-ttu-id="25587-112">Grup birleşimi kullanılarak uygulanan bir iç birleştirme.</span><span class="sxs-lookup"><span data-stu-id="25587-112">An inner join that is implemented by using a group join.</span></span>

## <a name="example---simple-key-join"></a><span data-ttu-id="25587-113">Örnek - Basit tuş birleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-113">Example - Simple key join</span></span>

<span data-ttu-id="25587-114">Aşağıdaki örnek, kullanıcı tanımlı iki türnesneleri içeren iki `Person` koleksiyon `Pet`oluşturur ve.</span><span class="sxs-lookup"><span data-stu-id="25587-114">The following example creates two collections that contain objects of two user-defined types, `Person` and `Pet`.</span></span> <span data-ttu-id="25587-115">Sorgu c# `join` tümcesini nesneleri `Person` nesneleri `Pet` olan `Owner` nesnelerle `Person`eşleştirmek için kullanır.</span><span class="sxs-lookup"><span data-stu-id="25587-115">The query uses the `join` clause in C# to match `Person` objects with `Pet` objects whose `Owner` is that `Person`.</span></span> <span data-ttu-id="25587-116">C# yan `select` tümcesi, elde edilen nesnelerin nasıl görüneceğini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="25587-116">The `select` clause in C# defines how the resulting objects will look.</span></span> <span data-ttu-id="25587-117">Bu örnekte ortaya çıkan nesneler, sahibinin adı ve evcil hayvanın adını oluşturan anonim türleridir.</span><span class="sxs-lookup"><span data-stu-id="25587-117">In this example the resulting objects are anonymous types that consist of the owner's first name and the pet's name.</span></span>

[!code-csharp[CsLINQProgJoining#1](~/samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_1.cs)]

<span data-ttu-id="25587-118">"Huff" `Person` olan `LastName` nesnenin sonuç kümesinde görünmediğini, çünkü `Pet` buna `Pet.Owner` `Person`eşit olan bir nesne olmadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="25587-118">Note that the `Person` object whose `LastName` is "Huff" does not appear in the result set because there is no `Pet` object that has `Pet.Owner` equal to that `Person`.</span></span>

## <a name="example---composite-key-join"></a><span data-ttu-id="25587-119">Örnek - Kompozit tuşu birleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-119">Example - Composite key join</span></span>

<span data-ttu-id="25587-120">Öğeleri tek bir özelliğe dayalı ilişkilendirmek yerine, birden çok özelliğe dayalı öğeleri karşılaştırmak için bileşik bir anahtar kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="25587-120">Instead of correlating elements based on just one property, you can use a composite key to compare elements based on multiple properties.</span></span> <span data-ttu-id="25587-121">Bunu yapmak için, karşılaştırmak istediğiniz özelliklerden oluşan anonim bir türü döndürmek için her koleksiyon için anahtar seçici işlevini belirtin.</span><span class="sxs-lookup"><span data-stu-id="25587-121">To do this, specify the key selector function for each collection to return an anonymous type that consists of the properties you want to compare.</span></span> <span data-ttu-id="25587-122">Özellikleri etiketlerseniz, her anahtarın anonim türünde aynı etikete sahip olmaları gerekir.</span><span class="sxs-lookup"><span data-stu-id="25587-122">If you label the properties, they must have the same label in each key's anonymous type.</span></span> <span data-ttu-id="25587-123">Özellikler de aynı sırada görünmelidir.</span><span class="sxs-lookup"><span data-stu-id="25587-123">The properties must also appear in the same order.</span></span>

<span data-ttu-id="25587-124">Aşağıdaki örnekte, hangi `Employee` çalışanların da öğrenci `Student` olduğunu belirlemek için nesnelerin listesi ve nesnelerin listesi kullanır.</span><span class="sxs-lookup"><span data-stu-id="25587-124">The following example uses a list of `Employee` objects and a list of `Student` objects to determine which employees are also students.</span></span> <span data-ttu-id="25587-125">Bu tür her `FirstName` ikisi `LastName` de bir <xref:System.String>ve türü bir özelliği vardır.</span><span class="sxs-lookup"><span data-stu-id="25587-125">Both of these types have a `FirstName` and a `LastName` property of type <xref:System.String>.</span></span> <span data-ttu-id="25587-126">Her listenin öğelerinden birleştirme anahtarlarını oluşturan işlevler, her `FirstName` öğenin ve `LastName` özelliklerinden oluşan anonim bir tür döndürür.</span><span class="sxs-lookup"><span data-stu-id="25587-126">The functions that create the join keys from each list's elements return an anonymous type that consists of the `FirstName` and `LastName` properties of each element.</span></span> <span data-ttu-id="25587-127">Birleştirme işlemi, eşitlik için bu bileşik anahtarları karşılaştırır ve hem ad hem de soyadının eşleştiği her listeden nesne çiftleri döndürür.</span><span class="sxs-lookup"><span data-stu-id="25587-127">The join operation compares these composite keys for equality and returns pairs of objects from each list where both the first name and the last name match.</span></span>

[!code-csharp[CsLINQProgJoining#2](~/samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_2.cs)]

## <a name="example---multiple-join"></a><span data-ttu-id="25587-128">Örnek - Çoklu birleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-128">Example - Multiple join</span></span>

<span data-ttu-id="25587-129">Birden çok birleştirme işlemi gerçekleştirmek için birbirinin yanına eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="25587-129">Any number of join operations can be appended to each other to perform a multiple join.</span></span> <span data-ttu-id="25587-130">C#'daki her `join` yan tümce, belirli bir veri kaynağını önceki birleştirme nin sonuçlarıyla ilişkilendirer.</span><span class="sxs-lookup"><span data-stu-id="25587-130">Each `join` clause in C# correlates a specified data source with the results of the previous join.</span></span>

<span data-ttu-id="25587-131">Aşağıdaki örnekte üç koleksiyon oluşturulur: `Person` nesnelerin listesi, `Cat` nesnelerin listesi ve `Dog` nesnelerin listesi.</span><span class="sxs-lookup"><span data-stu-id="25587-131">The following example creates three collections: a list of `Person` objects, a list of `Cat` objects, and a list of `Dog` objects.</span></span>

<span data-ttu-id="25587-132">C#'daki ilk `join` tümce, eşleşen `Person` bir `Cat.Owner`nesneye göre kişilerle kedilerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="25587-132">The first `join` clause in C# matches people and cats based on a `Person` object matching `Cat.Owner`.</span></span> <span data-ttu-id="25587-133">Nesneyi ve `Person` `Cat.Name`.</span><span class="sxs-lookup"><span data-stu-id="25587-133">It returns a sequence of anonymous types that contain the `Person` object and `Cat.Name`.</span></span>

<span data-ttu-id="25587-134">`join` C#'daki ikinci tümce, ilk olarak verilen köpekler `Dog` listesindeki nesnelerle birlikte döndürülen anonim türleri, türün `Owner` `Person`özelliğinden ve hayvanadının ilk harfinden oluşan bileşik anahtara dayalı olarak ilişkilendirer.</span><span class="sxs-lookup"><span data-stu-id="25587-134">The second `join` clause in C# correlates the anonymous types returned by the first join with `Dog` objects in the supplied list of dogs, based on a composite key that consists of the `Owner` property of type `Person`, and the first letter of the animal's name.</span></span> <span data-ttu-id="25587-135">Her eşleşen çiftin `Cat.Name` özelliklerini ve özelliklerini `Dog.Name` içeren bir dizi anonim türü döndürür.</span><span class="sxs-lookup"><span data-stu-id="25587-135">It returns a sequence of anonymous types that contain the `Cat.Name` and `Dog.Name` properties from each matching pair.</span></span> <span data-ttu-id="25587-136">Bu bir iç birleştirme olduğundan, yalnızca ikinci veri kaynağında eşleşme sayılsa ilk veri kaynağındaki nesneler döndürülür.</span><span class="sxs-lookup"><span data-stu-id="25587-136">Because this is an inner join, only those objects from the first data source that have a match in the second data source are returned.</span></span>

[!code-csharp[CsLINQProgJoining#3](~/samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_3.cs)]

## <a name="example---inner-join-by-using-grouped-join"></a><span data-ttu-id="25587-137">Örnek - Gruplanmış birleştirme kullanarak iç birleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-137">Example - Inner join by using grouped join</span></span>

<span data-ttu-id="25587-138">Aşağıdaki örnek, bir grup birleştirme kullanarak bir iç birleştirme nasıl uygulanacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="25587-138">The following example shows you how to implement an inner join by using a group join.</span></span>

<span data-ttu-id="25587-139">In, `query1` `Person` nesnelerin listesi `Pet` `Person` grup eşleşen `Pet.Owner` özelliğine dayalı nesneler listesine katıldı.</span><span class="sxs-lookup"><span data-stu-id="25587-139">In `query1`, the list of `Person` objects is group-joined to the list of `Pet` objects based on the `Person` matching the `Pet.Owner` property.</span></span> <span data-ttu-id="25587-140">Grup birleştirme, her grubun bir `Person` nesne ve eşleşen `Pet` nesnelerden oluşan bir diziden oluştuğu ara gruplardan oluşan bir koleksiyon oluşturur.</span><span class="sxs-lookup"><span data-stu-id="25587-140">The group join creates a collection of intermediate groups, where each group consists of a `Person` object and a sequence of matching `Pet` objects.</span></span>

<span data-ttu-id="25587-141">Sorguya ikinci `from` bir yan tümce eklenerek, bu dizi dizisi daha uzun bir dizide birleştirilir (veya düzleştirilmiştir).</span><span class="sxs-lookup"><span data-stu-id="25587-141">By adding a second `from` clause to the query, this sequence of sequences is combined (or flattened) into one longer sequence.</span></span> <span data-ttu-id="25587-142">Son sıranın öğelerinin türü yan tümcetarafından `select` belirtilir.</span><span class="sxs-lookup"><span data-stu-id="25587-142">The type of the elements of the final sequence is specified by the `select` clause.</span></span> <span data-ttu-id="25587-143">Bu örnekte, bu tür, her eşleşen `Person.FirstName` `Pet.Name` çift için ve özellikleri nden oluşan anonim bir türüdür.</span><span class="sxs-lookup"><span data-stu-id="25587-143">In this example, that type is an anonymous type that consists of the `Person.FirstName` and `Pet.Name` properties for each matching pair.</span></span>

<span data-ttu-id="25587-144">Bunun `query1` sonucu, bir iç birleştirme gerçekleştirmek için `join` `into` yan tümcesi kullanılarak elde edilen sonuç kümesine eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="25587-144">The result of `query1` is equivalent to the result set that would have been obtained by using the `join` clause without the `into` clause to perform an inner join.</span></span> <span data-ttu-id="25587-145">`query2` Değişken bu eşdeğer sorguyu gösterir.</span><span class="sxs-lookup"><span data-stu-id="25587-145">The `query2` variable demonstrates this equivalent query.</span></span>

[!code-csharp[CsLINQProgJoining#4](~/samples/snippets/csharp/concepts/linq/how-to-perform-inner-joins_4.cs)]

## <a name="see-also"></a><span data-ttu-id="25587-146">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="25587-146">See also</span></span>

- <xref:System.Linq.Enumerable.Join%2A>
- <xref:System.Linq.Enumerable.GroupJoin%2A>
- [<span data-ttu-id="25587-147">Gruplanmış birleşimler gerçekleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-147">Perform grouped joins</span></span>](perform-grouped-joins.md)
- [<span data-ttu-id="25587-148">Sol dış birleşimler gerçekleştirme</span><span class="sxs-lookup"><span data-stu-id="25587-148">Perform left outer joins</span></span>](perform-left-outer-joins.md)
- [<span data-ttu-id="25587-149">Anonim türleri</span><span class="sxs-lookup"><span data-stu-id="25587-149">Anonymous types</span></span>](../programming-guide/classes-and-structs/anonymous-types.md)
