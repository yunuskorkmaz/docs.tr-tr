---
title: "C'de Kalıtım #"
description: C# kitaplıklarında ve uygulamalarında devralma yı kullanmayı öğrenin.
ms.date: 07/05/2018
ms.technology: csharp-fundamentals
ms.assetid: aeb68c74-0ea0-406f-9fbe-2ce02d47ef31
ms.openlocfilehash: 78833110db0e4f0382e5c0c6de7c6c8be9a16c8d
ms.sourcegitcommit: a9b8945630426a575ab0a332e568edc807666d1b
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/30/2020
ms.locfileid: "80391155"
---
# <a name="inheritance-in-c-and-net"></a><span data-ttu-id="c7e49-103">C# ve .NET içinde devralma</span><span class="sxs-lookup"><span data-stu-id="c7e49-103">Inheritance in C# and .NET</span></span>

<span data-ttu-id="c7e49-104">Bu öğretici c# kalıtım size tanıttı.</span><span class="sxs-lookup"><span data-stu-id="c7e49-104">This tutorial introduces you to inheritance in C#.</span></span> <span data-ttu-id="c7e49-105">Devralma, belirli işlevsellik (veri ve davranış) sağlayan bir taban sınıf tanımlamanızı ve bu işlevselliği devralan veya geçersiz kılan türemiş sınıfları tanımlamanızı sağlayan nesne yönelimli programlama dillerinin bir özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-105">Inheritance is a feature of object-oriented programming languages that allows you to define a base class that provides specific functionality (data and behavior) and to define derived classes that either inherit or override that functionality.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="c7e49-106">Ön koşullar</span><span class="sxs-lookup"><span data-stu-id="c7e49-106">Prerequisites</span></span>

<span data-ttu-id="c7e49-107">Bu öğretici, .NET Core SDK'yı yüklediğinizi varsayar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-107">This tutorial assumes that you've installed the .NET Core SDK.</span></span> <span data-ttu-id="c7e49-108">İndirmek için [.NET Çekirdek İndirmeler](https://dotnet.microsoft.com/download) sayfasını ziyaret edin.</span><span class="sxs-lookup"><span data-stu-id="c7e49-108">Visit the [.NET Core Downloads](https://dotnet.microsoft.com/download) page to download it.</span></span> <span data-ttu-id="c7e49-109">Ayrıca bir kod düzenleyicisi gerekir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-109">You also need a code editor.</span></span> <span data-ttu-id="c7e49-110">Bu öğretici [Visual Studio Code](https://code.visualstudio.com)kullanır, ancak seçtiğiniz herhangi bir kod editörü kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-110">This tutorial uses [Visual Studio Code](https://code.visualstudio.com), although you can use any code editor of your choice.</span></span>

## <a name="running-the-examples"></a><span data-ttu-id="c7e49-111">Örnekleri çalıştırma</span><span class="sxs-lookup"><span data-stu-id="c7e49-111">Running the examples</span></span>

<span data-ttu-id="c7e49-112">Bu öğreticideki örnekleri oluşturmak ve çalıştırmak için komut satırındaki [dotnet](../../core/tools/dotnet.md) yardımcı programını kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-112">To create and run the examples in this tutorial, you use the [dotnet](../../core/tools/dotnet.md) utility from the command line.</span></span> <span data-ttu-id="c7e49-113">Her örnek için aşağıdaki adımları izleyin:</span><span class="sxs-lookup"><span data-stu-id="c7e49-113">Follow these steps for each example:</span></span>

1. <span data-ttu-id="c7e49-114">Örneği depolamak için bir dizin oluşturun.</span><span class="sxs-lookup"><span data-stu-id="c7e49-114">Create a directory to store the example.</span></span>
1. <span data-ttu-id="c7e49-115">Yeni bir .NET Core projesi oluşturmak için komut isteminde [dotnet yeni konsol](../../core/tools/dotnet-new.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="c7e49-115">Enter the [dotnet new console](../../core/tools/dotnet-new.md) command at a command prompt to create a new .NET Core project.</span></span>
1. <span data-ttu-id="c7e49-116">Örnekteki kodu kopyalayıp kod düzenleyicinize yapıştırın.</span><span class="sxs-lookup"><span data-stu-id="c7e49-116">Copy and paste the code from the example into your code editor.</span></span>
1. <span data-ttu-id="c7e49-117">Projenin bağımlılıklarını yüklemek veya geri yüklemek için komut satırından [dotnet geri yükleme](../../core/tools/dotnet-restore.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="c7e49-117">Enter the [dotnet restore](../../core/tools/dotnet-restore.md) command from the command line to load or restore the project's dependencies.</span></span>

   [!INCLUDE[DotNet Restore Note](~/includes/dotnet-restore-note.md)]

1. <span data-ttu-id="c7e49-118">Örneği derlemek ve yürütmek için [dotnet çalıştır](../../core/tools/dotnet-run.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="c7e49-118">Enter the [dotnet run](../../core/tools/dotnet-run.md) command to compile and execute the example.</span></span>

## <a name="background-what-is-inheritance"></a><span data-ttu-id="c7e49-119">Arka Plan: Kalıtım nedir?</span><span class="sxs-lookup"><span data-stu-id="c7e49-119">Background: What is inheritance?</span></span>

<span data-ttu-id="c7e49-120">*Kalıtım* nesne yönelimli programlamanın temel özelliklerinden biridir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-120">*Inheritance* is one of the fundamental attributes of object-oriented programming.</span></span> <span data-ttu-id="c7e49-121">Bir üst sınıfın davranışını yeniden kullanan (devralan), genişleten veya değiştiren bir alt sınıf tanımlamanıza olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-121">It allows you to define a child class that reuses (inherits), extends, or modifies the behavior of a parent class.</span></span> <span data-ttu-id="c7e49-122">Üyeleri devralınan *sınıfa taban sınıf*denir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-122">The class whose members are inherited is called the *base class*.</span></span> <span data-ttu-id="c7e49-123">Taban sınıfın üyelerini devralan *sınıfa türemiş sınıf*denir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-123">The class that inherits the members of the base class is called the *derived class*.</span></span>

<span data-ttu-id="c7e49-124">C# ve .NET yalnızca *tek kalıtım desteği.*</span><span class="sxs-lookup"><span data-stu-id="c7e49-124">C# and .NET support *single inheritance* only.</span></span> <span data-ttu-id="c7e49-125">Diğer bir diğer taraftan, bir sınıf yalnızca tek bir sınıftan devralınabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-125">That is, a class can only inherit from a single class.</span></span> <span data-ttu-id="c7e49-126">Ancak, devralma geçişli, hangi türleri kümesi için bir devralma hiyerarşisi tanımlamak için izin verir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-126">However, inheritance is transitive, which allows you to define an inheritance hierarchy for a set of types.</span></span> <span data-ttu-id="c7e49-127">Başka bir deyişle, `D` tür, `C`taban sınıf türünden `B` `A`devralan türden , türden devralabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-127">In other words, type `D` can inherit from type `C`, which inherits from type `B`, which inherits from the base class type `A`.</span></span> <span data-ttu-id="c7e49-128">Kalıtım geçişli olduğundan, tür `A` üyeleri türüne kullanılabilir. `D`</span><span class="sxs-lookup"><span data-stu-id="c7e49-128">Because inheritance is transitive, the members of type `A` are available to type `D`.</span></span>

<span data-ttu-id="c7e49-129">Bir taban sınıfın tüm üyeleri türetilmiş sınıflar tarafından devralınır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-129">Not all members of a base class are inherited by derived classes.</span></span> <span data-ttu-id="c7e49-130">Aşağıdaki üyeler ekideğildir:</span><span class="sxs-lookup"><span data-stu-id="c7e49-130">The following members are not inherited:</span></span>

- <span data-ttu-id="c7e49-131">Bir sınıfın statik verilerini ilk olarak alan [statik yapıcılar.](../programming-guide/classes-and-structs/static-constructors.md)</span><span class="sxs-lookup"><span data-stu-id="c7e49-131">[Static constructors](../programming-guide/classes-and-structs/static-constructors.md), which initialize the static data of a class.</span></span>

- <span data-ttu-id="c7e49-132">Sınıfın yeni bir örneğini oluşturmak için çağırdığınız [örnek oluşturucular.](../programming-guide/classes-and-structs/constructors.md)</span><span class="sxs-lookup"><span data-stu-id="c7e49-132">[Instance constructors](../programming-guide/classes-and-structs/constructors.md), which you call to create a new instance of the class.</span></span> <span data-ttu-id="c7e49-133">Her sınıf kendi oluşturucularını tanımlamalıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-133">Each class must define its own constructors.</span></span>

- <span data-ttu-id="c7e49-134">[Finalizers](../programming-guide/classes-and-structs/destructors.md), bir sınıfın örneklerini yok etmek için runtime çöp toplayıcı tarafından denir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-134">[Finalizers](../programming-guide/classes-and-structs/destructors.md), which are called by the runtime's garbage collector to destroy instances of a class.</span></span>

<span data-ttu-id="c7e49-135">Bir taban sınıfın diğer tüm üyeleri türemiş sınıflar tarafından devralınırken, görünür olup olmadıkları erişilebilirliklerine bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-135">While all other members of a base class are inherited by derived classes, whether they are visible or not depends on their accessibility.</span></span> <span data-ttu-id="c7e49-136">Bir üyenin erişilebilirliği, türetilmiş sınıflar için görünürlüğünü aşağıdaki gibi etkiler:</span><span class="sxs-lookup"><span data-stu-id="c7e49-136">A member's accessibility affects its visibility for derived classes as follows:</span></span>

- <span data-ttu-id="c7e49-137">[Özel](../language-reference/keywords/private.md) üyeler yalnızca taban sınıflarında iç içe olan türemiş sınıflarda görünür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-137">[Private](../language-reference/keywords/private.md) members are visible only in derived classes that are nested in their base class.</span></span> <span data-ttu-id="c7e49-138">Aksi takdirde, türemiş sınıflarda görünmezler.</span><span class="sxs-lookup"><span data-stu-id="c7e49-138">Otherwise, they are not visible in derived classes.</span></span> <span data-ttu-id="c7e49-139">Aşağıdaki örnekte, `A.B` 'den türeyen ve `A` `C` .'den `A`türeyen iç içe bir sınıftır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-139">In the following example, `A.B` is a nested class that derives from `A`, and `C` derives from `A`.</span></span> <span data-ttu-id="c7e49-140">Özel `A.value` alan A.B.'de görülebilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-140">The private `A.value` field is visible in A.B.</span></span> <span data-ttu-id="c7e49-141">Ancak, yorumları `C.GetValue` yöntemden kaldırır ve örneği derlemeye çalışırsanız, cs0122 derleyici hatası üretir: "'A.value' koruma düzeyi nedeniyle erişilemez."</span><span class="sxs-lookup"><span data-stu-id="c7e49-141">However, if you remove the comments from the `C.GetValue` method and attempt to compile the example, it produces compiler error CS0122: "'A.value' is inaccessible due to its protection level."</span></span>

  [!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/private.cs#1)]

- <span data-ttu-id="c7e49-142">[Korumalı](../language-reference/keywords/protected.md) üyeler yalnızca türemiş sınıflarda görülebilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-142">[Protected](../language-reference/keywords/protected.md) members are visible only in derived classes.</span></span>

- <span data-ttu-id="c7e49-143">[İç](../language-reference/keywords/internal.md) üyeler yalnızca taban sınıfla aynı derlemede bulunan türemiş sınıflarda görülebilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-143">[Internal](../language-reference/keywords/internal.md) members are visible only in derived classes that are located in the same assembly as the base class.</span></span> <span data-ttu-id="c7e49-144">Taban sınıftan farklı bir derlemede bulunan türemiş sınıflarda görünmezler.</span><span class="sxs-lookup"><span data-stu-id="c7e49-144">They are not visible in derived classes located in a different assembly from the base class.</span></span>

- <span data-ttu-id="c7e49-145">[Türemiş](../language-reference/keywords/public.md) sınıflarda genel üyeler görünür ve türetilmiş sınıfın ortak arabiriminin bir parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-145">[Public](../language-reference/keywords/public.md) members are visible in derived classes and are part of the derived class' public interface.</span></span> <span data-ttu-id="c7e49-146">Genel olarak devralınan üyeler, türemiş sınıfta tanımlanmış gibi çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-146">Public inherited members can be called just as if they are defined in the derived class.</span></span> <span data-ttu-id="c7e49-147">Aşağıdaki örnekte, `A` sınıf adlı `Method1`bir yöntem `B` tanımlar ve `A`sınıf sınıftan devralır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-147">In the following example, class `A` defines a method named `Method1`, and class `B` inherits from class `A`.</span></span> <span data-ttu-id="c7e49-148">Örnek daha `Method1` sonra bir örnek yöntemi `B`sanki çağırır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-148">The example then calls `Method1` as if it were an instance method on `B`.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/basics.cs#1)]

<span data-ttu-id="c7e49-149">Türetilen sınıflar, alternatif bir uygulama sağlayarak devralınan üyeleri de *geçersiz kılabilir.*</span><span class="sxs-lookup"><span data-stu-id="c7e49-149">Derived classes can also *override* inherited members by providing an alternate implementation.</span></span> <span data-ttu-id="c7e49-150">Bir üyeyi geçersiz kılabilmesi için, taban sınıftaki üyenin [sanal](../language-reference/keywords/virtual.md) anahtar kelimeyle işaretlemesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-150">In order to be able to override a member, the member in the base class must be marked with the [virtual](../language-reference/keywords/virtual.md) keyword.</span></span> <span data-ttu-id="c7e49-151">Varsayılan olarak, taban sınıf üyeleri `virtual` olarak işaretlenmemiştir ve geçersiz kılınmaz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-151">By default, base class members are not marked as `virtual` and cannot be overridden.</span></span> <span data-ttu-id="c7e49-152">Aşağıdaki örnekte olduğu gibi sanal olmayan bir üyeyi geçersiz kılmaya çalışmak, cs0506 derleyici hatası oluşturur: "\<üye>, sanal, soyut veya geçersiz kılınma olarak işaretlenmediği için devralınan üye \<> geçersiz kılamaz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-152">Attempting to override a non-virtual member, as the following example does, generates compiler error CS0506: "\<member> cannot override inherited member \<member> because it is not marked virtual, abstract, or override.</span></span>

```csharp
public class A
{
    public void Method1()
    {
        // Do something.
    }
}

public class B : A
{
    public override void Method1() // Generates CS0506.
    {
        // Do something else.
    }
}
```

<span data-ttu-id="c7e49-153">Bazı durumlarda, türetilmiş bir sınıfın taban sınıf uygulamasını geçersiz kılması *gerekir.*</span><span class="sxs-lookup"><span data-stu-id="c7e49-153">In some cases, a derived class *must* override the base class implementation.</span></span> <span data-ttu-id="c7e49-154">[Soyut](../language-reference/keywords/abstract.md) anahtar sözcüğüyle işaretlenmiş taban sınıf üyeleri, türemiş sınıfların bunları geçersiz kılmasını gerektirir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-154">Base class members marked with the [abstract](../language-reference/keywords/abstract.md) keyword require that derived classes override them.</span></span> <span data-ttu-id="c7e49-155">Aşağıdaki örneği derlemeye çalışırken derleyici hatası CS0534&lt;oluşturur, " sınıf&gt; kalıtsal &lt;&gt;soyut üye `B` üye "uygulamaz, çünkü sınıf için `A.Method1`hiçbir uygulama sağlar .</span><span class="sxs-lookup"><span data-stu-id="c7e49-155">Attempting to compile the following example generates compiler error CS0534, "&lt;class&gt; does not implement inherited abstract member &lt;member&gt;", because class `B` provides no implementation for `A.Method1`.</span></span>

```csharp
public abstract class A
{
    public abstract void Method1();
}

public class B : A // Generates CS0534.
{
    public void Method3()
    {
        // Do something.
    }
}
```

<span data-ttu-id="c7e49-156">Kalıtım yalnızca sınıflar ve arabirimler için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-156">Inheritance applies only to classes and interfaces.</span></span> <span data-ttu-id="c7e49-157">Diğer tür kategorileri (structs, delegates ve enums) devralma desteklemez.</span><span class="sxs-lookup"><span data-stu-id="c7e49-157">Other type categories (structs, delegates, and enums) do not support inheritance.</span></span> <span data-ttu-id="c7e49-158">Bu kurallar nedeniyle, aşağıdaki örnek gibi kod derlemek için çalışırken derleyici hatası CS0527 üretir: "Tür 'ValueType' arayüz listesinde bir arayüz değildir."</span><span class="sxs-lookup"><span data-stu-id="c7e49-158">Because of these rules, attempting to compile code like the following example produces compiler error CS0527: "Type 'ValueType' in interface list is not an interface."</span></span> <span data-ttu-id="c7e49-159">Hata iletisi, bir yapının uyguladığı arabirimleri tanımlayabildiğiniz halde kalıtımın desteklenmediğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-159">The error message indicates that, although you can define the interfaces that a struct implements, inheritance is not supported.</span></span>

```csharp
using System;

public struct ValueStructure : ValueType // Generates CS0527.
{
}
```

## <a name="implicit-inheritance"></a><span data-ttu-id="c7e49-160">Örtülü miras</span><span class="sxs-lookup"><span data-stu-id="c7e49-160">Implicit inheritance</span></span>

<span data-ttu-id="c7e49-161">Tek bir devralma yoluyla devralabilecekleri tüm türlerin yanı sıra, .NET <xref:System.Object> türü sistemindeki tüm türler örtülü olarak devralır veya ondan türetilen bir tür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-161">Besides any types that they may inherit from through single inheritance, all types in the .NET type system implicitly inherit from <xref:System.Object> or a type derived from it.</span></span> <span data-ttu-id="c7e49-162">Ortak işlevselliği <xref:System.Object> herhangi bir tür için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-162">The common functionality of <xref:System.Object> is available to any type.</span></span>

<span data-ttu-id="c7e49-163">Örtük devralmanın ne anlama geldiğini görmek için, `SimpleClass`sadece boş bir sınıf tanımı olan yeni bir sınıf tanımlayalım:</span><span class="sxs-lookup"><span data-stu-id="c7e49-163">To see what implicit inheritance means, let's define a new class, `SimpleClass`, that is simply an empty class definition:</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass.cs#1)]

<span data-ttu-id="c7e49-164">Daha sonra, `SimpleClass` türe ait üyelerin listesini almak için yansımayı (bu tür hakkında bilgi almak için bir türün meta verilerini incelemenize olanak tanır) kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-164">You can then use reflection (which lets you inspect a type's metadata to get information about that type) to get a list of the members that belong to the `SimpleClass` type.</span></span> <span data-ttu-id="c7e49-165">Sınıfınızda herhangi bir üye tanımlamamış olsanız `SimpleClass` da, örnekten elde edilen çıktı, aslında dokuz üyesi olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-165">Although you haven't defined any members in your `SimpleClass` class, output from the example indicates that it actually has nine members.</span></span> <span data-ttu-id="c7e49-166">Bu üyelerden biri, C# derleyicisi tarafından `SimpleClass` tür için otomatik olarak sağlanan parametresiz (veya varsayılan) bir oluşturucudur.</span><span class="sxs-lookup"><span data-stu-id="c7e49-166">One of these members is a parameterless (or default) constructor that is automatically supplied for the `SimpleClass` type by the C# compiler.</span></span> <span data-ttu-id="c7e49-167">Geri kalan sekiz <xref:System.Object>üye, .NET türü sistemindeki tüm sınıfların ve arabirimlerin sonuçta dolaylı olarak devraldığı türdedir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-167">The remaining eight are members of <xref:System.Object>, the type from which all classes and interfaces in the .NET type system ultimately implicitly inherit.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass.cs#2)]

<span data-ttu-id="c7e49-168">Sınıftan <xref:System.Object> örtülü devralma bu yöntemleri `SimpleClass` sınıf için kullanılabilir hale getirir:</span><span class="sxs-lookup"><span data-stu-id="c7e49-168">Implicit inheritance from the <xref:System.Object> class makes these methods available to the `SimpleClass` class:</span></span>

- <span data-ttu-id="c7e49-169">Bir `ToString` `SimpleClass` nesneyi dize gösterimine dönüştüren ortak yöntem, tam nitelikli tür adını döndürür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-169">The public `ToString` method, which converts a `SimpleClass` object to its string representation, returns the fully qualified type name.</span></span> <span data-ttu-id="c7e49-170">Bu durumda, `ToString` yöntem "SimpleClass" dizesini döndürür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-170">In this case, the `ToString` method returns the string "SimpleClass".</span></span>

- <span data-ttu-id="c7e49-171">İki nesnenin eşitliğini sınayan üç yöntem: genel `Equals(Object)` örnek `Equals(Object, Object)` yöntemi, genel `ReferenceEquals(Object, Object)` statik yöntem ve ortak statik yöntem.</span><span class="sxs-lookup"><span data-stu-id="c7e49-171">Three methods that test for equality of two objects: the public instance `Equals(Object)` method, the public static `Equals(Object, Object)` method, and the public static `ReferenceEquals(Object, Object)` method.</span></span> <span data-ttu-id="c7e49-172">Varsayılan olarak, bu yöntemler başvuru eşitliği için test; diğer bir şey, eşit olmak için iki nesne değişkeninin aynı nesneye başvurması gerekir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-172">By default, these methods test for reference equality; that is, to be equal, two object variables must refer to the same object.</span></span>

- <span data-ttu-id="c7e49-173">Türdeki `GetHashCode` bir örneğinin hashed koleksiyonlarında kullanılmasına izin veren bir değeri hesaplayan ortak yöntem.</span><span class="sxs-lookup"><span data-stu-id="c7e49-173">The public `GetHashCode` method, which computes a value that allows an instance of the type to be used in hashed collections.</span></span>

- <span data-ttu-id="c7e49-174">Türü `GetType` temsil eden bir <xref:System.Type> nesneyi döndüren ortak yöntem. `SimpleClass`</span><span class="sxs-lookup"><span data-stu-id="c7e49-174">The public `GetType` method, which returns a <xref:System.Type> object that represents the `SimpleClass` type.</span></span>

- <span data-ttu-id="c7e49-175">Bir <xref:System.Object.Finalize%2A> nesnenin belleği çöp toplayıcı tarafından geri önce yönetilmeyen kaynakları serbest bırakmak için tasarlanmış korumalı yöntem.</span><span class="sxs-lookup"><span data-stu-id="c7e49-175">The protected <xref:System.Object.Finalize%2A> method, which is designed to release unmanaged resources before an object's memory is reclaimed by the garbage collector.</span></span>

- <span data-ttu-id="c7e49-176">Geçerli <xref:System.Object.MemberwiseClone%2A> nesnenin sığ bir klon oluşturur korumalı yöntem.</span><span class="sxs-lookup"><span data-stu-id="c7e49-176">The protected <xref:System.Object.MemberwiseClone%2A> method, which creates a shallow clone of the current object.</span></span>

<span data-ttu-id="c7e49-177">Örtük devralma nedeniyle, bir nesneden devralınan herhangi bir `SimpleClass` üyeyi, sanki `SimpleClass` sınıfta tanımlanmış bir üyeymiş gibi arayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-177">Because of implicit inheritance, you can call any inherited member from a `SimpleClass` object just as if it was actually a member defined in the `SimpleClass` class.</span></span> <span data-ttu-id="c7e49-178">Örneğin, aşağıdaki örnekten `SimpleClass.ToString` `SimpleClass` devralınan yöntemi <xref:System.Object>çağırır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-178">For instance, the following example calls the `SimpleClass.ToString` method, which `SimpleClass` inherits from <xref:System.Object>.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass2.cs#1)]

<span data-ttu-id="c7e49-179">Aşağıdaki tabloda C#'da oluşturabileceğiniz türler ve bunların örtülü olarak devraldıkları türler listelenerilmiştir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-179">The following table lists the categories of types that you can create in C# and the types from which they implicitly inherit.</span></span> <span data-ttu-id="c7e49-180">Her taban türü, örtülü olarak türetilmiş türlere devralma yoluyla farklı bir üye kümesi sağlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-180">Each base type makes a different set of members available through inheritance to implicitly derived types.</span></span>

| <span data-ttu-id="c7e49-181">Tür kategorisi</span><span class="sxs-lookup"><span data-stu-id="c7e49-181">Type category</span></span> | <span data-ttu-id="c7e49-182">Dolaylı olarak devralır</span><span class="sxs-lookup"><span data-stu-id="c7e49-182">Implicitly inherits from</span></span>                                                      |
| ------------- | ----------------------------------------------------------------------------- |
| <span data-ttu-id="c7e49-183">sınıf</span><span class="sxs-lookup"><span data-stu-id="c7e49-183">class</span></span>         | <xref:System.Object>                                                          |
| <span data-ttu-id="c7e49-184">struct </span><span class="sxs-lookup"><span data-stu-id="c7e49-184">struct</span></span>        | <span data-ttu-id="c7e49-185"><xref:System.ValueType>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="c7e49-185"><xref:System.ValueType>, <xref:System.Object></span></span>                                 |
| <span data-ttu-id="c7e49-186">enum</span><span class="sxs-lookup"><span data-stu-id="c7e49-186">enum</span></span>          | <span data-ttu-id="c7e49-187"><xref:System.Enum>, <xref:System.ValueType>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="c7e49-187"><xref:System.Enum>, <xref:System.ValueType>, <xref:System.Object></span></span>             |
| <span data-ttu-id="c7e49-188">temsilci</span><span class="sxs-lookup"><span data-stu-id="c7e49-188">delegate</span></span>      | <span data-ttu-id="c7e49-189"><xref:System.MulticastDelegate>, <xref:System.Delegate>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="c7e49-189"><xref:System.MulticastDelegate>, <xref:System.Delegate>, <xref:System.Object></span></span> |

## <a name="inheritance-and-an-is-a-relationship"></a><span data-ttu-id="c7e49-190">Miras ve "bir" ilişkisi</span><span class="sxs-lookup"><span data-stu-id="c7e49-190">Inheritance and an "is a" relationship</span></span>

<span data-ttu-id="c7e49-191">Normalde, devralma, türemiş sınıfların taban sınıfın özel sürümleri olduğu bir taban sınıf ile bir veya daha fazla türetilmiş sınıf arasındaki "bir" ilişkiyi ifade etmek için kullanılır; türemiş sınıf taban sınıfın bir türüdür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-191">Ordinarily, inheritance is used to express an "is a" relationship between a base class and one or more derived classes, where the derived classes are specialized versions of the base class; the derived class is a type of the base class.</span></span> <span data-ttu-id="c7e49-192">Örneğin, `Publication` sınıf her türlü yayını temsil eder `Book` `Magazine` ve sınıflar belirli yayın türlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c7e49-192">For example, the `Publication` class represents a publication of any kind, and the `Book` and `Magazine` classes represent specific types of publications.</span></span>

> [!NOTE]
> <span data-ttu-id="c7e49-193">Bir sınıf veya yapı bir veya daha fazla arabirim uygulayabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-193">A class or struct can implement one or more interfaces.</span></span> <span data-ttu-id="c7e49-194">Arabirim uygulaması genellikle tek bir devralma için bir geçici çözüm olarak veya structs ile kalıtım kullanmanın bir yolu olarak sunulsa da, bir arayüz ve uygulama türü arasında farklı bir ilişki ("yapabilirim" ilişkisi) ifade etmek için tasarlanmıştır Devralma.</span><span class="sxs-lookup"><span data-stu-id="c7e49-194">While interface implementation is often presented as a workaround for single inheritance or as a way of using inheritance with structs, it is intended to express a different relationship (a "can do" relationship) between an interface and its implementing type than inheritance.</span></span> <span data-ttu-id="c7e49-195">Arabirim, arabirimin uygulama türlerine sunacağı bir işlev alt kümesini (eşitlik için sınama, nesneleri karşılaştırma veya sıralama veya kültüre duyarlı ayrıştırma ve biçimlendirmeyi destekleme gibi) tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-195">An interface defines a subset of functionality (such as the ability to test for equality, to compare or sort objects, or to support culture-sensitive parsing and formatting) that the interface makes available to its implementing types.</span></span>

<span data-ttu-id="c7e49-196">"A" da bir tür ve bu tür belirli bir anlık arasındaki ilişkiyi ifade ettiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="c7e49-196">Note that "is a" also expresses the relationship between a type and a specific instantiation of that type.</span></span> <span data-ttu-id="c7e49-197">Aşağıdaki örnekte, `Automobile` üç benzersiz salt okunur özelliği olan `Make`bir sınıftır: , otomobil üreticisi; `Model`, otomobil türü; ve `Year`, üretim yılı.</span><span class="sxs-lookup"><span data-stu-id="c7e49-197">In the following example, `Automobile` is a class that has three unique read-only properties: `Make`, the manufacturer of the automobile; `Model`, the kind of automobile; and `Year`, its year of manufacture.</span></span> <span data-ttu-id="c7e49-198">Sınıfınızın `Automobile` ayrıca, bağımsız değişkenleri özellik değerlerine atanmış bir oluşturucusu vardır ve <xref:System.Object.ToString%2A?displayProperty=nameWithType> `Automobile` `Automobile` sınıf yerine örneği benzersiz olarak tanımlayan bir dize oluşturmak için yöntemi geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-198">Your `Automobile` class also has a constructor whose arguments are assigned to the property values, and it overrides the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method to produce a string that uniquely identifies the `Automobile` instance rather than the `Automobile` class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/is-a.cs#1)]

<span data-ttu-id="c7e49-199">Bu durumda, belirli araba yapar ve modelleri temsil etmek için miras güvenmemelidir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-199">In this case, you shouldn't rely on inheritance to represent specific car makes and models.</span></span> <span data-ttu-id="c7e49-200">Örneğin, Packard Motor Car Company `Packard` tarafından üretilen otomobilleri temsil edecek bir tür tanımlamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c7e49-200">For example, you don't need to define a `Packard` type to represent automobiles manufactured by the Packard Motor Car Company.</span></span> <span data-ttu-id="c7e49-201">Bunun yerine, aşağıdaki örnekte `Automobile` olduğu gibi, sınıf oluşturucusuna geçirilen uygun değerlere sahip bir nesne oluşturarak onları temsil edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-201">Instead, you can represent them by creating an `Automobile` object with the appropriate values passed to its class constructor, as the following example does.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/is-a.cs#2)]

<span data-ttu-id="c7e49-202">Devralmaya dayalı bir ilişki, taban sınıfa ve taban sınıfa ek üye ekleyen veya taban sınıfta bulunmayan ek işlevsellik gerektiren türemiş sınıflara en iyi şekilde uygulanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-202">An is-a relationship based on inheritance is best applied to a base class and to derived classes that add additional members to the base class or that require additional functionality not present in the base class.</span></span>

## <a name="designing-the-base-class-and-derived-classes"></a><span data-ttu-id="c7e49-203">Taban sınıf ve türemiş sınıfların tasarımı</span><span class="sxs-lookup"><span data-stu-id="c7e49-203">Designing the base class and derived classes</span></span>

<span data-ttu-id="c7e49-204">Bir taban sınıf ve türetilmiş sınıflar tasarlama sürecine bakalım.</span><span class="sxs-lookup"><span data-stu-id="c7e49-204">Let's look at the process of designing a base class and its derived classes.</span></span> <span data-ttu-id="c7e49-205">Bu bölümde, kitap, dergi, gazete, dergi, `Publication`makale vb. gibi her türlü yayını temsil eden bir taban sınıf tanımlarsınız. Ayrıca, `Publication`'den `Book` türeyen bir sınıf tanımlarsınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-205">In this section, you'll define a base class, `Publication`, which represents a publication of any kind, such as a book, a magazine, a newspaper, a journal, an article, etc. You'll also define a `Book` class that derives from `Publication`.</span></span> <span data-ttu-id="c7e49-206">Örneği `Magazine`kolayca, " , `Journal`, `Newspaper`ve `Article`.</span><span class="sxs-lookup"><span data-stu-id="c7e49-206">You could easily extend the example to define other derived classes, such as `Magazine`, `Journal`, `Newspaper`, and `Article`.</span></span>

### <a name="the-base-publication-class"></a><span data-ttu-id="c7e49-207">Taban Yayın sınıfı</span><span class="sxs-lookup"><span data-stu-id="c7e49-207">The base Publication class</span></span>

<span data-ttu-id="c7e49-208">Sınıfınızı `Publication` tasarlarken, birkaç tasarım kararı almanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="c7e49-208">In designing your `Publication` class, you need to make several design decisions:</span></span>

- <span data-ttu-id="c7e49-209">Taban `Publication` sınıfınıza hangi üyelerin dahil edilip edilmeyeceği ve üyelerin yöntem uygulamaları sağlayıp sağlamadığı `Publication` veya türetilmiş sınıfları için şablon görevi sunan soyut bir taban sınıf olup olmadığı. `Publication`</span><span class="sxs-lookup"><span data-stu-id="c7e49-209">What members to include in your base `Publication` class, and whether the `Publication` members provide method implementations or whether `Publication` is an abstract base class that serves as a template for its derived classes.</span></span>

  <span data-ttu-id="c7e49-210">Bu durumda, `Publication` sınıf yöntem uygulamaları sağlayacaktır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-210">In this case, the `Publication` class will provide method implementations.</span></span> <span data-ttu-id="c7e49-211">[Soyut taban sınıfları ve bunların türemiş sınıfları](#abstract) tasarlama bölümü, türeyen sınıfların geçersiz kılmaları gereken yöntemleri tanımlamak için soyut bir taban sınıfı kullanan bir örnek içerir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-211">The [Designing abstract base classes and their derived classes](#abstract) section contains an example that uses an abstract base class to define the methods that derived classes must override.</span></span> <span data-ttu-id="c7e49-212">Türemiş sınıflar, türetilmiş türe uygun herhangi bir uygulama sağlamakta serbesttir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-212">Derived classes are free to provide any implementation that is suitable for the derived type.</span></span>

  <span data-ttu-id="c7e49-213">Kodu yeniden kullanabilme (diğer bir şekilde, birden çok türetilmiş sınıf, taban sınıf yöntemlerinin bildirimini ve uygulanmasını paylaşır ve bunları geçersiz kılmaya gerek yoktur) soyut olmayan temel sınıfların bir avantajıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-213">The ability to reuse code (that is, multiple derived classes share the declaration and implementation of base class methods and do not need to override them) is an advantage of non-abstract base classes.</span></span> <span data-ttu-id="c7e49-214">Bu nedenle, kodlarının `Publication` bazı veya en özel türler tarafından paylaşılmak olası olup olmadığını üyeler eklemeniz `Publication` gerekir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-214">Therefore, you should add members to `Publication` if their code is likely to be shared by some or most specialized `Publication` types.</span></span> <span data-ttu-id="c7e49-215">Taban sınıf uygulamalarını verimli bir şekilde sağlayamazsanız, taban sınıfta tek bir uygulama yerine türemiş sınıflarda büyük ölçüde aynı üye uygulamaları sağlamak zorunda kalırsınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-215">If you fail to provide base class implementations efficiently, you'll end up having to provide largely identical member implementations in derived classes rather a single implementation in the base class.</span></span> <span data-ttu-id="c7e49-216">Yinelenen kodu birden çok konumda tutma gereksinimi, olası bir hata kaynağıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-216">The need to maintain duplicated code in multiple locations is a potential source of bugs.</span></span>

  <span data-ttu-id="c7e49-217">Hem kod yeniden en üst düzeye çıkarmak hem de mantıksal ve sezgisel bir `Publication` devralma hiyerarşisi oluşturmak için, sınıfa yalnızca tüm veya çoğu yayında ortak olan verileri ve işlevselliği eklediğinizden emin olmak istersiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-217">Both to maximize code reuse and to create a logical and intuitive inheritance hierarchy, you want to be sure that you include in the `Publication` class only the data and functionality that is common to all or to most publications.</span></span> <span data-ttu-id="c7e49-218">Türemiş sınıflar daha sonra temsil ettikleri belirli yayın türlerine özgü üyeleri uygular.</span><span class="sxs-lookup"><span data-stu-id="c7e49-218">Derived classes then implement members that are unique to the particular kinds of publication that they represent.</span></span>

- <span data-ttu-id="c7e49-219">Sınıf hiyerarşinizi ne kadar genişletir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-219">How far to extend your class hierarchy.</span></span> <span data-ttu-id="c7e49-220">Basit bir taban sınıf ve bir veya daha fazla türemiş sınıf yerine üç veya daha fazla sınıftan oluşan bir hiyerarşi geliştirmek istiyor musunuz?</span><span class="sxs-lookup"><span data-stu-id="c7e49-220">Do you want to develop a hierarchy of three or more classes, rather than simply a base class and one or more derived classes?</span></span> <span data-ttu-id="c7e49-221">`Publication` Örneğin, sırayla bir taban `Periodical`sınıf olan `Magazine`bir taban sınıf `Journal` `Newspaper`olabilir , ve .</span><span class="sxs-lookup"><span data-stu-id="c7e49-221">For example, `Publication` could be a base class of `Periodical`, which in turn is a base class of `Magazine`, `Journal` and `Newspaper`.</span></span>

  <span data-ttu-id="c7e49-222">Örneğin, bir `Publication` sınıfın küçük hiyerarşisini ve tek bir türetilmiş `Book`sınıfın.</span><span class="sxs-lookup"><span data-stu-id="c7e49-222">For your example, you'll use the small hierarchy of a `Publication` class and a single derived class, `Book`.</span></span> <span data-ttu-id="c7e49-223">Örneğin, örneğinden `Publication` `Magazine` türeyen bir dizi ek sınıf oluşturmak için `Article`örneği kolayca genişletebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-223">You could easily extend the example to create a number of additional   classes that derive from `Publication`, such as `Magazine` and `Article`.</span></span>

- <span data-ttu-id="c7e49-224">Taban sınıfanlık mantıklı olup olmadığını.</span><span class="sxs-lookup"><span data-stu-id="c7e49-224">Whether it makes sense to instantiate the base class.</span></span> <span data-ttu-id="c7e49-225">Yoksa, [özet](../language-reference/keywords/abstract.md) anahtar sözcüğü sınıfa uygulamalısınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-225">If it does not, you should apply the [abstract](../language-reference/keywords/abstract.md) keyword to the class.</span></span> <span data-ttu-id="c7e49-226">Aksi takdirde, sınıfınız `Publication` sınıf oluşturucusu çağırarak anında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-226">Otherwise, your `Publication` class can be instantiated by calling its class constructor.</span></span> <span data-ttu-id="c7e49-227">`abstract` Anahtar kelimeyle işaretlenmiş bir sınıfı sınıf oluşturucuya doğrudan çağrı yaparak anında bir araya getirmeye çalışırsa, C# derleyicisi CS0144 hatası oluşturur: "Soyut sınıfın veya arabirimin bir örneğini oluşturamaz."</span><span class="sxs-lookup"><span data-stu-id="c7e49-227">If an attempt is made to instantiate a class marked with the `abstract` keyword by a direct call to its class constructor, the C# compiler generates error CS0144, "Cannot create an instance of the abstract class or interface."</span></span> <span data-ttu-id="c7e49-228">Yansıma kullanılarak sınıfı anında bir hale getirmeye çalışırsa, yansıma yöntemi bir <xref:System.MemberAccessException>.</span><span class="sxs-lookup"><span data-stu-id="c7e49-228">If an attempt is made to instantiate the class by using reflection, the reflection method throws a <xref:System.MemberAccessException>.</span></span>

  <span data-ttu-id="c7e49-229">Varsayılan olarak, bir taban sınıf, sınıf oluşturucusu çağırArak anında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-229">By default, a base class can be instantiated by calling its class constructor.</span></span> <span data-ttu-id="c7e49-230">Bir sınıf oluşturucusu açıkça tanımlamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c7e49-230">You do not have to explicitly define a class constructor.</span></span> <span data-ttu-id="c7e49-231">Temel sınıfın kaynak kodunda bir tane yoksa, C# derleyicisi otomatik olarak varsayılan (parametresiz) bir oluşturucu sağlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-231">If one is not present in the base class' source code, the C# compiler automatically provides a default (parameterless) constructor.</span></span>

  <span data-ttu-id="c7e49-232">Örneğin, `Publication` sınıfı anlık olarak işaretlenememesi için [soyut](../language-reference/keywords/abstract.md) olarak işaretlersiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-232">For your example, you'll mark the `Publication` class as [abstract](../language-reference/keywords/abstract.md) so that it cannot be instantiated.</span></span>  <span data-ttu-id="c7e49-233">Herhangi `abstract` `abstract` bir yöntem olmayan bir sınıf, bu sınıfın birkaç somut sınıf `Book`arasında `Journal`paylaşılan soyut bir kavramı temsil ettiğini gösterir (bir, gibi).</span><span class="sxs-lookup"><span data-stu-id="c7e49-233">An `abstract` class without any `abstract` methods indicates that this class represents an abstract concept that is shared among several concrete classes (like a `Book`, `Journal`).</span></span>

- <span data-ttu-id="c7e49-234">Türemiş sınıfların belirli üyelerin taban sınıf uygulamasını devralması gerekip gerekmediği, taban sınıf uygulamasını geçersiz kılma seçeneğiolup olmadığı veya bir uygulama sağlamaları gerekip gerekmediği.</span><span class="sxs-lookup"><span data-stu-id="c7e49-234">Whether derived classes must inherit the base class implementation of particular members, whether they have the option to override the base class implementation, or whether they must provide an implementation.</span></span> <span data-ttu-id="c7e49-235">Bir uygulama sağlamak için türetilen sınıfları zorlamak için [soyut](../language-reference/keywords/abstract.md) anahtar sözcük kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-235">You use the [abstract](../language-reference/keywords/abstract.md) keyword to force derived classes to provide an implementation.</span></span> <span data-ttu-id="c7e49-236">Türemiş sınıfların taban sınıf yöntemini geçersiz kılmasına izin vermek için [sanal](../language-reference/keywords/virtual.md) anahtar sözcüğü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-236">You use the [virtual](../language-reference/keywords/virtual.md) keyword to allow derived classes to override a base class method.</span></span> <span data-ttu-id="c7e49-237">Varsayılan olarak, taban sınıfta tanımlanan yöntemler geçersiz *kılınmaz.*</span><span class="sxs-lookup"><span data-stu-id="c7e49-237">By default, methods defined in the base class are *not* overridable.</span></span>

 <span data-ttu-id="c7e49-238">Sınıfın `Publication` herhangi bir `abstract` yöntemi yoktur, ancak `abstract`sınıfın kendisi.</span><span class="sxs-lookup"><span data-stu-id="c7e49-238">The `Publication` class does not have any `abstract` methods, but the class itself is `abstract`.</span></span>

- <span data-ttu-id="c7e49-239">Türetilen bir sınıfın devralma hiyerarşisinde son sınıfı temsil edip etmediği ve kendisi ek türemiş sınıflar için taban sınıf olarak kullanılamayacak.</span><span class="sxs-lookup"><span data-stu-id="c7e49-239">Whether a derived class represents the final class in the inheritance hierarchy and cannot itself be used as a base class for additional derived classes.</span></span> <span data-ttu-id="c7e49-240">Varsayılan olarak, herhangi bir sınıf bir taban sınıf olarak hizmet verebilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-240">By default, any class can serve as a base class.</span></span> <span data-ttu-id="c7e49-241">[Mühürlü](../language-reference/keywords/sealed.md) anahtar sözcüğü, bir sınıfın herhangi bir ek sınıf için taban sınıf olarak hizmet veremeyeceğini belirtmek için uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-241">You can apply the [sealed](../language-reference/keywords/sealed.md) keyword to indicate that a class cannot serve as a base class for any additional classes.</span></span> <span data-ttu-id="c7e49-242">CS0509 tarafından oluşturulan mühürlü bir sınıftan türemeye çalışan "mühürlü \<tür Adı>" elde edilemez.</span><span class="sxs-lookup"><span data-stu-id="c7e49-242">Attempting to derive from a sealed class generated compiler error CS0509, "cannot derive from sealed type \<typeName>".</span></span>

  <span data-ttu-id="c7e49-243">Örneğin, türemiş sınıfınızı `sealed`.</span><span class="sxs-lookup"><span data-stu-id="c7e49-243">For your example, you'll mark your derived class as `sealed`.</span></span>

<span data-ttu-id="c7e49-244">Aşağıdaki örnek, `Publication` sınıfın kaynak kodunun yanı sıra `PublicationType` özellik tarafından döndürülen bir `Publication.PublicationType` numaralandırmayı da gösterir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-244">The following example shows the source code for the `Publication` class, as well as a `PublicationType` enumeration that is returned by the `Publication.PublicationType` property.</span></span> <span data-ttu-id="c7e49-245"><xref:System.Object>Sınıf, devraldığı üyelere ek olarak aşağıdaki benzersiz üyeleri tanımlar ve üye geçersiz `Publication` kılar:</span><span class="sxs-lookup"><span data-stu-id="c7e49-245">In addition to the members that it inherits from <xref:System.Object>, the `Publication` class defines the following unique members and member overrides:</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/base-and-derived.cs#1)]

- <span data-ttu-id="c7e49-246">Bir yapıcı</span><span class="sxs-lookup"><span data-stu-id="c7e49-246">A constructor</span></span>

  <span data-ttu-id="c7e49-247">Sınıf `Publication` olduğu `abstract`için, aşağıdaki örnekgibi doğrudan koddan anında alınamaz:</span><span class="sxs-lookup"><span data-stu-id="c7e49-247">Because the `Publication` class is `abstract`, it cannot be instantiated directly from code like the following example:</span></span>

  ```csharp
  var publication = new Publication("Tiddlywinks for Experts", "Fun and Games",
                                    PublicationType.Book);
  ```

  <span data-ttu-id="c7e49-248">Ancak, `Book` örnek oluşturucu, sınıfın kaynak kodunun gösterdiği gibi, doğrudan türemiş sınıf oluşturucularından çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-248">However, its instance constructor can be called directly from derived class constructors, as the source code for the `Book` class shows.</span></span>

- <span data-ttu-id="c7e49-249">Yayınla ilgili iki özellik</span><span class="sxs-lookup"><span data-stu-id="c7e49-249">Two publication-related properties</span></span>

  <span data-ttu-id="c7e49-250">`Title`değeri yapıcıyı <xref:System.String> arayarak sağlanan salt okunur `Publication` bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-250">`Title` is a read-only <xref:System.String> property whose value is supplied by calling the `Publication` constructor.</span></span>

  <span data-ttu-id="c7e49-251">`Pages`yayının toplam <xref:System.Int32> kaç sayfaya sahip olduğunu gösteren bir okuma-yazma özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-251">`Pages` is a read-write <xref:System.Int32> property that indicates how many total pages the publication has.</span></span> <span data-ttu-id="c7e49-252">Değer, ". `totalPages`</span><span class="sxs-lookup"><span data-stu-id="c7e49-252">The value is stored in a private field named `totalPages`.</span></span> <span data-ttu-id="c7e49-253">Pozitif bir sayı olmalı <xref:System.ArgumentOutOfRangeException> veya bir atılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-253">It must be a positive number or an <xref:System.ArgumentOutOfRangeException> is thrown.</span></span>

- <span data-ttu-id="c7e49-254">Yayıncıyla ilgili üyeler</span><span class="sxs-lookup"><span data-stu-id="c7e49-254">Publisher-related members</span></span>

  <span data-ttu-id="c7e49-255">Salt okunur iki `Publisher` özellik `Type`ve .</span><span class="sxs-lookup"><span data-stu-id="c7e49-255">Two read-only properties, `Publisher` and `Type`.</span></span> <span data-ttu-id="c7e49-256">Değerler özgün olarak `Publication` sınıf oluşturucuya çağrı tarafından sağlanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-256">The values are originally supplied by the call to the `Publication` class constructor.</span></span>

- <span data-ttu-id="c7e49-257">Yayımlama ile ilgili üyeler</span><span class="sxs-lookup"><span data-stu-id="c7e49-257">Publishing-related members</span></span>

  <span data-ttu-id="c7e49-258">Ve `GetPublicationDate`, `Publish` ayarve yayın tarihini döndürün.</span><span class="sxs-lookup"><span data-stu-id="c7e49-258">Two methods, `Publish` and `GetPublicationDate`, set and return the publication date.</span></span> <span data-ttu-id="c7e49-259">Yöntem, `Publish` `published` `true` çağrıldığı zamana özel bir bayrak ayarlar ve özel `datePublished` alana bağımsız değişken olarak geçen tarihi atar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-259">The `Publish` method sets a private  `published` flag to `true` when it is called and assigns the date passed to it as an argument to the private `datePublished` field.</span></span> <span data-ttu-id="c7e49-260">`GetPublicationDate` Yöntem, `published` bayrak `false`ise "NYP" dizesini ve varsa `datePublished` alanın değerini `true`döndürür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-260">The `GetPublicationDate` method returns the string "NYP" if the `published` flag is `false`, and the value of the `datePublished` field if it is `true`.</span></span>

- <span data-ttu-id="c7e49-261">Telif hakkı yla ilgili üyeler</span><span class="sxs-lookup"><span data-stu-id="c7e49-261">Copyright-related members</span></span>

  <span data-ttu-id="c7e49-262">Yöntem, `Copyright` telif hakkı sahibinin adını ve telif hakkının yılını bağımsız `CopyrightName` değişken `CopyrightDate` olarak alır ve bunları bağımsız değişkenlere ve mülklere atar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-262">The `Copyright` method takes the name of the copyright holder and the year of the copyright as arguments and assigns them to the `CopyrightName` and `CopyrightDate` properties.</span></span>

- <span data-ttu-id="c7e49-263">`ToString` Yöntemin geçersiz kılındırı</span><span class="sxs-lookup"><span data-stu-id="c7e49-263">An override of the `ToString` method</span></span>

  <span data-ttu-id="c7e49-264">Bir tür <xref:System.Object.ToString%2A?displayProperty=nameWithType> yöntemi geçersiz kılmazsa, bir örneği diğerinden ayırt etmede çok az kullanılan türün tam nitelikli adını döndürür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-264">If a type does not override the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method, it returns the fully qualified name of the type, which is of little use in differentiating one instance from another.</span></span> <span data-ttu-id="c7e49-265">Sınıf `Publication` özelliğin <xref:System.Object.ToString%2A?displayProperty=nameWithType> değerini `Title` döndürmek için geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-265">The `Publication` class overrides <xref:System.Object.ToString%2A?displayProperty=nameWithType> to return the value of the `Title` property.</span></span>

<span data-ttu-id="c7e49-266">Aşağıdaki şekil, taban `Publication` sınıfınızla örtülü olarak devralınan <xref:System.Object> sınıfı arasındaki ilişkiyi göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-266">The following figure illustrates the relationship between your base `Publication` class and its implicitly inherited <xref:System.Object> class.</span></span>

![Nesne ve Yayın sınıfları](media/publication-class.jpg)

### <a name="the-book-class"></a><span data-ttu-id="c7e49-268">Sınıf `Book`</span><span class="sxs-lookup"><span data-stu-id="c7e49-268">The `Book` class</span></span>

<span data-ttu-id="c7e49-269">Sınıf, `Book` bir kitabı özel bir yayın türü olarak temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c7e49-269">The `Book` class represents a book as a specialized type of publication.</span></span> <span data-ttu-id="c7e49-270">Aşağıdaki örnek, `Book` sınıfın kaynak kodunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-270">The following example shows the source code for the `Book` class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/base-and-derived.cs#2)]

<span data-ttu-id="c7e49-271">`Publication`Sınıf, devraldığı üyelere ek olarak aşağıdaki benzersiz üyeleri tanımlar ve üye geçersiz `Book` kılar:</span><span class="sxs-lookup"><span data-stu-id="c7e49-271">In addition to the members that it inherits from `Publication`, the `Book` class defines the following unique members and member overrides:</span></span>

- <span data-ttu-id="c7e49-272">İki yapıcı</span><span class="sxs-lookup"><span data-stu-id="c7e49-272">Two constructors</span></span>

  <span data-ttu-id="c7e49-273">İki `Book` oluşturucu üç ortak parametreyi paylaşır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-273">The two `Book` constructors share three common parameters.</span></span> <span data-ttu-id="c7e49-274">İki, *başlık* ve *yayıncı,* `Publication` yapıcı parametrelerine karşılık gelir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-274">Two, *title* and *publisher*, correspond to parameters of the `Publication` constructor.</span></span> <span data-ttu-id="c7e49-275">Üçüncü *yazar,* bir kamu değişmez `Author` özelliği saklanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-275">The third is *author*, which is stored to a public immutable `Author` property.</span></span> <span data-ttu-id="c7e49-276">Bir `ISBN` oluşturucu, otomatik özellikte depolanan bir *isbn* parametresi içerir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-276">One constructor includes an *isbn* parameter, which is stored in the `ISBN` auto-property.</span></span>

  <span data-ttu-id="c7e49-277">İlk oluşturucu, [diğer](../language-reference/keywords/this.md) oluşturucuyu aramak için bu anahtar sözcüğü kullanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-277">The first constructor uses the [this](../language-reference/keywords/this.md) keyword to call the other constructor.</span></span> <span data-ttu-id="c7e49-278">Yapıcı zincirleme, yapıcıları tanımlamada yaygın bir desendir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-278">Constructor chaining is a common pattern in defining constructors.</span></span> <span data-ttu-id="c7e49-279">Daha az parametreye sahip yapıcılar, en fazla sayıda parametreyle yapıcıyı ararken varsayılan değerler sağlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-279">Constructors with fewer parameters provide default values when calling the constructor with the greatest number of parameters.</span></span>

  <span data-ttu-id="c7e49-280">İkinci oluşturucu, başlığı ve yayımcı adını taban sınıf oluşturucuya geçirmek için [temel](../language-reference/keywords/base.md) anahtar sözcüğü kullanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-280">The second constructor uses the [base](../language-reference/keywords/base.md) keyword to pass the title and publisher name to the base class constructor.</span></span> <span data-ttu-id="c7e49-281">Kaynak kodunuzdaki bir taban sınıf oluşturucuya açık bir çağrı yapmazsanız, C# derleyicisi temel sınıfın varsayılan veya parametresiz oluşturucusuna otomatik olarak çağrı sağlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-281">If you don't make an explicit call to a base class constructor in your source code, the C# compiler automatically supplies a call to the base class' default or parameterless constructor.</span></span>

- <span data-ttu-id="c7e49-282">Nesnenin `Book` Uluslararası `ISBN` Standart Kitap Numarasını, benzersiz 10 veya 13 basamaklı bir sayıyı döndüren salt okunur özellik.</span><span class="sxs-lookup"><span data-stu-id="c7e49-282">A read-only `ISBN` property, which returns the `Book` object's International Standard Book Number, a unique 10- or 13-digit number.</span></span> <span data-ttu-id="c7e49-283">`Book` ISBN, oluşturuculardan birine bağımsız değişken olarak verilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-283">The ISBN is supplied as an argument to one of the `Book` constructors.</span></span> <span data-ttu-id="c7e49-284">ISBN, derleyici tarafından otomatik olarak oluşturulan özel bir destek alanında depolanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-284">The ISBN is stored in a private backing field, which is auto-generated by the compiler.</span></span>

- <span data-ttu-id="c7e49-285">Salt `Author` okunur bir özellik.</span><span class="sxs-lookup"><span data-stu-id="c7e49-285">A read-only `Author` property.</span></span> <span data-ttu-id="c7e49-286">Yazar adı, her iki oluşturucuya da `Book` bağımsız değişken olarak verilir ve özellikte depolanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-286">The author name is supplied as an argument to both `Book` constructors and is stored in the property.</span></span>

- <span data-ttu-id="c7e49-287">Salt okunur fiyatla ilgili `Price` iki `Currency`özellik ve .</span><span class="sxs-lookup"><span data-stu-id="c7e49-287">Two read-only price-related properties, `Price` and `Currency`.</span></span> <span data-ttu-id="c7e49-288">Onların değerleri bir `SetPrice` yöntem çağrısında bağımsız değişken olarak sağlanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-288">Their values are provided as arguments in a `SetPrice` method call.</span></span> <span data-ttu-id="c7e49-289">Özellik `Currency` üç haneli ISO para birimi sembolüdür (örneğin, ABD doları için USD).</span><span class="sxs-lookup"><span data-stu-id="c7e49-289">The `Currency` property is the three-digit ISO currency symbol (for example, USD for the U.S. dollar).</span></span> <span data-ttu-id="c7e49-290">ISO para birimi sembolleri <xref:System.Globalization.RegionInfo.ISOCurrencySymbol%2A> tesisten alınabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-290">ISO currency symbols can be retrieved from the <xref:System.Globalization.RegionInfo.ISOCurrencySymbol%2A> property.</span></span> <span data-ttu-id="c7e49-291">Bu özelliklerin her ikisi de dışarıdan salt okunur, ancak `Book` her ikisi de sınıfta kod tarafından ayarlanabilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-291">Both of these properties are externally read-only, but both can be set by code in the `Book` class.</span></span>

- <span data-ttu-id="c7e49-292">Özelliklerin `SetPrice` `Price` ve `Currency` değerlerin değerlerini belirleyen bir yöntem.</span><span class="sxs-lookup"><span data-stu-id="c7e49-292">A `SetPrice` method, which sets the values of the `Price` and `Currency` properties.</span></span> <span data-ttu-id="c7e49-293">Bu değerler aynı özellikler tarafından döndürülür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-293">Those values are returned by those same properties.</span></span>

- <span data-ttu-id="c7e49-294">`ToString` Yönteme geçersiz kılar `Publication`(kalıtsal) ve <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> <xref:System.Object.GetHashCode%2A> yöntemleri <xref:System.Object>(devralınan).</span><span class="sxs-lookup"><span data-stu-id="c7e49-294">Overrides to the `ToString` method (inherited from `Publication`) and the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> and <xref:System.Object.GetHashCode%2A> methods (inherited from <xref:System.Object>).</span></span>

  <span data-ttu-id="c7e49-295">Geçersiz kılınmadığı sürece, <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> yöntem başvuru eşitliği için test edilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-295">Unless it is overridden, the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method tests for reference equality.</span></span> <span data-ttu-id="c7e49-296">Diğer bir arada, aynı nesneye başvururlarsa, iki nesne değişkeni eşit olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-296">That is, two object variables are considered to be equal if they refer to the same object.</span></span> <span data-ttu-id="c7e49-297">`Book` Sınıfta, diğer taraftan, iki `Book` nesne aynı ISBN varsa eşit olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-297">In the `Book` class, on the other hand, two `Book` objects should be equal if they have the same ISBN.</span></span>

  <span data-ttu-id="c7e49-298"><xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> Yöntemi geçersiz kaldığınızda, çalışma zamanının <xref:System.Object.GetHashCode%2A> maddeleri verimli alma için hashed koleksiyonlarında depolamak için kullandığı bir değeri döndüren yöntemi de geçersiz kılmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-298">When you override the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method, you must also override the <xref:System.Object.GetHashCode%2A> method, which returns a value that the runtime uses to store items in hashed collections for efficient retrieval.</span></span> <span data-ttu-id="c7e49-299">Karma kod, eşitlik testiyle tutarlı bir değer döndürmelidir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-299">The hash code should return a value that's consistent with the test for equality.</span></span> <span data-ttu-id="c7e49-300"><xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> `true` İki `Book` nesnenin ISBN özellikleri eşitse, <xref:System.String.GetHashCode%2A> `ISBN` özellik tarafından döndürülen dize yöntemini arayarak hesaplanan karma kodu döndürür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-300">Since you've overridden <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> to return `true` if the ISBN properties of two `Book` objects are equal, you return the hash code computed by calling the <xref:System.String.GetHashCode%2A> method of the string returned by the `ISBN` property.</span></span>

<span data-ttu-id="c7e49-301">Aşağıdaki şekil `Book` sınıf ve `Publication`, taban sınıf arasındaki ilişkiyi göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-301">The following figure illustrates the relationship between the `Book` class and `Publication`, its base class.</span></span>

![Yayın ve Kitap dersleri](media/book-class.jpg)

<span data-ttu-id="c7e49-303">Artık bir `Book` nesneyi anında anlayabilir, hem benzersiz hem de devralınan üyelerini çağırabilir ve aşağıdaki örnekte `Publication` görüldüğü `Book`gibi, bir tür veya tür parametresi bekleyen bir yönteme bağımsız değişken olarak geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-303">You can now instantiate a `Book` object, invoke both its unique and inherited members, and pass it as an argument to a method that expects a parameter of type `Publication` or of type `Book`, as the following example shows.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/use-publication.cs#1)]

## <a name="designing-abstract-base-classes-and-their-derived-classes"></a><span data-ttu-id="c7e49-304">Soyut taban sınıfları ve bunların türemiş sınıflarını tasarlama</span><span class="sxs-lookup"><span data-stu-id="c7e49-304">Designing abstract base classes and their derived classes</span></span>
<a name="abstract"></a>

<span data-ttu-id="c7e49-305">Önceki örnekte, türemiş sınıfların kodu paylaşmasına izin vermek için bir dizi yöntem için uygulama sağlayan bir taban sınıf tanımladınız.</span><span class="sxs-lookup"><span data-stu-id="c7e49-305">In the previous example, you defined a base class that provided an implementation for a number of methods to allow derived classes to share code.</span></span> <span data-ttu-id="c7e49-306">Ancak çoğu durumda, taban sınıfın bir uygulama sağlaması beklenmez.</span><span class="sxs-lookup"><span data-stu-id="c7e49-306">In many cases, however, the base class is not expected to provide an implementation.</span></span> <span data-ttu-id="c7e49-307">Bunun yerine, taban sınıf *soyut yöntemleri*bildiren soyut bir *sınıftır;* her türetilmiş sınıfın uygulaması gereken üyeleri tanımlayan bir şablon olarak hizmet vermektedir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-307">Instead, the base class is an *abstract class* that declares *abstract methods*; it serves as a template that defines the members that each derived class must implement.</span></span> <span data-ttu-id="c7e49-308">Genellikle soyut bir taban sınıfında, türetilen her türün uygulanması bu türe özgüdür.</span><span class="sxs-lookup"><span data-stu-id="c7e49-308">Typically in an abstract base class, the implementation of each derived type is unique to that type.</span></span> <span data-ttu-id="c7e49-309">Sınıf yayınlarda ortak işlevsellik uygulamaları sağlasa da, bir `Publication` nesneyi anında anımsamanın hiçbir anlamı olmadığından sınıfı soyut anahtar sözcükle işaretlediniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-309">You marked the class with the abstract keyword because it made no sense to instantiate a `Publication` object, although the class did provide implementations of functionality common to publications.</span></span>

<span data-ttu-id="c7e49-310">Örneğin, her kapalı iki boyutlu geometrik şekil iki özellik içerir: alan, şeklin iç kapsamı; ve çevre, ya da şeklin kenarları boyunca mesafe.</span><span class="sxs-lookup"><span data-stu-id="c7e49-310">For example, each closed two-dimensional geometric shape includes two properties: area, the inner extent of the shape; and perimeter, or the distance along the edges of the shape.</span></span> <span data-ttu-id="c7e49-311">Ancak, bu özelliklerin hesaplanma şekli tamamen belirli şekle bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-311">The way in which these properties are calculated, however, depends completely on the specific shape.</span></span> <span data-ttu-id="c7e49-312">Örneğin, bir dairenin çevresini (veya çevresini) hesaplama formülü, bir üçgeninkinden farklıdır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-312">The formula for calculating the perimeter (or circumference) of a circle, for example, is different from that of a triangle.</span></span> <span data-ttu-id="c7e49-313">Sınıf `Shape` yöntemleri `abstract` olan `abstract` bir sınıftır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-313">The `Shape` class is an `abstract` class with `abstract` methods.</span></span> <span data-ttu-id="c7e49-314">Bu, türetilmiş sınıfların aynı işlevselliği paylaştığını, ancak bu türemiş sınıfların bu işlevselliği farklı şekilde uyguladığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-314">That indicates derived classes share the same functionality, but those derived classes implement that functionality differently.</span></span>

<span data-ttu-id="c7e49-315">Aşağıdaki örnek, iki özelliği tanımlayan `Shape` soyut bir taban `Area` `Perimeter`sınıf tanımlar: ve .</span><span class="sxs-lookup"><span data-stu-id="c7e49-315">The following example defines an abstract base class named `Shape` that defines two properties: `Area` and `Perimeter`.</span></span> <span data-ttu-id="c7e49-316">Sınıfı [soyut](../language-reference/keywords/abstract.md) anahtar kelimeyle işaretlemenin yanı sıra, her örnek üye [soyut](../language-reference/keywords/abstract.md) anahtar kelimeyle de işaretlenir.</span><span class="sxs-lookup"><span data-stu-id="c7e49-316">In addition to marking the class with the [abstract](../language-reference/keywords/abstract.md) keyword, each instance member is also marked with the [abstract](../language-reference/keywords/abstract.md) keyword.</span></span> <span data-ttu-id="c7e49-317">Bu durumda, `Shape` aynı zamanda <xref:System.Object.ToString%2A?displayProperty=nameWithType> tam nitelikli adı yerine türün adını döndürmek için yöntemi geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-317">In this case, `Shape` also overrides the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method to return the name of the type, rather than its fully qualified name.</span></span> <span data-ttu-id="c7e49-318">Ve iki statik üye `GetArea` tanımlar `GetPerimeter`ve arayanların türemiş bir sınıfın bir örneğinin alanını ve çevresini kolayca almasına olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-318">And it defines two static members, `GetArea` and `GetPerimeter`, that allow callers to easily retrieve the area and perimeter of an instance of any derived class.</span></span> <span data-ttu-id="c7e49-319">Türetilmiş bir sınıfın bir örneğini bu yöntemlerden herhangi biri için geçtiğinde, çalışma zamanı türemiş sınıfın yöntem geçersiz kılınmasını çağırır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-319">When you pass an instance of a derived class to either of these methods, the runtime calls the method override of the derived class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#1)]

<span data-ttu-id="c7e49-320">Daha sonra belirli şekilleri `Shape` temsil eden bazı sınıflar elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-320">You can then derive some classes from `Shape` that represent specific shapes.</span></span> <span data-ttu-id="c7e49-321">Aşağıdaki örnekte üç sınıf `Triangle` `Rectangle`tanımlanır, `Circle`, , ve .</span><span class="sxs-lookup"><span data-stu-id="c7e49-321">The following example defines three classes, `Triangle`, `Rectangle`, and `Circle`.</span></span> <span data-ttu-id="c7e49-322">Her alan ve çevre hesaplamak için belirli bir şekil için benzersiz bir formül kullanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-322">Each uses a formula unique for that particular shape to compute the area and perimeter.</span></span> <span data-ttu-id="c7e49-323">Türetilen sınıflardan bazıları, temsil `Rectangle.Diagonal` ettikleri `Circle.Diameter`şekle özgü olan ve bu özellikleri de tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c7e49-323">Some of the derived classes also define properties, such as `Rectangle.Diagonal` and `Circle.Diameter`, that are unique to the shape that they represent.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#2)]

<span data-ttu-id="c7e49-324">Aşağıdaki örnekte `Shape`türetilen nesneler kullanır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-324">The following example uses objects derived from `Shape`.</span></span> <span data-ttu-id="c7e49-325">Türetilen bir dizi nesneyi anında `Shape` alır ve geri dönüş `Shape` `Shape` özelliği değerlerini saran sınıfın statik yöntemlerini çağırır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-325">It instantiates an array of objects derived from `Shape` and calls the static methods of the `Shape` class, which wraps return `Shape` property values.</span></span> <span data-ttu-id="c7e49-326">Çalışma zamanı, türemiş türlerin geçersiz kılınan özelliklerinden değerleri alır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-326">The runtime retrieves values from the overridden properties of the derived types.</span></span> <span data-ttu-id="c7e49-327">Örnek ayrıca dizideki `Shape` her nesneyi türetilmiş türüne atar ve döküm başarılı olursa, `Shape`o belirli alt sınıfın özelliklerini alır.</span><span class="sxs-lookup"><span data-stu-id="c7e49-327">The example also casts each `Shape` object in the array to its derived type and, if the cast succeeds, retrieves properties of that particular subclass of `Shape`.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#3)]

## <a name="see-also"></a><span data-ttu-id="c7e49-328">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="c7e49-328">See also</span></span>

- [<span data-ttu-id="c7e49-329">Sınıflar ve nesneler</span><span class="sxs-lookup"><span data-stu-id="c7e49-329">Classes and objects</span></span>](../tour-of-csharp/classes-and-objects.md)
- [<span data-ttu-id="c7e49-330">Devralma (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="c7e49-330">Inheritance (C# Programming Guide)</span></span>](../programming-guide/classes-and-structs/inheritance.md)
