---
title: "C 'de devralma #"
description: C# kitaplıkları ve uygulamalarında devralmayı kullanmayı öğrenin.
ms.date: 07/05/2018
ms.technology: csharp-fundamentals
ms.assetid: aeb68c74-0ea0-406f-9fbe-2ce02d47ef31
ms.openlocfilehash: 70db8716bea84984ad56d79fa9e26aab3a8182fa
ms.sourcegitcommit: 7476c20d2f911a834a00b8a7f5e8926bae6804d9
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/11/2020
ms.locfileid: "88063516"
---
# <a name="inheritance-in-c-and-net"></a><span data-ttu-id="dd7c9-103">C# ve .NET içinde devralma</span><span class="sxs-lookup"><span data-stu-id="dd7c9-103">Inheritance in C# and .NET</span></span>

<span data-ttu-id="dd7c9-104">Bu öğretici, C# dilinde devralma işlemini tanıtır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-104">This tutorial introduces you to inheritance in C#.</span></span> <span data-ttu-id="dd7c9-105">Devralma, belirli işlevleri (veri ve davranış) sağlayan bir temel sınıf tanımlamanızı ve bu işlevi devralıp geçersiz kılan türetilmiş sınıfları tanımlamanızı sağlayan nesne odaklı programlama dillerinin bir özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-105">Inheritance is a feature of object-oriented programming languages that allows you to define a base class that provides specific functionality (data and behavior) and to define derived classes that either inherit or override that functionality.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="dd7c9-106">Önkoşullar</span><span class="sxs-lookup"><span data-stu-id="dd7c9-106">Prerequisites</span></span>

<span data-ttu-id="dd7c9-107">Bu öğreticide .NET Core SDK yüklediğinizi varsayılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-107">This tutorial assumes that you've installed the .NET Core SDK.</span></span> <span data-ttu-id="dd7c9-108">[.NET Core İndirmeleri](https://dotnet.microsoft.com/download) sayfasını ziyaret ederek indirin.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-108">Visit the [.NET Core Downloads](https://dotnet.microsoft.com/download) page to download it.</span></span> <span data-ttu-id="dd7c9-109">Ayrıca bir kod düzenleyicisine ihtiyacınız vardır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-109">You also need a code editor.</span></span> <span data-ttu-id="dd7c9-110">Bu öğretici [Visual Studio Code](https://code.visualstudio.com)kullanır, ancak istediğiniz herhangi bir kod düzenleyicisini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-110">This tutorial uses [Visual Studio Code](https://code.visualstudio.com), although you can use any code editor of your choice.</span></span>

## <a name="running-the-examples"></a><span data-ttu-id="dd7c9-111">Örnekleri çalıştırma</span><span class="sxs-lookup"><span data-stu-id="dd7c9-111">Running the examples</span></span>

<span data-ttu-id="dd7c9-112">Bu öğreticide örnekleri oluşturmak ve çalıştırmak için, komut satırından [DotNet](../../core/tools/dotnet.md) yardımcı programını kullanın.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-112">To create and run the examples in this tutorial, you use the [dotnet](../../core/tools/dotnet.md) utility from the command line.</span></span> <span data-ttu-id="dd7c9-113">Her örnek için aşağıdaki adımları izleyin:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-113">Follow these steps for each example:</span></span>

1. <span data-ttu-id="dd7c9-114">Örneği depolamak için bir dizin oluşturun.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-114">Create a directory to store the example.</span></span>
1. <span data-ttu-id="dd7c9-115">Yeni bir .NET Core projesi oluşturmak için bir komut isteminde [DotNet yeni konsol](../../core/tools/dotnet-new.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-115">Enter the [dotnet new console](../../core/tools/dotnet-new.md) command at a command prompt to create a new .NET Core project.</span></span>
1. <span data-ttu-id="dd7c9-116">Kod düzenleyicinize örnekteki kodu kopyalayın ve yapıştırın.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-116">Copy and paste the code from the example into your code editor.</span></span>
1. <span data-ttu-id="dd7c9-117">Projenin bağımlılıklarını yüklemek veya geri yüklemek için komut satırından [DotNet restore](../../core/tools/dotnet-restore.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-117">Enter the [dotnet restore](../../core/tools/dotnet-restore.md) command from the command line to load or restore the project's dependencies.</span></span>

   [!INCLUDE[DotNet Restore Note](~/includes/dotnet-restore-note.md)]

1. <span data-ttu-id="dd7c9-118">Örneği derlemek ve yürütmek için [DotNet Run](../../core/tools/dotnet-run.md) komutunu girin.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-118">Enter the [dotnet run](../../core/tools/dotnet-run.md) command to compile and execute the example.</span></span>

## <a name="background-what-is-inheritance"></a><span data-ttu-id="dd7c9-119">Arka plan: devralma nedir?</span><span class="sxs-lookup"><span data-stu-id="dd7c9-119">Background: What is inheritance?</span></span>

<span data-ttu-id="dd7c9-120">*Devralma* , nesne odaklı programlamanın temel özniteliklerinden biridir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-120">*Inheritance* is one of the fundamental attributes of object-oriented programming.</span></span> <span data-ttu-id="dd7c9-121">Bir üst sınıfın davranışını yeniden kullanan (devralan), genişleten veya değiştiren bir alt sınıf tanımlamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-121">It allows you to define a child class that reuses (inherits), extends, or modifies the behavior of a parent class.</span></span> <span data-ttu-id="dd7c9-122">Üyelerine devralınan sınıfa *temel sınıf*denir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-122">The class whose members are inherited is called the *base class*.</span></span> <span data-ttu-id="dd7c9-123">Taban sınıfının üyelerini devralan sınıf *türetilmiş sınıf*olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-123">The class that inherits the members of the base class is called the *derived class*.</span></span>

<span data-ttu-id="dd7c9-124">C# ve .NET yalnızca *tek devralmayı* destekler.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-124">C# and .NET support *single inheritance* only.</span></span> <span data-ttu-id="dd7c9-125">Diğer bir deyişle, bir sınıf yalnızca tek bir sınıftan devralınabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-125">That is, a class can only inherit from a single class.</span></span> <span data-ttu-id="dd7c9-126">Ancak, devralma geçişlidir ve bu, bir tür kümesi için devralma hiyerarşisi tanımlamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-126">However, inheritance is transitive, which allows you to define an inheritance hierarchy for a set of types.</span></span> <span data-ttu-id="dd7c9-127">Diğer bir deyişle, türü, `D` `C` `B` temel sınıf türünden devralan türünden devralan türünden devralabilir `A` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-127">In other words, type `D` can inherit from type `C`, which inherits from type `B`, which inherits from the base class type `A`.</span></span> <span data-ttu-id="dd7c9-128">Devralma geçişli olduğundan, türü üyeleri `A` türü için kullanılabilir `D` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-128">Because inheritance is transitive, the members of type `A` are available to type `D`.</span></span>

<span data-ttu-id="dd7c9-129">Bir taban sınıfın tüm üyeleri türetilmiş sınıflar tarafından devralınmaz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-129">Not all members of a base class are inherited by derived classes.</span></span> <span data-ttu-id="dd7c9-130">Aşağıdaki Üyeler devralınmaz:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-130">The following members are not inherited:</span></span>

- <span data-ttu-id="dd7c9-131">Statik [oluşturucular](../programming-guide/classes-and-structs/static-constructors.md), bir sınıfın statik verilerini başlatır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-131">[Static constructors](../programming-guide/classes-and-structs/static-constructors.md), which initialize the static data of a class.</span></span>

- <span data-ttu-id="dd7c9-132">Sınıfının yeni bir örneğini oluşturmak için çağırdığınız [örnek oluşturucular](../programming-guide/classes-and-structs/constructors.md).</span><span class="sxs-lookup"><span data-stu-id="dd7c9-132">[Instance constructors](../programming-guide/classes-and-structs/constructors.md), which you call to create a new instance of the class.</span></span> <span data-ttu-id="dd7c9-133">Her sınıfın kendi oluşturucuları tanımlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-133">Each class must define its own constructors.</span></span>

- <span data-ttu-id="dd7c9-134">Çalışma zamanının atık toplayıcısı tarafından bir sınıfın örneklerini yok etmek için çağrılan [sonlandırıcılar](../programming-guide/classes-and-structs/destructors.md).</span><span class="sxs-lookup"><span data-stu-id="dd7c9-134">[Finalizers](../programming-guide/classes-and-structs/destructors.md), which are called by the runtime's garbage collector to destroy instances of a class.</span></span>

<span data-ttu-id="dd7c9-135">Bir temel sınıfın diğer tüm üyeleri türetilmiş sınıflar tarafından devralınırken, görünür olup olmadığı ve erişilebilirliğine bağlı olup olmadığı.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-135">While all other members of a base class are inherited by derived classes, whether they are visible or not depends on their accessibility.</span></span> <span data-ttu-id="dd7c9-136">Üyenin erişilebilirliği, türetilmiş sınıfların görünürlüğünü aşağıdaki gibi etkiler:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-136">A member's accessibility affects its visibility for derived classes as follows:</span></span>

- <span data-ttu-id="dd7c9-137">[Özel](../language-reference/keywords/private.md) Üyeler yalnızca kendi temel sınıfında iç içe yerleştirilmiş türetilmiş sınıflarda görünür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-137">[Private](../language-reference/keywords/private.md) members are visible only in derived classes that are nested in their base class.</span></span> <span data-ttu-id="dd7c9-138">Aksi takdirde, bunlar türetilmiş sınıflarda görünür değildir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-138">Otherwise, they are not visible in derived classes.</span></span> <span data-ttu-id="dd7c9-139">Aşağıdaki örnekte, `A.B` öğesinden türetilen `A` ve türeten türetilmiş bir iç içe sınıftır `C` `A` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-139">In the following example, `A.B` is a nested class that derives from `A`, and `C` derives from `A`.</span></span> <span data-ttu-id="dd7c9-140">Özel `A.value` alan A.B. içinde görünür</span><span class="sxs-lookup"><span data-stu-id="dd7c9-140">The private `A.value` field is visible in A.B.</span></span> <span data-ttu-id="dd7c9-141">Ancak, yöntemden açıklamaları kaldırır `C.GetValue` ve örneği derlemeye çalışırsanız, CS0122 derleyici hatası üretir: "' A. Value ', koruma düzeyi nedeniyle erişilemez."</span><span class="sxs-lookup"><span data-stu-id="dd7c9-141">However, if you remove the comments from the `C.GetValue` method and attempt to compile the example, it produces compiler error CS0122: "'A.value' is inaccessible due to its protection level."</span></span>

  [!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/private.cs#1)]

- <span data-ttu-id="dd7c9-142">[Korunan](../language-reference/keywords/protected.md) Üyeler yalnızca türetilmiş sınıflarda görülebilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-142">[Protected](../language-reference/keywords/protected.md) members are visible only in derived classes.</span></span>

- <span data-ttu-id="dd7c9-143">[İç](../language-reference/keywords/internal.md) Üyeler yalnızca temel sınıfla aynı derlemede bulunan türetilmiş sınıflarda görülebilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-143">[Internal](../language-reference/keywords/internal.md) members are visible only in derived classes that are located in the same assembly as the base class.</span></span> <span data-ttu-id="dd7c9-144">Bunlar, temel sınıftan farklı bir derlemede bulunan türetilmiş sınıflarda görünür değildir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-144">They are not visible in derived classes located in a different assembly from the base class.</span></span>

- <span data-ttu-id="dd7c9-145">[Ortak](../language-reference/keywords/public.md) Üyeler türetilmiş sınıflarda görünür ve türetilmiş sınıf ' genel arabiriminin bir parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-145">[Public](../language-reference/keywords/public.md) members are visible in derived classes and are part of the derived class' public interface.</span></span> <span data-ttu-id="dd7c9-146">Ortak devralınmış Üyeler, türetilmiş sınıfta tanımlandıklarında olduğu gibi çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-146">Public inherited members can be called just as if they are defined in the derived class.</span></span> <span data-ttu-id="dd7c9-147">Aşağıdaki örnekte, sınıfı `A` adlı bir yöntemi tanımlar ve sınıf sınıfından `Method1` `B` devralır `A` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-147">In the following example, class `A` defines a method named `Method1`, and class `B` inherits from class `A`.</span></span> <span data-ttu-id="dd7c9-148">Örnek daha sonra `Method1` ' de bir örnek yöntemi gibi çağırır `B` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-148">The example then calls `Method1` as if it were an instance method on `B`.</span></span>

  [!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/basics.cs#1)]

<span data-ttu-id="dd7c9-149">Türetilmiş sınıflar, alternatif bir uygulama sağlayarak devralınan üyeleri de *geçersiz kılabilir* .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-149">Derived classes can also *override* inherited members by providing an alternate implementation.</span></span> <span data-ttu-id="dd7c9-150">Bir üyeyi geçersiz kılabilmek için, temel sınıftaki üyenin [sanal](../language-reference/keywords/virtual.md) anahtar sözcüğüyle işaretlenmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-150">In order to be able to override a member, the member in the base class must be marked with the [virtual](../language-reference/keywords/virtual.md) keyword.</span></span> <span data-ttu-id="dd7c9-151">Varsayılan olarak, temel sınıf üyeleri olarak işaretlenir `virtual` ve geçersiz kılınamaz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-151">By default, base class members are not marked as `virtual` and cannot be overridden.</span></span> <span data-ttu-id="dd7c9-152">Aşağıdaki örnekte olduğu gibi sanal olmayan bir üyeyi geçersiz kılma girişimi, CS0506: " \<member> \<member> sanal, Özet veya geçersiz kılma olarak işaretlenmediğinden devralınan üye geçersiz kılınamaz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-152">Attempting to override a non-virtual member, as the following example does, generates compiler error CS0506: "\<member> cannot override inherited member \<member> because it is not marked virtual, abstract, or override.</span></span>

```csharp
public class A
{
    public void Method1()
    {
        // Do something.
    }
}

public class B : A
{
    public override void Method1() // Generates CS0506.
    {
        // Do something else.
    }
}
```

<span data-ttu-id="dd7c9-153">Bazı durumlarda, türetilmiş bir sınıf temel sınıf uygulamasını geçersiz *kılmalıdır* .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-153">In some cases, a derived class *must* override the base class implementation.</span></span> <span data-ttu-id="dd7c9-154">[Soyut](../language-reference/keywords/abstract.md) anahtar sözcükle işaretlenmiş temel sınıf üyeleri, türetilmiş sınıfların bunları geçersiz kılmasını gerektirir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-154">Base class members marked with the [abstract](../language-reference/keywords/abstract.md) keyword require that derived classes override them.</span></span> <span data-ttu-id="dd7c9-155">Aşağıdaki örnek derlenmeye çalışıldığında derleyici hatası CS0534, " &lt; sınıf &gt; devralınmış soyut üye &lt; üyesini uygulamıyor", &gt; çünkü sınıf `B` için uygulama sağlamaz `A.Method1` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-155">Attempting to compile the following example generates compiler error CS0534, "&lt;class&gt; does not implement inherited abstract member &lt;member&gt;", because class `B` provides no implementation for `A.Method1`.</span></span>

```csharp
public abstract class A
{
    public abstract void Method1();
}

public class B : A // Generates CS0534.
{
    public void Method3()
    {
        // Do something.
    }
}
```

<span data-ttu-id="dd7c9-156">Devralma yalnızca sınıflar ve arabirimler için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-156">Inheritance applies only to classes and interfaces.</span></span> <span data-ttu-id="dd7c9-157">Diğer tür kategorileri (yapılar, temsilciler ve numaralandırmalar) devralmayı desteklemez.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-157">Other type categories (structs, delegates, and enums) do not support inheritance.</span></span> <span data-ttu-id="dd7c9-158">Bu kurallar nedeniyle, aşağıdaki örnek gibi kodu derlemeye çalışmak derleyici hatası oluşturur CS0527: "tür ' ValueType ', arabirim listesinde bir arabirim değil."</span><span class="sxs-lookup"><span data-stu-id="dd7c9-158">Because of these rules, attempting to compile code like the following example produces compiler error CS0527: "Type 'ValueType' in interface list is not an interface."</span></span> <span data-ttu-id="dd7c9-159">Hata iletisi, bir yapının uyguladığı arabirimleri tanımlayabilseniz de devralmanın desteklenmediğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-159">The error message indicates that, although you can define the interfaces that a struct implements, inheritance is not supported.</span></span>

```csharp
using System;

public struct ValueStructure : ValueType // Generates CS0527.
{
}
```

## <a name="implicit-inheritance"></a><span data-ttu-id="dd7c9-160">Örtük devralma</span><span class="sxs-lookup"><span data-stu-id="dd7c9-160">Implicit inheritance</span></span>

<span data-ttu-id="dd7c9-161">Tek devralmayla devraldıkları türlerin yanı sıra, .NET tür sistemi 'ndeki tüm türler örtülü olarak <xref:System.Object> devralınır veya ondan türetilmiş bir tür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-161">Besides any types that they may inherit from through single inheritance, all types in the .NET type system implicitly inherit from <xref:System.Object> or a type derived from it.</span></span> <span data-ttu-id="dd7c9-162">Ortak işlevselliği <xref:System.Object> her türlü tür için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-162">The common functionality of <xref:System.Object> is available to any type.</span></span>

<span data-ttu-id="dd7c9-163">Örtük devralmanın ne anlama geldiğini görmek için, `SimpleClass` yalnızca boş bir sınıf tanımı olan yeni bir sınıf tanımlayalim:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-163">To see what implicit inheritance means, let's define a new class, `SimpleClass`, that is simply an empty class definition:</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass.cs#1)]

<span data-ttu-id="dd7c9-164">Daha sonra, türe ait üyelerin bir listesini almak için yansıma (Bu tür hakkında bilgi almak üzere bir türün meta verilerini incelemenizi sağlar) kullanabilirsiniz `SimpleClass` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-164">You can then use reflection (which lets you inspect a type's metadata to get information about that type) to get a list of the members that belong to the `SimpleClass` type.</span></span> <span data-ttu-id="dd7c9-165">Sınıfınızda herhangi bir üye tanımlamadıysanız `SimpleClass` , örnekteki çıkış gerçekten dokuz üyeye sahip olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-165">Although you haven't defined any members in your `SimpleClass` class, output from the example indicates that it actually has nine members.</span></span> <span data-ttu-id="dd7c9-166">Bu üyelerden biri, `SimpleClass` C# derleyicisi tarafından tür için otomatik olarak sağlanan parametresiz (veya varsayılan) bir oluşturucudur.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-166">One of these members is a parameterless (or default) constructor that is automatically supplied for the `SimpleClass` type by the C# compiler.</span></span> <span data-ttu-id="dd7c9-167">Kalan sekiz, <xref:System.Object> .net tür sistemindeki tüm sınıfların ve arabirimlerin sonunda dolaylı olarak devraldığı tür üyesidir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-167">The remaining eight are members of <xref:System.Object>, the type from which all classes and interfaces in the .NET type system ultimately implicitly inherit.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass.cs#2)]

<span data-ttu-id="dd7c9-168">Sınıfından örtük devralma, <xref:System.Object> Bu yöntemleri sınıfı için kullanılabilir hale getirir `SimpleClass` :</span><span class="sxs-lookup"><span data-stu-id="dd7c9-168">Implicit inheritance from the <xref:System.Object> class makes these methods available to the `SimpleClass` class:</span></span>

- <span data-ttu-id="dd7c9-169">`ToString`Bir nesneyi dize gösterimine dönüştüren public yöntemi `SimpleClass` , tam nitelikli tür adını döndürür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-169">The public `ToString` method, which converts a `SimpleClass` object to its string representation, returns the fully qualified type name.</span></span> <span data-ttu-id="dd7c9-170">Bu durumda, `ToString` yöntemi "SimpleClass" dizesini döndürür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-170">In this case, the `ToString` method returns the string "SimpleClass".</span></span>

- <span data-ttu-id="dd7c9-171">İki nesnenin eşitlik için test eden üç yöntem: ortak örnek `Equals(Object)` yöntemi, ortak statik `Equals(Object, Object)` Yöntem ve genel statik `ReferenceEquals(Object, Object)` Yöntem.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-171">Three methods that test for equality of two objects: the public instance `Equals(Object)` method, the public static `Equals(Object, Object)` method, and the public static `ReferenceEquals(Object, Object)` method.</span></span> <span data-ttu-id="dd7c9-172">Varsayılan olarak, bu yöntemler başvuru eşitliği için test; diğer bir deyişle, eşit olması için iki nesne değişkeninin aynı nesneye başvurması gerekir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-172">By default, these methods test for reference equality; that is, to be equal, two object variables must refer to the same object.</span></span>

- <span data-ttu-id="dd7c9-173">`GetHashCode`Karma koleksiyonlarda kullanılacak tür örneğine izin veren bir değeri hesaplayan ortak yöntemi.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-173">The public `GetHashCode` method, which computes a value that allows an instance of the type to be used in hashed collections.</span></span>

- <span data-ttu-id="dd7c9-174">`GetType` <xref:System.Type> Türü temsil eden bir nesne döndüren public yöntemi `SimpleClass` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-174">The public `GetType` method, which returns a <xref:System.Type> object that represents the `SimpleClass` type.</span></span>

- <span data-ttu-id="dd7c9-175"><xref:System.Object.Finalize%2A>Bir nesnenin belleğinden önce yönetilmeyen kaynakları serbest bırakmak için tasarlanan Protected yöntemi çöp toplayıcı tarafından geri kazanılır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-175">The protected <xref:System.Object.Finalize%2A> method, which is designed to release unmanaged resources before an object's memory is reclaimed by the garbage collector.</span></span>

- <span data-ttu-id="dd7c9-176"><xref:System.Object.MemberwiseClone%2A>Geçerli nesnesinin basit bir kopyasını oluşturan Protected yöntemi.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-176">The protected <xref:System.Object.MemberwiseClone%2A> method, which creates a shallow clone of the current object.</span></span>

<span data-ttu-id="dd7c9-177">Örtük devralma nedeniyle, bir nesneden devralınan herhangi bir üyeyi, `SimpleClass` aslında yalnızca sınıfında tanımlanmış bir üye gibi çağırabilirsiniz `SimpleClass` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-177">Because of implicit inheritance, you can call any inherited member from a `SimpleClass` object just as if it was actually a member defined in the `SimpleClass` class.</span></span> <span data-ttu-id="dd7c9-178">Örneğin, aşağıdaki örnek `SimpleClass.ToString` `SimpleClass` öğesinden devralan yöntemini çağırır <xref:System.Object> .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-178">For instance, the following example calls the `SimpleClass.ToString` method, which `SimpleClass` inherits from <xref:System.Object>.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/simpleclass2.cs#1)]

<span data-ttu-id="dd7c9-179">Aşağıdaki tabloda, C# ' de oluşturabileceğiniz türlerin ve örtülü olarak devraldığı türlerin kategorileri listelenmektedir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-179">The following table lists the categories of types that you can create in C# and the types from which they implicitly inherit.</span></span> <span data-ttu-id="dd7c9-180">Her temel tür, devralma yoluyla örtülü olarak türetilmiş türlere farklı bir üye kümesi sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-180">Each base type makes a different set of members available through inheritance to implicitly derived types.</span></span>

| <span data-ttu-id="dd7c9-181">Tür kategorisi</span><span class="sxs-lookup"><span data-stu-id="dd7c9-181">Type category</span></span> | <span data-ttu-id="dd7c9-182">Örtülü olarak devralır</span><span class="sxs-lookup"><span data-stu-id="dd7c9-182">Implicitly inherits from</span></span>                                                      |
| ------------- | ----------------------------------------------------------------------------- |
| <span data-ttu-id="dd7c9-183">sınıf</span><span class="sxs-lookup"><span data-stu-id="dd7c9-183">class</span></span>         | <xref:System.Object>                                                          |
| <span data-ttu-id="dd7c9-184">struct</span><span class="sxs-lookup"><span data-stu-id="dd7c9-184">struct</span></span>        | <span data-ttu-id="dd7c9-185"><xref:System.ValueType>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="dd7c9-185"><xref:System.ValueType>, <xref:System.Object></span></span>                                 |
| <span data-ttu-id="dd7c9-186">enum</span><span class="sxs-lookup"><span data-stu-id="dd7c9-186">enum</span></span>          | <span data-ttu-id="dd7c9-187"><xref:System.Enum>, <xref:System.ValueType>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="dd7c9-187"><xref:System.Enum>, <xref:System.ValueType>, <xref:System.Object></span></span>             |
| <span data-ttu-id="dd7c9-188">temsilci</span><span class="sxs-lookup"><span data-stu-id="dd7c9-188">delegate</span></span>      | <span data-ttu-id="dd7c9-189"><xref:System.MulticastDelegate>, <xref:System.Delegate>, <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="dd7c9-189"><xref:System.MulticastDelegate>, <xref:System.Delegate>, <xref:System.Object></span></span> |

## <a name="inheritance-and-an-is-a-relationship"></a><span data-ttu-id="dd7c9-190">Devralma ve "bir" ilişkidir</span><span class="sxs-lookup"><span data-stu-id="dd7c9-190">Inheritance and an "is a" relationship</span></span>

<span data-ttu-id="dd7c9-191">Genellikle devralma, bir temel sınıf ve bir veya daha fazla türetilmiş sınıf arasındaki "bir", türetilmiş sınıfların temel sınıfın özelleşmiş sürümü olduğu bir veya daha fazla türetilmiş sınıf arasındaki ilişkiyi ifade etmek için kullanılır; türetilmiş sınıf, temel sınıfın bir türüdür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-191">Ordinarily, inheritance is used to express an "is a" relationship between a base class and one or more derived classes, where the derived classes are specialized versions of the base class; the derived class is a type of the base class.</span></span> <span data-ttu-id="dd7c9-192">Örneğin, `Publication` sınıf herhangi bir türdeki yayını temsil eder ve `Book` ve `Magazine` sınıfları belirli yayın türlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-192">For example, the `Publication` class represents a publication of any kind, and the `Book` and `Magazine` classes represent specific types of publications.</span></span>

> [!NOTE]
> <span data-ttu-id="dd7c9-193">Bir sınıf veya yapı, bir veya daha fazla arabirim uygulayabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-193">A class or struct can implement one or more interfaces.</span></span> <span data-ttu-id="dd7c9-194">Arabirim uygulaması, tek devralma için bir geçici çözüm olarak veya yapı ile devralma kullanmanın bir yolu olarak sunulurken, bir arabirim ile devralma türü arasında farklı bir ilişki ("yapabilir" ilişki) ifade etmek için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-194">While interface implementation is often presented as a workaround for single inheritance or as a way of using inheritance with structs, it is intended to express a different relationship (a "can do" relationship) between an interface and its implementing type than inheritance.</span></span> <span data-ttu-id="dd7c9-195">Bir arabirim, bir işlev alt kümesini tanımlar (örneğin, eşitlik için test etme, nesneleri karşılaştırma veya sıralama veya kültüre duyarlı ayrıştırma ve biçimlendirmeyi destekleme), arabirimin uygulama türleri için kullanılabilir hale getiren özellik.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-195">An interface defines a subset of functionality (such as the ability to test for equality, to compare or sort objects, or to support culture-sensitive parsing and formatting) that the interface makes available to its implementing types.</span></span>

<span data-ttu-id="dd7c9-196">"Bunun bir" olduğuna ve bu türün belirli bir örneğini oluşturma arasındaki ilişkiyi ifade eder.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-196">Note that "is a" also expresses the relationship between a type and a specific instantiation of that type.</span></span> <span data-ttu-id="dd7c9-197">Aşağıdaki örnekte, `Automobile` üç benzersiz salt okuma özelliği olan bir sınıftır: `Make` , otomobil üreticisi;, `Model` otomobil türü; ve `Year` , üretim yılı.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-197">In the following example, `Automobile` is a class that has three unique read-only properties: `Make`, the manufacturer of the automobile; `Model`, the kind of automobile; and `Year`, its year of manufacture.</span></span> <span data-ttu-id="dd7c9-198">`Automobile`Sınıfınız Ayrıca, bağımsız değişkenleri özellik değerlerine atanan bir oluşturucuya sahiptir ve <xref:System.Object.ToString%2A?displayProperty=nameWithType> `Automobile` sınıfı yerine örneği benzersiz bir şekilde tanımlayan bir dize oluşturmak için yöntemini geçersiz kılar `Automobile` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-198">Your `Automobile` class also has a constructor whose arguments are assigned to the property values, and it overrides the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method to produce a string that uniquely identifies the `Automobile` instance rather than the `Automobile` class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/is-a.cs#1)]

<span data-ttu-id="dd7c9-199">Bu durumda, belirli otomobil yaptığı ve modellerinin temsil edilebilmesi için devralmadan güvenmemelisiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-199">In this case, you shouldn't rely on inheritance to represent specific car makes and models.</span></span> <span data-ttu-id="dd7c9-200">Örneğin, `Packard` Packard motor otomobil şirketi tarafından üretilen otomobil 'leri temsil eden bir tür tanımlamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-200">For example, you don't need to define a `Packard` type to represent automobiles manufactured by the Packard Motor Car Company.</span></span> <span data-ttu-id="dd7c9-201">Bunun yerine, `Automobile` Aşağıdaki örnekte olduğu gibi, sınıf oluşturucusuna geçirilmiş uygun değerlerle bir nesne oluşturarak bunları temsil edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-201">Instead, you can represent them by creating an `Automobile` object with the appropriate values passed to its class constructor, as the following example does.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/is-a.cs#2)]

<span data-ttu-id="dd7c9-202">Bir iş-devralma temelli bir ilişki taban sınıfına ve temel sınıfa ek Üyeler ekleyen ya da temel sınıfta ek işlevsellik gerektiren türetilmiş sınıflara en iyi şekilde uygulanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-202">An is-a relationship based on inheritance is best applied to a base class and to derived classes that add additional members to the base class or that require additional functionality not present in the base class.</span></span>

## <a name="designing-the-base-class-and-derived-classes"></a><span data-ttu-id="dd7c9-203">Temel sınıf ve türetilmiş sınıfları tasarlama</span><span class="sxs-lookup"><span data-stu-id="dd7c9-203">Designing the base class and derived classes</span></span>

<span data-ttu-id="dd7c9-204">Bir temel sınıf ve türetilmiş sınıfları tasarlama sürecine bakalım.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-204">Let's look at the process of designing a base class and its derived classes.</span></span> <span data-ttu-id="dd7c9-205">Bu bölümde, `Publication` bir kitap, dergi, gazete, bir günlük, makale vb. gibi her türlü yayını temsil eden bir temel sınıf tanımlayacaksınız. Ayrıca, `Book` öğesinden türetilen bir sınıf tanımlayacaksınız `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-205">In this section, you'll define a base class, `Publication`, which represents a publication of any kind, such as a book, a magazine, a newspaper, a journal, an article, etc. You'll also define a `Book` class that derives from `Publication`.</span></span> <span data-ttu-id="dd7c9-206">Örneğin,, ve gibi diğer türetilmiş sınıfları tanımlamak için örneği kolayca genişletebilirsiniz `Magazine` `Journal` `Newspaper` `Article` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-206">You could easily extend the example to define other derived classes, such as `Magazine`, `Journal`, `Newspaper`, and `Article`.</span></span>

### <a name="the-base-publication-class"></a><span data-ttu-id="dd7c9-207">Temel yayın sınıfı</span><span class="sxs-lookup"><span data-stu-id="dd7c9-207">The base Publication class</span></span>

<span data-ttu-id="dd7c9-208">`Publication`Sınıfınızı tasarlarken birkaç tasarım kararı vermeniz gerekir:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-208">In designing your `Publication` class, you need to make several design decisions:</span></span>

- <span data-ttu-id="dd7c9-209">Temel sınıfınıza hangi Üyeler dahil edileceğini `Publication` ve `Publication` üyelerin Yöntem uygulamaları sağlayıp sağlamadığını ya da `Publication` türetilmiş sınıfları için şablon görevi gören bir soyut temel sınıf olup olmadığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-209">What members to include in your base `Publication` class, and whether the `Publication` members provide method implementations or whether `Publication` is an abstract base class that serves as a template for its derived classes.</span></span>

  <span data-ttu-id="dd7c9-210">Bu durumda, `Publication` sınıfı Yöntem uygulamaları sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-210">In this case, the `Publication` class will provide method implementations.</span></span> <span data-ttu-id="dd7c9-211">[Soyut temel sınıfları ve bunların türetilmiş sınıflarını tasarlama](#abstract) bölümü, türetilen sınıfların geçersiz kılınması gereken yöntemleri tanımlamak için soyut bir temel sınıf kullanan bir örnek içerir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-211">The [Designing abstract base classes and their derived classes](#abstract) section contains an example that uses an abstract base class to define the methods that derived classes must override.</span></span> <span data-ttu-id="dd7c9-212">Türetilmiş sınıflar, türetilmiş tür için uygun olan herhangi bir uygulamayı sağlamak için ücretsizdir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-212">Derived classes are free to provide any implementation that is suitable for the derived type.</span></span>

  <span data-ttu-id="dd7c9-213">Kodu yeniden kullanma özelliği (yani, birden çok türetilmiş sınıflar temel sınıf yöntemlerinin bildirimini ve uygulamasını paylaşır ve bunları geçersiz kılmaları gerekmez) soyut olmayan taban sınıfların avantajlarından yararlanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-213">The ability to reuse code (that is, multiple derived classes share the declaration and implementation of base class methods and do not need to override them) is an advantage of non-abstract base classes.</span></span> <span data-ttu-id="dd7c9-214">Bu nedenle, `Publication` kendi kodunun bazı veya en özel türler tarafından paylaşılması olasılıklı Üyeler eklemeniz gerekir `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-214">Therefore, you should add members to `Publication` if their code is likely to be shared by some or most specialized `Publication` types.</span></span> <span data-ttu-id="dd7c9-215">Temel sınıf uygulamalarını verimli bir şekilde sağlayamadıysanız, temel sınıfta tek bir uygulama yerine türetilmiş sınıflarda büyük ölçüde özdeş üye uygulamaları sağlamak zorunda olacaksınız.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-215">If you fail to provide base class implementations efficiently, you'll end up having to provide largely identical member implementations in derived classes rather a single implementation in the base class.</span></span> <span data-ttu-id="dd7c9-216">Yinelenen kodu birden çok konumda sürdürme gereksinimi, olası hataların kaynağıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-216">The need to maintain duplicated code in multiple locations is a potential source of bugs.</span></span>

  <span data-ttu-id="dd7c9-217">Her ikisi de kod yeniden kullanımını en üst düzeye çıkarmak ve mantıksal ve sezgisel bir devralma hiyerarşisi oluşturmak için, `Publication` yalnızca tüm veya en çok yayımlarda ortak olan verileri ve işlevleri sınıfa dahil ettiğinizden emin olmak istersiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-217">Both to maximize code reuse and to create a logical and intuitive inheritance hierarchy, you want to be sure that you include in the `Publication` class only the data and functionality that is common to all or to most publications.</span></span> <span data-ttu-id="dd7c9-218">Türetilmiş sınıflar daha sonra temsil ettikleri belirli yayın türlerine özgü olan üyeleri uygular.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-218">Derived classes then implement members that are unique to the particular kinds of publication that they represent.</span></span>

- <span data-ttu-id="dd7c9-219">Sınıf hiyerarşinizin ne kadar uzaleceği.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-219">How far to extend your class hierarchy.</span></span> <span data-ttu-id="dd7c9-220">Yalnızca bir temel sınıf ve bir ya da daha fazla türetilmiş sınıf yerine üç veya daha fazla sınıf hiyerarşisi geliştirmek istiyor musunuz?</span><span class="sxs-lookup"><span data-stu-id="dd7c9-220">Do you want to develop a hierarchy of three or more classes, rather than simply a base class and one or more derived classes?</span></span> <span data-ttu-id="dd7c9-221">Örneğin, bir `Publication` taban sınıfı olabilir `Periodical` , bu, ' ın temel sınıfı `Magazine` ve ' dir `Journal` `Newspaper` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-221">For example, `Publication` could be a base class of `Periodical`, which in turn is a base class of `Magazine`, `Journal` and `Newspaper`.</span></span>

  <span data-ttu-id="dd7c9-222">Örneğinizde, bir sınıfın küçük hiyerarşisini `Publication` ve tek bir türetilmiş sınıf olan ' i kullanacaksınız `Book` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-222">For your example, you'll use the small hierarchy of a `Publication` class and a single derived class, `Book`.</span></span> <span data-ttu-id="dd7c9-223">Bu örneği, ve gibi öğesinden türeten bir dizi ek sınıf oluşturmak için kolayca genişletebilirsiniz `Publication` `Magazine` `Article` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-223">You could easily extend the example to create a number of additional classes that derive from `Publication`, such as `Magazine` and `Article`.</span></span>

- <span data-ttu-id="dd7c9-224">Temel sınıfın örneğini oluşturma konusunda anlamlı olup olmadığı.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-224">Whether it makes sense to instantiate the base class.</span></span> <span data-ttu-id="dd7c9-225">Değilse, sınıfa [soyut](../language-reference/keywords/abstract.md) anahtar sözcüğünü uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-225">If it does not, you should apply the [abstract](../language-reference/keywords/abstract.md) keyword to the class.</span></span> <span data-ttu-id="dd7c9-226">Aksi halde, sınıfınızın `Publication` sınıf oluşturucusu çağırarak örneklenebilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-226">Otherwise, your `Publication` class can be instantiated by calling its class constructor.</span></span> <span data-ttu-id="dd7c9-227">Sınıf oluşturucusuna doğrudan çağrı tarafından anahtar sözcükle işaretlenmiş bir sınıf örneği oluşturmak için bir girişimde bulunuldu, `abstract` C# derleyicisi hata CS0144 oluşturuyor, "soyut sınıfın veya arabirimin bir örneği oluşturulamaz."</span><span class="sxs-lookup"><span data-stu-id="dd7c9-227">If an attempt is made to instantiate a class marked with the `abstract` keyword by a direct call to its class constructor, the C# compiler generates error CS0144, "Cannot create an instance of the abstract class or interface."</span></span> <span data-ttu-id="dd7c9-228">Yansıma kullanarak sınıfın örneğini oluşturmaya yönelik bir girişim yapılırsa, yansıma yöntemi bir oluşturur <xref:System.MemberAccessException> .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-228">If an attempt is made to instantiate the class by using reflection, the reflection method throws a <xref:System.MemberAccessException>.</span></span>

  <span data-ttu-id="dd7c9-229">Varsayılan olarak, bir temel sınıf, sınıf oluşturucusu çağırarak örneklenebilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-229">By default, a base class can be instantiated by calling its class constructor.</span></span> <span data-ttu-id="dd7c9-230">Açıkça bir sınıf oluşturucusu tanımlamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-230">You do not have to explicitly define a class constructor.</span></span> <span data-ttu-id="dd7c9-231">Temel sınıfın kaynak kodunda bir tane yoksa, C# derleyicisi otomatik olarak varsayılan (parametresiz) bir oluşturucu sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-231">If one is not present in the base class' source code, the C# compiler automatically provides a default (parameterless) constructor.</span></span>

  <span data-ttu-id="dd7c9-232">Örneğinizdeki `Publication` sınıfı, örneklenemez hale gelecek şekilde bir [Özet](../language-reference/keywords/abstract.md) olarak işaretlersiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-232">For your example, you'll mark the `Publication` class as [abstract](../language-reference/keywords/abstract.md) so that it cannot be instantiated.</span></span>  <span data-ttu-id="dd7c9-233">`abstract`Herhangi bir yöntemi olmayan bir sınıf `abstract` , bu sınıfın birkaç somut sınıf (örneğin,) arasında paylaşılan bir soyut kavramı temsil ettiğini `Book` belirtir `Journal` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-233">An `abstract` class without any `abstract` methods indicates that this class represents an abstract concept that is shared among several concrete classes (like a `Book`, `Journal`).</span></span>

- <span data-ttu-id="dd7c9-234">Türetilmiş sınıfların belirli üyelerin temel sınıf uygulamasını devralması gerekip gerekmediğini, temel sınıf uygulamasını geçersiz kılma seçeneğine sahip olup olmadığı veya bir uygulama sağlayıp sağlamamaları gerekir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-234">Whether derived classes must inherit the base class implementation of particular members, whether they have the option to override the base class implementation, or whether they must provide an implementation.</span></span> <span data-ttu-id="dd7c9-235">Türetilmiş sınıfların bir uygulama sağlamasına zorlamak için [soyut](../language-reference/keywords/abstract.md) anahtar sözcüğünü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-235">You use the [abstract](../language-reference/keywords/abstract.md) keyword to force derived classes to provide an implementation.</span></span> <span data-ttu-id="dd7c9-236">Türetilmiş sınıfların bir temel sınıf yöntemini geçersiz kılmasına izin vermek için [sanal](../language-reference/keywords/virtual.md) anahtar sözcüğünü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-236">You use the [virtual](../language-reference/keywords/virtual.md) keyword to allow derived classes to override a base class method.</span></span> <span data-ttu-id="dd7c9-237">Varsayılan olarak, temel sınıfta tanımlanan Yöntemler geçersiz kılınabilir *değildir* .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-237">By default, methods defined in the base class are *not* overridable.</span></span>

  <span data-ttu-id="dd7c9-238">`Publication`Sınıfın herhangi bir `abstract` yöntemi yoktur, ancak sınıfın kendisi olur `abstract` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-238">The `Publication` class does not have any `abstract` methods, but the class itself is `abstract`.</span></span>

- <span data-ttu-id="dd7c9-239">Türetilmiş bir sınıfın devralma hiyerarşisinde son sınıfı temsil edip etmediği ve kendisi ek türetilmiş sınıflar için temel sınıf olarak kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-239">Whether a derived class represents the final class in the inheritance hierarchy and cannot itself be used as a base class for additional derived classes.</span></span> <span data-ttu-id="dd7c9-240">Varsayılan olarak, herhangi bir sınıf temel sınıf olarak görev yapabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-240">By default, any class can serve as a base class.</span></span> <span data-ttu-id="dd7c9-241">Bir sınıfın herhangi bir ek sınıf için temel sınıf olarak işlev veremeyeceğini belirtmek için [Sealed](../language-reference/keywords/sealed.md) anahtar sözcüğünü uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-241">You can apply the [sealed](../language-reference/keywords/sealed.md) keyword to indicate that a class cannot serve as a base class for any additional classes.</span></span> <span data-ttu-id="dd7c9-242">Sealed sınıfından türetmeye çalışılıyor CS0509, "mühürlü türden türetilemez \<typeName> ".</span><span class="sxs-lookup"><span data-stu-id="dd7c9-242">Attempting to derive from a sealed class generated compiler error CS0509, "cannot derive from sealed type \<typeName>".</span></span>

  <span data-ttu-id="dd7c9-243">Örneğinizdeki türetilmiş sınıfınızı olarak işaretlersiniz `sealed` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-243">For your example, you'll mark your derived class as `sealed`.</span></span>

<span data-ttu-id="dd7c9-244">Aşağıdaki örnek, sınıfının kaynak kodunu ve `Publication` `PublicationType` özelliği tarafından döndürülen bir numaralandırmayı gösterir `Publication.PublicationType` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-244">The following example shows the source code for the `Publication` class, as well as a `PublicationType` enumeration that is returned by the `Publication.PublicationType` property.</span></span> <span data-ttu-id="dd7c9-245">Öğesinden devraldığı üyelere ek olarak <xref:System.Object> , `Publication` sınıfı aşağıdaki benzersiz üyeleri ve üye geçersiz kılmalarını tanımlar:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-245">In addition to the members that it inherits from <xref:System.Object>, the `Publication` class defines the following unique members and member overrides:</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/base-and-derived.cs#1)]

- <span data-ttu-id="dd7c9-246">Bir Oluşturucu</span><span class="sxs-lookup"><span data-stu-id="dd7c9-246">A constructor</span></span>

  <span data-ttu-id="dd7c9-247">Sınıfı olduğundan `Publication` `abstract` , doğrudan aşağıdaki örnek gibi koddan başlatılamaz:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-247">Because the `Publication` class is `abstract`, it cannot be instantiated directly from code like the following example:</span></span>

  ```csharp
  var publication = new Publication("Tiddlywinks for Experts", "Fun and Games",
                                    PublicationType.Book);
  ```

  <span data-ttu-id="dd7c9-248">Ancak, sınıfının kaynak kodu gösterdiği gibi, örnek Oluşturucusu doğrudan türetilmiş sınıf oluşturucularından çağrılabilir `Book` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-248">However, its instance constructor can be called directly from derived class constructors, as the source code for the `Book` class shows.</span></span>

- <span data-ttu-id="dd7c9-249">İki yayınla ilgili özellikler</span><span class="sxs-lookup"><span data-stu-id="dd7c9-249">Two publication-related properties</span></span>

  <span data-ttu-id="dd7c9-250">`Title`, <xref:System.String> oluşturucuyu çağırarak değeri sağlanan salt okunurdur `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-250">`Title` is a read-only <xref:System.String> property whose value is supplied by calling the `Publication` constructor.</span></span>

  <span data-ttu-id="dd7c9-251">`Pages`, <xref:System.Int32> yayının kaç tane sayfa olduğunu gösteren bir okuma-yazma özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-251">`Pages` is a read-write <xref:System.Int32> property that indicates how many total pages the publication has.</span></span> <span data-ttu-id="dd7c9-252">Değer adlı bir özel alanda depolanır `totalPages` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-252">The value is stored in a private field named `totalPages`.</span></span> <span data-ttu-id="dd7c9-253">Pozitif bir sayı olmalı veya bir değer oluşturulmalıdır <xref:System.ArgumentOutOfRangeException> .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-253">It must be a positive number or an <xref:System.ArgumentOutOfRangeException> is thrown.</span></span>

- <span data-ttu-id="dd7c9-254">Yayımcının ilgili üyeleri</span><span class="sxs-lookup"><span data-stu-id="dd7c9-254">Publisher-related members</span></span>

  <span data-ttu-id="dd7c9-255">İki salt okuma özelliği `Publisher` ve `Type` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-255">Two read-only properties, `Publisher` and `Type`.</span></span> <span data-ttu-id="dd7c9-256">Değerler, başlangıçta sınıf oluşturucusuna yapılan çağrı tarafından sağlanır `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-256">The values are originally supplied by the call to the `Publication` class constructor.</span></span>

- <span data-ttu-id="dd7c9-257">Yayınlamayla ilgili Üyeler</span><span class="sxs-lookup"><span data-stu-id="dd7c9-257">Publishing-related members</span></span>

  <span data-ttu-id="dd7c9-258">İki yöntem `Publish` ve `GetPublicationDate` , yayınlama tarihini ayarlayın ve döndürür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-258">Two methods, `Publish` and `GetPublicationDate`, set and return the publication date.</span></span> <span data-ttu-id="dd7c9-259">`Publish`Yöntemi, `published` çağrıldığında için bir özel bayrak ayarlar `true` ve kendisine geçirilen tarihi özel alana bağımsız değişken olarak atar `datePublished` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-259">The `Publish` method sets a private  `published` flag to `true` when it is called and assigns the date passed to it as an argument to the private `datePublished` field.</span></span> <span data-ttu-id="dd7c9-260">`GetPublicationDate`Yöntemi, bayrak ise "NYP" dizesini `published` `false` ve `datePublished` varsa alanın değerini döndürür `true` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-260">The `GetPublicationDate` method returns the string "NYP" if the `published` flag is `false`, and the value of the `datePublished` field if it is `true`.</span></span>

- <span data-ttu-id="dd7c9-261">Telif hakkı ile ilgili Üyeler</span><span class="sxs-lookup"><span data-stu-id="dd7c9-261">Copyright-related members</span></span>

  <span data-ttu-id="dd7c9-262">`Copyright`Yöntemi, telif hakkı sahibinin adını ve telif hakkı yılını bağımsız değişken olarak alır ve bunları `CopyrightName` ve özelliklerine atar `CopyrightDate` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-262">The `Copyright` method takes the name of the copyright holder and the year of the copyright as arguments and assigns them to the `CopyrightName` and `CopyrightDate` properties.</span></span>

- <span data-ttu-id="dd7c9-263">Yöntemi geçersiz kılma `ToString`</span><span class="sxs-lookup"><span data-stu-id="dd7c9-263">An override of the `ToString` method</span></span>

  <span data-ttu-id="dd7c9-264">Bir tür yöntemi geçersiz kılmadığından, bir <xref:System.Object.ToString%2A?displayProperty=nameWithType> örneği diğerinden Farklılaştırmada çok az kullanılan, türün tam nitelikli adını döndürür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-264">If a type does not override the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method, it returns the fully qualified name of the type, which is of little use in differentiating one instance from another.</span></span> <span data-ttu-id="dd7c9-265">`Publication`Sınıfı, <xref:System.Object.ToString%2A?displayProperty=nameWithType> özelliğinin değerini döndürecek şekilde geçersiz kılar `Title` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-265">The `Publication` class overrides <xref:System.Object.ToString%2A?displayProperty=nameWithType> to return the value of the `Title` property.</span></span>

<span data-ttu-id="dd7c9-266">Aşağıdaki şekilde, taban `Publication` sınıfınız ve örtülü olarak devralınan sınıfı arasındaki ilişki gösterilmektedir <xref:System.Object> .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-266">The following figure illustrates the relationship between your base `Publication` class and its implicitly inherited <xref:System.Object> class.</span></span>

![Nesne ve yayın sınıfları](media/publication-class.jpg)

### <a name="the-book-class"></a><span data-ttu-id="dd7c9-268">`Book`Sınıfı</span><span class="sxs-lookup"><span data-stu-id="dd7c9-268">The `Book` class</span></span>

<span data-ttu-id="dd7c9-269">`Book`Sınıfı, bir kitabı özelleşmiş bir yayın türü olarak temsil eder.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-269">The `Book` class represents a book as a specialized type of publication.</span></span> <span data-ttu-id="dd7c9-270">Aşağıdaki örnek, sınıfının kaynak kodunu gösterir `Book` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-270">The following example shows the source code for the `Book` class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/base-and-derived.cs#2)]

<span data-ttu-id="dd7c9-271">Öğesinden devraldığı üyelere ek olarak `Publication` , `Book` sınıfı aşağıdaki benzersiz üyeleri ve üye geçersiz kılmalarını tanımlar:</span><span class="sxs-lookup"><span data-stu-id="dd7c9-271">In addition to the members that it inherits from `Publication`, the `Book` class defines the following unique members and member overrides:</span></span>

- <span data-ttu-id="dd7c9-272">İki Oluşturucu</span><span class="sxs-lookup"><span data-stu-id="dd7c9-272">Two constructors</span></span>

  <span data-ttu-id="dd7c9-273">İki `Book` Oluşturucu üç ortak parametreyi paylaşır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-273">The two `Book` constructors share three common parameters.</span></span> <span data-ttu-id="dd7c9-274">İki, *başlık* ve *Yayımcı*, oluşturucunun parametrelerine karşılık gelir `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-274">Two, *title* and *publisher*, correspond to parameters of the `Publication` constructor.</span></span> <span data-ttu-id="dd7c9-275">Üçüncü, genel bir sabit özelliğe depolanan *Yazar* `Author` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-275">The third is *author*, which is stored to a public immutable `Author` property.</span></span> <span data-ttu-id="dd7c9-276">Bir Oluşturucu, Auto-Property içinde depolanan bir *ISBN* parametresi içerir `ISBN` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-276">One constructor includes an *isbn* parameter, which is stored in the `ISBN` auto-property.</span></span>

  <span data-ttu-id="dd7c9-277">İlk Oluşturucu, diğer oluşturucuyu çağırmak için [this](../language-reference/keywords/this.md) anahtar sözcüğünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-277">The first constructor uses the [this](../language-reference/keywords/this.md) keyword to call the other constructor.</span></span> <span data-ttu-id="dd7c9-278">Oluşturucu zincirleme, Oluşturucu tanımlamalarında ortak bir modeldir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-278">Constructor chaining is a common pattern in defining constructors.</span></span> <span data-ttu-id="dd7c9-279">Daha az parametreye sahip oluşturucular, oluşturucuyu en fazla parametre sayısıyla çağırırken varsayılan değerleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-279">Constructors with fewer parameters provide default values when calling the constructor with the greatest number of parameters.</span></span>

  <span data-ttu-id="dd7c9-280">İkinci Oluşturucu, başlığı ve yayımcı adını temel sınıf oluşturucusuna geçirmek için [temel](../language-reference/keywords/base.md) anahtar sözcüğünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-280">The second constructor uses the [base](../language-reference/keywords/base.md) keyword to pass the title and publisher name to the base class constructor.</span></span> <span data-ttu-id="dd7c9-281">Kaynak kodunuzda bir taban sınıf oluşturucusuna açık bir çağrı yapmazsanız, C# derleyicisi otomatik olarak taban sınıfının varsayılan veya parametresiz oluşturucusuna bir çağrı sağlar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-281">If you don't make an explicit call to a base class constructor in your source code, the C# compiler automatically supplies a call to the base class' default or parameterless constructor.</span></span>

- <span data-ttu-id="dd7c9-282">`ISBN` `Book` Nesnenin uluslararası standart defter numarasını, 10 veya 13 basamaklı bir sayıyı döndüren salt okunurdur bir özellik.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-282">A read-only `ISBN` property, which returns the `Book` object's International Standard Book Number, a unique 10- or 13-digit number.</span></span> <span data-ttu-id="dd7c9-283">ISBN oluşturuculardan birine bir bağımsız değişken olarak sağlanır `Book` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-283">The ISBN is supplied as an argument to one of the `Book` constructors.</span></span> <span data-ttu-id="dd7c9-284">ISBN, derleyici tarafından otomatik olarak oluşturulan özel bir yedekleme alanında depolanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-284">The ISBN is stored in a private backing field, which is auto-generated by the compiler.</span></span>

- <span data-ttu-id="dd7c9-285">Salt okunurdur `Author` özelliği.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-285">A read-only `Author` property.</span></span> <span data-ttu-id="dd7c9-286">Yazar adı her iki Oluşturucu için bir bağımsız değişken olarak sağlanır `Book` ve özellikte depolanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-286">The author name is supplied as an argument to both `Book` constructors and is stored in the property.</span></span>

- <span data-ttu-id="dd7c9-287">İki salt okuma fiyatına ilişkin özellikler `Price` ve `Currency` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-287">Two read-only price-related properties, `Price` and `Currency`.</span></span> <span data-ttu-id="dd7c9-288">Değerleri bir yöntem çağrısında bağımsız değişken olarak sağlanır `SetPrice` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-288">Their values are provided as arguments in a `SetPrice` method call.</span></span> <span data-ttu-id="dd7c9-289">`Currency`Özelliği üç BASAMAKLı ISO para birimi simgedir (örneğin, ABD Doları IÇIN USD).</span><span class="sxs-lookup"><span data-stu-id="dd7c9-289">The `Currency` property is the three-digit ISO currency symbol (for example, USD for the U.S. dollar).</span></span> <span data-ttu-id="dd7c9-290">ISO para birimi sembolleri <xref:System.Globalization.RegionInfo.ISOCurrencySymbol%2A> özelliğinden alınabilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-290">ISO currency symbols can be retrieved from the <xref:System.Globalization.RegionInfo.ISOCurrencySymbol%2A> property.</span></span> <span data-ttu-id="dd7c9-291">Bu özelliklerin her ikisi de dışarıdan salt okunurdur, ancak her ikisi de sınıftaki kodla ayarlanabilir `Book` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-291">Both of these properties are externally read-only, but both can be set by code in the `Book` class.</span></span>

- <span data-ttu-id="dd7c9-292">`SetPrice`Ve özelliklerinin değerlerini ayarlayan bir yöntemi `Price` `Currency` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-292">A `SetPrice` method, which sets the values of the `Price` and `Currency` properties.</span></span> <span data-ttu-id="dd7c9-293">Bu değerler, aynı özelliklerle döndürülür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-293">Those values are returned by those same properties.</span></span>

- <span data-ttu-id="dd7c9-294">`ToString`Yöntemi (Devralındığı yer `Publication` ) ve <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> ve <xref:System.Object.GetHashCode%2A> yöntemlerini (öğesinden devralınan <xref:System.Object> ) geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-294">Overrides to the `ToString` method (inherited from `Publication`) and the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> and <xref:System.Object.GetHashCode%2A> methods (inherited from <xref:System.Object>).</span></span>

  <span data-ttu-id="dd7c9-295">Geçersiz kılınmadıkça, <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> yöntemi başvuru eşitliği için test eder.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-295">Unless it is overridden, the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method tests for reference equality.</span></span> <span data-ttu-id="dd7c9-296">Diğer bir deyişle, aynı nesneye başvurduklarında iki nesne değişkeninin eşit olduğu kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-296">That is, two object variables are considered to be equal if they refer to the same object.</span></span> <span data-ttu-id="dd7c9-297">`Book`Sınıfında, diğer yandan, `Book` aynı ISBN 'e sahip olmaları durumunda iki nesne eşit olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-297">In the `Book` class, on the other hand, two `Book` objects should be equal if they have the same ISBN.</span></span>

  <span data-ttu-id="dd7c9-298">Yöntemi geçersiz kıldığınızda, <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> <xref:System.Object.GetHashCode%2A> verimli alma için, çalışma zamanının öğeleri karma koleksiyonlara depolamak üzere kullandığı bir değer döndüren yöntemini de geçersiz kılmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-298">When you override the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method, you must also override the <xref:System.Object.GetHashCode%2A> method, which returns a value that the runtime uses to store items in hashed collections for efficient retrieval.</span></span> <span data-ttu-id="dd7c9-299">Karma kodu, eşitlik için testle tutarlı bir değer döndürmelidir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-299">The hash code should return a value that's consistent with the test for equality.</span></span> <span data-ttu-id="dd7c9-300"><xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> `true` Ikı nesnenin ISBN özellikleri eşitse döndürülecek şekilde geçersiz kılındığından `Book` , <xref:System.String.GetHashCode%2A> özelliği tarafından döndürülen dize yöntemini çağırarak hesaplanan karma kodu döndürün `ISBN` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-300">Since you've overridden <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> to return `true` if the ISBN properties of two `Book` objects are equal, you return the hash code computed by calling the <xref:System.String.GetHashCode%2A> method of the string returned by the `ISBN` property.</span></span>

<span data-ttu-id="dd7c9-301">Aşağıdaki şekilde, `Book` sınıfı ve temel sınıfı arasındaki ilişki gösterilmektedir `Publication` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-301">The following figure illustrates the relationship between the `Book` class and `Publication`, its base class.</span></span>

![Yayın ve kitap sınıfları](media/book-class.jpg)

<span data-ttu-id="dd7c9-303">Artık bir nesnesi örneği oluşturabilir `Book` , hem benzersiz hem de devralınan üyelerini çağırabilir ve `Publication` `Book` Aşağıdaki örnekte gösterildiği gibi, türü veya türü bir parametreyi bekleyen bir yönteme bağımsız değişken olarak geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-303">You can now instantiate a `Book` object, invoke both its unique and inherited members, and pass it as an argument to a method that expects a parameter of type `Publication` or of type `Book`, as the following example shows.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/use-publication.cs#1)]

## <a name="designing-abstract-base-classes-and-their-derived-classes"></a><span data-ttu-id="dd7c9-304">Soyut temel sınıfları ve bunların türetilmiş sınıflarını tasarlama</span><span class="sxs-lookup"><span data-stu-id="dd7c9-304">Designing abstract base classes and their derived classes</span></span>
<a name="abstract"></a>

<span data-ttu-id="dd7c9-305">Önceki örnekte, türetilmiş sınıfların kod paylaşmasına izin veren bir dizi yöntem için uygulama sağlayan bir temel sınıf tanımladınız.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-305">In the previous example, you defined a base class that provided an implementation for a number of methods to allow derived classes to share code.</span></span> <span data-ttu-id="dd7c9-306">Ancak çoğu durumda, temel sınıfın bir uygulama sağlaması beklenmez.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-306">In many cases, however, the base class is not expected to provide an implementation.</span></span> <span data-ttu-id="dd7c9-307">Bunun yerine, temel sınıf *soyut yöntemleri*bildiren *soyut bir sınıftır* ; Her türetilmiş sınıfın uygulaması gereken üyeleri tanımlayan bir şablon görevi görür.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-307">Instead, the base class is an *abstract class* that declares *abstract methods*; it serves as a template that defines the members that each derived class must implement.</span></span> <span data-ttu-id="dd7c9-308">Genellikle soyut bir temel sınıfta, her türetilmiş türün uygulanması o tür için benzersizdir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-308">Typically in an abstract base class, the implementation of each derived type is unique to that type.</span></span> <span data-ttu-id="dd7c9-309">Sınıf, `Publication` yayınlar için ortak işlevsellik uygulamaları sağlasa da, bir nesnenin örneğini oluşturma konusunda hiçbir fikir olmadığından, bu sınıfı soyut anahtar sözcükle işaretlenir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-309">You marked the class with the abstract keyword because it made no sense to instantiate a `Publication` object, although the class did provide implementations of functionality common to publications.</span></span>

<span data-ttu-id="dd7c9-310">Örneğin, her kapatılan iki boyutlu geometrik şekil iki özellik içerir: alan, şeklin iç kapsamı; ve çevre ya da şeklin kenarları arasındaki uzaklık.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-310">For example, each closed two-dimensional geometric shape includes two properties: area, the inner extent of the shape; and perimeter, or the distance along the edges of the shape.</span></span> <span data-ttu-id="dd7c9-311">Ancak, bu özelliklerin hesaplanma şekli, ancak tamamen belirli bir şekle bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-311">The way in which these properties are calculated, however, depends completely on the specific shape.</span></span> <span data-ttu-id="dd7c9-312">Örneğin, bir dairenin çevre (veya çevresi) hesaplama formülü, bir üçgenden farklıdır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-312">The formula for calculating the perimeter (or circumference) of a circle, for example, is different from that of a triangle.</span></span> <span data-ttu-id="dd7c9-313">`Shape`Sınıfı `abstract` Yöntemler içeren bir sınıftır `abstract` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-313">The `Shape` class is an `abstract` class with `abstract` methods.</span></span> <span data-ttu-id="dd7c9-314">Bu, türetilmiş sınıfların aynı işlevselliği paylaştığından, ancak bu türetilmiş sınıfların bu işlevselliği farklı şekilde uyguladığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-314">That indicates derived classes share the same functionality, but those derived classes implement that functionality differently.</span></span>

<span data-ttu-id="dd7c9-315">Aşağıdaki örnek, adlı bir soyut temel sınıfı tanımlar `Shape` ve iki özelliği tanımlar: `Area` ve `Perimeter` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-315">The following example defines an abstract base class named `Shape` that defines two properties: `Area` and `Perimeter`.</span></span> <span data-ttu-id="dd7c9-316">Sınıfı [soyut](../language-reference/keywords/abstract.md) anahtar sözcükle işaretlemeye ek olarak, her örnek üyesi de [soyut](../language-reference/keywords/abstract.md) anahtar sözcüğüyle işaretlenir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-316">In addition to marking the class with the [abstract](../language-reference/keywords/abstract.md) keyword, each instance member is also marked with the [abstract](../language-reference/keywords/abstract.md) keyword.</span></span> <span data-ttu-id="dd7c9-317">Bu durumda, `Shape` <xref:System.Object.ToString%2A?displayProperty=nameWithType> tam adı yerine türün adını döndürmek için yöntemini de geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-317">In this case, `Shape` also overrides the <xref:System.Object.ToString%2A?displayProperty=nameWithType> method to return the name of the type, rather than its fully qualified name.</span></span> <span data-ttu-id="dd7c9-318">Ve bu, iki statik üye tanımlar `GetArea` ve `GetPerimeter` Bu, çağıranların, türetilmiş bir sınıfın bir örneğinin alanını ve çevresini kolayca almasına imkan tanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-318">And it defines two static members, `GetArea` and `GetPerimeter`, that allow callers to easily retrieve the area and perimeter of an instance of any derived class.</span></span> <span data-ttu-id="dd7c9-319">Türetilmiş bir sınıfın bir örneğini bu yöntemlerin herhangi birine geçirdiğinizde, çalışma zamanı türetilmiş sınıfın yöntem geçersiz kılmasını çağırır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-319">When you pass an instance of a derived class to either of these methods, the runtime calls the method override of the derived class.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#1)]

<span data-ttu-id="dd7c9-320">Daha sonra `Shape` belirli şekilleri temsil eden bazı sınıfları türetebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-320">You can then derive some classes from `Shape` that represent specific shapes.</span></span> <span data-ttu-id="dd7c9-321">Aşağıdaki örnek üç sınıfı tanımlar,, `Triangle` `Rectangle` ve `Circle` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-321">The following example defines three classes, `Triangle`, `Rectangle`, and `Circle`.</span></span> <span data-ttu-id="dd7c9-322">Her biri, alanı ve çevresi hesaplamak için bu belirli bir şekil için benzersiz bir formül kullanır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-322">Each uses a formula unique for that particular shape to compute the area and perimeter.</span></span> <span data-ttu-id="dd7c9-323">Türetilmiş sınıflardan bazıları Ayrıca, ve gibi özellikleri tanımlar `Rectangle.Diagonal` ve `Circle.Diameter` temsil ettikleri şekle özeldir.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-323">Some of the derived classes also define properties, such as `Rectangle.Diagonal` and `Circle.Diameter`, that are unique to the shape that they represent.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#2)]

<span data-ttu-id="dd7c9-324">Aşağıdaki örnek, öğesinden türetilmiş nesneleri kullanır `Shape` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-324">The following example uses objects derived from `Shape`.</span></span> <span data-ttu-id="dd7c9-325">Sınıfından türetilmiş bir nesne dizisi oluşturur `Shape` ve `Shape` döndürülen özellik değerlerini sarmalayan sınıfın statik yöntemlerini çağırır `Shape` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-325">It instantiates an array of objects derived from `Shape` and calls the static methods of the `Shape` class, which wraps return `Shape` property values.</span></span> <span data-ttu-id="dd7c9-326">Çalışma zamanı türetilmiş türlerin geçersiz kılınan özelliklerinden değerleri alır.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-326">The runtime retrieves values from the overridden properties of the derived types.</span></span> <span data-ttu-id="dd7c9-327">Örnek ayrıca dizideki her bir `Shape` nesneyi türetilmiş türüne yayınlar ve atama başarılı olursa, belirli bir alt sınıfının özelliklerini alır `Shape` .</span><span class="sxs-lookup"><span data-stu-id="dd7c9-327">The example also casts each `Shape` object in the array to its derived type and, if the cast succeeds, retrieves properties of that particular subclass of `Shape`.</span></span>

[!code-csharp[Inheritance](../../../samples/snippets/csharp/tutorials/inheritance/shape.cs#3)]

## <a name="see-also"></a><span data-ttu-id="dd7c9-328">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="dd7c9-328">See also</span></span>

- [<span data-ttu-id="dd7c9-329">Devralma (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="dd7c9-329">Inheritance (C# Programming Guide)</span></span>](../programming-guide/classes-and-structs/inheritance.md)
