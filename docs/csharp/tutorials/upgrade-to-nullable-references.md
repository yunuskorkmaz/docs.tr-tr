---
title: Null yapılabilir başvuru türleriyle tasarım
description: Bu gelişmiş öğretici, null yapılabilir başvuru türlerine giriş sağlar. Başvuru değerleri null olduğunda ve derleyicinin null olmadıklarında zorunlu olmadığı durumlarda tasarım amacınızı ifade etmek için bilgi edineceksiniz.
ms.date: 02/19/2019
ms.technology: csharp-null-safety
ms.custom: mvc
ms.openlocfilehash: 9cb9ac1b292e61d6a8a5f84be29a6a6c323725fc
ms.sourcegitcommit: ad800f019ac976cb669e635fb0ea49db740e6890
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/29/2019
ms.locfileid: "73039679"
---
# <a name="tutorial-migrate-existing-code-with-nullable-reference-types"></a><span data-ttu-id="a0aaa-104">Öğretici: mevcut kodu Nullable başvuru türleriyle geçirme</span><span class="sxs-lookup"><span data-stu-id="a0aaa-104">Tutorial: Migrate existing code with nullable reference types</span></span>

<span data-ttu-id="a0aaa-105">C#8, null olabilen değer türleri için aynı şekilde, başvuru türlerini tamamlayan **null yapılabilir başvuru türlerini**tanıtır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-105">C# 8 introduces **nullable reference types**, which complement reference types the same way nullable value types complement value types.</span></span> <span data-ttu-id="a0aaa-106">Türe bir `?` ekleyerek **null olabilen bir başvuru türü** olarak bir değişken bildirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-106">You declare a variable to be a **nullable reference type** by appending a `?` to the type.</span></span> <span data-ttu-id="a0aaa-107">Örneğin `string?`, Nullable `string` temsil eder.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-107">For example, `string?` represents a nullable `string`.</span></span> <span data-ttu-id="a0aaa-108">Tasarım amacınızı daha net bir şekilde ifade etmek için bu yeni türleri kullanabilirsiniz: bazı değişkenlerin *her zaman bir değeri olması gerekir*, bazılarında *bir değer eksik*olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-108">You can use these new types to more clearly express your design intent: some variables *must always have a value*, others *may be missing a value*.</span></span> <span data-ttu-id="a0aaa-109">Bir başvuru türünün varolan değişkenleri, null olamayan bir başvuru türü olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-109">Any existing variables of a reference type would be interpreted as a non-nullable reference type.</span></span> 

<span data-ttu-id="a0aaa-110">Bu öğreticide, aşağıdakileri nasıl yapacağınızı öğreneceksiniz:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-110">In this tutorial, you'll learn how to:</span></span>

> [!div class="checklist"]
>
> - <span data-ttu-id="a0aaa-111">Kodla çalışırken null başvuru denetimlerini etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-111">Enable null reference checks as you work with code.</span></span>
> - <span data-ttu-id="a0aaa-112">Null değerlerle ilgili farklı uyarıları tanılayın ve düzeltin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-112">Diagnose and correct different warnings related to null values.</span></span>
> - <span data-ttu-id="a0aaa-113">Null yapılabilir etkin ve null yapılabilir devre dışı bağlamların arabirimini yönetin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-113">Manage the interface between nullable enabled and nullable disabled contexts.</span></span>
> - <span data-ttu-id="a0aaa-114">Null yapılabilir ek açıklama bağlamlarını denetleyin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-114">Control nullable annotation contexts.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="a0aaa-115">Prerequisites</span><span class="sxs-lookup"><span data-stu-id="a0aaa-115">Prerequisites</span></span>

<span data-ttu-id="a0aaa-116">Makinenizi, C# 8,0 derleyicisi dahil .NET Core çalıştıracak şekilde ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-116">You’ll need to set up your machine to run .NET Core, including the C# 8.0 compiler.</span></span> <span data-ttu-id="a0aaa-117">8 C# derleyicisi, [Visual Studio 2019 sürüm 16,3](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=inline+link&utm_content=download+vs2019) veya [.NET Core 3,0 SDK](https://dotnet.microsoft.com/download)ile başlayarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-117">The C# 8 compiler is available starting with [Visual Studio 2019 version 16.3](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=inline+link&utm_content=download+vs2019) or [.NET Core 3.0 SDK](https://dotnet.microsoft.com/download).</span></span>

<span data-ttu-id="a0aaa-118">Bu öğreticide, Visual Studio veya C# .NET Core CLI dahil olmak üzere, .net hakkında bilgi sahibi olduğunuz varsayılır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-118">This tutorial assumes you're familiar with C# and .NET, including either Visual Studio or the .NET Core CLI.</span></span>

## <a name="explore-the-sample-application"></a><span data-ttu-id="a0aaa-119">Örnek uygulamayı keşfet</span><span class="sxs-lookup"><span data-stu-id="a0aaa-119">Explore the sample application</span></span>

<span data-ttu-id="a0aaa-120">Geçirilecek örnek uygulama bir RSS Akış okuyucusu web uygulamasıdır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-120">The sample application that you'll migrate is an RSS feed reader web app.</span></span> <span data-ttu-id="a0aaa-121">Tek bir RSS akışından okur ve en son makalelerin özetlerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-121">It reads from a single RSS feed and displays summaries for the most recent articles.</span></span> <span data-ttu-id="a0aaa-122">Siteyi ziyaret etmek için makalelerden birine tıklayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-122">You can click on any of the articles to visit the site.</span></span> <span data-ttu-id="a0aaa-123">Uygulama nispeten yenidir ancak null yapılabilir başvuru türleri kullanılabilir olmadan önce yazılmıştır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-123">The application is relatively new but was written before nullable reference types were available.</span></span> <span data-ttu-id="a0aaa-124">Uygulama için gösterilen ve bu önemli dil özelliğinden yararlanmamak için sunulan tasarım kararları.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-124">The design decisions for the application represented sound principles, but don't take advantage of this important language feature.</span></span>

<span data-ttu-id="a0aaa-125">Örnek uygulama, uygulamanın ana işlevlerini doğrulayan bir birim testi kitaplığı içerir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-125">The sample application includes a unit test library that validates the major functionality of the app.</span></span> <span data-ttu-id="a0aaa-126">Oluşturulan uyarılara göre herhangi bir uygulamayı değiştirirseniz, bu proje güvenli bir şekilde yükseltmeyi daha kolay hale getirir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-126">That project will make it easier to upgrade safely, if you change any of the implementation based on the warnings generated.</span></span> <span data-ttu-id="a0aaa-127">Başlangıç kodunu [DotNet/Samples](https://github.com/dotnet/samples/tree/master/csharp/tutorials/nullable-reference-migration/start) GitHub deposundan indirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-127">You can download the starter code from the [dotnet/samples](https://github.com/dotnet/samples/tree/master/csharp/tutorials/nullable-reference-migration/start) GitHub repository.</span></span>

<span data-ttu-id="a0aaa-128">Bir projeyi geçirme hedefiniz yeni dil özelliklerinden faydalanmak için olmalıdır. böylece, amacınızı null olabilir ve bu nedenle, null yapılabilir ek açıklama bağlamına sahip olduğunuzda derleyicinin uyarı üretmemesi ve null yapılabilir uyarı bağlamı `enabled`olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-128">Your goal migrating a project should be to leverage the new language features so that you clearly express your intent on the nullability of variables, and do so in such a way that the compiler doesn't generate warnings when you have the nullable annotation context and nullable warning context set to `enabled`.</span></span>

## <a name="upgrade-the-projects-to-c-8"></a><span data-ttu-id="a0aaa-129">Projeleri 8 ' e C# yükseltin</span><span class="sxs-lookup"><span data-stu-id="a0aaa-129">Upgrade the projects to C# 8</span></span>

<span data-ttu-id="a0aaa-130">Geçiş görevinin kapsamını belirlemekte iyi bir ilk adım vardır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-130">A good first step is to determine the scope of the migration task.</span></span> <span data-ttu-id="a0aaa-131">Projeyi C# 8,0 (veya daha yeni) sürümüne yükselterek başlayın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-131">Start by upgrading the project to C# 8.0 (or newer).</span></span> <span data-ttu-id="a0aaa-132">Web projesi ve birim test projesi için `LangVersion` öğesini her iki csproj dosyasına ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-132">Add the `LangVersion` element to both csproj files for the web project and the unit test project:</span></span>

```xml
<LangVersion>8.0</LangVersion>
```

<span data-ttu-id="a0aaa-133">Dil sürümünü yükseltmek 8,0 seçer C# , ancak null yapılabilir ek açıklama bağlamını veya null yapılabilir uyarı bağlamını etkinleştirmez.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-133">Upgrading the language version selects C# 8.0, but does not enable the nullable annotation context or the nullable warning context.</span></span> <span data-ttu-id="a0aaa-134">Uyarı vermeden oluşturulduğundan emin olmak için projeyi yeniden derleyin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-134">Rebuild the project to ensure that it builds without warnings.</span></span>

<span data-ttu-id="a0aaa-135">İyi bir sonraki adım, null yapılabilir ek açıklama bağlamını açıp kaç uyarı oluşturulduğunu görmenizde yarar vardır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-135">A good next step is to turn on the nullable annotation context and see how many warnings are generated.</span></span> <span data-ttu-id="a0aaa-136">Çözümdeki her iki csproj dosyasına doğrudan `LangVersion` öğesi altında aşağıdaki öğeyi ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-136">Add the following element to both csproj files in the solution, directly under the `LangVersion` element:</span></span>

```xml
<Nullable>enable</Nullable>
```

<span data-ttu-id="a0aaa-137">Bir test derlemesi yapın ve uyarı listesine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-137">Do a test build, and notice the warning list.</span></span> <span data-ttu-id="a0aaa-138">Bu küçük uygulamada, derleyici beş uyarı üretir, bu nedenle null yapılabilir ek açıklama bağlamını etkin bırakıp tüm proje için uyarıları düzeltmeye başlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-138">In this small application, the compiler generates five warnings, so it's likely you'd leave the nullable annotation context enabled and start fixing warnings for the entire project.</span></span>

<span data-ttu-id="a0aaa-139">Bu strateji yalnızca daha küçük projeler için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-139">That strategy works only for smaller projects.</span></span> <span data-ttu-id="a0aaa-140">Daha büyük projeler için, tüm kod tabanı için Nullable ek açıklama bağlamını etkinleştirerek oluşturulan uyarı sayısı, uyarıları sistematik olarak gidermeyi zorlaştırır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-140">For any larger projects, the number of warnings generated by enabling the nullable annotation context for the entire codebase makes it harder to fix the warnings systematically.</span></span> <span data-ttu-id="a0aaa-141">Daha büyük kurumsal projelerde, genellikle bir projeyi tek seferde geçirmek isteyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-141">For larger enterprise projects, you'll often want to migrate one project at a time.</span></span> <span data-ttu-id="a0aaa-142">Her projede, tek bir sınıf veya dosyayı aynı anda geçirin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-142">In each project, migrate one class or file at a time.</span></span>

## <a name="warnings-help-discover-original-design-intent"></a><span data-ttu-id="a0aaa-143">Uyarılar özgün tasarım amacını bulmaya yardımcı olur</span><span class="sxs-lookup"><span data-stu-id="a0aaa-143">Warnings help discover original design intent</span></span>

<span data-ttu-id="a0aaa-144">Birden çok uyarı oluşturan iki sınıf vardır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-144">There are two classes that generate multiple warnings.</span></span> <span data-ttu-id="a0aaa-145">`NewsStoryViewModel` sınıfıyla başlayın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-145">Start with the `NewsStoryViewModel` class.</span></span> <span data-ttu-id="a0aaa-146">Uyarı kapsamını çalıştığınız kodun bölümleriyle sınırlayabilmeniz için her iki csproj dosyasından `Nullable` öğesini kaldırın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-146">Remove the `Nullable` element from both csproj files so that you can limit the scope of warnings to the sections of code you're working with.</span></span> <span data-ttu-id="a0aaa-147">*NewsStoryViewModel.cs* dosyasını açın ve aşağıdaki yönergeleri ekleyerek `NewsStoryViewModel` için Nullable ek açıklama bağlamını etkinleştirin ve bu sınıf tanımını izleyerek geri yükleyin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-147">Open the *NewsStoryViewModel.cs* file and add the following directives to enable the nullable annotation context for the `NewsStoryViewModel` and restore it following that class definition:</span></span>

```csharp
#nullable enable
public class NewsStoryViewModel
{
    public DateTimeOffset Published { get; set; }
    public string Title { get; set; }
    public string Uri { get; set; }
}
#nullable restore
```

<span data-ttu-id="a0aaa-148">Bu iki yönergeler, geçiş çabalarınıza odaklanmaya yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-148">These two directives help you focus your migration efforts.</span></span> <span data-ttu-id="a0aaa-149">Üzerinde etkin olarak çalışmakta olduğunuz kod alanı için null yapılabilir uyarılar oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-149">The nullable warnings are generated for the area of code you're actively working on.</span></span> <span data-ttu-id="a0aaa-150">Projenin tamamına yönelik uyarıları açmaya hazırsanız, bunları açık bırakmalısınız.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-150">You'll leave them on until you're ready to turn on the warnings for the entire project.</span></span> <span data-ttu-id="a0aaa-151">Tüm proje için null yapılabilir ek açıklamaları açtığınızda bağlamı yanlışlıkla devre dışı bırakabilmeniz için `disable` değeri yerine `restore` kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-151">You should use the `restore` rather than `disable` value so that you don't accidentally disable the context later when you've turned on nullable annotations for the entire project.</span></span> <span data-ttu-id="a0aaa-152">Tüm proje için null yapılabilir ek açıklama bağlamını etkinleştirdikten sonra, bu projeden tüm `#nullable` pragmaları kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-152">Once you've turned on the nullable annotation context for the entire project, you can remove all the `#nullable` pragmas from that project.</span></span>

<span data-ttu-id="a0aaa-153">`NewsStoryViewModel` sınıfı bir veri aktarım nesnesidir (DTO) ve özelliklerden ikisi okuma/yazma dizeleridir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-153">The `NewsStoryViewModel` class is a data transfer object (DTO) and two of the properties are read/write strings:</span></span>

[!code-csharp[InitialViewModel](~/samples/csharp/tutorials/nullable-reference-migration/start/SimpleFeedReader/ViewModels/NewsStoryViewModel.cs#StarterViewModel)]

<span data-ttu-id="a0aaa-154">Bu iki özellik `CS8618`, "null yapılamayan özelliğin başlatılmamış" olmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-154">These two properties cause `CS8618`, "Non-nullable property is uninitialized".</span></span> <span data-ttu-id="a0aaa-155">Bu yeterince net: her iki `string` özelliği de bir `NewsStoryViewModel` oluşturulduğunda `null` varsayılan değerine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-155">That's clear enough: both `string` properties have the default value of `null` when a `NewsStoryViewModel` is constructed.</span></span> <span data-ttu-id="a0aaa-156">Bulma için önemli olan özellikler `NewsStoryViewModel` nesneleri nasıl oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-156">What's important to discover is how `NewsStoryViewModel` objects are constructed.</span></span> <span data-ttu-id="a0aaa-157">Bu sınıfa bakarak, `null` değerinin tasarımın bir parçası olup olmadığını veya bu nesnelerin her biri oluşturulduğunda null olmayan değerlere ayarlanmış olduğunu söyleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-157">Looking at this class, you can't tell if the `null` value is part of the design, or if these objects are set to non-null values whenever one is created.</span></span> <span data-ttu-id="a0aaa-158">Haber hikayeleri, `NewsService` sınıfının `GetNews` yönteminde oluşturulur:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-158">The news stories are created in the `GetNews` method of the `NewsService` class:</span></span>

[!code-csharp[StarterCreateNewsItem](~/samples/csharp/tutorials/nullable-reference-migration/start/SimpleFeedReader/Services/NewsService.cs#CreateNewsItem)]

<span data-ttu-id="a0aaa-159">Yukarıdaki kod bloğunda oldukça bir bit vardır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-159">There's quite a bit going on in the preceding block of code.</span></span> <span data-ttu-id="a0aaa-160">Bu uygulama, bir `ISyndicationItem`haber öğesi oluşturmak için [Automaber](https://automapper.org/) NuGet paketini kullanır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-160">This application uses the [AutoMapper](https://automapper.org/) NuGet package to construct a news item from an `ISyndicationItem`.</span></span> <span data-ttu-id="a0aaa-161">Haber hikayesi öğelerinin oluşturulduğunu ve özellikler söz konusu bir bildirimde ayarlandığını keşfettiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-161">You've discovered that the news story items are constructed and the properties are set in that one statement.</span></span> <span data-ttu-id="a0aaa-162">Bu, `NewsStoryViewModel` tasarımının bu özelliklerin hiçbir durumda `null` değerine sahip olmayacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-162">That means the design for the `NewsStoryViewModel` indicates that these properties should never have the `null` value.</span></span> <span data-ttu-id="a0aaa-163">Bu özellikler **null atanamaz başvuru türleri**olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-163">These properties should be **nonnullable reference types**.</span></span> <span data-ttu-id="a0aaa-164">Bu, özgün tasarım amacını en iyi ifade eder.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-164">That best expresses the original design intent.</span></span> <span data-ttu-id="a0aaa-165">Aslında, hiçbir `NewsStoryViewModel` null olmayan değerler ile doğru *şekilde oluşturulur.*</span><span class="sxs-lookup"><span data-stu-id="a0aaa-165">In fact, any `NewsStoryViewModel` *is* correctly instantiated with non-null values.</span></span> <span data-ttu-id="a0aaa-166">Bu, aşağıdaki başlatma kodunu geçerli bir çözüm haline getirir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-166">That makes the following initialization code a valid fix:</span></span>

```csharp
public class NewsStoryViewModel
{
    public DateTimeOffset Published { get; set; }
    public string Title { get; set; } = default!;
    public string Uri { get; set; } = default!;
}
```

<span data-ttu-id="a0aaa-167">`string` tür için `null` olan `default` `Title` ve `Uri` ataması programın çalışma zamanı davranışını değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-167">The assignment of `Title` and `Uri` to `default` which is `null` for the `string` type doesn't change the runtime behavior of the program.</span></span> <span data-ttu-id="a0aaa-168">`NewsStoryViewModel` hala null değerlerle oluşturulmuş, ancak derleyici hiçbir uyarı raporluyor.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-168">The `NewsStoryViewModel` is still constructed with null values, but now the compiler reports no warnings.</span></span> <span data-ttu-id="a0aaa-169">**Null-forverme işleci**, `default` ifadesini izleyen `!` karakteri derleyiciye önceki ifadenin null olmadığını söyler.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-169">The **null-forgiving operator**, the `!` character following the `default` expression tells the compiler that the preceding expression is not null.</span></span> <span data-ttu-id="a0aaa-170">Bu teknik, diğer değişiklikler bir kod tabanında çok daha büyük değişiklikler yaparken, ancak bu uygulamada görece hızlı ve daha iyi bir çözüm olduğu zaman, `NewsStoryViewModel`: tüm özelliklerin oluşturucuda ayarlandığı sabit bir tür haline gelebilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-170">This technique may be expedient when other changes force much larger changes to a code base, but in this application there is a relatively quick and better solution: Make the `NewsStoryViewModel` an immutable type where all the properties are set in the constructor.</span></span> <span data-ttu-id="a0aaa-171">`NewsStoryViewModel`aşağıdaki değişiklikleri yapın:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-171">Make the following changes to the `NewsStoryViewModel`:</span></span>

[!code-csharp[FinishedViewModel](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/ViewModels/NewsStoryViewModel.cs#FinishedViewModel)]

<span data-ttu-id="a0aaa-172">Bu yapıldıktan sonra, özellikleri ayarlamak yerine, oluşturucuyu kullanması için Automaber 'yi yapılandıran kodu güncelleştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-172">Once that's done, you need to update the code that configures the AutoMapper so that it uses the constructor rather than setting properties.</span></span> <span data-ttu-id="a0aaa-173">`NewsService.cs` açın ve dosyanın en altında aşağıdaki kodu arayın:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-173">Open `NewsService.cs` and look for the following code at the bottom of the file:</span></span>

[!code-csharp[StarterAutoMapper](~/samples/csharp/tutorials/nullable-reference-migration/start/SimpleFeedReader/Services/NewsService.cs#ConfigureAutoMapper)]

<span data-ttu-id="a0aaa-174">Bu kod, `ISyndicationItem` nesnesinin özelliklerini `NewsStoryViewModel` özellikleriyle eşler.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-174">That code maps properties of the `ISyndicationItem` object to the `NewsStoryViewModel` properties.</span></span> <span data-ttu-id="a0aaa-175">Bunun yerine bir Oluşturucu kullanarak, Automaber 'nin eşleme sağlamasını istersiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-175">You want the AutoMapper to provide the mapping using a constructor instead.</span></span> <span data-ttu-id="a0aaa-176">Yukarıdaki kodu aşağıdaki automaber yapılandırmasıyla değiştirin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-176">Replace the above code with the following automapper configuration:</span></span>

[!code-csharp[FinishedViewModel](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Services/NewsService.cs#ConfigureAutoMapper)]

<span data-ttu-id="a0aaa-177">Bu sınıf küçük olduğu ve dikkatle incelediğiniz için, bu sınıf bildiriminin üzerinde `#nullable enable` yönergesini açmanız gerektiğine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-177">Notice that because this class is small, and you've examined carefully, you should turn on the `#nullable enable` directive above this class declaration.</span></span> <span data-ttu-id="a0aaa-178">Kurucudaki değişiklik bir şeyi bozarak, tüm testleri çalıştırmak ve devam etmeden önce uygulamayı test etmek daha fazla olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-178">The change to the constructor could have broken something, so it's worthwhile to run all the tests and test the application before moving on.</span></span>

<span data-ttu-id="a0aaa-179">İlk değişiklik kümesi, özgün tasarımın değişkenlerin `null`olarak ayarlanmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-179">The first set of changes showed you how to discover when the original design indicated that variables shouldn't be set to `null`.</span></span> <span data-ttu-id="a0aaa-180">Teknik, **oluşturma tarafından doğru**olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-180">The technique is referred to as **correct by construction**.</span></span> <span data-ttu-id="a0aaa-181">Bir nesne ve özellikleri oluşturulduğunda `null` olduğunu bildirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-181">You declare that an object and its properties cannot be `null` when it's constructed.</span></span> <span data-ttu-id="a0aaa-182">Derleyicinin akış analizi, bu özelliklerin oluşturulduktan sonra `null` olarak ayarlanmadığından emin olmak için güvence sağlar.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-182">The compiler's flow analysis provides assurance that those properties aren't set to `null` after construction.</span></span> <span data-ttu-id="a0aaa-183">Bu oluşturucunun dış kod tarafından çağrıldığını ve kodun **null değer atanabilir yükümlülüğü**olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-183">Note that this constructor is called by external code, and that code is **nullable oblivious**.</span></span> <span data-ttu-id="a0aaa-184">Yeni sözdizimi çalışma zamanı denetimi sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-184">The new syntax doesn't provide runtime checking.</span></span> <span data-ttu-id="a0aaa-185">Dış kod derleyicinin akış analizini atlatılabilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-185">External code might circumvent the compiler's flow analysis.</span></span> 

<span data-ttu-id="a0aaa-186">Diğer zamanlarda, bir sınıfın yapısı amaç için farklı ipuçları sağlar.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-186">Other times, the structure of a class provides different clues to the intent.</span></span> <span data-ttu-id="a0aaa-187">*Error.cshtml.cs* dosyasını *Sayfalar* klasöründe açın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-187">Open the *Error.cshtml.cs* file in the *Pages* folder.</span></span> <span data-ttu-id="a0aaa-188">`ErrorViewModel` aşağıdaki kodu içerir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-188">The `ErrorViewModel` contains the following code:</span></span>

[!code-csharp[StarterErrorModel](~/samples/csharp/tutorials/nullable-reference-migration/start/SimpleFeedReader/Pages/Error.cshtml.cs#StartErrorModel)]

<span data-ttu-id="a0aaa-189">Sınıf bildiriminden önce `#nullable enable` yönergesini ve sonra bir `#nullable restore` yönergesini ekleyin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-189">Add the `#nullable enable` directive before the class declaration, and a `#nullable restore` directive after it.</span></span> <span data-ttu-id="a0aaa-190">`RequestId` başlatılmamış bir uyarı alırsınız.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-190">You'll get one warning that `RequestId` is not initialized.</span></span> <span data-ttu-id="a0aaa-191">Sınıfına bakarak, bazı durumlarda `RequestId` özelliğinin null olması gerektiğine karar vermeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-191">By looking at the class, you should decide that the `RequestId` property should be null in some cases.</span></span> <span data-ttu-id="a0aaa-192">`ShowRequestId` özelliğinin varlığı eksik değerlerin mümkün olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-192">The existence of the `ShowRequestId` property indicates that missing values are possible.</span></span> <span data-ttu-id="a0aaa-193">`null` geçerli olduğundan, `RequestId` özelliğinin *null yapılabilir bir başvuru türü*olduğunu göstermek için `string` türüne `?` ekleyin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-193">Because `null` is valid, add the `?` on the `string` type to indicate the `RequestId` property is a *nullable reference type*.</span></span> <span data-ttu-id="a0aaa-194">Son sınıf aşağıdaki örneğe benzer şekilde görünür:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-194">The final class looks like the following example:</span></span>

[!code-csharp[FinishedErrorModel](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Pages/Error.cshtml.cs#ErrorModel)]

<span data-ttu-id="a0aaa-195">Özelliğin kullanımlarını denetleyin ve ilgili sayfada özelliği, biçimlendirme sırasında işlemeden önce, özelliğinin null olarak denetlendiğini görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-195">Check for the uses of the property, and you see that in the associated page, the property is checked for null before rendering it in markup.</span></span> <span data-ttu-id="a0aaa-196">Bu, null olabilen bir başvuru türünün güvenli bir kullanımı olduğundan bu sınıfla işiniz bitti.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-196">That's a safe use of a nullable reference type, so you're done with this class.</span></span>

## <a name="fixing-nulls-causes-change"></a><span data-ttu-id="a0aaa-197">Null değerleri düzeltme değişikliğine neden olur</span><span class="sxs-lookup"><span data-stu-id="a0aaa-197">Fixing nulls causes change</span></span>

<span data-ttu-id="a0aaa-198">Genellikle, bir uyarı kümesinin düzeltilmesi ilgili kodda yeni uyarılar oluşturur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-198">Frequently, the fix for one set of warnings creates new warnings in related code.</span></span> <span data-ttu-id="a0aaa-199">`index.cshtml.cs` sınıfını düzelterek, uyarıları eylemde görelim.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-199">Let's see the warnings in action by fixing the `index.cshtml.cs` class.</span></span> <span data-ttu-id="a0aaa-200">`index.cshtml.cs` dosyasını açın ve kodu inceleyin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-200">Open the `index.cshtml.cs` file and examine the code.</span></span> <span data-ttu-id="a0aaa-201">Bu dosya dizin sayfası için arkasındaki kodu içerir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-201">This file contains the code behind for the index page:</span></span>

[!code-csharp[StarterIndexModel](~/samples/csharp/tutorials/nullable-reference-migration/start/SimpleFeedReader/Pages/Index.cshtml.cs#IndexModelStart)]

<span data-ttu-id="a0aaa-202">`#nullable enable` yönergesini ekleyin ve iki uyarı görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-202">Add the `#nullable enable` directive and you'll see two warnings.</span></span> <span data-ttu-id="a0aaa-203">`ErrorText` özelliği ne de `NewsItems` özelliği başlatılmaz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-203">Neither the `ErrorText` property nor the `NewsItems` property is initialized.</span></span> <span data-ttu-id="a0aaa-204">Bu sınıfın incelenmesi, her iki özelliği de null yapılabilir başvuru türleri olması gerektiğini düşünmenize yol açacağından, her Ikisinin de özel ayarlayıcıları vardır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-204">An examination of this class would lead you to believe that both properties should be nullable reference types: Both have private setters.</span></span> <span data-ttu-id="a0aaa-205">`OnGet` yönteminde tam olarak bir atanır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-205">Exactly one is assigned in the `OnGet` method.</span></span> <span data-ttu-id="a0aaa-206">Değişiklik yapmadan önce her iki özelliği de tüketicilere bakın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-206">Before making changes, look at the consumers of both properties.</span></span> <span data-ttu-id="a0aaa-207">Sayfanın kendisinde, herhangi bir hata için biçimlendirme oluşturmadan önce `ErrorText` null olarak denetlenir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-207">In the page itself, the `ErrorText` is checked against null before generating markup for any errors.</span></span> <span data-ttu-id="a0aaa-208">`NewsItems` koleksiyonu `null`karşı denetlenir ve koleksiyonda öğeler olduğundan emin olmak için denetlenir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-208">The `NewsItems` collection is checked against `null`, and checked to ensure the collection has items.</span></span> <span data-ttu-id="a0aaa-209">Hızlı bir çözüm, her iki özelliği de null yapılabilir başvuru türlerini yapmak olacaktır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-209">A quick fix would be to make both properties nullable reference types.</span></span> <span data-ttu-id="a0aaa-210">Daha iyi bir çözüm, koleksiyonu null yapılamayan bir başvuru türü yapmak ve haberleri alırken mevcut koleksiyona öğe eklemek olacaktır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-210">A better fix would be to make the collection a nonnullable reference type, and add items to the existing collection when retrieving news.</span></span> <span data-ttu-id="a0aaa-211">İlk çözüm, `?` `ErrorText``string` türüne eklemektir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-211">The first fix is to add the `?` to the `string` type for the `ErrorText`:</span></span>

[!code-csharp[UpdateErrorText](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Pages/Index.cshtml.cs#UpdateErrorText)]

<span data-ttu-id="a0aaa-212">`ErrorText` özelliğine herhangi bir erişim null denetimleri tarafından zaten korunduğu için, bu değişiklik diğer kod aracılığıyla görünmez olmayacaktır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-212">That change won't ripple through other code, because any access to the `ErrorText` property was already guarded by null checks.</span></span> <span data-ttu-id="a0aaa-213">Sonra, `NewsItems` listesini başlatın ve özellik ayarlayıcısını kaldırarak salt okunur bir özellik yaparak:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-213">Next, initialize the `NewsItems` list and remove the property setter, making it a readonly property:</span></span>

[!code-csharp[InitializeNewsItems](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Pages/Index.cshtml.cs#InitializeNewsItems)]

<span data-ttu-id="a0aaa-214">Bu, uyarıyı düzeltti ancak bir hata sunmuştur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-214">That fixed the warning but introduced an error.</span></span> <span data-ttu-id="a0aaa-215">`NewsItems` listesi artık **yapım tarafından düzeltilir**, ancak `OnGet` listesini ayarlayan kod yeni API ile eşleşecek şekilde değişmelidir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-215">The `NewsItems` list is now **correct by construction**, but the code that sets the list in `OnGet` must change to match the new API.</span></span> <span data-ttu-id="a0aaa-216">Bir atama yerine, mevcut listeye haber öğelerini eklemek için `AddRange` çağırın:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-216">Instead of an assignment, call `AddRange` to add the news items to the existing list:</span></span>

[!code-csharp[AddRange](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Pages/Index.cshtml.cs#AddRange)]

<span data-ttu-id="a0aaa-217">Atama yerine `AddRange` kullanmak `GetNews` yönteminin `List`yerine `IEnumerable` döndürebileceği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-217">Using `AddRange` instead of an assignment means that the `GetNews` method can return an `IEnumerable` instead of a `List`.</span></span> <span data-ttu-id="a0aaa-218">Bu, bir ayırmayı kaydeder.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-218">That saves one allocation.</span></span> <span data-ttu-id="a0aaa-219">Metodun imzasını değiştirin ve aşağıdaki kod örneğinde gösterildiği gibi `ToList` çağrısını kaldırın:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-219">Change the signature of the method, and remove the `ToList` call, as shown in the following code sample:</span></span>

[!code-csharp[GetNews](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Services/NewsService.cs#GetNewsFinished)]

<span data-ttu-id="a0aaa-220">İmzanın değiştirilmesi, testlerin birini de keser.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-220">Changing the signature breaks one of tests as well.</span></span> <span data-ttu-id="a0aaa-221">`NewsServiceTests.cs` dosyasını `SimpleFeedReader.Tests` projesinin `Services` klasöründe açın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-221">Open the `NewsServiceTests.cs` file in the `Services` folder of the `SimpleFeedReader.Tests` project.</span></span> <span data-ttu-id="a0aaa-222">`Returns_News_Stories_Given_Valid_Uri` testine gidin ve `result` değişkeninin türünü `IEnumerable<NewsItem>`olarak değiştirin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-222">Navigate to the `Returns_News_Stories_Given_Valid_Uri` test and change the type of the `result` variable to `IEnumerable<NewsItem>`.</span></span> <span data-ttu-id="a0aaa-223">Türü değiştirmek `Count` özelliğinin artık kullanılamadığı anlamına gelir, bu nedenle `Assert` `Count` özelliğini `Any()`çağrısıyla değiştirin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-223">Changing the type means the `Count` property is no longer available, so replace the `Count` property in the `Assert` with a call to `Any()`:</span></span>

[!code-csharp[FixTests](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader.Tests/Services/NewsServiceTests.cs#FixTestSignature)]

<span data-ttu-id="a0aaa-224">Dosyanın başlangıcına de `using System.Linq` bir ifade eklemeniz gerekecektir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-224">You'll need to add a `using System.Linq` statement to the beginning of the file as well.</span></span>

<span data-ttu-id="a0aaa-225">Bu değişiklik kümesi, genel örneklemeleri içeren kodu güncelleştirirken özel bir değerlendirme vurgulamaktadır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-225">This set of changes highlights special consideration when updating code that includes generic instantiations.</span></span> <span data-ttu-id="a0aaa-226">Hem liste hem de öğeler null yapılamayan türler listesinde.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-226">Both the list and the elements in the list of non-nullable types.</span></span> <span data-ttu-id="a0aaa-227">Ya da her ikisi de null yapılabilir türler olabilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-227">Either or both could be nullable types.</span></span> <span data-ttu-id="a0aaa-228">Aşağıdaki bildirimlere izin verilir:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-228">All the following declarations are allowed:</span></span>

- <span data-ttu-id="a0aaa-229">`List<NewsStoryViewModel>`: yinelenmeyen görünüm modellerinin null yapılamayan listesi.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-229">`List<NewsStoryViewModel>`: nonnullable list of nonullable view models.</span></span>
- <span data-ttu-id="a0aaa-230">`List<NewsStoryViewModel?>`: null yapılabilir görünüm modellerinin Nullable listesi.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-230">`List<NewsStoryViewModel?>`: nonnullable list of nullable view models.</span></span>
- <span data-ttu-id="a0aaa-231">`List<NewsStoryViewModel>?`: null yapılamayan görünüm modellerinin null olabilir listesi.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-231">`List<NewsStoryViewModel>?`: nullable list of nonnullable view models.</span></span>
- <span data-ttu-id="a0aaa-232">`List<NewsStoryViewModel?>?`: null yapılabilir görünüm modellerinin null olabilir listesi.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-232">`List<NewsStoryViewModel?>?`: nullable list of nullable view models.</span></span>

## <a name="interfaces-with-external-code"></a><span data-ttu-id="a0aaa-233">Dış kod içeren arabirimler</span><span class="sxs-lookup"><span data-stu-id="a0aaa-233">Interfaces with external code</span></span>

<span data-ttu-id="a0aaa-234">`NewsService` sınıfında değişiklikler yaptınız, bu nedenle söz konusu sınıfın `#nullable enable` ek açıklamasını etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-234">You've made changes to the `NewsService` class, so turn on the `#nullable enable` annotation for that class.</span></span> <span data-ttu-id="a0aaa-235">Bu, yeni uyarı oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-235">This won't generate any new warnings.</span></span> <span data-ttu-id="a0aaa-236">Ancak, sınıfının dikkatle incelenmesi derleyicinin akış analizinin bazı sınırlamalarını göstermeye yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-236">However, careful examination of the class helps to illustrate some of the limitations of the compiler's flow analysis.</span></span> <span data-ttu-id="a0aaa-237">Oluşturucuyu inceleyin:</span><span class="sxs-lookup"><span data-stu-id="a0aaa-237">Examine the constructor:</span></span>

[!code-csharp[ServiceConstructor](~/samples/csharp/tutorials/nullable-reference-migration/finished/SimpleFeedReader/Services/NewsService.cs#ServiceConstructor)]

<span data-ttu-id="a0aaa-238">`IMapper` parametresi null atanamaz bir başvuru olarak yazılmış.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-238">The `IMapper` parameter is typed as a nonnullable reference.</span></span> <span data-ttu-id="a0aaa-239">ASP.NET Core altyapı kodu tarafından çağrılır, bu nedenle derleyici `IMapper` hiçbir şekilde null olmadığını bilmez.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-239">It's called by ASP.NET Core infrastructure code, so the compiler doesn't really know that the `IMapper` will never be null.</span></span> <span data-ttu-id="a0aaa-240">Varsayılan ASP.NET Core bağımlılık ekleme (dı) kapsayıcısı, gerekli bir hizmeti çözümleyemezse, kod doğru olduğunda bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-240">The default ASP.NET Core dependency injection (DI) container throws an exception if it can't resolve a necessary service, so the code is correct.</span></span> <span data-ttu-id="a0aaa-241">Kodunuz null yapılabilir ek açıklama bağlamlarıyla derlense bile derleyici ortak API 'lerinize yapılan tüm çağrıları doğrulayamaz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-241">The compiler can't validate all calls to your public APIs, even if your code is compiled with nullable annotation contexts enabled.</span></span> <span data-ttu-id="a0aaa-242">Ayrıca, kitaplıklarınız henüz null yapılabilir başvuru türlerini kullanarak onaylanmamış projeler tarafından tüketilebilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-242">Furthermore, your libraries may be consumed by projects that have not yet opted into using nullable reference types.</span></span> <span data-ttu-id="a0aaa-243">Bu girdileri null yapılamayan türler olarak bildirseniz bile ortak API 'lere yönelik girişleri doğrulayın.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-243">Validate inputs to public APIs even though you've declared them as nonnullable types.</span></span>

## <a name="get-the-code"></a><span data-ttu-id="a0aaa-244">Kodu alın</span><span class="sxs-lookup"><span data-stu-id="a0aaa-244">Get the code</span></span>

<span data-ttu-id="a0aaa-245">İlk test derlenmesi sırasında belirlediğiniz uyarıları düzelttiniz, bu nedenle artık her iki proje için de null yapılabilir ek açıklama bağlamını açabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-245">You've fixed the warnings you identified in the initial test compile, so now you can turn on the nullable annotation context for both projects.</span></span> <span data-ttu-id="a0aaa-246">Projeleri yeniden derleyin; Derleyici hiçbir uyarı rapor vermez.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-246">Rebuild the projects; the compiler reports no warnings.</span></span> <span data-ttu-id="a0aaa-247">[DotNet/Samples](https://github.com/dotnet/samples/tree/master/csharp/tutorials/nullable-reference-migration/finished) GitHub deposundaki tamamlanmış projenin kodunu alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-247">You can get the code for the finished project in the [dotnet/samples](https://github.com/dotnet/samples/tree/master/csharp/tutorials/nullable-reference-migration/finished) GitHub repository.</span></span>

<span data-ttu-id="a0aaa-248">Null yapılabilir başvuru türlerini destekleyen yeni özellikler, kodunuzda `null` değerleri nasıl işleyeceğinizi bulmanıza ve düzeltmenize yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-248">The new features that support nullable reference types help you find and fix potential errors in how you handle `null` values in your code.</span></span> <span data-ttu-id="a0aaa-249">Null yapılabilir ek açıklama bağlamını etkinleştirmek, tasarım amacınızı ifade etmenizi sağlar: bazı değişkenler hiçbir şekilde null olmamalıdır, diğer değişkenler de null değerler içerebilir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-249">Enabling the nullable annotation context allows you to express your design intent: some variables should never be null, other variables may contain null values.</span></span> <span data-ttu-id="a0aaa-250">Bu özellikler tasarım amacınızı bildirmenize daha kolay bir hale getirir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-250">These features make it easier for you to declare your design intent.</span></span> <span data-ttu-id="a0aaa-251">Benzer şekilde, null yapılabilir uyarı bağlamı derleyiciye bu amacı ihlal ettiğinizde uyarı vermesini söyler.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-251">Similarly, the nullable warning context instructs the compiler to issue warnings when you have violated that intent.</span></span> <span data-ttu-id="a0aaa-252">Bu uyarılar, kodunuzu daha dayanıklı hale getirmek ve yürütme sırasında `NullReferenceException` oluşturması olasılığını azaltır.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-252">Those warnings guide you to make updates that make your code more resilient and less likely to throw a `NullReferenceException` during execution.</span></span> <span data-ttu-id="a0aaa-253">Geri kalan kod temeli dokunulmamış olsa da, geçirilecek yerel kod alanına odaklanabilmeniz için bu bağlamların kapsamını kontrol edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-253">You can control the scope of these contexts so that you can focus on local areas of code to migrate while the remaining codebase is untouched.</span></span> <span data-ttu-id="a0aaa-254">Uygulamada, bu geçiş görevini sınıflarınıza düzenli bakımın bir parçası yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-254">In practice, you can make this migration task a part of regular maintenance to your classes.</span></span> <span data-ttu-id="a0aaa-255">Bu öğretici, bir uygulamayı null yapılabilir başvuru türlerini kullanacak şekilde geçirme işlemini göstermiştir.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-255">This tutorial demonstrated the process to migrate an application to use nullable reference types.</span></span> <span data-ttu-id="a0aaa-256">Bu işlemin daha büyük bir gerçek dünya örneğini, null yapılabilir başvuru türlerini [Nodadtime](https://github.com/nodatime/nodatime/pull/1240/commits)ile birleştirmek IÇIN yapılan PR [Jon iskeet](https://github.com/jskeet) 'i inceleyerek inceleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a0aaa-256">You can explore a larger real-world example of this process by examining the PR [Jon Skeet](https://github.com/jskeet) made to incorporate nullable reference types into [NodaTime](https://github.com/nodatime/nodatime/pull/1240/commits).</span></span>
