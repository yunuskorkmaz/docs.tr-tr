---
title: Nesne odaklı programlama (C#)
description: C#; soyutlama, kapsülleme, devralma ve çok biçimlilik dahil olmak üzere nesne odaklı programlama için tam destek sağlar.
ms.date: 09/30/2020
ms.openlocfilehash: 8a8dc8dc6d40c539b988ea203654d994e88c357a
ms.sourcegitcommit: 97405ed212f69b0a32faa66a5d5fae7e76628b68
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/01/2020
ms.locfileid: "91614798"
---
# <a name="object-oriented-programming-c"></a><span data-ttu-id="3bef4-103">Nesne odaklı programlama (C#)</span><span class="sxs-lookup"><span data-stu-id="3bef4-103">Object-Oriented programming (C#)</span></span>

<span data-ttu-id="3bef4-104">C#, nesne yönelimli bir dildir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-104">C# is an object-oriented language.</span></span> <span data-ttu-id="3bef4-105">Nesne odaklı programlamada kullanılan temel tekniklerin dördü şunlardır:</span><span class="sxs-lookup"><span data-stu-id="3bef4-105">Four of the key techniques used in object-oriented programming are:</span></span>

- <span data-ttu-id="3bef4-106">*Soyutlama* , tür tüketicilerden gereksiz ayrıntıların gizlenmesi anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-106">*Abstraction* means hiding the unnecessary details from type consumers.</span></span>
- <span data-ttu-id="3bef4-107">*Kapsülleme* , ilişkili özellikler, Yöntemler ve diğer üyelerin bir grubunun tek bir birim veya nesne olarak kabul edildiği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-107">*Encapsulation* means that a group of related properties, methods, and other members are treated as a single unit or object.</span></span>
- <span data-ttu-id="3bef4-108">*Devralma* , mevcut bir sınıfı temel alan yeni sınıflar oluşturma özelliğini açıklar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-108">*Inheritance* describes the ability to create new classes based on an existing class.</span></span>
- <span data-ttu-id="3bef4-109">Çok *biçimlilik* , her bir sınıf farklı yollarla aynı özellikleri veya yöntemleri uyguladığından bile, birbirinin yerine kullanılabilecek birden fazla sınıfa sahip olabileceği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-109">*Polymorphism* means that you can have multiple classes that can be used interchangeably, even though each class implements the same properties or methods in different ways.</span></span>

<span data-ttu-id="3bef4-110">Önceki öğreticide, hem *soyutlamayı* hem de *kapsüllemeyi*gördüğünüz [sınıflara giriş](introduction-to-classes.md) .</span><span class="sxs-lookup"><span data-stu-id="3bef4-110">In the preceding tutorial, [introduction to classes](introduction-to-classes.md) you saw both *abstraction* and *encapsulation*.</span></span> <span data-ttu-id="3bef4-111">`BankAccount`Sınıf, bir banka hesabı kavramı için bir soyutlama sağladı.</span><span class="sxs-lookup"><span data-stu-id="3bef4-111">The `BankAccount` class provided an abstraction for the concept of a bank account.</span></span> <span data-ttu-id="3bef4-112">Sınıfını kullanan herhangi bir kodu etkilemeden uygulamasını değiştirebilirsiniz `BankAccount` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-112">You could modify its implementation without affecting any of the code that used the `BankAccount` class.</span></span> <span data-ttu-id="3bef4-113">Hem `BankAccount` hem de `Transaction` sınıfları, koddaki bu kavramları anlatmak için gereken bileşenlerin kapsüllemesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-113">Both the `BankAccount` and `Transaction` classes provide encapsulation of the components needed to describe those concepts in code.</span></span>

<span data-ttu-id="3bef4-114">Bu öğreticide, yeni özellikler eklemek için bu uygulamayı *Devralma* ve çok *biçimlilik* kullanımını kullanacak şekilde genişleteceksiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-114">In this tutorial, you'll extend that application to make use of *inheritance* and *polymorphism* to add new features.</span></span> <span data-ttu-id="3bef4-115">Ayrıca, `BankAccount` önceki öğreticide öğrendiğiniz *soyutlama* ve *kapsülleme* tekniklerinden yararlanarak sınıfa özellikler ekleyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-115">You'll also add features to the `BankAccount` class, taking advantage of the *abstraction* and *encapsulation* techniques you learned in the preceding tutorial.</span></span>

## <a name="create-different-types-of-accounts"></a><span data-ttu-id="3bef4-116">Farklı türlerde hesaplar oluşturun</span><span class="sxs-lookup"><span data-stu-id="3bef4-116">Create different types of accounts</span></span>

<span data-ttu-id="3bef4-117">Bu programı oluşturduktan sonra, ona özellik ekleme istekleri alırsınız.</span><span class="sxs-lookup"><span data-stu-id="3bef4-117">After building this program, you get requests to add features to it.</span></span> <span data-ttu-id="3bef4-118">Yalnızca bir banka hesabı türü olduğu durumlarda harika bir şekilde çalışmaktadır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-118">It works great in the situation where there is only one bank account type.</span></span> <span data-ttu-id="3bef4-119">Zaman içinde, gereken değişiklik ve ilgili hesap türleri istenir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-119">Over time, needs change, and related account types are requested:</span></span>

- <span data-ttu-id="3bef4-120">Her ayın sonunda ilgi çekici bir ilgi çekici hesap.</span><span class="sxs-lookup"><span data-stu-id="3bef4-120">An interest earning account that accrues interest at the end of each month.</span></span>
- <span data-ttu-id="3bef4-121">Negatif bakiyesi olan bir kredi satırı, ancak bir bakiye söz konusu olduğunda, her ay bir faiz ücreti vardır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-121">A line of credit that can have a negative balance, but when there's a balance, there's an interest charge each month.</span></span>
- <span data-ttu-id="3bef4-122">Tek bir depozito ile başlayan ve yalnızca ücret ödemekte olan ön ödemeli bir hediye kartı hesabı.</span><span class="sxs-lookup"><span data-stu-id="3bef4-122">A pre-paid gift card account that starts with a single deposit, and only can be paid off.</span></span> <span data-ttu-id="3bef4-123">Her ayın başlangıcında bir kez daha doldurulabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-123">It can be refilled once at the start of each month.</span></span>

<span data-ttu-id="3bef4-124">Bu farklı hesapların tümü, `BankAccount` önceki öğreticide tanımlanan sınıfa benzerdir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-124">All of these different accounts are similar to `BankAccount` class defined in the earlier tutorial.</span></span> <span data-ttu-id="3bef4-125">Bu kodu kopyalayabilir, sınıfları yeniden adlandırabilir ve değişiklikler yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-125">You could copy that code, rename the classes, and make modifications.</span></span> <span data-ttu-id="3bef4-126">Bu teknik, kısa vadede çalışır, ancak zaman içinde daha fazla iş olur.</span><span class="sxs-lookup"><span data-stu-id="3bef4-126">That technique would work in the short term, but it would be more work over time.</span></span> <span data-ttu-id="3bef4-127">Tüm değişiklikler etkilenen sınıfların üzerine kopyalanabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-127">Any changes would be copied across all the affected classes.</span></span>

<span data-ttu-id="3bef4-128">Bunun yerine, `BankAccount` önceki öğreticide oluşturulan sınıftan yöntemleri ve verileri miras alan yeni banka hesap türleri oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-128">Instead, you can create new bank account types that inherit methods and data from the `BankAccount` class created in the preceding tutorial.</span></span> <span data-ttu-id="3bef4-129">Bu yeni sınıflar, her bir `BankAccount` tür için gereken belirli davranışla sınıfı genişletebilir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-129">These new classes can extend the `BankAccount` class with the specific behavior needed for each type:</span></span>

```csharp
public class InterestEarningAccount : BankAccount
{
}

public class LineOfCreditAccount : BankAccount
{
}

public class GiftCardAccount : BankAccount
{
}
```

<span data-ttu-id="3bef4-130">Bu sınıfların her biri paylaşılan davranışı paylaşılan *temel sınıfından*, sınıfından *devralır* `BankAccount` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-130">Each of these classes *inherits* the shared behavior from their shared *base class*, the `BankAccount` class.</span></span> <span data-ttu-id="3bef4-131">*Türetilmiş sınıfların*her birinde yeni ve farklı işlevlere yönelik uygulamalar yazın.</span><span class="sxs-lookup"><span data-stu-id="3bef4-131">Write the implementations for new and different functionality in each of the *derived classes*.</span></span>  <span data-ttu-id="3bef4-132">Bu türetilmiş sınıfların, sınıfında tanımlanmış tüm davranışları zaten var `BankAccount` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-132">These derived classes already have all the behavior defined in the `BankAccount` class.</span></span>

<span data-ttu-id="3bef4-133">Her yeni sınıfı farklı bir kaynak dosyasında oluşturmak iyi bir uygulamadır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-133">It's a good practice to create each new class in a different source file.</span></span> <span data-ttu-id="3bef4-134">[Visual Studio](https://visualstudio.com)'da projeye sağ tıklayıp *Sınıf Ekle* ' yi seçerek yeni bir dosyaya yeni bir sınıf ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-134">In [Visual Studio](https://visualstudio.com), you can right-click on the project, and select *add class* to add a new class in a new file.</span></span> <span data-ttu-id="3bef4-135">[Visual Studio Code](https://code.visualstudio.com), yeni bir kaynak dosya oluşturmak için *Dosya* ' yı ve ardından *Yeni* ' yi seçin.</span><span class="sxs-lookup"><span data-stu-id="3bef4-135">In [Visual Studio Code](https://code.visualstudio.com), select *File* then *New* to create a new source file.</span></span> <span data-ttu-id="3bef4-136">Her iki araçta, sınıfıyla eşleşecek dosyayı adlandırın: *InterestEarningAccount.cs*, *LineOfCreditAccount.cs*ve *GiftCardAccount.cs*.</span><span class="sxs-lookup"><span data-stu-id="3bef4-136">In either tool, name the file to match the class: *InterestEarningAccount.cs*, *LineOfCreditAccount.cs*, and *GiftCardAccount.cs*.</span></span>

<span data-ttu-id="3bef4-137">Önceki örnekte gösterildiği gibi sınıfları oluşturduğunuzda, türetilmiş sınıflarınızın derlenmediği fark edeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-137">When you create the classes as shown in the preceding sample, you'll find that none of your derived classes compile.</span></span> <span data-ttu-id="3bef4-138">Bir Oluşturucu, bir nesne başlatmaktan sorumludur.</span><span class="sxs-lookup"><span data-stu-id="3bef4-138">A constructor is responsible for initializing an object.</span></span> <span data-ttu-id="3bef4-139">Türetilmiş sınıf oluşturucusunun türetilmiş sınıfını başlatması ve türetilmiş sınıfta bulunan temel sınıf nesnesinin nasıl başlatılacağını gösteren yönergeler sağlaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-139">A derived class constructor must initialize the derived class, and provide instructions on how to initialize the base class object included in the derived class.</span></span> <span data-ttu-id="3bef4-140">Uygun başlatma normalde herhangi bir ek kod olmadan gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-140">The proper initialization normally happens without any extra code.</span></span> <span data-ttu-id="3bef4-141">`BankAccount`Sınıfı, aşağıdaki imzaya sahip bir ortak Oluşturucu bildirir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-141">The `BankAccount` class declares one public constructor with the following signature:</span></span>

```csharp
public BankAccount(string name, decimal initialBalance)
```

<span data-ttu-id="3bef4-142">Bir oluşturucuyu kendiniz tanımladığınızda derleyici varsayılan bir Oluşturucu oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-142">The compiler doesn't generate a default constructor when you define a constructor yourself.</span></span> <span data-ttu-id="3bef4-143">Yani, her türetilmiş sınıfın bu oluşturucuyu açıkça çağırması gerekir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-143">That means each derived class must explicitly call this constructor.</span></span> <span data-ttu-id="3bef4-144">Bağımsız değişkenleri temel sınıf oluşturucusuna geçirebilen bir Oluşturucu bildirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-144">You declare a constructor that can pass arguments to the base class constructor.</span></span>  <span data-ttu-id="3bef4-145">Aşağıdaki kod, için yapıcısını göstermektedir `InterestEarningAccount` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-145">The following code shows the constructor for the `InterestEarningAccount`:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/InterestEarningAccount.cs" ID="DerivedConstructor":::

<span data-ttu-id="3bef4-146">Bu yeni oluşturucuya yönelik parametreler, parametre türü ve temel sınıf oluşturucusunun adlarıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-146">The parameters to this new constructor match the parameter type and names of the base class constructor.</span></span> <span data-ttu-id="3bef4-147">`: base()`Bir temel sınıf oluşturucusuna yapılan çağrıyı göstermek için söz dizimini kullanın.</span><span class="sxs-lookup"><span data-stu-id="3bef4-147">You use the `: base()` syntax to indicate a call to a base class constructor.</span></span> <span data-ttu-id="3bef4-148">Bazı sınıflar birden çok Oluşturucu tanımlar ve bu sözdizimi, hangi temel sınıf oluşturucuyu çağıracağınızı seçmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-148">Some classes define multiple constructors, and this syntax enables you to pick which base class constructor you call.</span></span> <span data-ttu-id="3bef4-149">Oluşturucuları güncelleştirdikten sonra, türetilmiş sınıfların her biri için kodu geliştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-149">Once you've updated the constructors, you can develop the code for each of the derived classes.</span></span> <span data-ttu-id="3bef4-150">Yeni sınıfların gereksinimleri aşağıdaki gibi belirtilebilir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-150">The requirements for the new classes can be stated as follows:</span></span>

- <span data-ttu-id="3bef4-151">Bir faiz kazanç hesabı:</span><span class="sxs-lookup"><span data-stu-id="3bef4-151">An interest earning account:</span></span>
  - <span data-ttu-id="3bef4-152">Aylık son Bakiyenin %2 ' inin kredisi alınır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-152">Will get a credit of 2% of the month-ending-balance.</span></span>
- <span data-ttu-id="3bef4-153">Kredi satırı:</span><span class="sxs-lookup"><span data-stu-id="3bef4-153">A line of credit:</span></span>
  - <span data-ttu-id="3bef4-154">Negatif bir bakiyeye sahip olabilir, ancak kredi limitinden mutlak değerde daha büyük olamaz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-154">Can have a negative balance, but not be greater in absolute value than the credit limit.</span></span>
  - <span data-ttu-id="3bef4-155">Aylık bakiye 0 olmadığı her ay bir faiz ücreti alınacaktır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-155">Will incur an interest charge each month where the end of month balance isn't 0.</span></span>
  - <span data-ttu-id="3bef4-156">Kredi sınırının üzerinde geçen her çekme al üzerinden bir ücret uygulanır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-156">Will incur a fee on each withdrawal that goes over the credit limit.</span></span>
- <span data-ttu-id="3bef4-157">Bir hediye kartı hesabı:</span><span class="sxs-lookup"><span data-stu-id="3bef4-157">A gift card account:</span></span>
  - <span data-ttu-id="3bef4-158">Ayın son gününde her ay bir kez belirtilen miktarda yeniden doldurulabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-158">Can be refilled with a specified amount once each month, on the last day of the month.</span></span>

<span data-ttu-id="3bef4-159">Bu hesap türlerinin üçü de her ayın sonunda yer alan bir eyleme sahip olduğunu görebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-159">You can see that all three of these account types have an action that takes places at the end of each month.</span></span> <span data-ttu-id="3bef4-160">Ancak, her hesap türü farklı görevler yapar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-160">However, each account type does different tasks.</span></span> <span data-ttu-id="3bef4-161">Bu kodu uygulamak için çok *biçimlilik* kullanıyorsunuz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-161">You use *polymorphism* to implement this code.</span></span> <span data-ttu-id="3bef4-162">Sınıfında tek bir `virtual` Yöntem oluşturun `BankAccount` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-162">Create a single `virtual` method in the `BankAccount` class:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/BankAccount.cs" ID="DeclareMonthEndTransactions":::

<span data-ttu-id="3bef4-163">Yukarıdaki kod, `virtual` türetilmiş bir sınıfın farklı bir uygulama sağlayabileceği temel sınıfta bir yöntemi bildirmek için anahtar sözcüğünü nasıl kullanacağınızı gösterir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-163">The preceding code shows how you use the `virtual` keyword to declare a method in the base class that a derived class may provide a different implementation for.</span></span> <span data-ttu-id="3bef4-164">`virtual`Yöntemi, türetilmiş herhangi bir sınıfın yeniden uygulanmasını seçebileceği bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-164">A `virtual` method is a method where any derived class may choose to reimplement.</span></span> <span data-ttu-id="3bef4-165">Türetilmiş sınıflar, `override` Yeni uygulamayı tanımlamak için anahtar sözcüğünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-165">The derived classes use the `override` keyword to define the new implementation.</span></span> <span data-ttu-id="3bef4-166">Genellikle bunu "temel sınıf uygulamasını geçersiz kılma" olarak ifade edersiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-166">Typically you refer to this as "overriding the base class implementation".</span></span> <span data-ttu-id="3bef4-167">`virtual`Anahtar sözcüğü, türetilmiş sınıfların davranışı geçersiz kılabileceği belirtir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-167">The `virtual` keyword specifies that derived classes may override the behavior.</span></span> <span data-ttu-id="3bef4-168">Ayrıca, `abstract` türetilmiş sınıfların davranışı geçersiz kılması gereken yöntemleri de bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-168">You can also declare `abstract` methods where derived classes must override the behavior.</span></span> <span data-ttu-id="3bef4-169">Temel sınıf, bir yöntem için uygulama sağlamıyor `abstract` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-169">The base class does not provide an implementation for an `abstract` method.</span></span> <span data-ttu-id="3bef4-170">Daha sonra, oluşturduğunuz yeni sınıfların ikisi için uygulamayı tanımlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-170">Next, you need to define the implementation for two of the new classes you've created.</span></span> <span data-ttu-id="3bef4-171">İle başlayın `InterestEarningAccount` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-171">Start with the `InterestEarningAccount`:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/InterestEarningAccount.cs" ID="ApplyMonthendInterest":::

<span data-ttu-id="3bef4-172">Aşağıdaki kodu öğesine ekleyin `LineOfCreditAccount` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-172">Add the following code to the `LineOfCreditAccount`.</span></span> <span data-ttu-id="3bef4-173">Kod, hesaptan alınmış pozitif bir faiz ücreti hesaplamak için bakiyeyi geçersiz kılar:</span><span class="sxs-lookup"><span data-stu-id="3bef4-173">The code negates the balance to compute a positive interest charge that is withdrawn from the account:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/LineOfCreditAccount.cs" ID="ApplyMonthendInterest":::

<span data-ttu-id="3bef4-174">`GiftCardAccount`Sınıfın ay sonu işlevlerini uygulamak için iki değişiklik yapması gerekir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-174">The `GiftCardAccount` class needs two changes to implement its month-end functionality.</span></span> <span data-ttu-id="3bef4-175">İlk olarak, oluşturucuyu her ay eklenecek isteğe bağlı bir miktar içerecek şekilde değiştirin:</span><span class="sxs-lookup"><span data-stu-id="3bef4-175">First, modify the constructor to include an optional amount to add each month:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/GiftCardAccount.cs" ID="GiftCardAccountConstruction":::

<span data-ttu-id="3bef4-176">Oluşturucu, bir değer için varsayılan değer sağlar, `monthlyDeposit` böylece çağıranlar `0` aylık depozito olmaması için bunu atlayabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-176">The constructor provides a default value for the `monthlyDeposit` value so callers can omit a `0` for no monthly deposit.</span></span> <span data-ttu-id="3bef4-177">Sonra, `PerformMonthEndTransactions` Oluşturucu içinde sıfır olmayan bir değere ayarlandıysa, aylık depozito eklemek için yöntemini geçersiz kılın:</span><span class="sxs-lookup"><span data-stu-id="3bef4-177">Next, override the `PerformMonthEndTransactions` method to add the monthly deposit, if it was set to a non-zero value in the constructor:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/GiftCardAccount.cs" ID="AddMonthlyDeposit":::

<span data-ttu-id="3bef4-178">Geçersiz kılma, oluşturucuda aylık depozito kümesini uygular.</span><span class="sxs-lookup"><span data-stu-id="3bef4-178">The override applies the monthly deposit set in the constructor.</span></span> <span data-ttu-id="3bef4-179">`Main`Ve için bu değişiklikleri test etmek üzere yöntemine aşağıdaki kodu ekleyin `GiftCardAccount` `InterestEarningAccount` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-179">Add the following code to the `Main` method to test these changes for the `GiftCardAccount` and the `InterestEarningAccount`:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/Program.cs" ID="FirstTests":::

<span data-ttu-id="3bef4-180">Sonuçları doğrulayın.</span><span class="sxs-lookup"><span data-stu-id="3bef4-180">Verify the results.</span></span> <span data-ttu-id="3bef4-181">Şimdi, için benzer bir test kodu kümesi ekleyin `LineOfCreditAccount` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-181">Now, add a similar set of test code for the `LineOfCreditAccount`:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/Program.cs" ID="TestLineOfCredit":::

<span data-ttu-id="3bef4-182">Yukarıdaki kodu eklediğinizde ve programı çalıştırdığınızda aşağıdaki hata gibi bir şey görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="3bef4-182">When you add the preceding code and run the program, you'll see something like the following error:</span></span>

```console
Unhandled exception. System.ArgumentOutOfRangeException: Amount of deposit must be positive (Parameter 'amount')
   at OOProgramming.BankAccount.MakeDeposit(Decimal amount, DateTime date, String note) in BankAccount.cs:line 42
   at OOProgramming.BankAccount..ctor(String name, Decimal initialBalance) in BankAccount.cs:line 31
   at OOProgramming.LineOfCreditAccount..ctor(String name, Decimal initialBalance) in LineOfCreditAccount.cs:line 9
   at OOProgramming.Program.Main(String[] args) in Program.cs:line 29
```

> [!NOTE]
> <span data-ttu-id="3bef4-183">Gerçek çıktı, projenin bulunduğu klasörün tam yolunu içerir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-183">The actual output includes the full path to the folder with the project.</span></span> <span data-ttu-id="3bef4-184">Dosya adları, breçekimi için atlandı.</span><span class="sxs-lookup"><span data-stu-id="3bef4-184">The folder names were omitted for brevity.</span></span> <span data-ttu-id="3bef4-185">Ayrıca, kod biçimlerinize bağlı olarak, satır numaraları biraz farklı olabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-185">Also, depending on your code format, the line numbers may be slightly different.</span></span>

<span data-ttu-id="3bef4-186">Bu kod, `BankAccount` ilk Bakiyenin 0 ' dan büyük olması gerektiğini varsaydığından başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="3bef4-186">This code fails because the `BankAccount` assumes that the initial balance must be greater than 0.</span></span> <span data-ttu-id="3bef4-187">Sınıfa yönelik başka bir varsayım, `BankAccount` Bakiyenin negatif gidemez.</span><span class="sxs-lookup"><span data-stu-id="3bef4-187">Another assumption baked into the `BankAccount` class is that the balance can't go negative.</span></span> <span data-ttu-id="3bef4-188">Bunun yerine, hesabı fazla çizen tüm çekme al reddedilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-188">Instead, any withdrawal that overdraws the account is rejected.</span></span> <span data-ttu-id="3bef4-189">Bu varsayımlar her ikisinin de değiştirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-189">Both of those assumptions need to change.</span></span> <span data-ttu-id="3bef4-190">Kredi hesabı satırı 0 ' dan başlar ve genellikle negatif bir bakiyeye sahip olur.</span><span class="sxs-lookup"><span data-stu-id="3bef4-190">The line of credit account starts at 0, and generally will have a negative balance.</span></span> <span data-ttu-id="3bef4-191">Ayrıca, bir müşteri çok fazla para alıyorsa, ücretlendirilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-191">Also, if a customer borrows too much money, they incur a fee.</span></span> <span data-ttu-id="3bef4-192">İşlem kabul edilir, yalnızca daha fazla maliyetde olur.</span><span class="sxs-lookup"><span data-stu-id="3bef4-192">The transaction is accepted, it just costs more.</span></span> <span data-ttu-id="3bef4-193">İlk kural, `BankAccount` oluşturucuya en düşük dengeyi belirten isteğe bağlı bir bağımsız değişken eklenerek uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-193">The first rule can be implemented by adding an optional argument to the `BankAccount` constructor that specifies the minimum balance.</span></span> <span data-ttu-id="3bef4-194">Varsayılan değer: `0`.</span><span class="sxs-lookup"><span data-stu-id="3bef4-194">The default is `0`.</span></span> <span data-ttu-id="3bef4-195">İkinci kural, türetilmiş sınıfların varsayılan algoritmayı değiştirmesini sağlayan bir mekanizmayı gerektirir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-195">The second rule requires a mechanism that enables derived classes to modify the default algorithm.</span></span> <span data-ttu-id="3bef4-196">Bir anlamda, temel sınıf "ister" türetilmiş türü, aşırı taslak olduğunda ne olması gerektiğini ifade etmelidir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-196">In a sense, the base class "asks" the derived type what should happen when there's an overdraft.</span></span> <span data-ttu-id="3bef4-197">Varsayılan davranış, bir özel durum oluşturarak işlemi reddetmektir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-197">The default behavior is to reject the transaction by throwing an exception.</span></span>

<span data-ttu-id="3bef4-198">İsteğe bağlı bir parametre içeren ikinci bir Oluşturucu ekleyerek başlayalım `minimumBalance` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-198">Let's start by adding a second constructor that includes an optional `minimumBalance` parameter.</span></span> <span data-ttu-id="3bef4-199">Bu yeni Oluşturucu, mevcut Oluşturucu tarafından gerçekleştirilen tüm işlemleri yapar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-199">This new constructor does all the actions done by the existing constructor.</span></span> <span data-ttu-id="3bef4-200">Ayrıca, Minimum bakiye özelliğini ayarlar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-200">Also, it sets the minimum balance property.</span></span> <span data-ttu-id="3bef4-201">Var olan oluşturucunun gövdesini kopyalayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-201">You could copy the body of the existing constructor.</span></span> <span data-ttu-id="3bef4-202">Ancak bu, gelecekte değiştirilecek iki konum anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-202">but that means two locations to change in the future.</span></span> <span data-ttu-id="3bef4-203">Bunun yerine, bir oluşturucunun başka bir oluşturucuyu aramasını sağlamak için *Oluşturucu zincirlemeyi* kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="3bef4-203">Instead, you can use *constructor chaining* to have one constructor call another.</span></span> <span data-ttu-id="3bef4-204">Aşağıdaki kod iki Oluşturucusu ve yeni ek alanı göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-204">The following code shows the two constructors and the new additional field:</span></span>

 :::code language="csharp" source="./snippets/object-oriented-programming/BankAccount.cs" ID="ConstructorModifications":::

<span data-ttu-id="3bef4-205">Yukarıdaki kodda iki yeni teknik gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-205">The preceding code shows two new techniques.</span></span> <span data-ttu-id="3bef4-206">İlk olarak, `minimumBalance` alan olarak işaretlenir `readonly` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-206">First, the `minimumBalance` field is marked as `readonly`.</span></span> <span data-ttu-id="3bef4-207">Diğer bir deyişle, nesne oluşturulduktan sonra değer değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="3bef4-207">That means the value cannot be changed after the object is constructed.</span></span> <span data-ttu-id="3bef4-208">Bir, oluşturulduktan sonra `BankAccount` `minimumBalance` değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="3bef4-208">Once a `BankAccount` is created, the `minimumBalance` can't change.</span></span> <span data-ttu-id="3bef4-209">İkincisi, iki parametre alan Oluşturucu `: this(name, initialBalance, 0) { }` uygulama olarak kullanır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-209">Second, the constructor that takes two parameters uses `: this(name, initialBalance, 0) { }` as its implementation.</span></span> <span data-ttu-id="3bef4-210">`: this()`İfade, üç parametreli diğeri olan diğer oluşturucuyu çağırır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-210">The `: this()` expression calls the other constructor, the one with three parameters.</span></span> <span data-ttu-id="3bef4-211">Bu teknik, istemci kodu birçok oluşturucudan birini seçebilse bile, bir nesneyi başlatmak için tek bir uygulamaya sahip etmenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-211">This technique allows you to have a single implementation for initializing an object even though client code can choose one of many constructors.</span></span>

<span data-ttu-id="3bef4-212">Bu uygulama `MakeDeposit` yalnızca ilk bakiye şundan büyükse çağrılır `0` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-212">This implementation calls `MakeDeposit` only if the initial balance is greater than `0`.</span></span> <span data-ttu-id="3bef4-213">Bu, mevduonun pozitif olması gereken kuralı koruyan, ancak kredi hesabının bir `0` bakiyeyle açılmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-213">That preserves the rule that deposits must be positive, yet lets the credit account open with a `0` balance.</span></span>

<span data-ttu-id="3bef4-214">Artık `BankAccount` sınıfın Minimum bakiye için salt bir salt okunurdur alanı olduğuna göre, Son değişiklik, bu durumda sabit kodu yönteminde olarak değiştirmektedir `0` `minimumBalance` `MakeWithdrawal` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-214">Now that the `BankAccount` class has a read-only field for the minimum balance, the final change is to change the hard code `0` to `minimumBalance` in the `MakeWithdrawal` method:</span></span>

```csharp
if (Balance - amount < minimumBalance)
```

<span data-ttu-id="3bef4-215">Sınıfı genişlettikten sonra `BankAccount` , `LineOfCreditAccount` aşağıdaki kodda gösterildiği gibi oluşturucuyu yeni temel oluşturucuyu çağırmak üzere değiştirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="3bef4-215">After extending the `BankAccount` class, you can modify the `LineOfCreditAccount` constructor to call the new base constructor, as shown in the following code:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/LineOfCreditAccount.cs" ID="ConstructLineOfCredit":::

<span data-ttu-id="3bef4-216">Oluşturucunun, parametrenin bir `LineOfCreditAccount` `creditLimit` anlamı ile eşleşecek şekilde parametre işaretini değiştirdiğine dikkat edin `minimumBalance` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-216">Notice that the `LineOfCreditAccount` constructor changes the sign of the `creditLimit` parameter so it matches the meaning of the `minimumBalance` parameter.</span></span>

## <a name="different-overdraft-rules"></a><span data-ttu-id="3bef4-217">Farklı fazla taslak kuralları</span><span class="sxs-lookup"><span data-stu-id="3bef4-217">Different overdraft rules</span></span>

<span data-ttu-id="3bef4-218">Eklenecek son özellik, `LineOfCreditAccount` bir ücreti, işlemin yerine geçen kredi sınırının üzerinde hareket etmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="3bef4-218">The last feature to add enables the `LineOfCreditAccount` to charge a fee for going over the credit limit instead of refusing the transaction.</span></span>

<span data-ttu-id="3bef4-219">Bir yöntem, gerekli davranışı uyguladığınız sanal bir işlevi tanımlamaktır.</span><span class="sxs-lookup"><span data-stu-id="3bef4-219">One technique is to define a virtual function where you implement the required behavior.</span></span> <span data-ttu-id="3bef4-220">`Bank Account`Sınıfı `MakeWithdrawal` iki yönteme şunların.</span><span class="sxs-lookup"><span data-stu-id="3bef4-220">The `Bank Account` class refactors the `MakeWithdrawal` method into two methods.</span></span> <span data-ttu-id="3bef4-221">Yeni yöntem, çekme al değeri en düşük değerin altına alırsa belirtilen eylemi gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-221">The new method does the specified action when the withdrawal takes the balance below the minimum.</span></span> <span data-ttu-id="3bef4-222">Mevcut `MakeWithdrawal` Yöntem aşağıdaki koda sahiptir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-222">The existing `MakeWithdrawal` method has the following code:</span></span>

```csharp
public void MakeWithdrawal(decimal amount, DateTime date, string note)
{
    if (amount <= 0)
    {
        throw new ArgumentOutOfRangeException(nameof(amount), "Amount of withdrawal must be positive");
    }
    if (Balance - amount < minimumBalance)
    {
        throw new InvalidOperationException("Not sufficient funds for this withdrawal");
    }
    var withdrawal = new Transaction(-amount, date, note);
    allTransactions.Add(withdrawal);
}
```

<span data-ttu-id="3bef4-223">Aşağıdaki kodla değiştirin:</span><span class="sxs-lookup"><span data-stu-id="3bef4-223">Replace it with the following code:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/BankAccount.cs" ID="RefactoredMakeWithdrawal":::

<span data-ttu-id="3bef4-224">Eklenen yöntem, yalnızca türetilmiş sınıflardan çağrılabilecek anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-224">The added method is  , which means that it can be called only from derived classes.</span></span> <span data-ttu-id="3bef4-225">Bu bildirim, diğer istemcilerin yöntemi aramasını engeller.</span><span class="sxs-lookup"><span data-stu-id="3bef4-225">That declaration prevents other clients from calling the method.</span></span> <span data-ttu-id="3bef4-226">Ayrıca `virtual` , türetilmiş sınıfların davranışı değiştirebilmesini sağlayacak.</span><span class="sxs-lookup"><span data-stu-id="3bef4-226">It's also `virtual` so that derived classes can change the behavior.</span></span> <span data-ttu-id="3bef4-227">Dönüş türü bir `Transaction?` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-227">The return type is a `Transaction?`.</span></span> <span data-ttu-id="3bef4-228">`?`Ek açıklama, yöntemin dönebileceğini belirtir `null` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-228">The `?` annotation indicates that the method may return `null`.</span></span> <span data-ttu-id="3bef4-229">`LineOfCreditAccount`Çekme sınırı aşıldığında bir ücret ücreti almak için ' de aşağıdaki uygulamayı ekleyin:</span><span class="sxs-lookup"><span data-stu-id="3bef4-229">Add the following implementation in the `LineOfCreditAccount` to charge a fee when the withdrawal limit is exceeded:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/LineOfCreditAccount.cs" ID="AddOverdraftFee":::

<span data-ttu-id="3bef4-230">Geçersiz kılma, hesap fazla çizilmiş olduğunda bir ücret işlemi döndürür.</span><span class="sxs-lookup"><span data-stu-id="3bef4-230">The override returns a fee transaction when the account is overdrawn.</span></span> <span data-ttu-id="3bef4-231">Çekme al, sınırın üzerinde değilse, yöntem bir `null` işlem döndürür.</span><span class="sxs-lookup"><span data-stu-id="3bef4-231">If the withdrawal doesn't go over the limit, the method returns a `null` transaction.</span></span> <span data-ttu-id="3bef4-232">Bu, ücretsiz olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="3bef4-232">That indicates there's no fee.</span></span> <span data-ttu-id="3bef4-233">Aşağıdaki kodu sınıfındaki yöntemine ekleyerek bu değişiklikleri test edin `Main` `Program` :</span><span class="sxs-lookup"><span data-stu-id="3bef4-233">Test these changes by adding the following code to your `Main` method in the `Program` class:</span></span>

:::code language="csharp" source="./snippets/object-oriented-programming/Program.cs" ID="TestLineOfCredit":::

<span data-ttu-id="3bef4-234">Programı çalıştırın ve sonuçları denetleyin.</span><span class="sxs-lookup"><span data-stu-id="3bef4-234">Run the program, and check the results.</span></span>

## <a name="summary"></a><span data-ttu-id="3bef4-235">Özet</span><span class="sxs-lookup"><span data-stu-id="3bef4-235">Summary</span></span>

<span data-ttu-id="3bef4-236">Bu öğreticide, nesne odaklı programlamada kullanılan birçok teknik gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="3bef4-236">This tutorial demonstrated many of the techniques used in Object-Oriented programming:</span></span>

- <span data-ttu-id="3bef4-237">Her sınıfta çok sayıda ayrıntı sakladığınızda *soyutlama* kullandınız `private` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-237">You used *Abstraction* when you kept many details `private` in each class.</span></span>
- <span data-ttu-id="3bef4-238">Farklı hesap türlerinin her biri için sınıflar tanımladığınızda *kapsülleme* kullandınız.</span><span class="sxs-lookup"><span data-stu-id="3bef4-238">You used *Encapsulation* when you defined classes for each of the different account types.</span></span> <span data-ttu-id="3bef4-239">Bu sınıflar, bu hesap türünün davranışını açıklandı.</span><span class="sxs-lookup"><span data-stu-id="3bef4-239">Those classes described the behavior for that type of account.</span></span>
- <span data-ttu-id="3bef4-240">Kodu kaydetmek için sınıfında önceden oluşturulmuş uygulamayı yararlanılabilir kullandığınızda *devralmayı* kullandınız `BankAccount` .</span><span class="sxs-lookup"><span data-stu-id="3bef4-240">You used *Inheritance* when you leveraged the implementation already created in the `BankAccount` class to save code.</span></span>
- <span data-ttu-id="3bef4-241">*Polymorphism* `virtual` Türetilmiş sınıfların bu hesap türü için belirli davranışlar oluşturmak üzere geçersiz kılabileceği Yöntemler oluşturduğunuzda çok biçimlilik kullandınız.</span><span class="sxs-lookup"><span data-stu-id="3bef4-241">You used *Polymorphism* when you created `virtual` methods that derived classes could override to create specific behavior for that account type.</span></span>

<span data-ttu-id="3bef4-242">Tebrikler, C# öğreticilerine giriş yaptığımızı tamamladınız.</span><span class="sxs-lookup"><span data-stu-id="3bef4-242">Congratulations, you've finished all of our introduction to C# tutorials.</span></span> <span data-ttu-id="3bef4-243">Daha fazla bilgi edinmek için [öğreticilerimizi](../index.md)deneyin.</span><span class="sxs-lookup"><span data-stu-id="3bef4-243">To learn more, try more of our [tutorials](../index.md).</span></span>
