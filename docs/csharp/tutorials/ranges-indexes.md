---
title: Dizinler ve aralıklar kullanarak veri aralıklarını keşfet
description: Bu gelişmiş öğreticide, sıralı veri kümesinin sürekli bir aralığını incelemek üzere dizinler ve aralıklar kullanarak verileri araştırmanızı öğretilir.
ms.date: 09/11/2020
ms.technology: csharp-fundamentals
ms.custom: mvc
ms.openlocfilehash: cf6c83484332ed517b2326b3fd9d7458f191227e
ms.sourcegitcommit: a8730298170b8d96b4272e0c3dfc9819c606947b
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/17/2020
ms.locfileid: "90738872"
---
# <a name="indices-and-ranges"></a><span data-ttu-id="add6b-103">Dizinler ve aralıklar</span><span class="sxs-lookup"><span data-stu-id="add6b-103">Indices and ranges</span></span>

<span data-ttu-id="add6b-104">Aralıklar ve dizinler, bir dizideki tek öğe veya aralıklara erişmek için bir kısa sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="add6b-104">Ranges and indices provide a succinct syntax for accessing single elements or ranges in a sequence.</span></span>

<span data-ttu-id="add6b-105">Bu öğreticide aşağıdakilerin nasıl yapılacağını öğreneceksiniz:</span><span class="sxs-lookup"><span data-stu-id="add6b-105">In this tutorial, you'll learn how to:</span></span>

> [!div class="checklist"]
>
> - <span data-ttu-id="add6b-106">Bir dizideki aralıklar için söz dizimini kullanın.</span><span class="sxs-lookup"><span data-stu-id="add6b-106">Use the syntax for ranges in a sequence.</span></span>
> - <span data-ttu-id="add6b-107">Her bir sıranın başlangıcı ve bitişi için tasarım kararlarını anlayın.</span><span class="sxs-lookup"><span data-stu-id="add6b-107">Understand the design decisions for the start and end of each sequence.</span></span>
> - <span data-ttu-id="add6b-108">Ve türleri için senaryolar <xref:System.Index> öğrenin <xref:System.Range> .</span><span class="sxs-lookup"><span data-stu-id="add6b-108">Learn scenarios for the <xref:System.Index> and <xref:System.Range> types.</span></span>

## <a name="language-support-for-indices-and-ranges"></a><span data-ttu-id="add6b-109">Dizinler ve aralıklar için dil desteği</span><span class="sxs-lookup"><span data-stu-id="add6b-109">Language support for indices and ranges</span></span>

<span data-ttu-id="add6b-110">Bu dil desteği iki yeni türe ve iki yeni işleçlere dayanır:</span><span class="sxs-lookup"><span data-stu-id="add6b-110">This language support relies on two new types and two new operators:</span></span>

- <span data-ttu-id="add6b-111"><xref:System.Index?displayProperty=nameWithType> bir dizinin dizisini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="add6b-111"><xref:System.Index?displayProperty=nameWithType> represents an index into a sequence.</span></span>
- <span data-ttu-id="add6b-112">`^`Bir dizinin bir sıranın sonuna göreli olduğunu belirten bitiş işlecinden dizin.</span><span class="sxs-lookup"><span data-stu-id="add6b-112">The index from end operator `^`, which specifies that an index is relative to the end of a sequence.</span></span>
- <span data-ttu-id="add6b-113"><xref:System.Range?displayProperty=nameWithType> bir dizinin alt aralığını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="add6b-113"><xref:System.Range?displayProperty=nameWithType> represents a sub range of a sequence.</span></span>
- <span data-ttu-id="add6b-114">`..`Aralık işleci, bir aralığın işlenenlerinin başlangıcını ve sonunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="add6b-114">The range operator `..`, which specifies the start and end of a range as its operands.</span></span>

<span data-ttu-id="add6b-115">Dizin kurallarıyla başlayalım.</span><span class="sxs-lookup"><span data-stu-id="add6b-115">Let's start with the rules for indices.</span></span> <span data-ttu-id="add6b-116">Bir dizi düşünün `sequence` .</span><span class="sxs-lookup"><span data-stu-id="add6b-116">Consider an array `sequence`.</span></span> <span data-ttu-id="add6b-117">`0`Dizin, ile aynıdır `sequence[0]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-117">The `0` index is the same as `sequence[0]`.</span></span> <span data-ttu-id="add6b-118">`^0`Dizin, ile aynıdır `sequence[sequence.Length]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-118">The `^0` index is the same as `sequence[sequence.Length]`.</span></span> <span data-ttu-id="add6b-119">İfadesi olduğu `sequence[^0]` gibi bir özel durum oluşturur `sequence[sequence.Length]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-119">The expression `sequence[^0]` does throw an exception, just as `sequence[sequence.Length]` does.</span></span> <span data-ttu-id="add6b-120">Herhangi bir sayı için `n` Dizin `^n` aynı olur `sequence[sequence.Length - n]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-120">For any number `n`, the index `^n` is the same as `sequence[sequence.Length - n]`.</span></span>

```csharp
string[] words = new string[]
{
                // index from start    index from end
    "The",      // 0                   ^9
    "quick",    // 1                   ^8
    "brown",    // 2                   ^7
    "fox",      // 3                   ^6
    "jumped",   // 4                   ^5
    "over",     // 5                   ^4
    "the",      // 6                   ^3
    "lazy",     // 7                   ^2
    "dog"       // 8                   ^1
};              // 9 (or words.Length) ^0
```

<span data-ttu-id="add6b-121">Dizinle son sözcüğü alabilirsiniz `^1` .</span><span class="sxs-lookup"><span data-stu-id="add6b-121">You can retrieve the last word with the `^1` index.</span></span> <span data-ttu-id="add6b-122">Başlatmanın altına aşağıdaki kodu ekleyin:</span><span class="sxs-lookup"><span data-stu-id="add6b-122">Add the following code below the initialization:</span></span>

[!code-csharp[LastIndex](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_LastIndex)]

<span data-ttu-id="add6b-123">Aralık, bir aralığın *başlangıcını* ve *sonunu* belirtir.</span><span class="sxs-lookup"><span data-stu-id="add6b-123">A range specifies the *start* and *end* of a range.</span></span> <span data-ttu-id="add6b-124">Aralıklar dışlamalı, yani *bitiş* aralığa dahil değildir.</span><span class="sxs-lookup"><span data-stu-id="add6b-124">Ranges are exclusive, meaning the *end* isn't included in the range.</span></span> <span data-ttu-id="add6b-125">Aralık, tüm aralığı temsil eden `[0..^0]` tüm aralığı temsil eder `[0..sequence.Length]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-125">The range `[0..^0]` represents the entire range, just as `[0..sequence.Length]` represents the entire range.</span></span>

<span data-ttu-id="add6b-126">Aşağıdaki kod, "hızlı", "kahverengi" ve "Fox" sözcüklerinin bulunduğu bir alt Aralık oluşturur.</span><span class="sxs-lookup"><span data-stu-id="add6b-126">The following code creates a subrange with the words "quick", "brown", and "fox".</span></span> <span data-ttu-id="add6b-127">İle içerir `words[1]` `words[3]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-127">It includes `words[1]` through `words[3]`.</span></span> <span data-ttu-id="add6b-128">Öğe `words[4]` Aralık içinde değil.</span><span class="sxs-lookup"><span data-stu-id="add6b-128">The element `words[4]` isn't in the range.</span></span> <span data-ttu-id="add6b-129">Aşağıdaki kodu aynı yönteme ekleyin.</span><span class="sxs-lookup"><span data-stu-id="add6b-129">Add the following code to the same method.</span></span> <span data-ttu-id="add6b-130">Etkileşimli pencerenin alt kısmına kopyalayıp yapıştırın.</span><span class="sxs-lookup"><span data-stu-id="add6b-130">Copy and paste it at the bottom of the interactive window.</span></span>

[!code-csharp[Range](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_Range)]

<span data-ttu-id="add6b-131">Aşağıdaki kod, "Lazy" ve "köpek" ile aralığı döndürür.</span><span class="sxs-lookup"><span data-stu-id="add6b-131">The following code returns the range with "lazy" and "dog".</span></span> <span data-ttu-id="add6b-132">Ve içerir `words[^2]` `words[^1]` .</span><span class="sxs-lookup"><span data-stu-id="add6b-132">It includes `words[^2]` and `words[^1]`.</span></span> <span data-ttu-id="add6b-133">Son dizin `words[^0]` dahil değildir.</span><span class="sxs-lookup"><span data-stu-id="add6b-133">The end index `words[^0]` isn't included.</span></span> <span data-ttu-id="add6b-134">Aşağıdaki kodu da ekleyin:</span><span class="sxs-lookup"><span data-stu-id="add6b-134">Add the following code as well:</span></span>

[!code-csharp[LastRange](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_LastRange)]

<span data-ttu-id="add6b-135">Aşağıdaki örnekler, başlangıç, bitiş veya her ikisi için açık olarak biten aralıklar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="add6b-135">The following examples create ranges that are open ended for the start, end, or both:</span></span>

[!code-csharp[PartialRange](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_PartialRanges)]

<span data-ttu-id="add6b-136">Ayrıca, aralıkları veya dizinleri değişken olarak da bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="add6b-136">You can also declare ranges or indices as variables.</span></span> <span data-ttu-id="add6b-137">Değişken daha sonra `[` ve karakterleri içinde kullanılabilir `]` :</span><span class="sxs-lookup"><span data-stu-id="add6b-137">The variable can then be used inside the `[` and `]` characters:</span></span>

[!code-csharp[IndexRangeTypes](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_RangeIndexTypes)]

<span data-ttu-id="add6b-138">Aşağıdaki örnekte, bu seçimlerin pek çok nedeni gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="add6b-138">The following sample shows many of the reasons for those choices.</span></span> <span data-ttu-id="add6b-139">`x` `y` `z` Farklı birleşimler denemek için, ve değiştirin.</span><span class="sxs-lookup"><span data-stu-id="add6b-139">Modify `x`, `y`, and `z` to try different combinations.</span></span> <span data-ttu-id="add6b-140">Denemeler yaptığınızda, `x` ' den küçük olan değerleri kullanın `y` ve `y` `z` geçerli kombinasyonlardan daha küçüktür.</span><span class="sxs-lookup"><span data-stu-id="add6b-140">When you experiment, use values where `x` is less than `y`, and `y` is less than `z` for valid combinations.</span></span> <span data-ttu-id="add6b-141">Aşağıdaki kodu yeni bir yöntemine ekleyin.</span><span class="sxs-lookup"><span data-stu-id="add6b-141">Add the following code in a new method.</span></span> <span data-ttu-id="add6b-142">Farklı birleşimler deneyin:</span><span class="sxs-lookup"><span data-stu-id="add6b-142">Try different combinations:</span></span>

[!code-csharp[SemanticsExamples](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_Semantics)]

## <a name="type-support-for-indices-and-ranges"></a><span data-ttu-id="add6b-143">Dizinler ve aralıklar için tür desteği</span><span class="sxs-lookup"><span data-stu-id="add6b-143">Type support for indices and ranges</span></span>

<span data-ttu-id="add6b-144">Dizinler ve aralıklar, tek bir öğeye veya dizideki bir dizi öğeye erişmek için net, kısa sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="add6b-144">Indexes and ranges provide clear, concise syntax to access a single element or a range of elements in a sequence.</span></span> <span data-ttu-id="add6b-145">Bir dizin ifadesi genellikle bir dizinin öğelerinin türünü döndürür.</span><span class="sxs-lookup"><span data-stu-id="add6b-145">An index expression typically returns the type of the elements of a sequence.</span></span> <span data-ttu-id="add6b-146">Bir Aralık ifadesi genellikle kaynak dizisiyle aynı sıra türünü döndürür.</span><span class="sxs-lookup"><span data-stu-id="add6b-146">A range expression typically returns the same sequence type as the source sequence.</span></span>

<span data-ttu-id="add6b-147">Or parametresi olan bir [Dizin Oluşturucu](../programming-guide/indexers/index.md) sağlayan herhangi bir tür, <xref:System.Index> <xref:System.Range> sırasıyla dizin veya aralıkları açıkça destekler.</span><span class="sxs-lookup"><span data-stu-id="add6b-147">Any type that provides an [indexer](../programming-guide/indexers/index.md) with an <xref:System.Index> or <xref:System.Range> parameter explicitly supports indices or ranges respectively.</span></span> <span data-ttu-id="add6b-148">Tek bir parametre alan bir Dizin Oluşturucu <xref:System.Range> , gibi farklı bir dizi türü döndürebilir <xref:System.Span%601?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="add6b-148">An indexer that takes a single <xref:System.Range> parameter may return a different sequence type, such as <xref:System.Span%601?displayProperty=nameWithType>.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="add6b-149">Aralık işleci kullanılarak kodun performansı, dizi işleneninin türüne bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="add6b-149">The performance of code using the range operator depends on the type of the sequence operand.</span></span>
>
> <span data-ttu-id="add6b-150">Aralık işlecinin zaman karmaşıklığı, dizi türüne bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="add6b-150">The time complexity of the range operator depends on the sequence type.</span></span> <span data-ttu-id="add6b-151">Örneğin, dizi bir `string` veya bir diziyse, sonuç girişin belirtilen bölümünün bir kopyasıdır, bu nedenle zaman karmaşıklığı O kadar ( *n)* olur (burada n aralığın uzunluğudur).</span><span class="sxs-lookup"><span data-stu-id="add6b-151">For example, if the sequence is a `string` or an array, then the result is a copy of the specified section of the input, so the time complexity is *O(N)* (where N is the length of the range).</span></span> <span data-ttu-id="add6b-152">Diğer taraftan, bir veya ise, <xref:System.Span%601?displayProperty=nameWithType> <xref:System.Memory%601?displayProperty=nameWithType> sonuç aynı yedekleme deposuna başvurur. Bu, kopya olmadığı ve işlemin *O (1)* olduğu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="add6b-152">On the other hand, if it's a <xref:System.Span%601?displayProperty=nameWithType> or a <xref:System.Memory%601?displayProperty=nameWithType>, the result references the same backing store, which means there is no copy and the operation is *O(1)*.</span></span>
>
> <span data-ttu-id="add6b-153">Zaman karmaşıklığına ek olarak, bu, ek ayırmaya ve kopyalara neden olur ve performansı etkiler.</span><span class="sxs-lookup"><span data-stu-id="add6b-153">In addition to the time complexity, this causes extra allocations and copies, impacting performance.</span></span> <span data-ttu-id="add6b-154">Performans duyarlı kodda, `Span<T>` `Memory<T>` Aralık operatörü kendileri için ayrılmadığından, dizi türü olarak veya kullanmayı düşünün.</span><span class="sxs-lookup"><span data-stu-id="add6b-154">In performance sensitive code, consider using `Span<T>` or `Memory<T>` as the sequence type, since the range operator does not allocate for them.</span></span>

<span data-ttu-id="add6b-155">Bir **tür,** erişilebilir bir `Length` alıcı veya bir `Count` dönüş türü olan veya adında bir özelliğe sahipse bir tür `int` .</span><span class="sxs-lookup"><span data-stu-id="add6b-155">A type is **countable** if it has a property named `Length` or `Count` with an accessible getter and a return type of `int`.</span></span> <span data-ttu-id="add6b-156">Dizinleri veya aralıkları açıkça desteklemeyen bir sayılabilir türü, bunlar için örtülü bir destek sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="add6b-156">A countable type that doesn't explicitly support indices or ranges may provide an implicit support for them.</span></span> <span data-ttu-id="add6b-157">Daha fazla bilgi için, [özellik teklifi notunun](~/_csharplang/proposals/csharp-8.0/ranges.md) [örtük Dizin desteği](~/_csharplang/proposals/csharp-8.0/ranges.md#implicit-index-support) ve [örtük Aralık desteği](~/_csharplang/proposals/csharp-8.0/ranges.md#implicit-range-support) bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="add6b-157">For more information, see the [Implicit Index support](~/_csharplang/proposals/csharp-8.0/ranges.md#implicit-index-support) and [Implicit Range support](~/_csharplang/proposals/csharp-8.0/ranges.md#implicit-range-support) sections of the [feature proposal note](~/_csharplang/proposals/csharp-8.0/ranges.md).</span></span> <span data-ttu-id="add6b-158">Örtük Aralık desteği kullanan aralıklar, kaynak dizisiyle aynı sıra türünü döndürür.</span><span class="sxs-lookup"><span data-stu-id="add6b-158">Ranges using implicit range support return the same sequence type as the source sequence.</span></span>

<span data-ttu-id="add6b-159">Örneğin, aşağıdaki .NET türleri hem dizinleri hem de aralıkları destekler: <xref:System.String> , <xref:System.Span%601> ve <xref:System.ReadOnlySpan%601> .</span><span class="sxs-lookup"><span data-stu-id="add6b-159">For example, the following .NET types support both indices and ranges: <xref:System.String>, <xref:System.Span%601>, and <xref:System.ReadOnlySpan%601>.</span></span> <span data-ttu-id="add6b-160"><xref:System.Collections.Generic.List%601>Dizinleri destekler ancak aralıkları desteklemez.</span><span class="sxs-lookup"><span data-stu-id="add6b-160">The <xref:System.Collections.Generic.List%601> supports indices but doesn't support ranges.</span></span>

<span data-ttu-id="add6b-161"><xref:System.Array> daha fazla anormal davranışa sahiptir.</span><span class="sxs-lookup"><span data-stu-id="add6b-161"><xref:System.Array> has more nuanced behavior.</span></span> <span data-ttu-id="add6b-162">Tek boyutlu diziler hem dizinleri hem de aralıkları destekler.</span><span class="sxs-lookup"><span data-stu-id="add6b-162">Single dimension arrays support both indices and ranges.</span></span> <span data-ttu-id="add6b-163">Çok boyutlu diziler değildir.</span><span class="sxs-lookup"><span data-stu-id="add6b-163">Multi-dimensional arrays don't.</span></span> <span data-ttu-id="add6b-164">Çok boyutlu bir dizinin dizin oluşturucusunun tek bir parametre değil birden çok parametresi vardır.</span><span class="sxs-lookup"><span data-stu-id="add6b-164">The indexer for a multi-dimensional array has multiple parameters, not a single parameter.</span></span> <span data-ttu-id="add6b-165">Dizi dizileri olarak da adlandırılan pürüzlü Diziler, hem aralıkları hem de dizin oluşturucuyu destekler.</span><span class="sxs-lookup"><span data-stu-id="add6b-165">Jagged arrays, also referred to as an array of arrays, support both ranges and indexers.</span></span> <span data-ttu-id="add6b-166">Aşağıdaki örnek, pürüzlü bir dizinin dikdörtgen alt bölümünün nasıl yineyükleneceğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="add6b-166">The following example shows how to iterate a rectangular subsection of a jagged array.</span></span> <span data-ttu-id="add6b-167">İlk ve son üç satırı ve seçili her satırdaki ilk ve son iki sütunu dışlayarak ortadaki bölümü yineler:</span><span class="sxs-lookup"><span data-stu-id="add6b-167">It iterates the section in the center, excluding the first and last three rows, and the first and last two columns from each selected row:</span></span>

[!code-csharp[JaggedArrays](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_JaggedArrays)]

<span data-ttu-id="add6b-168">Her durumda, için Aralık işleci <xref:System.Array> döndürülen öğeleri depolamak için bir dizi ayırır.</span><span class="sxs-lookup"><span data-stu-id="add6b-168">In all cases, the range operator for <xref:System.Array> allocates an array to store the elements returned.</span></span>

## <a name="scenarios-for-indices-and-ranges"></a><span data-ttu-id="add6b-169">Dizinler ve aralıklar için senaryolar</span><span class="sxs-lookup"><span data-stu-id="add6b-169">Scenarios for indices and ranges</span></span>

<span data-ttu-id="add6b-170">Daha büyük bir dizinin bir bölümünü çözümlemek istediğinizde genellikle aralıklar ve dizinler kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="add6b-170">You'll often use ranges and indices when you want to analyze a portion of a larger sequence.</span></span> <span data-ttu-id="add6b-171">Yeni söz dizimi, sıranın tam olarak hangi kısmının dahil olduğunu okumaktan daha net bir şeydir.</span><span class="sxs-lookup"><span data-stu-id="add6b-171">The new syntax is clearer in reading exactly what portion of the sequence is involved.</span></span> <span data-ttu-id="add6b-172">Yerel işlev, `MovingAverage` <xref:System.Range> bağımsız değişkeni olarak bir kullanır.</span><span class="sxs-lookup"><span data-stu-id="add6b-172">The local function `MovingAverage` takes a <xref:System.Range> as its argument.</span></span> <span data-ttu-id="add6b-173">Bu yöntem daha sonra Min, Max ve Average hesaplama yaparken yalnızca o aralığı numaralandırır.</span><span class="sxs-lookup"><span data-stu-id="add6b-173">The method then enumerates just that range when calculating the min, max, and average.</span></span> <span data-ttu-id="add6b-174">Projenizde aşağıdaki kodu deneyin:</span><span class="sxs-lookup"><span data-stu-id="add6b-174">Try the following code in your project:</span></span>

[!code-csharp[MovingAverages](~/samples/snippets/csharp/tutorials/RangesIndexes/IndicesAndRanges.cs#IndicesAndRanges_MovingAverage)]
