---
title: Özel Durumları Kullanma - C# Programlama Kılavuzu
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: 4012027dc1a9bd2543d0a4195360e5f7e0586fe1
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "75705268"
---
# <a name="using-exceptions-c-programming-guide"></a><span data-ttu-id="d803b-102">Özel Durumlar Kullanma (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="d803b-102">Using Exceptions (C# Programming Guide)</span></span>
<span data-ttu-id="d803b-103">C#'da, programdaki hatalar çalışma zamanında özel durumlar adı verilen bir mekanizma kullanılarak program aracılığıyla yayılır.</span><span class="sxs-lookup"><span data-stu-id="d803b-103">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="d803b-104">Özel durumlar, bir hatayla karşılaşan ve hatayı düzeltebilecek kod tarafından yakalanan kod tarafından atılır.</span><span class="sxs-lookup"><span data-stu-id="d803b-104">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="d803b-105">Özel durumlar .NET Framework ortak dil çalışma zamanı (CLR) veya bir programdaki kod tarafından atılabilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-105">Exceptions can be thrown by the .NET Framework common language runtime (CLR) or by code in a program.</span></span> <span data-ttu-id="d803b-106">Bir özel durum atıldığında, özel durum `catch` bildirimi bulunana kadar çağrı yığınını yayır.</span><span class="sxs-lookup"><span data-stu-id="d803b-106">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="d803b-107">Yakalanmayan özel durumlar, iletişim kutusu görüntüleyen sistem tarafından sağlanan genel bir özel durum işleyicisi tarafından işlenir.</span><span class="sxs-lookup"><span data-stu-id="d803b-107">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="d803b-108">Özel durumlar, 'den <xref:System.Exception>türetilen sınıflar tarafından temsil edilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-108">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="d803b-109">Bu sınıf özel durum türünü tanımlar ve özel durum la ilgili ayrıntıları olan özellikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="d803b-109">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="d803b-110">Özel durum atma, özel durum türetilmiş bir sınıfın bir örneğini oluşturmayı, isteğe `throw` bağlı olarak özel durum özelliklerini yapılandırmayı ve ardından anahtar kelimeyi kullanarak nesneyi atmayı içerir.</span><span class="sxs-lookup"><span data-stu-id="d803b-110">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="d803b-111">Örnek:</span><span class="sxs-lookup"><span data-stu-id="d803b-111">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#1)]  
  
 <span data-ttu-id="d803b-112">Bir özel durum atıldıktan sonra, çalışma zamanı bir `try` blok içinde olup olmadığını görmek için geçerli deyimi denetler.</span><span class="sxs-lookup"><span data-stu-id="d803b-112">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="d803b-113">Bu ysa, `catch` `try` özel durumu yakalayıp yakalayamayacaklarını görmek için blokla ilişkili bloklar denetlenir.</span><span class="sxs-lookup"><span data-stu-id="d803b-113">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="d803b-114">`Catch`bloklar genellikle özel durum türlerini belirtir; `catch` bloğun türü özel durumla aynı türdeyse veya özel durum taban `catch` sınıfıysa, blok yöntemi işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-114">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="d803b-115">Örnek:</span><span class="sxs-lookup"><span data-stu-id="d803b-115">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#2)]  
  
 <span data-ttu-id="d803b-116">Özel durum atan deyim `try` bir blok içinde değilse `try` veya içine giren bloğun eşleşen `catch` bir bloğu yoksa, çalışma zamanı `try` deyimi `catch` ve blokları için arama yöntemini denetler.</span><span class="sxs-lookup"><span data-stu-id="d803b-116">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="d803b-117">Çalışma süresi, uyumlu `catch` bir blok aramak için arama yığınına kadar devam ediyor.</span><span class="sxs-lookup"><span data-stu-id="d803b-117">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="d803b-118">`catch` Blok bulunduktan ve yürütüldükten sonra, denetim `catch` bu bloktan sonra bir sonraki ifadeye geçirilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-118">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="d803b-119">Bir `try` deyim birden `catch` fazla blok içerebilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-119">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="d803b-120">Özel `catch` durum işleyebilir ilk deyimi yürütülür; uyumlu `catch` olsalar bile aşağıdaki ifadeler göz ardı edilir.</span><span class="sxs-lookup"><span data-stu-id="d803b-120">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="d803b-121">Bu nedenle, catch blokları her zaman en özel (veya en türetilmiş) en az özel sıralanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="d803b-121">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="d803b-122">Örnek:</span><span class="sxs-lookup"><span data-stu-id="d803b-122">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#3)]  
  
 <span data-ttu-id="d803b-123">Blok `catch` yürütülmeden önce, çalışma `finally` zamanı blokları denetler.</span><span class="sxs-lookup"><span data-stu-id="d803b-123">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="d803b-124">`Finally`bloklar, programcının iptal edilmiş `try` bir bloktan arta kalan herhangi bir belirsiz durumu temizlemesini veya nesneleri sonuçlandırmak için çalışma zamanında çöp toplayıcısını beklemeden herhangi bir dış kaynağı (grafik tutamaçları, veritabanı bağlantıları veya dosya akışları gibi) serbest bırakmasına olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="d803b-124">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="d803b-125">Örnek:</span><span class="sxs-lookup"><span data-stu-id="d803b-125">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#4)]  
  
 <span data-ttu-id="d803b-126">Bir `WriteByte()` özel durum atılırsa, `try` dosyayı yeniden açmaya çalışan ikinci `file.Close()` bloktaki kod çağrılmazsa başarısız olur ve dosya kilitli kalır.</span><span class="sxs-lookup"><span data-stu-id="d803b-126">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="d803b-127">Bir `finally` özel durum atılsa bile `finally` bloklar yürütüldeğinden, önceki örnekteki blok dosyanın doğru şekilde kapatılmasına izin verir ve bir hatayı önlemeye yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="d803b-127">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="d803b-128">Özel durum `catch` atıldıktan sonra çağrı yığınında uyumlu bir blok bulunamazsa, üç şeyden biri oluşur:</span><span class="sxs-lookup"><span data-stu-id="d803b-128">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
- <span data-ttu-id="d803b-129">İstisna bir sonlandırıcı içinde yse, sonlandırıcı iptal edilir ve varsa temel sonlandırıcı çağrılır.</span><span class="sxs-lookup"><span data-stu-id="d803b-129">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
- <span data-ttu-id="d803b-130">Çağrı yığını statik bir oluşturucu veya statik alan baş <xref:System.TypeInitializationException> harflerini içeriyorsa, yeni <xref:System.Exception.InnerException%2A> özel durum özelliğine atanan özgün özel durumla birlikte bir atılmıştır.</span><span class="sxs-lookup"><span data-stu-id="d803b-130">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
- <span data-ttu-id="d803b-131">İş parçacığının başlangıcına ulaşılırsa, iş parçacığı sonlandırılır.</span><span class="sxs-lookup"><span data-stu-id="d803b-131">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d803b-132">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="d803b-132">See also</span></span>

- [<span data-ttu-id="d803b-133">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="d803b-133">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="d803b-134">Özel Durumlar ve Özel Durum Kullanımı</span><span class="sxs-lookup"><span data-stu-id="d803b-134">Exceptions and Exception Handling</span></span>](./index.md)
