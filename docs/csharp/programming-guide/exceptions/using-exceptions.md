---
title: Özel durumlar kullanma-C# Programlama Kılavuzu
description: Özel durumları nasıl kullanacağınızı öğrenin. Özel durumlar, bir hatayla karşılaştığında ve hatayı düzelten kodla yakalanan kodla oluşturulur.
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: fb45381f1c6cfa2f27d036ead8e662b7a0d8d924
ms.sourcegitcommit: 6f58a5f75ceeb936f8ee5b786e9adb81a9a3bee9
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 07/28/2020
ms.locfileid: "87303380"
---
# <a name="use-exceptions-c-programming-guide"></a><span data-ttu-id="6c957-104">Özel durumlar kullanma (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="6c957-104">Use exceptions (C# programming guide)</span></span>

<span data-ttu-id="6c957-105">C# ' de, çalışma zamanında programdaki hatalar, özel durumlar adlı bir mekanizma kullanılarak program aracılığıyla dağıtılır.</span><span class="sxs-lookup"><span data-stu-id="6c957-105">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="6c957-106">Özel durumlar, hata ile karşılaştığında ve hatayı düzeltebileceğiniz kodla yakalanan kodla oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="6c957-106">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="6c957-107">Özel durumlar .NET çalışma zamanı veya bir programdaki kodla oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="6c957-107">Exceptions can be thrown by the .NET runtime or by code in a program.</span></span> <span data-ttu-id="6c957-108">Bir özel durum oluşturulduktan sonra, özel durum için bir ifade bulunana kadar çağrı yığınını yayar `catch` .</span><span class="sxs-lookup"><span data-stu-id="6c957-108">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="6c957-109">Yakalanamayan özel durumlar, bir iletişim kutusu görüntüleyen sistem tarafından sunulan genel bir özel durum işleyicisi tarafından işlenir.</span><span class="sxs-lookup"><span data-stu-id="6c957-109">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="6c957-110">Özel durumlar, öğesinden türetilmiş sınıflar tarafından temsil edilir <xref:System.Exception> .</span><span class="sxs-lookup"><span data-stu-id="6c957-110">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="6c957-111">Bu sınıf, özel durum türünü tanımlar ve özel durumla ilgili ayrıntıları içeren özellikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="6c957-111">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="6c957-112">Özel durum oluşturmak, özel durum türetilmiş sınıfın bir örneğini oluşturmayı, isteğe bağlı olarak özel durumun özelliklerini yapılandırmayı ve sonra anahtar sözcüğünü kullanarak nesneyi oluşturmayı içerir `throw` .</span><span class="sxs-lookup"><span data-stu-id="6c957-112">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="6c957-113">Örnek:</span><span class="sxs-lookup"><span data-stu-id="6c957-113">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#1)]  
  
 <span data-ttu-id="6c957-114">Bir özel durum oluşturulduktan sonra, çalışma zamanı bir blok içinde olup olmadığını görmek için geçerli ifadeyi denetler `try` .</span><span class="sxs-lookup"><span data-stu-id="6c957-114">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="6c957-115">Eğer ise, `catch` bloğuyla ilişkili herhangi bir blok, `try` özel durumu yakalayıp yakalayamayacağını görmek için denetlenir.</span><span class="sxs-lookup"><span data-stu-id="6c957-115">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="6c957-116">`Catch`bloklar genellikle özel durum türlerini belirtir; `catch`bloğun türü özel durumla aynı türde veya özel durumun temel bir sınıfı ise, `catch` blok yöntemi işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="6c957-116">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="6c957-117">Örnek:</span><span class="sxs-lookup"><span data-stu-id="6c957-117">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#2)]  
  
 <span data-ttu-id="6c957-118">Bir özel durum oluşturan ifade bir blok içinde değilse `try` veya `try` kendisini kapsayan blok eşleşen bir `catch` blok içindeyse, çalışma zamanı bir `try` deyimin ve blokların çağırma yöntemini denetler `catch` .</span><span class="sxs-lookup"><span data-stu-id="6c957-118">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="6c957-119">Çalışma zamanı, arama yığınını devam ettirir ve uyumlu bir blok arar `catch` .</span><span class="sxs-lookup"><span data-stu-id="6c957-119">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="6c957-120">Blok bulduktan `catch` ve yürütüldükten sonra, denetim bu blok sonrasında sonraki ifadeye geçirilir `catch` .</span><span class="sxs-lookup"><span data-stu-id="6c957-120">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="6c957-121">Bir `try` ifade, birden fazla blok içerebilir `catch` .</span><span class="sxs-lookup"><span data-stu-id="6c957-121">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="6c957-122">`catch`Özel durumu işleyebilen ilk deyim yürütülür; `catch` uyumlu olsalar bile aşağıdaki deyimler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="6c957-122">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="6c957-123">Bu nedenle, catch blokları her zaman en belirli (veya en çok türetilen) en azından belirli bir şekilde sıralanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="6c957-123">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="6c957-124">Örnek:</span><span class="sxs-lookup"><span data-stu-id="6c957-124">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#3)]  
  
 <span data-ttu-id="6c957-125">`catch`Blok yürütülmeden önce, çalışma zamanı `finally` blokları denetler.</span><span class="sxs-lookup"><span data-stu-id="6c957-125">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="6c957-126">`Finally`bloklar, programcı 'nin durdurulmuş bir `try` bloktan veya herhangi bir dış kaynağın (grafik tutamaçları, veritabanı bağlantıları veya dosya akışları gibi), çalışma zamanındaki çöp toplayıcısının nesneleri sonlandırmasına gerek kalmadan serbest bırakılmış olan herhangi bir durumu temizlemesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="6c957-126">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="6c957-127">Örnek:</span><span class="sxs-lookup"><span data-stu-id="6c957-127">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#4)]  
  
 <span data-ttu-id="6c957-128">`WriteByte()`Bir özel durum, `try` çağrılırsa dosyayı yeniden açmaya çalışan ikinci bloktaki kod başarısız olur `file.Close()` ve dosya kilitli kalır.</span><span class="sxs-lookup"><span data-stu-id="6c957-128">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="6c957-129">`finally`Bir özel durum oluşsa bile bloklar yürütüldüğü `finally` için, önceki örnekteki blok dosyanın düzgün şekilde kapatılmasını sağlar ve bir hatadan kaçınmaya yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="6c957-129">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="6c957-130">`catch`Bir özel durum oluşturulduktan sonra çağrı yığınında uyumlu bir blok bulunmazsa, üç durumdan biri oluşur:</span><span class="sxs-lookup"><span data-stu-id="6c957-130">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
- <span data-ttu-id="6c957-131">Özel durum bir Sonlandırıcı içindeyse Sonlandırıcı iptal edilir ve varsa taban sonlandırıcısı çağırılır.</span><span class="sxs-lookup"><span data-stu-id="6c957-131">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
- <span data-ttu-id="6c957-132">Çağrı yığını statik bir Oluşturucu içeriyorsa veya bir statik alan başlatıcısı varsa, <xref:System.TypeInitializationException> özgün özel durum <xref:System.Exception.InnerException%2A> Yeni özel durumun özelliğine atanır.</span><span class="sxs-lookup"><span data-stu-id="6c957-132">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
- <span data-ttu-id="6c957-133">İş parçacığının başlangıcına ulaşıldığında, iş parçacığı sonlandırılır.</span><span class="sxs-lookup"><span data-stu-id="6c957-133">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6c957-134">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="6c957-134">See also</span></span>

- [<span data-ttu-id="6c957-135">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="6c957-135">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="6c957-136">Özel durumlar ve özel durum Işleme</span><span class="sxs-lookup"><span data-stu-id="6c957-136">Exceptions and Exception Handling</span></span>](./index.md)
