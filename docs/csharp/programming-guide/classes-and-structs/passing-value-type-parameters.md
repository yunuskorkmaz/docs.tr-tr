---
title: Değer türü parametrelerini geçirme-C# Programlama Kılavuzu
description: C# ' deki değere göre bir yönteme değer türü değişkeni geçirdiğinizde, tüm değişikliklerin özgün veriler üzerinde hiçbir etkisi olmaz. Değeri değiştirmek için başvuruya göre geçirin.
ms.date: 07/20/2015
helpviewer_keywords:
- method parameters [C#], value types
- parameters [C#], value
ms.assetid: 193ab86f-5f9b-4359-ac29-7cdf8afad3a6
ms.openlocfilehash: ccc97e2f6e8c6b2b8fd15206a8bcd5cd8a5a8431
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91186074"
---
# <a name="passing-value-type-parameters-c-programming-guide"></a><span data-ttu-id="2001e-104">Değer Türü Parametrelerini Geçirme (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="2001e-104">Passing Value-Type Parameters (C# Programming Guide)</span></span>

<span data-ttu-id="2001e-105">Bir [değer türü](../../language-reference/builtin-types/value-types.md) değişkeni, veri başvurusunu içeren bir [başvuru türü](../../language-reference/keywords/reference-types.md) değişkeni aksine doğrudan verilerini içerir.</span><span class="sxs-lookup"><span data-stu-id="2001e-105">A [value-type](../../language-reference/builtin-types/value-types.md) variable contains its data directly as opposed to a [reference-type](../../language-reference/keywords/reference-types.md) variable, which contains a reference to its data.</span></span> <span data-ttu-id="2001e-106">Değer türü bir değişkeni değere göre yönteme geçirmek, değişkenin bir kopyasının yöntemine geçirilmesi anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="2001e-106">Passing a value-type variable to a method by value means passing a copy of the variable to the method.</span></span> <span data-ttu-id="2001e-107">Yöntemin içinde gerçekleşen parametresindeki tüm değişiklikler, bağımsız değişken değişkeninde depolanan özgün veriler üzerinde hiçbir etkiye sahip değildir.</span><span class="sxs-lookup"><span data-stu-id="2001e-107">Any changes to the parameter that take place inside the method have no effect on the original data stored in the argument variable.</span></span> <span data-ttu-id="2001e-108">Çağrılan yöntemin bağımsız değişkenin değerini değiştirmesini istiyorsanız [ref](../../language-reference/keywords/ref.md) veya [Out](../../language-reference/keywords/out-parameter-modifier.md) anahtar sözcüğünü kullanarak başvuruya göre geçirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="2001e-108">If you want the called method to change the value of the argument, you must pass it by reference, using the [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="2001e-109">Ayrıca, değerin değişmediğinden emin olmak için bir değer parametresini başvuruya göre geçirmek için [ın](../../language-reference/keywords/in-parameter-modifier.md) anahtar sözcüğünü kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="2001e-109">You may also use the [in](../../language-reference/keywords/in-parameter-modifier.md) keyword to pass a value parameter by reference to avoid the copy while guaranteeing that the value will not be changed.</span></span> <span data-ttu-id="2001e-110">Basitlik için aşağıdaki örneklerde kullanılması gerekir `ref` .</span><span class="sxs-lookup"><span data-stu-id="2001e-110">For simplicity, the following examples use `ref`.</span></span>  
  
## <a name="passing-value-types-by-value"></a><span data-ttu-id="2001e-111">Değere göre değer türlerini geçirme</span><span class="sxs-lookup"><span data-stu-id="2001e-111">Passing Value Types by Value</span></span>  

 <span data-ttu-id="2001e-112">Aşağıdaki örnek değer türü parametrelerini değere göre geçirmeyi gösterir.</span><span class="sxs-lookup"><span data-stu-id="2001e-112">The following example demonstrates passing value-type parameters by value.</span></span> <span data-ttu-id="2001e-113">Değişken `n` değerine göre yöntemine geçirilir `SquareIt` .</span><span class="sxs-lookup"><span data-stu-id="2001e-113">The variable `n` is passed by value to the method `SquareIt`.</span></span> <span data-ttu-id="2001e-114">Yöntemin içinde gerçekleşen tüm değişikliklerin, değişkenin özgün değeri üzerinde hiçbir etkisi yoktur.</span><span class="sxs-lookup"><span data-stu-id="2001e-114">Any changes that take place inside the method have no effect on the original value of the variable.</span></span>  
  
 [!code-csharp[csProgGuideParameters#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#3)]  
  
 <span data-ttu-id="2001e-115">Değişken `n` bir değer türüdür.</span><span class="sxs-lookup"><span data-stu-id="2001e-115">The variable `n` is a value type.</span></span> <span data-ttu-id="2001e-116">Verileri, değerini içerir `5` .</span><span class="sxs-lookup"><span data-stu-id="2001e-116">It contains its data, the value `5`.</span></span> <span data-ttu-id="2001e-117">`SquareIt`Çağrıldığında, öğesinin içeriği `n` `x` , yönteminin içindeki kare içinde olan parametresine kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="2001e-117">When `SquareIt` is invoked, the contents of `n` are copied into the parameter `x`, which is squared inside the method.</span></span> <span data-ttu-id="2001e-118">`Main`Ancak içindeki değeri, `n` `SquareIt` yöntemi öncesindeki gibi çağrıldıktan sonra aynı olur.</span><span class="sxs-lookup"><span data-stu-id="2001e-118">In `Main`, however, the value of `n` is the same after calling the `SquareIt` method as it was before.</span></span> <span data-ttu-id="2001e-119">Metodun içinde yer alan değişiklik yalnızca yerel değişkeni etkiler `x` .</span><span class="sxs-lookup"><span data-stu-id="2001e-119">The change that takes place inside the method only affects the local variable `x`.</span></span>  
  
## <a name="passing-value-types-by-reference"></a><span data-ttu-id="2001e-120">Değer türlerini başvuruya göre geçirme</span><span class="sxs-lookup"><span data-stu-id="2001e-120">Passing Value Types by Reference</span></span>  

 <span data-ttu-id="2001e-121">Aşağıdaki örnek, bağımsız değişkenin parametre olarak geçirilmesi dışında, önceki örnekle aynıdır `ref` .</span><span class="sxs-lookup"><span data-stu-id="2001e-121">The following example is the same as the previous example, except that the argument is passed as a `ref` parameter.</span></span> <span data-ttu-id="2001e-122">Temel bağımsız değişkenin değeri, yönteminde değiştiğinde `n` değiştirilir `x` .</span><span class="sxs-lookup"><span data-stu-id="2001e-122">The value of the underlying argument, `n`, is changed when `x` is changed in the method.</span></span>  
  
 [!code-csharp[csProgGuideParameters#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#4)]  
  
 <span data-ttu-id="2001e-123">Bu örnekte, geçirilen değeri değildir `n` ; bunun yerine, öğesine bir başvuru `n` geçirilir.</span><span class="sxs-lookup"><span data-stu-id="2001e-123">In this example, it is not the value of `n` that is passed; rather, a reference to `n` is passed.</span></span> <span data-ttu-id="2001e-124">Parametre `x` bir [int](../../language-reference/builtin-types/integral-numeric-types.md)değil; `int` Bu durumda öğesine bir başvuru `n` .</span><span class="sxs-lookup"><span data-stu-id="2001e-124">The parameter `x` is not an [int](../../language-reference/builtin-types/integral-numeric-types.md); it is a reference to an `int`, in this case, a reference to `n`.</span></span> <span data-ttu-id="2001e-125">Bu nedenle, `x` yöntemin içinde kare içinde olduğunda, ne zaman kare olarak ifade edilir `x` `n` .</span><span class="sxs-lookup"><span data-stu-id="2001e-125">Therefore, when `x` is squared inside the method, what actually is squared is what `x` refers to, `n`.</span></span>  
  
## <a name="swapping-value-types"></a><span data-ttu-id="2001e-126">Değer türlerini değiştirme</span><span class="sxs-lookup"><span data-stu-id="2001e-126">Swapping Value Types</span></span>  

 <span data-ttu-id="2001e-127">Bağımsız değişkenlerin değerlerini değiştirmenin yaygın bir örneği, yöntemine iki değişken geçirdiğiniz ve yöntemi içeriğini değiştiren bir takas yöntemidir.</span><span class="sxs-lookup"><span data-stu-id="2001e-127">A common example of changing the values of arguments is a swap method, where you pass two variables to the method, and the method swaps their contents.</span></span> <span data-ttu-id="2001e-128">Bağımsız değişkenleri başvuruya göre takas yöntemine geçirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="2001e-128">You must pass the arguments to the swap method by reference.</span></span> <span data-ttu-id="2001e-129">Aksi takdirde, yöntemin içindeki yerel kopyaları değiştirirsiniz ve çağırma yönteminde hiçbir değişiklik gerçekleşmez.</span><span class="sxs-lookup"><span data-stu-id="2001e-129">Otherwise, you swap local copies of the parameters inside the method, and no change occurs in the calling method.</span></span> <span data-ttu-id="2001e-130">Aşağıdaki örnek, tamsayı değerlerini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="2001e-130">The following example swaps integer values.</span></span>  
  
 [!code-csharp[csProgGuideParameters#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#5)]  
  
 <span data-ttu-id="2001e-131">`SwapByRef`Yöntemini çağırdığınızda, `ref` Aşağıdaki örnekte gösterildiği gibi, çağrısındaki anahtar sözcüğünü kullanın.</span><span class="sxs-lookup"><span data-stu-id="2001e-131">When you call the `SwapByRef` method, use the `ref` keyword in the call, as shown in the following example.</span></span>  
  
 [!code-csharp[csProgGuideParameters#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#6)]  
  
## <a name="see-also"></a><span data-ttu-id="2001e-132">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="2001e-132">See also</span></span>

- [<span data-ttu-id="2001e-133">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="2001e-133">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="2001e-134">Parametreleri geçirme</span><span class="sxs-lookup"><span data-stu-id="2001e-134">Passing Parameters</span></span>](./passing-parameters.md)
- [<span data-ttu-id="2001e-135">Başvuru Türü Parametreleri Geçirme</span><span class="sxs-lookup"><span data-stu-id="2001e-135">Passing Reference-Type Parameters</span></span>](./passing-reference-type-parameters.md)
