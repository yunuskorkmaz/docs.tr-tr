---
title: Async İade Türleri (C#)
ms.date: 05/29/2017
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 9926fea5308f9088ad924bcc98d8deed319c6300
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79170045"
---
# <a name="async-return-types-c"></a><span data-ttu-id="cbfa5-102">Async İade Türleri (C#)</span><span class="sxs-lookup"><span data-stu-id="cbfa5-102">Async Return Types (C#)</span></span>
<span data-ttu-id="cbfa5-103">Async yöntemleri aşağıdaki iade türlerine sahip olabilir:</span><span class="sxs-lookup"><span data-stu-id="cbfa5-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="cbfa5-104"><xref:System.Threading.Tasks.Task%601>, bir değer döndüren bir async yöntemi için.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>

- <span data-ttu-id="cbfa5-105"><xref:System.Threading.Tasks.Task>, bir işlemi gerçekleştiren ancak değer döndüren bir async yöntemi için.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>

- <span data-ttu-id="cbfa5-106">`void`, bir olay işleyicisi için.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-106">`void`, for an event handler.</span></span>

- <span data-ttu-id="cbfa5-107">C# 7.0 ile başlayarak, erişilebilir `GetAwaiter` bir yönteme sahip herhangi bir tür.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="cbfa5-108">`GetAwaiter` Yöntem tarafından döndürülen nesne <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> arabirimi uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
  
<span data-ttu-id="cbfa5-109">Async yöntemleri hakkında daha fazla bilgi için, [async ile Asynchronous Programming'e bakın ve (C#) bekleyin.](./index.md)</span><span class="sxs-lookup"><span data-stu-id="cbfa5-109">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
<span data-ttu-id="cbfa5-110">Her dönüş türü aşağıdaki bölümlerden birinde incelenir ve konunun sonunda üç türü de kullanan tam bir örnek bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-110">Each return type is examined in one of the following sections, and you can find a full example that uses all three types at the end of the topic.</span></span>  
  
## <a name="BKMK_TaskTReturnType"></a><span data-ttu-id="cbfa5-111">Görev\<Sonuç\> İade Türü</span><span class="sxs-lookup"><span data-stu-id="cbfa5-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="cbfa5-112">İade <xref:System.Threading.Tasks.Task%601> türü, operand'ın türüne [return](../../../language-reference/keywords/return.md) `TResult`sahip olduğu bir return (C#) deyimini içeren bir async yöntemi için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand has type `TResult`.</span></span>  
  
<span data-ttu-id="cbfa5-113">Aşağıdaki örnekte, `GetLeisureHours` async yöntemi `return` bir tamsayı döndüren bir deyim içerir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="cbfa5-114">Bu nedenle, yöntem bildirimi bir `Task<int>`dönüş türü belirtmelidir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="cbfa5-115">Async <xref:System.Threading.Tasks.Task.FromResult%2A> yöntemi, dize döndüren bir işlem için bir yer tutucudur.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1.cs)]

<span data-ttu-id="cbfa5-116">Yöntemdeki bekleyen ifadesinin içinden `GetLeisureHours` çağrıldığında, bekleyen ifade `GetLeisureHours` yöntem tarafından döndürülen `leisureHours`görevde depolanan hesaplaç değerini (değeri) alır. `ShowTodaysInfo`</span><span class="sxs-lookup"><span data-stu-id="cbfa5-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="cbfa5-117">Bekleyen ifadeler hakkında daha fazla bilgi için, [bkz.](../../../language-reference/operators/await.md)</span><span class="sxs-lookup"><span data-stu-id="cbfa5-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="cbfa5-118">Aşağıdaki kodun gösterdiği `GetLeisureHours` `await`gibi, aramayı uygulamadan ayırarak bunun nasıl olduğunu daha iyi anlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-118">You can better understand how this happens by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="cbfa5-119">Hemen beklenmeyen `GetLeisureHours` yöntem çağrısı, yöntemin bildiriminden beklediğiniz gibi bir `Task<int>`döndürür.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="cbfa5-120">Görev örnekteki `integerTask` değişkene atanır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="cbfa5-121">Çünkü `integerTask` bir <xref:System.Threading.Tasks.Task%601>, bu <xref:System.Threading.Tasks.Task%601.Result> tür `TResult`bir özellik içerir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="cbfa5-122">Bu durumda, `TResult` bir tamsayı türünü temsil eder.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="cbfa5-123">Uygulandığında `await` `integerTask`, bekleme ifadesi <xref:System.Threading.Tasks.Task%601.Result%2A> özelliğinin içeriğini `integerTask`değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="cbfa5-124">Değer `ret` değişkene atanır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="cbfa5-125">Tesis, <xref:System.Threading.Tasks.Task%601.Result%2A> engelleyici bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="cbfa5-126">Görevi tamamlanmadan önce erişmeye çalışırsanız, görev tamamlanana ve değer kullanılabilir olana kadar şu anda etkin olan iş parçacığı engellenir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="cbfa5-127">Çoğu durumda, doğrudan özelliğine erişmek `await` yerine kullanarak değere erişmelisiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="cbfa5-128">Önceki örnek, <xref:System.Threading.Tasks.Task%601.Result%2A> `ShowTodaysInfo` yöntemin uygulama sona ermeden önce yürütmeyi bitirebilmeleri için ana iş parçacığı engellemek için özelliğin değerini aldı.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1a.cs#1)]
  
## <a name="BKMK_TaskReturnType"></a><span data-ttu-id="cbfa5-129">Görev İade Türü</span><span class="sxs-lookup"><span data-stu-id="cbfa5-129">Task Return Type</span></span>  
<span data-ttu-id="cbfa5-130">İfade `return` içermeyen veya oper return olmayan ve `return` genellikle bir dönüş türüne sahip bir deyim <xref:System.Threading.Tasks.Task>içermeyen async yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="cbfa5-131">Bu tür `void` yöntemler eşzamanlı çalıştırılırsa geri döner.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="cbfa5-132">Async yöntemi <xref:System.Threading.Tasks.Task> için bir iade türü kullanıyorsanız, bir `await` arama yöntemi çağrılan async yöntemi tamamlanana kadar arayanın tamamlanmasını askıya almak için bir işleç kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="cbfa5-133">Aşağıdaki örnekte, `WaitAndApologize` async yöntemi bir `return` deyim içermez, bu nedenle <xref:System.Threading.Tasks.Task> yöntem bir nesne döndürür.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="cbfa5-134">`WaitAndApologize` Bu beklenen sağlar.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-134">This enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="cbfa5-135">İade değeri <xref:System.Threading.Tasks.Task> olmadığından türün `Result` bir özellik içermediğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-135">Note that the <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2.cs)]  
  
<span data-ttu-id="cbfa5-136">`WaitAndApologize`senkron boşluk döndürme yöntemi için çağrı deyimine benzer bir bekleme ifadesi yerine bir bekleme ifadesi kullanılarak beklenen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="cbfa5-137">Bu durumda bekleyen bir işleç uygulaması bir değer üretmez.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="cbfa5-138">Önceki <xref:System.Threading.Tasks.Task%601> örnekte olduğu gibi, aşağıdaki `WaitAndApologize` kodun gösterdiği gibi, aramayı bekleyen bir işlecinin uygulamasından ayırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="cbfa5-139">Ancak, a'nın `Task` bir `Result` özelliği olmadığını ve bekleyen bir işleç bir `Task`.'e uygulandığında hiçbir değer inanMadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="cbfa5-140">Aşağıdaki kod, `WaitAndApologize` metod çağırma yöntemini yöntemin döndürdettiği görevi beklemekten ayırır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2a.cs#1)]  

## <a name="BKMK_VoidReturnType"></a><span data-ttu-id="cbfa5-141">Geçersiz dönüş türü</span><span class="sxs-lookup"><span data-stu-id="cbfa5-141">Void return type</span></span>

<span data-ttu-id="cbfa5-142">`void` İade türünü, iade türü gerektiren eşsenkronize olay işleyicilerinde kullanırsınız. `void`</span><span class="sxs-lookup"><span data-stu-id="cbfa5-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="cbfa5-143">Bir değer döndürmeyen olay işleyicileri dışındaki yöntemler için, <xref:System.Threading.Tasks.Task> döndüren `void` bir async yöntemi beklenemediği için bunun yerine bir döndürmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="cbfa5-144">Böyle bir yöntemin herhangi bir arayan, çağrılan async yönteminin tamamlanmasını beklemeden tamamlamaya devam edebilmeli ve arayanın async yönteminin oluşturduğu değerlerden veya özel durumlardan bağımsız olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-144">Any caller of such a method must be able to continue to completion without waiting for the called async method to finish, and the caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="cbfa5-145">Geçersiz döndüren bir async yöntemini arayan yöntemden atılan özel durumları yakalayamaz ve bu tür işlenmemiş özel durumlar uygulamanızın başarısız olması muhtemeldir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-145">The caller of a void-returning async method can't catch exceptions that are thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="cbfa5-146">Bir özel durum a veya <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601>döndüren bir özel durum yönteminde oluşursa, özel durum döndürülen görevde depolanır ve görev baran yeniden atılır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-146">If an exception occurs in an async method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, the exception is stored in the returned task and is rethrown when the task is awaited.</span></span> <span data-ttu-id="cbfa5-147">Bu nedenle, bir özel durum oluşturabilen herhangi bir <xref:System.Threading.Tasks.Task> async yönteminin bir dönüş türü olduğundan veya <xref:System.Threading.Tasks.Task%601> yönteme çağrı beklediğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-147">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="cbfa5-148">Async yöntemlerindeki özel durumları nasıl yakalayacakları hakkında daha fazla bilgi için, [try-catch](../../../language-reference/keywords/try-catch.md) konusunun [Async Yöntemleri](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) bölümündeki Özel Durumlar bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-148">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) topic.</span></span>  
  
<span data-ttu-id="cbfa5-149">Aşağıdaki örnek, bir async olay işleyicisi davranışını gösterir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-149">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="cbfa5-150">Örnek kodda, bir async olay işleyicisinin ana iş parçacığının ne zaman bitacağını bilmesi gerektiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-150">Note that in the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="cbfa5-151">Daha sonra ana iş parçacığı programdan çıkmadan önce tamamlanması için bir async olay işleyicisi için bekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-151">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns3.cs)]  

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="cbfa5-152">Genelleştirilmiş async dönüş türleri\<ve ValueTask TResult\></span><span class="sxs-lookup"><span data-stu-id="cbfa5-152">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="cbfa5-153">C# 7.0 ile başlayarak, bir async yöntemi `GetAwaiter` erişilebilir bir yöntem eki olan herhangi bir türü döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-153">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="cbfa5-154">Başvuru <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> türleri olduğundan ve kaynak türleri olduğundan, performans açısından kritik yollarda bellek ayırma, özellikle de ayırmalar sıkı döngülerde gerçekleştiğinde, performansı olumsuz etkileyebilir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-154">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="cbfa5-155">Genelleştirilmiş iade türleri için destek, ek bellek ayırmalarını önlemek için başvuru türü yerine hafif bir değer türü döndürebileceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-155">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="cbfa5-156">.NET, <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> genelleştirilmiş görev döndüren değerin hafif bir uygulaması olarak yapıyı sağlar.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-156">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="cbfa5-157"><xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> Türü kullanmak için Projenize `System.Threading.Tasks.Extensions` NuGet paketini eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-157">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="cbfa5-158">Aşağıdaki örnek, <xref:System.Threading.Tasks.ValueTask%601> iki zar rulo değerini almak için yapıyı kullanır.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-158">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-valuetask.cs)]

## <a name="see-also"></a><span data-ttu-id="cbfa5-159">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="cbfa5-159">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="cbfa5-160">Walkthrough: Async kullanarak Web'e erişim ve bekleme (C#)</span><span class="sxs-lookup"><span data-stu-id="cbfa5-160">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="cbfa5-161">Async Programlarında Kontrol Akışı (C#)</span><span class="sxs-lookup"><span data-stu-id="cbfa5-161">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="cbfa5-162">async</span><span class="sxs-lookup"><span data-stu-id="cbfa5-162">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="cbfa5-163">await</span><span class="sxs-lookup"><span data-stu-id="cbfa5-163">await</span></span>](../../../language-reference/operators/await.md)
