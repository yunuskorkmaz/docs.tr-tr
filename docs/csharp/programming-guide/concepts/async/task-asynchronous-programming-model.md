---
title: Async ve await (C#) ile görev zaman uyumsuz programlama (TAP) modeli
description: Görev tabanlı zaman uyumsuz programlamayı ne zaman ve nasıl kullanacağınızı, C# ' de zaman uyumsuz programlamaya basitleştirilmiş bir yaklaşım olduğunu öğrenin.
ms.date: 08/19/2020
ms.assetid: 9bcf896a-5826-4189-8c1a-3e35fa08243a
ms.openlocfilehash: 1014e38dcb3e2c4f56c8b3f3dade9bdbff3abd27
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/15/2020
ms.locfileid: "90556043"
---
# <a name="task-asynchronous-programming-model"></a><span data-ttu-id="43cf4-103">Zaman uyumsuz görev programlama modeli</span><span class="sxs-lookup"><span data-stu-id="43cf4-103">Task asynchronous programming model</span></span>

<span data-ttu-id="43cf4-104">Zaman uyumsuz programlama kullanarak performans sorunlarını önleyebilir ve uygulamanızın genel yanıt verme becerisini geliştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-104">You can avoid performance bottlenecks and enhance the overall responsiveness of your application by using asynchronous programming.</span></span> <span data-ttu-id="43cf4-105">Ancak, zaman uyumsuz uygulamalar yazmaya yönelik geleneksel teknikler karmaşık olabilir ve bu nedenle yazılmaları, hataların ayıklanması ve bakım yapılması zorlaşabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-105">However, traditional techniques for writing asynchronous applications can be complicated, making them difficult to write, debug, and maintain.</span></span>

<span data-ttu-id="43cf4-106">[C# 5](../../../whats-new/csharp-version-history.md#c-version-50) , .NET Framework 4,5 ve üzeri, .NET Core ve Windows çalışma zamanı zaman uyumsuz destekten yararlanan, zaman uyumsuz programlama ile basitleştirilmiş bir yaklaşım getirmiştir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-106">[C# 5](../../../whats-new/csharp-version-history.md#c-version-50) introduced a simplified approach, async programming, that leverages asynchronous support in the .NET Framework 4.5 and higher, .NET Core, and the Windows Runtime.</span></span> <span data-ttu-id="43cf4-107">Derleyici, normalde geliştiricinin yaptığı zor işi yapar ve uygulamanız zaman uyumlu koda benzer bir mantıksal yapıyı korur.</span><span class="sxs-lookup"><span data-stu-id="43cf4-107">The compiler does the difficult work that the developer used to do, and your application retains a logical structure that resembles synchronous code.</span></span> <span data-ttu-id="43cf4-108">Sonuç olarak, zaman uyumsuz programlama avantajlarının tamamını çok daha az çaba harcayarak elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-108">As a result, you get all the advantages of asynchronous programming with a fraction of the effort.</span></span>

<span data-ttu-id="43cf4-109">Bu konu zaman uyumsuz programlamanın ne zaman ve nasıl kullanılması gerektiği hakkında genel bakış içerir ve ayrıntılar ve örnekler içeren destek konularına bağlantılar sunar.</span><span class="sxs-lookup"><span data-stu-id="43cf4-109">This topic provides an overview of when and how to use async programming and includes links to support topics that contain details and examples.</span></span>

## <a name="async-improves-responsiveness"></a><a name="BKMK_WhentoUseAsynchrony"></a> <span data-ttu-id="43cf4-110">Zaman uyumsuz, yanıt hızını geliştirir</span><span class="sxs-lookup"><span data-stu-id="43cf4-110">Async improves responsiveness</span></span>

<span data-ttu-id="43cf4-111">Asynchrony, Web erişimi gibi olası engelleme olasılığı olan etkinlikler için gereklidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-111">Asynchrony is essential for activities that are potentially blocking, such as web access.</span></span> <span data-ttu-id="43cf4-112">Web kaynağına erişim bazen yavaş veya gecikmeli olabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-112">Access to a web resource sometimes is slow or delayed.</span></span> <span data-ttu-id="43cf4-113">Bu tür bir etkinlik zaman uyumlu bir işlemde engellenirse, uygulamanın tamamının beklemesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-113">If such an activity is blocked in a synchronous process, the entire application must wait.</span></span> <span data-ttu-id="43cf4-114">Uygulama, zaman uyumsuz bir işlemde olası engelleme görevi sona erinceye kadar web kaynağına bağlı olmayan diğer işlerle devam eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-114">In an asynchronous process, the application can continue with other work that doesn't depend on the web resource until the potentially blocking task finishes.</span></span>

<span data-ttu-id="43cf4-115">Aşağıdaki tabloda, zaman uyumsuz programlamanın yanıt verme hızını geliştirdiği genel alanlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-115">The following table shows typical areas where asynchronous programming improves responsiveness.</span></span> <span data-ttu-id="43cf4-116">.NET ve Windows Çalışma Zamanı listelenen API 'Ler, zaman uyumsuz programlamayı destekleyen yöntemler içerir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-116">The listed APIs from .NET and the Windows Runtime contain methods that support async programming.</span></span>

| <span data-ttu-id="43cf4-117">Uygulama alanı</span><span class="sxs-lookup"><span data-stu-id="43cf4-117">Application area</span></span> | <span data-ttu-id="43cf4-118">Zaman uyumsuz yöntemleriyle .NET türleri</span><span class="sxs-lookup"><span data-stu-id="43cf4-118">.NET types with async methods</span></span> | <span data-ttu-id="43cf4-119">Zaman uyumsuz yöntemlere sahip Windows Çalışma Zamanı türleri</span><span class="sxs-lookup"><span data-stu-id="43cf4-119">Windows Runtime types with async methods</span></span> |
|--|--|--|
| <span data-ttu-id="43cf4-120">Web erişimi</span><span class="sxs-lookup"><span data-stu-id="43cf4-120">Web access</span></span> | <xref:System.Net.Http.HttpClient> | <xref:Windows.Web.Http.HttpClient?displayProperty=nameWithType> <br> <xref:Windows.Web.Syndication.SyndicationClient> |
| <span data-ttu-id="43cf4-121">Dosyalarla çalışma</span><span class="sxs-lookup"><span data-stu-id="43cf4-121">Working with files</span></span> | <xref:System.Text.Json.JsonSerializer> <br> <xref:System.IO.StreamReader> <br> <xref:System.IO.StreamWriter> <br> <xref:System.Xml.XmlReader> <br> <xref:System.Xml.XmlWriter> | <xref:Windows.Storage.StorageFile> |
| <span data-ttu-id="43cf4-122">Görüntülerle çalışma</span><span class="sxs-lookup"><span data-stu-id="43cf4-122">Working with images</span></span> |  | <xref:Windows.Media.Capture.MediaCapture> <br> <xref:Windows.Graphics.Imaging.BitmapEncoder> <br> <xref:Windows.Graphics.Imaging.BitmapDecoder> |
| <span data-ttu-id="43cf4-123">WCF programlama</span><span class="sxs-lookup"><span data-stu-id="43cf4-123">WCF programming</span></span> | [<span data-ttu-id="43cf4-124">Zaman Uyumlu ve Zaman Uyumsuz İşlemler</span><span class="sxs-lookup"><span data-stu-id="43cf4-124">Synchronous and Asynchronous Operations</span></span>](../../../../framework/wcf/synchronous-and-asynchronous-operations.md) |  |

<span data-ttu-id="43cf4-125">Tüm kullanıcı arabirimi ilişkili faaliyetler genellikle tek bir iş parçacığını paylaştığından, zaman uyumsuzluğun kullanıcı arabirimi iş parçacığına erişen uygulamalar için özellikle önem taşıdığı kanıtlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-125">Asynchrony proves especially valuable for applications that access the UI thread because all UI-related activity usually shares one thread.</span></span> <span data-ttu-id="43cf4-126">Herhangi bir işlem zaman uyumlu bir uygulamada engellenirse, tümü engellenir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-126">If any process is blocked in a synchronous application, all are blocked.</span></span> <span data-ttu-id="43cf4-127">Uygulamanız yanıt vermiyordur ve bunu uygulamanın beklediği değil de başarısız olduğu şeklinde yorumlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-127">Your application stops responding, and you might conclude that it has failed when instead it's just waiting.</span></span>

<span data-ttu-id="43cf4-128">Zaman uyumsuz yöntemleri kullandığınızda, uygulama arabirime yanıt vermeye devam eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-128">When you use asynchronous methods, the application continues to respond to the UI.</span></span> <span data-ttu-id="43cf4-129">Bir pencereyi yeniden boyutlandırabilir veya simge durumuna küçültebilir ya da bitmesini beklemek istemiyorsanız kapatabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-129">You can resize or minimize a window, for example, or you can close the application if you don't want to wait for it to finish.</span></span>

<span data-ttu-id="43cf4-130">Zaman uyumsuz tabanlı yaklaşım otomatik bir iletimin eşdeğerini, zaman uyumsuz işlemler tasarlarken seçebileceğiniz seçenekler listesine ekler.</span><span class="sxs-lookup"><span data-stu-id="43cf4-130">The async-based approach adds the equivalent of an automatic transmission to the list of options that you can choose from when designing asynchronous operations.</span></span> <span data-ttu-id="43cf4-131">Diğer bir deyişle, geleneksel zaman uyumsuz programlamanın tüm avantajlarından yararlanabilirsiniz, buna rağmen geliştiricinin daha az çaba sarf etmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-131">That is, you get all the benefits of traditional asynchronous programming but with much less effort from the developer.</span></span>

## <a name="async-methods-are-easy-to-write"></a><a name="BKMK_HowtoWriteanAsyncMethod"></a> <span data-ttu-id="43cf4-132">Zaman uyumsuz yöntemlerin yazılması kolaydır</span><span class="sxs-lookup"><span data-stu-id="43cf4-132">Async methods are easy to write</span></span>

<span data-ttu-id="43cf4-133">Async [ve](../../../language-reference/keywords/async.md) [await](../../../language-reference/operators/await.md) anahtar sözcükleri, zaman uyumsuz programlamanın kalbidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-133">The [async](../../../language-reference/keywords/async.md) and [await](../../../language-reference/operators/await.md) keywords in C# are the heart of async programming.</span></span> <span data-ttu-id="43cf4-134">Bu iki anahtar sözcüğü kullanarak, bir zaman uyumlu Yöntem oluştururken neredeyse kolayca zaman uyumsuz bir yöntem oluşturmak için .NET Framework, .NET Core veya Windows Çalışma Zamanı içindeki kaynakları kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-134">By using those two keywords, you can use resources in .NET Framework, .NET Core, or the Windows Runtime to create an asynchronous method almost as easily as you create a synchronous method.</span></span> <span data-ttu-id="43cf4-135">Anahtar sözcüğünü kullanarak tanımladığınız zaman uyumsuz yöntemler, `async` *zaman uyumsuz yöntemler*olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-135">Asynchronous methods that you define by using the `async` keyword are referred to as *async methods*.</span></span>

<span data-ttu-id="43cf4-136">Aşağıdaki örnekte zaman uyumsuz bir yöntem gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-136">The following example shows an async method.</span></span> <span data-ttu-id="43cf4-137">Koddaki neredeyse her şey size tanıdık gelmelidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-137">Almost everything in the code should look familiar to you.</span></span>

<span data-ttu-id="43cf4-138">Zaman [uyumsuz programlamayı, C# ' de zaman uyumsuz ve await ile](/samples/dotnet/samples/async-and-await-cs)karşıdan yüklenebilecek bir bütün WINDOWS PRESENTATION FOUNDATION (WPF) örneği bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-138">You can find a complete Windows Presentation Foundation (WPF) example available for download from [Asynchronous programming with async and await in C#](/samples/dotnet/samples/async-and-await-cs).</span></span>

:::code language="csharp" source="snippets/access-web/Program.cs" id="ControlFlow":::

<span data-ttu-id="43cf4-139">Yukarıdaki örnekten birkaç uygulama öğrenebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-139">You can learn several practices from the preceding sample.</span></span> <span data-ttu-id="43cf4-140">Yöntem imzasıyla başlayın.</span><span class="sxs-lookup"><span data-stu-id="43cf4-140">Start with the method signature.</span></span> <span data-ttu-id="43cf4-141">`async`Değiştiricisini içerir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-141">It includes the `async` modifier.</span></span> <span data-ttu-id="43cf4-142">Dönüş türü `Task<int>` (daha fazla seçenek için "dönüş türleri" bölümüne bakın).</span><span class="sxs-lookup"><span data-stu-id="43cf4-142">The return type is `Task<int>` (See "Return Types" section for more options).</span></span> <span data-ttu-id="43cf4-143">Yöntem adı içinde biter `Async` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-143">The method name ends in `Async`.</span></span> <span data-ttu-id="43cf4-144">Yönteminin gövdesinde, `GetStringAsync` bir döndürür `Task<string>` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-144">In the body of the method, `GetStringAsync` returns a `Task<string>`.</span></span> <span data-ttu-id="43cf4-145">Yani, `await` bir görev `string` () alacağınız zaman `contents` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-145">That means that when you `await` the task you'll get a `string` (`contents`).</span></span> <span data-ttu-id="43cf4-146">Görevi kapatmadan önce, ' a bağımlı olmayan iş yapabilirsiniz `string` `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-146">Before awaiting the task, you can do work that doesn't rely on the `string` from `GetStringAsync`.</span></span>

<span data-ttu-id="43cf4-147">Operatöre yakın ilgilenilmesi için ödeme yapın `await` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-147">Pay close attention to the `await` operator.</span></span> <span data-ttu-id="43cf4-148">Askıya alınır `GetUrlContentLengthAsync` :</span><span class="sxs-lookup"><span data-stu-id="43cf4-148">It suspends `GetUrlContentLengthAsync`:</span></span>

- <span data-ttu-id="43cf4-149">`GetUrlContentLengthAsync` tamamlanana kadar devam edilemiyor `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-149">`GetUrlContentLengthAsync` can't continue until `getStringTask` is complete.</span></span>
- <span data-ttu-id="43cf4-150">Bu arada, Denetim çağırana döner `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-150">Meanwhile, control returns to the caller of `GetUrlContentLengthAsync`.</span></span>
- <span data-ttu-id="43cf4-151">Tamamlandığında, Denetim burada sürdürülür `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-151">Control resumes here when `getStringTask` is complete.</span></span>
- <span data-ttu-id="43cf4-152">`await`İşleci bundan sonra sonucu alır `string` `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-152">The `await` operator then retrieves the `string` result from `getStringTask`.</span></span>

<span data-ttu-id="43cf4-153">Return ifadesinde bir tamsayı sonucu belirtilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-153">The return statement specifies an integer result.</span></span> <span data-ttu-id="43cf4-154">Bekleyen tüm yöntemler `GetUrlContentLengthAsync` uzunluk değerini alır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-154">Any methods that are awaiting `GetUrlContentLengthAsync` retrieve the length value.</span></span>

<span data-ttu-id="43cf4-155">`GetUrlContentLengthAsync`Çağrısı ve tamamlanmasını bekleme arasında yapabilecek herhangi bir işi yoksa `GetStringAsync` , aşağıdaki tek deyime çağırarak ve bekleyen kodunuzu basitleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-155">If `GetUrlContentLengthAsync` doesn't have any work that it can do between calling `GetStringAsync` and awaiting its completion, you can simplify your code by calling and awaiting in the following single statement.</span></span>

```csharp
string contents = await client.GetStringAsync("https://docs.microsoft.com/dotnet");
```

<span data-ttu-id="43cf4-156">Aşağıdaki özellikler, önceki örneği nasıl zaman uyumsuz bir yöntem yaptığını özetler:</span><span class="sxs-lookup"><span data-stu-id="43cf4-156">The following characteristics summarize what makes the previous example an async method:</span></span>

- <span data-ttu-id="43cf4-157">Yöntem imzası bir değiştirici içerir `async` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-157">The method signature includes an `async` modifier.</span></span>
- <span data-ttu-id="43cf4-158">Zaman uyumsuz yöntemin adı kurala göre "Async" soneki ile sona erer.</span><span class="sxs-lookup"><span data-stu-id="43cf4-158">The name of an async method, by convention, ends with an "Async" suffix.</span></span>
- <span data-ttu-id="43cf4-159">Dönüş türü aşağıdaki türlerden biridir:</span><span class="sxs-lookup"><span data-stu-id="43cf4-159">The return type is one of the following types:</span></span>

  - <span data-ttu-id="43cf4-160"><xref:System.Threading.Tasks.Task%601> yönteminizin işlenenin türü olan bir return bildirisi varsa `TResult` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-160"><xref:System.Threading.Tasks.Task%601> if your method has a return statement in which the operand has type `TResult`.</span></span>
  - <span data-ttu-id="43cf4-161"><xref:System.Threading.Tasks.Task> yönteminizin Return bildirisi yoksa veya işleneni olmayan bir return ifadesine sahip.</span><span class="sxs-lookup"><span data-stu-id="43cf4-161"><xref:System.Threading.Tasks.Task> if your method has no return statement or has a return statement with no operand.</span></span>
  - <span data-ttu-id="43cf4-162">`void` zaman uyumsuz bir olay işleyicisi yazıyorsanız.</span><span class="sxs-lookup"><span data-stu-id="43cf4-162">`void` if you're writing an async event handler.</span></span>
  - <span data-ttu-id="43cf4-163">Metodu olan diğer herhangi bir tür `GetAwaiter` (C# 7,0 ile başlar).</span><span class="sxs-lookup"><span data-stu-id="43cf4-163">Any other type that has a `GetAwaiter` method (starting with C# 7.0).</span></span>

  <span data-ttu-id="43cf4-164">Daha fazla bilgi için bkz. [dönüş türleri ve parametreleri](#BKMK_ReturnTypesandParameters) bölümü.</span><span class="sxs-lookup"><span data-stu-id="43cf4-164">For more information, see the [Return types and parameters](#BKMK_ReturnTypesandParameters) section.</span></span>

- <span data-ttu-id="43cf4-165">Yöntemi genellikle, `await` beklenen zaman uyumsuz işlem tamamlanana kadar yöntemin devam edemediği bir noktayı işaretleyen en az bir ifade içerir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-165">The method usually includes at least one `await` expression, which marks a point where the method can't continue until the awaited asynchronous operation is complete.</span></span> <span data-ttu-id="43cf4-166">Bu sırada yöntem askıya alınır ve denetim yöntemi arayana döner.</span><span class="sxs-lookup"><span data-stu-id="43cf4-166">In the meantime, the method is suspended, and control returns to the method's caller.</span></span> <span data-ttu-id="43cf4-167">Bu konunun sonraki bölümünde askıya alma noktasında neler olduğu gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-167">The next section of this topic illustrates what happens at the suspension point.</span></span>

<span data-ttu-id="43cf4-168">Zaman uyumsuz yöntemlerde ne yapmak istediğinizi belirtmek için sağlanan anahtar sözcükleri ve türleri kullanırsınız ve denetim, askıya alınan bir yöntemde await noktasına geldiğinde olması gerekenlerin izlenmesi dahil olmak üzere geri kalan işlemleri derleyici yapar.</span><span class="sxs-lookup"><span data-stu-id="43cf4-168">In async methods, you use the provided keywords and types to indicate what you want to do, and the compiler does the rest, including keeping track of what must happen when control returns to an await point in a suspended method.</span></span> <span data-ttu-id="43cf4-169">Döngüler ve özel durum işleme gibi bazı rutin işlemlerin geleneksel zaman uyumsuz kodla yapılması zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-169">Some routine processes, such as loops and exception handling, can be difficult to handle in traditional asynchronous code.</span></span> <span data-ttu-id="43cf4-170">Zaman uyumsuz bir yöntemde, bu öğeleri olabildiğince zaman uyumlu çözümde yazarsınız ve sorun çözülür.</span><span class="sxs-lookup"><span data-stu-id="43cf4-170">In an async method, you write these elements much as you would in a synchronous solution, and the problem is solved.</span></span>

<span data-ttu-id="43cf4-171">Önceki .NET Framework sürümlerindeki zaman uyumsuzluğu hakkında daha fazla bilgi için bkz. [TPL ve geleneksel .NET Framework zaman uyumsuz programlama](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span><span class="sxs-lookup"><span data-stu-id="43cf4-171">For more information about asynchrony in previous versions of .NET Framework, see [TPL and traditional .NET Framework asynchronous programming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span></span>

## <a name="what-happens-in-an-async-method"></a><a name="BKMK_WhatHappensUnderstandinganAsyncMethod"></a> <span data-ttu-id="43cf4-172">Zaman uyumsuz bir yöntemde ne olur?</span><span class="sxs-lookup"><span data-stu-id="43cf4-172">What happens in an async method</span></span>

<span data-ttu-id="43cf4-173">Zaman uyumsuz programlama ile ilgili olarak anlamanız gereken en önemli şey, denetim akışının yöntemden yönteme nasıl geçtiğidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-173">The most important thing to understand in asynchronous programming is how the control flow moves from method to method.</span></span> <span data-ttu-id="43cf4-174">Aşağıdaki diyagramda süreç boyunca size yol gösterir:</span><span class="sxs-lookup"><span data-stu-id="43cf4-174">The following diagram leads you through the process:</span></span>

:::image type="content" source="media/task-asynchronous-programming-model/navigation-trace-async-program.png" alt-text="Zaman uyumsuz denetim akışının izleme gezintisi" lightbox="media/task-asynchronous-programming-model/navigation-trace-async-program.png":::

<span data-ttu-id="43cf4-176">Diyagramdaki sayılar, bir çağırma yöntemi zaman uyumsuz yöntemi çağırdığında başlatılan aşağıdaki adımlara karşılık gelir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-176">The numbers in the diagram correspond to the following steps, initiated when a calling method calls the async method.</span></span>

1. <span data-ttu-id="43cf4-177">Çağırma yöntemi, `GetUrlContentLengthAsync` zaman uyumsuz metodu çağırır ve bekler.</span><span class="sxs-lookup"><span data-stu-id="43cf4-177">A calling method calls and awaits the `GetUrlContentLengthAsync` async method.</span></span>

1. <span data-ttu-id="43cf4-178">`GetUrlContentLengthAsync` bir <xref:System.Net.Http.HttpClient> örnek oluşturur ve <xref:System.Net.Http.HttpClient.GetStringAsync%2A> bir Web sitesinin içeriklerini dize olarak indirmek için zaman uyumsuz yöntemi çağırır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-178">`GetUrlContentLengthAsync` creates an <xref:System.Net.Http.HttpClient> instance and calls the <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronous method to download the contents of a website as a string.</span></span>

1. <span data-ttu-id="43cf4-179">İçinde `GetStringAsync` ilerleme durumunu askıya alan bir sorun var.</span><span class="sxs-lookup"><span data-stu-id="43cf4-179">Something happens in `GetStringAsync` that suspends its progress.</span></span> <span data-ttu-id="43cf4-180">Bir web sitesinin indirmesini veya başka bir engelleyen etkinliği beklemesi gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-180">Perhaps it must wait for a website to download or some other blocking activity.</span></span> <span data-ttu-id="43cf4-181">Kaynakları engellemeyi önlemek için, `GetStringAsync` kendi çağıranına denetim verir `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-181">To avoid blocking resources, `GetStringAsync` yields control to its caller, `GetUrlContentLengthAsync`.</span></span>

     <span data-ttu-id="43cf4-182">`GetStringAsync` öğesini döndürür <xref:System.Threading.Tasks.Task%601> , burada `TResult` bir dizedir ve `GetUrlContentLengthAsync` görevi `getStringTask` değişkenine atar.</span><span class="sxs-lookup"><span data-stu-id="43cf4-182">`GetStringAsync` returns a <xref:System.Threading.Tasks.Task%601>, where `TResult` is a string, and `GetUrlContentLengthAsync` assigns the task to the `getStringTask` variable.</span></span> <span data-ttu-id="43cf4-183">Görev, `GetStringAsync` iş tamamlandığında gerçek bir dize değeri üretme taahhüdünde, çağrısı için devam eden işlemi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-183">The task represents the ongoing process for the call to `GetStringAsync`, with a commitment to produce an actual string value when the work is complete.</span></span>

1. <span data-ttu-id="43cf4-184">Henüz beklenmediği `getStringTask` `GetUrlContentLengthAsync` için, nihai sonuca bağlı olmayan diğer çalışmalarla devam edebilir `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-184">Because `getStringTask` hasn't been awaited yet, `GetUrlContentLengthAsync` can continue with other work that doesn't depend on the final result from `GetStringAsync`.</span></span> <span data-ttu-id="43cf4-185">Bu iş, zaman uyumlu yöntem çağrısıyla temsil edilir `DoIndependentWork` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-185">That work is represented by a call to the synchronous method `DoIndependentWork`.</span></span>

1. <span data-ttu-id="43cf4-186">`DoIndependentWork` , işini yapan ve çağırana döndüren zaman uyumlu bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-186">`DoIndependentWork` is a synchronous method that does its work and returns to its caller.</span></span>

1. <span data-ttu-id="43cf4-187">`GetUrlContentLengthAsync` , uygulamasından bir sonuç olmadan yapabilmeden iş dışında çalışıyor `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-187">`GetUrlContentLengthAsync` has run out of work that it can do without a result from `getStringTask`.</span></span> <span data-ttu-id="43cf4-188">`GetUrlContentLengthAsync` ardından, indirilen dizenin uzunluğunu hesaplamak ve döndürmek ister, ancak yöntem dizeye gelinceye kadar bu değeri hesaplayamaz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-188">`GetUrlContentLengthAsync` next wants to calculate and return the length of the downloaded string, but the method can't calculate that value until the method has the string.</span></span>

    <span data-ttu-id="43cf4-189">Bu nedenle, `GetUrlContentLengthAsync` ilerleme durumunu askıya almak ve denetimi çağıran yönteme eklemek için bir Await işleci kullanır `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-189">Therefore, `GetUrlContentLengthAsync` uses an await operator to suspend its progress and to yield control to the method that called `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="43cf4-190">`GetUrlContentLengthAsync` çağırana döndürür `Task<int>` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-190">`GetUrlContentLengthAsync` returns a `Task<int>` to the caller.</span></span> <span data-ttu-id="43cf4-191">Görev, indirilen dizenin uzunluğu olan bir tamsayı sonucu verecek bir taahhüdü temsil eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-191">The task represents a promise to produce an integer result that's the length of the downloaded string.</span></span>

    > [!NOTE]
    > <span data-ttu-id="43cf4-192">`GetStringAsync`(Ve bu nedenle `getStringTask` ), bekleden önce tamamlanırsa `GetUrlContentLengthAsync` , denetim içinde kalır `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-192">If `GetStringAsync` (and therefore `getStringTask`) completes before `GetUrlContentLengthAsync` awaits it, control remains in `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="43cf4-193">`GetUrlContentLengthAsync`Çağrılan zaman uyumsuz işlem `getStringTask` zaten tamamlanmışsa ve nihai sonucu beklemek zorunda değilse, ' ın askıya alınması ve geri döndürülmesi `GetUrlContentLengthAsync` ücretlidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-193">The expense of suspending and then returning to `GetUrlContentLengthAsync` would be wasted if the called asynchronous process `getStringTask` has already completed and `GetUrlContentLengthAsync` doesn't have to wait for the final result.</span></span>

    <span data-ttu-id="43cf4-194">Çağırma yöntemi içinde işleme deseninin devam ettiği.</span><span class="sxs-lookup"><span data-stu-id="43cf4-194">Inside the calling method the processing pattern continues.</span></span> <span data-ttu-id="43cf4-195">Çağıran, sonucu beklemeden önce sonucu kendisine bağlı olmayan başka bir iş `GetUrlContentLengthAsync` ya da çağıran hemen bekleme edebilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-195">The caller might do other work that doesn't depend on the result from `GetUrlContentLengthAsync` before awaiting that result, or the caller might await immediately.</span></span> <span data-ttu-id="43cf4-196">Çağırma yöntemi için bekliyor `GetUrlContentLengthAsync` ve `GetUrlContentLengthAsync` bekliyor `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-196">The calling method is waiting for `GetUrlContentLengthAsync`, and `GetUrlContentLengthAsync` is waiting for `GetStringAsync`.</span></span>

1. <span data-ttu-id="43cf4-197">`GetStringAsync` tamamlar ve bir dize sonucu üretir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-197">`GetStringAsync` completes and produces a string result.</span></span> <span data-ttu-id="43cf4-198">Dize sonucu, bekleneceğiniz şekilde çağrısı tarafından döndürülmedi `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-198">The string result isn't returned by the call to `GetStringAsync` in the way that you might expect.</span></span> <span data-ttu-id="43cf4-199">(Metodun adım 3 ' te bir görevi zaten döndürdüğünü unutmayın.) Bunun yerine, dize sonucu, yönteminin tamamlandığını temsil eden görevde saklanır `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-199">(Remember that the method already returned a task in step 3.) Instead, the string result is stored in the task that represents the completion of the method, `getStringTask`.</span></span> <span data-ttu-id="43cf4-200">Await işleci sonucunu alır `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-200">The await operator retrieves the result from `getStringTask`.</span></span> <span data-ttu-id="43cf4-201">Atama ekstresi alındı sonucunu öğesine atar `contents` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-201">The assignment statement assigns the retrieved result to `contents`.</span></span>

1. <span data-ttu-id="43cf4-202">`GetUrlContentLengthAsync`Dize sonucu olduğunda, yöntemi dizenin uzunluğunu hesaplayabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-202">When `GetUrlContentLengthAsync` has the string result, the method can calculate the length of the string.</span></span> <span data-ttu-id="43cf4-203">Bu durumda, çalışması `GetUrlContentLengthAsync` da tamamlanır ve bekleyen olay işleyicisi sürdürülebilirler.</span><span class="sxs-lookup"><span data-stu-id="43cf4-203">Then the work of `GetUrlContentLengthAsync` is also complete, and the waiting event handler can resume.</span></span> <span data-ttu-id="43cf4-204">Konunun sonundaki tam örnekte olay işleyicisinin uzunluk sonucundaki değeri aldığını ve yazdığını onaylayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-204">In the full example at the end of the topic, you can confirm that the event handler retrieves and prints the value of the length result.</span></span>
<span data-ttu-id="43cf4-205">Zaman uyumsuz programlama konusunda yeniyseniz, zaman uyumlu ve zaman uyumsuz davranış arasındaki farkları değerlendirmek için bir dakikanızı ayırın.</span><span class="sxs-lookup"><span data-stu-id="43cf4-205">If you are new to asynchronous programming, take a minute to consider the difference between synchronous and asynchronous behavior.</span></span> <span data-ttu-id="43cf4-206">Zaman uyumlu yöntem, işi tamamlandığında döndürür (5. adım), ancak zaman uyumsuz bir yöntem işi askıya alındığında görev değeri döndürür (3 ve 6. adım).</span><span class="sxs-lookup"><span data-stu-id="43cf4-206">A synchronous method returns when its work is complete (step 5), but an async method returns a task value when its work is suspended (steps 3 and 6).</span></span> <span data-ttu-id="43cf4-207">Zaman uyumsuz yöntem çalışmasını tamamladığında görev tamamlandı olarak işaretlenir ve varsa sonuç görevde depolanır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-207">When the async method eventually completes its work, the task is marked as completed and the result, if any, is stored in the task.</span></span>

## <a name="api-async-methods"></a><a name="BKMK_APIAsyncMethods"></a> <span data-ttu-id="43cf4-208">API zaman uyumsuz yöntemleri</span><span class="sxs-lookup"><span data-stu-id="43cf4-208">API async methods</span></span>

<span data-ttu-id="43cf4-209">Zaman uyumsuz programlamayı destekleyen gibi yöntemlerin nerede bulunacağını merak ediyor olabilirsiniz `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-209">You might be wondering where to find methods such as `GetStringAsync` that support async programming.</span></span> <span data-ttu-id="43cf4-210">.NET Framework 4,5 veya üzeri ve .NET Core, ve ile çalışan birçok üye `async` içerir `await` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-210">.NET Framework 4.5 or higher and .NET Core contain many members that work with `async` and `await`.</span></span> <span data-ttu-id="43cf4-211">Bunları, üye adına eklenen "Async" sonekine ve ya da dönüş türlerine göre tanıyabilirsiniz <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="43cf4-211">You can recognize them by the "Async" suffix that's appended to the member name, and by their return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="43cf4-212">Örneğin, sınıfı,, ve gibi `System.IO.Stream` yöntemleri, <xref:System.IO.Stream.CopyToAsync%2A> ve <xref:System.IO.Stream.ReadAsync%2A> <xref:System.IO.Stream.WriteAsync%2A> ile zaman uyumlu yöntemleri içerir <xref:System.IO.Stream.CopyTo%2A> <xref:System.IO.Stream.Read%2A> <xref:System.IO.Stream.Write%2A> .</span><span class="sxs-lookup"><span data-stu-id="43cf4-212">For example, the `System.IO.Stream` class contains methods such as <xref:System.IO.Stream.CopyToAsync%2A>, <xref:System.IO.Stream.ReadAsync%2A>, and <xref:System.IO.Stream.WriteAsync%2A> alongside the synchronous methods <xref:System.IO.Stream.CopyTo%2A>, <xref:System.IO.Stream.Read%2A>, and <xref:System.IO.Stream.Write%2A>.</span></span>

<span data-ttu-id="43cf4-213">Windows Çalışma Zamanı ayrıca `async` Windows uygulamalarında ve ile kullanabileceğiniz birçok yöntem içerir `await` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-213">The Windows Runtime also contains many methods that you can use with `async` and `await` in Windows apps.</span></span> <span data-ttu-id="43cf4-214">Daha fazla bilgi için bkz. UWP geliştirme için [Iş parçacığı ve zaman uyumsuz programlama](/windows/uwp/threading-async/) ve [zaman uyumsuz programlama (Windows Mağazası uygulamaları)](/previous-versions/windows/apps/hh464924(v=win.10)) ve hızlı başlangıç: Windows çalışma zamanı önceki sürümlerini kullanıyorsanız, [C# veya Visual Basic zaman uyumsuz API 'leri çağırma](/previous-versions/windows/apps/hh452713(v=win.10)) .</span><span class="sxs-lookup"><span data-stu-id="43cf4-214">For more information, see [Threading and async programming](/windows/uwp/threading-async/) for UWP development, and [Asynchronous programming (Windows Store apps)](/previous-versions/windows/apps/hh464924(v=win.10)) and [Quickstart: Calling asynchronous APIs in C# or Visual Basic](/previous-versions/windows/apps/hh452713(v=win.10)) if you use earlier versions of the Windows Runtime.</span></span>

## <a name="threads"></a><a name="BKMK_Threads"></a> <span data-ttu-id="43cf4-215">Akışları</span><span class="sxs-lookup"><span data-stu-id="43cf4-215">Threads</span></span>

<span data-ttu-id="43cf4-216">Zaman uyumsuz yöntemlerin engelleyici olmayan işlemler olmaları amaçlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-216">Async methods are intended to be non-blocking operations.</span></span> <span data-ttu-id="43cf4-217">`await`Zaman uyumsuz yöntemdeki bir ifade, beklenen görev çalışırken geçerli iş parçacığını engellemez.</span><span class="sxs-lookup"><span data-stu-id="43cf4-217">An `await` expression in an async method doesn't block the current thread while the awaited task is running.</span></span> <span data-ttu-id="43cf4-218">Bunun yerine ifade, yöntemin geri kalanını yöntemin devamı olarak imzalar ve denetimi zaman uyumsuz yöntemi arayan kişiye verir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-218">Instead, the expression signs up the rest of the method as a continuation and returns control to the caller of the async method.</span></span>

<span data-ttu-id="43cf4-219">`async`Ve `await` anahtar sözcükleri ek iş parçacıklarının oluşturulmasına neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-219">The `async` and `await` keywords don't cause additional threads to be created.</span></span> <span data-ttu-id="43cf4-220">Zaman uyumsuz yöntem kendi iş parçacığı üzerinde çalışmadığı için zaman uyumsuz yöntemler çoklu iş parçacığı kullanımı gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="43cf4-220">Async methods don't require multithreading because an async method doesn't run on its own thread.</span></span> <span data-ttu-id="43cf4-221">Yöntem geçerli eşitleme kapsamının üzerinde çalışır ve yalnızca yöntem etkin olduğunda iş parçacığındaki zamanı kullanır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-221">The method runs on the current synchronization context and uses time on the thread only when the method is active.</span></span> <span data-ttu-id="43cf4-222"><xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType>CPU 'ya bağlanan işi arka plan iş parçacığına taşımak için kullanabilirsiniz, ancak arka plan iş parçacığı yalnızca sonuçların kullanılabilir hale gelmesini bekleyen bir işlem konusunda yardımcı olmaz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-222">You can use <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> to move CPU-bound work to a background thread, but a background thread doesn't help with a process that's just waiting for results to become available.</span></span>

<span data-ttu-id="43cf4-223">Zaman uyumsuz programlamaya zaman uyumsuz yaklaşım, hemen hemen her durumda varolan yaklaşımlara tercih edilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-223">The async-based approach to asynchronous programming is preferable to existing approaches in almost every case.</span></span> <span data-ttu-id="43cf4-224">Özellikle, bu yaklaşım, <xref:System.ComponentModel.BackgroundWorker> kod daha basit olduğundan ve yarış koşullarına karşı koruma sağlamak zorunda olmadığınızdan g/ç ile bağlantılı işlemler için olan sınıftan daha iyidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-224">In particular, this approach is better than the <xref:System.ComponentModel.BackgroundWorker> class for I/O-bound operations because the code is simpler and you don't have to guard against race conditions.</span></span> <span data-ttu-id="43cf4-225">, Zaman uyumsuz programlama, <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> <xref:System.ComponentModel.BackgroundWorker> `Task.Run` iş parçacığı havuzuna aktarılan çalışmalardan kodunuzu çalıştırmanın koordinasyon ayrıntılarını ayırdığından, zaman uyumsuz programlama, CPU 'ya bağlanan işlemlerden daha iyidir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-225">In combination with the <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> method, async programming is better than <xref:System.ComponentModel.BackgroundWorker> for CPU-bound operations because async programming separates the coordination details of running your code from the work that `Task.Run` transfers to the thread pool.</span></span>

## <a name="async-and-await"></a><a name="BKMK_AsyncandAwait"></a> <span data-ttu-id="43cf4-226">Async ve await</span><span class="sxs-lookup"><span data-stu-id="43cf4-226">async and await</span></span>

<span data-ttu-id="43cf4-227">[Zaman](../../../language-reference/keywords/async.md) uyumsuz değiştirici kullanarak bir yöntemin zaman uyumsuz bir yöntem olduğunu belirtirseniz, aşağıdaki iki özelliği etkinleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-227">If you specify that a method is an async method by using the [async](../../../language-reference/keywords/async.md) modifier, you enable the following two capabilities.</span></span>

- <span data-ttu-id="43cf4-228">İşaretlenen zaman uyumsuz yöntem, askıya alma noktaları belirlemek için [await](../../../language-reference/operators/await.md) kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-228">The marked async method can use [await](../../../language-reference/operators/await.md) to designate suspension points.</span></span> <span data-ttu-id="43cf4-229">İşleci, zaman uyumsuz yöntemin, beklenen `await` zaman uyumsuz işlem tamamlanana kadar bu noktanın ötesine devam edemeyeceğini derleyiciye bildirir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-229">The `await` operator tells the compiler that the async method can't continue past that point until the awaited asynchronous process is complete.</span></span> <span data-ttu-id="43cf4-230">Bu sırada denetim, zaman uyumsuz yönteminin arayanına döner.</span><span class="sxs-lookup"><span data-stu-id="43cf4-230">In the meantime, control returns to the caller of the async method.</span></span>

     <span data-ttu-id="43cf4-231">Bir ifadede zaman uyumsuz bir yöntemin askıya alınması `await` yöntemden bir çıkış oluşturmaz ve `finally` bloklar çalıştırılmaz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-231">The suspension of an async method at an `await` expression doesn't constitute an exit from the method, and `finally` blocks don't run.</span></span>

- <span data-ttu-id="43cf4-232">İşaretli zaman uyumsuz yöntem, kendisinin çağırdığı yöntemler tarafından bekleniyor olabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-232">The marked async method can itself be awaited by methods that call it.</span></span>

<span data-ttu-id="43cf4-233">Zaman uyumsuz bir yöntem, genellikle bir işlecin bir veya daha fazla örneğini içerir `await` , ancak ifadelerin yokluğu `await` bir derleyici hatasına neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-233">An async method typically contains one or more occurrences of an `await` operator, but the absence of `await` expressions doesn't cause a compiler error.</span></span> <span data-ttu-id="43cf4-234">Zaman uyumsuz bir yöntem, askıya alma `await` noktasını işaretlemek için bir operatör kullanmıyorsa Yöntem, değiştiriciye rağmen zaman uyumlu bir yöntem olarak yürütülür `async` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-234">If an async method doesn't use an `await` operator to mark a suspension point, the method executes as a synchronous method does, despite the `async` modifier.</span></span> <span data-ttu-id="43cf4-235">Derleyici bu tür yöntemler için bir uyarı verir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-235">The compiler issues a warning for such methods.</span></span>

<span data-ttu-id="43cf4-236">`async` ve `await` bağlamsal anahtar sözcüklerdir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-236">`async` and `await` are contextual keywords.</span></span> <span data-ttu-id="43cf4-237">Daha fazla bilgi ve örnek için aşağıdaki konulara bakın:</span><span class="sxs-lookup"><span data-stu-id="43cf4-237">For more information and examples, see the following topics:</span></span>

- [<span data-ttu-id="43cf4-238">async</span><span class="sxs-lookup"><span data-stu-id="43cf4-238">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="43cf4-239">await</span><span class="sxs-lookup"><span data-stu-id="43cf4-239">await</span></span>](../../../language-reference/operators/await.md)

## <a name="return-types-and-parameters"></a><a name="BKMK_ReturnTypesandParameters"></a> <span data-ttu-id="43cf4-240">Dönüş türleri ve parametreleri</span><span class="sxs-lookup"><span data-stu-id="43cf4-240">Return types and parameters</span></span>

<span data-ttu-id="43cf4-241">Zaman uyumsuz bir yöntem genellikle bir <xref:System.Threading.Tasks.Task> veya döndürür <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="43cf4-241">An async method typically returns a <xref:System.Threading.Tasks.Task> or a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="43cf4-242">Zaman uyumsuz bir yöntemde, bir `await` işleç, başka bir zaman uyumsuz metoda yapılan çağrıdan döndürülen bir göreve uygulanır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-242">Inside an async method, an `await` operator is applied to a task that's returned from a call to another async method.</span></span>

<span data-ttu-id="43cf4-243">Yöntemi, <xref:System.Threading.Tasks.Task%601> [`return`](../../../language-reference/keywords/return.md) türünde bir işleneni belirten bir ifade içeriyorsa, dönüş türü olarak belirtirsiniz `TResult` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-243">You specify <xref:System.Threading.Tasks.Task%601> as the return type if the method contains a [`return`](../../../language-reference/keywords/return.md) statement that specifies an operand of type `TResult`.</span></span>

<span data-ttu-id="43cf4-244"><xref:System.Threading.Tasks.Task>Metodun Return ifadesine sahip olmaması veya bir işleneni döndürmeyen bir return ifadesine sahip olması durumunda dönüş türü olarak kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="43cf4-244">You use <xref:System.Threading.Tasks.Task>  as the return type if the method has no return statement or has a return statement that doesn't return an operand.</span></span>

<span data-ttu-id="43cf4-245">C# 7,0 ' den başlayarak, türün bir yöntemi içermesi kaydıyla, başka bir dönüş türü de belirtebilirsiniz `GetAwaiter` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-245">Starting with C# 7.0, you can also specify any other return type, provided that the type includes a `GetAwaiter` method.</span></span> <span data-ttu-id="43cf4-246"><xref:System.Threading.Tasks.ValueTask%601> Bu tür bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-246"><xref:System.Threading.Tasks.ValueTask%601> is an example of such a type.</span></span> <span data-ttu-id="43cf4-247">[System. Threading. Tasks. Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet paketinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-247">It is available in the [System.Threading.Tasks.Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet package.</span></span>

<span data-ttu-id="43cf4-248">Aşağıdaki örnek, bir veya döndüren bir yöntemi nasıl bildirmenizi ve çağırakullanacağınızı gösterir <xref:System.Threading.Tasks.Task%601> <xref:System.Threading.Tasks.Task> :</span><span class="sxs-lookup"><span data-stu-id="43cf4-248">The following example shows how you declare and call a method that returns a <xref:System.Threading.Tasks.Task%601> or a <xref:System.Threading.Tasks.Task>:</span></span>

```csharp
async Task<int> GetTaskOfTResultAsync()
{
    int hours = 0;
    await Task.Delay(0);

    return hours;
}


Task<int> returnedTaskTResult = GetTaskOfTResultAsync();
int intResult = await returnedTaskTResult;
// Single line
// int intResult = await GetTaskOfTResultAsync();

async Task GetTaskAsync()
{
    await Task.Delay(0);
    // No return statement needed
}

Task returnedTask = GetTaskAsync();
await returnedTask;
// Single line
await GetTaskAsync();
```

<span data-ttu-id="43cf4-249">Döndürülmüş her görev, devam eden bir çalışmayı temsil eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-249">Each returned task represents ongoing work.</span></span> <span data-ttu-id="43cf4-250">Bir görev, zaman uyumsuz işlemin durumu hakkındaki bilgileri saklar ve sonunda, işlemden alınan nihai sonuca veya başarısız olursa işlemin neden olduğu bir özel duruma ilişkin bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-250">A task encapsulates information about the state of the asynchronous process and, eventually, either the final result from the process or the exception that the process raises if it doesn't succeed.</span></span>

<span data-ttu-id="43cf4-251">Zaman uyumsuz bir yöntemin `void` dönüş türü de olabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-251">An async method can also have a `void` return type.</span></span> <span data-ttu-id="43cf4-252">Bu dönüş türü öncelikle bir dönüş türünün gerekli olduğu olay işleyicilerini tanımlamak için kullanılır `void` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-252">This return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="43cf4-253">Zaman uyumsuz olay işleyicileri, genellikle zaman uyumsuz programlar için başlangıç noktası olarak hizmet eder.</span><span class="sxs-lookup"><span data-stu-id="43cf4-253">Async event handlers often serve as the starting point for async programs.</span></span>

<span data-ttu-id="43cf4-254">Dönüş türüne sahip bir zaman uyumsuz yöntem beklenemez `void` ve void döndüren bir yöntem çağıran yöntemin aldığı özel durumları yakalayabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-254">An async method that has a `void` return type can't be awaited, and the caller of a void-returning method can't catch any exceptions that the method throws.</span></span>

<span data-ttu-id="43cf4-255">Zaman uyumsuz bir yöntem [içinde](../../../language-reference/keywords/in-parameter-modifier.md), [ref](../../../language-reference/keywords/ref.md) veya [Out](../../../language-reference/keywords/out-parameter-modifier.md) parametrelerini bildiremez, ancak yöntem bu parametrelere sahip yöntemleri çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-255">An async method can't declare [in](../../../language-reference/keywords/in-parameter-modifier.md), [ref](../../../language-reference/keywords/ref.md) or [out](../../../language-reference/keywords/out-parameter-modifier.md) parameters, but the method can call methods that have such parameters.</span></span> <span data-ttu-id="43cf4-256">Benzer şekilde, zaman uyumsuz bir yöntem başvuruya göre değer döndüremez, ancak başvuru dönüş değerleri içeren yöntemler çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-256">Similarly, an async method can't return a value by reference, although it can call methods with ref return values.</span></span>

<span data-ttu-id="43cf4-257">Daha fazla bilgi ve örnek için bkz. [Async Return Types (C#)](async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="43cf4-257">For more information and examples, see [Async return types (C#)](async-return-types.md).</span></span> <span data-ttu-id="43cf4-258">Zaman uyumsuz yöntemlerde özel durumları yakalama hakkında daha fazla bilgi için bkz. [try-catch](../../../language-reference/keywords/try-catch.md).</span><span class="sxs-lookup"><span data-stu-id="43cf4-258">For more information about how to catch exceptions in async methods, see [try-catch](../../../language-reference/keywords/try-catch.md).</span></span>

<span data-ttu-id="43cf4-259">Windows Çalışma Zamanı programlamadaki zaman uyumsuz API 'Ler, görevlerle benzer olan aşağıdaki dönüş türlerinden birine sahiptir:</span><span class="sxs-lookup"><span data-stu-id="43cf4-259">Asynchronous APIs in Windows Runtime programming have one of the following return types, which are similar to tasks:</span></span>

- <span data-ttu-id="43cf4-260"><xref:Windows.Foundation.IAsyncOperation%601>öğesine karşılık gelen <xref:System.Threading.Tasks.Task%601></span><span class="sxs-lookup"><span data-stu-id="43cf4-260"><xref:Windows.Foundation.IAsyncOperation%601>, which corresponds to <xref:System.Threading.Tasks.Task%601></span></span>
- <span data-ttu-id="43cf4-261"><xref:Windows.Foundation.IAsyncAction>öğesine karşılık gelen <xref:System.Threading.Tasks.Task></span><span class="sxs-lookup"><span data-stu-id="43cf4-261"><xref:Windows.Foundation.IAsyncAction>, which corresponds to <xref:System.Threading.Tasks.Task></span></span>
- <xref:Windows.Foundation.IAsyncActionWithProgress%601>
- <xref:Windows.Foundation.IAsyncOperationWithProgress%602>

## <a name="naming-convention"></a><a name="BKMK_NamingConvention"></a> <span data-ttu-id="43cf4-262">Adlandırma kuralı</span><span class="sxs-lookup"><span data-stu-id="43cf4-262">Naming convention</span></span>

<span data-ttu-id="43cf4-263">Kurala göre, yaygın olarak kullanılan türleri (örneğin,,,,,,,) döndüren yöntemlerin `Task` `Task<T>` `ValueTask` `ValueTask<T>` "Async" ile biten adları olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="43cf4-263">By convention, methods that return commonly awaitable types (for example, `Task`, `Task<T>`, `ValueTask`, `ValueTask<T>`) should have names that end with "Async".</span></span> <span data-ttu-id="43cf4-264">Zaman uyumsuz bir işlem Başlatan ancak bir awasever türü döndürmeyen Yöntemler, "Async" ile biten adlara sahip olmamalıdır, ancak bu yöntemin işlemin sonucunu döndürmeyeceğini veya oluşturmadığını önermek için "BEGIN", "Start" ya da başka bir fiil ile başlayabilir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-264">Methods that start an asynchronous operation but do not return an awaitable type should not have names that end with "Async", but may start with "Begin", "Start", or some other verb to suggest this method does not return or throw the result of the operation.</span></span>

<span data-ttu-id="43cf4-265">Bir olay, taban sınıf veya arabirim sözleşmesi farklı bir ad öneriyorsa kuralı yoksayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-265">You can ignore the convention where an event, base class, or interface contract suggests a different name.</span></span> <span data-ttu-id="43cf4-266">Örneğin, gibi yaygın olay işleyicilerini yeniden adlandırmamanız gerekir `OnButtonClick` .</span><span class="sxs-lookup"><span data-stu-id="43cf4-266">For example, you shouldn't rename common event handlers, such as `OnButtonClick`.</span></span>

## <a name="related-topics-and-samples-visual-studio"></a><a name="BKMK_RelatedTopics"></a> <span data-ttu-id="43cf4-267">İlgili konular ve örnekler (Visual Studio)</span><span class="sxs-lookup"><span data-stu-id="43cf4-267">Related topics and samples (Visual Studio)</span></span>

| <span data-ttu-id="43cf4-268">Başlık</span><span class="sxs-lookup"><span data-stu-id="43cf4-268">Title</span></span> | <span data-ttu-id="43cf4-269">Açıklama</span><span class="sxs-lookup"><span data-stu-id="43cf4-269">Description</span></span> | <span data-ttu-id="43cf4-270">Örnek</span><span class="sxs-lookup"><span data-stu-id="43cf4-270">Sample</span></span> |
|--|--|--|
| [<span data-ttu-id="43cf4-271">Async ve await kullanarak birden çok web isteğini paralel hale getirme (C#)</span><span class="sxs-lookup"><span data-stu-id="43cf4-271">How to make multiple web requests in parallel by using async and await (C#)</span></span>](./index.md) | <span data-ttu-id="43cf4-272">Nasıl aynı anda birkaç görevi başlatacağınızı gösterir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-272">Demonstrates how to start several tasks at the same time.</span></span> | [<span data-ttu-id="43cf4-273">Zaman uyumsuz örnek: birden çok Web Isteğini paralel hale getirme</span><span class="sxs-lookup"><span data-stu-id="43cf4-273">Async Sample: Make Multiple Web Requests in Parallel</span></span>](https://code.msdn.microsoft.com/Async-Make-Multiple-Web-49adb82e) |
| [<span data-ttu-id="43cf4-274">Zaman uyumsuz dönüş türleri (C#)</span><span class="sxs-lookup"><span data-stu-id="43cf4-274">Async return types (C#)</span></span>](async-return-types.md) | <span data-ttu-id="43cf4-275">Zaman uyumsuz yöntemlerin döndürebileceği türleri gösterir ve her türün ne zaman uygun olduğunu açıklar.</span><span class="sxs-lookup"><span data-stu-id="43cf4-275">Illustrates the types that async methods can return, and explains when each type is appropriate.</span></span> |  |
| <span data-ttu-id="43cf4-276">Bir iptal belirteci olan görevleri sinyal mekanizması olarak iptal edin.</span><span class="sxs-lookup"><span data-stu-id="43cf4-276">Cancel tasks with a cancellation token as a signaling mechanism.</span></span> | <span data-ttu-id="43cf4-277">Zaman uyumsuz çözümünüze aşağıdaki işlevin nasıl ekleneceğini gösterir:</span><span class="sxs-lookup"><span data-stu-id="43cf4-277">Shows how to add the following functionality to your async solution:</span></span><br><br> <span data-ttu-id="43cf4-278">- [Görev listesini iptal etme (C#)](cancel-an-async-task-or-a-list-of-tasks.md)</span><span class="sxs-lookup"><span data-stu-id="43cf4-278">- [Cancel a list of tasks (C#)](cancel-an-async-task-or-a-list-of-tasks.md)</span></span><br><span data-ttu-id="43cf4-279">- [Bir süre sonra görevleri iptal et (C#)](cancel-async-tasks-after-a-period-of-time.md)</span><span class="sxs-lookup"><span data-stu-id="43cf4-279">- [Cancel tasks after a period of time (C#)](cancel-async-tasks-after-a-period-of-time.md)</span></span><br><span data-ttu-id="43cf4-280">- [Zaman uyumsuz görevi tamamlandıklarında işle (C#)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span><span class="sxs-lookup"><span data-stu-id="43cf4-280">- [Process asynchronous task as they complete (C#)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span></span> |  |
| [<span data-ttu-id="43cf4-281">Dosya erişimi için Async Kullanma (C#)</span><span class="sxs-lookup"><span data-stu-id="43cf4-281">Using async for file access (C#)</span></span>](using-async-for-file-access.md) | <span data-ttu-id="43cf4-282">Dosyalara erişmek için zaman uyumsuz yöntemin ve await işlecinin kullanılmasına ilişkin avantajları listeler ve gösterir.</span><span class="sxs-lookup"><span data-stu-id="43cf4-282">Lists and demonstrates the benefits of using async and await to access files.</span></span> |  |
| [<span data-ttu-id="43cf4-283">Görev tabanlı zaman uyumsuz model (TAP)</span><span class="sxs-lookup"><span data-stu-id="43cf4-283">Task-based asynchronous pattern (TAP)</span></span>](../../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md) | <span data-ttu-id="43cf4-284">Zaman uyumsuz bir model tanımlar, model ve türlerini temel alır <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="43cf4-284">Describes an asynchronous pattern, the pattern is based on the <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> types.</span></span> |  |
| [<span data-ttu-id="43cf4-285">Channel 9 ' da zaman uyumsuz videolar</span><span class="sxs-lookup"><span data-stu-id="43cf4-285">Async Videos on Channel 9</span></span>](https://channel9.msdn.com/search?term=async%20&type=All#pubDate=year&ch9Search&lang-en=en) | <span data-ttu-id="43cf4-286">Zaman uyumsuz programlama hakkında çeşitli videoların bağlantılarını sağlar.</span><span class="sxs-lookup"><span data-stu-id="43cf4-286">Provides links to a variety of videos about async programming.</span></span> |  |

## <a name="see-also"></a><span data-ttu-id="43cf4-287">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="43cf4-287">See also</span></span>

- [<span data-ttu-id="43cf4-288">async</span><span class="sxs-lookup"><span data-stu-id="43cf4-288">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="43cf4-289">await</span><span class="sxs-lookup"><span data-stu-id="43cf4-289">await</span></span>](../../../language-reference/operators/await.md)
- [<span data-ttu-id="43cf4-290">Zaman uyumsuz programlama</span><span class="sxs-lookup"><span data-stu-id="43cf4-290">Asynchronous programming</span></span>](../../../async.md)
- [<span data-ttu-id="43cf4-291">Zaman uyumsuz genel bakış</span><span class="sxs-lookup"><span data-stu-id="43cf4-291">Async overview</span></span>](../../../../standard/async.md)
