---
title: "LINQ'te Sorgu Sözdizimi ve Yöntem Sözdizimi (C#)"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-csharp
ms.topic: article
helpviewer_keywords:
- LINQ [C#], query syntax vs. method syntax
- queries [LINQ in C#], syntax comparisons
ms.assetid: eedd6dd9-fec2-428c-9581-5b8783810ded
caps.latest.revision: "30"
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 0127ee0815c4ba6a697456fe45bd373bcf9ba4e4
ms.sourcegitcommit: 7e99f66ef09d2903e22c789c67ff5a10aa953b2f
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/18/2017
---
# <a name="query-syntax-and-method-syntax-in-linq-c"></a><span data-ttu-id="4cc93-102">LINQ'te Sorgu Sözdizimi ve Yöntem Sözdizimi (C#)</span><span class="sxs-lookup"><span data-stu-id="4cc93-102">Query Syntax and Method Syntax in LINQ (C#)</span></span>
<span data-ttu-id="4cc93-103">Çoğu sorgularda giriş dil ile tümleşik sorgu ([!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)]) belgeleri LINQ bildirim temelli sorgu sözdizimi kullanılarak yazılır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-103">Most queries in the introductory Language Integrated Query ([!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)]) documentation are written by using the LINQ declarative query syntax.</span></span> <span data-ttu-id="4cc93-104">Bununla birlikte, kod derlendiğinde sorgu söz dizimi .NET ortak dil çalışma zamanı (CLR) için yöntem çağrılarını içine çevrilmelidir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-104">However, the query syntax must be translated into method calls for the .NET common language runtime (CLR) when the code is compiled.</span></span> <span data-ttu-id="4cc93-105">Bu yöntem çağrılarını gibi adlara sahip standart sorgu işleçleri çağırma `Where`, `Select`, `GroupBy`, `Join`, `Max`, ve `Average`.</span><span class="sxs-lookup"><span data-stu-id="4cc93-105">These method calls invoke the standard query operators, which have names such as `Where`, `Select`, `GroupBy`, `Join`, `Max`, and `Average`.</span></span> <span data-ttu-id="4cc93-106">Sorgu sözdizimi yerine doğrudan yöntemi sözdizimini kullanarak bunları çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="4cc93-106">You can call them directly by using method syntax instead of query syntax.</span></span>  
  
 <span data-ttu-id="4cc93-107">Sorgu sözdizimi ve yöntem sözdizimi anlam olarak aynıdır, ancak çoğu kişi sorgu söz dizimi daha basit ve okuması daha kolay bulmak.</span><span class="sxs-lookup"><span data-stu-id="4cc93-107">Query syntax and method syntax are semantically identical, but many people find query syntax simpler and easier to read.</span></span> <span data-ttu-id="4cc93-108">Bazı sorgular yöntem çağrılarını ifade edilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-108">Some queries must be expressed as method calls.</span></span> <span data-ttu-id="4cc93-109">Örneğin, belirli bir koşul eşleşen öğe sayısını alır. bir sorgu ifade etmek için bir yöntem çağrısı kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-109">For example, you must use a method call to express a query that retrieves the number of elements that match a specified condition.</span></span> <span data-ttu-id="4cc93-110">Ayrıca, bir kaynak dizisinde en büyük değer olan öğeyi alır. bir sorgu için bir yöntem çağrısı kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-110">You also must use a method call for a query that retrieves the element that has the maximum value in a source sequence.</span></span> <span data-ttu-id="4cc93-111">Standart sorgu işleçleri için başvuru belgeleri <xref:System.Linq> ad alanı genellikle yöntemi sözdizimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-111">The reference documentation for the standard query operators in the <xref:System.Linq> namespace generally uses method syntax.</span></span> <span data-ttu-id="4cc93-112">Bu nedenle, bile yazma Başlarken [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] sorgular, bu, sorgular ve sorgu ifadeleri kendilerini yöntemi sözdizimini kullanma hakkında bilgi sahibi olmanız yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-112">Therefore, even when getting started writing [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] queries, it is useful to be familiar with how to use method syntax in queries and in query expressions themselves.</span></span>  
  
## <a name="standard-query-operator-extension-methods"></a><span data-ttu-id="4cc93-113">Standart Sorgu İşleci Genişletme Yöntemleri</span><span class="sxs-lookup"><span data-stu-id="4cc93-113">Standard Query Operator Extension Methods</span></span>  
 <span data-ttu-id="4cc93-114">Aşağıdaki örnekte basit bir *sorgu ifadesi* ve anlam olarak eşdeğer sorgu olarak yazılmış bir *yöntemi tabanlı sorgu*.</span><span class="sxs-lookup"><span data-stu-id="4cc93-114">The following example shows a simple *query expression* and the semantically equivalent query written as a *method-based query*.</span></span>  
  
 [!code-csharp[csLINQGettingStarted#22](../../../../csharp/programming-guide/concepts/linq/codesnippet/CSharp/query-syntax-and-method-syntax-in-linq_1.cs)]  
  
 <span data-ttu-id="4cc93-115">İki örnek çıkışı aynıdır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-115">The output from the two examples is identical.</span></span> <span data-ttu-id="4cc93-116">Sorgu değişkeninin türü hem formlarında aynı olduğunu görebilirsiniz: <xref:System.Collections.Generic.IEnumerable%601>.</span><span class="sxs-lookup"><span data-stu-id="4cc93-116">You can see that the type of the query variable is the same in both forms: <xref:System.Collections.Generic.IEnumerable%601>.</span></span>  
  
 <span data-ttu-id="4cc93-117">Yöntem temelli sorgu anlamak için onu daha yakından inceleyelim.</span><span class="sxs-lookup"><span data-stu-id="4cc93-117">To understand the method-based query, let's examine it more closely.</span></span> <span data-ttu-id="4cc93-118">İfadesinin sağ tarafta dikkat `where` yan tümcesi şimdi ifade bir örnek yöntemi olarak `numbers` geri çağırma, aynı nesne türü olan `IEnumerable<int>`.</span><span class="sxs-lookup"><span data-stu-id="4cc93-118">On the right side of the expression, notice that the `where` clause is now expressed as an instance method on the `numbers` object, which as you will recall has a type of `IEnumerable<int>`.</span></span> <span data-ttu-id="4cc93-119">Genel ile biliyorsanız <xref:System.Collections.Generic.IEnumerable%601> arabirimi, bildiğiniz değil sahip bir `Where` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="4cc93-119">If you are familiar with the generic <xref:System.Collections.Generic.IEnumerable%601> interface, you know that it does not have a `Where` method.</span></span> <span data-ttu-id="4cc93-120">Ancak, Visual Studio IDE içinde IntelliSense tamamlanma listesi çağırma, göreceğiniz yalnızca bir `Where` yöntemi, ancak gibi birçok diğer yöntemler `Select`, `SelectMany`, `Join`, ve `Orderby`.</span><span class="sxs-lookup"><span data-stu-id="4cc93-120">However, if you invoke the IntelliSense completion list in the Visual Studio IDE, you will see not only a `Where` method, but many other methods such as `Select`, `SelectMany`, `Join`, and `Orderby`.</span></span> <span data-ttu-id="4cc93-121">Tüm standart sorgu işleçleri bunlar.</span><span class="sxs-lookup"><span data-stu-id="4cc93-121">These are all the standard query operators.</span></span>  
  
 <span data-ttu-id="4cc93-122">![IntelliSense içinde standart sorgu işleçleri](../../../../csharp/programming-guide/concepts/linq/media/standardqueryops.png "StandardQueryOps")</span><span class="sxs-lookup"><span data-stu-id="4cc93-122">![Standard Query Operators in Intellisense](../../../../csharp/programming-guide/concepts/linq/media/standardqueryops.png "StandardQueryOps")</span></span>  
  
 <span data-ttu-id="4cc93-123">Görünüyor ancak gibi <xref:System.Collections.Generic.IEnumerable%601> bu değil durumu aslında bu ek yöntemleri eklemek için tanımlandı.</span><span class="sxs-lookup"><span data-stu-id="4cc93-123">Although it looks as if <xref:System.Collections.Generic.IEnumerable%601> has been redefined to include these additional methods, in fact this is not the case.</span></span> <span data-ttu-id="4cc93-124">Standart sorgu işleçleri yöntemi olarak adlandırılan yeni bir tür olarak uygulanan *genişletme yöntemleri*.</span><span class="sxs-lookup"><span data-stu-id="4cc93-124">The standard query operators are implemented as a new kind of method called *extension methods*.</span></span> <span data-ttu-id="4cc93-125">Genişletme yöntemleri "mevcut bir türle genişletmek"; türün örnek yöntemleri değilmiş gibi bunlar çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-125">Extensions methods "extend" an existing type; they can be called as if they were instance methods on the type.</span></span> <span data-ttu-id="4cc93-126">Standart sorgu işleçleri genişletmek <xref:System.Collections.Generic.IEnumerable%601> ve diğer bir deyişle neden yazabilirsiniz `numbers.Where(...)`.</span><span class="sxs-lookup"><span data-stu-id="4cc93-126">The standard query operators extend <xref:System.Collections.Generic.IEnumerable%601> and that is why you can write `numbers.Where(...)`.</span></span>  
  
 <span data-ttu-id="4cc93-127">Kullanmaya başlamak için [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)], gerçekten uzantı yöntemleri hakkında bilmeniz gereken tek şey bunları uygulamanız kapsamında doğru kullanarak duruma getirmek nasıl `using` yönergeleri.</span><span class="sxs-lookup"><span data-stu-id="4cc93-127">To get started using [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)], all that you really have to know about extension methods is how to bring them into scope in your application by using the correct `using` directives.</span></span> <span data-ttu-id="4cc93-128">Uygulamanızın açısından bakıldığında, bir genişletme yöntemi ve normal örnek yöntemi aynıdır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-128">From your application's point of view, an extension method and a regular instance method are the same.</span></span>  
  
 <span data-ttu-id="4cc93-129">Uzantı yöntemleri hakkında daha fazla bilgi için bkz: [genişletme yöntemleri](../../../../csharp/programming-guide/classes-and-structs/extension-methods.md).</span><span class="sxs-lookup"><span data-stu-id="4cc93-129">For more information about extension methods, see [Extension Methods](../../../../csharp/programming-guide/classes-and-structs/extension-methods.md).</span></span> <span data-ttu-id="4cc93-130">Standart sorgu işleçleri hakkında daha fazla bilgi için bkz: [standart sorgu işleçlerine genel bakış (C#)](../../../../csharp/programming-guide/concepts/linq/standard-query-operators-overview.md).</span><span class="sxs-lookup"><span data-stu-id="4cc93-130">For more information about standard query operators, see [Standard Query Operators Overview (C#)](../../../../csharp/programming-guide/concepts/linq/standard-query-operators-overview.md).</span></span> <span data-ttu-id="4cc93-131">Bazı [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] sağlayıcıları gibi [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] ve [!INCLUDE[sqltecxlinq](~/includes/sqltecxlinq-md.md)], kendi standart sorgu işleçleri ve yanı sıra diğer türleri için ek genişletme yöntemleri uygulamak <xref:System.Collections.Generic.IEnumerable%601>.</span><span class="sxs-lookup"><span data-stu-id="4cc93-131">Some [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] providers, such as [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] and [!INCLUDE[sqltecxlinq](~/includes/sqltecxlinq-md.md)], implement their own standard query operators and additional extension methods for other types besides <xref:System.Collections.Generic.IEnumerable%601>.</span></span>  
  
## <a name="lambda-expressions"></a><span data-ttu-id="4cc93-132">Lambda İfadeleri</span><span class="sxs-lookup"><span data-stu-id="4cc93-132">Lambda Expressions</span></span>  
 <span data-ttu-id="4cc93-133">Önceki örnekte dikkat koşullu ifade (`num % 2 == 0`) satır içi bağımsız değişken olarak geçirilen `Where` yöntemi: `Where(num => num % 2 == 0).` bu satır içi ifade lambda ifadesi olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-133">In the previous example, notice that the conditional expression (`num % 2 == 0`) is passed as an in-line argument to the `Where` method: `Where(num => num % 2 == 0).` This inline expression is called a lambda expression.</span></span> <span data-ttu-id="4cc93-134">Aksi takdirde daha kullanışsız formunda anonim bir yöntemi veya genel temsilcisi veya bir ifade ağacına yazılmış zorunda kod yazmak için uygun bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="4cc93-134">It is a convenient way to write code that would otherwise have to be written in more cumbersome form as an anonymous method or a generic delegate or an expression tree.</span></span> <span data-ttu-id="4cc93-135">C# `=>` "giden" olarak okuma lambda işleci.</span><span class="sxs-lookup"><span data-stu-id="4cc93-135">In C# `=>` is the lambda operator, which is read as "goes to".</span></span> <span data-ttu-id="4cc93-136">`num` İşlecinin sol tarafta karşılık gelen giriş değişkenidir `num` sorgu ifadesinde.</span><span class="sxs-lookup"><span data-stu-id="4cc93-136">The `num` on the left of the operator is the input variable which corresponds to `num` in the query expression.</span></span> <span data-ttu-id="4cc93-137">Derleyici türünü çıkarımını `num` , bildiği için `numbers` genel <xref:System.Collections.Generic.IEnumerable%601> türü.</span><span class="sxs-lookup"><span data-stu-id="4cc93-137">The compiler can infer the type of `num` because it knows that `numbers` is a generic <xref:System.Collections.Generic.IEnumerable%601> type.</span></span> <span data-ttu-id="4cc93-138">Lambda tıpkı sorgu söz dizimi veya herhangi bir C# ifadesi veya deyimi ifade olarak gövdesidir; Yöntem çağrıları ve diğer karmaşık bir mantık içerebilir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-138">The body of the lambda is just the same as the expression in query syntax or in any other C# expression or statement; it can include method calls and other complex logic.</span></span> <span data-ttu-id="4cc93-139">"Dönüş değerini" yalnızca ifade sonucudur.</span><span class="sxs-lookup"><span data-stu-id="4cc93-139">The "return value" is just the expression result.</span></span>  
  
 <span data-ttu-id="4cc93-140">Kullanmaya başlamak için [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)], Lambda'lar yaygın kullanmak zorunda değil.</span><span class="sxs-lookup"><span data-stu-id="4cc93-140">To get started using [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)], you do not have to use lambdas extensively.</span></span> <span data-ttu-id="4cc93-141">Ancak, bazı sorgular yalnızca yöntemi sözdiziminde ifade ve bazıları lambda ifadeleri gerektirir.</span><span class="sxs-lookup"><span data-stu-id="4cc93-141">However, certain queries can only be expressed in method syntax and some of those require lambda expressions.</span></span> <span data-ttu-id="4cc93-142">Lambda'lar ile daha tanıdık sonra güçlü ve esnek aracında olduğunu göreceksiniz, [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] araç.</span><span class="sxs-lookup"><span data-stu-id="4cc93-142">After you become more familiar with lambdas, you will find that they are a powerful and flexible tool in your [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)] toolbox.</span></span> <span data-ttu-id="4cc93-143">Daha fazla bilgi için bkz: [Lambda ifadeleri](../../../../csharp/programming-guide/statements-expressions-operators/lambda-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="4cc93-143">For more information, see [Lambda Expressions](../../../../csharp/programming-guide/statements-expressions-operators/lambda-expressions.md).</span></span>  
  
## <a name="composability-of-queries"></a><span data-ttu-id="4cc93-144">Sorgu Oluşturabilirliği</span><span class="sxs-lookup"><span data-stu-id="4cc93-144">Composability of Queries</span></span>  
 <span data-ttu-id="4cc93-145">Önceki kod örneğinde unutmayın `OrderBy` yöntemi çağrılır çağrıda dot işleci kullanılarak `Where`.</span><span class="sxs-lookup"><span data-stu-id="4cc93-145">In the previous code example, note that the `OrderBy` method is invoked by using the dot operator on the call to `Where`.</span></span> <span data-ttu-id="4cc93-146">`Where`Filtre uygulanmış bir sıra oluşturur ve ardından `Orderby` sıralama tarafından bu dizisi üzerinde çalışır.</span><span class="sxs-lookup"><span data-stu-id="4cc93-146">`Where` produces a filtered sequence, and then `Orderby` operates on that sequence by sorting it.</span></span> <span data-ttu-id="4cc93-147">Sorguları döndürür çünkü bir `IEnumerable`, siz bunları yöntemi sözdiziminde yöntem çağrılarını birlikte zincirleme tarafından oluştur.</span><span class="sxs-lookup"><span data-stu-id="4cc93-147">Because queries return an `IEnumerable`, you compose them in method syntax by chaining the method calls together.</span></span> <span data-ttu-id="4cc93-148">Sorgu sözdizimi kullanılarak sorguları yazdığınızda derleyici arka planda ne yaptığını budur.</span><span class="sxs-lookup"><span data-stu-id="4cc93-148">This is what the compiler does behind the scenes when you write queries by using query syntax.</span></span> <span data-ttu-id="4cc93-149">Ve bir sorgu değişkeni sorgu sonuçlarını depolamaz çünkü değiştirin veya hatta yürütüldükten sonra herhangi bir zamanda yeni bir sorgu için temel olarak kullanmak.</span><span class="sxs-lookup"><span data-stu-id="4cc93-149">And because a query variable does not store the results of the query, you can modify it or use it as the basis for a new query at any time, even after it has been executed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="4cc93-150">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="4cc93-150">See Also</span></span>  
 [<span data-ttu-id="4cc93-151">C# üzerinde LINQ ile çalışmaya başlama</span><span class="sxs-lookup"><span data-stu-id="4cc93-151">Getting Started with LINQ in C#</span></span>](../../../../csharp/programming-guide/concepts/linq/getting-started-with-linq.md)
