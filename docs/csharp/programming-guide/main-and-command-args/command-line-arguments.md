---
title: Komut satırı bağımsız değişkenleri-C# Programlama Kılavuzu
ms.date: 07/20/2015
helpviewer_keywords:
- command-line arguments [C#]
ms.assetid: 0e597e0d-ea7a-41ba-a38a-0198122f3c26
ms.openlocfilehash: c203716d9bb8298c934a999a496793c294949ddb
ms.sourcegitcommit: 03fec33630b46e78d5e81e91b40518f32c4bd7b5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/27/2020
ms.locfileid: "84007760"
---
# <a name="command-line-arguments-c-programming-guide"></a><span data-ttu-id="7a85e-102">Komut Satırı Bağımsız Değişkenleri (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="7a85e-102">Command-Line Arguments (C# Programming Guide)</span></span>

<span data-ttu-id="7a85e-103">Yöntemi `Main` aşağıdaki yöntemlerden biriyle tanımlayarak yöntemine bağımsız değişkenler gönderebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="7a85e-103">You can send arguments to the `Main` method by defining the method in one of the following ways:</span></span>

[!code-csharp[csProgGuideMain#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#2)]  

[!code-csharp[csProgGuideMain#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#3)]

> [!NOTE]
> <span data-ttu-id="7a85e-104">Bir Windows Forms uygulamasındaki yönteminde komut satırı bağımsız değişkenlerini etkinleştirmek için `Main` , program.cs içindeki imzasını el ile değiştirmeniz gerekir `Main` . *program.cs*</span><span class="sxs-lookup"><span data-stu-id="7a85e-104">To enable command-line arguments in the `Main` method in a Windows Forms application, you must manually modify the signature of `Main` in *program.cs*.</span></span> <span data-ttu-id="7a85e-105">Windows Forms Tasarımcısı tarafından oluşturulan kod, `Main` giriş parametresi olmadan bir oluşturur.</span><span class="sxs-lookup"><span data-stu-id="7a85e-105">The code generated by the Windows Forms designer creates a `Main` without an input parameter.</span></span> <span data-ttu-id="7a85e-106">Ayrıca <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> , komut satırı bağımsız değişkenlerine konsol veya Windows uygulamasındaki herhangi bir noktadan erişmek için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7a85e-106">You can also use <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> or <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> to access the command-line arguments from any point in a console or Windows application.</span></span>

<span data-ttu-id="7a85e-107">Yönteminin parametresi, `Main` <xref:System.String> komut satırı bağımsız değişkenlerini temsil eden bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="7a85e-107">The parameter of the `Main` method is a <xref:System.String> array that represents the command-line arguments.</span></span> <span data-ttu-id="7a85e-108">Genellikle, özelliği test ederek bağımsız değişkenlerin mevcut olup olmadığını belirlersiniz `Length` . Örneğin:</span><span class="sxs-lookup"><span data-stu-id="7a85e-108">Usually you determine whether arguments exist by testing the `Length` property, for example:</span></span>

[!code-csharp[csProgGuideMain#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#4)]

> [!TIP]
> <span data-ttu-id="7a85e-109">`args`Dizi null olamaz.</span><span class="sxs-lookup"><span data-stu-id="7a85e-109">The `args` array cannot be null.</span></span> <span data-ttu-id="7a85e-110">Bu nedenle, `Length` özelliği null denetimi olmadan erişmek güvenlidir.</span><span class="sxs-lookup"><span data-stu-id="7a85e-110">So, it's safe to access the `Length` property without null checking.</span></span>

<span data-ttu-id="7a85e-111">Ayrıca, sınıfını veya yöntemini kullanarak dize bağımsız değişkenlerini sayısal türlere dönüştürebilirsiniz <xref:System.Convert> `Parse` .</span><span class="sxs-lookup"><span data-stu-id="7a85e-111">You can also convert the string arguments to numeric types by using the <xref:System.Convert> class or the `Parse` method.</span></span> <span data-ttu-id="7a85e-112">Örneğin, aşağıdaki ifade, `string` `long` yöntemini kullanarak öğesini bir sayıya dönüştürür <xref:System.Int64.Parse%2A> :</span><span class="sxs-lookup"><span data-stu-id="7a85e-112">For example, the following statement converts the `string` to a `long` number by using the <xref:System.Int64.Parse%2A> method:</span></span>

```csharp
long num = Int64.Parse(args[0]);
```

<span data-ttu-id="7a85e-113">C# türü de kullanılabilir `long` , diğer ad `Int64` :</span><span class="sxs-lookup"><span data-stu-id="7a85e-113">It is also possible to use the C# type `long`, which aliases `Int64`:</span></span>

```csharp
long num = long.Parse(args[0]);
```

<span data-ttu-id="7a85e-114">`Convert` `ToInt64` Aynı şeyi yapmak için de sınıf yöntemini kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="7a85e-114">You can also use the `Convert` class method `ToInt64` to do the same thing:</span></span>

```csharp
long num = Convert.ToInt64(s);
```

<span data-ttu-id="7a85e-115">Daha fazla bilgi için <xref:System.Int64.Parse%2A> ve <xref:System.Convert> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="7a85e-115">For more information, see <xref:System.Int64.Parse%2A> and <xref:System.Convert>.</span></span>

## <a name="example"></a><span data-ttu-id="7a85e-116">Örnek</span><span class="sxs-lookup"><span data-stu-id="7a85e-116">Example</span></span>

<span data-ttu-id="7a85e-117">Aşağıdaki örnek, bir konsol uygulamasında komut satırı bağımsız değişkenlerinin nasıl kullanılacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="7a85e-117">The following example shows how to use command-line arguments in a console application.</span></span> <span data-ttu-id="7a85e-118">Uygulama çalışma zamanında bir bağımsız değişken alır, bağımsız değişkeni bir tamsayıya dönüştürür ve sayının faktöriyelini hesaplar.</span><span class="sxs-lookup"><span data-stu-id="7a85e-118">The application takes one argument at run time, converts the argument to an integer, and calculates the factorial of the number.</span></span> <span data-ttu-id="7a85e-119">Bağımsız değişken sağlanmazsa, uygulama programın doğru kullanımını açıklayan bir ileti yayınlar.</span><span class="sxs-lookup"><span data-stu-id="7a85e-119">If no arguments are supplied, the application issues a message that explains the correct usage of the program.</span></span>

<span data-ttu-id="7a85e-120">Uygulamayı bir komut isteminden derlemek ve çalıştırmak için aşağıdaki adımları izleyin:</span><span class="sxs-lookup"><span data-stu-id="7a85e-120">To compile and run the application from a command prompt, follow these steps:</span></span>

1. <span data-ttu-id="7a85e-121">Aşağıdaki kodu herhangi bir metin düzenleyicisine yapıştırın ve sonra dosyayı *Factorial.cs*adlı bir metin dosyası olarak kaydedin.</span><span class="sxs-lookup"><span data-stu-id="7a85e-121">Paste the following code into any text editor, and then save the file as  a text file with the name *Factorial.cs*.</span></span>

     [!code-csharp[csProgGuideMain#16](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class1.cs#16)]

2. <span data-ttu-id="7a85e-122">**Başlat** ekranından veya **Başlat** menüsünden bir Visual Studio **Geliştirici komut istemi** penceresi açın ve ardından yeni oluşturduğunuz dosyayı içeren klasöre gidin.</span><span class="sxs-lookup"><span data-stu-id="7a85e-122">From the **Start** screen or **Start** menu, open a Visual Studio **Developer Command Prompt** window, and then navigate to the folder that contains the file that you just created.</span></span>

3. <span data-ttu-id="7a85e-123">Uygulamayı derlemek için aşağıdaki komutu girin.</span><span class="sxs-lookup"><span data-stu-id="7a85e-123">Enter the following command to compile the application.</span></span>
  
     `csc Factorial.cs`  
  
     <span data-ttu-id="7a85e-124">Uygulamanızda hiçbir derleme hatası yoksa, *çarpınımı. exe* adlı bir yürütülebilir dosya oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="7a85e-124">If your application has no compilation errors, an executable file that's named *Factorial.exe* is created.</span></span>
  
4. <span data-ttu-id="7a85e-125">3 ' ün çarpımını hesaplamak için aşağıdaki komutu girin:</span><span class="sxs-lookup"><span data-stu-id="7a85e-125">Enter the following command to calculate the factorial of 3:</span></span>
  
     `Factorial 3`  
  
5. <span data-ttu-id="7a85e-126">Komut bu çıktıyı üretir:`The factorial of 3 is 6.`</span><span class="sxs-lookup"><span data-stu-id="7a85e-126">The command produces this output: `The factorial of 3 is 6.`</span></span>

> [!NOTE]
> <span data-ttu-id="7a85e-127">Visual Studio 'da bir uygulama çalıştırırken, [hata ayıklama sayfasında, proje Tasarımcısı](/visualstudio/ide/reference/debug-page-project-designer)' nda komut satırı bağımsız değişkenlerini belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7a85e-127">When running an application in Visual Studio, you can specify command-line arguments in the [Debug Page, Project Designer](/visualstudio/ide/reference/debug-page-project-designer).</span></span>

## <a name="see-also"></a><span data-ttu-id="7a85e-128">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="7a85e-128">See also</span></span>

- <xref:System.Environment?displayProperty=nameWithType>
- [<span data-ttu-id="7a85e-129">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="7a85e-129">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="7a85e-130">Main () ve komut satırı bağımsız değişkenleri</span><span class="sxs-lookup"><span data-stu-id="7a85e-130">Main() and Command-Line Arguments</span></span>](index.md)
- [<span data-ttu-id="7a85e-131">Komut satırı bağımsız değişkenlerini görüntüleme</span><span class="sxs-lookup"><span data-stu-id="7a85e-131">How to display command line arguments</span></span>](how-to-display-command-line-arguments.md)
- [<span data-ttu-id="7a85e-132">Ana() Dönüş Değerleri</span><span class="sxs-lookup"><span data-stu-id="7a85e-132">Main() Return Values</span></span>](main-return-values.md)
- [<span data-ttu-id="7a85e-133">Sınıflar</span><span class="sxs-lookup"><span data-stu-id="7a85e-133">Classes</span></span>](../classes-and-structs/classes.md)
