---
title: Komut Satırı Bağımsız Değişkenleri - C# Programlama Kılavuzu
ms.date: 07/20/2015
helpviewer_keywords:
- command-line arguments [C#]
ms.assetid: 0e597e0d-ea7a-41ba-a38a-0198122f3c26
ms.openlocfilehash: d6775263e6f1afb227aa263b01d60f5181da74f3
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "77093516"
---
# <a name="command-line-arguments-c-programming-guide"></a><span data-ttu-id="cd416-102">Komut Satırı Bağımsız Değişkenleri (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="cd416-102">Command-Line Arguments (C# Programming Guide)</span></span>

<span data-ttu-id="cd416-103">Yöntemi aşağıdaki yollardan `Main` biriyle tanımlayarak bağımsız değişkenleri yönteme gönderebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="cd416-103">You can send arguments to the `Main` method by defining the method in one of the following ways:</span></span>

[!code-csharp[csProgGuideMain#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#2)]  

[!code-csharp[csProgGuideMain#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#3)]

> [!NOTE]
> <span data-ttu-id="cd416-104">Windows Forms uygulamasındayöntemdeki `Main` komut satırı bağımsız değişkenlerini etkinleştirmek `Main` *için, program.cs'daki*imzayı el ile değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="cd416-104">To enable command-line arguments in the `Main` method in a Windows Forms application, you must manually modify the signature of `Main` in *program.cs*.</span></span> <span data-ttu-id="cd416-105">Windows Forms tasarımcısı tarafından oluşturulan kod, giriş parametresi olmadan bir `Main` gelir oluşturur.</span><span class="sxs-lookup"><span data-stu-id="cd416-105">The code generated by the Windows Forms designer creates a `Main` without an input parameter.</span></span> <span data-ttu-id="cd416-106">Konsol veya <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> Windows uygulamasındaki herhangi bir noktadan komut satırı bağımsız değişkenlerini de kullanabilir veya bunları erişebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cd416-106">You can also use <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> or <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> to access the command-line arguments from any point in a console or Windows application.</span></span>

<span data-ttu-id="cd416-107">`Main` Yöntemin parametresi komut <xref:System.String> satırı bağımsız değişkenlerini temsil eden bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="cd416-107">The parameter of the `Main` method is a <xref:System.String> array that represents the command-line arguments.</span></span> <span data-ttu-id="cd416-108">Genellikle bağımsız değişkenlerin `Length` özelliği sınayarak var olup olmadığını belirlersiniz, örneğin:</span><span class="sxs-lookup"><span data-stu-id="cd416-108">Usually you determine whether arguments exist by testing the `Length` property, for example:</span></span>

[!code-csharp[csProgGuideMain#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#4)]

<span data-ttu-id="cd416-109"><xref:System.Convert> Ayrıca, sınıfı veya yöntemi kullanarak dize bağımsız değişkenlerini `Parse` sayısal türlere dönüştürebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cd416-109">You can also convert the string arguments to numeric types by using the <xref:System.Convert> class or the `Parse` method.</span></span> <span data-ttu-id="cd416-110">Örneğin, aşağıdaki deyim `string` `long` <xref:System.Int64.Parse%2A> yöntemi kullanarak bir sayıya dönüştürür:</span><span class="sxs-lookup"><span data-stu-id="cd416-110">For example, the following statement converts the `string` to a `long` number by using the <xref:System.Int64.Parse%2A> method:</span></span>

```csharp
long num = Int64.Parse(args[0]);
```

<span data-ttu-id="cd416-111">C# türünü `long`de kullanmak mümkündür, hangi `Int64`diğer adlar:</span><span class="sxs-lookup"><span data-stu-id="cd416-111">It is also possible to use the C# type `long`, which aliases `Int64`:</span></span>

```csharp
long num = long.Parse(args[0]);
```

<span data-ttu-id="cd416-112">Aynı şeyi yapmak `Convert` için `ToInt64` sınıf yöntemini de kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="cd416-112">You can also use the `Convert` class method `ToInt64` to do the same thing:</span></span>

```csharp
long num = Convert.ToInt64(s);
```

<span data-ttu-id="cd416-113">Daha fazla bilgi için <xref:System.Int64.Parse%2A> ve <xref:System.Convert> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="cd416-113">For more information, see <xref:System.Int64.Parse%2A> and <xref:System.Convert>.</span></span>

## <a name="example"></a><span data-ttu-id="cd416-114">Örnek</span><span class="sxs-lookup"><span data-stu-id="cd416-114">Example</span></span>

<span data-ttu-id="cd416-115">Aşağıdaki örnekte, konsol uygulamasında komut satırı bağımsız değişkenleri nasıl kullanılacağı gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="cd416-115">The following example shows how to use command-line arguments in a console application.</span></span> <span data-ttu-id="cd416-116">Uygulama çalışma zamanında bir bağımsız değişken alır, bağımsız değişkeni bir arayine dönüştürür ve sayının faktöriyelini hesaplar.</span><span class="sxs-lookup"><span data-stu-id="cd416-116">The application takes one argument at run time, converts the argument to an integer, and calculates the factorial of the number.</span></span> <span data-ttu-id="cd416-117">Bağımsız değişken sağlanmazsa, uygulama programın doğru kullanımını açıklayan bir ileti yayınlar.</span><span class="sxs-lookup"><span data-stu-id="cd416-117">If no arguments are supplied, the application issues a message that explains the correct usage of the program.</span></span>

<span data-ttu-id="cd416-118">Uygulamayı bir komut isteminden derlemek ve çalıştırmak için aşağıdaki adımları izleyin:</span><span class="sxs-lookup"><span data-stu-id="cd416-118">To compile and run the application from a command prompt, follow these steps:</span></span>

1. <span data-ttu-id="cd416-119">Aşağıdaki kodu herhangi bir metin düzenleyicisine yapıştırın ve ardından dosyayı *Factorial.cs*adıyla bir metin dosyası olarak kaydedin.</span><span class="sxs-lookup"><span data-stu-id="cd416-119">Paste the following code into any text editor, and then save the file as  a text file with the name *Factorial.cs*.</span></span>

     [!code-csharp[csProgGuideMain#16](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class1.cs#16)]

2. <span data-ttu-id="cd416-120">**Başlangıç** ekranından veya **Başlat** menüsünden, Visual Studio Developer **Command Prompt** penceresini açın ve ardından az önce oluşturduğunuz dosyayı içeren klasöre gidin.</span><span class="sxs-lookup"><span data-stu-id="cd416-120">From the **Start** screen or **Start** menu, open a Visual Studio **Developer Command Prompt** window, and then navigate to the folder that contains the file that you just created.</span></span>

3. <span data-ttu-id="cd416-121">Uygulamayı derlemek için aşağıdaki komutu girin.</span><span class="sxs-lookup"><span data-stu-id="cd416-121">Enter the following command to compile the application.</span></span>
  
     `csc Factorial.cs`  
  
     <span data-ttu-id="cd416-122">Uygulamanızda derleme hatası yoksa, *Factorial.exe* adında bir yürütülebilir dosya oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="cd416-122">If your application has no compilation errors, an executable file that's named *Factorial.exe* is created.</span></span>
  
4. <span data-ttu-id="cd416-123">3 faktöriyel hesaplamak için aşağıdaki komutu girin:</span><span class="sxs-lookup"><span data-stu-id="cd416-123">Enter the following command to calculate the factorial of 3:</span></span>
  
     `Factorial 3`  
  
5. <span data-ttu-id="cd416-124">Komut bu çıktıyı üretir:`The factorial of 3 is 6.`</span><span class="sxs-lookup"><span data-stu-id="cd416-124">The command produces this output: `The factorial of 3 is 6.`</span></span>

> [!NOTE]
> <span data-ttu-id="cd416-125">Visual Studio'da bir uygulama çalıştırırken Hata [Ayıklama Sayfasında, Proje Tasarımcısı'nda](/visualstudio/ide/reference/debug-page-project-designer)komut satırı bağımsız değişkenlerini belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="cd416-125">When running an application in Visual Studio, you can specify command-line arguments in the [Debug Page, Project Designer](/visualstudio/ide/reference/debug-page-project-designer).</span></span>

## <a name="see-also"></a><span data-ttu-id="cd416-126">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="cd416-126">See also</span></span>

- <xref:System.Environment?displayProperty=nameWithType>
- [<span data-ttu-id="cd416-127">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="cd416-127">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="cd416-128">Ana() ve Komut Satırı Bağımsız Değişkenleri</span><span class="sxs-lookup"><span data-stu-id="cd416-128">Main() and Command-Line Arguments</span></span>](index.md)
- [<span data-ttu-id="cd416-129">Komut satırı bağımsız değişkenleri nasıl görüntülenir?</span><span class="sxs-lookup"><span data-stu-id="cd416-129">How to display command line arguments</span></span>](how-to-display-command-line-arguments.md)
- [<span data-ttu-id="cd416-130">Ana() Dönüş Değerleri</span><span class="sxs-lookup"><span data-stu-id="cd416-130">Main() Return Values</span></span>](main-return-values.md)
- [<span data-ttu-id="cd416-131">Sınıflar</span><span class="sxs-lookup"><span data-stu-id="cd416-131">Classes</span></span>](../classes-and-structs/classes.md)
