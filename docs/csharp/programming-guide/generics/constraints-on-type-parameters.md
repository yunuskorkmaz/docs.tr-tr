---
title: Tür parametrelerindeki kısıtlamalar - C# Programlama Kılavuzu
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 2962de53eab132ad02aaf679fdd6037bd24fa714
ms.sourcegitcommit: 927b7ea6b2ea5a440c8f23e3e66503152eb85591
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/16/2020
ms.locfileid: "81463880"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="1c246-102">Tür parametreleri üzerindeki kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="1c246-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="1c246-103">Kısıtlamalar derleyiciyi bir tür bağımsız değişkeninin sahip olması gereken özellikler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="1c246-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="1c246-104">Herhangi bir kısıtlama olmadan, tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="1c246-105">Derleyici yalnızca herhangi bir <xref:System.Object?displayProperty=nameWithType>.NET türü için nihai taban sınıf olan üyeleri varsayar.</span><span class="sxs-lookup"><span data-stu-id="1c246-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="1c246-106">Daha fazla bilgi için neden [kısıtlamaları kullanın'a](#why-use-constraints)bakın.</span><span class="sxs-lookup"><span data-stu-id="1c246-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="1c246-107">İstemci kodu kısıtlamayı karşılamayan bir tür kullanıyorsa, derleyici bir hata yayınlar.</span><span class="sxs-lookup"><span data-stu-id="1c246-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="1c246-108">Kısıtlamalar `where` bağlamsal anahtar kelime kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="1c246-109">Aşağıdaki tabloda yedi tür kısıtlama listelenebvardır:</span><span class="sxs-lookup"><span data-stu-id="1c246-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="1c246-110">Kısıtlama</span><span class="sxs-lookup"><span data-stu-id="1c246-110">Constraint</span></span>|<span data-ttu-id="1c246-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="1c246-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="1c246-112">Tür bağımsız değişkeni nullable olmayan bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="1c246-113">Nullable değer türleri hakkında bilgi için, [Nullable değer türleri](../../language-reference/builtin-types/nullable-value-types.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="1c246-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="1c246-114">Tüm değer türlerinin erişilebilir bir parametresiz `struct` oluşturucusu olduğundan, kısıtlama `new()` kısıtlamayı `new()` ima eder ve kısıtlamayla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="1c246-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="1c246-115">Ayrıca kısıtlama ile `struct` kısıtlama birleştiremezsiniz. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="1c246-115">You also can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="1c246-116">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-116">The type argument must be a reference type.</span></span> <span data-ttu-id="1c246-117">Bu kısıtlama herhangi bir sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="1c246-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="1c246-118">C# 8.0 veya sonraki bir şekilde `T` nullable bağlamında, nullable olmayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="1c246-119">Tür bağımsız değişkeni, geçersiz veya nullable olmayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="1c246-120">Bu kısıtlama herhangi bir sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="1c246-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="1c246-121">Tür bağımsız değişkeni nullable olmayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="1c246-122">Bağımsız değişken, C# 8.0 veya sonraki bir şekilde nullable olmayan bir başvuru türü veya nullable olmayan bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="1c246-123">Tür bağımsız değişkeni, geçersiz olmayan [yönetilmeyen](../../language-reference/builtin-types/unmanaged-types.md)bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="1c246-124">Kısıtlama `unmanaged` kısıtlamayı `struct` ima eder ve kısıtlamalarla `struct` `new()` birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="1c246-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="1c246-125">Tür bağımsız değişkeninin ortak parametresiz bir oluşturucusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="1c246-126">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama nın en son belirtilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="1c246-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="1c246-127">Kısıtlama `new()` `struct` ve `unmanaged` kısıtlamalarla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="1c246-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="1c246-128">`where T :`taban sınıf adı>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="1c246-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="1c246-129">Tür bağımsız değişkeni, belirtilen taban sınıftan türemelidir.</span><span class="sxs-lookup"><span data-stu-id="1c246-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="1c246-130">C# 8.0 ve sonraki nullable `T` bağlamında, belirtilen taban sınıftan türetilen bir nullable olmayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="1c246-131">`where T :`taban sınıf adı>? \* \<\*</span><span class="sxs-lookup"><span data-stu-id="1c246-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="1c246-132">Tür bağımsız değişkeni, belirtilen taban sınıftan türemelidir.</span><span class="sxs-lookup"><span data-stu-id="1c246-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="1c246-133">C# 8.0 ve sonraki bir şekilde `T` nullable bağlamında, belirtilen taban sınıftan türetilen nullable veya unnullable türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="1c246-134">`where T :`arayüz adı>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="1c246-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="1c246-135">Tür bağımsız değişkeni, belirtilen arabirimi olmalıdır veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="1c246-136">Birden çok arabirim kısıtlaması belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="1c246-137">Kısıtlayıcı arabirim de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="1c246-138">C# 8.0 ve sonraki bir nullable bağlamında, `T` belirtilen arabirimi uygulayan nullable olmayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="1c246-139">`where T :`arayüz adı>? \* \<\*</span><span class="sxs-lookup"><span data-stu-id="1c246-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="1c246-140">Tür bağımsız değişkeni, belirtilen arabirimi olmalıdır veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="1c246-141">Birden çok arabirim kısıtlaması belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="1c246-142">Kısıtlayıcı arabirim de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="1c246-143">C# 8.0'daki nullable `T` bağlamında, nullable başvuru türü, nullable olmayan bir başvuru türü veya bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="1c246-144">`T`nullable değer türü olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="1c246-145">Için `T` verilen tür bağımsız değişkeni, '' için `U`verilen bağımsız değişkenden türemelidir.</span><span class="sxs-lookup"><span data-stu-id="1c246-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="1c246-146">Nullable bağlamında, nullable olmayan bir başvuru türü `T` ise, `U` nullable olmayan başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="1c246-147">Nullable `U` bir başvuru türü `T` ise, nullable veya unnullable olabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="1c246-148">Neden kısıtlamaları kullanın</span><span class="sxs-lookup"><span data-stu-id="1c246-148">Why use constraints</span></span>

<span data-ttu-id="1c246-149">Kısıtlamalar, bir tür parametresinin yeteneklerini ve beklentilerini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1c246-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="1c246-150">Bu kısıtlamaları bildirmek, kısıtlama türündeki işlemleri ve yöntem çağrılarını kullanabileceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1c246-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="1c246-151">Genel sınıfınız veya yönteminiz, genel üyeler üzerinde basit atamanın ötesinde <xref:System.Object?displayProperty=nameWithType>herhangi bir işlem kullanıyorsa veya desteklenmeyen yöntemleri arıyorsa, tür parametresine kısıtlamalar uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1c246-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="1c246-152">Örneğin, taban sınıf kısıtlaması derleyiciye yalnızca bu türdeki veya bu türden türetilen nesnelerin tür bağımsız değişkeni olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="1c246-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="1c246-153">Derleyici bu garantiye sahip olduktan sonra, bu tür yöntemlerin genel sınıfta çağrılmasını sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="1c246-154">Aşağıdaki kod örneği, taban sınıf kısıtlaması `GenericList<T>` uygulayarak sınıfa [(Genel Lere](../../../standard/generics/index.md)Giriş'te) ekleyebileceğiniz işlevselliği gösterir.</span><span class="sxs-lookup"><span data-stu-id="1c246-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="1c246-155">Kısıtlama, genel sınıfın `Employee.Name` özelliği kullanmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="1c246-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="1c246-156">Kısıtlama, türdeki `T` tüm öğelerin bir `Employee` nesne veya devralan bir nesne olarak `Employee`garanti edilir olduğunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="1c246-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="1c246-157">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamaların kendileri aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="1c246-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="1c246-158">Kısıtlamayı `where T : class` uygularken, bu `==` `!=` işleçler değer eşitliği için değil, yalnızca referans kimliği için test edecek tür parametresi üzerinde ve işleçleri kaçının.</span><span class="sxs-lookup"><span data-stu-id="1c246-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="1c246-159">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklenmiş olsa bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="1c246-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="1c246-160">Aşağıdaki kod bu noktayı göstermektedir; <xref:System.String> sınıf `==` işleci aşırı yüklese bile çıktı yanlıştır.</span><span class="sxs-lookup"><span data-stu-id="1c246-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="1c246-161">Derleyici yalnızca derleme `T` zamanında bir başvuru türü olduğunu bilir ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1c246-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="1c246-162">Değer eşitliği için sınamanız gerekiyorsa, önerilen yol `where T : IEquatable<T>` `where T : IComparable<T>` aynı zamanda kısıtlamayı uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimi uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="1c246-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="1c246-163">Birden çok parametreyi zorlama</span><span class="sxs-lookup"><span data-stu-id="1c246-163">Constraining multiple parameters</span></span>

<span data-ttu-id="1c246-164">Aşağıdaki örnekte gösterildiği gibi, kısıtlamaları birden çok parametreye ve birden çok kısıtlamayı tek bir parametreye uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1c246-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="1c246-165">Sınırsız tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="1c246-165">Unbounded type parameters</span></span>

 <span data-ttu-id="1c246-166">Ortak sınıfta `SampleClass<T>{}`Ki'si gibi kısıtlaması olmayan tür parametreleri, sınırsız tür parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="1c246-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="1c246-167">Sınırsız tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="1c246-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="1c246-168">Somut `!=` `==` tür bağımsız değişkeninin bu işleçleri destekleyeceğinin garantisi olmadığından, ve işleçler kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1c246-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="1c246-169">Bunlar herhangi bir arabirim `System.Object` türüne dönüştürülebilir veya açıkça herhangi bir arabirim türüne dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="1c246-170">Null [ile](../../language-reference/keywords/null.md)karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1c246-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="1c246-171">Sınırlanmamış bir parametre ile `null`karşılaştırıldığında, tür bağımsız değişkeni bir değer türüise karşılaştırma her zaman yanlış döndürecektir.</span><span class="sxs-lookup"><span data-stu-id="1c246-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="1c246-172">Parametreleri kısıtlama olarak yazın</span><span class="sxs-lookup"><span data-stu-id="1c246-172">Type parameters as constraints</span></span>

<span data-ttu-id="1c246-173">Genel bir tür parametresinin kısıtlama olarak kullanılması, kendi türü parametresine sahip bir üye işlevin, aşağıdaki örnekte gösterildiği gibi, bu parametreyi içeren tür türü parametresiyle sınırlandırması gerektiğinde yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="1c246-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="1c246-174">Önceki örnekte, `T` `Add` yöntem bağlamında bir tür kısıtlaması ve `List` sınıf bağlamında sınırsız bir tür parametresi vardır.</span><span class="sxs-lookup"><span data-stu-id="1c246-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="1c246-175">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="1c246-176">Tip parametresi, açı braketi içinde diğer tip parametrelerle birlikte bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="1c246-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="1c246-177">Derleyici, `System.Object`tür parametresi hakkında türetilmiştir dışında hiçbir şey varsayamayacağından, genel sınıflarla birlikte kısıtlama olarak tür parametrelerinin kullanışlılığı sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="1c246-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="1c246-178">İki tür paramı arasında devralma ilişkisini zorlamak istediğiniz senaryolarda genel sınıflarda tür parametrelerini kısıtlama olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="1c246-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="1c246-179">NotNull kısıtlama</span><span class="sxs-lookup"><span data-stu-id="1c246-179">NotNull constraint</span></span>

<span data-ttu-id="1c246-180">Nullable bağlamında C# 8.0 ile başlayarak, `notnull` tür bağımsız değişkeninin nullable olmayan bir değer türü veya nullable olmayan başvuru türü olması gerektiğini belirtmek için kısıtlamak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1c246-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="1c246-181">Kısıtlama `notnull` yalnızca bir `nullable enable` bağlam içinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1c246-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="1c246-182">Kısıtlamayı `notnull` boş bir içeriğe eklerseniz derleyici bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1c246-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="1c246-183">Diğer kısıtlamaların aksine, bir tür `notnull` bağımsız değişkeni kısıtlamayı ihlal ettiğinde, derleyici bu `nullable enable` kod bir bağlam içinde derlendiğinde bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1c246-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="1c246-184">Kod boş bir şekilde derlenirse, derleyici herhangi bir uyarı veya hata oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="1c246-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="1c246-185">Nullable bağlamında C# 8.0 ile `class` başlayarak, kısıtlama türü bağımsız değişkeninin nullable olmayan bir başvuru türü olması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1c246-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="1c246-186">Nullable bağlamında, bir tür parametresi nullable başvuru türü olduğunda, derleyici bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1c246-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="1c246-187">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="1c246-187">Unmanaged constraint</span></span>

<span data-ttu-id="1c246-188">C# 7.3 ile başlayarak, `unmanaged` tür parametresinin geçersiz olmayan [yönetilmeyen](../../language-reference/builtin-types/unmanaged-types.md)bir tür olması gerektiğini belirtmek için kısıtlamayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1c246-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="1c246-189">Kısıtlama, `unmanaged` aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak değiştirilebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="1c246-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="1c246-190">İşleç, yerleşik bir tür `unsafe` olarak bilinen bir türde işleci kullandığından, önceki yöntem bir bağlamda derlenmelidir. `sizeof`</span><span class="sxs-lookup"><span data-stu-id="1c246-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="1c246-191">Kısıtlama `unmanaged` `sizeof` olmadan, işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1c246-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="1c246-192">Kısıtlama `unmanaged` kısıtlamayı `struct` ima eder ve onunla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="1c246-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="1c246-193">`struct` Kısıtlama kısıtlamayı `new()` ima ettiği `unmanaged` için, kısıtlama `new()` kısıtlamayla da birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="1c246-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="1c246-194">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="1c246-194">Delegate constraints</span></span>

<span data-ttu-id="1c246-195">Ayrıca C# 7.3 ile başlayarak, kullanabilirsiniz <xref:System.Delegate?displayProperty=nameWithType> veya <xref:System.MulticastDelegate?displayProperty=nameWithType> bir taban sınıf kısıtlaması olarak.</span><span class="sxs-lookup"><span data-stu-id="1c246-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="1c246-196">CLR her zaman bu kısıtlamaya izin verdi, ancak C# dili buna izin vermedi.</span><span class="sxs-lookup"><span data-stu-id="1c246-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="1c246-197">Kısıtlama, `System.Delegate` temsilcilerle tür güvenli bir şekilde çalışan kod yazmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="1c246-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="1c246-198">Aşağıdaki kod, aynı türde olmaları koşuluyla iki temsilciyi birleştiren bir uzantı yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="1c246-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="1c246-199">Yukarıdaki yöntemi, aynı türdeki temsilcileri birleştirmek için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1c246-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="1c246-200">Son satırı açıklamazsanız, derleme olmaz.</span><span class="sxs-lookup"><span data-stu-id="1c246-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="1c246-201">Her `first` `test` ikisi de ve temsilci türleri, ancak farklı temsilci türleri.</span><span class="sxs-lookup"><span data-stu-id="1c246-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="1c246-202">Enum kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="1c246-202">Enum constraints</span></span>

<span data-ttu-id="1c246-203">C# 7.3'ten başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü taban sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1c246-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="1c246-204">CLR her zaman bu kısıtlamaya izin verdi, ancak C# dili buna izin vermedi.</span><span class="sxs-lookup"><span data-stu-id="1c246-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="1c246-205">Genel olarak `System.Enum` kullanan genel `System.Enum`ler, statik yöntemleri kullanarak sonuçları önbelleğe almak için tür için güvenli programlama sağlar.</span><span class="sxs-lookup"><span data-stu-id="1c246-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="1c246-206">Aşağıdaki örnek, bir enum türü için tüm geçerli değerleri bulur ve sonra bu değerleri dize gösterimi ile eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1c246-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="1c246-207">Performans etkileri olan yansımakullanımı nda kullanılan yöntemler.</span><span class="sxs-lookup"><span data-stu-id="1c246-207">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="1c246-208">Bu yöntemi, yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için arayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1c246-208">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="1c246-209">Bir enum oluşturmak ve değerleri ve adlarından oluşan bir sözlük oluşturmak için aşağıdaki örnekte gösterildiği gibi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1c246-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="1c246-210">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="1c246-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="1c246-211">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="1c246-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="1c246-212">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="1c246-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="1c246-213">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="1c246-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="1c246-214">yeni Kısıtlama</span><span class="sxs-lookup"><span data-stu-id="1c246-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
