---
title: Tür parametrelerine yönelik kısıtlamalar- C# Programlama Kılavuzu
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: bb545d9da73154c237f55809a3a72ff0f121ce1a
ms.sourcegitcommit: 4e2d355baba82814fa53efd6b8bbb45bfe054d11
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/04/2019
ms.locfileid: "70253015"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="24ad1-102">Tür Parametrelerindeki Kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="24ad1-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="24ad1-103">Kısıtlamalar, derleyicisini bir tür bağımsız değişkeni olması gereken yetenekler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="24ad1-104">Herhangi bir kısıtlama olmadan tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="24ad1-105">Derleyici yalnızca <xref:System.Object?displayProperty=nameWithType>, tüm .net türleri için en son temel sınıf olan üyelerini kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="24ad1-106">Daha fazla bilgi için bkz. [kısıtlamaların neden kullanılması](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="24ad1-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="24ad1-107">İstemci kodu bir kısıtlama tarafından izin verilmeyen bir tür kullanarak sınıfınızın örneğini oluşturmaya çalışırsa, sonuç derleme zamanı hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="24ad1-108">Kısıtlamalar, `where` bağlamsal anahtar sözcüğü kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="24ad1-109">Aşağıdaki tabloda yedi tür kısıtlama listelenmektedir:</span><span class="sxs-lookup"><span data-stu-id="24ad1-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="24ad1-110">Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="24ad1-110">Constraint</span></span>|<span data-ttu-id="24ad1-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="24ad1-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="24ad1-112">Tür bağımsız değişkeni bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-112">The type argument must be a value type.</span></span> <span data-ttu-id="24ad1-113">Dışında <xref:System.Nullable%601> herhangi bir değer türü belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="24ad1-114">Null yapılabilir türler hakkında daha fazla bilgi için bkz. [Nullable türler](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="24ad1-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="24ad1-115">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-115">The type argument must be a reference type.</span></span> <span data-ttu-id="24ad1-116">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="24ad1-117">Tür bağımsız değişkeni null yapılamayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-117">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="24ad1-118">Bağımsız değişken, C# 8,0 veya sonraki bir sürümde null atanamaz bir başvuru türü veya null yapılamayan bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-118">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="24ad1-119">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-119">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="24ad1-120">Tür bağımsız değişkeni [yönetilmeyen bir tür](../../language-reference/builtin-types/unmanaged-types.md)olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-120">The type argument must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span>|
|`where T : new()`|<span data-ttu-id="24ad1-121">Tür bağımsız değişkeni Ortak parametresiz bir oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-121">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="24ad1-122">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama en son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-122">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="24ad1-123">`where T :`temel sınıf adı >  *\<*</span><span class="sxs-lookup"><span data-stu-id="24ad1-123">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="24ad1-124">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-124">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="24ad1-125">`where T :` *arabirimadı\<>*</span><span class="sxs-lookup"><span data-stu-id="24ad1-125">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="24ad1-126">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-126">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="24ad1-127">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-127">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="24ad1-128">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-128">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="24ad1-129">T için sağlanan tür bağımsız değişkeni U için sağlanan bağımsız değişkenden türetilmiş veya türemelidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-129">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="24ad1-130">Bazı kısıtlamalar birbirini dışlıyor.</span><span class="sxs-lookup"><span data-stu-id="24ad1-130">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="24ad1-131">Tüm değer türlerinin erişilebilir bir parametresiz oluşturucusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-131">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="24ad1-132">Kısıtlama kısıtlamayı belirtir ve`new()` kısıtlama `struct` kısıtlama ile birleştirilemez. `new()` `struct`</span><span class="sxs-lookup"><span data-stu-id="24ad1-132">The `struct` constraint implies the `new()` constraint and the `new()` constraint can't be combined with the `struct` constraint.</span></span> <span data-ttu-id="24ad1-133">`unmanaged` Kısıtlama kısıtlamayı`struct` gösterir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-133">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="24ad1-134">Kısıtlama, `struct` ya`new()` da kısıtlamalarıyla birleştirilemez. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="24ad1-134">The `unmanaged` constraint can't be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="24ad1-135">Neden kısıtlama kullanılmalıdır</span><span class="sxs-lookup"><span data-stu-id="24ad1-135">Why use constraints</span></span>

<span data-ttu-id="24ad1-136">Tür parametresini kısıtlayan, izin verilen işlem ve metot çağrılarının sayısını kısıtlayan türü tarafından desteklenenlere ve devralma hiyerarşisindeki tüm türlere göre artırırsınız.</span><span class="sxs-lookup"><span data-stu-id="24ad1-136">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="24ad1-137">Genel sınıfları veya yöntemleri tasarlarken, Genel Üyeler üzerinde basit atamanın ötesinde herhangi bir işlem gerçekleştiriyor veya tarafından <xref:System.Object?displayProperty=nameWithType>desteklenmeyen herhangi bir yöntemi çağırdıktan sonra, tür parametresine kısıtlamalar uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-137">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="24ad1-138">Örneğin, temel sınıf kısıtlaması derleyiciye yalnızca bu türden veya bu türden türetilmiş nesnelerin tür bağımsız değişkenleri olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="24ad1-138">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="24ad1-139">Derleyiciye bu garanti verildikten sonra, genel sınıfta bu tür yöntemlerin çağrılmasına izin verebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-139">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="24ad1-140">Aşağıdaki kod örneği, bir temel sınıf kısıtlaması uygulayarak `GenericList<T>` sınıfına ekleyebileceğiniz işlevselliği gösterir (Genel türlere [giriş](introduction-to-generics.md)olarak).</span><span class="sxs-lookup"><span data-stu-id="24ad1-140">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="24ad1-141">Kısıtlama, genel sınıfın `Employee.Name` özelliğini kullanmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="24ad1-141">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="24ad1-142">Kısıtlama, türündeki `T` tüm öğelerin bir `Employee` nesne ya da öğesinden `Employee`devralan nesne olduğunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-142">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="24ad1-143">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamalar aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="24ad1-143">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="24ad1-144">`where T : class` Kısıtlama uygulanırken tür parametresindeki `==` ve `!=` işleçlerden kaçının, çünkü bu işleçler yalnızca başvuru kimliğini test edecek, değer eşitlik için değil.</span><span class="sxs-lookup"><span data-stu-id="24ad1-144">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="24ad1-145">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklense bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-145">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="24ad1-146">Aşağıdaki kod bu noktayı gösterir; <xref:System.String> sınıf işleci`==` aşırı yüklese de çıkış yanlış olur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-146">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="24ad1-147">Derleyici yalnızca derleme zamanında bir `T` başvuru türü olduğunu ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerektiğini bilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-147">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="24ad1-148">Değer eşitlik için test etmeniz gerekiyorsa, önerilen yol, `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlamasını uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimini uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-148">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="24ad1-149">Birden çok parametreyi kısıtlama</span><span class="sxs-lookup"><span data-stu-id="24ad1-149">Constraining multiple parameters</span></span>

<span data-ttu-id="24ad1-150">Aşağıdaki örnekte gösterildiği gibi birden çok parametreye kısıtlama uygulayabilir ve tek bir parametreye birden çok kısıtlama uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="24ad1-150">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="24ad1-151">Sınırlandırılmamış tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="24ad1-151">Unbounded type parameters</span></span>

 <span data-ttu-id="24ad1-152">Public sınıfında `SampleClass<T>{}`T gibi hiçbir kısıtlaması olmayan tür parametrelerine, sınırlandırılmamış tür parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-152">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="24ad1-153">Sınırlandırılmamış tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="24ad1-153">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="24ad1-154">`!=` Ve`==` işleçleri, somut tür bağımsız değişkeninin bu işleçleri destekleyeceği garantisi olmadığından kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-154">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="24ad1-155">Bunlar, herhangi bir arabirim türüne dönüştürülebilir `System.Object` veya buradan dönüştürülebilir ya da açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-155">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="24ad1-156">Bunları [null](../../language-reference/keywords/null.md)ile karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-156">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="24ad1-157">Sınırlandırılmamış bir parametre ile `null`karşılaştırılabiliyorsanız, tür bağımsız değişkeni bir değer türündeyse karşılaştırma her zaman yanlış olarak döndürülür.</span><span class="sxs-lookup"><span data-stu-id="24ad1-157">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="24ad1-158">Parametreleri kısıtlamalar olarak yazın</span><span class="sxs-lookup"><span data-stu-id="24ad1-158">Type parameters as constraints</span></span>

<span data-ttu-id="24ad1-159">Genel tür parametresinin bir kısıtlama olarak kullanılması, aşağıdaki örnekte gösterildiği gibi, kendi tür parametresine sahip bir üye işlevi bu parametreyi kapsayan türün tür parametresiyle kısıtlamak için yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="24ad1-159">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="24ad1-160">Önceki örnekte, `T` `Add` yönteminin bağlamındaki bir tür kısıtlamasıdır ve `List` sınıfı bağlamında sınırsız bir tür parametresi olur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-160">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="24ad1-161">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-161">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="24ad1-162">Tür parametresi, diğer tür parametreleriyle birlikte açılı ayraç içinde bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="24ad1-162">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="24ad1-163">Tür parametrelerinin genel sınıflarla kısıtlamalar olarak yararlılığı sınırlıdır, çünkü derleyici tür parametresi hakkında türetilmediği `System.Object`hariç hiçbir şey kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-163">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="24ad1-164">Tür parametrelerini, iki tür parametresi arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda genel sınıflarda kısıtlamalar olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="24ad1-164">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="24ad1-165">NotNull kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="24ad1-165">NotNull constraint</span></span>

<span data-ttu-id="24ad1-166">8,0 ile C# başlayarak, tür bağımsız değişkeninin null `notnull` yapılamayan bir değer türü veya null yapılamayan bir başvuru türü olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-166">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="24ad1-167">Kısıtlama yalnızca bir `nullable enable` bağlamda kullanılabilir. `notnull`</span><span class="sxs-lookup"><span data-stu-id="24ad1-167">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="24ad1-168">Kısıtlama, `notnull` null yapılabilir bir zorunluluvou bağlamına eklerseniz bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-168">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span> 

<span data-ttu-id="24ad1-169">Diğer kısıtlamaların aksine, bir tür bağımsız değişkeni `notnull` kısıtlamayı ihlal ettiğinde, derleyici bu kod bir `nullable enable` bağlamda derlenirse bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-169">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="24ad1-170">Kod, null yapılabilir bir zorunluluvou bağlamında derlenmişse, derleyici hiçbir uyarı veya hata oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-170">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="24ad1-171">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="24ad1-171">Unmanaged constraint</span></span>

<span data-ttu-id="24ad1-172">7,3 ile C# başlayarak, tür parametresinin yönetilmeyen bir `unmanaged` [tür](../../language-reference/builtin-types/unmanaged-types.md)olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-172">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="24ad1-173">`unmanaged` Kısıtlama, aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak işlenebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="24ad1-173">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="24ad1-174">Önceki yöntem, yerleşik bir tür olarak bilinen `unsafe` bir tür üzerinde `sizeof` işleç kullandığından bir bağlamda derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="24ad1-174">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="24ad1-175">`unmanaged` Kısıtlama`sizeof` olmadan işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-175">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="24ad1-176">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="24ad1-176">Delegate constraints</span></span>

<span data-ttu-id="24ad1-177">Ayrıca, 7,3 C# ' den itibaren, veya <xref:System.Delegate?displayProperty=nameWithType> <xref:System.MulticastDelegate?displayProperty=nameWithType> temel sınıf kısıtlaması olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="24ad1-178">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dil izin vermez.</span><span class="sxs-lookup"><span data-stu-id="24ad1-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="24ad1-179">Kısıtlama `System.Delegate` , temsilcilerle birlikte, tür açısından güvenli bir şekilde kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="24ad1-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="24ad1-180">Aşağıdaki kod, iki temsilciyi aynı tür olduklarından birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="24ad1-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="24ad1-181">Aynı türdeki temsilcileri birleştirmek için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="24ad1-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="24ad1-182">Son satırın açıklamasını kaldırırsanız, derlenmez.</span><span class="sxs-lookup"><span data-stu-id="24ad1-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="24ad1-183">Hem hem de `first` temsilci türlerdir,ancakfarklıtemsilcitürleridir.`test`</span><span class="sxs-lookup"><span data-stu-id="24ad1-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="24ad1-184">Sabit listesi kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="24ad1-184">Enum constraints</span></span>

<span data-ttu-id="24ad1-185">C# 7,3 ' den başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü temel sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="24ad1-186">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dil izin vermez.</span><span class="sxs-lookup"><span data-stu-id="24ad1-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="24ad1-187">Kullanan `System.Enum` genel türler, içindeki `System.Enum`statik yöntemleri kullanarak sonuçları önbelleğe almak için tür kullanımı uyumlu programlama sağlar.</span><span class="sxs-lookup"><span data-stu-id="24ad1-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="24ad1-188">Aşağıdaki örnek, bir sabit listesi türü için geçerli tüm değerleri bulur ve sonra bu değerleri dize gösterimiyle eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="24ad1-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="24ad1-189">Kullanılan yöntemler, performans etkilerine sahip olan yansıma kullanımını kullanır.</span><span class="sxs-lookup"><span data-stu-id="24ad1-189">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="24ad1-190">Bu yöntemi, yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="24ad1-191">Aşağıdaki örnekte gösterildiği gibi, bir sabit listesi oluşturmak ve değerlerini ve adlarını bir sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="24ad1-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="24ad1-192">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="24ad1-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="24ad1-193">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="24ad1-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="24ad1-194">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="24ad1-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="24ad1-195">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="24ad1-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="24ad1-196">new Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="24ad1-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
