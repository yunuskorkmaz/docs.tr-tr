---
title: -Tür parametrelerindeki kısıtlamalar C# Programlama Kılavuzu
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 08863e677413c27461d621c7126c64f2b76c33a8
ms.sourcegitcommit: 41c0637e894fbcd0713d46d6ef1866f08dc321a2
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/01/2019
ms.locfileid: "57202606"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="e791b-102">(C# programlama Kılavuzu) tür parametrelerindeki kısıtlamalar</span><span class="sxs-lookup"><span data-stu-id="e791b-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="e791b-103">Kısıtlamaları, derleyici bir tür bağımsız değişkeni olmalıdır özellikleri hakkında bilgilendirin.</span><span class="sxs-lookup"><span data-stu-id="e791b-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="e791b-104">Herhangi bir kısıtlama tür bağımsız değişkeni, herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="e791b-105">Derleyici, yalnızca üyelerinin varsayabilirsiniz <xref:System.Object?displayProperty=nameWithType>, üstün temel sınıf için herhangi bir .NET türü.</span><span class="sxs-lookup"><span data-stu-id="e791b-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="e791b-106">Daha fazla bilgi için [kısıtlamaları neden kullanılmalı](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="e791b-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="e791b-107">İstemci kodu, sınıf kısıtlaması tarafından izin verilmeyen bir türü kullanarak örneği oluşturmak çalışırsa, sonucu bir derleme zamanı hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="e791b-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="e791b-108">Kısıtlamaları kullanılarak belirtilir `where` bağlamsal anahtar sözcük.</span><span class="sxs-lookup"><span data-stu-id="e791b-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="e791b-109">Aşağıdaki tabloda, yedi kısıtlamalar türleri listelenmektedir:</span><span class="sxs-lookup"><span data-stu-id="e791b-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="e791b-110">Kısıtlama</span><span class="sxs-lookup"><span data-stu-id="e791b-110">Constraint</span></span>|<span data-ttu-id="e791b-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="e791b-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="e791b-112">Tür bağımsız değişkeni bir değer türü olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="e791b-112">The type argument must be a value type.</span></span> <span data-ttu-id="e791b-113">Herhangi bir değer türü hariç <xref:System.Nullable%601> belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="e791b-114">Boş değer atanabilir türler hakkında daha fazla bilgi için bkz: [boş değer atanabilir türler](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="e791b-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="e791b-115">Tür bağımsız değişkeninin bir başvuru türü olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="e791b-115">The type argument must be a reference type.</span></span> <span data-ttu-id="e791b-116">Bu kısıtlama, tüm sınıf, arabirim, temsilci ya da dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="e791b-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="e791b-117">Tür bağımsız değişkeninin bir başvuru türü olmalıdır ve iç içe geçme herhangi bir düzeyde herhangi bir başvuru türü üye içermemelidir.</span><span class="sxs-lookup"><span data-stu-id="e791b-117">The type argument must not be a reference type and must not contain any reference type members at any level of nesting.</span></span>|
|`where T : new()`|<span data-ttu-id="e791b-118">Tür bağımsız değişkeni genel bir parametresiz oluşturucusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="e791b-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="e791b-119">Diğer kısıtlamalarla birlikte kullanıldığında `new()` kısıtlaması en son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="e791b-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="e791b-120">`where T :` *\<temel sınıf adı >*</span><span class="sxs-lookup"><span data-stu-id="e791b-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="e791b-121">Tür bağımsız değişkeni, olabilir veya belirtilen temel sınıfından türetilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="e791b-122">`where T :` *\<Arabirim adı >*</span><span class="sxs-lookup"><span data-stu-id="e791b-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="e791b-123">Tür bağımsız değişkeni, olabilir veya belirtilen arabirim uygular.</span><span class="sxs-lookup"><span data-stu-id="e791b-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="e791b-124">Birden çok arabirim kısıtlamasını belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="e791b-125">Kısıtlama arabirimi genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="e791b-126">T olabilir veya u için sağlanan bağımsız değişken türetmek için sağlanan tür bağımsız değişkeni</span><span class="sxs-lookup"><span data-stu-id="e791b-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="e791b-127">Bazı kısıtlamalar birbirini dışlar.</span><span class="sxs-lookup"><span data-stu-id="e791b-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="e791b-128">Tüm değer türleri, erişilebilir bir parametresiz oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="e791b-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="e791b-129">`struct` Kısıtlaması gelir `new()` kısıtlaması ve `new()` kısıtlaması ile birleştirilemez `struct` kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="e791b-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="e791b-130">`unmanaged` Kısıtlaması gelir `struct` kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="e791b-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="e791b-131">`unmanaged` Kısıtlaması ile birleştirilemez `struct` veya `new()` kısıtlamaları.</span><span class="sxs-lookup"><span data-stu-id="e791b-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="e791b-132">Kısıtlamaları neden kullanılır?</span><span class="sxs-lookup"><span data-stu-id="e791b-132">Why use constraints</span></span>

<span data-ttu-id="e791b-133">Tür parametresi kısıtlayarak, izin verilen işlemler ve kısıtlama türü ve devralma hiyerarşisindeki tüm türleri tarafından desteklenen yöntem çağrılarını sayısını artırın.</span><span class="sxs-lookup"><span data-stu-id="e791b-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="e791b-134">Tasarlarken genel sınıfları veya yöntemleri, basit atamasını ötesindeki genel üyeleri üzerinde herhangi bir işlemi gerçekleştirme veya kaldırılacak desteklenmeyen herhangi bir yöntem çağırma varsa <xref:System.Object?displayProperty=nameWithType>, tür parametresi kısıtlamaları uygulamak gerekir.</span><span class="sxs-lookup"><span data-stu-id="e791b-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="e791b-135">Örneğin, temel sınıf kısıtlaması, yalnızca bu tür nesnelerin derleyiciye veya bundan türetilmiş türü, tür bağımsız değişkeni olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="e791b-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="e791b-136">Derleyici bu garanti olduğunda, bu türdeki genel sınıfta çağrılacak yöntem izin verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="e791b-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="e791b-137">Aşağıdaki kod örneği ekleyebilirsiniz işlevselliğini gösterir `GenericList<T>` sınıfı (içinde [genel türlere giriş](introduction-to-generics.md)) uygulayarak bir temel sınıf kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="e791b-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="e791b-138">Kullanılacak genel sınıf kısıtlaması sağlayan `Employee.Name` özelliği.</span><span class="sxs-lookup"><span data-stu-id="e791b-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="e791b-139">Kısıtlama belirten türündeki tüm öğeleri `T` aşağıdakilerden biri olması garanti bir `Employee` veya devralınan bir nesneyi `Employee`.</span><span class="sxs-lookup"><span data-stu-id="e791b-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="e791b-140">Birden çok kısıtlaması aynı tür parametresi için uygulanabilir ve sınırlamalar genel türler, aşağıdaki gibi olabilir:</span><span class="sxs-lookup"><span data-stu-id="e791b-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="e791b-141">Uygularken `where T : class` kısıtlaması, kaçının `==` ve `!=` işleçlerini tür parametresi olduğundan, bu işleçler yalnızca başvuru kimliği değil değeri eşitlik için test eder.</span><span class="sxs-lookup"><span data-stu-id="e791b-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="e791b-142">Bağımsız değişken olarak kullanılan bir tür içinde bu işleçler aşırı yüklenmiş olsa bile bu davranış oluşur.</span><span class="sxs-lookup"><span data-stu-id="e791b-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="e791b-143">Aşağıdaki kod bu noktayı gösterir; Çıkış olsa bile yanlış <xref:System.String> sınıfı aşırı `==` işleci.</span><span class="sxs-lookup"><span data-stu-id="e791b-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="e791b-144">Derleyici, yalnızca T derleme zamanında bir başvuru türüdür ve tüm başvuru türleri için geçerli varsayılan işleçlerini kullanmalısınız bilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="e791b-145">İçin değer eşitliği test etmeniz gerekir, ayrıca uygulamak için önerilen yöntem olduğu `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlaması ve genel bir sınıf oluşturmak için kullanılan herhangi bir sınıf içinde arabirim uygular.</span><span class="sxs-lookup"><span data-stu-id="e791b-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="e791b-146">Birden çok parametre sınırlama</span><span class="sxs-lookup"><span data-stu-id="e791b-146">Constraining multiple parameters</span></span>

<span data-ttu-id="e791b-147">Aşağıdaki örnekte gösterildiği gibi birden çok parametre ve birden çok kısıtlaması tek bir parametre için kısıtlamalar uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="e791b-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="e791b-148">Sınırsız tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="e791b-148">Unbounded type parameters</span></span>

 <span data-ttu-id="e791b-149">Tür T genel sınıfında gibi herhangi bir kısıtlama söz konusu parametrelerindeki `SampleClass<T>{}`, sınırsız tür parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="e791b-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="e791b-150">Sınırsız tür parametreleri, aşağıdaki kurallar vardır:</span><span class="sxs-lookup"><span data-stu-id="e791b-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="e791b-151">`!=` Ve `==` işleçleri somut tür bağımsız değişkeni, bu işleçler destekleyecek garanti olduğundan kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="e791b-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="e791b-152">Ve ondan dönüştürülebilir `System.Object` veya herhangi bir arabirim türüne açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="e791b-153">Bunları karşılaştıran [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="e791b-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="e791b-154">Sınırsız bir parametre karşılaştırılır, `null`, karşılaştırma her zaman tür bağımsız değişkeni bir değer türü ise false döndürür.</span><span class="sxs-lookup"><span data-stu-id="e791b-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="e791b-155">Kısıtlama olarak tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="e791b-155">Type parameters as constraints</span></span>

<span data-ttu-id="e791b-156">Kısıtlama yararlı bir üye işlevi çalıştırıldığında, kendi tür parametresi olduğundan aşağıdaki örnekte gösterildiği gibi kapsanan türün tür parametresi, parametre sınırlamak bir genel tür parametresi kullanımı vardır:</span><span class="sxs-lookup"><span data-stu-id="e791b-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="e791b-157">Önceki örnekte, `T` bağlamında bir tür kısıtlaması `Add` yöntemi ve bir sınırsız tür parametre bağlamında `List` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="e791b-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="e791b-158">Tür parametreleri, genel sınıf tanımlarındaki kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="e791b-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="e791b-159">Tür parametresi, diğer tür parametrelerinin birlikte açılı ayraçlar içinde bildirilmesi gerekir:</span><span class="sxs-lookup"><span data-stu-id="e791b-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="e791b-160">Öğesinden türetilen dışında derleyicinin tür parametresi hakkında hiçbir şey kabul edilebilir olduğundan, tür parametreleri ile genel sınıflar kısıtlama olarak kullanışlılığını sınırlıdır `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="e791b-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="e791b-161">Tür parametreleri, iki tür parametreleri arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda Genel sınıflar üzerinde kısıtlama olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="e791b-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="e791b-162">Yönetilmeyen kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="e791b-162">Unmanaged constraint</span></span>

<span data-ttu-id="e791b-163">Kullanabileceğiniz C# 7.3 ile başlayarak, `unmanaged` kısıtlaması, tür parametresi olması gerektiğini belirtmek için bir **yönetilmeyen tür**.</span><span class="sxs-lookup"><span data-stu-id="e791b-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an **unmanaged type**.</span></span> <span data-ttu-id="e791b-164">Bir **yönetilmeyen tür** , bir başvuru türü değil ve başvuru türü alanları iç içe geçme herhangi bir düzeyde içermeyen bir türdür.</span><span class="sxs-lookup"><span data-stu-id="e791b-164">An **unmanaged type** is a type that is not a reference type and doesn't contain reference type fields at any level of nesting.</span></span> <span data-ttu-id="e791b-165">`unmanaged` Kısıtlaması, aşağıdaki örnekte gösterildiği gibi bellek bloklarını yönetilebilir türleriyle çalışmak için yeniden kullanılabilir rutinleri yazmanıza olanak sağlar:</span><span class="sxs-lookup"><span data-stu-id="e791b-165">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="e791b-166">Önceki yöntem derlenmesi gereken bir `unsafe` bağlam kullandığından `sizeof` işleci yerleşik bir tür olarak bilinmeyen bir türde.</span><span class="sxs-lookup"><span data-stu-id="e791b-166">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="e791b-167">Olmadan `unmanaged` kısıtlaması `sizeof` işleci kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="e791b-167">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="e791b-168">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="e791b-168">Delegate constraints</span></span>

<span data-ttu-id="e791b-169">Kullanabileceğiniz ayrıca C# 7.3 ile başlayarak, <xref:System.Delegate?displayProperty=nameWithType> veya <xref:System.MulticastDelegate?displayProperty=nameWithType> bir temel sınıf kısıtlama olarak.</span><span class="sxs-lookup"><span data-stu-id="e791b-169">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="e791b-170">CLR, bu kısıtlama her zaman izin verilir, ancak C# dili izin verilmiyor.</span><span class="sxs-lookup"><span data-stu-id="e791b-170">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="e791b-171">`System.Delegate` Kısıtlaması ile temsilciler bir tür kullanımı uyumlu şekilde çalışan kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="e791b-171">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="e791b-172">Aşağıdaki kod, aynı türde oldukları sağlanan iki temsilcilerin birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="e791b-172">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="e791b-173">Aynı türdeyse temsilcileri birleştirme için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="e791b-173">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="e791b-174">Son satırı açıklamadan çıkarın, derlenemeyecektir.</span><span class="sxs-lookup"><span data-stu-id="e791b-174">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="e791b-175">Her ikisi de `first` ve `test` temsilci türleri, ancak bunlar farklı temsilci türleri.</span><span class="sxs-lookup"><span data-stu-id="e791b-175">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="e791b-176">Enum kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="e791b-176">Enum constraints</span></span>

<span data-ttu-id="e791b-177">C# 7.3 başlayarak, ayrıca belirtebileceğiniz <xref:System.Enum?displayProperty=nameWithType> bir temel sınıf kısıtlaması türü.</span><span class="sxs-lookup"><span data-stu-id="e791b-177">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="e791b-178">CLR, bu kısıtlama her zaman izin verilir, ancak C# dili izin verilmiyor.</span><span class="sxs-lookup"><span data-stu-id="e791b-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="e791b-179">Genel türler kullanarak `System.Enum` tür kullanımı uyumlu programlama sonuçlarını önbelleğe alma statik yöntemleri kullanmasını sağlamak `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="e791b-179">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="e791b-180">Aşağıdaki örnek, bir sabit listesi türü için geçerli tüm değerlerin bulur ve sonra bu değerleri dize gösterimine eşleyen sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="e791b-180">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="e791b-181">Kullanılan yöntemleri olun performans şifrelemelerini yansıma kullanın.</span><span class="sxs-lookup"><span data-stu-id="e791b-181">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="e791b-182">Önbelleğe alınan ve yeniden bir koleksiyon oluşturmak için bu yöntem çağırabilirsiniz yansıma gerektiren çağrıları yinelenen yerine.</span><span class="sxs-lookup"><span data-stu-id="e791b-182">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="e791b-183">Bunu aşağıdaki örnekte gösterildiği gibi bir sabit listesi ve adları ve değerleri sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="e791b-183">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="e791b-184">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="e791b-184">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="e791b-185">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="e791b-185">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)
- [<span data-ttu-id="e791b-186">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="e791b-186">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/introduction-to-generics.md)
- [<span data-ttu-id="e791b-187">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="e791b-187">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)
- [<span data-ttu-id="e791b-188">new Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="e791b-188">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)
