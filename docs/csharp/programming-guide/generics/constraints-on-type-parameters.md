---
title: Tür parametrelerine yönelik kısıtlamalar- C# Programlama Kılavuzu
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 4f0277ef5883a238cf2579d2d9ea956bc06061e2
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/19/2019
ms.locfileid: "69589865"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="f0971-102">Tür Parametrelerindeki Kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="f0971-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="f0971-103">Kısıtlamalar, derleyicisini bir tür bağımsız değişkeni olması gereken yetenekler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="f0971-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="f0971-104">Herhangi bir kısıtlama olmadan tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="f0971-105">Derleyici yalnızca <xref:System.Object?displayProperty=nameWithType>, tüm .net türleri için en son temel sınıf olan üyelerini kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="f0971-106">Daha fazla bilgi için bkz. [kısıtlamaların neden kullanılması](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="f0971-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="f0971-107">İstemci kodu bir kısıtlama tarafından izin verilmeyen bir tür kullanarak sınıfınızın örneğini oluşturmaya çalışırsa, sonuç derleme zamanı hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="f0971-108">Kısıtlamalar, `where` bağlamsal anahtar sözcüğü kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="f0971-109">Aşağıdaki tabloda yedi tür kısıtlama listelenmektedir:</span><span class="sxs-lookup"><span data-stu-id="f0971-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="f0971-110">Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="f0971-110">Constraint</span></span>|<span data-ttu-id="f0971-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="f0971-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="f0971-112">Tür bağımsız değişkeni bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-112">The type argument must be a value type.</span></span> <span data-ttu-id="f0971-113">Dışında <xref:System.Nullable%601> herhangi bir değer türü belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="f0971-114">Null yapılabilir türler hakkında daha fazla bilgi için bkz. [Nullable türler](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="f0971-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="f0971-115">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-115">The type argument must be a reference type.</span></span> <span data-ttu-id="f0971-116">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="f0971-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="f0971-117">Tür bağımsız değişkeni [yönetilmeyen bir tür](../../language-reference/builtin-types/unmanaged-types.md)olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-117">The type argument must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span>|
|`where T : new()`|<span data-ttu-id="f0971-118">Tür bağımsız değişkeni Ortak parametresiz bir oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="f0971-119">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama en son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="f0971-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="f0971-120">`where T :`temel sınıf adı >  *\<*</span><span class="sxs-lookup"><span data-stu-id="f0971-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="f0971-121">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="f0971-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="f0971-122">`where T :` *arabirimadı\<>*</span><span class="sxs-lookup"><span data-stu-id="f0971-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="f0971-123">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="f0971-124">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="f0971-125">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="f0971-126">T için sağlanan tür bağımsız değişkeni U için sağlanan bağımsız değişkenden türetilmiş veya türemelidir.</span><span class="sxs-lookup"><span data-stu-id="f0971-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="f0971-127">Bazı kısıtlamalar birbirini dışlıyor.</span><span class="sxs-lookup"><span data-stu-id="f0971-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="f0971-128">Tüm değer türlerinin erişilebilir bir parametresiz oluşturucusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f0971-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="f0971-129">Kısıtlama kısıtlamayı belirtir ve`new()` kısıtlama `struct` kısıtlama ile birleştirilemez. `new()` `struct`</span><span class="sxs-lookup"><span data-stu-id="f0971-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="f0971-130">`unmanaged` Kısıtlama kısıtlamayı`struct` gösterir.</span><span class="sxs-lookup"><span data-stu-id="f0971-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="f0971-131">Kısıtlama, `struct` ya`new()` da kısıtlamalarıyla birleştirilemez. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="f0971-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="f0971-132">Neden kısıtlama kullanılmalıdır</span><span class="sxs-lookup"><span data-stu-id="f0971-132">Why use constraints</span></span>

<span data-ttu-id="f0971-133">Tür parametresini kısıtlayan, izin verilen işlem ve metot çağrılarının sayısını kısıtlayan türü tarafından desteklenenlere ve devralma hiyerarşisindeki tüm türlere göre artırırsınız.</span><span class="sxs-lookup"><span data-stu-id="f0971-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="f0971-134">Genel sınıfları veya yöntemleri tasarlarken, Genel Üyeler üzerinde basit atamanın ötesinde herhangi bir işlem gerçekleştirmeye veya tarafından <xref:System.Object?displayProperty=nameWithType>desteklenmeyen herhangi bir yöntemi çağırdıktan sonra, tür parametresine kısıtlamalar uygulamanız gerekecektir.</span><span class="sxs-lookup"><span data-stu-id="f0971-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="f0971-135">Örneğin, temel sınıf kısıtlaması derleyiciye yalnızca bu türden veya bu türden türetilmiş nesnelerin tür bağımsız değişkenleri olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="f0971-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="f0971-136">Derleyiciye bu garanti verildikten sonra, genel sınıfta bu tür yöntemlerin çağrılmasına izin verebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="f0971-137">Aşağıdaki kod örneği, bir temel sınıf kısıtlaması uygulayarak `GenericList<T>` sınıfına ekleyebileceğiniz işlevselliği gösterir (Genel türlere [giriş](introduction-to-generics.md)olarak).</span><span class="sxs-lookup"><span data-stu-id="f0971-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="f0971-138">Kısıtlama, genel sınıfın `Employee.Name` özelliğini kullanmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="f0971-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="f0971-139">Kısıtlama, türündeki `T` tüm öğelerin bir `Employee` nesne ya da öğesinden `Employee`devralan nesne olduğunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="f0971-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="f0971-140">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamalar aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="f0971-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="f0971-141">`where T : class` Kısıtlama uygulanırken tür parametresindeki `==` ve `!=` işleçlerden kaçının, çünkü bu işleçler yalnızca başvuru kimliğini test edecek, değer eşitlik için değil.</span><span class="sxs-lookup"><span data-stu-id="f0971-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="f0971-142">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklense bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="f0971-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="f0971-143">Aşağıdaki kod bu noktayı gösterir; <xref:System.String> sınıf işleci`==` aşırı yüklese de çıkış yanlış olur.</span><span class="sxs-lookup"><span data-stu-id="f0971-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="f0971-144">Derleyici yalnızca T 'in derleme zamanında bir başvuru türü olduğunu ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerektiğini bilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="f0971-145">Değer eşitlik için test etmeniz gerekiyorsa, önerilen yol, `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlamasını uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimini uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="f0971-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="f0971-146">Birden çok parametreyi kısıtlama</span><span class="sxs-lookup"><span data-stu-id="f0971-146">Constraining multiple parameters</span></span>

<span data-ttu-id="f0971-147">Aşağıdaki örnekte gösterildiği gibi birden çok parametreye kısıtlama uygulayabilir ve tek bir parametreye birden çok kısıtlama uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f0971-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="f0971-148">Sınırlandırılmamış tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="f0971-148">Unbounded type parameters</span></span>

 <span data-ttu-id="f0971-149">Public sınıfında `SampleClass<T>{}`T gibi hiçbir kısıtlaması olmayan tür parametrelerine, sınırlandırılmamış tür parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="f0971-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="f0971-150">Sınırlandırılmamış tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="f0971-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="f0971-151">`!=` Ve`==` işleçleri, somut tür bağımsız değişkeninin bu işleçleri destekleyeceği garantisi olmadığından kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="f0971-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="f0971-152">Bunlar, herhangi bir arabirim türüne dönüştürülebilir `System.Object` veya buradan dönüştürülebilir ya da açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="f0971-153">Bunları [null](../../language-reference/keywords/null.md)ile karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0971-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="f0971-154">Sınırlandırılmamış bir parametre ile `null`karşılaştırılabiliyorsanız, tür bağımsız değişkeni bir değer türündeyse karşılaştırma her zaman yanlış olarak döndürülür.</span><span class="sxs-lookup"><span data-stu-id="f0971-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="f0971-155">Parametreleri kısıtlamalar olarak yazın</span><span class="sxs-lookup"><span data-stu-id="f0971-155">Type parameters as constraints</span></span>

<span data-ttu-id="f0971-156">Genel tür parametresinin bir kısıtlama olarak kullanılması, aşağıdaki örnekte gösterildiği gibi, kendi tür parametresine sahip bir üye işlevi bu parametreyi kapsayan türün tür parametresiyle kısıtlamak için yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="f0971-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="f0971-157">Önceki örnekte, `T` `Add` yönteminin bağlamındaki bir tür kısıtlamasıdır ve `List` sınıfı bağlamında sınırsız bir tür parametresi olur.</span><span class="sxs-lookup"><span data-stu-id="f0971-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="f0971-158">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="f0971-159">Tür parametresi, diğer tür parametreleriyle birlikte açılı ayraç içinde bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="f0971-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="f0971-160">Tür parametrelerinin genel sınıflarla kısıtlamalar olarak yararlılığı sınırlıdır, çünkü derleyici tür parametresi hakkında türetilmediği `System.Object`hariç hiçbir şey kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="f0971-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="f0971-161">Tür parametrelerini, iki tür parametresi arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda genel sınıflarda kısıtlamalar olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="f0971-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="f0971-162">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="f0971-162">Unmanaged constraint</span></span>

<span data-ttu-id="f0971-163">7,3 ile C# başlayarak, tür parametresinin yönetilmeyen bir `unmanaged` [tür](../../language-reference/builtin-types/unmanaged-types.md)olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0971-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="f0971-164">`unmanaged` Kısıtlama, aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak işlenebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="f0971-164">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="f0971-165">Önceki yöntem, yerleşik bir tür olarak bilinen `unsafe` bir tür üzerinde `sizeof` işleç kullandığından bir bağlamda derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="f0971-165">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="f0971-166">`unmanaged` Kısıtlama`sizeof` olmadan işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="f0971-166">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="f0971-167">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="f0971-167">Delegate constraints</span></span>

<span data-ttu-id="f0971-168">Ayrıca, 7,3 C# ' den itibaren, veya <xref:System.Delegate?displayProperty=nameWithType> <xref:System.MulticastDelegate?displayProperty=nameWithType> temel sınıf kısıtlaması olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0971-168">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="f0971-169">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dil izin vermez.</span><span class="sxs-lookup"><span data-stu-id="f0971-169">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="f0971-170">Kısıtlama `System.Delegate` , temsilcilerle birlikte, tür açısından güvenli bir şekilde kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="f0971-170">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="f0971-171">Aşağıdaki kod, aynı türde olmaları şartıyla iki temsilciyi birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="f0971-171">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="f0971-172">Aynı türdeki temsilcileri birleştirmek için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f0971-172">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="f0971-173">Son satırın açıklamasını kaldırırsanız, derlenmez.</span><span class="sxs-lookup"><span data-stu-id="f0971-173">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="f0971-174">Hem hem de `first` temsilci türlerdir,ancakfarklıtemsilcitürlerdir.`test`</span><span class="sxs-lookup"><span data-stu-id="f0971-174">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="f0971-175">Sabit listesi kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="f0971-175">Enum constraints</span></span>

<span data-ttu-id="f0971-176">C# 7,3 ' den başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü temel sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0971-176">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="f0971-177">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dil izin vermez.</span><span class="sxs-lookup"><span data-stu-id="f0971-177">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="f0971-178">Kullanan `System.Enum` genel türler, içindeki `System.Enum`statik yöntemleri kullanarak sonuçları önbelleğe almak için tür kullanımı uyumlu programlama sağlar.</span><span class="sxs-lookup"><span data-stu-id="f0971-178">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="f0971-179">Aşağıdaki örnek, bir sabit listesi türü için geçerli tüm değerleri bulur ve sonra bu değerleri dize gösterimiyle eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f0971-179">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="f0971-180">Kullanılan yöntemler, performans etkilerine sahip olan yansıma kullanımını kullanır.</span><span class="sxs-lookup"><span data-stu-id="f0971-180">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="f0971-181">Bu yöntemi, yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f0971-181">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="f0971-182">Aşağıdaki örnekte gösterildiği gibi, bir sabit listesi oluşturmak ve değerlerini ve adlarını bir sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f0971-182">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="f0971-183">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="f0971-183">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="f0971-184">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="f0971-184">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="f0971-185">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="f0971-185">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="f0971-186">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="f0971-186">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="f0971-187">new Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="f0971-187">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
