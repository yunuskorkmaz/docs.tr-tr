---
title: Tür parametrelerine yönelik kısıtlamalar-C# Programlama Kılavuzu
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 376befe4c969ac653e234479c8946d7fd4242999
ms.sourcegitcommit: 7b1497c1927cb449cefd313bc5126ae37df30746
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/16/2020
ms.locfileid: "83442221"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="da99c-102">Tür Parametrelerindeki Kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="da99c-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="da99c-103">Kısıtlamalar, derleyicisini bir tür bağımsız değişkeni olması gereken yetenekler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="da99c-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="da99c-104">Herhangi bir kısıtlama olmadan tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="da99c-105">Derleyici yalnızca <xref:System.Object?displayProperty=nameWithType> , tüm .net türleri için en son temel sınıf olan üyelerini kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="da99c-106">Daha fazla bilgi için bkz. [kısıtlamaların neden kullanılması](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="da99c-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="da99c-107">İstemci kodu bir kısıtlamayı karşılamayan bir tür kullanıyorsa, derleyici bir hata verir.</span><span class="sxs-lookup"><span data-stu-id="da99c-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="da99c-108">Kısıtlamalar, `where` bağlamsal anahtar sözcüğü kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="da99c-109">Aşağıdaki tabloda yedi tür kısıtlama listelenmektedir:</span><span class="sxs-lookup"><span data-stu-id="da99c-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="da99c-110">Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="da99c-110">Constraint</span></span>|<span data-ttu-id="da99c-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="da99c-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="da99c-112">Tür bağımsız değişkeni null yapılamayan bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="da99c-113">Nullable değer türleri hakkında daha fazla bilgi için bkz. [Nullable değer türleri](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="da99c-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="da99c-114">Tüm değer türlerinde erişilebilir parametresiz bir Oluşturucu olduğundan, kısıtlama `struct` `new()` kısıtlamayı gösterir ve `new()` kısıtlamayla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="da99c-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="da99c-115">Kısıtlamayı `struct` `unmanaged` kısıtlamasıyla birleştiremezsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-115">You can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="da99c-116">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-116">The type argument must be a reference type.</span></span> <span data-ttu-id="da99c-117">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="da99c-118">C# 8,0 veya sonraki bir sürümde null yapılabilir bir bağlamda, `T` null atanamaz bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="da99c-119">Tür bağımsız değişkeni, null yapılabilir veya null yapılamayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="da99c-120">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="da99c-121">Tür bağımsız değişkeni null yapılamayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="da99c-122">Bağımsız değişken, C# 8,0 veya sonraki bir sürümde null atanamaz bir başvuru türü veya null yapılamayan bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="da99c-123">Tür bağımsız değişkeni null atanamaz bir [yönetilmeyen tür](../../language-reference/builtin-types/unmanaged-types.md)olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="da99c-124">`unmanaged`Kısıtlama `struct` kısıtlamayı gösterir ve ya da kısıtlamalarıyla birleştirilemez `struct` `new()` .</span><span class="sxs-lookup"><span data-stu-id="da99c-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="da99c-125">Tür bağımsız değişkeni Ortak parametresiz bir oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="da99c-126">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama en son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="da99c-127">`new()`Kısıtlama `struct` ve `unmanaged` kısıtlamalarıyla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="da99c-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="da99c-128">`where T :`\* \< temel sınıf adı>\*</span><span class="sxs-lookup"><span data-stu-id="da99c-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="da99c-129">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="da99c-130">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen temel sınıftan türetilmiş null atanamaz bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="da99c-131">`where T :`\* \< temel sınıf adı>?\*</span><span class="sxs-lookup"><span data-stu-id="da99c-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="da99c-132">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="da99c-133">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen temel sınıftan türetilmiş null yapılabilen veya null yapılamayan bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="da99c-134">`where T :`\* \< arabirim adı>\*</span><span class="sxs-lookup"><span data-stu-id="da99c-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="da99c-135">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="da99c-136">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="da99c-137">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="da99c-138">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen arabirimi uygulayan null yapılamayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="da99c-139">`where T :`\* \< arabirim adı>?\*</span><span class="sxs-lookup"><span data-stu-id="da99c-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="da99c-140">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="da99c-141">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="da99c-142">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="da99c-143">C# 8,0 ' de null yapılabilir bir bağlamda, `T` null yapılabilir bir başvuru türü, null olamayan bir başvuru türü veya değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="da99c-144">`T`null yapılabilen bir değer türü olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="da99c-145">İçin sağlanan tür bağımsız değişkeni, `T` için sağlanan bağımsız değişkenden türetilmiş veya türetilmiş olmalıdır `U` .</span><span class="sxs-lookup"><span data-stu-id="da99c-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="da99c-146">Null yapılabilir bir bağlamda, `U` null atanamaz bir başvuru türü ise, `T` null olamayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="da99c-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="da99c-147">`U`Null yapılabilir bir başvuru türü ise, `T` null yapılabilir veya null atanamaz olabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="da99c-148">Neden kısıtlama kullanılmalıdır</span><span class="sxs-lookup"><span data-stu-id="da99c-148">Why use constraints</span></span>

<span data-ttu-id="da99c-149">Kısıtlamalar, bir tür parametresinin yeteneklerini ve beklentilerini belirtir.</span><span class="sxs-lookup"><span data-stu-id="da99c-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="da99c-150">Bu kısıtlamaları bildirmek, kısıtlama türünün işlemlerini ve Yöntem çağrılarını kullanabileceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="da99c-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="da99c-151">Genel bir sınıf veya yönteminiz, Genel Üyeler üzerinde basit atamanın ötesinde veya tarafından desteklenmeyen herhangi bir yöntemi çağıran herhangi bir işlem kullanıyorsa <xref:System.Object?displayProperty=nameWithType> , tür parametresine kısıtlamalar uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="da99c-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="da99c-152">Örneğin, temel sınıf kısıtlaması derleyiciye yalnızca bu türden veya bu türden türetilmiş nesnelerin tür bağımsız değişkenleri olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="da99c-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="da99c-153">Derleyiciye bu garanti verildikten sonra, genel sınıfta bu tür yöntemlerin çağrılmasına izin verebilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="da99c-154">Aşağıdaki kod örneği, `GenericList<T>` bir temel sınıf kısıtlaması uygulayarak sınıfına ekleyebileceğiniz işlevselliği gösterir ( [Genel türlere giriş](../../../standard/generics/index.md)olarak).</span><span class="sxs-lookup"><span data-stu-id="da99c-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="da99c-155">Kısıtlama, genel sınıfın özelliğini kullanmasını sağlar `Employee.Name` .</span><span class="sxs-lookup"><span data-stu-id="da99c-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="da99c-156">Kısıtlama, türündeki tüm öğelerin `T` bir `Employee` nesne ya da öğesinden devralan nesne olduğunu belirtir `Employee` .</span><span class="sxs-lookup"><span data-stu-id="da99c-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="da99c-157">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamalar aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="da99c-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="da99c-158">`where T : class`Kısıtlama uygulanırken `==` `!=` tür parametresindeki ve işleçlerden kaçının, çünkü bu işleçler yalnızca başvuru kimliğini test edecek, değer eşitlik için değil.</span><span class="sxs-lookup"><span data-stu-id="da99c-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="da99c-159">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklense bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="da99c-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="da99c-160">Aşağıdaki kod bu noktayı gösterir; sınıf işleci aşırı yüklese de çıkış yanlış olur <xref:System.String> `==` .</span><span class="sxs-lookup"><span data-stu-id="da99c-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="da99c-161">Derleyici yalnızca `T` derleme zamanında bir başvuru türü olduğunu ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerektiğini bilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="da99c-162">Değer eşitlik için test etmeniz gerekiyorsa, önerilen yol, `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlamasını uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimini uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="da99c-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="da99c-163">Birden çok parametreyi kısıtlama</span><span class="sxs-lookup"><span data-stu-id="da99c-163">Constraining multiple parameters</span></span>

<span data-ttu-id="da99c-164">Aşağıdaki örnekte gösterildiği gibi birden çok parametreye kısıtlama uygulayabilir ve tek bir parametreye birden çok kısıtlama uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="da99c-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="da99c-165">Sınırlandırılmamış tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="da99c-165">Unbounded type parameters</span></span>

 <span data-ttu-id="da99c-166">Public sınıfında T gibi hiçbir kısıtlaması olmayan tür parametrelerine, `SampleClass<T>{}` sınırlandırılmamış tür parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="da99c-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="da99c-167">Sınırlandırılmamış tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="da99c-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="da99c-168">`!=`Ve `==` işleçleri, somut tür bağımsız değişkeninin bu işleçleri destekleyeceği garantisi olmadığından kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="da99c-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="da99c-169">Bunlar, `System.Object` herhangi bir arabirim türüne dönüştürülebilir veya buradan dönüştürülebilir ya da açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="da99c-170">Bunları [null](../../language-reference/keywords/null.md)ile karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="da99c-171">Sınırlandırılmamış bir parametre ile karşılaştırılabiliyorsanız `null` , tür bağımsız değişkeni bir değer türündeyse karşılaştırma her zaman yanlış olarak döndürülür.</span><span class="sxs-lookup"><span data-stu-id="da99c-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="da99c-172">Parametreleri kısıtlamalar olarak yazın</span><span class="sxs-lookup"><span data-stu-id="da99c-172">Type parameters as constraints</span></span>

<span data-ttu-id="da99c-173">Genel tür parametresinin bir kısıtlama olarak kullanılması, aşağıdaki örnekte gösterildiği gibi, kendi tür parametresine sahip bir üye işlevi bu parametreyi kapsayan türün tür parametresiyle kısıtlamak için yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="da99c-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="da99c-174">Önceki örnekte, `T` yönteminin bağlamındaki bir tür kısıtlamasıdır `Add` ve sınıfı bağlamında sınırsız bir tür parametresi olur `List` .</span><span class="sxs-lookup"><span data-stu-id="da99c-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="da99c-175">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="da99c-176">Tür parametresi, diğer tür parametreleriyle birlikte açılı ayraç içinde bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="da99c-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="da99c-177">Tür parametrelerinin genel sınıflarla kısıtlamalar olarak yararlılığı sınırlıdır, çünkü derleyici tür parametresi hakkında türetilmediği hariç hiçbir şey kabul edebilir `System.Object` .</span><span class="sxs-lookup"><span data-stu-id="da99c-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="da99c-178">Tür parametrelerini, iki tür parametresi arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda genel sınıflarda kısıtlamalar olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="da99c-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="da99c-179">NotNull kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="da99c-179">NotNull constraint</span></span>

<span data-ttu-id="da99c-180">Bağımsız değişken olmayan bir bağlamda C# 8,0 ile başlayarak, `notnull` tür bağımsız değişkeninin null yapılamayan bir değer türü veya null yapılamayan bir başvuru türü olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="da99c-181">`notnull`Kısıtlama yalnızca bir `nullable enable` bağlamda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="da99c-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="da99c-182">`notnull`Kısıtlama, null yapılabilir bir zorunluluvou bağlamına eklerseniz bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="da99c-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="da99c-183">Diğer kısıtlamaların aksine, bir tür bağımsız değişkeni kısıtlamayı ihlal ettiğinde `notnull` , derleyici bu kod bir bağlamda derlenirse bir uyarı oluşturur `nullable enable` .</span><span class="sxs-lookup"><span data-stu-id="da99c-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="da99c-184">Kod, null yapılabilir bir zorunluluvou bağlamında derlenmişse, derleyici hiçbir uyarı veya hata oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="da99c-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="da99c-185">Null olabilen bir bağlamda C# 8,0 ' den başlayarak `class` kısıtlama, tür bağımsız değişkeninin null yapılamayan bir başvuru türü olması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="da99c-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="da99c-186">Null yapılabilir bir bağlamda, bir tür parametresi null olabilen bir başvuru türü olduğunda, derleyici bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="da99c-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="da99c-187">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="da99c-187">Unmanaged constraint</span></span>

<span data-ttu-id="da99c-188">C# 7,3 ' den başlayarak, `unmanaged` tür parametresinin null atanamaz [yönetilmeyen bir tür](../../language-reference/builtin-types/unmanaged-types.md)olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="da99c-189">`unmanaged`Kısıtlama, aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak işlenebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="da99c-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="da99c-190">Önceki yöntem, `unsafe` `sizeof` yerleşik bir tür olarak bilinen bir tür üzerinde işleç kullandığından bir bağlamda derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="da99c-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="da99c-191">Kısıtlama olmadan `unmanaged` `sizeof` işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="da99c-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="da99c-192">`unmanaged`Kısıtlama `struct` kısıtlamayı belirtir ve onunla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="da99c-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="da99c-193">Kısıtlama `struct` kısıtlamayı gösterdiği için kısıtlama `new()` `unmanaged` kısıtlama ile birleştirilemez `new()` .</span><span class="sxs-lookup"><span data-stu-id="da99c-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="da99c-194">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="da99c-194">Delegate constraints</span></span>

<span data-ttu-id="da99c-195">C# 7,3 ' den itibaren, <xref:System.Delegate?displayProperty=nameWithType> ya da <xref:System.MulticastDelegate?displayProperty=nameWithType> temel sınıf kısıtlaması olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="da99c-196">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dili izin vermez.</span><span class="sxs-lookup"><span data-stu-id="da99c-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="da99c-197">`System.Delegate`Kısıtlama, temsilcilerle birlikte, tür açısından güvenli bir şekilde kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="da99c-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="da99c-198">Aşağıdaki kod, iki temsilciyi aynı tür olduklarından birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="da99c-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="da99c-199">Aynı türdeki temsilcileri birleştirmek için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="da99c-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="da99c-200">Son satırın açıklamasını kaldırırsanız, derlenmez.</span><span class="sxs-lookup"><span data-stu-id="da99c-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="da99c-201">Hem hem de `first` `test` temsilci türlerdir, ancak farklı temsilci türleridir.</span><span class="sxs-lookup"><span data-stu-id="da99c-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="da99c-202">Sabit listesi kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="da99c-202">Enum constraints</span></span>

<span data-ttu-id="da99c-203">C# 7,3 ' den başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü temel sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="da99c-204">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dili izin vermez.</span><span class="sxs-lookup"><span data-stu-id="da99c-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="da99c-205">Kullanan genel türler `System.Enum` , içindeki statik yöntemleri kullanarak sonuçları önbelleğe almak için tür kullanımı uyumlu programlama sağlar `System.Enum` .</span><span class="sxs-lookup"><span data-stu-id="da99c-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="da99c-206">Aşağıdaki örnek, bir sabit listesi türü için geçerli tüm değerleri bulur ve sonra bu değerleri dize gösterimiyle eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="da99c-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="da99c-207">`Enum.GetValues`ve `Enum.GetName` performans etkilerine sahip olan yansıma kullanın.</span><span class="sxs-lookup"><span data-stu-id="da99c-207">`Enum.GetValues` and `Enum.GetName` use reflection, which has performance implications.</span></span> <span data-ttu-id="da99c-208">`EnumNamedValues`Yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için öğesini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="da99c-208">You can call `EnumNamedValues` to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="da99c-209">Aşağıdaki örnekte gösterildiği gibi, bir sabit listesi oluşturmak ve değerlerini ve adlarını bir sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="da99c-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="da99c-210">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="da99c-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="da99c-211">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="da99c-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="da99c-212">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="da99c-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="da99c-213">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="da99c-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="da99c-214">Yeni kısıtlama</span><span class="sxs-lookup"><span data-stu-id="da99c-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
