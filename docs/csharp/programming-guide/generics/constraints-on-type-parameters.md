---
title: Tür Parametrelerindeki Kısıtlamalar (C# Programlama Kılavuzu)
ms.date: 04/12/2018
ms.prod: .net
ms.technology:
- devlang-csharp
ms.topic: article
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 25c4a1137df335080659bf878fce5e3a3270dfda
ms.sourcegitcommit: 2042de78fcdceebb6b8ac4b7a292b93e8782cbf5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/27/2018
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="7f9b6-102">Tür parametreleri (C# programlama Kılavuzu) kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="7f9b6-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="7f9b6-103">Kısıtlamaları derleyici tür bağımsız değişkeni olmalıdır özellikleri hakkında bildirin.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="7f9b6-104">Tüm kısıtlamaları, tür bağımsız değişkeni herhangi bir türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="7f9b6-105">Derleyici üyeleri yalnızca kabul edilebilir <xref:System.Object?displayPropety=nameWithType>, herhangi bir .NET türü için ultimate temel sınıfı olan.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-105">The compiler can only assume the members of <xref:System.Object?displayPropety=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="7f9b6-106">Daha fazla bilgi için bkz: [neden kısıtlamaları kullanmak](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="7f9b6-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="7f9b6-107">İstemci kodu kısıtlaması tarafından izin verilmeyen bir türünü kullanarak, sınıfının örneği çalışırsa, bir derleme zamanı hatası sonucudur.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="7f9b6-108">Kısıtlamaları belirtilen kullanarak `where` bağlamsal anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="7f9b6-109">Aşağıdaki tabloda kısıtlamaları yedi türlerini listeler:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="7f9b6-110">Kısıtlama</span><span class="sxs-lookup"><span data-stu-id="7f9b6-110">Constraint</span></span>|<span data-ttu-id="7f9b6-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="7f9b6-111">Description</span></span>|
|----------------|-----------------|
|`where T: struct`|<span data-ttu-id="7f9b6-112">Tür bağımsız değişkeni bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-112">The type argument must be a value type.</span></span> <span data-ttu-id="7f9b6-113">Herhangi bir değer türü dışında <xref:System.Nullable> belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-113">Any value type except <xref:System.Nullable> can be specified.</span></span> <span data-ttu-id="7f9b6-114">Daha fazla bilgi için bkz: [kullanarak boş değer atanabilir türler](../nullable-types/using-nullable-types.md).</span><span class="sxs-lookup"><span data-stu-id="7f9b6-114">For more information, see [Using Nullable Types](../nullable-types/using-nullable-types.md).</span></span>|
|`where T : class`|<span data-ttu-id="7f9b6-115">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-115">The type argument must be a reference type.</span></span> <span data-ttu-id="7f9b6-116">Bu sınırlama herhangi sınıfı, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="7f9b6-117">Tür bağımsız değişkeni bir başvuru türü olmalıdır ve iç içe geçme herhangi bir düzeyde herhangi bir başvuru türü üye içermemelidir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-117">The type argument must not be a reference type and must not contain any reference type members at any level of nesting.</span></span>|
|`where T : new()`|<span data-ttu-id="7f9b6-118">Tür bağımsız değişkeni genel bir parametresiz oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="7f9b6-119">Diğer kısıtlamalar ile birlikte kullanıldığında `new()` kısıtlaması son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="7f9b6-120">`where T :` *\<taban sınıf adı >*</span><span class="sxs-lookup"><span data-stu-id="7f9b6-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="7f9b6-121">Tür bağımsız değişkeni, olabilir veya belirtilen temel sınıfından türetilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="7f9b6-122">`where T :` *\<Arabirim adı >*</span><span class="sxs-lookup"><span data-stu-id="7f9b6-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="7f9b6-123">Tür bağımsız değişkeni olabilir veya belirtilen arabirimini uygulaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="7f9b6-124">Birden çok arabirim kısıtlamaları belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="7f9b6-125">Kısıtlayan arabirimi genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="7f9b6-126">T olması veya u için sağlanan bağımsız değişken öğesinden türetilen için sağlanan tür bağımsız değişkeni</span><span class="sxs-lookup"><span data-stu-id="7f9b6-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="7f9b6-127">Bazı kısıtlamalar karşılıklı olarak birbirini dışlar.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="7f9b6-128">Tüm değer türleri erişilebilir bir parametresiz oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="7f9b6-129">`struct` Kısıtlaması gelir `new()` kısıtlama ve `new()` kısıtlaması ile ilişkilendirilemez `struct` kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="7f9b6-130">`unmanaged` Kısıtlaması gelir `struct` kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="7f9b6-131">`unmanaged` Kısıtlaması ile ya da ilişkilendirilemez `struct` veya `new()` kısıtlamaları.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="7f9b6-132">Kısıtlamaları neden kullanılır?</span><span class="sxs-lookup"><span data-stu-id="7f9b6-132">Why use constraints</span></span>

<span data-ttu-id="7f9b6-133">Tür parametresi kısıtlayarak, izin verilen işlemler ve kısıtlama türü ve devralma hiyerarşisi içindeki tüm türler tarafından desteklenen yöntem çağrılarını sayısını artırın.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="7f9b6-134">Tasarlarken genel sınıfları veya yöntemleri, basit atama ötesinde Genel üyeler üzerinde herhangi bir işlemi gerçekleştirme veya kaldırılacak tarafından desteklenmeyen herhangi bir yöntem çağırma varsa <xref:System.Object?displayProperty=nameWithType>, tür parametresi kısıtlamaları uygulamak gerekir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="7f9b6-135">Örneğin, taban sınıf kısıtlaması bu tür nesneler yalnızca derleyici söyler veya öğesinden türetilmiş türü tür bağımsız değişkenleri kullanılır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="7f9b6-136">Derleyici bu garantisi sonra genel sınıfında çağrılacak türü yöntemleri izin verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="7f9b6-137">Aşağıdaki kod örneği için ekleme işlevini gösterir `GenericList<T>` sınıfı (içinde [genel türlere giriş](introduction-to-generics.md)) bir taban sınıf kısıtlaması uygulayarak.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="7f9b6-138">Kullanmak genel bir sınıf kısıtlaması etkinleştirir `Employee.Name` özelliği.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="7f9b6-139">Kısıtlama belirleyen türündeki tüm öğeleri `T` ya da olması garanti bir `Employee` veya öğesinden devralınan bir nesneyi `Employee`.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="7f9b6-140">Aynı tür parametresi birden çok kısıtlama uygulanabilir ve kısıtlamaları kendilerini genel türleri, aşağıdaki gibi olabilir:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="7f9b6-141">Uygularken `where T : class` kısıtlaması, kaçının `==` ve `!=` tür parametresi işleçlerini çünkü bu işleçlere yalnızca başvuru kimliği olmayan değer eşitlik için test.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="7f9b6-142">Bağımsız değişken olarak kullanılan bir türdeki Bu işleçleri aşırı yüklenmiş olsa bile bu davranış oluşur.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="7f9b6-143">Aşağıdaki kod bu noktayı gösterir; Çıktı halde false şeklindedir <xref:System.String> sınıf aşırı `==` işleci.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="7f9b6-144">Derleyici yalnızca T derleme zamanında bir başvuru türüdür ve tüm başvuru türleri için geçerli varsayılan işleçleri kullanmalıdır bilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="7f9b6-145">Değer eşitlik için test etmeniz gerekir, önerilen yöntem de uygulamak için ise `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlaması ve genel bir sınıf oluşturmak için kullanılan herhangi bir sınıf arabiriminde uygulayın.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="7f9b6-146">Birden çok parametre sınırlama</span><span class="sxs-lookup"><span data-stu-id="7f9b6-146">Constraining multiple parameters</span></span>

<span data-ttu-id="7f9b6-147">Aşağıdaki örnekte gösterildiği gibi birden çok parametre ve tek bir parametre için birden çok kısıtlama kısıtlamaları uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="7f9b6-148">Sınırsız tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="7f9b6-148">Unbounded type parameters</span></span>

 <span data-ttu-id="7f9b6-149">Tür ortak sınıfı T gibi herhangi bir kısıtlama söz konusu parametrelerindeki `SampleClass<T>{}`, sınırsız tür parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="7f9b6-150">Sınırsız tür parametreleri aşağıdaki kurallar vardır:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="7f9b6-151">`!=` Ve `==` somut tür bağımsız değişkeni bu işleçlere destekleyecek garanti olduğundan işleçleri kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="7f9b6-152">Ve ondan dönüştürülebilir `System.Object` veya herhangi bir arabirim türü açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="7f9b6-153">Bunları karşılaştırabilirsiniz [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="7f9b6-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="7f9b6-154">Sınırsız bir parametre karşılaştırılır varsa `null`, karşılaştırma her zaman tür bağımsız değişkeni bir değer türü ise false döndürür.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="7f9b6-155">Tür parametreleri kısıtlamaları olarak</span><span class="sxs-lookup"><span data-stu-id="7f9b6-155">Type parameters as constraints</span></span>

<span data-ttu-id="7f9b6-156">Bir kısıtlama yararlı üye işlevi kendi tür parametresi ile olduğu gibi aşağıdaki örnekte gösterildiği gibi kapsayan tür tür parametresi bu parametreye sınırlamak bir genel tür parametresi kullanımını sahiptir:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="7f9b6-157">Önceki örnekte, `T` türü kısıtlaması bağlamında `Add` yöntemi ve sınırsız tür parametresi bağlamında `List` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="7f9b6-158">Tür parametreleri, genel bir sınıf tanımları kısıtlamalar olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="7f9b6-159">Tür parametresi herhangi bir tür parametre birlikte köşeli ayraç içinde bildirilmesi gerekir:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="7f9b6-160">Tür parametreleri yararlılığını Genel sınıflar kısıtlamalarıyla olarak den türemesi dışında derleyici tür parametresi hakkında hiçbir şey kabul edilebilir olduğundan sınırlıdır `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="7f9b6-161">Genel sınıflar, iki tür parametreleri arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda kısıtlamalar olarak tür parametreleri kullanın.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="7f9b6-162">Yönetilmeyen kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="7f9b6-162">Unmanaged constraint</span></span>

<span data-ttu-id="7f9b6-163">C# 7.3 ile başlayarak, kullanabilirsiniz `unmanaged` tür parametresi gerektiğini belirtmek için kısıtlama bir **yönetilmeyen türü**.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an **unmanaged type**.</span></span> <span data-ttu-id="7f9b6-164">Bir **yönetilmeyen türü** bir başvuru türü olmayan ve tüm iç içe geçme düzeyini başvuru türü alanlarını içermiyor. bir tür.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-164">An **unmanaged type** is a type that is not a reference type and doesn't contain reference type fields at any level of nesting.</span></span> <span data-ttu-id="7f9b6-165">`unmanaged` Kısıtlaması, aşağıdaki örnekte gösterildiği gibi bellek bloğu yönetilebilir türleriyle çalışmak için yeniden kullanılabilir yordamları yazmanıza olanak sağlar:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-165">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="7f9b6-166">Yukarıdaki yöntem içinde derlenmelidir bir `unsafe` bağlamı kullandığından `sizeof` işlecinin yerleşik bir tür olarak bilinmeyen bir tür.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-166">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="7f9b6-167">Olmadan `unmanaged` kısıtlamayı `sizeof` işleci kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-167">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="7f9b6-168">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="7f9b6-168">Delegate constraints</span></span>

<span data-ttu-id="7f9b6-169">Ayrıca C# 7.3 ile başlayarak, kullanabilirsiniz <xref:System.Delegate?displayProperty=nameWithType> veya <xref:System.MulticastDelegate?displayProperty=nameWithType> bir temel sınıf kısıtlaması olarak.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-169">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="7f9b6-170">CLR her zaman bu kısıtlamayı izin ancak C# dili izin verilmiyor.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-170">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="7f9b6-171">`System.Delegate` Kısıtlaması temsilciler ile bir tür kullanımı uyumlu şekilde çalışan kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-171">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="7f9b6-172">Aşağıdaki kod, aynı türde olması koşuluyla, iki temsilciler birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-172">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="7f9b6-173">Aynı türde olan temsilcileri birleştirme için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-173">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="7f9b6-174">Son satırı açıklamadan çıkarın, derleme olmaz.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-174">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="7f9b6-175">Her ikisi de `first` ve `test` temsilci türleri, ancak bunlar farklı temsilci türleri.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-175">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="7f9b6-176">Enum kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="7f9b6-176">Enum constraints</span></span>

<span data-ttu-id="7f9b6-177">C# 7.3 içinde başlayarak, ayrıca belirtebilirsiniz <xref:System.Enum?displayProperty=nameWithType> bir temel sınıf kısıtlaması türü.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-177">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="7f9b6-178">CLR her zaman bu kısıtlamayı izin ancak C# dili izin verilmiyor.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="7f9b6-179">Genel türler kullanma `System.Enum` statik yöntemleri kullanarak sonuçlarını önbelleğe alma tür kullanımı uyumlu programlama sağlamak `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-179">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="7f9b6-180">Aşağıdaki örnek, bir numaralandırma türü için geçerli tüm değerlerin bulur ve bu değerleri, dize gösterimi eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-180">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="7f9b6-181">Kullanılan yöntemleri olun performans etkileri olduğu yansıma kullanın.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-181">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="7f9b6-182">Önbelleğe alınan ve yeniden bir koleksiyon oluşturmak için bu yöntemi çağırın yansıma gerektiren çağrıları yinelenen yerine.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-182">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="7f9b6-183">Bunu aşağıdaki örnekte gösterildiği gibi bir numaralandırma ve adları ve değerleri sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="7f9b6-183">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="7f9b6-184">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="7f9b6-184">See also</span></span>

 <span data-ttu-id="7f9b6-185"><xref:System.Collections.Generic> [C# programlama kılavuzu](../../../csharp/programming-guide/index.md)</span><span class="sxs-lookup"><span data-stu-id="7f9b6-185"><xref:System.Collections.Generic> [C# Programming Guide](../../../csharp/programming-guide/index.md)</span></span>  
 [<span data-ttu-id="7f9b6-186">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="7f9b6-186">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/introduction-to-generics.md)  
 [<span data-ttu-id="7f9b6-187">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="7f9b6-187">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)  
 [<span data-ttu-id="7f9b6-188">new Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="7f9b6-188">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)  
