---
title: Tür parametrelerine yönelik kısıtlamalar-C# Programlama Kılavuzu
description: Tür parametrelerinin kısıtlamaları hakkında bilgi edinin. Kısıtlamalar derleyiciye bir tür bağımsız değişkeninin sahip olması gereken özellikleri söyler.
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 91807fa05ce49b8507ee6913ff2620452fcbfab5
ms.sourcegitcommit: 6f58a5f75ceeb936f8ee5b786e9adb81a9a3bee9
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 07/28/2020
ms.locfileid: "87301950"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="9b14a-104">Tür Parametrelerindeki Kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="9b14a-104">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="9b14a-105">Kısıtlamalar, derleyicisini bir tür bağımsız değişkeni olması gereken yetenekler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-105">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="9b14a-106">Herhangi bir kısıtlama olmadan tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-106">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="9b14a-107">Derleyici yalnızca <xref:System.Object?displayProperty=nameWithType> , tüm .net türleri için en son temel sınıf olan üyelerini kabul edebilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-107">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="9b14a-108">Daha fazla bilgi için bkz. [kısıtlamaların neden kullanılması](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="9b14a-108">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="9b14a-109">İstemci kodu bir kısıtlamayı karşılamayan bir tür kullanıyorsa, derleyici bir hata verir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-109">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="9b14a-110">Kısıtlamalar, `where` bağlamsal anahtar sözcüğü kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-110">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="9b14a-111">Aşağıdaki tabloda yedi tür kısıtlama listelenmektedir:</span><span class="sxs-lookup"><span data-stu-id="9b14a-111">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="9b14a-112">Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="9b14a-112">Constraint</span></span>|<span data-ttu-id="9b14a-113">Description</span><span class="sxs-lookup"><span data-stu-id="9b14a-113">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="9b14a-114">Tür bağımsız değişkeni null yapılamayan bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-114">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="9b14a-115">Nullable değer türleri hakkında daha fazla bilgi için bkz. [Nullable değer türleri](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="9b14a-115">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="9b14a-116">Tüm değer türlerinde erişilebilir parametresiz bir Oluşturucu olduğundan, kısıtlama `struct` `new()` kısıtlamayı gösterir ve `new()` kısıtlamayla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-116">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="9b14a-117">Kısıtlamayı `struct` `unmanaged` kısıtlamasıyla birleştiremezsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-117">You can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="9b14a-118">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-118">The type argument must be a reference type.</span></span> <span data-ttu-id="9b14a-119">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-119">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="9b14a-120">C# 8,0 veya sonraki bir sürümde null yapılabilir bir bağlamda, `T` null atanamaz bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-120">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="9b14a-121">Tür bağımsız değişkeni, null yapılabilir veya null yapılamayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-121">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="9b14a-122">Bu kısıtlama, her sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-122">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="9b14a-123">Tür bağımsız değişkeni null yapılamayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-123">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="9b14a-124">Bağımsız değişken, C# 8,0 veya sonraki bir sürümde null atanamaz bir başvuru türü veya null yapılamayan bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-124">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="9b14a-125">Tür bağımsız değişkeni null atanamaz bir [yönetilmeyen tür](../../language-reference/builtin-types/unmanaged-types.md)olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-125">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="9b14a-126">`unmanaged`Kısıtlama `struct` kısıtlamayı gösterir ve ya da kısıtlamalarıyla birleştirilemez `struct` `new()` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-126">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="9b14a-127">Tür bağımsız değişkeni Ortak parametresiz bir oluşturucuya sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-127">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="9b14a-128">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama en son belirtilmelidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-128">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="9b14a-129">`new()`Kısıtlama `struct` ve `unmanaged` kısıtlamalarıyla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-129">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="9b14a-130">`where T :` *\<base class name>*</span><span class="sxs-lookup"><span data-stu-id="9b14a-130">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="9b14a-131">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-131">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="9b14a-132">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen temel sınıftan türetilmiş null atanamaz bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-132">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="9b14a-133">`where T :` *\<base class name>?*</span><span class="sxs-lookup"><span data-stu-id="9b14a-133">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="9b14a-134">Tür bağımsız değişkeni belirtilen temel sınıftan olmalıdır veya türetilmelidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-134">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="9b14a-135">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen temel sınıftan türetilmiş null yapılabilen veya null yapılamayan bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-135">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="9b14a-136">`where T :` *\<interface name>*</span><span class="sxs-lookup"><span data-stu-id="9b14a-136">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="9b14a-137">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-137">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="9b14a-138">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-138">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="9b14a-139">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-139">The constraining interface can also be generic.</span></span> <span data-ttu-id="9b14a-140">C# 8,0 ve üzeri bir null yapılabilir bağlamda, `T` belirtilen arabirimi uygulayan null yapılamayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-140">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="9b14a-141">`where T :` *\<interface name>?*</span><span class="sxs-lookup"><span data-stu-id="9b14a-141">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="9b14a-142">Tür bağımsız değişkeni belirtilen arabirimi içermelidir veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-142">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="9b14a-143">Birden çok arabirim kısıtlaması belirlenebilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-143">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="9b14a-144">Kısıtlama arabirimi de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-144">The constraining interface can also be generic.</span></span> <span data-ttu-id="9b14a-145">C# 8,0 ' de null yapılabilir bir bağlamda, `T` null yapılabilir bir başvuru türü, null olamayan bir başvuru türü veya değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-145">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="9b14a-146">`T`null yapılabilen bir değer türü olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-146">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="9b14a-147">İçin sağlanan tür bağımsız değişkeni, `T` için sağlanan bağımsız değişkenden türetilmiş veya türetilmiş olmalıdır `U` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-147">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="9b14a-148">Null yapılabilir bir bağlamda, `U` null atanamaz bir başvuru türü ise, `T` null olamayan bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-148">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="9b14a-149">`U`Null yapılabilir bir başvuru türü ise, `T` null yapılabilir veya null atanamaz olabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-149">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="9b14a-150">Neden kısıtlama kullanılmalıdır</span><span class="sxs-lookup"><span data-stu-id="9b14a-150">Why use constraints</span></span>

<span data-ttu-id="9b14a-151">Kısıtlamalar, bir tür parametresinin yeteneklerini ve beklentilerini belirtir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-151">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="9b14a-152">Bu kısıtlamaları bildirmek, kısıtlama türünün işlemlerini ve Yöntem çağrılarını kullanabileceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-152">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="9b14a-153">Genel bir sınıf veya yönteminiz, Genel Üyeler üzerinde basit atamanın ötesinde veya tarafından desteklenmeyen herhangi bir yöntemi çağıran herhangi bir işlem kullanıyorsa <xref:System.Object?displayProperty=nameWithType> , tür parametresine kısıtlamalar uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-153">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="9b14a-154">Örneğin, temel sınıf kısıtlaması derleyiciye yalnızca bu türden veya bu türden türetilmiş nesnelerin tür bağımsız değişkenleri olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="9b14a-154">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="9b14a-155">Derleyiciye bu garanti verildikten sonra, genel sınıfta bu tür yöntemlerin çağrılmasına izin verebilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-155">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="9b14a-156">Aşağıdaki kod örneği, `GenericList<T>` bir temel sınıf kısıtlaması uygulayarak sınıfına ekleyebileceğiniz işlevselliği gösterir ( [Genel türlere giriş](../../../standard/generics/index.md)olarak).</span><span class="sxs-lookup"><span data-stu-id="9b14a-156">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#9)]

<span data-ttu-id="9b14a-157">Kısıtlama, genel sınıfın özelliğini kullanmasını sağlar `Employee.Name` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-157">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="9b14a-158">Kısıtlama, türündeki tüm öğelerin `T` bir `Employee` nesne ya da öğesinden devralan nesne olduğunu belirtir `Employee` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-158">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="9b14a-159">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamalar aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="9b14a-159">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#10)]

<span data-ttu-id="9b14a-160">`where T : class`Kısıtlama uygulanırken `==` `!=` tür parametresindeki ve işleçlerden kaçının, çünkü bu işleçler yalnızca başvuru kimliğini test edecek, değer eşitlik için değil.</span><span class="sxs-lookup"><span data-stu-id="9b14a-160">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="9b14a-161">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklense bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="9b14a-161">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="9b14a-162">Aşağıdaki kod bu noktayı gösterir; sınıf işleci aşırı yüklese de çıkış yanlış olur <xref:System.String> `==` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-162">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#11)]

<span data-ttu-id="9b14a-163">Derleyici yalnızca `T` derleme zamanında bir başvuru türü olduğunu ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerektiğini bilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-163">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="9b14a-164">Değer eşitlik için test etmeniz gerekiyorsa, önerilen yol, `where T : IEquatable<T>` veya `where T : IComparable<T>` kısıtlamasını uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimini uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="9b14a-164">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="9b14a-165">Birden çok parametreyi kısıtlama</span><span class="sxs-lookup"><span data-stu-id="9b14a-165">Constraining multiple parameters</span></span>

<span data-ttu-id="9b14a-166">Aşağıdaki örnekte gösterildiği gibi birden çok parametreye kısıtlama uygulayabilir ve tek bir parametreye birden çok kısıtlama uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="9b14a-166">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="9b14a-167">Sınırlandırılmamış tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="9b14a-167">Unbounded type parameters</span></span>

 <span data-ttu-id="9b14a-168">Public sınıfında T gibi hiçbir kısıtlaması olmayan tür parametrelerine, `SampleClass<T>{}` sınırlandırılmamış tür parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-168">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="9b14a-169">Sınırlandırılmamış tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="9b14a-169">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="9b14a-170">`!=`Ve `==` işleçleri, somut tür bağımsız değişkeninin bu işleçleri destekleyeceği garantisi olmadığından kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-170">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="9b14a-171">Bunlar, `System.Object` herhangi bir arabirim türüne dönüştürülebilir veya buradan dönüştürülebilir ya da açıkça dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-171">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="9b14a-172">Bunları [null](../../language-reference/keywords/null.md)ile karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-172">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="9b14a-173">Sınırlandırılmamış bir parametre ile karşılaştırılabiliyorsanız `null` , tür bağımsız değişkeni bir değer türündeyse karşılaştırma her zaman yanlış olarak döndürülür.</span><span class="sxs-lookup"><span data-stu-id="9b14a-173">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="9b14a-174">Parametreleri kısıtlamalar olarak yazın</span><span class="sxs-lookup"><span data-stu-id="9b14a-174">Type parameters as constraints</span></span>

<span data-ttu-id="9b14a-175">Genel tür parametresinin bir kısıtlama olarak kullanılması, aşağıdaki örnekte gösterildiği gibi, kendi tür parametresine sahip bir üye işlevi bu parametreyi kapsayan türün tür parametresiyle kısıtlamak için yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="9b14a-175">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#13)]

<span data-ttu-id="9b14a-176">Önceki örnekte, `T` yönteminin bağlamındaki bir tür kısıtlamasıdır `Add` ve sınıfı bağlamında sınırsız bir tür parametresi olur `List` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-176">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="9b14a-177">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-177">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="9b14a-178">Tür parametresi, diğer tür parametreleriyle birlikte açılı ayraç içinde bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="9b14a-178">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#14)]

<span data-ttu-id="9b14a-179">Tür parametrelerinin genel sınıflarla kısıtlamalar olarak yararlılığı sınırlıdır, çünkü derleyici tür parametresi hakkında türetilmediği hariç hiçbir şey kabul edebilir `System.Object` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-179">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="9b14a-180">Tür parametrelerini, iki tür parametresi arasında bir devralma ilişkisi uygulamak istediğiniz senaryolarda genel sınıflarda kısıtlamalar olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="9b14a-180">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="9b14a-181">NotNull kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="9b14a-181">NotNull constraint</span></span>

<span data-ttu-id="9b14a-182">Bağımsız değişken olmayan bir bağlamda C# 8,0 ile başlayarak, `notnull` tür bağımsız değişkeninin null yapılamayan bir değer türü veya null yapılamayan bir başvuru türü olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-182">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="9b14a-183">`notnull`Kısıtlama yalnızca bir `nullable enable` bağlamda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-183">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="9b14a-184">`notnull`Kısıtlama, null yapılabilir bir zorunluluvou bağlamına eklerseniz bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="9b14a-184">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="9b14a-185">Diğer kısıtlamaların aksine, bir tür bağımsız değişkeni kısıtlamayı ihlal ettiğinde `notnull` , derleyici bu kod bir bağlamda derlenirse bir uyarı oluşturur `nullable enable` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-185">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="9b14a-186">Kod, null yapılabilir bir zorunluluvou bağlamında derlenmişse, derleyici hiçbir uyarı veya hata oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-186">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="9b14a-187">Null olabilen bir bağlamda C# 8,0 ' den başlayarak `class` kısıtlama, tür bağımsız değişkeninin null yapılamayan bir başvuru türü olması gerektiğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-187">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="9b14a-188">Null yapılabilir bir bağlamda, bir tür parametresi null olabilen bir başvuru türü olduğunda, derleyici bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="9b14a-188">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="9b14a-189">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="9b14a-189">Unmanaged constraint</span></span>

<span data-ttu-id="9b14a-190">C# 7,3 ' den başlayarak, `unmanaged` tür parametresinin null atanamaz [yönetilmeyen bir tür](../../language-reference/builtin-types/unmanaged-types.md)olması gerektiğini belirtmek için kısıtlamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-190">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="9b14a-191">`unmanaged`Kısıtlama, aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak işlenebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="9b14a-191">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#15)]

<span data-ttu-id="9b14a-192">Önceki yöntem, `unsafe` `sizeof` yerleşik bir tür olarak bilinen bir tür üzerinde işleç kullandığından bir bağlamda derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-192">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="9b14a-193">Kısıtlama olmadan `unmanaged` `sizeof` işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-193">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="9b14a-194">`unmanaged`Kısıtlama `struct` kısıtlamayı belirtir ve onunla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-194">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="9b14a-195">Kısıtlama `struct` kısıtlamayı gösterdiği için kısıtlama `new()` `unmanaged` kısıtlama ile birleştirilemez `new()` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-195">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="9b14a-196">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="9b14a-196">Delegate constraints</span></span>

<span data-ttu-id="9b14a-197">C# 7,3 ' den itibaren, <xref:System.Delegate?displayProperty=nameWithType> ya da <xref:System.MulticastDelegate?displayProperty=nameWithType> temel sınıf kısıtlaması olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-197">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="9b14a-198">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dili izin vermez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-198">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="9b14a-199">`System.Delegate`Kısıtlama, temsilcilerle birlikte, tür açısından güvenli bir şekilde kod yazmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="9b14a-199">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="9b14a-200">Aşağıdaki kod, iki temsilciyi aynı tür olduklarından birleştiren bir genişletme yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="9b14a-200">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](snippets/GenericWhereConstraints.cs#16)]

<span data-ttu-id="9b14a-201">Aynı türdeki temsilcileri birleştirmek için yukarıdaki yöntemi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="9b14a-201">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#17)]

<span data-ttu-id="9b14a-202">Son satırın açıklamasını kaldırırsanız, derlenmez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-202">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="9b14a-203">Hem hem de `first` `test` temsilci türlerdir, ancak farklı temsilci türleridir.</span><span class="sxs-lookup"><span data-stu-id="9b14a-203">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="9b14a-204">Sabit listesi kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="9b14a-204">Enum constraints</span></span>

<span data-ttu-id="9b14a-205">C# 7,3 ' den başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü temel sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-205">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="9b14a-206">CLR bu kısıtlamaya her zaman izin verilir, ancak C# dili izin vermez.</span><span class="sxs-lookup"><span data-stu-id="9b14a-206">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="9b14a-207">Kullanan genel türler `System.Enum` , içindeki statik yöntemleri kullanarak sonuçları önbelleğe almak için tür kullanımı uyumlu programlama sağlar `System.Enum` .</span><span class="sxs-lookup"><span data-stu-id="9b14a-207">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="9b14a-208">Aşağıdaki örnek, bir sabit listesi türü için geçerli tüm değerleri bulur ve sonra bu değerleri dize gösterimiyle eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="9b14a-208">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](snippets/GenericWhereConstraints.cs#18)]

<span data-ttu-id="9b14a-209">`Enum.GetValues`ve `Enum.GetName` performans etkilerine sahip olan yansıma kullanın.</span><span class="sxs-lookup"><span data-stu-id="9b14a-209">`Enum.GetValues` and `Enum.GetName` use reflection, which has performance implications.</span></span> <span data-ttu-id="9b14a-210">`EnumNamedValues`Yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için öğesini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-210">You can call `EnumNamedValues` to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="9b14a-211">Aşağıdaki örnekte gösterildiği gibi, bir sabit listesi oluşturmak ve değerlerini ve adlarını bir sözlüğü oluşturmak için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="9b14a-211">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](snippets/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](snippets/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="9b14a-212">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="9b14a-212">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="9b14a-213">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="9b14a-213">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="9b14a-214">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="9b14a-214">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="9b14a-215">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="9b14a-215">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="9b14a-216">Yeni kısıtlama</span><span class="sxs-lookup"><span data-stu-id="9b14a-216">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
