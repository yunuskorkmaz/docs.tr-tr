---
title: Tür parametrelerindeki kısıtlamalar - C# Programlama Kılavuzu
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 76cd00b9c84f128d2a181115293df910d8deb6cb
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399800"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="c27dc-102">Tür parametreleri üzerindeki kısıtlamalar (C# Programlama Kılavuzu)</span><span class="sxs-lookup"><span data-stu-id="c27dc-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="c27dc-103">Kısıtlamalar derleyiciyi bir tür bağımsız değişkeninin sahip olması gereken özellikler hakkında bilgilendirir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="c27dc-104">Herhangi bir kısıtlama olmadan, tür bağımsız değişkeni herhangi bir tür olabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="c27dc-105">Derleyici yalnızca herhangi bir <xref:System.Object?displayProperty=nameWithType>.NET türü için nihai taban sınıf olan üyeleri varsayar.</span><span class="sxs-lookup"><span data-stu-id="c27dc-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="c27dc-106">Daha fazla bilgi için neden [kısıtlamaları kullanın'a](#why-use-constraints)bakın.</span><span class="sxs-lookup"><span data-stu-id="c27dc-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="c27dc-107">İstemci kodu, kısıtlamanın izin verilmeyen bir tür kullanarak sınıfınızı anında kullanmaya çalışırsa, sonuç derleme zamanı hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="c27dc-108">Kısıtlamalar `where` bağlamsal anahtar kelime kullanılarak belirtilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="c27dc-109">Aşağıdaki tabloda yedi tür kısıtlama listelenebvardır:</span><span class="sxs-lookup"><span data-stu-id="c27dc-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="c27dc-110">Kısıtlama</span><span class="sxs-lookup"><span data-stu-id="c27dc-110">Constraint</span></span>|<span data-ttu-id="c27dc-111">Açıklama</span><span class="sxs-lookup"><span data-stu-id="c27dc-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="c27dc-112">Tür bağımsız değişkeni nullable olmayan bir değer türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="c27dc-113">Nullable değer türleri hakkında bilgi için, [Nullable değer türleri](../../language-reference/builtin-types/nullable-value-types.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="c27dc-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="c27dc-114">Tüm değer türlerinin erişilebilir bir parametresiz `struct` oluşturucusu olduğundan, kısıtlama `new()` kısıtlamayı `new()` ima eder ve kısıtlamayla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="c27dc-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="c27dc-115">`struct` Ayrıca kısıtlama ile kısıtlama `unmanaged` birleştiremezsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-115">You also cannot combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="c27dc-116">Tür bağımsız değişkeni bir başvuru türü olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-116">The type argument must be a reference type.</span></span> <span data-ttu-id="c27dc-117">Bu kısıtlama herhangi bir sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-117">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="c27dc-118">Tür bağımsız değişkeni nullable olmayan bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-118">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="c27dc-119">Bağımsız değişken, C# 8.0 veya sonraki bir şekilde nullable olmayan bir başvuru türü veya nullable olmayan bir değer türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-119">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="c27dc-120">Bu kısıtlama herhangi bir sınıf, arabirim, temsilci veya dizi türü için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="c27dc-121">Tür bağımsız değişkeni, geçersiz olmayan [yönetilmeyen](../../language-reference/builtin-types/unmanaged-types.md)bir tür olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-121">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="c27dc-122">Kısıtlama `unmanaged` kısıtlamayı `struct` ima eder ve kısıtlamalarla `struct` `new()` birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="c27dc-122">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="c27dc-123">Tür bağımsız değişkeninin ortak parametresiz bir oluşturucusu olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-123">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="c27dc-124">Diğer kısıtlamalarla birlikte kullanıldığında, `new()` kısıtlama nın en son belirtilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-124">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="c27dc-125">Kısıtlama `new()` `struct` ve `unmanaged` kısıtlamalarla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="c27dc-125">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="c27dc-126">`where T :`taban sınıf adı>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="c27dc-126">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="c27dc-127">Tür bağımsız değişkeni, belirtilen taban sınıftan türemelidir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-127">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="c27dc-128">`where T :`arayüz adı>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="c27dc-128">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="c27dc-129">Tür bağımsız değişkeni, belirtilen arabirimi olmalıdır veya uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-129">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="c27dc-130">Birden çok arabirim kısıtlaması belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-130">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="c27dc-131">Kısıtlayıcı arabirim de genel olabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-131">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="c27dc-132">T için verilen tür bağımsız değişkeni U için sağlanan bağımsız değişkenden türemelidir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-132">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

## <a name="why-use-constraints"></a><span data-ttu-id="c27dc-133">Neden kısıtlamaları kullanın</span><span class="sxs-lookup"><span data-stu-id="c27dc-133">Why use constraints</span></span>

<span data-ttu-id="c27dc-134">Tür parametresini kısıtlayarak, kısıtlanan tür ve kalıtım hiyerarşisindeki tüm türler tarafından desteklenenlere izin verilen işlem ve yöntem çağrılarının sayısını artırırsınız.</span><span class="sxs-lookup"><span data-stu-id="c27dc-134">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="c27dc-135">Genel sınıflar veya yöntemler tasarlarken, genel üyeler üzerinde basit atamanın ötesinde herhangi bir işlem <xref:System.Object?displayProperty=nameWithType>gerçekleştirecekseniz veya desteklenmeyen yöntemleri çağıracaksanız, tür parametresine kısıtlamalar uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-135">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="c27dc-136">Örneğin, taban sınıf kısıtlaması derleyiciye yalnızca bu türdeki veya bu türden türetilen nesnelerin tür bağımsız değişkeni olarak kullanılacağını söyler.</span><span class="sxs-lookup"><span data-stu-id="c27dc-136">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="c27dc-137">Derleyici bu garantiye sahip olduktan sonra, bu tür yöntemlerin genel sınıfta çağrılmasını sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-137">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="c27dc-138">Aşağıdaki kod örneği, taban sınıf kısıtlaması `GenericList<T>` uygulayarak sınıfa [(Genel Lere](../../../standard/generics/index.md)Giriş'te) ekleyebileceğiniz işlevselliği gösterir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-138">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="c27dc-139">Kısıtlama, genel sınıfın `Employee.Name` özelliği kullanmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="c27dc-139">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="c27dc-140">Kısıtlama, türdeki `T` tüm öğelerin bir `Employee` nesne veya devralan bir nesne olarak `Employee`garanti edilir olduğunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-140">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="c27dc-141">Aynı tür parametresine birden çok kısıtlama uygulanabilir ve kısıtlamaların kendileri aşağıdaki gibi genel türler olabilir:</span><span class="sxs-lookup"><span data-stu-id="c27dc-141">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="c27dc-142">Kısıtlamayı `where T : class` uygularken, bu `==` `!=` işleçler değer eşitliği için değil, yalnızca referans kimliği için test edecek tür parametresi üzerinde ve işleçleri kaçının.</span><span class="sxs-lookup"><span data-stu-id="c27dc-142">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="c27dc-143">Bu davranış, bu işleçler bağımsız değişken olarak kullanılan bir türde aşırı yüklenmiş olsa bile oluşur.</span><span class="sxs-lookup"><span data-stu-id="c27dc-143">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="c27dc-144">Aşağıdaki kod bu noktayı göstermektedir; <xref:System.String> sınıf `==` işleci aşırı yüklese bile çıktı yanlıştır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-144">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="c27dc-145">Derleyici yalnızca derleme `T` zamanında bir başvuru türü olduğunu bilir ve tüm başvuru türleri için geçerli olan varsayılan işleçleri kullanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-145">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="c27dc-146">Değer eşitliği için sınamanız gerekiyorsa, önerilen yol `where T : IEquatable<T>` `where T : IComparable<T>` aynı zamanda kısıtlamayı uygulamak ve genel sınıfı oluşturmak için kullanılacak herhangi bir sınıfta arabirimi uygulamaktır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-146">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="c27dc-147">Birden çok parametreyi zorlama</span><span class="sxs-lookup"><span data-stu-id="c27dc-147">Constraining multiple parameters</span></span>

<span data-ttu-id="c27dc-148">Aşağıdaki örnekte gösterildiği gibi, kısıtlamaları birden çok parametreye ve birden çok kısıtlamayı tek bir parametreye uygulayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c27dc-148">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="c27dc-149">Sınırsız tür parametreleri</span><span class="sxs-lookup"><span data-stu-id="c27dc-149">Unbounded type parameters</span></span>

 <span data-ttu-id="c27dc-150">Ortak sınıfta `SampleClass<T>{}`Ki'si gibi kısıtlaması olmayan tür parametreleri, sınırsız tür parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-150">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="c27dc-151">Sınırsız tür parametreleri aşağıdaki kurallara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="c27dc-151">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="c27dc-152">Somut `!=` `==` tür bağımsız değişkeninin bu işleçleri destekleyeceğinin garantisi olmadığından, ve işleçler kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-152">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="c27dc-153">Bunlar herhangi bir arabirim `System.Object` türüne dönüştürülebilir veya açıkça herhangi bir arabirim türüne dönüştürülebilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-153">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="c27dc-154">Null [ile](../../language-reference/keywords/null.md)karşılaştırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-154">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="c27dc-155">Sınırlanmamış bir parametre ile `null`karşılaştırıldığında, tür bağımsız değişkeni bir değer türüise karşılaştırma her zaman yanlış döndürecektir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-155">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="c27dc-156">Parametreleri kısıtlama olarak yazın</span><span class="sxs-lookup"><span data-stu-id="c27dc-156">Type parameters as constraints</span></span>

<span data-ttu-id="c27dc-157">Genel bir tür parametresinin kısıtlama olarak kullanılması, kendi türü parametresine sahip bir üye işlevin, aşağıdaki örnekte gösterildiği gibi, bu parametreyi içeren tür türü parametresiyle sınırlandırması gerektiğinde yararlıdır:</span><span class="sxs-lookup"><span data-stu-id="c27dc-157">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="c27dc-158">Önceki örnekte, `T` `Add` yöntem bağlamında bir tür kısıtlaması ve `List` sınıf bağlamında sınırsız bir tür parametresi vardır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-158">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="c27dc-159">Tür parametreleri, genel sınıf tanımlarında kısıtlama olarak da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-159">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="c27dc-160">Tip parametresi, açı braketi içinde diğer tip parametrelerle birlikte bildirilmelidir:</span><span class="sxs-lookup"><span data-stu-id="c27dc-160">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="c27dc-161">Derleyici, `System.Object`tür parametresi hakkında türetilmiştir dışında hiçbir şey varsayamayacağından, genel sınıflarla birlikte kısıtlama olarak tür parametrelerinin kullanışlılığı sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="c27dc-161">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="c27dc-162">İki tür paramı arasında devralma ilişkisini zorlamak istediğiniz senaryolarda genel sınıflarda tür parametrelerini kısıtlama olarak kullanın.</span><span class="sxs-lookup"><span data-stu-id="c27dc-162">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="c27dc-163">NotNull kısıtlama</span><span class="sxs-lookup"><span data-stu-id="c27dc-163">NotNull constraint</span></span>

<span data-ttu-id="c27dc-164">C# 8.0 ile başlayarak, `notnull` tür bağımsız değişkeninin nullable olmayan bir değer türü veya nullable olmayan başvuru türü olması gerektiğini belirtmek için kısıtlamak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-164">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="c27dc-165">Kısıtlama `notnull` yalnızca bir `nullable enable` bağlam içinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c27dc-165">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="c27dc-166">Kısıtlamayı `notnull` boş bir içeriğe eklerseniz derleyici bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c27dc-166">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="c27dc-167">Diğer kısıtlamaların aksine, bir tür `notnull` bağımsız değişkeni kısıtlamayı ihlal ettiğinde, derleyici bu `nullable enable` kod bir bağlam içinde derlendiğinde bir uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c27dc-167">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="c27dc-168">Kod boş bir şekilde derlenirse, derleyici herhangi bir uyarı veya hata oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-168">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="c27dc-169">Yönetilmeyen kısıtlama</span><span class="sxs-lookup"><span data-stu-id="c27dc-169">Unmanaged constraint</span></span>

<span data-ttu-id="c27dc-170">C# 7.3 ile başlayarak, `unmanaged` tür parametresinin geçersiz olmayan [yönetilmeyen](../../language-reference/builtin-types/unmanaged-types.md)bir tür olması gerektiğini belirtmek için kısıtlamayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-170">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="c27dc-171">Kısıtlama, `unmanaged` aşağıdaki örnekte gösterildiği gibi, bellek blokları olarak değiştirilebilen türlerle çalışmak için yeniden kullanılabilir yordamlar yazmanızı sağlar:</span><span class="sxs-lookup"><span data-stu-id="c27dc-171">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="c27dc-172">İşleç, yerleşik bir tür `unsafe` olarak bilinen bir türde işleci kullandığından, önceki yöntem bir bağlamda derlenmelidir. `sizeof`</span><span class="sxs-lookup"><span data-stu-id="c27dc-172">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="c27dc-173">Kısıtlama `unmanaged` `sizeof` olmadan, işleç kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-173">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="c27dc-174">Kısıtlama `unmanaged` kısıtlamayı `struct` ima eder ve onunla birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="c27dc-174">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="c27dc-175">`struct` Kısıtlama kısıtlamayı `new()` ima ettiği `unmanaged` için, kısıtlama `new()` kısıtlamayla da birleştirilemez.</span><span class="sxs-lookup"><span data-stu-id="c27dc-175">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="c27dc-176">Temsilci kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="c27dc-176">Delegate constraints</span></span>

<span data-ttu-id="c27dc-177">Ayrıca C# 7.3 ile başlayarak, kullanabilirsiniz <xref:System.Delegate?displayProperty=nameWithType> veya <xref:System.MulticastDelegate?displayProperty=nameWithType> bir taban sınıf kısıtlaması olarak.</span><span class="sxs-lookup"><span data-stu-id="c27dc-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="c27dc-178">CLR her zaman bu kısıtlamaya izin verdi, ancak C# dili buna izin vermedi.</span><span class="sxs-lookup"><span data-stu-id="c27dc-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="c27dc-179">Kısıtlama, `System.Delegate` temsilcilerle tür güvenli bir şekilde çalışan kod yazmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="c27dc-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="c27dc-180">Aşağıdaki kod, aynı türde olmaları koşuluyla iki temsilciyi birleştiren bir uzantı yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="c27dc-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="c27dc-181">Yukarıdaki yöntemi, aynı türdeki temsilcileri birleştirmek için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c27dc-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="c27dc-182">Son satırı açıklamazsanız, derleme olmaz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="c27dc-183">Her `first` `test` ikisi de ve temsilci türleri, ancak farklı temsilci türleri.</span><span class="sxs-lookup"><span data-stu-id="c27dc-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="c27dc-184">Enum kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="c27dc-184">Enum constraints</span></span>

<span data-ttu-id="c27dc-185">C# 7.3'ten başlayarak, <xref:System.Enum?displayProperty=nameWithType> türü taban sınıf kısıtlaması olarak da belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="c27dc-186">CLR her zaman bu kısıtlamaya izin verdi, ancak C# dili buna izin vermedi.</span><span class="sxs-lookup"><span data-stu-id="c27dc-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="c27dc-187">Genel olarak `System.Enum` kullanan genel `System.Enum`ler, statik yöntemleri kullanarak sonuçları önbelleğe almak için tür için güvenli programlama sağlar.</span><span class="sxs-lookup"><span data-stu-id="c27dc-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="c27dc-188">Aşağıdaki örnek, bir enum türü için tüm geçerli değerleri bulur ve sonra bu değerleri dize gösterimi ile eşleyen bir sözlük oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c27dc-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="c27dc-189">Performans etkileri olan yansımakullanımı nda kullanılan yöntemler.</span><span class="sxs-lookup"><span data-stu-id="c27dc-189">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="c27dc-190">Bu yöntemi, yansıma gerektiren çağrıları yinelemek yerine önbelleğe alınmış ve yeniden kullanılan bir koleksiyon oluşturmak için arayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="c27dc-191">Bir enum oluşturmak ve değerleri ve adlarından oluşan bir sözlük oluşturmak için aşağıdaki örnekte gösterildiği gibi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c27dc-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="c27dc-192">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="c27dc-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="c27dc-193">C# Programlama Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="c27dc-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="c27dc-194">Genel Türlere Giriş</span><span class="sxs-lookup"><span data-stu-id="c27dc-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="c27dc-195">Genel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="c27dc-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="c27dc-196">new Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="c27dc-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
