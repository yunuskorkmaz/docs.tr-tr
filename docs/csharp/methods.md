---
title: Yöntemler- C# kılavuz
description: Yöntemlere, yöntem parametrelerine ve yöntem dönüş değerlerine genel bakış
author: rpetrusha
ms.author: ronpet
ms.date: 05/21/2018
ms.assetid: 577a8527-1081-4b36-9b9e-0685b6553c6e
ms.openlocfilehash: b95818e06d37b0e98bf55428ff97dd476f92fac7
ms.sourcegitcommit: 1b020356e421a9314dd525539da12463d980ce7a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/30/2019
ms.locfileid: "70168512"
---
# <a name="methods"></a><span data-ttu-id="c4899-103">Yöntemler</span><span class="sxs-lookup"><span data-stu-id="c4899-103">Methods</span></span>

<span data-ttu-id="c4899-104">Yöntemi, bir dizi deyim içeren bir kod bloğudur.</span><span class="sxs-lookup"><span data-stu-id="c4899-104">A method is a code block that contains a series of statements.</span></span> <span data-ttu-id="c4899-105">Program, metodu çağırarak ve gerekli Yöntem bağımsız değişkenlerini belirterek deyimlerin yürütülmesine neden olur.</span><span class="sxs-lookup"><span data-stu-id="c4899-105">A program causes the statements to be executed by calling the method and specifying any required method arguments.</span></span> <span data-ttu-id="c4899-106">' C#De, her yürütülen yönerge bir yöntem bağlamında gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-106">In C#, every executed instruction is performed in the context of a method.</span></span> <span data-ttu-id="c4899-107">Yöntemi her C# uygulamanın giriş noktasıdır ve program başlatıldığında ortak dil çalışma zamanı (CLR) tarafından çağrılır. `Main`</span><span class="sxs-lookup"><span data-stu-id="c4899-107">The `Main` method is the entry point for every C# application and it is called by the common language runtime (CLR) when the program is started.</span></span>

> [!NOTE]
> <span data-ttu-id="c4899-108">Bu konuda adlandırılmış yöntemler ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="c4899-108">This topic discusses named methods.</span></span> <span data-ttu-id="c4899-109">Anonim işlevler hakkında daha fazla bilgi için bkz. [Anonim işlevler](programming-guide/statements-expressions-operators/anonymous-functions.md).</span><span class="sxs-lookup"><span data-stu-id="c4899-109">For information about anonymous functions, see [Anonymous Functions](programming-guide/statements-expressions-operators/anonymous-functions.md).</span></span>

<span data-ttu-id="c4899-110">Bu konu aşağıdaki bölümleri içermektedir:</span><span class="sxs-lookup"><span data-stu-id="c4899-110">This topic contains the following sections:</span></span>

- [<span data-ttu-id="c4899-111">Yöntem imzaları</span><span class="sxs-lookup"><span data-stu-id="c4899-111">Method signatures</span></span>](#signatures)
- [<span data-ttu-id="c4899-112">Yöntem çağırma</span><span class="sxs-lookup"><span data-stu-id="c4899-112">Method invocation</span></span>](#invocation)
- [<span data-ttu-id="c4899-113">Devralınan ve geçersiz kılınan Yöntemler</span><span class="sxs-lookup"><span data-stu-id="c4899-113">Inherited and overridden methods</span></span>](#inherited)
- [<span data-ttu-id="c4899-114">Parametreleri geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-114">Passing parameters</span></span>](#passing)
  - [<span data-ttu-id="c4899-115">Parametreleri değere göre geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-115">Passing parameters by value</span></span>](#byval)
  - [<span data-ttu-id="c4899-116">Parametreleri başvuruya göre geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-116">Passing parameters by reference</span></span>](#byref)
  - [<span data-ttu-id="c4899-117">Parametre dizileri</span><span class="sxs-lookup"><span data-stu-id="c4899-117">Parameter arrays</span></span>](#paramarray)
- [<span data-ttu-id="c4899-118">İsteğe bağlı parametreler ve bağımsız değişkenler</span><span class="sxs-lookup"><span data-stu-id="c4899-118">Optional parameters and arguments</span></span>](#optional)
- [<span data-ttu-id="c4899-119">Dönüş değerleri</span><span class="sxs-lookup"><span data-stu-id="c4899-119">Return values</span></span>](#return)
- [<span data-ttu-id="c4899-120">Uzantı yöntemleri</span><span class="sxs-lookup"><span data-stu-id="c4899-120">Extension methods</span></span>](#extension)
- [<span data-ttu-id="c4899-121">Zaman uyumsuz yöntemler</span><span class="sxs-lookup"><span data-stu-id="c4899-121">Async Methods</span></span>](#async)
- [<span data-ttu-id="c4899-122">İfade gövdeli üyeler</span><span class="sxs-lookup"><span data-stu-id="c4899-122">Expression-bodied members</span></span>](#expr)
- [<span data-ttu-id="c4899-123">Yineleyiciler</span><span class="sxs-lookup"><span data-stu-id="c4899-123">Iterators</span></span>](#iterators)

<a name="signatures"></a>

## <a name="method-signatures"></a><span data-ttu-id="c4899-124">Yöntem imzaları</span><span class="sxs-lookup"><span data-stu-id="c4899-124">Method signatures</span></span>

<span data-ttu-id="c4899-125">Yöntemler bir `class` veya `struct` şunu belirterek belirtilir:</span><span class="sxs-lookup"><span data-stu-id="c4899-125">Methods are declared in a `class` or `struct` by specifying:</span></span>

- <span data-ttu-id="c4899-126">`public` Veya`private`gibi isteğe bağlı erişim düzeyi.</span><span class="sxs-lookup"><span data-stu-id="c4899-126">An optional access level, such as `public` or `private`.</span></span> <span data-ttu-id="c4899-127">Varsayılan, `private` değeridir.</span><span class="sxs-lookup"><span data-stu-id="c4899-127">The default is `private`.</span></span>
- <span data-ttu-id="c4899-128">`abstract` Veya`sealed`gibi isteğe bağlı değiştiriciler.</span><span class="sxs-lookup"><span data-stu-id="c4899-128">Optional modifiers such as `abstract` or `sealed`.</span></span>
- <span data-ttu-id="c4899-129">Dönüş değeri veya `void` metotta hiçbiri yoksa.</span><span class="sxs-lookup"><span data-stu-id="c4899-129">The return value, or `void` if the method has none.</span></span>
- <span data-ttu-id="c4899-130">Yöntem adı.</span><span class="sxs-lookup"><span data-stu-id="c4899-130">The method name.</span></span>
- <span data-ttu-id="c4899-131">Herhangi bir yöntem parametresi.</span><span class="sxs-lookup"><span data-stu-id="c4899-131">Any method parameters.</span></span> <span data-ttu-id="c4899-132">Yöntem parametreleri parantez içine alınır ve virgülle ayrılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-132">Method parameters are enclosed in parentheses and are separated by commas.</span></span> <span data-ttu-id="c4899-133">Boş parantezler, yöntemin hiçbir parametre gerektirmediğini belirtir.</span><span class="sxs-lookup"><span data-stu-id="c4899-133">Empty parentheses indicate that the method requires no parameters.</span></span>

<span data-ttu-id="c4899-134">Bu parçalar birlikte yöntem imzasını oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c4899-134">These parts together form the method signature.</span></span>

> [!NOTE]
> <span data-ttu-id="c4899-135">Bir yöntemin dönüş türü, yöntem aşırı yüklemesi amaçları için yöntemin imzasının bir parçası değildir.</span><span class="sxs-lookup"><span data-stu-id="c4899-135">A return type of a method is not part of the signature of the method for the purposes of method overloading.</span></span> <span data-ttu-id="c4899-136">Ancak, bir temsilci ve işaret ettiği yöntem arasındaki uyumluluğun belirlenmesi sırasında yönteminin imzasının bir parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-136">However, it is part of the signature of the method when determining the compatibility between a delegate and the method that it points to.</span></span>

<span data-ttu-id="c4899-137">Aşağıdaki örnek, beş yöntem içeren adlı `Motorcycle` bir sınıfı tanımlar:</span><span class="sxs-lookup"><span data-stu-id="c4899-137">The following example defines a class named `Motorcycle` that contains five methods:</span></span>

[!code-csharp[csSnippets.Methods#40](../../samples/snippets/csharp/concepts/methods/methods40.cs#40)]

<span data-ttu-id="c4899-138">Sınıfının aşırı yüklenmiş bir `Drive`yöntem içerdiğini unutmayın. `Motorcycle`</span><span class="sxs-lookup"><span data-stu-id="c4899-138">Note that the `Motorcycle` class includes an overloaded method, `Drive`.</span></span> <span data-ttu-id="c4899-139">İki yöntem aynı ada sahiptir, ancak parametre türlerine göre farklılaştıralınmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-139">Two methods have the same name, but must be differentiated by their parameter types.</span></span>

<a name="invocation"></a>

## <a name="method-invocation"></a><span data-ttu-id="c4899-140">Yöntem çağırma</span><span class="sxs-lookup"><span data-stu-id="c4899-140">Method invocation</span></span>

<span data-ttu-id="c4899-141">Yöntemler *örnek* veya *statik*olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-141">Methods can be either *instance* or *static*.</span></span> <span data-ttu-id="c4899-142">Örnek yöntemini çağırmak için bir nesnesi örneği oluşturabilir ve yöntemi bu nesnede çağırabilirsiniz. örnek yöntemi Bu örnek ve verileri üzerinde çalışır.</span><span class="sxs-lookup"><span data-stu-id="c4899-142">Invoking an instance method requires that you instantiate an object and call the method on that object; an instance method operates on that instance and its data.</span></span> <span data-ttu-id="c4899-143">Yöntemin ait olduğu türün adına başvurarak statik bir yöntem çağırılır; statik yöntemler örnek verilerinde çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="c4899-143">You invoke a static method by referencing the name of the type to which the method belongs; static methods do not operate on instance data.</span></span> <span data-ttu-id="c4899-144">Bir nesne örneği aracılığıyla statik bir yöntemi çağırma girişimi bir derleyici hatası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c4899-144">Attempting to call a static method through an object instance generates a compiler error.</span></span>

<span data-ttu-id="c4899-145">Bir yöntemi çağırmak, bir alana erişme gibidir.</span><span class="sxs-lookup"><span data-stu-id="c4899-145">Calling a method is like accessing a field.</span></span> <span data-ttu-id="c4899-146">Nesne adından sonra (bir örnek yöntemi arıyorsanız) veya tür adı (bir `static` yöntemi çağırırken), bir nokta, yöntemin adı ve parantez ekleyin.</span><span class="sxs-lookup"><span data-stu-id="c4899-146">After the object name (if you are calling an instance method) or the type name (if you are calling a `static` method), add a period, the name of the method, and parentheses.</span></span> <span data-ttu-id="c4899-147">Bağımsız değişkenler parantez içinde listelenir ve virgülle ayrılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-147">Arguments are listed within the parentheses, and are separated by commas.</span></span>

<span data-ttu-id="c4899-148">Yöntem tanımı, gerekli parametrelerin adlarını ve türlerini belirtir.</span><span class="sxs-lookup"><span data-stu-id="c4899-148">The method definition specifies the names and types of any parameters that are required.</span></span> <span data-ttu-id="c4899-149">Bir çağıran yöntemini çağırdığında, her bir parametre için bağımsız değişken olarak adlandırılan somut değerler sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-149">When a caller invokes the method, it provides concrete values, called arguments, for each parameter.</span></span> <span data-ttu-id="c4899-150">Bağımsız değişkenlerin parametre türüyle uyumlu olması gerekir, ancak çağıran kodda bir tane kullanılırsa bağımsız değişken adının, yönteminde tanımlanan parametre ile aynı olması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c4899-150">The arguments must be compatible with the parameter type, but the argument name, if one is used in the calling code, does not have to be the same as the parameter named defined in the method.</span></span> <span data-ttu-id="c4899-151">Aşağıdaki örnekte, `Square` yöntemi *i* `int` adında tek bir parametre içerir.</span><span class="sxs-lookup"><span data-stu-id="c4899-151">In the following example, the `Square` method includes a single parameter of type `int` named *i*.</span></span> <span data-ttu-id="c4899-152">İlk yöntem çağrısı, `Square` yöntemi *num*; ikinci, sayısal bir sabit ve üçüncü, bir ifade `int` adlı bir değişken olarak geçirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-152">The first method call passes the `Square` method a variable of type `int` named *num*; the second, a numeric constant; and the third, an expression.</span></span>

[!code-csharp[csSnippets.Methods#74](../../samples/snippets/csharp/concepts/methods/params74.cs#74)]

<span data-ttu-id="c4899-153">Yöntem çağırma tarafından kullanılan en yaygın biçim Konumsal bağımsız değişkenler; Yöntem parametreleriyle aynı sırada bağımsız değişkenler sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-153">The most common form of method invocation used positional arguments; it supplies arguments in the same order as method parameters.</span></span> <span data-ttu-id="c4899-154">Bu nedenle, `Motorcycle` sınıfının yöntemleri aşağıdaki örnekte olduğu gibi çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-154">The methods of the `Motorcycle` class can therefore be called as in the following example.</span></span> <span data-ttu-id="c4899-155">`Drive` Yöntemine yapılan çağrı, örneğin, yöntemin sözdiziminde iki parametreye karşılık gelen iki bağımsız değişken içerir.</span><span class="sxs-lookup"><span data-stu-id="c4899-155">The call to the `Drive` method, for example, includes two arguments that correspond to the two parameters in the method's syntax.</span></span> <span data-ttu-id="c4899-156">İlki parametrenin değeri `miles` , ikinci `speed` parametrenin değeri olur.</span><span class="sxs-lookup"><span data-stu-id="c4899-156">The first becomes the value of the `miles` parameter, the second the value of the `speed` parameter.</span></span>

[!code-csharp[csSnippets.Methods#41](../../samples/snippets/csharp/concepts/methods/methods40.cs#41)]

<span data-ttu-id="c4899-157">Bir yöntemi çağırırken Konumsal bağımsız değişkenler yerine *adlandırılmış bağımsız değişkenler* de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-157">You can also used *named arguments* instead of positional arguments when invoking a method.</span></span> <span data-ttu-id="c4899-158">Adlandırılmış bağımsız değişkenler kullanılırken, ardından iki nokta üst üste (":") ve bağımsız değişken ile parametre adını belirtirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-158">When using named arguments, you specify the parameter name followed by a colon (":") and the argument.</span></span> <span data-ttu-id="c4899-159">Yöntem için bağımsız değişkenler tüm gerekli bağımsız değişkenler bulunduğu sürece herhangi bir sırada görünebilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-159">Arguments to the method can appear in any order, as long as all required arguments are present.</span></span> <span data-ttu-id="c4899-160">Aşağıdaki örnek, `TestMotorcycle.Drive` yöntemini çağırmak için adlandırılmış bağımsız değişkenleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-160">The following example uses named arguments to invoke the `TestMotorcycle.Drive` method.</span></span> <span data-ttu-id="c4899-161">Bu örnekte, adlandırılmış bağımsız değişkenler metodun parametre listesinden ters sırada geçirilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-161">In this example, the named arguments are passed in the opposite order from the method's parameter list.</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/named1.cs#45)]

<span data-ttu-id="c4899-162">Bir yöntemi, hem Konumsal bağımsız değişkenleri hem de adlandırılmış bağımsız değişkenleri kullanarak çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-162">You can invoke a method using both positional arguments and named arguments.</span></span> <span data-ttu-id="c4899-163">Ancak, Konumsal bağımsız değişken adlandırılmış bir bağımsız değişkeni izleyemez.</span><span class="sxs-lookup"><span data-stu-id="c4899-163">However, a positional argument cannot follow a named argument.</span></span> <span data-ttu-id="c4899-164">Aşağıdaki örnek, bir konum `TestMotorcycle.Drive` bağımsız değişkeni ve bir adlandırılmış bağımsız değişken kullanarak önceki örnekteki yöntemi çağırır.</span><span class="sxs-lookup"><span data-stu-id="c4899-164">The following example invokes the `TestMotorcycle.Drive` method from the previous example using one positional argument and one named argument.</span></span>

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/named2.cs#46)]

<a name="inherited"></a>

## <a name="inherited-and-overridden-methods"></a><span data-ttu-id="c4899-165">Devralınan ve geçersiz kılınan Yöntemler</span><span class="sxs-lookup"><span data-stu-id="c4899-165">Inherited and overridden methods</span></span>

<span data-ttu-id="c4899-166">Bir tür içinde açıkça tanımlanan üyelere ek olarak, bir tür, temel sınıflarında tanımlanan üyeleri devralır.</span><span class="sxs-lookup"><span data-stu-id="c4899-166">In addition to the members that are explicitly defined in a type, a type inherits members defined in its base classes.</span></span> <span data-ttu-id="c4899-167">Yönetilen tür sistemindeki tüm türler doğrudan <xref:System.Object> veya dolaylı <xref:System.Object.GetType>olarak <xref:System.Object.Equals(System.Object)>sınıfından devraldığı için, tüm türler,, ve <xref:System.Object.ToString>gibi üyelerini alırlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-167">Since all types in the managed type system inherit directly or indirectly from the <xref:System.Object> class, all types inherit its members, such as <xref:System.Object.Equals(System.Object)>, <xref:System.Object.GetType>, and <xref:System.Object.ToString>.</span></span> <span data-ttu-id="c4899-168">Aşağıdaki örnek, bir `Person` sınıfı tanımlar, iki `Person` `Person.Equals` nesneyi örnekleyen ve iki nesnenin eşit olup olmadığını belirleyen yöntemini çağırır.</span><span class="sxs-lookup"><span data-stu-id="c4899-168">The following example defines a `Person` class, instantiates two `Person` objects, and calls the `Person.Equals` method to determine whether the two objects are equal.</span></span> <span data-ttu-id="c4899-169">Ancak yöntemi sınıfında tanımlı değildir; öğesinden <xref:System.Object>devralınır. `Person` `Equals`</span><span class="sxs-lookup"><span data-stu-id="c4899-169">The `Equals` method, however, is not defined in the `Person` class; it is inherited from <xref:System.Object>.</span></span>

[!code-csharp[csSnippets.Methods#104](../../samples/snippets/csharp/concepts/methods/inherited1.cs#104)]

<span data-ttu-id="c4899-170">Türler, `override` anahtar sözcüğünü kullanarak devralınan üyeleri geçersiz kılabilir ve geçersiz kılınan yöntem için bir uygulama sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-170">Types can override inherited members by using the `override` keyword and providing an implementation for the overridden method.</span></span> <span data-ttu-id="c4899-171">Yöntem imzası, geçersiz kılınan metodun ile aynı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-171">The method signature must be the same as that of the overridden method.</span></span> <span data-ttu-id="c4899-172">Aşağıdaki örnek, <xref:System.Object.Equals(System.Object)> yönteminin geçersiz kılınmasının dışında, önceki bir yönteme benzer.</span><span class="sxs-lookup"><span data-stu-id="c4899-172">The following example is like the previous one, except that it overrides the <xref:System.Object.Equals(System.Object)> method.</span></span> <span data-ttu-id="c4899-173">(Aynı zamanda <xref:System.Object.GetHashCode> yöntemi geçersiz kılar, çünkü iki yöntem tutarlı sonuçlar sağlamaya yöneliktir.)</span><span class="sxs-lookup"><span data-stu-id="c4899-173">(It also overrides the <xref:System.Object.GetHashCode> method, since the two methods are intended to provide consistent results.)</span></span>

[!code-csharp[csSnippets.Methods#105](../../samples/snippets/csharp/concepts/methods/overridden1.cs#105)]

<a name="passing"></a>

## <a name="passing-parameters"></a><span data-ttu-id="c4899-174">Parametreleri geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-174">Passing parameters</span></span>

<span data-ttu-id="c4899-175">İçindeki C# türler, *değer türleri* ya da *başvuru türleridir*.</span><span class="sxs-lookup"><span data-stu-id="c4899-175">Types in C# are either *value types* or *reference types*.</span></span> <span data-ttu-id="c4899-176">Yerleşik değer türlerinin bir listesi için bkz. [türler ve değişkenler](./tour-of-csharp/types-and-variables.md).</span><span class="sxs-lookup"><span data-stu-id="c4899-176">For a list of built-in value types, see [Types and variables](./tour-of-csharp/types-and-variables.md).</span></span> <span data-ttu-id="c4899-177">Varsayılan olarak, değer türleri ve başvuru türleri değere göre bir yönteme geçirilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-177">By default, both value types and reference types are passed to a method by value.</span></span>

<a name="byval"></a>

### <a name="passing-parameters-by-value"></a><span data-ttu-id="c4899-178">Parametreleri değere göre geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-178">Passing parameters by value</span></span>

<span data-ttu-id="c4899-179">Değer türü bir yönteme değere göre geçirildiğinde, nesnesine nesnenin kendisi yerine bir kopyası geçirilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-179">When a value type is passed to a method by value, a copy of the object instead of the object itself is passed to the method.</span></span> <span data-ttu-id="c4899-180">Bu nedenle, çağrılan yöntemdeki nesne üzerindeki değişikliklerin, Denetim çağırana döndüğünde özgün nesne üzerinde hiçbir etkisi olmaz.</span><span class="sxs-lookup"><span data-stu-id="c4899-180">Therefore, changes to the object in the called method have no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="c4899-181">Aşağıdaki örnek bir değer türünü değere göre bir yönteme geçirir ve çağrılan yöntem değer türünün değerini değiştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="c4899-181">The following example passes a value type to a method by value, and the called method attempts to change the value type's value.</span></span> <span data-ttu-id="c4899-182">Bir değer türü olan türünde `int`bir değişken tanımlar, değerini 20 olarak başlatır ve değişkenin değerini 30 olarak değiştiren adlı `ModifyValue` bir yönteme geçirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-182">It defines a variable of type `int`, which is a value type, initializes its value to 20, and passes it to a method named `ModifyValue` that changes the variable's value to 30.</span></span> <span data-ttu-id="c4899-183">Ancak yöntemi döndüğünde, değişkenin değeri değişmeden kalır.</span><span class="sxs-lookup"><span data-stu-id="c4899-183">When the method returns, however, the variable's value remains unchanged.</span></span>

[!code-csharp[csSnippets.Methods#10](../../samples/snippets/csharp/concepts/methods/byvalue10.cs#10)]

<span data-ttu-id="c4899-184">Başvuru türündeki bir nesne bir değere göre yönteme geçirildiğinde, nesnesine bir başvuru değeri ile geçirilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-184">When an object of a reference type is passed to a method by value, a reference to the object is passed by value.</span></span> <span data-ttu-id="c4899-185">Diğer bir deyişle, yöntem nesnenin kendisini değil, nesnenin konumunu gösteren bir bağımsız değişkendir.</span><span class="sxs-lookup"><span data-stu-id="c4899-185">That is, the method receives not the object itself, but an argument that indicates the location of the object.</span></span> <span data-ttu-id="c4899-186">Bu başvuruyu kullanarak nesnesinin bir üyesini değiştirirseniz, Denetim çağıran yönteme döndüğünde değişiklik nesnesine yansıtılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-186">If you change a member of the object by using this reference, the change is reflected in the object when control returns to the calling method.</span></span> <span data-ttu-id="c4899-187">Ancak, metoduna geçirilen nesnenin değiştirilmesi, Denetim çağırana döndüğünde özgün nesneyi etkilemez.</span><span class="sxs-lookup"><span data-stu-id="c4899-187">However, replacing the object passed to the method has no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="c4899-188">Aşağıdaki örnek adlı `SampleRefType`bir sınıfı (bir başvuru türü) tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-188">The following example defines a class (which is a reference type) named `SampleRefType`.</span></span> <span data-ttu-id="c4899-189">Bir `SampleRefType` nesnesi başlatır, `value` alanına 44 atar ve nesneyi `ModifyObject` yöntemine geçirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-189">It instantiates a `SampleRefType` object, assigns 44 to its `value` field, and passes the object to the `ModifyObject` method.</span></span> <span data-ttu-id="c4899-190">Bu örnek temelde önceki örnekle aynı şeyi yapar; bir bağımsız değişkeni değere göre bir yönteme geçirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-190">This example does essentially the same thing as the previous example -- it passes an argument by value to a method.</span></span> <span data-ttu-id="c4899-191">Ancak, bir başvuru türü kullanıldığından, sonuç farklıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-191">But because a reference type is used, the result is different.</span></span> <span data-ttu-id="c4899-192">`ModifyObject` `value` `rt` `Main` Alanda yapılan değişiklik, örnekte gösterildiği gibi, yönteminde 33 olan bağımsız değişkenin alanını da değiştirir. `obj.value`</span><span class="sxs-lookup"><span data-stu-id="c4899-192">The modification that is made in `ModifyObject` to the `obj.value` field also changes the `value` field of the argument, `rt`, in the `Main` method to 33, as the output from the example shows.</span></span>

[!code-csharp[csSnippets.Methods#42](../../samples/snippets/csharp/concepts/methods/byvalue42.cs#42)]

<a name="byref"></a>

### <a name="passing-parameters-by-reference"></a><span data-ttu-id="c4899-193">Parametreleri başvuruya göre geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-193">Passing parameters by reference</span></span>

<span data-ttu-id="c4899-194">Bir yöntemde bir bağımsız değişkenin değerini değiştirmek istediğinizde ve denetim çağırma yöntemine döndüğünde bu değişikliği yansıtmak istediğinizde, parametreyi başvuruya göre geçirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-194">You pass a parameter by reference when you want to change the value of an argument in a method and want to reflect that change when control returns to the calling method.</span></span> <span data-ttu-id="c4899-195">Bir parametreyi başvuruya göre geçirmek için [`ref`](language-reference/keywords/ref.md) veya [`out`](language-reference/keywords/out-parameter-modifier.md) anahtar sözcüğünü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="c4899-195">To pass a parameter by reference, you use the [`ref`](language-reference/keywords/ref.md) or [`out`](language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="c4899-196">Kopyalamayı önlemek için bir değeri başvuruya göre geçirebilir, ancak yine de [`in`](language-reference/keywords/in-parameter-modifier.md) anahtar sözcüğü kullanarak değişiklikleri önleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-196">You can also pass a value by reference to avoid copying but still prevent modifications using the [`in`](language-reference/keywords/in-parameter-modifier.md) keyword.</span></span>

<span data-ttu-id="c4899-197">Aşağıdaki örnek, değeri başvuruya `ModifyValue` göre geçirilme dışında öncekiyle aynıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-197">The following example is identical to the previous one, except the value is passed by reference to the `ModifyValue` method.</span></span> <span data-ttu-id="c4899-198">`ModifyValue` Yönteminde parametresinin değeri değiştirildiğinde, Denetim çağırana döndüğünde değer değişikliği yansıtılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-198">When the value of the parameter is modified in the `ModifyValue` method, the change in value is reflected when control returns to the caller.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/byref106.cs#106)]

<span data-ttu-id="c4899-199">Ref parametreleri tarafından kullanılan ortak bir model, değişkenlerin değerlerini değiştirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="c4899-199">A common pattern that uses by ref parameters involves swapping the values of variables.</span></span> <span data-ttu-id="c4899-200">İki değişkeni başvuruya göre bir yönteme geçirirseniz ve yöntemi içeriğini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-200">You pass two variables to a method by reference, and the method swaps their contents.</span></span> <span data-ttu-id="c4899-201">Aşağıdaki örnek, tamsayı değerlerini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-201">The following example swaps integer values.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/swap107.cs#107)]

<span data-ttu-id="c4899-202">Bir başvuru türü parametresinin geçirilmesi, tek öğelerinin veya alanlarının değeri yerine başvurunun kendisinin değerini değiştirmenize izin verir.</span><span class="sxs-lookup"><span data-stu-id="c4899-202">Passing a reference-type parameter allows you to change the value of the reference itself, rather than the value of its individual elements or fields.</span></span>

<a name="paramarray"></a>

### <a name="parameter-arrays"></a><span data-ttu-id="c4899-203">Parametre dizileri</span><span class="sxs-lookup"><span data-stu-id="c4899-203">Parameter arrays</span></span>

<span data-ttu-id="c4899-204">Bazen, yönteminiz için bağımsız değişkenlerin tam sayısını belirttiğiniz gereksinim kısıtlayıcıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-204">Sometimes, the requirement that you specify the exact number of arguments to your method is restrictive.</span></span> <span data-ttu-id="c4899-205">Bir parametrenin parametre `params` dizisi olduğunu göstermek için anahtar sözcüğünü kullanarak, yönteminizin değişken sayıda bağımsız değişkenle çağrılmasına izin verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-205">By using the `params` keyword to indicate that a parameter is a parameter array, you allow your method to be called with a variable number of arguments.</span></span> <span data-ttu-id="c4899-206">`params` Anahtar kelimesiyle etiketlenmiş parametre bir dizi türü olmalıdır ve yöntemin parametre listesindeki son parametre olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-206">The parameter tagged with the `params` keyword must be an array type, and it must be the last parameter in the method's parameter list.</span></span>

<span data-ttu-id="c4899-207">Bir çağıran daha sonra yöntemi üç yöntemden birini çağırabilir:</span><span class="sxs-lookup"><span data-stu-id="c4899-207">A caller can then invoke the method in either of three ways:</span></span>

- <span data-ttu-id="c4899-208">İstenen sayıda öğe içeren uygun türdeki bir diziyi geçirerek.</span><span class="sxs-lookup"><span data-stu-id="c4899-208">By passing an array of the appropriate type that contains the desired number of elements.</span></span>
- <span data-ttu-id="c4899-209">Uygun türdeki bağımsız değişkenlerin virgülle ayrılmış bir listesini yöntemine geçirerek.</span><span class="sxs-lookup"><span data-stu-id="c4899-209">By passing a comma-separated list of individual arguments of the appropriate type to the method.</span></span>
- <span data-ttu-id="c4899-210">Parametre dizisine bir bağımsız değişken sağlamıyor.</span><span class="sxs-lookup"><span data-stu-id="c4899-210">By not providing an argument to the parameter array.</span></span>

<span data-ttu-id="c4899-211">Aşağıdaki örnek, bir parametre dizisinin tüm `GetVowels` sesli harfleri döndüren adlı bir yöntemi tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-211">The following example defines a method named `GetVowels` that returns all the vowels from a parameter array.</span></span> <span data-ttu-id="c4899-212">`Main` Yöntemi, yöntemi çağırmanın üç yolunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="c4899-212">The `Main` method illustrates all three ways of invoking the method.</span></span> <span data-ttu-id="c4899-213">`params` Değiştirici içeren parametrelere ilişkin bağımsız değişkenler sağlamak için çağıranlar gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="c4899-213">Callers are not required to supply any arguments for parameters that include the `params` modifier.</span></span> <span data-ttu-id="c4899-214">Bu durumda, parametresi olur `null`.</span><span class="sxs-lookup"><span data-stu-id="c4899-214">In that case, the parameter is `null`.</span></span>

[!code-csharp[csSnippets.Methods#75](~/samples/snippets/csharp/concepts/methods/params75.cs#75)]

<a name="optional"></a>

## <a name="optional-parameters-and-arguments"></a><span data-ttu-id="c4899-215">İsteğe bağlı parametreler ve bağımsız değişkenler</span><span class="sxs-lookup"><span data-stu-id="c4899-215">Optional parameters and arguments</span></span>

<span data-ttu-id="c4899-216">Yöntem tanımı parametrelerinin gerekli olduğunu veya isteğe bağlı olduğunu belirtebilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-216">A method definition can specify that its parameters are required or that they are optional.</span></span> <span data-ttu-id="c4899-217">Varsayılan olarak Parametreler gereklidir.</span><span class="sxs-lookup"><span data-stu-id="c4899-217">By default, parameters are required.</span></span> <span data-ttu-id="c4899-218">İsteğe bağlı parametreler, parametrenin varsayılan değeri Yöntem tanımına eklenerek belirtilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-218">Optional parameters are specified by including the parameter's default value in the method definition.</span></span> <span data-ttu-id="c4899-219">Yöntemi çağrıldığında, isteğe bağlı bir parametre için bir bağımsız değişken sağlanmazsa, bunun yerine varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-219">When the method is called, if no argument is supplied for an optional parameter, the default value is used instead.</span></span>

<span data-ttu-id="c4899-220">Parametrenin varsayılan değeri aşağıdaki tür ifadelerden biri tarafından atanmalıdır:</span><span class="sxs-lookup"><span data-stu-id="c4899-220">The parameter's default value must be assigned by one of the following kinds of expressions:</span></span>

- <span data-ttu-id="c4899-221">Sabit dize veya sayı gibi bir sabit.</span><span class="sxs-lookup"><span data-stu-id="c4899-221">A constant, such as a literal string or number.</span></span>
- <span data-ttu-id="c4899-222">Formun `new ValType`bir ifadesi, burada `ValType` bir değer türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4899-222">An expression of the form `new ValType`, where `ValType` is a value type.</span></span> <span data-ttu-id="c4899-223">Bunun, türün gerçek üyesi olmayan, değer türünün örtük parametresiz oluşturucusunu çağırdığına unutmayın.</span><span class="sxs-lookup"><span data-stu-id="c4899-223">Note that this invokes the value type's implicit parameterless constructor, which is not an actual member of the type.</span></span>
- <span data-ttu-id="c4899-224">Formun `default(ValType)`bir ifadesi, burada `ValType` bir değer türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4899-224">An expression of the form `default(ValType)`, where `ValType` is a value type.</span></span>

<span data-ttu-id="c4899-225">Bir yöntem hem gerekli hem de isteğe bağlı parametreleri içeriyorsa, isteğe bağlı parametreler parametre listesinin sonunda, tüm gerekli parametrelerden sonra tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-225">If a method includes both required and optional parameters, optional parameters are defined at the end of the parameter list, after all required parameters.</span></span>

<span data-ttu-id="c4899-226">Aşağıdaki örnek, bir zorunlu ve iki `ExampleMethod`isteğe bağlı parametre içeren bir yöntemini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-226">The following example defines a method, `ExampleMethod`, that has one required and two optional parameters.</span></span>

[!code-csharp[csSnippets.Methods#21](../../samples/snippets/csharp/concepts/methods/optional1.cs#21)]

<span data-ttu-id="c4899-227">Birden çok isteğe bağlı bağımsız değişkeni olan bir yöntem Konumsal bağımsız değişkenler kullanılarak çağrılırsa, çağıran ilk bir bağımsız değişken için bağımsız değişkenin sağlandığı en son bir parametre için bir bağımsız değişken sağlamalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-227">If a method with multiple optional arguments is invoked using positional arguments, the caller must supply an argument for all optional parameters from the first one to the last one for which an argument is supplied.</span></span> <span data-ttu-id="c4899-228">`ExampleMethod` Yöntemin söz konusu olduğunda, örneğin, çağıran `description` parametre için bir bağımsız değişken sağlarsa, bu `optionalInt` parametre için de bir tane sağlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-228">In the case of the  `ExampleMethod` method, for example, if the caller supplies an argument for the `description` parameter, it must also supply one for the `optionalInt` parameter.</span></span> <span data-ttu-id="c4899-229">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");`geçerli bir yöntem çağrıdır; `opt.ExampleMethod(2, , "Addition of 2 and 0");` "bağımsız değişken eksik" derleyici hatası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c4899-229">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");` is a valid method call; `opt.ExampleMethod(2, , "Addition of 2 and 0");` generates an "Argument missing" compiler error.</span></span>

<span data-ttu-id="c4899-230">Bir yöntem adlandırılmış bağımsız değişkenler veya konumsal ve adlandırılmış bağımsız değişkenlerin bir birleşimi kullanılarak çağrılırsa, çağıran yöntem çağrısındaki son Konumsal bağımsız değişkeni izleyen tüm bağımsız değişkenleri atlayabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-230">If a method is called using named arguments or a combination of positional and named arguments, the caller can omit any arguments that follow the last positional argument in the method call.</span></span>

<span data-ttu-id="c4899-231">Aşağıdaki örnek, `ExampleMethod` yöntemi üç kez çağırır.</span><span class="sxs-lookup"><span data-stu-id="c4899-231">The following example calls the `ExampleMethod` method three times.</span></span>  <span data-ttu-id="c4899-232">İlk iki yöntem çağıran konum değişkenlerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-232">The first two method calls use positional arguments.</span></span> <span data-ttu-id="c4899-233">İlki hem isteğe bağlı bağımsız değişkenleri atlar, ikincisi ise son bağımsız değişkeni atlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-233">The first omits both optional arguments, while the second omits the last argument.</span></span> <span data-ttu-id="c4899-234">Üçüncü yöntem çağrısı, gerekli parametre için bir Konumsal bağımsız değişken sağlar, ancak `description` `optionalInt` bağımsız değişkeni atlayarak parametreye bir değer sağlamak için adlandırılmış bir bağımsız değişken kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-234">The third method call supplies a positional argument for the required parameter, but uses a named argument to supply a value to the `description` parameter while omitting the `optionalInt` argument.</span></span>

[!code-csharp[csSnippets.Methods#22](../../samples/snippets/csharp/concepts/methods/optional1.cs#22)]

<span data-ttu-id="c4899-235">İsteğe bağlı parametrelerin kullanımı, *aşırı yükleme çözümlemesini*etkiler veya C# derleyicinin hangi belirli aşırı yüklemeyi bir yöntem çağrısı tarafından çağrılması gerektiğini belirleyen yöntemi aşağıda gösterildiği gibi:</span><span class="sxs-lookup"><span data-stu-id="c4899-235">The use of optional parameters affects *overload resolution*, or the way in which the C# compiler determines which particular overload should be invoked by a method call, as follows:</span></span>

- <span data-ttu-id="c4899-236">Bir yöntem, Dizin Oluşturucu veya Oluşturucu, parametrelerinden her biri isteğe bağlı veya bir konuma göre, çağırma deyimindeki tek bir bağımsız değişkene ve bu bağımsız değişken parametre türüne dönüştürülebileceğinden yürütme için bir adaydır.</span><span class="sxs-lookup"><span data-stu-id="c4899-236">A method, indexer, or constructor is a candidate for execution if each of its parameters either is optional or corresponds, by name or by position, to a single argument in the calling statement, and that argument can be converted to the type of the parameter.</span></span>
- <span data-ttu-id="c4899-237">Birden fazla aday bulunursa, tercih edilen dönüştürmeler için aşırı yükleme çözümleme kuralları, açıkça belirtilen bağımsız değişkenlere uygulanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-237">If more than one candidate is found, overload resolution rules for preferred conversions are applied to the arguments that are explicitly specified.</span></span> <span data-ttu-id="c4899-238">İsteğe bağlı parametreler için Atlanan bağımsız değişkenler yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="c4899-238">Omitted arguments for optional parameters are ignored.</span></span>
- <span data-ttu-id="c4899-239">İki aday eşit derecede iyi bir şekilde yarar olursa, tercih, çağrıda bağımsız değişkenlerin atlandığı isteğe bağlı parametreleri olmayan bir adaya gider.</span><span class="sxs-lookup"><span data-stu-id="c4899-239">If two candidates are judged to be equally good, preference goes to a candidate that does not have optional parameters for which arguments were omitted in the call.</span></span> <span data-ttu-id="c4899-240">Bu, daha az parametreye sahip adaylar için aşırı yükleme çözünürlüğünde genel bir tercihin sonucudur.</span><span class="sxs-lookup"><span data-stu-id="c4899-240">This is a consequence of a general preference in overload resolution for candidates that have fewer parameters.</span></span>

<a name="return"></a>

## <a name="return-values"></a><span data-ttu-id="c4899-241">Döndürülen değerler</span><span class="sxs-lookup"><span data-stu-id="c4899-241">Return values</span></span>

<span data-ttu-id="c4899-242">Yöntemler çağırana bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-242">Methods can return a value to the caller.</span></span> <span data-ttu-id="c4899-243">Dönüş türü (yöntem adından önce listelenen tür) değilse `void`, yöntemi `return` anahtar sözcüğünü kullanarak değeri döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-243">If the return type (the type listed before the method name) is not `void`, the method can return the value by using the `return` keyword.</span></span> <span data-ttu-id="c4899-244">`return` Anahtar sözcüğünü içeren bir ifade, ardından dönüş türüyle eşleşen bir değişken, sabit veya ifade, bu değeri çağıran yönteme döndürür.</span><span class="sxs-lookup"><span data-stu-id="c4899-244">A statement with the `return` keyword followed by a variable, constant, or expression that matches the return type will return that value to the method caller.</span></span> <span data-ttu-id="c4899-245">Bir değer döndürmek için `return` anahtar sözcüğünü kullanmak için void olmayan bir dönüş türüne sahip metotlar gereklidir.</span><span class="sxs-lookup"><span data-stu-id="c4899-245">Methods with a non-void return type are required to use the `return` keyword to return a value.</span></span> <span data-ttu-id="c4899-246">`return` Anahtar sözcüğü, yönteminin yürütülmesini de sonlandırır.</span><span class="sxs-lookup"><span data-stu-id="c4899-246">The `return` keyword also stops the execution of the method.</span></span>

<span data-ttu-id="c4899-247">Dönüş türü ise `void`, değeri olmayan bir `return` ifade, metodun yürütülmesini durdurmak için hala yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-247">If the return type is `void`, a `return` statement without a value is still useful to stop the execution of the method.</span></span> <span data-ttu-id="c4899-248">`return` Anahtar sözcüğü olmadan Yöntem, kod bloğunun sonuna ulaştığında yürütmeyi durdurur.</span><span class="sxs-lookup"><span data-stu-id="c4899-248">Without the `return` keyword, the method will stop executing when it reaches the end of the code block.</span></span>

<span data-ttu-id="c4899-249">Örneğin, bu iki yöntem tamsayılar döndürmek için `return` anahtar sözcüğünü kullanır:</span><span class="sxs-lookup"><span data-stu-id="c4899-249">For example, these two methods use the `return` keyword to return integers:</span></span>

[!code-csharp[csSnippets.Methods#44](../../samples/snippets/csharp/concepts/methods/return44.cs#44)]

<span data-ttu-id="c4899-250">Bir yöntemden döndürülen bir değer kullanmak için, çağırma yöntemi yöntemi tek bir değer olan her yerde, aynı türde bir değer yeterli olacak şekilde kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-250">To use a value returned from a method, the calling method can use the method call itself anywhere a value of the same type would be sufficient.</span></span> <span data-ttu-id="c4899-251">Dönüş değerini bir değişkene de atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-251">You can also assign the return value to a variable.</span></span> <span data-ttu-id="c4899-252">Örneğin, aşağıdaki iki kod örneği aynı hedefi yerine getirmektedir:</span><span class="sxs-lookup"><span data-stu-id="c4899-252">For example, the following two code examples accomplish the same goal:</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/return44.cs#45)]

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/return44.cs#46)]

<span data-ttu-id="c4899-253">Bu durumda `result`, bir değeri depolamak için yerel bir değişken kullanmak isteğe bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-253">Using a local variable, in this case, `result`, to store a value is optional.</span></span> <span data-ttu-id="c4899-254">Kodun okunabilirliğini yardımcı olabilir veya metodun tüm kapsamı için bağımsız değişkenin özgün değerini depolamanız gerekirse gerekli olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-254">It may help the readability of the code, or it may be necessary if you need to store the original value of the argument for the entire scope of the method.</span></span>

<span data-ttu-id="c4899-255">Bazen yönteminizin tek bir değerden fazlasını döndürmesini istersiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-255">Sometimes, you want your method to return more than a single value.</span></span> <span data-ttu-id="c4899-256">7,0 ' C# den başlayarak, *kayıt düzeni türlerini* ve *demet sabit*değerlerini kullanarak bunu kolayca yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-256">Starting with C# 7.0, you can do this easily by using *tuple types* and *tuple literals*.</span></span> <span data-ttu-id="c4899-257">Demet türü, demet öğelerinin veri türlerini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-257">The tuple type defines the data types of the tuple's elements.</span></span> <span data-ttu-id="c4899-258">Demet sabit değerleri, döndürülen tanımlama grubunun gerçek değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-258">Tuple literals provide the actual values of the returned tuple.</span></span> <span data-ttu-id="c4899-259">Aşağıdaki örnekte, `(string, string, string, int)` `GetPersonalInfo` yöntemi tarafından döndürülen demet türünü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-259">In the following example, `(string, string, string, int)` defines the tuple type that is returned by the `GetPersonalInfo` method.</span></span> <span data-ttu-id="c4899-260">İfade `(per.FirstName, per.MiddleName, per.LastName, per.Age)` , tanımlama grubu değişmez değeri; Yöntem, bir `PersonInfo` nesnenin yaş ve adının yanı sıra birinci, orta ve soyadı döndürür.</span><span class="sxs-lookup"><span data-stu-id="c4899-260">The expression `(per.FirstName, per.MiddleName, per.LastName, per.Age)` is the tuple literal; the method returns the first, middle, and last name, along with the age, of a `PersonInfo` object.</span></span>

```csharp
public (string, string, string, int) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    return (per.FirstName, per.MiddleName, per.LastName, per.Age);
}
```

<span data-ttu-id="c4899-261">Çağıran daha sonra aşağıdaki gibi kodla döndürülen kayıt grubunu kullanabilir:</span><span class="sxs-lookup"><span data-stu-id="c4899-261">The caller can then consume the returned tuple with code like the following:</span></span>

```csharp
var person = GetPersonalInfo("111111111")
Console.WriteLine("{person.Item1} {person.Item3}: age = {person.Item4}");
```

<span data-ttu-id="c4899-262">Adlar, kayıt düzeni türü tanımındaki demet öğelerine de atanabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-262">Names can also be assigned to the tuple elements in the tuple type definition.</span></span> <span data-ttu-id="c4899-263">Aşağıdaki örnek, adlandırılmış öğeleri kullanan `GetPersonalInfo` yönteminin alternatif bir sürümünü göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="c4899-263">The following example shows an alternate version of the `GetPersonalInfo` method that uses named elements:</span></span>

```csharp
public (string FName, string MName, string LName, int Age) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    return (per.FirstName, per.MiddleName, per.LastName, per.Age);
}
```

<span data-ttu-id="c4899-264">Önceki `GetPersonInfo` yönteme yapılan çağrı daha sonra aşağıdaki gibi değiştirilebilir:</span><span class="sxs-lookup"><span data-stu-id="c4899-264">The previous call to the `GetPersonInfo` method can then be modified as follows:</span></span>

```csharp
var person = GetPersonalInfo("111111111");
Console.WriteLine("{person.FName} {person.LName}: age = {person.Age}");
```

<span data-ttu-id="c4899-265">Bir yöntem bir diziyi bağımsız değişken olarak geçirse ve tek tek öğelerin değerini değiştirirse, yöntemin diziyi döndürmesi gerekli değildir, ancak bunun için uygun stil veya işlevsel akış için bunu seçebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-265">If a method is passed an array as an argument and modifies the value of individual elements, it is not necessary for the method to return the array, although you may choose to do so for good style or functional flow of values.</span></span>  <span data-ttu-id="c4899-266">Bunun nedeni C# , tüm başvuru türlerini değere göre geçirir ve dizi başvurusunun değeri dizi işaretçisidir.</span><span class="sxs-lookup"><span data-stu-id="c4899-266">This is because C# passes all reference types by value, and the value of an array reference is the pointer to the array.</span></span> <span data-ttu-id="c4899-267">Aşağıdaki örnekte, `values` `DoubleValues` yönteminde yapılan dizi içeriklerinde yapılan değişiklikler, dizi başvurusu olan herhangi bir kod tarafından Observable ' tır.</span><span class="sxs-lookup"><span data-stu-id="c4899-267">In the following example, changes to the contents of the `values` array that are made in the `DoubleValues` method are observable by any code that has a reference to the array.</span></span>

[!code-csharp[csSnippets.Methods#101](../../samples/snippets/csharp/concepts/methods/returnarray1.cs#101)]

<a name="extension"></a>

## <a name="extension-methods"></a><span data-ttu-id="c4899-268">Uzantı yöntemleri</span><span class="sxs-lookup"><span data-stu-id="c4899-268">Extension methods</span></span>

<span data-ttu-id="c4899-269">Genellikle, varolan bir türe bir yöntem eklemenin iki yolu vardır:</span><span class="sxs-lookup"><span data-stu-id="c4899-269">Ordinarily, there are two ways to add a method to an existing type:</span></span>

- <span data-ttu-id="c4899-270">Bu tür için kaynak kodu değiştirin.</span><span class="sxs-lookup"><span data-stu-id="c4899-270">Modify the source code for that type.</span></span> <span data-ttu-id="c4899-271">Bu, türün kaynak kodunu sahipsiz olmadığınız takdirde bunu yapamazlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-271">You cannot do this, of course, if you do not own the type's source code.</span></span> <span data-ttu-id="c4899-272">Ayrıca, yöntemi desteklemek için herhangi bir özel veri alanı eklerseniz, bu bir büyük değişiklik haline gelir.</span><span class="sxs-lookup"><span data-stu-id="c4899-272">And this becomes a breaking change if you also add any private data fields to support the method.</span></span>
- <span data-ttu-id="c4899-273">Yeni metodu türetilmiş bir sınıfta tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="c4899-273">Define the new method in a derived class.</span></span> <span data-ttu-id="c4899-274">Bir yöntem, yapılar ve numaralandırmalar gibi diğer türler için devralma kullanılarak bu şekilde eklenemez.</span><span class="sxs-lookup"><span data-stu-id="c4899-274">A method cannot be added in this way using inheritance for other types, such as structures and enumerations.</span></span> <span data-ttu-id="c4899-275">Ayrıca, korumalı bir sınıfa bir yöntemi "eklemek" için de kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-275">Nor can it be used to "add" a method to a sealed class.</span></span>

<span data-ttu-id="c4899-276">Uzantı yöntemleri, türün kendisini değiştirmeden veya yeni yöntemi devralınmış bir tür içinde uygulamadan, varolan bir türe "eklemenize" olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4899-276">Extension methods let you "add" a method to an existing type without modifying the type itself or implementing the new method in an inherited type.</span></span> <span data-ttu-id="c4899-277">Uzantı yönteminin, genişlettiği türle aynı derlemede bulunması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c4899-277">The extension method also does not have to reside in the same assembly as the type it extends.</span></span> <span data-ttu-id="c4899-278">Bir uzantı yöntemini bir türün tanımlı üyesi gibi çağırın.</span><span class="sxs-lookup"><span data-stu-id="c4899-278">You call an extension method as if it were a defined member of a type.</span></span>

<span data-ttu-id="c4899-279">Daha fazla bilgi için bkz. [Uzantı yöntemleri](programming-guide/classes-and-structs/extension-methods.md).</span><span class="sxs-lookup"><span data-stu-id="c4899-279">For more information, see [Extension Methods](programming-guide/classes-and-structs/extension-methods.md).</span></span>

<a name="async"></a>

## <a name="async-methods"></a><span data-ttu-id="c4899-280">Zaman uyumsuz yöntemler</span><span class="sxs-lookup"><span data-stu-id="c4899-280">Async Methods</span></span>

<span data-ttu-id="c4899-281">Async özelliğini kullanarak, açık geri çağırmaları kullanmadan zaman uyumsuz yöntemleri çağırabilir veya kodunuzu birden çok yöntemde veya Lambda ifadelerinde el ile böedebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-281">By using the async feature, you can invoke asynchronous methods without using explicit callbacks or manually splitting your code across multiple methods or lambda expressions.</span></span>

<span data-ttu-id="c4899-282">[Zaman uyumsuz](language-reference/keywords/async.md) değiştiriciyle bir yöntemi işaretlerseniz, yönteminde [await](language-reference/operators/await.md) işlecini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4899-282">If you mark a method with the [async](language-reference/keywords/async.md) modifier, you can use the [await](language-reference/operators/await.md) operator in the method.</span></span> <span data-ttu-id="c4899-283">Denetim zaman uyumsuz yöntemde `await` bir ifadeye ulaştığında, beklenen görev tamamlanmazsa denetim çağırana döner ve beklenen görev tamamlanana kadar `await` anahtar sözcüğü ile yöntemdeki işlem askıya alınır.</span><span class="sxs-lookup"><span data-stu-id="c4899-283">When control reaches an `await` expression in the async method, control returns to the caller if the awaited task is not completed, and progress in the method with the `await` keyword is suspended until the awaited task completes.</span></span> <span data-ttu-id="c4899-284">Görev tamamlandığında, yürütme yöntemi içinde çalışmaya çalışabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-284">When the task is complete, execution can resume in the method.</span></span>

> [!NOTE]
> <span data-ttu-id="c4899-285">Zaman uyumsuz bir yöntem, henüz tamamlanmamış olan ilk beklemiş nesneyle karşılaştığında çağırana döner veya zaman uyumsuz yöntemin sonuna kadar, hangisi önce gerçekleşiyorsa.</span><span class="sxs-lookup"><span data-stu-id="c4899-285">An async method returns to the caller when either it encounters the first awaited object that’s not yet complete or it gets to the end of the async method, whichever occurs first.</span></span>

<span data-ttu-id="c4899-286">Zaman uyumsuz bir yöntem <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.Task>, veya `void`dönüş türüne sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-286">An async method can have a return type of <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.Task>, or `void`.</span></span> <span data-ttu-id="c4899-287">Dönüş türü öncelikle bir `void` dönüş türünün gerekli olduğu olay işleyicilerini tanımlamak için kullanılır. `void`</span><span class="sxs-lookup"><span data-stu-id="c4899-287">The `void` return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="c4899-288">Döndüren `void` zaman uyumsuz bir yöntem beklenemez ve void döndüren bir yöntemi çağıran yöntemin aldığı özel durumları yakalayamaz.</span><span class="sxs-lookup"><span data-stu-id="c4899-288">An async method that returns `void` can't be awaited, and the caller of a void-returning method can't catch exceptions that the method throws.</span></span> <span data-ttu-id="c4899-289">7,0 ile C# başlayarak, zaman uyumsuz bir yöntem [herhangi bir görev benzeri dönüş türüne](./whats-new/csharp-7.md#generalized-async-return-types)sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-289">Starting with C# 7.0, an async method can have [any task-like return type](./whats-new/csharp-7.md#generalized-async-return-types).</span></span>

<span data-ttu-id="c4899-290">Aşağıdaki örnekte, `DelayAsync` bir tamsayı döndüren Return ifadesine sahip zaman uyumsuz bir yöntemdir.</span><span class="sxs-lookup"><span data-stu-id="c4899-290">In the following example, `DelayAsync` is an async method that has a return statement that returns an integer.</span></span> <span data-ttu-id="c4899-291">Zaman uyumsuz bir yöntem olduğundan, metot bildiriminin dönüş türü `Task<int>`olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="c4899-291">Because it is an async method, its method declaration must have a return type of `Task<int>`.</span></span> <span data-ttu-id="c4899-292">Dönüş türü olduğu `Task<int>`için, `DoSomethingAsync` içindeki `await` ifadesinin değerlendirmesi, aşağıdaki `int result = await delayTask` deyimde gösterildiği gibi bir tamsayı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c4899-292">Because the return type is `Task<int>`, the evaluation of the `await` expression in `DoSomethingAsync` produces an integer, as the following `int result = await delayTask` statement demonstrates.</span></span>

[!code-csharp[csSnippets.Methods#102](../../samples/snippets/csharp/concepts/methods/async1.cs#102)]

<span data-ttu-id="c4899-293">Zaman uyumsuz bir yöntem hiçbir [içinde](language-reference/keywords/in-parameter-modifier.md), [ref](language-reference/keywords/ref.md)veya [Out](language-reference/keywords/out-parameter-modifier.md) parametrelerini bildiremez, ancak bu parametrelere sahip yöntemleri çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-293">An async method can't declare any [in](language-reference/keywords/in-parameter-modifier.md), [ref](language-reference/keywords/ref.md), or [out](language-reference/keywords/out-parameter-modifier.md) parameters, but it can call methods that have such parameters.</span></span>

 <span data-ttu-id="c4899-294">Zaman uyumsuz yöntemler hakkında daha fazla bilgi için bkz. [Async ve await Ile zaman uyumsuz programlama](async.md), [zaman uyumsuz programlarda denetim akışı](programming-guide/concepts/async/control-flow-in-async-programs.md)ve [zaman uyumsuz dönüş türleri](programming-guide/concepts/async/async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="c4899-294">For more information about async methods, see [Asynchronous Programming with Async and Await](async.md), [Control Flow in Async Programs](programming-guide/concepts/async/control-flow-in-async-programs.md), and [Async Return Types](programming-guide/concepts/async/async-return-types.md).</span></span>

<a name="expr"></a>

## <a name="expression-bodied-members"></a><span data-ttu-id="c4899-295">İfade gövdeli üyeler</span><span class="sxs-lookup"><span data-stu-id="c4899-295">Expression-bodied members</span></span>

<span data-ttu-id="c4899-296">Yalnızca bir ifadenin sonucuyla hemen dönen veya yöntemin gövdesi olarak tek bir deyimi olan yöntem tanımlarının olması yaygındır.</span><span class="sxs-lookup"><span data-stu-id="c4899-296">It is common to have method definitions that simply return immediately with the result of an expression, or that have a single statement as the body of the method.</span></span>  <span data-ttu-id="c4899-297">Kullanarak `=>`bu tür yöntemleri tanımlamaya yönelik bir sözdizimi kısayolu vardır:</span><span class="sxs-lookup"><span data-stu-id="c4899-297">There is a syntax shortcut for defining such methods using `=>`:</span></span>

```csharp
public Point Move(int dx, int dy) => new Point(x + dx, y + dy);
public void Print() => Console.WriteLine(First + " " + Last);
// Works with operators, properties, and indexers too.
public static Complex operator +(Complex a, Complex b) => a.Add(b);
public string Name => First + " " + Last;
public Customer this[long id] => store.LookupCustomer(id);
```

<span data-ttu-id="c4899-298">Yöntemi bir zaman uyumsuz `void` yöntem döndürürse veya ise, yönteminin gövdesi bir deyim ifadesi olmalıdır (Lambdalar ile aynı).</span><span class="sxs-lookup"><span data-stu-id="c4899-298">If the method returns `void` or is an async method, the body of the method must be a statement expression (same as with lambdas).</span></span>  <span data-ttu-id="c4899-299">Özellikler ve Dizin oluşturucular için, salt okunurdur ve `get` erişimci anahtar sözcüğünü kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="c4899-299">For properties and indexers, they must be read-only, and you do not use the `get` accessor keyword.</span></span>

<a name="iterators"></a>

## <a name="iterators"></a><span data-ttu-id="c4899-300">Yineleyiciler</span><span class="sxs-lookup"><span data-stu-id="c4899-300">Iterators</span></span>

<span data-ttu-id="c4899-301">Yineleyici, bir koleksiyon üzerinde liste veya dizi gibi özel bir yineleme gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="c4899-301">An iterator performs a custom iteration over a collection, such as a list or an array.</span></span> <span data-ttu-id="c4899-302">Bir yineleyici, her öğeyi birer birer döndürmek için [yield return](language-reference/keywords/yield.md) ifadesini kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-302">An iterator uses the [yield return](language-reference/keywords/yield.md) statement to return each element one at a time.</span></span> <span data-ttu-id="c4899-303">Bir `yield return` ifadeye ulaşıldığında, çağıranın sıradaki öğeyi istemesi için geçerli konum hatırlanır.</span><span class="sxs-lookup"><span data-stu-id="c4899-303">When a `yield return` statement is reached, the current location is remembered so that the caller can request the next element in the sequence.</span></span>

<span data-ttu-id="c4899-304">Yineleyicinin <xref:System.Collections.IEnumerable>dönüş türü <xref:System.Collections.Generic.IEnumerable%601> <xref:System.Collections.IEnumerator>,,, veya <xref:System.Collections.Generic.IEnumerator%601>olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4899-304">The return type of an iterator can be <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, <xref:System.Collections.IEnumerator>, or <xref:System.Collections.Generic.IEnumerator%601>.</span></span>

<span data-ttu-id="c4899-305">Daha fazla bilgi için bkz. [yineleyiciler](programming-guide/concepts/iterators.md).</span><span class="sxs-lookup"><span data-stu-id="c4899-305">For more information, see [Iterators](programming-guide/concepts/iterators.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="c4899-306">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="c4899-306">See also</span></span>

- [<span data-ttu-id="c4899-307">Erişim Değiştiricileri</span><span class="sxs-lookup"><span data-stu-id="c4899-307">Access Modifiers</span></span>](language-reference/keywords/access-modifiers.md)
- [<span data-ttu-id="c4899-308">Statik Sınıflar ve Statik Sınıf Üyeleri</span><span class="sxs-lookup"><span data-stu-id="c4899-308">Static Classes and Static Class Members</span></span>](programming-guide/classes-and-structs/static-classes-and-static-class-members.md)
- [<span data-ttu-id="c4899-309">Devralma</span><span class="sxs-lookup"><span data-stu-id="c4899-309">Inheritance</span></span>](programming-guide/classes-and-structs/inheritance.md)
- [<span data-ttu-id="c4899-310">Soyut ve Korumalı Sınıflar ve Sınıf Üyeleri</span><span class="sxs-lookup"><span data-stu-id="c4899-310">Abstract and Sealed Classes and Class Members</span></span>](programming-guide/classes-and-structs/abstract-and-sealed-classes-and-class-members.md)
- [<span data-ttu-id="c4899-311">params</span><span class="sxs-lookup"><span data-stu-id="c4899-311">params</span></span>](language-reference/keywords/params.md)
- [<span data-ttu-id="c4899-312">out</span><span class="sxs-lookup"><span data-stu-id="c4899-312">out</span></span>](language-reference/keywords/out-parameter-modifier.md)
- [<span data-ttu-id="c4899-313">ref</span><span class="sxs-lookup"><span data-stu-id="c4899-313">ref</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="c4899-314">in</span><span class="sxs-lookup"><span data-stu-id="c4899-314">in</span></span>](language-reference/keywords/in-parameter-modifier.md)
- [<span data-ttu-id="c4899-315">Parametreleri Geçirme</span><span class="sxs-lookup"><span data-stu-id="c4899-315">Passing Parameters</span></span>](programming-guide/classes-and-structs/passing-parameters.md)
