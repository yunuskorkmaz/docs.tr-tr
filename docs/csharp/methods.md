---
title: Yöntemler - C# Kılavuzu
description: Yöntemlere, yöntem parametrelerine ve yöntem iade değerlerine genel bakış
ms.technology: csharp-fundamentals
ms.date: 05/21/2018
ms.assetid: 577a8527-1081-4b36-9b9e-0685b6553c6e
ms.openlocfilehash: f44c83408e884d76eef5e2b5abbca511fbae2a1f
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399450"
---
# <a name="methods"></a><span data-ttu-id="834e7-103">Yöntemler</span><span class="sxs-lookup"><span data-stu-id="834e7-103">Methods</span></span>

<span data-ttu-id="834e7-104">Yöntem, bir dizi deyim içeren bir kod bloğudur.</span><span class="sxs-lookup"><span data-stu-id="834e7-104">A method is a code block that contains a series of statements.</span></span> <span data-ttu-id="834e7-105">Program, yöntem çağırArak ve gerekli yöntem bağımsız değişkenlerini belirterek deyimlerin yürütülmesine neden olur.</span><span class="sxs-lookup"><span data-stu-id="834e7-105">A program causes the statements to be executed by calling the method and specifying any required method arguments.</span></span> <span data-ttu-id="834e7-106">C#'da, çalıştırılan her yönerge bir yöntem bağlamında gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-106">In C#, every executed instruction is performed in the context of a method.</span></span> <span data-ttu-id="834e7-107">Yöntem `Main` her C# uygulaması için giriş noktasıdır ve program başlatıldığında ortak dil çalışma süresi (CLR) olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-107">The `Main` method is the entry point for every C# application and it is called by the common language runtime (CLR) when the program is started.</span></span>

> [!NOTE]
> <span data-ttu-id="834e7-108">Bu konu adlı yöntemleri tartışır.</span><span class="sxs-lookup"><span data-stu-id="834e7-108">This topic discusses named methods.</span></span> <span data-ttu-id="834e7-109">Anonim işlevler hakkında bilgi için [Bkz. Anonim Fonksiyonlar.](programming-guide/statements-expressions-operators/anonymous-functions.md)</span><span class="sxs-lookup"><span data-stu-id="834e7-109">For information about anonymous functions, see [Anonymous Functions](programming-guide/statements-expressions-operators/anonymous-functions.md).</span></span>

<a name="signatures"></a>

## <a name="method-signatures"></a><span data-ttu-id="834e7-110">Yöntem imzaları</span><span class="sxs-lookup"><span data-stu-id="834e7-110">Method signatures</span></span>

<span data-ttu-id="834e7-111">Yöntemler bir `class` veya `struct` belirterek bildirilir:</span><span class="sxs-lookup"><span data-stu-id="834e7-111">Methods are declared in a `class` or `struct` by specifying:</span></span>

- <span data-ttu-id="834e7-112">İsteğe bağlı bir `public` erişim `private`düzeyi, örneğin.</span><span class="sxs-lookup"><span data-stu-id="834e7-112">An optional access level, such as `public` or `private`.</span></span> <span data-ttu-id="834e7-113">Varsayılan değer: `private`.</span><span class="sxs-lookup"><span data-stu-id="834e7-113">The default is `private`.</span></span>
- <span data-ttu-id="834e7-114">Gibi isteğe bağlı `abstract` `sealed`değiştiriciler veya .</span><span class="sxs-lookup"><span data-stu-id="834e7-114">Optional modifiers such as `abstract` or `sealed`.</span></span>
- <span data-ttu-id="834e7-115">İade değeri veya `void` yöntemde yok.</span><span class="sxs-lookup"><span data-stu-id="834e7-115">The return value, or `void` if the method has none.</span></span>
- <span data-ttu-id="834e7-116">Yöntem adı.</span><span class="sxs-lookup"><span data-stu-id="834e7-116">The method name.</span></span>
- <span data-ttu-id="834e7-117">Herhangi bir yöntem parametreleri.</span><span class="sxs-lookup"><span data-stu-id="834e7-117">Any method parameters.</span></span> <span data-ttu-id="834e7-118">Yöntem parametreleri parantez içinde eklenir ve virgülle ayrılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-118">Method parameters are enclosed in parentheses and are separated by commas.</span></span> <span data-ttu-id="834e7-119">Boş parantezler yöntemin parametre gerektirmediğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="834e7-119">Empty parentheses indicate that the method requires no parameters.</span></span>

<span data-ttu-id="834e7-120">Bu parçalar birlikte yöntem imzasını oluşturur.</span><span class="sxs-lookup"><span data-stu-id="834e7-120">These parts together form the method signature.</span></span>

> [!NOTE]
> <span data-ttu-id="834e7-121">Yöntemin dönüş türü, yöntem aşırı yükleme amacıyla yöntemin imzasının bir parçası değildir.</span><span class="sxs-lookup"><span data-stu-id="834e7-121">A return type of a method is not part of the signature of the method for the purposes of method overloading.</span></span> <span data-ttu-id="834e7-122">Ancak, bir temsilci ve işaret ettiği yöntem arasındaki uyumluluğu belirlerken yöntemin imzasının bir parçasıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-122">However, it is part of the signature of the method when determining the compatibility between a delegate and the method that it points to.</span></span>

<span data-ttu-id="834e7-123">Aşağıdaki örnekte beş yöntem `Motorcycle` içeren adlandırılmış bir sınıf tanımlanır:</span><span class="sxs-lookup"><span data-stu-id="834e7-123">The following example defines a class named `Motorcycle` that contains five methods:</span></span>

[!code-csharp[csSnippets.Methods#40](../../samples/snippets/csharp/concepts/methods/methods40.cs#40)]

<span data-ttu-id="834e7-124">`Motorcycle` Sınıfın aşırı yüklü bir yöntem `Drive`içerdiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="834e7-124">Note that the `Motorcycle` class includes an overloaded method, `Drive`.</span></span> <span data-ttu-id="834e7-125">İki yöntem aynı ada sahiptir, ancak parametre türlerine göre ayırt edilmelidir.</span><span class="sxs-lookup"><span data-stu-id="834e7-125">Two methods have the same name, but must be differentiated by their parameter types.</span></span>

<a name="invocation"></a>

## <a name="method-invocation"></a><span data-ttu-id="834e7-126">Yöntem çağırma</span><span class="sxs-lookup"><span data-stu-id="834e7-126">Method invocation</span></span>

<span data-ttu-id="834e7-127">Yöntemler *örnek* veya *statik*olabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-127">Methods can be either *instance* or *static*.</span></span> <span data-ttu-id="834e7-128">Örnek yöntem çağırmak, bir nesneyi anında alamalısınız ve bu nesneüzerindeki yöntemi çağırın; bir örnek yöntemi o örnek ve verileri üzerinde çalışır.</span><span class="sxs-lookup"><span data-stu-id="834e7-128">Invoking an instance method requires that you instantiate an object and call the method on that object; an instance method operates on that instance and its data.</span></span> <span data-ttu-id="834e7-129">Yöntemin ait olduğu türün adını başvurarak statik bir yöntem çağırırsınız; statik yöntemler örnek veriler üzerinde çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="834e7-129">You invoke a static method by referencing the name of the type to which the method belongs; static methods do not operate on instance data.</span></span> <span data-ttu-id="834e7-130">Bir nesne örneği aracılığıyla statik bir yöntem çağırmaya çalışmak derleyici hatası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="834e7-130">Attempting to call a static method through an object instance generates a compiler error.</span></span>

<span data-ttu-id="834e7-131">Bir yöntemi çağırmak, bir alana erişmek gibidir.</span><span class="sxs-lookup"><span data-stu-id="834e7-131">Calling a method is like accessing a field.</span></span> <span data-ttu-id="834e7-132">Nesne adı (örnek yöntemi ni çağırıyorsanız) veya tür adı (bir `static` yöntem çağırıyorsanız), bir nokta, yöntemin adını ekleyin ve parantez edin.</span><span class="sxs-lookup"><span data-stu-id="834e7-132">After the object name (if you are calling an instance method) or the type name (if you are calling a `static` method), add a period, the name of the method, and parentheses.</span></span> <span data-ttu-id="834e7-133">Bağımsız değişkenler parantez içinde listelenir ve virgülle ayrılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-133">Arguments are listed within the parentheses, and are separated by commas.</span></span>

<span data-ttu-id="834e7-134">Yöntem tanımı, gerekli parametrelerin adlarını ve türlerini belirtir.</span><span class="sxs-lookup"><span data-stu-id="834e7-134">The method definition specifies the names and types of any parameters that are required.</span></span> <span data-ttu-id="834e7-135">Bir arayan yöntemi çağırdığında, her parametre için bağımsız değişken adı verilen somut değerler sağlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-135">When a caller invokes the method, it provides concrete values, called arguments, for each parameter.</span></span> <span data-ttu-id="834e7-136">Bağımsız değişkenler parametre türüyle uyumlu olmalıdır, ancak çağrı kodunda kullanılan bağımsız değişken adı, yöntemde tanımlanan parametreyle aynı olması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="834e7-136">The arguments must be compatible with the parameter type, but the argument name, if one is used in the calling code, does not have to be the same as the parameter named defined in the method.</span></span> <span data-ttu-id="834e7-137">Aşağıdaki örnekte, `Square` yöntem `int` *i*adlı tür tek bir parametre içerir.</span><span class="sxs-lookup"><span data-stu-id="834e7-137">In the following example, the `Square` method includes a single parameter of type `int` named *i*.</span></span> <span data-ttu-id="834e7-138">İlk yöntem çağrısı, `Square` *num*adlı `int` tip değişkeninyöntemini geçer; ikincisi, sayısal bir sabit; ve üçüncüsü, bir ifade.</span><span class="sxs-lookup"><span data-stu-id="834e7-138">The first method call passes the `Square` method a variable of type `int` named *num*; the second, a numeric constant; and the third, an expression.</span></span>

[!code-csharp[csSnippets.Methods#74](../../samples/snippets/csharp/concepts/methods/params74.cs#74)]

<span data-ttu-id="834e7-139">Yöntem çağırma en yaygın formu konumsal argümanlar kullanılır; bağımsız değişkenleri yöntem parametreleri ile aynı sırada sağlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-139">The most common form of method invocation used positional arguments; it supplies arguments in the same order as method parameters.</span></span> <span data-ttu-id="834e7-140">Bu nedenle `Motorcycle` sınıfın yöntemleri aşağıdaki örnekte olduğu gibi çağrılabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-140">The methods of the `Motorcycle` class can therefore be called as in the following example.</span></span> <span data-ttu-id="834e7-141">`Drive` Yönteme çağrı, örneğin, yöntemin sözdiziminde iki parametreye karşılık gelen iki bağımsız değişken içerir.</span><span class="sxs-lookup"><span data-stu-id="834e7-141">The call to the `Drive` method, for example, includes two arguments that correspond to the two parameters in the method's syntax.</span></span> <span data-ttu-id="834e7-142">İlk `miles` parametre değeri olur, ikinci `speed` parametre değeri.</span><span class="sxs-lookup"><span data-stu-id="834e7-142">The first becomes the value of the `miles` parameter, the second the value of the `speed` parameter.</span></span>

[!code-csharp[csSnippets.Methods#41](../../samples/snippets/csharp/concepts/methods/methods40.cs#41)]

<span data-ttu-id="834e7-143">Bir yöntem çağırırken konumsal bağımsız değişkenler yerine *adlandırılmış bağımsız değişkenler* de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-143">You can also used *named arguments* instead of positional arguments when invoking a method.</span></span> <span data-ttu-id="834e7-144">Adlandırılmış bağımsız değişkenleri kullanırken, bir üst nokta üst üste (":") ve bağımsız değişkenin ardından parametre adını belirtirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-144">When using named arguments, you specify the parameter name followed by a colon (":") and the argument.</span></span> <span data-ttu-id="834e7-145">Gerekli tüm bağımsız değişkenler mevcut olduğu sürece, yöntemin bağımsız değişkenleri herhangi bir sırada görünebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-145">Arguments to the method can appear in any order, as long as all required arguments are present.</span></span> <span data-ttu-id="834e7-146">Aşağıdaki örnek, yöntemi çağırmak `TestMotorcycle.Drive` için adlandırılmış bağımsız değişkenleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-146">The following example uses named arguments to invoke the `TestMotorcycle.Drive` method.</span></span> <span data-ttu-id="834e7-147">Bu örnekte, adlandırılmış bağımsız değişkenler yöntemin parametre listesinden ters sırada geçirilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-147">In this example, the named arguments are passed in the opposite order from the method's parameter list.</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/named1.cs#45)]

<span data-ttu-id="834e7-148">Hem konumsal bağımsız değişkenleri hem de adlandırılmış bağımsız değişkenleri kullanarak bir yöntem çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-148">You can invoke a method using both positional arguments and named arguments.</span></span> <span data-ttu-id="834e7-149">Ancak, konumsal bir bağımsız değişken adlandırılmış bir bağımsız değişkeni izleyemez.</span><span class="sxs-lookup"><span data-stu-id="834e7-149">However, a positional argument cannot follow a named argument.</span></span> <span data-ttu-id="834e7-150">Aşağıdaki örnek, bir `TestMotorcycle.Drive` konumsal bağımsız değişken ve bir adlandırılmış bağımsız değişken kullanarak önceki örnekten yöntemi çağırır.</span><span class="sxs-lookup"><span data-stu-id="834e7-150">The following example invokes the `TestMotorcycle.Drive` method from the previous example using one positional argument and one named argument.</span></span>

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/named2.cs#46)]

<a name="inherited"></a>

## <a name="inherited-and-overridden-methods"></a><span data-ttu-id="834e7-151">Kalıtsal ve geçersiz kılınan yöntemler</span><span class="sxs-lookup"><span data-stu-id="834e7-151">Inherited and overridden methods</span></span>

<span data-ttu-id="834e7-152">Bir türde açıkça tanımlanan üyelere ek olarak, bir tür, temel sınıflarında tanımlanan üyeleri devralır.</span><span class="sxs-lookup"><span data-stu-id="834e7-152">In addition to the members that are explicitly defined in a type, a type inherits members defined in its base classes.</span></span> <span data-ttu-id="834e7-153">Yönetilen <xref:System.Object> tür sistemindeki tüm türler sınıftan doğrudan veya dolaylı olarak devraldığı <xref:System.Object.Equals(System.Object)>için, tüm türler , , <xref:System.Object.GetType>ve <xref:System.Object.ToString>.</span><span class="sxs-lookup"><span data-stu-id="834e7-153">Since all types in the managed type system inherit directly or indirectly from the <xref:System.Object> class, all types inherit its members, such as <xref:System.Object.Equals(System.Object)>, <xref:System.Object.GetType>, and <xref:System.Object.ToString>.</span></span> <span data-ttu-id="834e7-154">Aşağıdaki örnek, bir `Person` sınıf tanımlar, iki `Person` nesneyi anlık `Person.Equals` olarak çağırır ve iki nesnenin eşit olup olmadığını belirlemek için yöntemi çağırır.</span><span class="sxs-lookup"><span data-stu-id="834e7-154">The following example defines a `Person` class, instantiates two `Person` objects, and calls the `Person.Equals` method to determine whether the two objects are equal.</span></span> <span data-ttu-id="834e7-155">Ancak `Equals` yöntem sınıfta tanımlanmaz; `Person` bu miras. <xref:System.Object></span><span class="sxs-lookup"><span data-stu-id="834e7-155">The `Equals` method, however, is not defined in the `Person` class; it is inherited from <xref:System.Object>.</span></span>

[!code-csharp[csSnippets.Methods#104](../../samples/snippets/csharp/concepts/methods/inherited1.cs#104)]

<span data-ttu-id="834e7-156">`override` Türler, anahtar sözcüğü kullanarak ve geçersiz kılınan yöntem için bir uygulama sağlayarak devralınan üyeleri geçersiz kılabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-156">Types can override inherited members by using the `override` keyword and providing an implementation for the overridden method.</span></span> <span data-ttu-id="834e7-157">Yöntem imzası, geçersiz kılınan yöntemle aynı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-157">The method signature must be the same as that of the overridden method.</span></span> <span data-ttu-id="834e7-158">Aşağıdaki örnek, <xref:System.Object.Equals(System.Object)> yöntemi geçersiz kılmadışında, önceki örneği gibidir.</span><span class="sxs-lookup"><span data-stu-id="834e7-158">The following example is like the previous one, except that it overrides the <xref:System.Object.Equals(System.Object)> method.</span></span> <span data-ttu-id="834e7-159">(Ayrıca, iki <xref:System.Object.GetHashCode> yöntem tutarlı sonuçlar sağlamak için tasarlanmıştır beri yöntemi geçersiz kılar.)</span><span class="sxs-lookup"><span data-stu-id="834e7-159">(It also overrides the <xref:System.Object.GetHashCode> method, since the two methods are intended to provide consistent results.)</span></span>

[!code-csharp[csSnippets.Methods#105](../../samples/snippets/csharp/concepts/methods/overridden1.cs#105)]

<a name="passing"></a>

## <a name="passing-parameters"></a><span data-ttu-id="834e7-160">Parametreleri geçirme</span><span class="sxs-lookup"><span data-stu-id="834e7-160">Passing parameters</span></span>

<span data-ttu-id="834e7-161">C# türü *değer türleri* veya *başvuru türleridir.*</span><span class="sxs-lookup"><span data-stu-id="834e7-161">Types in C# are either *value types* or *reference types*.</span></span> <span data-ttu-id="834e7-162">Yerleşik değer türlerinin listesi [için, Türleri ve değişkenleri](./tour-of-csharp/types-and-variables.md)görün.</span><span class="sxs-lookup"><span data-stu-id="834e7-162">For a list of built-in value types, see [Types and variables](./tour-of-csharp/types-and-variables.md).</span></span> <span data-ttu-id="834e7-163">Varsayılan olarak, hem değer türleri hem de başvuru türleri değere göre bir yönteme aktarılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-163">By default, both value types and reference types are passed to a method by value.</span></span>

<a name="byval"></a>

### <a name="passing-parameters-by-value"></a><span data-ttu-id="834e7-164">Parametreleri değere göre geçirme</span><span class="sxs-lookup"><span data-stu-id="834e7-164">Passing parameters by value</span></span>

<span data-ttu-id="834e7-165">Değer olarak bir yönteme bir değer aktarıldığında, nesnenin kendisi yerine nesnenin bir kopyası yönteme geçirilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-165">When a value type is passed to a method by value, a copy of the object instead of the object itself is passed to the method.</span></span> <span data-ttu-id="834e7-166">Bu nedenle, çağrılan yöntemde nesnedeğişiklikleri, denetim arayana döndüğünde özgün nesne üzerinde hiçbir etkisi yoktur.</span><span class="sxs-lookup"><span data-stu-id="834e7-166">Therefore, changes to the object in the called method have no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="834e7-167">Aşağıdaki örnek, değere göre bir yönteme bir değer türü geçer ve çağrılan yöntem değer türü değerini değiştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="834e7-167">The following example passes a value type to a method by value, and the called method attempts to change the value type's value.</span></span> <span data-ttu-id="834e7-168">Değer türü olan bir `int`tür değişkenini tanımlar, değerini 20'ye başlar ve değişkenin değerini 30'a değiştiren bir `ModifyValue` yönteme geçirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-168">It defines a variable of type `int`, which is a value type, initializes its value to 20, and passes it to a method named `ModifyValue` that changes the variable's value to 30.</span></span> <span data-ttu-id="834e7-169">Ancak yöntem döndüğünde değişkenin değeri değişmeden kalır.</span><span class="sxs-lookup"><span data-stu-id="834e7-169">When the method returns, however, the variable's value remains unchanged.</span></span>

[!code-csharp[csSnippets.Methods#10](../../samples/snippets/csharp/concepts/methods/byvalue10.cs#10)]

<span data-ttu-id="834e7-170">Başvuru türündeki bir nesne değer ekiile yönteme geçirildiğinde, nesneye yapılan bir başvuru değere göre geçirilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-170">When an object of a reference type is passed to a method by value, a reference to the object is passed by value.</span></span> <span data-ttu-id="834e7-171">Diğer bir zamanda, yöntem nesnenin kendisini değil, nesnenin konumunu gösteren bir bağımsız değişkeni alır.</span><span class="sxs-lookup"><span data-stu-id="834e7-171">That is, the method receives not the object itself, but an argument that indicates the location of the object.</span></span> <span data-ttu-id="834e7-172">Bu başvuruyu kullanarak nesnenin bir üyesini değiştirirseniz, denetim arama yöntemine döndüğünde değişiklik nesneye yansıtılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-172">If you change a member of the object by using this reference, the change is reflected in the object when control returns to the calling method.</span></span> <span data-ttu-id="834e7-173">Ancak, yönteme geçirilen nesnenin değiştirilmesi, denetim arayana döndüğünde özgün nesne üzerinde hiçbir etkisi yoktur.</span><span class="sxs-lookup"><span data-stu-id="834e7-173">However, replacing the object passed to the method has no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="834e7-174">Aşağıdaki örnekte bir sınıf (bir başvuru türü) adlı `SampleRefType`tanımlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-174">The following example defines a class (which is a reference type) named `SampleRefType`.</span></span> <span data-ttu-id="834e7-175">Bir `SampleRefType` nesneyi anında atar, `value` alanına 44 atar ve nesneyi `ModifyObject` yönteme geçirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-175">It instantiates a `SampleRefType` object, assigns 44 to its `value` field, and passes the object to the `ModifyObject` method.</span></span> <span data-ttu-id="834e7-176">Bu örnek, temelde önceki örnekle aynı şeyi yapar -- bir bağımsız değişkeni bir yönteme değer olarak geçirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-176">This example does essentially the same thing as the previous example -- it passes an argument by value to a method.</span></span> <span data-ttu-id="834e7-177">Ancak bir başvuru türü kullanıldığından, sonuç farklıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-177">But because a reference type is used, the result is different.</span></span> <span data-ttu-id="834e7-178">`obj.value` Alana yapılan `ModifyObject` değişiklik, örnekteki çıktının `value` gösterdiği gibi, `rt` `Main` yöntemdeki bağımsız değişkenalanını da 33'e değiştirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-178">The modification that is made in `ModifyObject` to the `obj.value` field also changes the `value` field of the argument, `rt`, in the `Main` method to 33, as the output from the example shows.</span></span>

[!code-csharp[csSnippets.Methods#42](../../samples/snippets/csharp/concepts/methods/byvalue42.cs#42)]

<a name="byref"></a>

### <a name="passing-parameters-by-reference"></a><span data-ttu-id="834e7-179">Parametreleri referansa göre geçirme</span><span class="sxs-lookup"><span data-stu-id="834e7-179">Passing parameters by reference</span></span>

<span data-ttu-id="834e7-180">Bir yöntemdeki bir bağımsız değişkenin değerini değiştirmek istediğinizde ve denetim arama yöntemine döndüğünde bu değişikliği yansıtmak istediğinizde, bir parametreyi başvuru yla geçirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-180">You pass a parameter by reference when you want to change the value of an argument in a method and want to reflect that change when control returns to the calling method.</span></span> <span data-ttu-id="834e7-181">Bir parametreyi referansla geçirmek [`ref`](language-reference/keywords/ref.md) için, anahtar kelimeyi veya [`out`](language-reference/keywords/out-parameter-modifier.md) anahtar sözcüğü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="834e7-181">To pass a parameter by reference, you use the [`ref`](language-reference/keywords/ref.md) or [`out`](language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="834e7-182">Ayrıca kopyalamayı önlemek için referans olarak bir değer geçirebilir, [`in`](language-reference/keywords/in-parameter-modifier.md) ancak anahtar kelimeyi kullanarak değişiklikleri yine de önleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-182">You can also pass a value by reference to avoid copying but still prevent modifications using the [`in`](language-reference/keywords/in-parameter-modifier.md) keyword.</span></span>

<span data-ttu-id="834e7-183">Aşağıdaki örnek, `ModifyValue` yönteme başvuruyla geçirilen değer dışında, öncekiyle aynıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-183">The following example is identical to the previous one, except the value is passed by reference to the `ModifyValue` method.</span></span> <span data-ttu-id="834e7-184">`ModifyValue` Yöntemde parametrenin değeri değiştirildiğinde, denetim arayana döndüğünde değer değişikliği yansıtılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-184">When the value of the parameter is modified in the `ModifyValue` method, the change in value is reflected when control returns to the caller.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/byref106.cs#106)]

<span data-ttu-id="834e7-185">Ref parametreleri tarafından kullanılan yaygın bir desen değişkenlerin değerlerini takas içerir.</span><span class="sxs-lookup"><span data-stu-id="834e7-185">A common pattern that uses by ref parameters involves swapping the values of variables.</span></span> <span data-ttu-id="834e7-186">İki değişkeni başvuru yla bir yönteme geçirirsiniz ve yöntem içeriğini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-186">You pass two variables to a method by reference, and the method swaps their contents.</span></span> <span data-ttu-id="834e7-187">Aşağıdaki örnek, sonsayı değerlerini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-187">The following example swaps integer values.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/swap107.cs#107)]

<span data-ttu-id="834e7-188">Başvuru türü parametresi geçmek, tek tek öğelerinin veya alanlarının değeri yerine başvurunun değerini değiştirmenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-188">Passing a reference-type parameter allows you to change the value of the reference itself, rather than the value of its individual elements or fields.</span></span>

<a name="paramarray"></a>

### <a name="parameter-arrays"></a><span data-ttu-id="834e7-189">Parametre dizileri</span><span class="sxs-lookup"><span data-stu-id="834e7-189">Parameter arrays</span></span>

<span data-ttu-id="834e7-190">Bazen, yönteminizin bağımsız değişkenlerinin tam sayısını belirtme gereksinimi kısıtlayıcıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-190">Sometimes, the requirement that you specify the exact number of arguments to your method is restrictive.</span></span> <span data-ttu-id="834e7-191">`params` Bir parametrenin bir parametre dizisi olduğunu belirtmek için anahtar sözcüğü kullanarak, yönteminizin değişken sayıda bağımsız değişkenle çağrılmasını sağlarsınız.</span><span class="sxs-lookup"><span data-stu-id="834e7-191">By using the `params` keyword to indicate that a parameter is a parameter array, you allow your method to be called with a variable number of arguments.</span></span> <span data-ttu-id="834e7-192">`params` Anahtar kelimeyle etiketlenen parametre bir dizi türü olmalı ve yöntemin parametre listesindeki son parametre olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-192">The parameter tagged with the `params` keyword must be an array type, and it must be the last parameter in the method's parameter list.</span></span>

<span data-ttu-id="834e7-193">Arayan daha sonra yöntemi üç şekilde çağırabilir:</span><span class="sxs-lookup"><span data-stu-id="834e7-193">A caller can then invoke the method in either of three ways:</span></span>

- <span data-ttu-id="834e7-194">İstenilen sayıda öğe içeren uygun türde bir dizi geçirerek.</span><span class="sxs-lookup"><span data-stu-id="834e7-194">By passing an array of the appropriate type that contains the desired number of elements.</span></span>
- <span data-ttu-id="834e7-195">Yönteme uygun türde bireysel bağımsız değişkenlerin virgülden ayrılmış bir listesini geçirerek.</span><span class="sxs-lookup"><span data-stu-id="834e7-195">By passing a comma-separated list of individual arguments of the appropriate type to the method.</span></span>
- <span data-ttu-id="834e7-196">Parametre dizilimi için bir bağımsız değişken sağlamayarak.</span><span class="sxs-lookup"><span data-stu-id="834e7-196">By not providing an argument to the parameter array.</span></span>

<span data-ttu-id="834e7-197">Aşağıdaki örnek, bir parametre dizisinden tüm sesli harfleri döndüren adlandırılmış `GetVowels` bir yöntem tanımlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-197">The following example defines a method named `GetVowels` that returns all the vowels from a parameter array.</span></span> <span data-ttu-id="834e7-198">Yöntem, `Main` yöntemi çağırmak için üç yolu da gösterir.</span><span class="sxs-lookup"><span data-stu-id="834e7-198">The `Main` method illustrates all three ways of invoking the method.</span></span> <span data-ttu-id="834e7-199">Arayanlar, `params` değiştiriciiçeren parametreler için herhangi bir bağımsız değişken sağlamaları gerekmez.</span><span class="sxs-lookup"><span data-stu-id="834e7-199">Callers are not required to supply any arguments for parameters that include the `params` modifier.</span></span> <span data-ttu-id="834e7-200">Bu durumda, parametre `null`.</span><span class="sxs-lookup"><span data-stu-id="834e7-200">In that case, the parameter is `null`.</span></span>

[!code-csharp[csSnippets.Methods#75](~/samples/snippets/csharp/concepts/methods/params75.cs#75)]

<a name="optional"></a>

## <a name="optional-parameters-and-arguments"></a><span data-ttu-id="834e7-201">İsteğe bağlı parametreler ve bağımsız değişkenler</span><span class="sxs-lookup"><span data-stu-id="834e7-201">Optional parameters and arguments</span></span>

<span data-ttu-id="834e7-202">Yöntem tanımı, parametrelerinin gerekli olduğunu veya isteğe bağlı olduğunu belirtebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-202">A method definition can specify that its parameters are required or that they are optional.</span></span> <span data-ttu-id="834e7-203">Varsayılan olarak, parametreler gereklidir.</span><span class="sxs-lookup"><span data-stu-id="834e7-203">By default, parameters are required.</span></span> <span data-ttu-id="834e7-204">İsteğe bağlı parametreler, yöntem tanımına parametrenin varsayılan değeri ekleyerek belirtilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-204">Optional parameters are specified by including the parameter's default value in the method definition.</span></span> <span data-ttu-id="834e7-205">Yöntem çağrıldığında, isteğe bağlı bir parametre için bağımsız değişken sağlanmazsa, varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-205">When the method is called, if no argument is supplied for an optional parameter, the default value is used instead.</span></span>

<span data-ttu-id="834e7-206">Parametrenin varsayılan değeri aşağıdaki ifade türlerinden biri tarafından atanmalıdır:</span><span class="sxs-lookup"><span data-stu-id="834e7-206">The parameter's default value must be assigned by one of the following kinds of expressions:</span></span>

- <span data-ttu-id="834e7-207">Bir sabit, bir edebi dize veya sayı gibi.</span><span class="sxs-lookup"><span data-stu-id="834e7-207">A constant, such as a literal string or number.</span></span>
- <span data-ttu-id="834e7-208">Formun `new ValType()`bir ifadesi `ValType` , değer türü nerede.</span><span class="sxs-lookup"><span data-stu-id="834e7-208">An expression of the form `new ValType()`, where `ValType` is a value type.</span></span> <span data-ttu-id="834e7-209">Bunun, değer türünün gerçek bir üyesi olmayan örtük parametresiz oluşturucusu çağırır.</span><span class="sxs-lookup"><span data-stu-id="834e7-209">Note that this invokes the value type's implicit parameterless constructor, which is not an actual member of the type.</span></span>
- <span data-ttu-id="834e7-210">Formun `default(ValType)`bir ifadesi `ValType` , değer türü nerede.</span><span class="sxs-lookup"><span data-stu-id="834e7-210">An expression of the form `default(ValType)`, where `ValType` is a value type.</span></span>

<span data-ttu-id="834e7-211">Bir yöntem hem gerekli hem de isteğe bağlı parametreler içeriyorsa, gerekli tüm parametrelerden sonra parametre listesinin sonunda isteğe bağlı parametreler tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-211">If a method includes both required and optional parameters, optional parameters are defined at the end of the parameter list, after all required parameters.</span></span>

<span data-ttu-id="834e7-212">Aşağıdaki örnekte, `ExampleMethod`bir gerekli ve iki isteğe bağlı parametre içeren bir yöntem tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-212">The following example defines a method, `ExampleMethod`, that has one required and two optional parameters.</span></span>

[!code-csharp[csSnippets.Methods#21](../../samples/snippets/csharp/concepts/methods/optional1.cs#21)]

<span data-ttu-id="834e7-213">Birden çok isteğe bağlı bağımsız değişkeniçeren bir yöntem konumsal bağımsız değişkenler kullanılarak çağrılırsa, arayan ilkinden bağımsız değişkenin sağlandığı son yönteme kadar tüm isteğe bağlı parametreler için bir bağımsız değişken sağlamalıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-213">If a method with multiple optional arguments is invoked using positional arguments, the caller must supply an argument for all optional parameters from the first one to the last one for which an argument is supplied.</span></span> <span data-ttu-id="834e7-214">`ExampleMethod` Yöntem söz konusu olduğunda, örneğin, arayan `description` parametre için bir bağımsız değişken sağlıyorsa, `optionalInt` parametre için de bir bağımsız değişken sağlaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="834e7-214">In the case of the  `ExampleMethod` method, for example, if the caller supplies an argument for the `description` parameter, it must also supply one for the `optionalInt` parameter.</span></span> <span data-ttu-id="834e7-215">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");`geçerli bir yöntem çağrısıdır; `opt.ExampleMethod(2, , "Addition of 2 and 0");` bir "Bağımsız değişken eksik" derleyici hatası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="834e7-215">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");` is a valid method call; `opt.ExampleMethod(2, , "Addition of 2 and 0");` generates an "Argument missing" compiler error.</span></span>

<span data-ttu-id="834e7-216">Adlandırılmış bağımsız değişkenler veya konumsal ve adlandırılmış bağımsız değişkenlerin birleşimi kullanılarak bir yöntem çağrılırsa, arayan yöntem çağrısındaki son konumsal bağımsız değişkeni izleyen tüm bağımsız değişkenleri atlayabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-216">If a method is called using named arguments or a combination of positional and named arguments, the caller can omit any arguments that follow the last positional argument in the method call.</span></span>

<span data-ttu-id="834e7-217">Aşağıdaki örnek, `ExampleMethod` yöntemi üç kez çağırır.</span><span class="sxs-lookup"><span data-stu-id="834e7-217">The following example calls the `ExampleMethod` method three times.</span></span>  <span data-ttu-id="834e7-218">İlk iki yöntem konumsal bağımsız değişkenleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-218">The first two method calls use positional arguments.</span></span> <span data-ttu-id="834e7-219">İlki her iki isteğe bağlı bağımsız değişkeni atlarken, ikinci bağımsız değişken son bağımsız değişkeni atlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-219">The first omits both optional arguments, while the second omits the last argument.</span></span> <span data-ttu-id="834e7-220">Üçüncü yöntem çağrı, gerekli parametre için konumsal bir bağımsız değişken sağlar, `description` ancak `optionalInt` bağımsız değişkeni atlarken parametreye değer sağlamak için adlandırılmış bir bağımsız değişken kullanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-220">The third method call supplies a positional argument for the required parameter, but uses a named argument to supply a value to the `description` parameter while omitting the `optionalInt` argument.</span></span>

[!code-csharp[csSnippets.Methods#22](../../samples/snippets/csharp/concepts/methods/optional1.cs#22)]

<span data-ttu-id="834e7-221">İsteğe bağlı parametrelerin kullanımı *aşırı yük çözünürlüğünü*veya C# derleyicisinin bir yöntem çağrısı yla hangi aşırı yükün çağrılması gerektiğini belirleme biçimini aşağıdaki gibi etkiler:</span><span class="sxs-lookup"><span data-stu-id="834e7-221">The use of optional parameters affects *overload resolution*, or the way in which the C# compiler determines which particular overload should be invoked by a method call, as follows:</span></span>

- <span data-ttu-id="834e7-222">Parametrelerinin her biri isteğe bağlı ysa veya ada veya konuma göre, çağrı deyimindeki tek bir bağımsız değişkene karşılık gelen ve bu bağımsız değişken parametre türüne dönüştürülebiliyorsa, bir yöntem, dizin oluşturucu veya oluşturucu yürütme için adaydır.</span><span class="sxs-lookup"><span data-stu-id="834e7-222">A method, indexer, or constructor is a candidate for execution if each of its parameters either is optional or corresponds, by name or by position, to a single argument in the calling statement, and that argument can be converted to the type of the parameter.</span></span>
- <span data-ttu-id="834e7-223">Birden fazla aday bulunursa, tercih edilen dönüşümler için aşırı yükleme çözümleme kuralları açıkça belirtilen bağımsız değişkenlere uygulanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-223">If more than one candidate is found, overload resolution rules for preferred conversions are applied to the arguments that are explicitly specified.</span></span> <span data-ttu-id="834e7-224">İsteğe bağlı parametreler için atlanan bağımsız değişkenler yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-224">Omitted arguments for optional parameters are ignored.</span></span>
- <span data-ttu-id="834e7-225">İki adayın eşit derecede iyi olduğuna karar vereliise, tercih, çağrıda bağımsız değişkenlerin atlandığı isteğe bağlı parametrelere sahip olmayan bir adaya gider.</span><span class="sxs-lookup"><span data-stu-id="834e7-225">If two candidates are judged to be equally good, preference goes to a candidate that does not have optional parameters for which arguments were omitted in the call.</span></span> <span data-ttu-id="834e7-226">Bu, daha az parametreye sahip adaylar için aşırı yük çözümünde genel bir tercihin sonucudur.</span><span class="sxs-lookup"><span data-stu-id="834e7-226">This is a consequence of a general preference in overload resolution for candidates that have fewer parameters.</span></span>

<a name="return"></a>

## <a name="return-values"></a><span data-ttu-id="834e7-227">Döndürülen değerler</span><span class="sxs-lookup"><span data-stu-id="834e7-227">Return values</span></span>

<span data-ttu-id="834e7-228">Yöntemler arayana bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-228">Methods can return a value to the caller.</span></span> <span data-ttu-id="834e7-229">İade türü (yöntem adından önce listelenen tür) değilse, `void`yöntem `return` anahtar sözcüğü kullanarak değeri döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-229">If the return type (the type listed before the method name) is not `void`, the method can return the value by using the `return` keyword.</span></span> <span data-ttu-id="834e7-230">Anahtar kelimenin `return` ardından gelen değişken, sabit veya ifadeyle eşleşen bir ifade, bu değeri yöntem arayana döndürecektir.</span><span class="sxs-lookup"><span data-stu-id="834e7-230">A statement with the `return` keyword followed by a variable, constant, or expression that matches the return type will return that value to the method caller.</span></span> <span data-ttu-id="834e7-231">Geçersiz olmayan bir iade türüne sahip `return` yöntemlerin, bir değeri döndürmek için anahtar sözcüğü kullanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="834e7-231">Methods with a non-void return type are required to use the `return` keyword to return a value.</span></span> <span data-ttu-id="834e7-232">Anahtar `return` kelime de yöntemin yürütülmesini durdurur.</span><span class="sxs-lookup"><span data-stu-id="834e7-232">The `return` keyword also stops the execution of the method.</span></span>

<span data-ttu-id="834e7-233">İade türü ise, `void` `return` değeri olmayan bir deyim, yöntemin yürütülmesini durdurmak için yine de yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-233">If the return type is `void`, a `return` statement without a value is still useful to stop the execution of the method.</span></span> <span data-ttu-id="834e7-234">`return` Anahtar kelime olmadan, yöntem kod bloğunun sonuna ulaştığında yürütmeyi durdurur.</span><span class="sxs-lookup"><span data-stu-id="834e7-234">Without the `return` keyword, the method will stop executing when it reaches the end of the code block.</span></span>

<span data-ttu-id="834e7-235">Örneğin, bu iki yöntem, sonlu ları döndürmek için `return` anahtar sözcüğü kullanır:</span><span class="sxs-lookup"><span data-stu-id="834e7-235">For example, these two methods use the `return` keyword to return integers:</span></span>

[!code-csharp[csSnippets.Methods#44](../../samples/snippets/csharp/concepts/methods/return44.cs#44)]

<span data-ttu-id="834e7-236">Bir yöntemden döndürülen bir değeri kullanmak için, arama yöntemi nin kendisini her yerde aynı türde bir değerin yeterli olduğu her yerde çağırın yöntemini kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-236">To use a value returned from a method, the calling method can use the method call itself anywhere a value of the same type would be sufficient.</span></span> <span data-ttu-id="834e7-237">İade değerini bir değişkene de atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-237">You can also assign the return value to a variable.</span></span> <span data-ttu-id="834e7-238">Örneğin, aşağıdaki iki kod örneği aynı amacı gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="834e7-238">For example, the following two code examples accomplish the same goal:</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/return44.cs#45)]

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/return44.cs#46)]

<span data-ttu-id="834e7-239">Yerel bir değişkenkullanarak, bu `result`durumda, bir değeri depolamak için isteğe bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-239">Using a local variable, in this case, `result`, to store a value is optional.</span></span> <span data-ttu-id="834e7-240">Kodun okunabilirliğine yardımcı olabilir veya yöntemin tüm kapsamı için bağımsız değişkenin özgün değerini depolamanız gerekiyorsa gerekli olabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-240">It may help the readability of the code, or it may be necessary if you need to store the original value of the argument for the entire scope of the method.</span></span>

<span data-ttu-id="834e7-241">Bazen, yönteminizin tek bir değerden daha fazlasını döndürmesini istersiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-241">Sometimes, you want your method to return more than a single value.</span></span> <span data-ttu-id="834e7-242">C# 7.0 ile başlayarak, *tuple türleri* ve *tuple literals*kullanarak kolayca yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-242">Starting with C# 7.0, you can do this easily by using *tuple types* and *tuple literals*.</span></span> <span data-ttu-id="834e7-243">Tuple türü, tuple elemanlarının veri türlerini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-243">The tuple type defines the data types of the tuple's elements.</span></span> <span data-ttu-id="834e7-244">Tuple literals döndürülen tuple gerçek değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-244">Tuple literals provide the actual values of the returned tuple.</span></span> <span data-ttu-id="834e7-245">Aşağıdaki örnekte, `(string, string, string, int)` `GetPersonalInfo` yöntem tarafından döndürülen tuple türünü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="834e7-245">In the following example, `(string, string, string, int)` defines the tuple type that is returned by the `GetPersonalInfo` method.</span></span> <span data-ttu-id="834e7-246">İfade `(per.FirstName, per.MiddleName, per.LastName, per.Age)` tuple literal olduğunu; yöntem, bir `PersonInfo` nesnenin yaşıyla birlikte ilk, orta ve soyadını döndürür.</span><span class="sxs-lookup"><span data-stu-id="834e7-246">The expression `(per.FirstName, per.MiddleName, per.LastName, per.Age)` is the tuple literal; the method returns the first, middle, and last name, along with the age, of a `PersonInfo` object.</span></span>

```csharp
public (string, string, string, int) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    return (per.FirstName, per.MiddleName, per.LastName, per.Age);
}
```

<span data-ttu-id="834e7-247">Arayan daha sonra aşağıdaki gibi kod ile döndürülen tuple tüketebilir:</span><span class="sxs-lookup"><span data-stu-id="834e7-247">The caller can then consume the returned tuple with code like the following:</span></span>

```csharp
var person = GetPersonalInfo("111111111")
Console.WriteLine("{person.Item1} {person.Item3}: age = {person.Item4}");
```

<span data-ttu-id="834e7-248">Adlar, tuple türü tanımındaki tuple öğelerine de atanabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-248">Names can also be assigned to the tuple elements in the tuple type definition.</span></span> <span data-ttu-id="834e7-249">Aşağıdaki örnek, adı geçen `GetPersonalInfo` öğeleri kullanan yöntemin alternatif bir sürümünü gösterir:</span><span class="sxs-lookup"><span data-stu-id="834e7-249">The following example shows an alternate version of the `GetPersonalInfo` method that uses named elements:</span></span>

```csharp
public (string FName, string MName, string LName, int Age) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    return (per.FirstName, per.MiddleName, per.LastName, per.Age);
}
```

<span data-ttu-id="834e7-250">`GetPersonInfo` Yönteme önceki çağrı daha sonra aşağıdaki gibi değiştirilebilir:</span><span class="sxs-lookup"><span data-stu-id="834e7-250">The previous call to the `GetPersonInfo` method can then be modified as follows:</span></span>

```csharp
var person = GetPersonalInfo("111111111");
Console.WriteLine("{person.FName} {person.LName}: age = {person.Age}");
```

<span data-ttu-id="834e7-251">Bir yöntem bir dizi bağımsız değişken olarak geçirilirse ve tek tek öğelerin değerini değiştirirse, iyi stil veya işlevsel değerler akışı için bunu seçebilirsiniz rağmen, dizi döndürmek için yöntem için gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="834e7-251">If a method is passed an array as an argument and modifies the value of individual elements, it is not necessary for the method to return the array, although you may choose to do so for good style or functional flow of values.</span></span>  <span data-ttu-id="834e7-252">Bunun nedeni, C#'ın tüm başvuru türlerini değere göre geçirmesi ve bir dizi referansının değerinin diziişaretçisi olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="834e7-252">This is because C# passes all reference types by value, and the value of an array reference is the pointer to the array.</span></span> <span data-ttu-id="834e7-253">Aşağıdaki örnekte, `values` `DoubleValues` yöntemde yapılan dizinin içeriğindeki değişiklikler, diziye başvuru yapan herhangi bir kod tarafından gözlemlenebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-253">In the following example, changes to the contents of the `values` array that are made in the `DoubleValues` method are observable by any code that has a reference to the array.</span></span>

[!code-csharp[csSnippets.Methods#101](../../samples/snippets/csharp/concepts/methods/returnarray1.cs#101)]

<a name="extension"></a>

## <a name="extension-methods"></a><span data-ttu-id="834e7-254">Uzatma yöntemleri</span><span class="sxs-lookup"><span data-stu-id="834e7-254">Extension methods</span></span>

<span data-ttu-id="834e7-255">Normalde, varolan bir türe yöntem eklemenin iki yolu vardır:</span><span class="sxs-lookup"><span data-stu-id="834e7-255">Ordinarily, there are two ways to add a method to an existing type:</span></span>

- <span data-ttu-id="834e7-256">Bu tür için kaynak kodunu değiştirin.</span><span class="sxs-lookup"><span data-stu-id="834e7-256">Modify the source code for that type.</span></span> <span data-ttu-id="834e7-257">Türün kaynak koduna sahip değilseniz, elbette bunu yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="834e7-257">You cannot do this, of course, if you do not own the type's source code.</span></span> <span data-ttu-id="834e7-258">Ayrıca yöntemi desteklemek için herhangi bir özel veri alanları eklerseniz bu bir kırılma değişikliği olur.</span><span class="sxs-lookup"><span data-stu-id="834e7-258">And this becomes a breaking change if you also add any private data fields to support the method.</span></span>
- <span data-ttu-id="834e7-259">Türemiş bir sınıfta yeni yöntemi tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="834e7-259">Define the new method in a derived class.</span></span> <span data-ttu-id="834e7-260">Yapılar ve sayısallaştırmalar gibi diğer türler için kalıtım kullanılarak bu şekilde bir yöntem eklenemez.</span><span class="sxs-lookup"><span data-stu-id="834e7-260">A method cannot be added in this way using inheritance for other types, such as structures and enumerations.</span></span> <span data-ttu-id="834e7-261">Ne de mühürlü bir sınıfa bir yöntem "eklemek" için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-261">Nor can it be used to "add" a method to a sealed class.</span></span>

<span data-ttu-id="834e7-262">Uzantı yöntemleri, tür kendisini değiştirmeden veya devralınan bir türde yeni yöntemi uygulamadan varolan bir türe bir yöntem "eklemenize" izin vermez.</span><span class="sxs-lookup"><span data-stu-id="834e7-262">Extension methods let you "add" a method to an existing type without modifying the type itself or implementing the new method in an inherited type.</span></span> <span data-ttu-id="834e7-263">Uzantı yöntemi de uzattığı türle aynı derlemede ikamet etmek zorunda değildir.</span><span class="sxs-lookup"><span data-stu-id="834e7-263">The extension method also does not have to reside in the same assembly as the type it extends.</span></span> <span data-ttu-id="834e7-264">Bir uzantı yöntemini, bir türün tanımlı bir üyesiymiş gibi çağırırsınız.</span><span class="sxs-lookup"><span data-stu-id="834e7-264">You call an extension method as if it were a defined member of a type.</span></span>

<span data-ttu-id="834e7-265">Daha fazla bilgi için [Uzantı Yöntemleri'ne](programming-guide/classes-and-structs/extension-methods.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="834e7-265">For more information, see [Extension Methods](programming-guide/classes-and-structs/extension-methods.md).</span></span>

<a name="async"></a>

## <a name="async-methods"></a><span data-ttu-id="834e7-266">Async Yöntemleri</span><span class="sxs-lookup"><span data-stu-id="834e7-266">Async Methods</span></span>

<span data-ttu-id="834e7-267">Async özelliğini kullanarak, açık geri aramalar kullanmadan veya kodunuzu birden çok yöntem veya lambda ifadeleri arasında el ile bölmeden eşzamanlı yöntemler çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-267">By using the async feature, you can invoke asynchronous methods without using explicit callbacks or manually splitting your code across multiple methods or lambda expressions.</span></span>

<span data-ttu-id="834e7-268">Bir yöntemi [async](language-reference/keywords/async.md) değiştirici ile işaretlerseniz, yöntemde [bekleme](language-reference/operators/await.md) işlecini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="834e7-268">If you mark a method with the [async](language-reference/keywords/async.md) modifier, you can use the [await](language-reference/operators/await.md) operator in the method.</span></span> <span data-ttu-id="834e7-269">Denetim async `await` yönteminde bir ifadeye ulaştığında, beklenen görev tamamlanmadığında denetim arayanın alıbına döner ve beklenen görev tamamlanana kadar `await` anahtar sözcük ile yöntemdeki ilerleme askıya alınır.</span><span class="sxs-lookup"><span data-stu-id="834e7-269">When control reaches an `await` expression in the async method, control returns to the caller if the awaited task is not completed, and progress in the method with the `await` keyword is suspended until the awaited task completes.</span></span> <span data-ttu-id="834e7-270">Görev tamamlandığında, yürütme yöntemde devam edebilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-270">When the task is complete, execution can resume in the method.</span></span>

> [!NOTE]
> <span data-ttu-id="834e7-271">Async yöntemi, henüz tamamlanmamış ilk beklenen nesneyle karşılaştığında veya önce hangisi gerçekleşirse async yönteminin sonuna geldiğinde arayanın karşısına geçer.</span><span class="sxs-lookup"><span data-stu-id="834e7-271">An async method returns to the caller when either it encounters the first awaited object that’s not yet complete or it gets to the end of the async method, whichever occurs first.</span></span>

<span data-ttu-id="834e7-272">Bir async yöntemi <xref:System.Threading.Tasks.Task%601>, , <xref:System.Threading.Tasks.Task>veya `void`.</span><span class="sxs-lookup"><span data-stu-id="834e7-272">An async method can have a return type of <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.Task>, or `void`.</span></span> <span data-ttu-id="834e7-273">`void` İade türü öncelikle, `void` iade türünün gerekli olduğu olay işleyicilerini tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="834e7-273">The `void` return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="834e7-274">Döndüren `void` bir async yöntemi beklenemez ve geçersiz döndüren bir yöntemin arayan yöntematar özel durumlar yakalamak olamaz.</span><span class="sxs-lookup"><span data-stu-id="834e7-274">An async method that returns `void` can't be awaited, and the caller of a void-returning method can't catch exceptions that the method throws.</span></span> <span data-ttu-id="834e7-275">C# 7.0 ile başlayarak, bir async yöntemi [herhangi bir görev benzeri dönüş türü](./whats-new/csharp-7.md#generalized-async-return-types)ne olabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-275">Starting with C# 7.0, an async method can have [any task-like return type](./whats-new/csharp-7.md#generalized-async-return-types).</span></span>

<span data-ttu-id="834e7-276">Aşağıdaki örnekte, `DelayAsync` bir tamsayı döndüren bir iade deyimi olan bir async yöntemidir.</span><span class="sxs-lookup"><span data-stu-id="834e7-276">In the following example, `DelayAsync` is an async method that has a return statement that returns an integer.</span></span> <span data-ttu-id="834e7-277">Bir async yöntemi olduğundan, yöntem bildiriminin `Task<int>`bir dönüş türü ne sahip olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="834e7-277">Because it is an async method, its method declaration must have a return type of `Task<int>`.</span></span> <span data-ttu-id="834e7-278">`Task<int>`İade türü olduğundan, aşağıdaki `await` `DoSomethingAsync` `int result = await delayTask` ifadenin gösterdiği gibi, ifadenin değerlendirilmesi bir karşımlama üretir.</span><span class="sxs-lookup"><span data-stu-id="834e7-278">Because the return type is `Task<int>`, the evaluation of the `await` expression in `DoSomethingAsync` produces an integer, as the following `int result = await delayTask` statement demonstrates.</span></span>

[!code-csharp[csSnippets.Methods#102](../../samples/snippets/csharp/concepts/methods/async1.cs#102)]

<span data-ttu-id="834e7-279">Bir async yöntemi [herhangi](language-reference/keywords/in-parameter-modifier.md)bir bildiremez , [ref](language-reference/keywords/ref.md), veya [dışarı](language-reference/keywords/out-parameter-modifier.md) parametreleri, ama bu tür parametreleri olan yöntemleri çağırabilir.</span><span class="sxs-lookup"><span data-stu-id="834e7-279">An async method can't declare any [in](language-reference/keywords/in-parameter-modifier.md), [ref](language-reference/keywords/ref.md), or [out](language-reference/keywords/out-parameter-modifier.md) parameters, but it can call methods that have such parameters.</span></span>

 <span data-ttu-id="834e7-280">Async yöntemleri hakkında daha fazla bilgi için, [Async ve Await ile Asynchronous Programlama](async.md)bakın , [Async Programları Kontrol Akışı](programming-guide/concepts/async/control-flow-in-async-programs.md), ve [Async İade Türleri](programming-guide/concepts/async/async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="834e7-280">For more information about async methods, see [Asynchronous Programming with Async and Await](async.md), [Control Flow in Async Programs](programming-guide/concepts/async/control-flow-in-async-programs.md), and [Async Return Types](programming-guide/concepts/async/async-return-types.md).</span></span>

<a name="expr"></a>

## <a name="expression-bodied-members"></a><span data-ttu-id="834e7-281">İfade gövdeli üyeler</span><span class="sxs-lookup"><span data-stu-id="834e7-281">Expression-bodied members</span></span>

<span data-ttu-id="834e7-282">Yalnızca bir ifadenin sonucuyla hemen dönen veya yöntemin gövdesi olarak tek bir ifadeye sahip yöntem tanımlarına sahip olması yaygındır.</span><span class="sxs-lookup"><span data-stu-id="834e7-282">It is common to have method definitions that simply return immediately with the result of an expression, or that have a single statement as the body of the method.</span></span>  <span data-ttu-id="834e7-283">Bu tür yöntemleri tanımlamak için bir sözdizimi kısayolu vardır: `=>`</span><span class="sxs-lookup"><span data-stu-id="834e7-283">There is a syntax shortcut for defining such methods using `=>`:</span></span>

```csharp
public Point Move(int dx, int dy) => new Point(x + dx, y + dy);
public void Print() => Console.WriteLine(First + " " + Last);
// Works with operators, properties, and indexers too.
public static Complex operator +(Complex a, Complex b) => a.Add(b);
public string Name => First + " " + Last;
public Customer this[long id] => store.LookupCustomer(id);
```

<span data-ttu-id="834e7-284">Yöntem döndürür `void` veya bir async yöntemi ise, yöntemin gövdesi bir deyim ifadesi olmalıdır (lambdas ile aynı).</span><span class="sxs-lookup"><span data-stu-id="834e7-284">If the method returns `void` or is an async method, the body of the method must be a statement expression (same as with lambdas).</span></span>  <span data-ttu-id="834e7-285">Özellikler ve dizin leyiciler için salt okunur olmaları gerekir `get` ve erişimci anahtar sözcüğü kullanmazsınız.</span><span class="sxs-lookup"><span data-stu-id="834e7-285">For properties and indexers, they must be read-only, and you do not use the `get` accessor keyword.</span></span>

<a name="iterators"></a>

## <a name="iterators"></a><span data-ttu-id="834e7-286">Yineleyiciler</span><span class="sxs-lookup"><span data-stu-id="834e7-286">Iterators</span></span>

<span data-ttu-id="834e7-287">Yineleyici, liste veya dizi gibi bir koleksiyon üzerinde özel bir yineleme gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="834e7-287">An iterator performs a custom iteration over a collection, such as a list or an array.</span></span> <span data-ttu-id="834e7-288">Bir yineleyici, her öğeyi birer birer döndürmek için [verim iade](language-reference/keywords/yield.md) deyimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-288">An iterator uses the [yield return](language-reference/keywords/yield.md) statement to return each element one at a time.</span></span> <span data-ttu-id="834e7-289">Bir `yield return` bildirime ulaşıldığında, arayan kişinin dizideki bir sonraki öğeyi isteyebileceği şekilde geçerli konum hatırlanır.</span><span class="sxs-lookup"><span data-stu-id="834e7-289">When a `yield return` statement is reached, the current location is remembered so that the caller can request the next element in the sequence.</span></span>

<span data-ttu-id="834e7-290">Bir yineleyicinin dönüş türü <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, <xref:System.Collections.IEnumerator>, <xref:System.Collections.Generic.IEnumerator%601>veya .</span><span class="sxs-lookup"><span data-stu-id="834e7-290">The return type of an iterator can be <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, <xref:System.Collections.IEnumerator>, or <xref:System.Collections.Generic.IEnumerator%601>.</span></span>

<span data-ttu-id="834e7-291">Daha fazla bilgi için [bkz.](programming-guide/concepts/iterators.md)</span><span class="sxs-lookup"><span data-stu-id="834e7-291">For more information, see [Iterators](programming-guide/concepts/iterators.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="834e7-292">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="834e7-292">See also</span></span>

- [<span data-ttu-id="834e7-293">Erişim Değiştiricileri</span><span class="sxs-lookup"><span data-stu-id="834e7-293">Access Modifiers</span></span>](language-reference/keywords/access-modifiers.md)
- [<span data-ttu-id="834e7-294">Statik Sınıflar ve Statik Sınıf Üyeleri</span><span class="sxs-lookup"><span data-stu-id="834e7-294">Static Classes and Static Class Members</span></span>](programming-guide/classes-and-structs/static-classes-and-static-class-members.md)
- [<span data-ttu-id="834e7-295">Devralma</span><span class="sxs-lookup"><span data-stu-id="834e7-295">Inheritance</span></span>](programming-guide/classes-and-structs/inheritance.md)
- [<span data-ttu-id="834e7-296">Soyut ve Korumalı Sınıflar ve Sınıf Üyeleri</span><span class="sxs-lookup"><span data-stu-id="834e7-296">Abstract and Sealed Classes and Class Members</span></span>](programming-guide/classes-and-structs/abstract-and-sealed-classes-and-class-members.md)
- [<span data-ttu-id="834e7-297">params</span><span class="sxs-lookup"><span data-stu-id="834e7-297">params</span></span>](language-reference/keywords/params.md)
- [<span data-ttu-id="834e7-298">çıkış</span><span class="sxs-lookup"><span data-stu-id="834e7-298">out</span></span>](language-reference/keywords/out-parameter-modifier.md)
- [<span data-ttu-id="834e7-299">Referans</span><span class="sxs-lookup"><span data-stu-id="834e7-299">ref</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="834e7-300">Inç</span><span class="sxs-lookup"><span data-stu-id="834e7-300">in</span></span>](language-reference/keywords/in-parameter-modifier.md)
- [<span data-ttu-id="834e7-301">Parametreleri Geçirme</span><span class="sxs-lookup"><span data-stu-id="834e7-301">Passing Parameters</span></span>](programming-guide/classes-and-structs/passing-parameters.md)
