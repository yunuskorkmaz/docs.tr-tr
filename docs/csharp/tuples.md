---
title: Tuple türleri - C# Kılavuzu
description: "C'de adsız ve adlandırılmış tuple türleri hakkında bilgi edinin #"
ms.date: 05/15/2018
ms.technology: csharp-fundamentals
ms.assetid: ee8bf7c3-aa3e-4c9e-a5c6-e05cc6138baa
ms.openlocfilehash: 9ce9e1d4395d1a75f36004384ec215c615cd9802
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79156915"
---
# <a name="c-tuple-types"></a><span data-ttu-id="5a92a-103">C# tuple türleri</span><span class="sxs-lookup"><span data-stu-id="5a92a-103">C# tuple types</span></span>

<span data-ttu-id="5a92a-104">C# tuples hafif bir sözdizimi kullanarak tanımladığınız türleridir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-104">C# tuples are types that you define using a lightweight syntax.</span></span> <span data-ttu-id="5a92a-105">Avantajları arasında daha basit bir sözdizimi, sayıya (kardinallik olarak anılacaktır) dayalı dönüşümkuralları ve öğe türleri ve kopyalar, eşitlik testleri ve atamalar için tutarlı kurallar yer almaktadır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-105">The advantages include a simpler syntax, rules for conversions based on number (referred to as cardinality) and types of elements, and consistent rules for copies, equality tests, and assignments.</span></span> <span data-ttu-id="5a92a-106">Bir tradeoff olarak, tuples bazı nesne yönelimli deyimler devralma ile ilişkili desteklemez.</span><span class="sxs-lookup"><span data-stu-id="5a92a-106">As a tradeoff, tuples do not support some of the object-oriented idioms associated with inheritance.</span></span> <span data-ttu-id="5a92a-107">[C# 7.0](whats-new/csharp-7.md#tuples) makalesinde yeni olan tuples bölümüne genel bir bakış alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-107">You can get an overview in the section on [tuples in the What's new in C# 7.0](whats-new/csharp-7.md#tuples) article.</span></span>

<span data-ttu-id="5a92a-108">Bu makalede, C# 7.0 ve sonraki sürümlerde tuples yöneten dil kuralları, bunları kullanmak için farklı yollar ve tuples ile çalışma ilk rehberlik öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-108">In this article, you'll learn the language rules governing tuples in C# 7.0 and later versions, different ways to use them, and initial guidance on working with tuples.</span></span>

> [!NOTE]
> <span data-ttu-id="5a92a-109">Yeni tuples özellikleri <xref:System.ValueTuple> türleri gerektirir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-109">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="5a92a-110">Türleri içermeyen platformlarda [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) kullanmak için NuGet paketini eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-110">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="5a92a-111">Bu, çerçevede teslim edilen türlere dayanan diğer dil özelliklerine benzer.</span><span class="sxs-lookup"><span data-stu-id="5a92a-111">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="5a92a-112">Örnekler `async` arasında `await` `INotifyCompletion` arayüze ve LINQ'a `IEnumerable<T>`güvenerek.</span><span class="sxs-lookup"><span data-stu-id="5a92a-112">Examples include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="5a92a-113">Ancak,.NET daha platformbağımsız hale geldikçe teslimat mekanizması değişiyor.</span><span class="sxs-lookup"><span data-stu-id="5a92a-113">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="5a92a-114">.NET Framework her zaman dil derleyicisi ile aynı cadence üzerinde gemi olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-114">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="5a92a-115">Yeni dil özellikleri yeni türlere dayandığında, bu tür ler dil özellikleri geldiğinde NuGet paketleri olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-115">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="5a92a-116">Bu yeni türler .NET Standart API'ye eklenip çerçevenin bir parçası olarak teslim edildikçe, NuGet paket gereksinimi kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-116">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="5a92a-117">Yeni tuple desteği ekleme nin nedenleri ile başlayalım.</span><span class="sxs-lookup"><span data-stu-id="5a92a-117">Let's start with the reasons for adding new tuple support.</span></span> <span data-ttu-id="5a92a-118">Yöntemler tek bir nesneyi döndürer.</span><span class="sxs-lookup"><span data-stu-id="5a92a-118">Methods return a single object.</span></span> <span data-ttu-id="5a92a-119">Tuples, o tek nesnedeki birden çok değeri daha kolay paketlemenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-119">Tuples enable you to package multiple values in that single object more easily.</span></span>

<span data-ttu-id="5a92a-120">.NET Framework zaten `Tuple` genel sınıflara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-120">The .NET Framework already has generic `Tuple` classes.</span></span> <span data-ttu-id="5a92a-121">Ancak bu sınıfların iki büyük sınırlaması vardı.</span><span class="sxs-lookup"><span data-stu-id="5a92a-121">These classes, however, had two major limitations.</span></span> <span data-ttu-id="5a92a-122">İlk olarak, `Tuple` sınıflar kendi `Item1` `Item2`özellikleri , ve benzeri adlı.</span><span class="sxs-lookup"><span data-stu-id="5a92a-122">For one, the `Tuple` classes named their properties `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="5a92a-123">Bu isimler anlamsal bilgi taşımama.</span><span class="sxs-lookup"><span data-stu-id="5a92a-123">Those names carry no semantic information.</span></span> <span data-ttu-id="5a92a-124">Bu `Tuple` türleri kullanmak, özelliklerin her birinin anlamını iletmesini sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-124">Using these `Tuple` types does not enable communicating the meaning of each of the properties.</span></span> <span data-ttu-id="5a92a-125">Yeni dil özellikleri, bir tuple'daki öğeler için anlamsal olarak anlamlı adlar beyan etmenizi ve kullanmanıza olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-125">The new language features enable you to declare and use semantically meaningful names for the elements in a tuple.</span></span>

<span data-ttu-id="5a92a-126">Sınıflar, `Tuple` başvuru türleri olduğundan daha fazla performans endişesine neden olur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-126">The `Tuple` classes cause more performance concerns because they are reference types.</span></span> <span data-ttu-id="5a92a-127">`Tuple` Türlerden birini kullanmak, nesneleri ayırmaanlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-127">Using one of the `Tuple` types means allocating objects.</span></span> <span data-ttu-id="5a92a-128">Sıcak yollarda, birçok küçük nesne ayırmak uygulamanızın performansı üzerinde ölçülebilir bir etki yapabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-128">On hot paths, allocating many small objects can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="5a92a-129">Bu nedenle, tuples için dil `ValueTuple` desteği yeni structs yararlanır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-129">Therefore, the language support for tuples leverages the new `ValueTuple` structs.</span></span>

<span data-ttu-id="5a92a-130">Bu eksiklikleri önlemek için, birden `class` çok `struct` öğe taşımak için bir veya a oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-130">To avoid those deficiencies, you could create a `class` or a `struct` to carry multiple elements.</span></span> <span data-ttu-id="5a92a-131">Ne yazık ki, bu sizin için daha fazla iş, ve tasarım niyetini gizler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-131">Unfortunately, that's more work for you, and it obscures your design intent.</span></span> <span data-ttu-id="5a92a-132">Hem `struct` veri `class` hem de davranış içeren bir tür tanımladığınızı veya bir tür tanımladığınızı ima etme.</span><span class="sxs-lookup"><span data-stu-id="5a92a-132">Making a `struct` or `class` implies that you are defining a type with both data and behavior.</span></span> <span data-ttu-id="5a92a-133">Çoğu zaman, yalnızca tek bir nesnede birden çok değer depolamak istiyorum.</span><span class="sxs-lookup"><span data-stu-id="5a92a-133">Many times, you simply want to store multiple values in a single object.</span></span>

<span data-ttu-id="5a92a-134">Dil özellikleri ve `ValueTuple` genel yapı, bu tuple türlerine herhangi bir davranış (yöntem) ekleyemeyeceğiniz kuralını uygular.</span><span class="sxs-lookup"><span data-stu-id="5a92a-134">The language features and the `ValueTuple` generic structs enforce the rule that you cannot add any behavior (methods) to these tuple types.</span></span>
<span data-ttu-id="5a92a-135">Tüm `ValueTuple` türleri *mutable structs*vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-135">All the `ValueTuple` types are *mutable structs*.</span></span> <span data-ttu-id="5a92a-136">Her üye alanı ortak bir alandır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-136">Each member field is a public field.</span></span> <span data-ttu-id="5a92a-137">Bu onları çok hafif yapar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-137">That makes them very lightweight.</span></span> <span data-ttu-id="5a92a-138">Ancak, bu, değişmezliğin önemli olduğu yerlerde tuples kullanılmamalıdır anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-138">However, that means tuples should not be used where immutability is important.</span></span>

<span data-ttu-id="5a92a-139">Tuples hem basit ve daha esnek `class` `struct` veri kapsayıcıları ve türleri vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-139">Tuples are both simpler and more flexible data containers than `class` and `struct` types.</span></span> <span data-ttu-id="5a92a-140">Bu farklılıkları inceleyelim.</span><span class="sxs-lookup"><span data-stu-id="5a92a-140">Let's explore those differences.</span></span>

## <a name="named-and-unnamed-tuples"></a><span data-ttu-id="5a92a-141">Adlandırılmış ve adsız tuples</span><span class="sxs-lookup"><span data-stu-id="5a92a-141">Named and unnamed tuples</span></span>

<span data-ttu-id="5a92a-142">`ValueTuple` Yapı, varolan `Item1` `Item2` `Item3` `Tuple` türlerde tanımlanan özelliklere benzer şekilde , , , vb. adlı alanlara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-142">The `ValueTuple` struct has fields named `Item1`, `Item2`, `Item3`, and so on, similar to the properties defined in the existing `Tuple` types.</span></span>
<span data-ttu-id="5a92a-143">Bu *adlar, adsız tuples*için kullanabileceğiniz tek adlardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-143">These names are the only names you can use for *unnamed tuples*.</span></span> <span data-ttu-id="5a92a-144">Bir tuple'a alternatif alan adları sağlamadığınızda, adsız bir tuple oluşturdunuz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-144">When you do not provide any alternative field names to a tuple, you've created an unnamed tuple:</span></span>

[!code-csharp[UnnamedTuple](../../samples/snippets/csharp/tuples/program.cs#01_UnNamedTuple "Unnamed tuple")]

<span data-ttu-id="5a92a-145">Önceki örnekte tuple literal sabitler kullanılarak başharfe getirilmiştir ve C# 7.1'de *tuple alan adı projeksiyonları* kullanılarak oluşturulan öğe adları yoktur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-145">The tuple in the previous example was initialized using literal constants and won't have element names created using *tuple field name projections* in C# 7.1.</span></span>

<span data-ttu-id="5a92a-146">Ancak, bir tuple'ı başharfe aldığınızda, her alana daha iyi adlar veren yeni dil özelliklerini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-146">However, when you initialize a tuple, you can use new language features that give better names to each field.</span></span> <span data-ttu-id="5a92a-147">Bunu yapmak adlandırılmış bir *tuple*oluşturur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-147">Doing so creates a *named tuple*.</span></span>
<span data-ttu-id="5a92a-148">Adlandırılmış tuples hala `Item1` `Item2`adlı `Item3` öğeleri var , , ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="5a92a-148">Named tuples still have elements named `Item1`, `Item2`, `Item3` and so on.</span></span>
<span data-ttu-id="5a92a-149">Ama aynı zamanda sizin adlandırdığınız bu öğelerin herhangi biri için eşanlamlı var.</span><span class="sxs-lookup"><span data-stu-id="5a92a-149">But they also have synonyms for any of those elements that you have named.</span></span>
<span data-ttu-id="5a92a-150">Her öğeiçin adları belirterek adlandırılmış bir tuple oluşturursunuz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-150">You create a named tuple by specifying the names for each element.</span></span> <span data-ttu-id="5a92a-151">Bir yolu tuple başlatma nın bir parçası olarak adları belirtmektir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-151">One way is to specify the names as part of the tuple initialization:</span></span>

[!code-csharp[NamedTuple](../../samples/snippets/csharp/tuples/program.cs#02_NamedTuple "Named tuple")]

<span data-ttu-id="5a92a-152">Bu eşanlamlılar derleyici ve dil tarafından işlenir, böylece adlandırılmış tuples'ı etkili bir şekilde kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-152">These synonyms are handled by the compiler and the language so that you can use named tuples effectively.</span></span> <span data-ttu-id="5a92a-153">IID'ler ve editörler bu anlamsal adları Roslyn API'lerini kullanarak okuyabilirler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-153">IDEs and editors can read these semantic names using the Roslyn APIs.</span></span> <span data-ttu-id="5a92a-154">Adlandırılmış bir tuple'ın öğelerini, aynı derlemenin herhangi bir yerinde ki anlamsal adlarla başvurulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-154">You can reference the elements of a named tuple by those semantic names anywhere in the same assembly.</span></span> <span data-ttu-id="5a92a-155">Derleyici, derlenmiş çıktıyı oluştururken `Item*` tanımladığınız adları eşdeğerleriyle değiştirir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-155">The compiler replaces the names you've defined with `Item*` equivalents when generating the compiled output.</span></span> <span data-ttu-id="5a92a-156">Derlenmiş Microsoft Intermediate Language (MSIL), bu öğeleri vermiş olduğunuz adları içermez.</span><span class="sxs-lookup"><span data-stu-id="5a92a-156">The compiled Microsoft Intermediate Language (MSIL) does not include the names you've given these elements.</span></span>

<span data-ttu-id="5a92a-157">C# 7.1 ile başlayarak, bir tuple için alan adları tuple başlatılması için kullanılan değişkenlerden sağlanabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-157">Beginning with C# 7.1, the field names for a tuple may be provided from the variables used to initialize the tuple.</span></span> <span data-ttu-id="5a92a-158">Bu **[tuple projeksiyon başharfleri](#tuple-projection-initializers)** olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-158">This is referred to as **[tuple projection initializers](#tuple-projection-initializers)**.</span></span> <span data-ttu-id="5a92a-159">Aşağıdaki kod, öğeler `accumulation` `count` (bir tamsayı) ve `sum` (çift) ile birlikte bir tuple oluşturur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-159">The following code creates a tuple named `accumulation` with elements `count` (an integer), and `sum` (a double).</span></span>

[!code-csharp[ProjectedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectedTupleNames "Named tuple")]

<span data-ttu-id="5a92a-160">Derleyici, ortak yöntemlerden veya özelliklerden döndürülen tuples için oluşturduğunuz adları iletmelidir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-160">The compiler must communicate those names you created for tuples that are returned from public methods or properties.</span></span> <span data-ttu-id="5a92a-161">Bu gibi durumlarda, derleyici <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> yönteme bir öznitelik ekler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-161">In those cases, the compiler adds a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> attribute on the method.</span></span> <span data-ttu-id="5a92a-162">Bu öznitelik, <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> tuple'daki öğelerin her birine verilen adları içeren bir liste özelliği içerir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-162">This attribute contains a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> list property that contains the names given to each of the elements in the tuple.</span></span>

> [!NOTE]
> <span data-ttu-id="5a92a-163">Visual Studio gibi Geliştirme Araçları da bu meta verileri okur ve meta veri alan adlarını kullanarak IntelliSense ve diğer özellikleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-163">Development Tools, such as Visual Studio, also read that metadata, and provide IntelliSense and other features using the metadata field names.</span></span>

<span data-ttu-id="5a92a-164">Yeni tuples bu temelleri ve `ValueTuple` birbirlerine adlandırılmış tuples atama kurallarını anlamak için türü anlamak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-164">It is important to understand these underlying fundamentals of the new tuples and the `ValueTuple` type in order to understand the rules for assigning named tuples to each other.</span></span>

## <a name="tuple-projection-initializers"></a><span data-ttu-id="5a92a-165">Tuple projeksiyon başfçıları</span><span class="sxs-lookup"><span data-stu-id="5a92a-165">Tuple projection initializers</span></span>

<span data-ttu-id="5a92a-166">Genel olarak, tuple projeksiyon başfasyonları, bir tuple başlatma deyiminin sağ tarafındaki değişken veya alan adlarını kullanarak çalışır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-166">In general, tuple projection initializers work by using the variable or field names from the right-hand side of a tuple initialization statement.</span></span>
<span data-ttu-id="5a92a-167">Açık bir ad verilirse, bu, yansıtılan herhangi bir ada göre önceliklidir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-167">If an explicit name is given, that takes precedence over any projected name.</span></span> <span data-ttu-id="5a92a-168">Örneğin, aşağıdaki baş harfe yönelik olarak, `explicitFieldTwo`öğeler `localVariableOne` `localVariableTwo` `explicitFieldOne` ve , değil ve:</span><span class="sxs-lookup"><span data-stu-id="5a92a-168">For example, in the following initializer, the elements are `explicitFieldOne` and `explicitFieldTwo`, not `localVariableOne` and `localVariableTwo`:</span></span>

[!code-csharp[ExplicitNamedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectionExample_Explicit "Explicitly named tuple")]

<span data-ttu-id="5a92a-169">Açık bir ad verilen herhangi bir alan için, geçerli bir örtülü ad yansıtılır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-169">For any field where an explicit name is not provided, an applicable implicit name is projected.</span></span> <span data-ttu-id="5a92a-170">Anlamsal adlar sağlamak için herhangi bir gereklilik yoktur, ya açık veya örtülü.</span><span class="sxs-lookup"><span data-stu-id="5a92a-170">There is no requirement to provide semantic names, either explicitly or implicitly.</span></span> <span data-ttu-id="5a92a-171">Aşağıdaki baş harf, değeri `Item1` `42` `stringContent`"Her şeyin cevabı" olan alan adları vardır:</span><span class="sxs-lookup"><span data-stu-id="5a92a-171">The following initializer has     field names `Item1`, whose value is `42` and `stringContent`, whose value is "The answer to everything":</span></span>

[!code-csharp[MixedTuple](../../samples/snippets/csharp/tuples/program.cs#MixedTuple "mixed tuple")]

<span data-ttu-id="5a92a-172">Aday alan adlarının tuple alanına yansıtılan olmayan iki koşulu vardır:</span><span class="sxs-lookup"><span data-stu-id="5a92a-172">There are two conditions where candidate field names are not projected onto the tuple field:</span></span>

1. <span data-ttu-id="5a92a-173">Aday adı ayrılmış bir tuple adı olduğunda.</span><span class="sxs-lookup"><span data-stu-id="5a92a-173">When the candidate name is a reserved tuple name.</span></span> <span data-ttu-id="5a92a-174">Örnekler `Item3`arasında `ToString`, `Rest`, veya .</span><span class="sxs-lookup"><span data-stu-id="5a92a-174">Examples include `Item3`, `ToString`, or `Rest`.</span></span>
1. <span data-ttu-id="5a92a-175">Aday adı açık veya örtülü başka bir tuple alan adının bir kopyası olduğunda.</span><span class="sxs-lookup"><span data-stu-id="5a92a-175">When the candidate name is a duplicate of another tuple field name, either explicit or implicit.</span></span>

<span data-ttu-id="5a92a-176">Bu koşullar belirsizliği önler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-176">These conditions avoid ambiguity.</span></span> <span data-ttu-id="5a92a-177">Bu adlar, bir tuple'daki bir alanın alan adları olarak kullanılırsa belirsizliğe neden olur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-177">These names would cause an ambiguity if they were used as the field names for a field in a tuple.</span></span> <span data-ttu-id="5a92a-178">Bu koşulların hiçbiri derleme zamanı hatalarına neden değildir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-178">Neither of these conditions cause compile-time errors.</span></span> <span data-ttu-id="5a92a-179">Bunun yerine, yansıtılan adları olmayan öğeler için yansıtılan anlamsal adlar yoktur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-179">Instead, the elements without projected names do not have semantic names projected for them.</span></span>  <span data-ttu-id="5a92a-180">Aşağıdaki örnekler bu koşulları göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-180">The following examples demonstrate these conditions:</span></span>

[!code-csharp-interactive[Ambiguity](../../samples/snippets/csharp/tuples/program.cs#ProjectionAmbiguities "tuples where projections are not performed")]

<span data-ttu-id="5a92a-181">Bu durumlar derleyici hatalarına neden olmaz, çünkü bu, tuple alan adı projeksiyonları olmadığında C# 7.0 ile yazılmış kod için bir bozma değişikliği olacaktır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-181">These situations do not cause compiler errors because that would be a breaking change for code written with C# 7.0, when tuple field name projections were not available.</span></span>

## <a name="equality-and-tuples"></a><span data-ttu-id="5a92a-182">Eşitlik ve tuples</span><span class="sxs-lookup"><span data-stu-id="5a92a-182">Equality and tuples</span></span>

<span data-ttu-id="5a92a-183">C# 7.3 ile başlayarak, tuple türleri `==` ve `!=` işleçleri destekler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-183">Beginning with C# 7.3, tuple types support the `==` and `!=` operators.</span></span> <span data-ttu-id="5a92a-184">Bu işleçler, sol bağımsız değişkenin her üyesini sırayla sağ bağımsız değişkenin her üyesiyle karşılaştırarak çalışır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-184">These operators work by comparing each member of the left argument to each member of the right argument in order.</span></span> <span data-ttu-id="5a92a-185">Bu karşılaştırmalar kısa devre.</span><span class="sxs-lookup"><span data-stu-id="5a92a-185">These comparisons short-circuit.</span></span> <span data-ttu-id="5a92a-186">Bir çift eşit olmadığı anda üyeleri değerlendirmeyi bırakırlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-186">They will stop evaluating members as soon as one pair is not equal.</span></span> <span data-ttu-id="5a92a-187">Aşağıdaki kod örnekleri `==`kullanır, ancak karşılaştırma `!=`kurallarının tümü .</span><span class="sxs-lookup"><span data-stu-id="5a92a-187">The following code examples use `==`, but the comparison rules all apply to `!=`.</span></span> <span data-ttu-id="5a92a-188">Aşağıdaki kod örneği, iki çift bir sonda için eşitlik karşılaştırması gösterir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-188">The following code example shows an equality comparison for two pairs of integers:</span></span>

[!code-csharp-interactive[TupleEquality](../../samples/snippets/csharp/tuples/program.cs#Equality "Testing tuples for equality")]

<span data-ttu-id="5a92a-189">Tuple eşitlik testlerini daha kullanışlı hale getiren birkaç kural vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-189">There are several rules that make tuple equality tests more convenient.</span></span> <span data-ttu-id="5a92a-190">Tuple eşitliği, aşağıdaki kodda gösterildiği gibi, tuples biri nullable tuple ise [kaldırılmış dönüşümler](~/_csharplang/spec/conversions.md#lifted-conversion-operators) gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-190">Tuple equality performs [lifted conversions](~/_csharplang/spec/conversions.md#lifted-conversion-operators) if one of the tuples is a nullable tuple, as shown in the following code:</span></span>

[!code-csharp-interactive[NullableTupleEquality](../../samples/snippets/csharp/tuples/program.cs#NullableEquality "Comparing Tuples and nullable tuples")]

<span data-ttu-id="5a92a-191">Tuple eşitliği aynı zamanda her iki tuples her üyesi üzerinde örtülü dönüşümler gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-191">Tuple equality also performs implicit conversions on each member of both tuples.</span></span> <span data-ttu-id="5a92a-192">Bunlar arasında kaldırılmış dönüşümler, genişletme dönüşümleri veya diğer örtülü dönüşümler yer almaktadır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-192">These include lifted conversions, widening conversions, or other implicit conversions.</span></span> <span data-ttu-id="5a92a-193">Aşağıdaki örnekler, tamsayıdan uzuna örtülü dönüşüm nedeniyle bir tamsayı 2-tuple ile karşılaştırılabilen bir tamsayı 2-tuple'ı göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-193">The following examples show that an integer 2-tuple can be compared to a long 2-tuple because of the implicit conversion from integer to long:</span></span>

[!code-csharp-interactive[SnippetMemberConversions](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberConversions "converting tuples for equality tests")]

<span data-ttu-id="5a92a-194">Tuple üyelerinin adları eşitlik testlerine katılmaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-194">The names of the tuple members do not participate in tests for equality.</span></span> <span data-ttu-id="5a92a-195">Ancak, operands biri açık adlarla bir tuple literal ise, derleyici bu adlar diğer operand adlarını eşleşmiyorsa CS8383 uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="5a92a-195">However, if one of the operands is a tuple literal with explicit names, the compiler generates warning CS8383 if those names do not match the names of the other operand.</span></span>
<span data-ttu-id="5a92a-196">Her iki operands tuple literals olduğu durumlarda, uyarı sağ operand aşağıdaki örnekte gösterildiği gibi:</span><span class="sxs-lookup"><span data-stu-id="5a92a-196">In the case where both operands are tuple literals, the warning is on the right operand as shown in the following example:</span></span>

[!code-csharp-interactive[MemberNames](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberNames "Tuple member names do not participate in equality tests")]

<span data-ttu-id="5a92a-197">Son olarak, tuples iç içe tuples içerebilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-197">Finally, tuples may contain nested tuples.</span></span> <span data-ttu-id="5a92a-198">Tuple eşitliği aşağıdaki örnekte gösterildiği gibi iç içe geçen tupüller aracılığıyla her operand "şekli" karşılaştırır:</span><span class="sxs-lookup"><span data-stu-id="5a92a-198">Tuple equality compares the "shape" of each operand through nested tuples as shown in the following example:</span></span>

[!code-csharp-interactive[NestedTuples](../../samples/snippets/csharp/tuples/program.cs#SnippetNestedTuples "Tuples may contain nested tuples that participate in tuple equality.")]

<span data-ttu-id="5a92a-199">Farklı şekillere sahip olduklarında eşitlik (veya eşitsizlik) için iki tuple'ı karşılaştırmak için bir derleme zaman hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-199">It's a compile time error to compare two tuples for equality (or inequality) when they have different shapes.</span></span> <span data-ttu-id="5a92a-200">Derleyici, iç içe geçmiş tupülleri karşılaştırmak için herhangi bir yapısızlaştırma girişiminde bulunmaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-200">The compiler won't attempt any deconstruction of nested tuples in order to compare them.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="5a92a-201">Atama ve tuples</span><span class="sxs-lookup"><span data-stu-id="5a92a-201">Assignment and tuples</span></span>

<span data-ttu-id="5a92a-202">Dil, her sağ yan öğenin örtülü olarak karşılık gelen sol yan öğesine dönüştürülebileceği aynı sayıda öğeye sahip tuple türleri arasındaki atamayı destekler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-202">The language supports assignment between tuple types that have the same number of elements, where each right-hand side element can be implicitly converted to its corresponding left-hand side element.</span></span> <span data-ttu-id="5a92a-203">Diğer dönüşümler atamalar için dikkate alınmaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-203">Other conversions aren't considered for assignments.</span></span> <span data-ttu-id="5a92a-204">Farklı şekiller olduğunda bir tuple'ı diğerine atamak için bir derleme zaman hatasıdır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-204">It's a compile time error to assign one tuple to another when they have different shapes.</span></span> <span data-ttu-id="5a92a-205">Derleyici, iç içe geçmiş tupülleri atamak için herhangi bir yapısızlaştırma girişiminde bulunmaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-205">The compiler won't attempt any deconstruction of nested tuples in order to assign them.</span></span>
<span data-ttu-id="5a92a-206">Tuple türleri arasında izin verilen atama türlerine bakalım.</span><span class="sxs-lookup"><span data-stu-id="5a92a-206">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="5a92a-207">Aşağıdaki örneklerde kullanılan bu değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="5a92a-207">Consider these variables used in the following examples:</span></span>

[!code-csharp[VariableCreation](../../samples/snippets/csharp/tuples/program.cs#03_VariableCreation "Variable creation")]

<span data-ttu-id="5a92a-208">İlk iki değişken `unnamed` ve `anonymous` öğeler için sağlanan anlamsal adları yok.</span><span class="sxs-lookup"><span data-stu-id="5a92a-208">The first two variables, `unnamed` and `anonymous` do not have semantic names provided for the elements.</span></span> <span data-ttu-id="5a92a-209">Alan adları `Item1` ve. `Item2`</span><span class="sxs-lookup"><span data-stu-id="5a92a-209">The field names are `Item1` and `Item2`.</span></span>
<span data-ttu-id="5a92a-210">Son iki değişken `named` ve `differentName` öğeler için verilen anlamsal adlara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-210">The last two variables, `named` and `differentName` have semantic names given for the elements.</span></span> <span data-ttu-id="5a92a-211">Bu iki tuples öğeleri için farklı adlar vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-211">These two tuples have different names for the elements.</span></span>

<span data-ttu-id="5a92a-212">Bu tuples dört öğeleri aynı sayıda ('kardinallik' olarak anılacaktır) ve bu öğelerin türleri aynıdır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-212">All four of these tuples have the same number of elements (referred to as 'cardinality') and the types of those elements are identical.</span></span> <span data-ttu-id="5a92a-213">Bu nedenle, tüm bu atamalar çalışır:</span><span class="sxs-lookup"><span data-stu-id="5a92a-213">Therefore, all of these assignments work:</span></span>

[!code-csharp[VariableAssignment](../../samples/snippets/csharp/tuples/program.cs#04_VariableAssignment "Variable assignment")]

<span data-ttu-id="5a92a-214">Tuples adlarının atanmamış olduğuna dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="5a92a-214">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="5a92a-215">Öğelerin değerleri, tuple'daki öğelerin sırasına göre atanır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-215">The values of the elements are assigned following the order of the elements in the tuple.</span></span>

<span data-ttu-id="5a92a-216">Farklı türde veya sayıda öğenin tuples atanamaz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-216">Tuples of different types or numbers of elements are not assignable:</span></span>

```csharp
// Does not compile.
// CS0029: Cannot assign Tuple(int,int,int) to Tuple(int, string)
var differentShape = (1, 2, 3);
named = differentShape;
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="5a92a-217">Yöntem döndürme değerleri olarak tuples</span><span class="sxs-lookup"><span data-stu-id="5a92a-217">Tuples as method return values</span></span>

<span data-ttu-id="5a92a-218">Tuples için en yaygın kullanımlarından biri bir yöntem dönüş değeri olarak.</span><span class="sxs-lookup"><span data-stu-id="5a92a-218">One of the most common uses for tuples is as a method return value.</span></span> <span data-ttu-id="5a92a-219">Bir örnek üzerinden bakalım.</span><span class="sxs-lookup"><span data-stu-id="5a92a-219">Let's walk through one example.</span></span> <span data-ttu-id="5a92a-220">Bir sayı dizisi için standart sapmayı hesaplayan bu yöntemi göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="5a92a-220">Consider this method that computes the standard deviation for a sequence of numbers:</span></span>

[!code-csharp[StandardDeviation](../../samples/snippets/csharp/tuples/statistics.cs#05_StandardDeviation "Compute Standard Deviation")]

> [!NOTE]
> <span data-ttu-id="5a92a-221">Bu örnekler düzeltilmemiş örnek standart sapması hesaplamak.</span><span class="sxs-lookup"><span data-stu-id="5a92a-221">These examples compute the uncorrected sample standard deviation.</span></span>
> <span data-ttu-id="5a92a-222">Düzeltilmiş örnek standart sapma formülü, `Average` uzantı yönteminin yaptığı gibi, kareli farklılıkların toplamını N yerine (N-1) ortalamasından böler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-222">The corrected sample standard deviation formula would divide the sum of the squared differences from the mean by (N-1) instead of N, as the `Average` extension method does.</span></span> <span data-ttu-id="5a92a-223">Standart sapma için bu formüller arasındaki farklar hakkında daha fazla bilgi için bir istatistik metnine başvurun.</span><span class="sxs-lookup"><span data-stu-id="5a92a-223">Consult a statistics text for more details on the differences between these formulas for standard deviation.</span></span>

<span data-ttu-id="5a92a-224">Önceki kod standart sapma için ders kitabı formülü izler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-224">The preceding code follows the textbook formula for the standard deviation.</span></span> <span data-ttu-id="5a92a-225">Doğru cevabı üretiyor, ama verimsiz bir uygulama.</span><span class="sxs-lookup"><span data-stu-id="5a92a-225">It produces the correct answer, but it's an inefficient implementation.</span></span> <span data-ttu-id="5a92a-226">Bu yöntem sırayı iki kez sıralar: Bir kez ortalamaüretmek için, ve bir kez ortalama farkı nın kare ortalamasını üretmek için.</span><span class="sxs-lookup"><span data-stu-id="5a92a-226">This method enumerates the sequence twice: Once to produce the average, and once to produce the average of the square of the difference of the average.</span></span>
<span data-ttu-id="5a92a-227">(LINQ sorgularının tembelce değerlendirildiğini unutmayın, bu nedenle bu farkların ortalaması ve ortalaması arasındaki farkların hesaplanması yalnızca bir numaralandırma yapar.)</span><span class="sxs-lookup"><span data-stu-id="5a92a-227">(Remember that LINQ queries are evaluated lazily, so the computation of the differences from the mean and the average of those differences makes only one enumeration.)</span></span>

<span data-ttu-id="5a92a-228">Sıranın yalnızca bir numaralandırmasını kullanarak standart sapmayı hesaplayan alternatif bir formül vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-228">There is an alternative formula that computes standard deviation using only one enumeration of the sequence.</span></span>  <span data-ttu-id="5a92a-229">Bu hesaplama, sırayı sıralarken iki değer üretir: dizideki tüm öğelerin toplamı ve karedeki her değerin toplamı:</span><span class="sxs-lookup"><span data-stu-id="5a92a-229">This computation produces two values as it enumerates the sequence: the sum of all items in the sequence, and the sum of the each value squared:</span></span>

[!code-csharp[SumOfSquaresFormula](../../samples/snippets/csharp/tuples/statistics.cs#06_SumOfSquaresFormula "Compute Standard Deviation using the sum of squares")]

<span data-ttu-id="5a92a-230">Bu sürüm, diziyi tam olarak bir kez sıralar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-230">This version enumerates the sequence exactly once.</span></span> <span data-ttu-id="5a92a-231">Ama bu yeniden kullanılabilir bir kod değil.</span><span class="sxs-lookup"><span data-stu-id="5a92a-231">But it's not reusable code.</span></span> <span data-ttu-id="5a92a-232">Çalışmaya devam ettikçe, birçok farklı istatistiksel hesaplamanın dizideki öğe sayısını, sıranın toplamını ve sıranın karelerinin toplamını kullandığını göreceksiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-232">As you keep working, you'll find that many different statistical computations use the number of items in the sequence, the sum of the sequence, and the sum of the squares of the sequence.</span></span> <span data-ttu-id="5a92a-233">Bu yöntemi yeniden düzenlemeyi ve bu değerlerin üçünü de üreten bir yardımcı program yöntemi yazalım.</span><span class="sxs-lookup"><span data-stu-id="5a92a-233">Let's refactor this method and write a utility method that produces all three of those values.</span></span> <span data-ttu-id="5a92a-234">Her üç değer bir tuple olarak döndürülebilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-234">All three values can be returned as a tuple.</span></span>

<span data-ttu-id="5a92a-235">Numaralandırma sırasında hesaplanan üç değer bir tuple'da depolansın diye bu yöntemi güncelleştirelim.</span><span class="sxs-lookup"><span data-stu-id="5a92a-235">Let's update this method so the three values computed during the enumeration are stored in a tuple.</span></span> <span data-ttu-id="5a92a-236">Bu sürüm oluşturur:</span><span class="sxs-lookup"><span data-stu-id="5a92a-236">That creates this version:</span></span>

[!code-csharp[TupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#07_TupleVersion "Refactor to use tuples")]

<span data-ttu-id="5a92a-237">Visual Studio'nun Refactoring desteği, çekirdek istatistiklerinin işlevselliğini özel bir yönteme dönüştürmeyi kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-237">Visual Studio's Refactoring support makes it easy to extract the functionality for the core statistics into a private method.</span></span> <span data-ttu-id="5a92a-238">Bu size `private static` `Sum`üç değer , , `SumOfSquares`ve `Count`: tuple türü döndüren bir yöntem verir</span><span class="sxs-lookup"><span data-stu-id="5a92a-238">That gives you a `private static` method that returns the tuple type with the three values of `Sum`, `SumOfSquares`, and `Count`:</span></span>

[!code-csharp[TupleMethodVersion](../../samples/snippets/csharp/tuples/statistics.cs#08_TupleMethodVersion "After extracting utility method")]

<span data-ttu-id="5a92a-239">Dil, elle birkaç hızlı düzenleme yapmak istiyorsanız, kullanabileceğiniz birkaç seçenek daha sağlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-239">The language enables a couple more options that you can use, if you want to make a few quick edits by hand.</span></span> <span data-ttu-id="5a92a-240">İlk olarak, `var` `ComputeSumAndSumOfSquares` yöntem çağrısından tuple sonucunu açmak için bildirimi kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-240">First, you can use the `var` declaration to initialize the tuple result from the `ComputeSumAndSumOfSquares` method call.</span></span> <span data-ttu-id="5a92a-241">Ayrıca `ComputeSumAndSumOfSquares` yöntem içinde üç ayrı değişken oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-241">You can also create three discrete variables inside the `ComputeSumAndSumOfSquares` method.</span></span> <span data-ttu-id="5a92a-242">Son sürüm aşağıdaki kodda gösterilir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-242">The final version is shown in the following code:</span></span>

[!code-csharp[CleanedTupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#09_CleanedTupleVersion "After final cleanup")]

<span data-ttu-id="5a92a-243">Bu son sürüm, bu üç değere veya bunların herhangi bir alt kümesine ihtiyaç duyan herhangi bir yöntem için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-243">This final version can be used for any method that needs those three values, or any subset of them.</span></span>

<span data-ttu-id="5a92a-244">Dil, bu tuple returning yöntemleriöğelerinin adlarını yönetmede diğer seçenekleri destekler.</span><span class="sxs-lookup"><span data-stu-id="5a92a-244">The language supports other options in managing the names of the elements in these tuple-returning methods.</span></span>

<span data-ttu-id="5a92a-245">Alan adlarını iade değer bildiriminden kaldırabilir ve adsız bir tuple döndürebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-245">You can remove the field names from the return value declaration and return an unnamed tuple:</span></span>

```csharp
private static (double, double, int) ComputeSumAndSumOfSquares(IEnumerable<double> sequence)
{
    double sum = 0;
    double sumOfSquares = 0;
    int count = 0;

    foreach (var item in sequence)
    {
        count++;
        sum += item;
        sumOfSquares += item * item;
    }

    return (sum, sumOfSquares, count);
}
```

<span data-ttu-id="5a92a-246">Bu tuple alanları , `Item1` `Item2`, `Item3`ve .</span><span class="sxs-lookup"><span data-stu-id="5a92a-246">The fields of this tuple are named `Item1`, `Item2`, and `Item3`.</span></span>
<span data-ttu-id="5a92a-247">Yöntemlerden döndürülen tuples öğelerine anlamsal adlar sağlamanız önerilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-247">It's recommended that you provide semantic names to the elements of tuples returned from methods.</span></span>

<span data-ttu-id="5a92a-248">Tuples yararlı olabilir başka bir deyim linq sorguları yazarken.</span><span class="sxs-lookup"><span data-stu-id="5a92a-248">Another idiom where tuples can be useful is when you are authoring LINQ queries.</span></span> <span data-ttu-id="5a92a-249">Son öngörülen sonuç genellikle seçili nesnelerin özelliklerinin bazılarını içerir, ancak tümü içermez.</span><span class="sxs-lookup"><span data-stu-id="5a92a-249">The final projected result often contains some, but not all, of the properties of the objects being selected.</span></span>

<span data-ttu-id="5a92a-250">Sorgunun sonuçlarını geleneksel olarak anonim bir tür olan nesneler dizisine yansıtın.</span><span class="sxs-lookup"><span data-stu-id="5a92a-250">You would traditionally project the results of the query into a sequence of objects that were an anonymous type.</span></span> <span data-ttu-id="5a92a-251">Bu, öncelikle anonim türleri uygun bir yöntem için dönüş türünde adlandırılamaz çünkü birçok sınırlamalar sundu.</span><span class="sxs-lookup"><span data-stu-id="5a92a-251">That presented many limitations, primarily because anonymous types could not conveniently be named in the return type for a method.</span></span> <span data-ttu-id="5a92a-252">Alternatifler `object` kullanarak `dynamic` veya sonuç türü olarak önemli performans maliyetleri ile geldi.</span><span class="sxs-lookup"><span data-stu-id="5a92a-252">Alternatives using `object` or `dynamic` as the type of the result came with significant performance costs.</span></span>

<span data-ttu-id="5a92a-253">Bir tuple türünden bir sırayı döndürmek kolaydır ve öğelerin adları ve türleri derleme zamanında ve IDE araçları aracılığıyla kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-253">Returning a sequence of a tuple type is easy, and the names and types of the elements are available at compile time and through IDE tools.</span></span>
<span data-ttu-id="5a92a-254">Örneğin, bir ToDo uygulamasını düşünün.</span><span class="sxs-lookup"><span data-stu-id="5a92a-254">For example, consider a ToDo application.</span></span> <span data-ttu-id="5a92a-255">Yapılacaklar listesinde tek bir girişi temsil etmek için aşağıdakilere benzer bir sınıf tanımlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-255">You might define a class similar to the following to represent a single entry in the ToDo list:</span></span>

[!code-csharp[ToDoItem](../../samples/snippets/csharp/tuples/projectionsample.cs#14_ToDoItem "To Do Item")]

<span data-ttu-id="5a92a-256">Mobil uygulamalarınız, yalnızca başlığı görüntüleyen geçerli ToDo öğelerinin kompakt bir biçimini destekleyebilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-256">Your mobile applications may support a compact form of the current ToDo items that only displays the title.</span></span> <span data-ttu-id="5a92a-257">Bu LINQ sorgusu yalnızca kimlik ve başlık içeren bir projeksiyon yapacak.</span><span class="sxs-lookup"><span data-stu-id="5a92a-257">That LINQ query would make a projection that includes only the ID and the title.</span></span> <span data-ttu-id="5a92a-258">Tuples bir dizi döndüren bir yöntem iyi tasarım ifade eder:</span><span class="sxs-lookup"><span data-stu-id="5a92a-258">A method that returns a sequence of tuples expresses that design well:</span></span>

[!code-csharp[QueryReturningTuple](../../samples/snippets/csharp/tuples/projectionsample.cs#15_QueryReturningTuple "Query returning a tuple")]

> [!NOTE]
> <span data-ttu-id="5a92a-259">C# 7.1'de, tuple projeksiyonları, adlandırılmış tuples'ı anonim türlerdeki özellik adlandırmasına benzer bir şekilde öğeleri kullanarak oluşturmanıza olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-259">In C# 7.1, tuple projections enable you to create named tuples using elements, in a manner similar to the property naming in anonymous types.</span></span> <span data-ttu-id="5a92a-260">Yukarıdaki kodda, `select` sorgu projeksiyonundaki deyim, öğeleri `ID` olan bir `Title`tuple oluşturur ve .</span><span class="sxs-lookup"><span data-stu-id="5a92a-260">In the above code, the `select` statement in the query projection creates a tuple that has elements `ID` and `Title`.</span></span>

<span data-ttu-id="5a92a-261">Adlandırılmış tuple imzanın bir parçası olabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-261">The named tuple can be part of the signature.</span></span> <span data-ttu-id="5a92a-262">Derleyici ve IDE araçlarının sonucu doğru kullandığınızı statik olarak denetlemesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-262">It lets the compiler and IDE tools provide static checking that you are using the result correctly.</span></span> <span data-ttu-id="5a92a-263">Adlandırılmış tuple da sonuçları ile çalışmak için yansıma veya dinamik bağlama gibi pahalı çalışma süresi özellikleri kullanmaya gerek yoktur, böylece statik türü bilgileri taşır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-263">The named tuple also carries the static type information so there is no need to use expensive run time features like reflection or dynamic binding to work with the results.</span></span>

## <a name="deconstruction"></a><span data-ttu-id="5a92a-264">Yapısızlaştırma</span><span class="sxs-lookup"><span data-stu-id="5a92a-264">Deconstruction</span></span>

<span data-ttu-id="5a92a-265">Bir yöntemle döndürülen tuple'ı *dekonstrükte ederek* bir tuple'daki tüm öğeleri kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-265">You can unpackage all the items in a tuple by *deconstructing* the tuple returned by a method.</span></span> <span data-ttu-id="5a92a-266">Tuples deconstructing için üç farklı yaklaşım vardır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-266">There are three different approaches to deconstructing tuples.</span></span>  <span data-ttu-id="5a92a-267">İlk olarak, tuple'daki öğelerin her biri için ayrı değişkenler oluşturmak için parantez içindeki her alanın türünü açıkça bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-267">First, you can explicitly declare the type of each field inside parentheses to create discrete variables for each of the elements in the tuple:</span></span>

[!code-csharp[Deconstruct](../../samples/snippets/csharp/tuples/statistics.cs#10_Deconstruct "Deconstruct")]

<span data-ttu-id="5a92a-268">Ayrıca, parantez dışındaki `var` anahtar sözcüğü kullanarak bir tuple'daki her alan için örtülü olarak yazılan değişkenleri de bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-268">You can also declare implicitly typed variables for each field in a tuple by using the `var` keyword outside the parentheses:</span></span>

[!code-csharp[DeconstructToVar](../../samples/snippets/csharp/tuples/statistics.cs#11_DeconstructToVar "Deconstruct to Var")]

<span data-ttu-id="5a92a-269">Anahtar kelimeyi `var` parantez içindeki değişken bildirimlerin herhangi biriyle veya tümüyle kullanmak da yasaldır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-269">It is also legal to use the `var` keyword with any, or all of the variable declarations inside the parentheses.</span></span>

```csharp
(double sum, var sumOfSquares, var count) = ComputeSumAndSumOfSquares(sequence);
```

<span data-ttu-id="5a92a-270">Tuple'daki her alan aynı türe sahip olsa bile, parantez dışında belirli bir tür kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="5a92a-270">You cannot use a specific type outside the parentheses, even if every field in the tuple has the same type.</span></span>

<span data-ttu-id="5a92a-271">Varolan bildirimlerle tuples'ı da çözebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5a92a-271">You can deconstruct tuples with existing declarations as well:</span></span>

```csharp
public class Point
{
    public int X { get; set; }
    public int Y { get; set; }

    public Point(int x, int y) => (X, Y) = (x, y);
}
```

> [!WARNING]
> <span data-ttu-id="5a92a-272">Varolan bildirimleri parantez içindeki bildirimlerle karıştıramazsınız.</span><span class="sxs-lookup"><span data-stu-id="5a92a-272">You cannot mix existing declarations with declarations inside the parentheses.</span></span> <span data-ttu-id="5a92a-273">Örneğin, aşağıdakiizin verilmez: `(var x, y) = MyMethod();`.</span><span class="sxs-lookup"><span data-stu-id="5a92a-273">For instance, the following is not allowed: `(var x, y) = MyMethod();`.</span></span> <span data-ttu-id="5a92a-274">Bu hata CS8184 üretir, çünkü *x* parantez içinde bildirilir ve *y* daha önce başka bir yerde bildirilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-274">This produces error CS8184 because *x* is declared inside the parentheses and *y* is previously declared elsewhere.</span></span>

### <a name="deconstructing-user-defined-types"></a><span data-ttu-id="5a92a-275">Kullanıcı tanımlı türleri deconstructing</span><span class="sxs-lookup"><span data-stu-id="5a92a-275">Deconstructing user-defined types</span></span>

<span data-ttu-id="5a92a-276">Herhangi bir tuple türü yukarıda gösterildiği gibi dekonstrüktülüolabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-276">Any tuple type can be deconstructed as shown above.</span></span> <span data-ttu-id="5a92a-277">Ayrıca, kullanıcı tarafından tanımlanan herhangi bir türde (sınıflar, yapılar ve hatta arabirimler) yapısızlaştırmayı etkinleştirmek de kolaydır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-277">It's also easy to enable deconstruction on any user-defined type (classes, structs, or even interfaces).</span></span>

<span data-ttu-id="5a92a-278">Tür yazarı, türü oluşturan `Deconstruct` veri öğelerini temsil eden `out` herhangi bir sayıda değişkene değer atayabilen bir veya daha fazla yöntem tanımlayabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-278">The type author can define one or more `Deconstruct` methods that assign values to any number of `out` variables representing the data elements that make up the type.</span></span> <span data-ttu-id="5a92a-279">Örneğin, aşağıdaki `Person` tür, bir `Deconstruct` kişi nesnesini ilk adı ve soyadını temsil eden öğelere dönüştüren bir yöntem tanımlar:</span><span class="sxs-lookup"><span data-stu-id="5a92a-279">For example, the following `Person` type defines a `Deconstruct` method that deconstructs a person object into the elements representing the first name and last name:</span></span>

[!code-csharp[TypeWithDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#12_TypeWithDeconstructMethod "Type with a deconstruct method")]

<span data-ttu-id="5a92a-280">Deconstruct yöntemi, atamayı `Person` bir den iki dizeye kadar, aşağıdakileri `FirstName` ve `LastName` özellikleri temsil eden şekilde sağlar:</span><span class="sxs-lookup"><span data-stu-id="5a92a-280">The deconstruct method enables assignment from a `Person` to two strings, representing the `FirstName` and `LastName` properties:</span></span>

[!code-csharp[Deconstruct Type](../../samples/snippets/csharp/tuples/program.cs#12A_DeconstructType "Deconstruct a class type")]

<span data-ttu-id="5a92a-281">Yazmadığınız türler için bile yapısızlaştırmayı etkinleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-281">You can enable deconstruction even for types you did not author.</span></span>
<span data-ttu-id="5a92a-282">Yöntem, `Deconstruct` bir nesnenin erişilebilir veri üyelerini paketleyen bir uzantı yöntemi olabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-282">The `Deconstruct` method can be an extension method that unpackages the accessible data members of an object.</span></span> <span data-ttu-id="5a92a-283">Aşağıdaki örnekte, `Student` `Person` türden türetilen bir tür ve `Student` bir üç değişkene ayıran bir `FirstName`uzantı yöntemi gösterilmektedir, ", `LastName`" ve : `GPA`</span><span class="sxs-lookup"><span data-stu-id="5a92a-283">The example below shows a `Student` type, derived from the `Person` type, and an extension method that deconstructs a `Student` into three variables, representing the `FirstName`, the `LastName`, and the `GPA`:</span></span>

[!code-csharp[ExtensionDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#13_ExtensionDeconstructMethod "Type with a deconstruct extension method")]

<span data-ttu-id="5a92a-284">Bir `Student` nesnenin artık `Deconstruct` iki erişilebilir yöntemi `Student` vardır: türler için `Person` bildirilen uzantı yöntemi ve türün üyesi.</span><span class="sxs-lookup"><span data-stu-id="5a92a-284">A `Student` object now has two accessible `Deconstruct` methods: the extension method declared for `Student` types, and the member of the `Person` type.</span></span> <span data-ttu-id="5a92a-285">Her ikisi de kapsamdadır ve `Student` bu, a'nın iki değişken veya üç değişkene dönüştürülmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="5a92a-285">Both are in scope, and that enables a `Student` to be deconstructed into either two variables or three.</span></span>
<span data-ttu-id="5a92a-286">Bir öğrenciyi üç değişkene atarsanız, ad, soyad ve genel not ortalaması döndürülür.</span><span class="sxs-lookup"><span data-stu-id="5a92a-286">If you assign a student to three variables, the first name, last name, and GPA are all returned.</span></span> <span data-ttu-id="5a92a-287">Bir öğrenciyi iki değişkene atarsanız, yalnızca ad ve soyadı döndürülür.</span><span class="sxs-lookup"><span data-stu-id="5a92a-287">If you assign a student to two variables, only the first name and the last name are returned.</span></span>

[!code-csharp[Deconstruct extension method](../../samples/snippets/csharp/tuples/program.cs#13A_DeconstructExtension "Deconstruct a class type using an extension method")]

<span data-ttu-id="5a92a-288">Bir sınıf veya sınıf `Deconstruct` hiyerarşisinde birden çok yöntemi tanımlarken dikkatli olmalısınız.</span><span class="sxs-lookup"><span data-stu-id="5a92a-288">You should be careful defining multiple `Deconstruct` methods in a class or a class hierarchy.</span></span> <span data-ttu-id="5a92a-289">Aynı `Deconstruct` sayıda `out` parametreye sahip birden çok yöntem hızla belirsizliklere neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-289">Multiple `Deconstruct` methods that have the same number of `out` parameters can quickly cause ambiguities.</span></span> <span data-ttu-id="5a92a-290">Arayanlar istenilen `Deconstruct` yöntemi kolayca arayamayabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-290">Callers may not be able to easily call the desired `Deconstruct` method.</span></span>

<span data-ttu-id="5a92a-291">Bu örnekte, belirsiz bir arama için `Deconstruct` en `Person` az şans vardır, `Deconstruct` çünkü `Student` yöntem iki çıkış parametreye sahiptir ve yöntem üçtür.</span><span class="sxs-lookup"><span data-stu-id="5a92a-291">In this example, there is minimal chance for an ambiguous call because the `Deconstruct` method for `Person` has two output parameters, and the `Deconstruct` method for `Student` has three.</span></span>

<span data-ttu-id="5a92a-292">Dekonstrüksiyon operatörleri eşitliği test etmeye katılmaz.</span><span class="sxs-lookup"><span data-stu-id="5a92a-292">Deconstruction operators do not participate in testing equality.</span></span> <span data-ttu-id="5a92a-293">Aşağıdaki örnek derleyici hatası CS0019 oluşturur:</span><span class="sxs-lookup"><span data-stu-id="5a92a-293">The following example generates compiler error CS0019:</span></span>

```csharp
Person p = new Person("Althea", "Goodwin");
if (("Althea", "Goodwin") == p)
    Console.WriteLine(p);
```

<span data-ttu-id="5a92a-294">Yöntem `Deconstruct` nesneyi `Person` `p` iki dize içeren bir tuple dönüştürebilir, ancak eşitlik testleri bağlamında geçerli değildir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-294">The `Deconstruct` method could convert the `Person` object `p` to a tuple containing two strings, but it is not applicable in the context of equality tests.</span></span>

## <a name="tuples-as-out-parameters"></a><span data-ttu-id="5a92a-295">Çıkış parametreleri olarak Tuples</span><span class="sxs-lookup"><span data-stu-id="5a92a-295">Tuples as out parameters</span></span>

<span data-ttu-id="5a92a-296">Tuples dışarı parametreleri *kendileri*olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-296">Tuples can be used as out parameters *themselves*.</span></span> <span data-ttu-id="5a92a-297">[Yapısızlaştırma](#deconstruction) bölümünde daha önce bahsedilen herhangi bir belirsizlik ile karıştırılmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="5a92a-297">Not to be confused with any ambiguity previously mentioned in the [Deconstruction](#deconstruction) section.</span></span> <span data-ttu-id="5a92a-298">Yöntem çağrısında yalnızca tuple'ın şeklini açıklamanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="5a92a-298">In a method call, you need only describe the tuple's shape:</span></span>

[!code-csharp[TuplesAsOutParameters](~/samples/snippets/csharp/tuples/program.cs#01_TupleAsOutVariable "Tuples as out parameters")]

<span data-ttu-id="5a92a-299">Alternatif olarak, [_adsız_](#named-and-unnamed-tuples) bir tuple kullanabilirsiniz `Item1` ve `Item2`alanları bakın ve:</span><span class="sxs-lookup"><span data-stu-id="5a92a-299">Alternatively, you could use an [_unnamed_](#named-and-unnamed-tuples) tuple and refer to its fields as `Item1` and `Item2`:</span></span>

```csharp
dict.TryGetValue(2, out (int, string) pair);
// ...
Console.WriteLine($"{pair.Item1}: {pair.Item2}");
```

## <a name="conclusion"></a><span data-ttu-id="5a92a-300">Sonuç</span><span class="sxs-lookup"><span data-stu-id="5a92a-300">Conclusion</span></span>

<span data-ttu-id="5a92a-301">Adlandırılmış tuples için yeni dil ve kitaplık desteği, birden çok öğe depolayan ancak davranışları tanımlamayan, sınıflar ve yapılar gibi veri yapıları kullanan tasarımlarla çalışmayı çok daha kolay hale getirir.</span><span class="sxs-lookup"><span data-stu-id="5a92a-301">The new language and library support for named tuples makes it much easier to work with designs that use data structures that store multiple elements but do not define behavior, as classes and structs do.</span></span> <span data-ttu-id="5a92a-302">Bu tür için tuples kullanmak kolay ve özlü.</span><span class="sxs-lookup"><span data-stu-id="5a92a-302">It's easy and concise to use tuples for those types.</span></span> <span data-ttu-id="5a92a-303">Daha ayrıntılı `class` veya `struct` sözdizimi kullanarak yazar türlerine gerek kalmadan statik tür denetiminin tüm avantajlarından yararlanırsınız.</span><span class="sxs-lookup"><span data-stu-id="5a92a-303">You get all the benefits of static type checking, without needing to author types using the more verbose `class` or `struct` syntax.</span></span> <span data-ttu-id="5a92a-304">Buna rağmen, onlar en yararlı olan `private`yardımcı `internal`yöntemleri için yararlıdır , ya da .</span><span class="sxs-lookup"><span data-stu-id="5a92a-304">Even so, they are most useful for utility methods that are `private`, or `internal`.</span></span> <span data-ttu-id="5a92a-305">Ortak yöntemleriniz birden `class` çok `struct` öğeiçeren bir değer döndürdüğünde kullanıcı tanımlı türler oluşturun veya türleri oluşturun.</span><span class="sxs-lookup"><span data-stu-id="5a92a-305">Create user-defined types, either `class` or `struct` types when your public methods return a value that has multiple elements.</span></span>
