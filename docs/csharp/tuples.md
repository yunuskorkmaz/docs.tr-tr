---
title: Demet türleri- C# kılavuz
description: İçinde adlandırılmamış ve adlandırılmış demet türleri hakkında bilgi edininC#
ms.date: 05/15/2018
ms.assetid: ee8bf7c3-aa3e-4c9e-a5c6-e05cc6138baa
ms.openlocfilehash: 00330af38044b07128551b7dc74c7d831c7a5626
ms.sourcegitcommit: 6f28b709592503d27077b16fff2e2eacca569992
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/28/2019
ms.locfileid: "70105905"
---
# <a name="c-tuple-types"></a><span data-ttu-id="69673-103">C#demet türleri</span><span class="sxs-lookup"><span data-stu-id="69673-103">C# tuple types</span></span>

<span data-ttu-id="69673-104">C#tanımlama grupları, hafif bir sözdizimi kullanarak tanımladığınız türlerdir.</span><span class="sxs-lookup"><span data-stu-id="69673-104">C# tuples are types that you define using a lightweight syntax.</span></span> <span data-ttu-id="69673-105">Daha basit bir sözdizimi, sayıya göre dönüştürme kuralları (kardinalite olarak adlandırılır) ve öğe türleri ve kopyalar, eşitlik testleri ve atamalar için tutarlı kurallar vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-105">The advantages include a simpler syntax, rules for conversions based on number (referred to as cardinality) and types of elements, and consistent rules for copies, equality tests, and assignments.</span></span> <span data-ttu-id="69673-106">Zorunluluğunu getirir olarak, tanımlama grupları devralma ile ilişkili nesne odaklı bazı deyimleri desteklemez.</span><span class="sxs-lookup"><span data-stu-id="69673-106">As a tradeoff, tuples do not support some of the object-oriented idioms associated with inheritance.</span></span> <span data-ttu-id="69673-107">7,0 sürümündeki yenilikler makalesindeki tanımlama bilgileri bölümüne genel bakış alabilirsiniz. [ C# ](whats-new/csharp-7.md#tuples)</span><span class="sxs-lookup"><span data-stu-id="69673-107">You can get an overview in the section on [tuples in the What's new in C# 7.0](whats-new/csharp-7.md#tuples) article.</span></span>

<span data-ttu-id="69673-108">Bu makalede, C# 7,0 ve sonraki sürümlerde tanımlama gruplarını ve bunları kullanmanın farklı yollarını ve tanımlama grupları ile çalışmaya yönelik ilk Kılavuzu düzenleyen dil kurallarını öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-108">In this article, you'll learn the language rules governing tuples in C# 7.0 and later versions, different ways to use them, and initial guidance on working with tuples.</span></span>

> [!NOTE]
> <span data-ttu-id="69673-109">Yeni tanımlama grubu özellikleri <xref:System.ValueTuple> türleri gerektirir.</span><span class="sxs-lookup"><span data-stu-id="69673-109">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="69673-110">NuGet paketini [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) türleri içermeyen platformlarda kullanabilmek için eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="69673-110">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="69673-111">Bu, çerçeveye teslim edilen türleri kullanan diğer dil özelliklerine benzerdir.</span><span class="sxs-lookup"><span data-stu-id="69673-111">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="69673-112">Örnek olarak ,`IEnumerable<T>`arabirime ve LINQ uygulamasına bağlı olarak verilebilir. `INotifyCompletion` `async` `await`</span><span class="sxs-lookup"><span data-stu-id="69673-112">Examples include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="69673-113">Bununla birlikte, .NET daha fazla platforma bağımlı olduğu için teslim mekanizması değişiyor.</span><span class="sxs-lookup"><span data-stu-id="69673-113">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="69673-114">.NET Framework, dil derleyicisi ile aynı temposunda her zaman yollanmayabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-114">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="69673-115">Yeni dil özellikleri yeni türlere dayandığınızda, bu türler dil Özellikleri sevk edildiğinde NuGet paketleri olarak kullanılabilir olacaktır.</span><span class="sxs-lookup"><span data-stu-id="69673-115">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="69673-116">Bu yeni türler .NET Standard API 'sine eklendikçe ve Framework 'ün bir parçası olarak teslim edildiğinde, NuGet paket gereksinimi kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="69673-116">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="69673-117">Yeni demet desteği ekleme nedenlerinden başlayalım.</span><span class="sxs-lookup"><span data-stu-id="69673-117">Let's start with the reasons for adding new tuple support.</span></span> <span data-ttu-id="69673-118">Yöntemler tek bir nesne döndürür.</span><span class="sxs-lookup"><span data-stu-id="69673-118">Methods return a single object.</span></span> <span data-ttu-id="69673-119">Tanımlama grupları bu tek nesnede birden çok değeri daha kolay paketlemenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="69673-119">Tuples enable you to package multiple values in that single object more easily.</span></span>

<span data-ttu-id="69673-120">.NET Framework zaten genel `Tuple` sınıflar içeriyor.</span><span class="sxs-lookup"><span data-stu-id="69673-120">The .NET Framework already has generic `Tuple` classes.</span></span> <span data-ttu-id="69673-121">Ancak, bu sınıfların iki önemli sınırlaması vardı.</span><span class="sxs-lookup"><span data-stu-id="69673-121">These classes, however, had two major limitations.</span></span> <span data-ttu-id="69673-122">Bunlardan biri `Tuple` için, `Item1` `Item2`özellikleri, vb. olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="69673-122">For one, the `Tuple` classes named their properties `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="69673-123">Bu adlar hiçbir anlam bilgisi içermez.</span><span class="sxs-lookup"><span data-stu-id="69673-123">Those names carry no semantic information.</span></span> <span data-ttu-id="69673-124">Bu `Tuple` türlerin kullanılması, özelliklerin her birinin anlamını karşılayarak iletişim kurmasına imkan vermez.</span><span class="sxs-lookup"><span data-stu-id="69673-124">Using these `Tuple` types does not enable communicating the meaning of each of the properties.</span></span> <span data-ttu-id="69673-125">Yeni dil özellikleri, bir tanımlama grubu içindeki öğeler için anlamsal anlamlı adlar bildirme ve kullanma imkanı sağlar.</span><span class="sxs-lookup"><span data-stu-id="69673-125">The new language features enable you to declare and use semantically meaningful names for the elements in a tuple.</span></span>

<span data-ttu-id="69673-126">Sınıflar `Tuple` , başvuru türleri olduklarından daha fazla performans sorunlarına neden olur.</span><span class="sxs-lookup"><span data-stu-id="69673-126">The `Tuple` classes cause more performance concerns because they are reference types.</span></span> <span data-ttu-id="69673-127">`Tuple` Türlerden birini kullanmak nesneleri ayırmayı gösterir.</span><span class="sxs-lookup"><span data-stu-id="69673-127">Using one of the `Tuple` types means allocating objects.</span></span> <span data-ttu-id="69673-128">Etkin yollarda birçok küçük nesne ayırmak uygulamanızın performansı üzerinde ölçülebilir bir etkiye sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-128">On hot paths, allocating many small objects can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="69673-129">Bu nedenle, tanımlama grupları için dil desteği yeni `ValueTuple` yapıları kullanır.</span><span class="sxs-lookup"><span data-stu-id="69673-129">Therefore, the language support for tuples leverages the new `ValueTuple` structs.</span></span>

<span data-ttu-id="69673-130">Bu eksiklikleri önlemek için, birden çok öğe taşımak `class` `struct` üzere bir veya oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-130">To avoid those deficiencies, you could create a `class` or a `struct` to carry multiple elements.</span></span> <span data-ttu-id="69673-131">Ne yazık ki, sizin için daha fazla çalışma ve tasarım amacınızı gizler.</span><span class="sxs-lookup"><span data-stu-id="69673-131">Unfortunately, that's more work for you, and it obscures your design intent.</span></span> <span data-ttu-id="69673-132">`struct` Ya da`class` hem veri hem de davranışla bir tür tanımladığınız anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="69673-132">Making a `struct` or `class` implies that you are defining a type with both data and behavior.</span></span> <span data-ttu-id="69673-133">Birçok kez yalnızca birden çok değeri tek bir nesnede depolamak istiyorsunuz.</span><span class="sxs-lookup"><span data-stu-id="69673-133">Many times, you simply want to store multiple values in a single object.</span></span>

<span data-ttu-id="69673-134">Dil özellikleri ve `ValueTuple` genel yapılar, bu demet türlerine herhangi bir davranış (Yöntem) ekleyemeyeceği kuralını zorlar.</span><span class="sxs-lookup"><span data-stu-id="69673-134">The language features and the `ValueTuple` generic structs enforce the rule that you cannot add any behavior (methods) to these tuple types.</span></span>
<span data-ttu-id="69673-135">Tüm türler *değişebilir yapılarda oluşturulur.* `ValueTuple`</span><span class="sxs-lookup"><span data-stu-id="69673-135">All the `ValueTuple` types are *mutable structs*.</span></span> <span data-ttu-id="69673-136">Her üye alanı bir ortak alandır.</span><span class="sxs-lookup"><span data-stu-id="69673-136">Each member field is a public field.</span></span> <span data-ttu-id="69673-137">Böylece çok hafif hale gelir.</span><span class="sxs-lookup"><span data-stu-id="69673-137">That makes them very lightweight.</span></span> <span data-ttu-id="69673-138">Bununla birlikte, bu, her ne kadar önemli olduğu durumlarda başlıkların kullanılması gerektiği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="69673-138">However, that means tuples should not be used where immutability is important.</span></span>

<span data-ttu-id="69673-139">Tanımlama grupları, ve `class` `struct` türlerine göre daha basit ve daha esnek veri kapsayıcılarıdır.</span><span class="sxs-lookup"><span data-stu-id="69673-139">Tuples are both simpler and more flexible data containers than `class` and `struct` types.</span></span> <span data-ttu-id="69673-140">Bu farklılıkları keşfedelim.</span><span class="sxs-lookup"><span data-stu-id="69673-140">Let's explore those differences.</span></span>

## <a name="named-and-unnamed-tuples"></a><span data-ttu-id="69673-141">Adlandırılmış ve adlandırılmamış diziler</span><span class="sxs-lookup"><span data-stu-id="69673-141">Named and unnamed tuples</span></span>

<span data-ttu-id="69673-142">`Item2` `Item1` `Tuple` Yapının,,, vb. adlı `Item3`alanları var olan türlerde tanımlanan özelliklere benzer şekilde. `ValueTuple`</span><span class="sxs-lookup"><span data-stu-id="69673-142">The `ValueTuple` struct has fields named `Item1`, `Item2`, `Item3`, and so on, similar to the properties defined in the existing `Tuple` types.</span></span>
<span data-ttu-id="69673-143">Bu adlar, *adlandırılmamış tanımlama grupları*için kullanabileceğiniz tek adlardır.</span><span class="sxs-lookup"><span data-stu-id="69673-143">These names are the only names you can use for *unnamed tuples*.</span></span> <span data-ttu-id="69673-144">Bir tanımlama grubu için alternatif alan adı sağlamadığınızda, adlandırılmamış bir tanımlama grubu oluşturdunuz:</span><span class="sxs-lookup"><span data-stu-id="69673-144">When you do not provide any alternative field names to a tuple, you've created an unnamed tuple:</span></span>

[!code-csharp[UnnamedTuple](../../samples/snippets/csharp/tuples/program.cs#01_UnNamedTuple "Unnamed tuple")]

<span data-ttu-id="69673-145">Önceki örnekteki tanımlama grubu, değişmez sabitler kullanılarak başlatılmış ve 7,1 içinde C# *demet alan adı tahminleri* kullanılarak oluşturulan öğe adlarına sahip olmayacaktır.</span><span class="sxs-lookup"><span data-stu-id="69673-145">The tuple in the previous example was initialized using literal constants and won't have element names created using *tuple field name projections* in C# 7.1.</span></span>

<span data-ttu-id="69673-146">Ancak, bir tanımlama grubu başlattığınızda, her bir alana daha iyi adlar veren yeni dil özellikleri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-146">However, when you initialize a tuple, you can use new language features that give better names to each field.</span></span> <span data-ttu-id="69673-147">Bunun yapılması adlandırılmış bir *tanımlama grubu*oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69673-147">Doing so creates a *named tuple*.</span></span>
<span data-ttu-id="69673-148">Adlandırılmış tanımlama gruplarının hala `Item1` `Item2` `Item3` ,, vb. adlı öğeleri vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-148">Named tuples still have elements named `Item1`, `Item2`, `Item3` and so on.</span></span>
<span data-ttu-id="69673-149">Ancak, adlandırmış olduğunuz öğelerin herhangi biri için eş anlamlıları da vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-149">But they also have synonyms for any of those elements that you have named.</span></span>
<span data-ttu-id="69673-150">Her öğe için ad belirterek adlandırılmış bir tanımlama grubu oluşturursunuz.</span><span class="sxs-lookup"><span data-stu-id="69673-150">You create a named tuple by specifying the names for each element.</span></span> <span data-ttu-id="69673-151">Bir yol, kayıt kümesi başlatmasının parçası olarak adları belirtmektir:</span><span class="sxs-lookup"><span data-stu-id="69673-151">One way is to specify the names as part of the tuple initialization:</span></span>

[!code-csharp[NamedTuple](../../samples/snippets/csharp/tuples/program.cs#02_NamedTuple "Named tuple")]

<span data-ttu-id="69673-152">Adlandırılmış tanımlama gruplarını etkin bir şekilde kullanabilmeniz için bu eş anlamlılar derleyici ve dil tarafından işlenir.</span><span class="sxs-lookup"><span data-stu-id="69673-152">These synonyms are handled by the compiler and the language so that you can use named tuples effectively.</span></span> <span data-ttu-id="69673-153">Ides ve düzenleyiciler, Roslyn API 'Lerini kullanarak bu anlam adlarını okuyabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-153">IDEs and editors can read these semantic names using the Roslyn APIs.</span></span> <span data-ttu-id="69673-154">Adlandırılmış bir tanımlama grubunun öğelerine aynı derlemenin herhangi bir yerindeki anlam adlarıyla başvurabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-154">You can reference the elements of a named tuple by those semantic names anywhere in the same assembly.</span></span> <span data-ttu-id="69673-155">Derleyici, derlenmiş çıktıyı oluştururken `Item*` eşdeğerleriyle tanımladığınız adların yerini alır.</span><span class="sxs-lookup"><span data-stu-id="69673-155">The compiler replaces the names you've defined with `Item*` equivalents when generating the compiled output.</span></span> <span data-ttu-id="69673-156">Derlenen Microsoft ara dili (MSIL), bu öğeleri verdiğiniz adları içermez.</span><span class="sxs-lookup"><span data-stu-id="69673-156">The compiled Microsoft Intermediate Language (MSIL) does not include the names you've given these elements.</span></span>

<span data-ttu-id="69673-157">7,1 ' C# den başlayarak, kayıt düzeni için alan adları, kayıt düzeni başlatmak için kullanılan değişkenlerden bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-157">Beginning with C# 7.1, the field names for a tuple may be provided from the variables used to initialize the tuple.</span></span> <span data-ttu-id="69673-158">Bu, **[demet yansıtma başlatıcıları](#tuple-projection-initializers)** olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="69673-158">This is referred to as **[tuple projection initializers](#tuple-projection-initializers)**.</span></span> <span data-ttu-id="69673-159">Aşağıdaki kod öğeleriyle `count` (bir tamsayı) `accumulation` ve `sum` (bir Double) adlı bir tanımlama grubu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69673-159">The following code creates a tuple named `accumulation` with elements `count` (an integer), and `sum` (a double).</span></span>

[!code-csharp[ProjectedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectedTupleNames "Named tuple")]

<span data-ttu-id="69673-160">Derleyici, ortak Yöntemler veya özelliklerden döndürülen tanımlama grupları için oluşturduğunuz adları iletmelidir.</span><span class="sxs-lookup"><span data-stu-id="69673-160">The compiler must communicate those names you created for tuples that are returned from public methods or properties.</span></span> <span data-ttu-id="69673-161">Bu durumlarda, derleyici yöntemine bir <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> öznitelik ekler.</span><span class="sxs-lookup"><span data-stu-id="69673-161">In those cases, the compiler adds a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> attribute on the method.</span></span> <span data-ttu-id="69673-162">Bu öznitelik, kayıt <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> grubundaki her öğeye verilen adları içeren bir List özelliği içerir.</span><span class="sxs-lookup"><span data-stu-id="69673-162">This attribute contains a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> list property that contains the names given to each of the elements in the tuple.</span></span>

> [!NOTE]
> <span data-ttu-id="69673-163">Visual Studio gibi geliştirme araçları da bu meta verileri okur ve meta veri alanı adlarını kullanarak IntelliSense ve diğer özellikleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="69673-163">Development Tools, such as Visual Studio, also read that metadata, and provide IntelliSense and other features using the metadata field names.</span></span>

<span data-ttu-id="69673-164">Adlandırılmış tanımlama gruplarını birbirlerine atamaya yönelik kuralları anlamak için yeni tanımlama bilgileri ve `ValueTuple` türün temel temellerini anlamak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="69673-164">It is important to understand these underlying fundamentals of the new tuples and the `ValueTuple` type in order to understand the rules for assigning named tuples to each other.</span></span>

## <a name="tuple-projection-initializers"></a><span data-ttu-id="69673-165">Demet projeksiyon başlatıcıları</span><span class="sxs-lookup"><span data-stu-id="69673-165">Tuple projection initializers</span></span>

<span data-ttu-id="69673-166">Genel olarak, demet yansıtma başlatıcıları, bir demet başlatma bildiriminin sağ tarafındaki değişken veya alan adlarını kullanarak çalışır.</span><span class="sxs-lookup"><span data-stu-id="69673-166">In general, tuple projection initializers work by using the variable or field names from the right-hand side of a tuple initialization statement.</span></span>
<span data-ttu-id="69673-167">Açık bir ad verilirse, bu, yansıtılan herhangi bir adın önüne geçer.</span><span class="sxs-lookup"><span data-stu-id="69673-167">If an explicit name is given, that takes precedence over any projected name.</span></span> <span data-ttu-id="69673-168">Örneğin, `explicitFieldOne` aşağıdaki başlatıcıda öğeler ve `explicitFieldTwo`değil `localVariableOne` `localVariableTwo`, ve:</span><span class="sxs-lookup"><span data-stu-id="69673-168">For example, in the following initializer, the elements are `explicitFieldOne` and `explicitFieldTwo`, not `localVariableOne` and `localVariableTwo`:</span></span>

[!code-csharp[ExplicitNamedTuple](../../samples/snippets/csharp/tuples/program.cs#ProjectionExample_Explicit "Explicitly named tuple")]

<span data-ttu-id="69673-169">Açık bir adın sağlanmadığı herhangi bir alan için, geçerli bir örtük ad yansıtıldır.</span><span class="sxs-lookup"><span data-stu-id="69673-169">For any field where an explicit name is not provided, an applicable implicit name is projected.</span></span> <span data-ttu-id="69673-170">Açıkça veya örtük olarak anlamsal adlar sağlama gereksinimi yoktur.</span><span class="sxs-lookup"><span data-stu-id="69673-170">There is no requirement to provide semantic names, either explicitly or implicitly.</span></span> <span data-ttu-id="69673-171">Aşağıdaki başlatıcıda `42` değeri olan ve `Item1` `stringContent`değeri "her şeye yanıt" olan alan adlarına sahiptir:</span><span class="sxs-lookup"><span data-stu-id="69673-171">The following initializer has     field names `Item1`, whose value is `42` and `stringContent`, whose value is "The answer to everything":</span></span>

[!code-csharp[MixedTuple](../../samples/snippets/csharp/tuples/program.cs#MixedTuple "mixed tuple")]

<span data-ttu-id="69673-172">Aday alan adlarının demet alanı üzerinde yansıtılmamaları gereken iki koşul vardır:</span><span class="sxs-lookup"><span data-stu-id="69673-172">There are two conditions where candidate field names are not projected onto the tuple field:</span></span>

1. <span data-ttu-id="69673-173">Aday adı, ayrılmış bir tanımlama grubu adı olduğunda.</span><span class="sxs-lookup"><span data-stu-id="69673-173">When the candidate name is a reserved tuple name.</span></span> <span data-ttu-id="69673-174">Örnekler şunlardır `Item3`. `ToString`</span><span class="sxs-lookup"><span data-stu-id="69673-174">Examples include `Item3`, `ToString`.</span></span> <span data-ttu-id="69673-175">veya `Rest`.</span><span class="sxs-lookup"><span data-stu-id="69673-175">or `Rest`.</span></span>
1. <span data-ttu-id="69673-176">Aday adı, başka bir demet alan adının bir yinelemesi olduğunda açık veya kapalı olur.</span><span class="sxs-lookup"><span data-stu-id="69673-176">When the candidate name is a duplicate of another tuple field name, either explicit or implicit.</span></span>

<span data-ttu-id="69673-177">Bu koşullar belirsizlik kullanmaktan kaçının.</span><span class="sxs-lookup"><span data-stu-id="69673-177">These conditions avoid ambiguity.</span></span> <span data-ttu-id="69673-178">Bu adlar, bir tanımlama grubu içindeki bir alanın alan adları olarak kullanıldıklarında belirsizliğe neden olur.</span><span class="sxs-lookup"><span data-stu-id="69673-178">These names would cause an ambiguity if they were used as the field names for a field in a tuple.</span></span> <span data-ttu-id="69673-179">Bu koşullardan hiçbiri derleme zamanı hatalarına neden olur.</span><span class="sxs-lookup"><span data-stu-id="69673-179">Neither of these conditions cause compile-time errors.</span></span> <span data-ttu-id="69673-180">Bunun yerine, yansıtılan adlara sahip öğeler kendileri için öngörülen semantik adlara sahip değildir.</span><span class="sxs-lookup"><span data-stu-id="69673-180">Instead, the elements without projected names do not have semantic names projected for them.</span></span>  <span data-ttu-id="69673-181">Aşağıdaki örneklerde bu koşullar gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="69673-181">The following examples demonstrate these conditions:</span></span>

[!code-csharp-interactive[Ambiguity](../../samples/snippets/csharp/tuples/program.cs#ProjectionAmbiguities "tuples where projections are not performed")]

<span data-ttu-id="69673-182">Bu durumlar derleyici hatalarına neden olmaz çünkü demet alan adı projeksiyonu kullanılabilir olmadığında, 7,0 ile C# yazılan koda yönelik bir değişiklik olacaktır.</span><span class="sxs-lookup"><span data-stu-id="69673-182">These situations do not cause compiler errors because that would be a breaking change for code written with C# 7.0, when tuple field name projections were not available.</span></span>

## <a name="equality-and-tuples"></a><span data-ttu-id="69673-183">Eşitlik ve tanımlama grupları</span><span class="sxs-lookup"><span data-stu-id="69673-183">Equality and tuples</span></span>

<span data-ttu-id="69673-184">7,3 ' C# den başlayarak demet türleri `==` ve `!=` işleçlerini destekler.</span><span class="sxs-lookup"><span data-stu-id="69673-184">Beginning with C# 7.3, tuple types support the `==` and `!=` operators.</span></span> <span data-ttu-id="69673-185">Bu işleçler, sol bağımsız değişkenin her bir üyesini sırasıyla doğru bağımsız değişkenin her bir üyesiyle karşılaştırarak çalışır.</span><span class="sxs-lookup"><span data-stu-id="69673-185">These operators work by comparing each member of the left argument to each member of the right argument in order.</span></span> <span data-ttu-id="69673-186">Bu karşılaştırmalar kısa devre.</span><span class="sxs-lookup"><span data-stu-id="69673-186">These comparisons short-circuit.</span></span> <span data-ttu-id="69673-187">Bir çift eşit olmadığı anda üyelerin değerlendirilmesi durdurulur.</span><span class="sxs-lookup"><span data-stu-id="69673-187">They will stop evaluating members as soon as one pair is not equal.</span></span> <span data-ttu-id="69673-188">Aşağıdaki kod örnekleri kullanılır `==`, ancak tüm karşılaştırma kuralları için `!=`geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="69673-188">The following code examples use `==`, but the comparison rules all apply to `!=`.</span></span> <span data-ttu-id="69673-189">Aşağıdaki kod örneği iki tamsayı çifti için bir eşitlik karşılaştırması gösterir:</span><span class="sxs-lookup"><span data-stu-id="69673-189">The following code example shows an equality comparison for two pairs of integers:</span></span>

[!code-csharp-interactive[TupleEquality](../../samples/snippets/csharp/tuples/program.cs#Equality "Testing tuples for equality")]

<span data-ttu-id="69673-190">Demet eşitlik testlerini daha uygun hale getirmek için çeşitli kurallar vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-190">There are several rules that make tuple equality tests more convenient.</span></span> <span data-ttu-id="69673-191">Tanımlama grubu eşitliği, aşağıdaki kodda gösterildiği gibi, tanımlama gruplarının biri null yapılabilir bir tanımlama grubu ise [yükseltilmemiş dönüştürmeleri](~/_csharplang/spec/conversions.md#lifted-conversion-operators) gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="69673-191">Tuple equality performs [lifted conversions](~/_csharplang/spec/conversions.md#lifted-conversion-operators) if one of the tuples is a nullable tuple, as shown in the following code:</span></span>

[!code-csharp-interactive[NullableTupleEquality](../../samples/snippets/csharp/tuples/program.cs#NullableEquality "Comparing Tuples and nullable tuples")]

<span data-ttu-id="69673-192">Tanımlama grubu eşitliği, her iki başlığın de her bir üyesinde örtük dönüştürmeler de gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="69673-192">Tuple equality also performs implicit conversions on each member of both tuples.</span></span> <span data-ttu-id="69673-193">Bunlar, yükseltilmemiş dönüştürmeleri, genişleyen dönüşümler veya diğer örtük dönüştürmeler içerir.</span><span class="sxs-lookup"><span data-stu-id="69673-193">These include lifted conversions, widening conversions, or other implicit conversions.</span></span> <span data-ttu-id="69673-194">Aşağıdaki örneklerde, tamsayı 2 kayıt düzeninin, tamsayıdan Long 'a örtük dönüştürme nedeniyle uzun 2 tanımlama grubu ile karşılaştırılabilmesinin gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="69673-194">The following examples show that an integer 2-tuple can be compared to a long 2-tuple because of the implicit conversion from integer to long:</span></span>

[!code-csharp-interactive[SnippetMemberConversions](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberConversions "converting tuples for equality tests")]

<span data-ttu-id="69673-195">Demet üyelerinin adları, eşitlik için testlere katılmaz.</span><span class="sxs-lookup"><span data-stu-id="69673-195">The names of the tuple members do not participate in tests for equality.</span></span> <span data-ttu-id="69673-196">Ancak, işlenenden biri açık adlara sahip bir demet sabit değeri ise, bu adlar diğer işlenenin adlarıyla eşleşmezse, derleyici uyarı CS8383 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69673-196">However, if one of the operands is a tuple literal with explicit names, the compiler generates warning CS8383 if those names do not match the names of the other operand.</span></span>
<span data-ttu-id="69673-197">Her iki işlenenin de demet sabit değerleri olduğu durumlarda, aşağıdaki örnekte gösterildiği gibi uyarı sağ işlenende olur:</span><span class="sxs-lookup"><span data-stu-id="69673-197">In the case where both operands are tuple literals, the warning is on the right operand as shown in the following example:</span></span>

[!code-csharp-interactive[MemberNames](../../samples/snippets/csharp/tuples/program.cs#SnippetMemberNames "Tuple member names do not participate in equality tests")]

<span data-ttu-id="69673-198">Son olarak, tanımlama grupları iç içe diziler içerebilir.</span><span class="sxs-lookup"><span data-stu-id="69673-198">Finally, tuples may contain nested tuples.</span></span> <span data-ttu-id="69673-199">Tanımlama grubu eşitliği, aşağıdaki örnekte gösterildiği gibi, iç içe geçmiş diziler aracılığıyla her bir işlenenin "şeklini" karşılaştırır:</span><span class="sxs-lookup"><span data-stu-id="69673-199">Tuple equality compares the "shape" of each operand through nested tuples as shown in the following example:</span></span>

[!code-csharp-interactive[NestedTuples](../../samples/snippets/csharp/tuples/program.cs#SnippetNestedTuples "Tuples may contain nested tuples that participate in tuple equality.")]

<span data-ttu-id="69673-200">Farklı şekilleri olduğunda, eşitlik (veya eşitsizlik) için iki tanımlama grubunu karşılaştırmak üzere bir derleme zamanı hatası.</span><span class="sxs-lookup"><span data-stu-id="69673-200">It's a compile time error to compare two tuples for equality (or inequality) when they have different shapes.</span></span> <span data-ttu-id="69673-201">Derleyici, iç içe geçmiş tanımlama gruplarının kendisini karşılaştırmak için denenmez.</span><span class="sxs-lookup"><span data-stu-id="69673-201">The compiler won't attempt any deconstruction of nested tuples in order to compare them.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="69673-202">Atama ve tanımlama grupları</span><span class="sxs-lookup"><span data-stu-id="69673-202">Assignment and tuples</span></span>

<span data-ttu-id="69673-203">Dil, her bir sağ taraftaki öğenin örtük olarak karşılık gelen sol taraftaki öğesine dönüştürülebileceği, aynı sayıda öğeye sahip demet türleri arasında atamayı destekler.</span><span class="sxs-lookup"><span data-stu-id="69673-203">The language supports assignment between tuple types that have the same number of elements, where each right-hand side element can be implicitly converted to its corresponding left-hand side element.</span></span> <span data-ttu-id="69673-204">Diğer dönüşümler atamalar için kabul edilmez.</span><span class="sxs-lookup"><span data-stu-id="69673-204">Other conversions aren't considered for assignments.</span></span> <span data-ttu-id="69673-205">Farklı şekillerde bir tanımlama grubu atamak için bir derleme zamanı hatası vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-205">It's a compile time error to assign one tuple to another when they have different shapes.</span></span> <span data-ttu-id="69673-206">Derleyici, iç içe geçmiş tanımlama gruplarının atamasını atamak için denenmez.</span><span class="sxs-lookup"><span data-stu-id="69673-206">The compiler won't attempt any deconstruction of nested tuples in order to assign them.</span></span>
<span data-ttu-id="69673-207">Demet türleri arasında izin verilen atama türlerine bakalım.</span><span class="sxs-lookup"><span data-stu-id="69673-207">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="69673-208">Aşağıdaki örneklerde kullanılan değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="69673-208">Consider these variables used in the following examples:</span></span>

[!code-csharp[VariableCreation](../../samples/snippets/csharp/tuples/program.cs#03_VariableCreation "Variable creation")]

<span data-ttu-id="69673-209">İlk iki değişken, `unnamed` ve `anonymous` öğeleri için belirtilen semantik adlara sahip değildir.</span><span class="sxs-lookup"><span data-stu-id="69673-209">The first two variables, `unnamed` and `anonymous` do not have semantic names provided for the elements.</span></span> <span data-ttu-id="69673-210">Alan adları ve ' `Item1` `Item2`dir.</span><span class="sxs-lookup"><span data-stu-id="69673-210">The field names are `Item1` and `Item2`.</span></span>
<span data-ttu-id="69673-211">Son iki değişken `named` ve `differentName` öğeler için belirtilen semantik adlara sahip.</span><span class="sxs-lookup"><span data-stu-id="69673-211">The last two variables, `named` and `differentName` have semantic names given for the elements.</span></span> <span data-ttu-id="69673-212">Bu iki başlık, öğeler için farklı adlara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="69673-212">These two tuples have different names for the elements.</span></span>

<span data-ttu-id="69673-213">Bu başlıkların dördü, aynı sayıda öğeye sahiptir (' kardinalite ' olarak adlandırılır) ve bu öğelerin türleri aynıdır.</span><span class="sxs-lookup"><span data-stu-id="69673-213">All four of these tuples have the same number of elements (referred to as 'cardinality') and the types of those elements are identical.</span></span> <span data-ttu-id="69673-214">Bu nedenle, bu atamaların hepsi çalışır:</span><span class="sxs-lookup"><span data-stu-id="69673-214">Therefore, all of these assignments work:</span></span>

[!code-csharp[VariableAssignment](../../samples/snippets/csharp/tuples/program.cs#04_VariableAssignment "Variable assignment")]

<span data-ttu-id="69673-215">Başlıkların adlarının atanmadığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="69673-215">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="69673-216">Öğelerin değerleri, kayıt düzeni içindeki öğelerin sırasını izleyerek atanır.</span><span class="sxs-lookup"><span data-stu-id="69673-216">The values of the elements are assigned following the order of the elements in the tuple.</span></span>

<span data-ttu-id="69673-217">Farklı türlerin veya öğe sayılarının başlıkları atanamaz:</span><span class="sxs-lookup"><span data-stu-id="69673-217">Tuples of different types or numbers of elements are not assignable:</span></span>

```csharp
// Does not compile.
// CS0029: Cannot assign Tuple(int,int,int) to Tuple(int, string)
var differentShape = (1, 2, 3);
named = differentShape;
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="69673-218">Yöntem dönüş değerleri olarak tanımlama grubu</span><span class="sxs-lookup"><span data-stu-id="69673-218">Tuples as method return values</span></span>

<span data-ttu-id="69673-219">Tanımlama grupları için en yaygın kullanımdan biri yöntem dönüş değeri olarak belirlenir.</span><span class="sxs-lookup"><span data-stu-id="69673-219">One of the most common uses for tuples is as a method return value.</span></span> <span data-ttu-id="69673-220">Bir örnek adım adım inceleyelim.</span><span class="sxs-lookup"><span data-stu-id="69673-220">Let's walk through one example.</span></span> <span data-ttu-id="69673-221">Bir dizi sayı için standart sapmayı hesaplayan bu yöntemi göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="69673-221">Consider this method that computes the standard deviation for a sequence of numbers:</span></span>

[!code-csharp[StandardDeviation](../../samples/snippets/csharp/tuples/statistics.cs#05_StandardDeviation "Compute Standard Deviation")]

> [!NOTE]
> <span data-ttu-id="69673-222">Bu örnekler düzeltilmeyen örnek standart sapmayı hesaplar.</span><span class="sxs-lookup"><span data-stu-id="69673-222">These examples compute the uncorrected sample standard deviation.</span></span>
> <span data-ttu-id="69673-223">Düzeltilen örnek standart sapma formülü, `Average` iç kare farklarının toplamını, genişletme yöntemi olduğu gibi n yerine, her ne kadar (n-1) olacak şekilde böler.</span><span class="sxs-lookup"><span data-stu-id="69673-223">The corrected sample standard deviation formula would divide the sum of the squared differences from the mean by (N-1) instead of N, as the `Average` extension method does.</span></span> <span data-ttu-id="69673-224">Standart sapma için bu formüller arasındaki farklılıklar hakkında daha fazla ayrıntı için bir istatistik metnine danışın.</span><span class="sxs-lookup"><span data-stu-id="69673-224">Consult a statistics text for more details on the differences between these formulas for standard deviation.</span></span>

<span data-ttu-id="69673-225">Yukarıdaki kod, standart sapma için textbook formülünü izler.</span><span class="sxs-lookup"><span data-stu-id="69673-225">The preceding code follows the textbook formula for the standard deviation.</span></span> <span data-ttu-id="69673-226">Doğru yanıtı üretir ancak bu, verimsiz bir uygulama.</span><span class="sxs-lookup"><span data-stu-id="69673-226">It produces the correct answer, but it's an inefficient implementation.</span></span> <span data-ttu-id="69673-227">Bu yöntem, diziyi iki kez numaralandırır: Ortalama olarak bir kez ve ortalamanın farkının ortalamasının ortalamasını üretmek için bir kez.</span><span class="sxs-lookup"><span data-stu-id="69673-227">This method enumerates the sequence twice: Once to produce the average, and once to produce the average of the square of the difference of the average.</span></span>
<span data-ttu-id="69673-228">(LINQ sorgularının geç değerlendirildiğini unutmayın. bu nedenle, ortalama farklar ve bu farkların ortalaması yalnızca bir numaralandırma yapar.)</span><span class="sxs-lookup"><span data-stu-id="69673-228">(Remember that LINQ queries are evaluated lazily, so the computation of the differences from the mean and the average of those differences makes only one enumeration.)</span></span>

<span data-ttu-id="69673-229">Dizinin yalnızca bir listesini kullanarak standart sapmayı hesaplayan alternatif bir formül vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-229">There is an alternative formula that computes standard deviation using only one enumeration of the sequence.</span></span>  <span data-ttu-id="69673-230">Bu hesaplama, diziyi numaralandırdığından iki değer üretir: dizideki tüm öğelerin toplamı ve her bir değerin kare toplamı:</span><span class="sxs-lookup"><span data-stu-id="69673-230">This computation produces two values as it enumerates the sequence: the sum of all items in the sequence, and the sum of the each value squared:</span></span>

[!code-csharp[SumOfSquaresFormula](../../samples/snippets/csharp/tuples/statistics.cs#06_SumOfSquaresFormula "Compute Standard Deviation using the sum of squares")]

<span data-ttu-id="69673-231">Bu sürüm, diziyi tam olarak bir kez numaralandırır.</span><span class="sxs-lookup"><span data-stu-id="69673-231">This version enumerates the sequence exactly once.</span></span> <span data-ttu-id="69673-232">Ancak yeniden kullanılabilir kod değildir.</span><span class="sxs-lookup"><span data-stu-id="69673-232">But it's not reusable code.</span></span> <span data-ttu-id="69673-233">Çalışmaya devam ederseniz, birçok farklı istatistiksel hesaplamaların dizideki öğelerin sayısını, sıranın toplamını ve dizi karelerinin toplamını kullanmasını göreceksiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-233">As you keep working, you'll find that many different statistical computations use the number of items in the sequence, the sum of the sequence, and the sum of the squares of the sequence.</span></span> <span data-ttu-id="69673-234">Bu yöntemi yeniden düzenleme ve bu değerlerin üçünü de üreten bir yardımcı program yöntemi yazma.</span><span class="sxs-lookup"><span data-stu-id="69673-234">Let's refactor this method and write a utility method that produces all three of those values.</span></span> <span data-ttu-id="69673-235">Üç değer de bir tanımlama grubu olarak döndürülebilir.</span><span class="sxs-lookup"><span data-stu-id="69673-235">All three values can be returned as a tuple.</span></span>

<span data-ttu-id="69673-236">Bu yöntemi, numaralandırma sırasında hesaplanan üç değerin bir kayıt düzeninde depolanması için güncelleştirelim.</span><span class="sxs-lookup"><span data-stu-id="69673-236">Let's update this method so the three values computed during the enumeration are stored in a tuple.</span></span> <span data-ttu-id="69673-237">Bu sürümü oluşturur:</span><span class="sxs-lookup"><span data-stu-id="69673-237">That creates this version:</span></span>

[!code-csharp[TupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#07_TupleVersion "Refactor to use tuples")]

<span data-ttu-id="69673-238">Visual Studio 'nun yeniden düzenleme desteği, çekirdek istatistik işlevlerinin özel bir yönteme ayıklanabilmesini kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="69673-238">Visual Studio's Refactoring support makes it easy to extract the functionality for the core statistics into a private method.</span></span> <span data-ttu-id="69673-239">Bu,, ve `private static` `SumOfSquares` `Sum` '`Count`nin üç değeri ile demet türünü döndüren bir yöntem sunar:</span><span class="sxs-lookup"><span data-stu-id="69673-239">That gives you a `private static` method that returns the tuple type with the three values of `Sum`, `SumOfSquares`, and `Count`:</span></span>

[!code-csharp[TupleMethodVersion](../../samples/snippets/csharp/tuples/statistics.cs#08_TupleMethodVersion "After extracting utility method")]
 
<span data-ttu-id="69673-240">Dil, el ile birkaç hızlı düzenleme yapmak istiyorsanız kullanabileceğiniz birkaç seçenek sağlar.</span><span class="sxs-lookup"><span data-stu-id="69673-240">The language enables a couple more options that you can use, if you want to make a few quick edits by hand.</span></span> <span data-ttu-id="69673-241">İlk olarak, `var` bildirimi kullanarak `ComputeSumAndSumOfSquares` yöntem çağrısından kayıt düzeni sonucunu başlatabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-241">First, you can use the `var` declaration to initialize the tuple result from the `ComputeSumAndSumOfSquares` method call.</span></span> <span data-ttu-id="69673-242">Ayrıca, `ComputeSumAndSumOfSquares` yöntemi içinde üç ayrı değişken oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-242">You can also create three discrete variables inside the `ComputeSumAndSumOfSquares` method.</span></span> <span data-ttu-id="69673-243">Son sürüm aşağıdaki kodda gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="69673-243">The final version is shown in the following code:</span></span>

[!code-csharp[CleanedTupleVersion](../../samples/snippets/csharp/tuples/statistics.cs#09_CleanedTupleVersion "After final cleanup")]

<span data-ttu-id="69673-244">Bu son sürüm, bu üç değere veya bunların herhangi bir alt kümesine ihtiyacı olan herhangi bir yöntem için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-244">This final version can be used for any method that needs those three values, or any subset of them.</span></span>

<span data-ttu-id="69673-245">Dil, bu demet döndürme yöntemlerinde öğelerin adlarını yönetmenin diğer seçeneklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="69673-245">The language supports other options in managing the names of the elements in these tuple-returning methods.</span></span>

<span data-ttu-id="69673-246">Dönüş değeri bildiriminden alan adlarını kaldırabilir ve adlandırılmamış bir tanımlama grubu döndürebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69673-246">You can remove the field names from the return value declaration and return an unnamed tuple:</span></span>

```csharp
private static (double, double, int) ComputeSumAndSumOfSquares(IEnumerable<double> sequence)
{
    double sum = 0;
    double sumOfSquares = 0;
    int count = 0;

    foreach (var item in sequence)
    {
        count++;
        sum += item;
        sumOfSquares += item * item;
    }

    return (sum, sumOfSquares, count);
}
```

<span data-ttu-id="69673-247">Bu kayıt düzeninin alanları, ve `Item1` `Item3`olarak adlandırılır `Item2`.</span><span class="sxs-lookup"><span data-stu-id="69673-247">The fields of this tuple are named `Item1`, `Item2`, and `Item3`.</span></span>
<span data-ttu-id="69673-248">Metotlardan döndürülen başlıkların öğelerine anlam isimleri sağlamanız önerilir.</span><span class="sxs-lookup"><span data-stu-id="69673-248">It's recommended that you provide semantic names to the elements of tuples returned from methods.</span></span>

<span data-ttu-id="69673-249">LINQ sorguları yazarken, tanımlama gruplarının yararlı olabilecek başka bir IOM.</span><span class="sxs-lookup"><span data-stu-id="69673-249">Another idiom where tuples can be useful is when you are authoring LINQ queries.</span></span> <span data-ttu-id="69673-250">Son öngörülen sonuç, seçilen nesnelerin özelliklerinin tümünü değil, genellikle bazılarını içerir.</span><span class="sxs-lookup"><span data-stu-id="69673-250">The final projected result often contains some, but not all, of the properties of the objects being selected.</span></span>

<span data-ttu-id="69673-251">Genellikle sorgunun sonuçlarını anonim bir tür bir nesne dizisine proje olarak projecekti.</span><span class="sxs-lookup"><span data-stu-id="69673-251">You would traditionally project the results of the query into a sequence of objects that were an anonymous type.</span></span> <span data-ttu-id="69673-252">Bu çok sayıda kısıtlama sunuyordu, öncelikle anonim türler bir yöntem için dönüş türünde kolaylıkla adlandırılamadığından.</span><span class="sxs-lookup"><span data-stu-id="69673-252">That presented many limitations, primarily because anonymous types could not conveniently be named in the return type for a method.</span></span> <span data-ttu-id="69673-253">Sonuç türü `object` olarak `dynamic` veya olarak kullanmanın alternatifleri, önemli performans maliyetleriyle birlikte geldi.</span><span class="sxs-lookup"><span data-stu-id="69673-253">Alternatives using `object` or `dynamic` as the type of the result came with significant performance costs.</span></span>

<span data-ttu-id="69673-254">Bir demet türü dizisinin döndürülmesi kolaydır ve öğelerin adları ve türleri derleme zamanında ve IDE araçları aracılığıyla kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-254">Returning a sequence of a tuple type is easy, and the names and types of the elements are available at compile time and through IDE tools.</span></span>
<span data-ttu-id="69673-255">Örneğin, bir ToDo uygulaması düşünün.</span><span class="sxs-lookup"><span data-stu-id="69673-255">For example, consider a ToDo application.</span></span> <span data-ttu-id="69673-256">Yapılacaklar listesindeki tek bir girişi göstermek için aşağıdakine benzer bir sınıf tanımlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69673-256">You might define a class similar to the following to represent a single entry in the ToDo list:</span></span>

[!code-csharp[ToDoItem](../../samples/snippets/csharp/tuples/projectionsample.cs#14_ToDoItem "To Do Item")]

<span data-ttu-id="69673-257">Mobil uygulamalarınız, yalnızca başlığı görüntüleyen geçerli ToDo öğelerinin Compact formunu destekleyebilir.</span><span class="sxs-lookup"><span data-stu-id="69673-257">Your mobile applications may support a compact form of the current ToDo items that only displays the title.</span></span> <span data-ttu-id="69673-258">Bu LINQ sorgusu, yalnızca KIMLIĞI ve başlığı içeren bir projeksiyon oluşturacak.</span><span class="sxs-lookup"><span data-stu-id="69673-258">That LINQ query would make a projection that includes only the ID and the title.</span></span> <span data-ttu-id="69673-259">Bir tanımlama grubu sırası döndüren bir yöntem tasarımın iyi olduğunu ifade eder:</span><span class="sxs-lookup"><span data-stu-id="69673-259">A method that returns a sequence of tuples expresses that design well:</span></span>

[!code-csharp[QueryReturningTuple](../../samples/snippets/csharp/tuples/projectionsample.cs#15_QueryReturningTuple "Query returning a tuple")]

> [!NOTE]
> <span data-ttu-id="69673-260">7,1 C# ' de demet projeksiyonları, anonim türlerde özellik adlandırmasına benzer şekilde, öğeleri kullanarak, adlandırılmış tanımlama grupları oluşturmanıza imkan tanır.</span><span class="sxs-lookup"><span data-stu-id="69673-260">In C# 7.1, tuple projections enable you to create named tuples using elements, in a manner similar to the property naming in anonymous types.</span></span> <span data-ttu-id="69673-261">Yukarıdaki kodda, `select` sorgu projeksiyonun öğesi ve `Title`öğeleri `ID` içeren bir tanımlama grubu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69673-261">In the above code, the `select` statement in the query projection creates a tuple that has elements `ID` and `Title`.</span></span>

<span data-ttu-id="69673-262">Adlandırılmış tanımlama grubu, imzanın bir parçası olabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-262">The named tuple can be part of the signature.</span></span> <span data-ttu-id="69673-263">Derleyicinin ve IDE araçlarının, sonucu doğru şekilde kullandığınızı, statik denetim sağlamasına imkan tanır.</span><span class="sxs-lookup"><span data-stu-id="69673-263">It lets the compiler and IDE tools provide static checking that you are using the result correctly.</span></span> <span data-ttu-id="69673-264">Adlandırılmış tanımlama grubu statik tür bilgilerini de taşır, bu nedenle, sonuçlarla çalışmak için yansıma veya dinamik bağlama gibi pahalı çalışma süresi özelliklerinin kullanılmasına gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="69673-264">The named tuple also carries the static type information so there is no need to use expensive run time features like reflection or dynamic binding to work with the results.</span></span>

## <a name="deconstruction"></a><span data-ttu-id="69673-265">Ayrıştırma</span><span class="sxs-lookup"><span data-stu-id="69673-265">Deconstruction</span></span>

<span data-ttu-id="69673-266">Bir yöntem tarafından döndürülen kayıt grubunu kaldırarak bir kayıt grubundaki tüm öğelerin paketini kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-266">You can unpackage all the items in a tuple by *deconstructing* the tuple returned by a method.</span></span> <span data-ttu-id="69673-267">Başlıkların çıkarılması için üç farklı yaklaşım vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-267">There are three different approaches to deconstructing tuples.</span></span>  <span data-ttu-id="69673-268">İlk olarak, her bir alanın türünü parantez içindeki her bir öğe için ayrı değişkenler oluşturmak üzere açıkça bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69673-268">First, you can explicitly declare the type of each field inside parentheses to create discrete variables for each of the elements in the tuple:</span></span>

[!code-csharp[Deconstruct](../../samples/snippets/csharp/tuples/statistics.cs#10_Deconstruct "Deconstruct")]

<span data-ttu-id="69673-269">Ayrıca, parantez dışında bir `var` anahtar sözcüğü kullanarak, bir kayıt grubundaki her bir alan için örtük olarak belirlenmiş değişkenler de bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69673-269">You can also declare implicitly typed variables for each field in a tuple by using the `var` keyword outside the parentheses:</span></span>

[!code-csharp[DeconstructToVar](../../samples/snippets/csharp/tuples/statistics.cs#11_DeconstructToVar "Deconstruct to Var")]

<span data-ttu-id="69673-270">Parantez içinde herhangi bir veya tüm değişken `var` bildirimlerinin anahtar sözcüğünü kullanmak da geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="69673-270">It is also legal to use the `var` keyword with any, or all of the variable declarations inside the parentheses.</span></span> 

```csharp
(double sum, var sumOfSquares, var count) = ComputeSumAndSumOfSquares(sequence);
```

<span data-ttu-id="69673-271">Kayıt grubundaki her alan aynı türde olsa bile, parantez dışında belirli bir tür kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="69673-271">You cannot use a specific type outside the parentheses, even if every field in the tuple has the same type.</span></span>

<span data-ttu-id="69673-272">Mevcut bildirimlerle de tanımlama gruplarını kaldırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69673-272">You can deconstruct tuples with existing declarations as well:</span></span>

```csharp
public class Point
{
    public int X { get; set; }
    public int Y { get; set; }

    public Point(int x, int y) => (X, Y) = (x, y);
}
```

> [!WARNING]
> <span data-ttu-id="69673-273">Parantez içindeki bildirimlerle mevcut bildirimleri karıştıramazsınız.</span><span class="sxs-lookup"><span data-stu-id="69673-273">You cannot mix existing declarations with declarations inside the parentheses.</span></span> <span data-ttu-id="69673-274">Örneğin, aşağıdakilere izin verilmez: `(var x, y) = MyMethod();`.</span><span class="sxs-lookup"><span data-stu-id="69673-274">For instance, the following is not allowed: `(var x, y) = MyMethod();`.</span></span> <span data-ttu-id="69673-275">*X* , parantez içinde bildirildiği ve *y* daha önce başka bir yerde BILDIRILDIĞI için bu hata CS8184 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69673-275">This produces error CS8184 because *x* is declared inside the parentheses and *y* is previously declared elsewhere.</span></span>

### <a name="deconstructing-user-defined-types"></a><span data-ttu-id="69673-276">Kullanıcı tanımlı türleri kaldırma</span><span class="sxs-lookup"><span data-stu-id="69673-276">Deconstructing user-defined types</span></span>

<span data-ttu-id="69673-277">Herhangi bir demet türü, yukarıda gösterilen şekilde kaldırılabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-277">Any tuple type can be deconstructed as shown above.</span></span> <span data-ttu-id="69673-278">Kullanıcı tanımlı herhangi bir tür (sınıflar, yapılar veya hatta arabirimler) üzerinde oluşturmayı etkinleştirmek de kolaydır.</span><span class="sxs-lookup"><span data-stu-id="69673-278">It's also easy to enable deconstruction on any user-defined type (classes, structs, or even interfaces).</span></span>

<span data-ttu-id="69673-279">Tür yazarı, türü oluşturan veri öğelerini temsil `Deconstruct` eden herhangi bir `out` sayıda değişkene değer atayan bir veya daha fazla yöntem tanımlayabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-279">The type author can define one or more `Deconstruct` methods that assign values to any number of `out` variables representing the data elements that make up the type.</span></span> <span data-ttu-id="69673-280">Örneğin, aşağıdaki `Person` tür bir kişi nesnesini ilk `Deconstruct` adı ve soyadı temsil eden öğelere oluşturan bir yöntemi tanımlar:</span><span class="sxs-lookup"><span data-stu-id="69673-280">For example, the following `Person` type defines a `Deconstruct` method that deconstructs a person object into the elements representing the first name and last name:</span></span>

[!code-csharp[TypeWithDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#12_TypeWithDeconstructMethod "Type with a deconstruct method")]

<span data-ttu-id="69673-281">Deyapý yöntemi, `Person` `FirstName` ve `LastName` özelliklerini temsil eden bir ile iki dizeden atamayı sağlar:</span><span class="sxs-lookup"><span data-stu-id="69673-281">The deconstruct method enables assignment from a `Person` to two strings, representing the `FirstName` and `LastName` properties:</span></span>

[!code-csharp[Deconstruct Type](../../samples/snippets/csharp/tuples/program.cs#12A_DeconstructType "Deconstruct a class type")]

<span data-ttu-id="69673-282">Yazmayan türler için bile, oluşturmayı etkinleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69673-282">You can enable deconstruction even for types you did not author.</span></span>
<span data-ttu-id="69673-283">Yöntemi `Deconstruct` , bir nesnenin erişilebilir veri üyelerini paketten uygulayan bir genişletme yöntemi olabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-283">The `Deconstruct` method can be an extension method that unpackages the accessible data members of an object.</span></span> <span data-ttu-id="69673-284">Aşağıdaki örnekte, `Person` türünden türetilmiş `Student` bir tür ve,,, ve `GPA`öğelerini temsil eden `FirstName` `LastName`üç değişkene bir `Student` oluşturan uzantı yöntemi gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="69673-284">The example below shows a `Student` type, derived from the `Person` type, and an extension method that deconstructs a `Student` into three variables, representing the `FirstName`, the `LastName`, and the `GPA`:</span></span>

[!code-csharp[ExtensionDeconstructMethod](../../samples/snippets/csharp/tuples/person.cs#13_ExtensionDeconstructMethod "Type with a deconstruct extension method")]

<span data-ttu-id="69673-285">Bir `Student` nesne artık iki adet erişilebilir `Deconstruct` yönteme sahiptir: türler için `Student` belirtilen genişletme yöntemi ve `Person` türün üyesi.</span><span class="sxs-lookup"><span data-stu-id="69673-285">A `Student` object now has two accessible `Deconstruct` methods: the extension method declared for `Student` types, and the member of the `Person` type.</span></span> <span data-ttu-id="69673-286">Her ikisi de kapsamdadır ve bir `Student` , iki değişkene veya üçüne parçalanmalarını sağlar.</span><span class="sxs-lookup"><span data-stu-id="69673-286">Both are in scope, and that enables a `Student` to be deconstructed into either two variables or three.</span></span>
<span data-ttu-id="69673-287">Üç değişkene bir öğrenci atarsanız, ilk ad, soyadı ve GPA döndürülür.</span><span class="sxs-lookup"><span data-stu-id="69673-287">If you assign a student to three variables, the first name, last name, and GPA are all returned.</span></span> <span data-ttu-id="69673-288">İki değişkene bir öğrenci atarsanız, yalnızca ad ve soyadı döndürülür.</span><span class="sxs-lookup"><span data-stu-id="69673-288">If you assign a student to two variables, only the first name and the last name are returned.</span></span>

[!code-csharp[Deconstruct extension method](../../samples/snippets/csharp/tuples/program.cs#13A_DeconstructExtension "Deconstruct a class type using an extension method")]

<span data-ttu-id="69673-289">Bir sınıfta veya sınıf hiyerarşisinde birden `Deconstruct` çok yöntem tanımlamanız dikkatli olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="69673-289">You should be careful defining multiple `Deconstruct` methods in a class or a class hierarchy.</span></span> <span data-ttu-id="69673-290">`Deconstruct` Aynı`out` sayıda parametreye sahip birden çok yöntem, belirsizlikleri hızlı bir şekilde neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-290">Multiple `Deconstruct` methods that have the same number of `out` parameters can quickly cause ambiguities.</span></span> <span data-ttu-id="69673-291">Çağıranlar, istenen `Deconstruct` yöntemi kolayca çağıramayabilir.</span><span class="sxs-lookup"><span data-stu-id="69673-291">Callers may not be able to easily call the desired `Deconstruct` method.</span></span>

<span data-ttu-id="69673-292">`Deconstruct` Bu örnekte, için `Person` yönteminin iki `Deconstruct` çıkış parametresi olduğundan ve için `Student` yönteminin üç olması nedeniyle belirsiz bir çağrı için en az şans vardır.</span><span class="sxs-lookup"><span data-stu-id="69673-292">In this example, there is minimal chance for an ambiguous call because the `Deconstruct` method for `Person` has two output parameters, and the `Deconstruct` method for `Student` has three.</span></span>

<span data-ttu-id="69673-293">Yinelenenleri kaldırma işleçleri, test eşitliğine katılmaz.</span><span class="sxs-lookup"><span data-stu-id="69673-293">Deconstruction operators do not participate in testing equality.</span></span> <span data-ttu-id="69673-294">Aşağıdaki örnek, CS0019 derleyici hatası oluşturur:</span><span class="sxs-lookup"><span data-stu-id="69673-294">The following example generates compiler error CS0019:</span></span>

```csharp
Person p = new Person("Althea", "Goodwin");
if (("Althea", "Goodwin") == p)
    Console.WriteLine(p);
```

<span data-ttu-id="69673-295">Yöntemi, `Person` nesneyi`p` iki dize içeren bir kayıt türüne dönüştürebilir, ancak eşitlik testleri bağlamında geçerli değildir. `Deconstruct`</span><span class="sxs-lookup"><span data-stu-id="69673-295">The `Deconstruct` method could convert the `Person` object `p` to a tuple containing two strings, but it is not applicable in the context of equality tests.</span></span>

## <a name="conclusion"></a><span data-ttu-id="69673-296">Sonuç</span><span class="sxs-lookup"><span data-stu-id="69673-296">Conclusion</span></span> 

<span data-ttu-id="69673-297">Adlandırılmış tanımlama grupları için yeni dil ve kitaplık desteği, birden çok öğeyi depolayan ancak sınıflar ve yapılar için davranış tanımlamadığınız veri yapılarını kullanan tasarımlarla çalışmayı çok daha kolay hale getirir.</span><span class="sxs-lookup"><span data-stu-id="69673-297">The new language and library support for named tuples makes it much easier to work with designs that use data structures that store multiple elements but do not define behavior, as classes and structs do.</span></span> <span data-ttu-id="69673-298">Bu türler için tanımlama gruplarını kullanmak kolaydır.</span><span class="sxs-lookup"><span data-stu-id="69673-298">It's easy and concise to use tuples for those types.</span></span> <span data-ttu-id="69673-299">Daha ayrıntılı `class` veya `struct` sözdizimini kullanarak tür yazmak zorunda kalmadan statik tür denetimi avantajlarından yararlanın.</span><span class="sxs-lookup"><span data-stu-id="69673-299">You get all the benefits of static type checking, without needing to author types using the more verbose `class` or `struct` syntax.</span></span> <span data-ttu-id="69673-300">Bu `private`sayede bile, veya `internal`olan yardımcı program yöntemleri için en yararlı seçenektir.</span><span class="sxs-lookup"><span data-stu-id="69673-300">Even so, they are most useful for utility methods that are `private`, or `internal`.</span></span> <span data-ttu-id="69673-301">`class` Ya`struct` da genel yöntemleriniz birden çok öğeye sahip bir değer döndürzaman, ya da tür Kullanıcı tanımlı türler oluşturun.</span><span class="sxs-lookup"><span data-stu-id="69673-301">Create user-defined types, either `class` or `struct` types when your public methods return a value that has multiple elements.</span></span>
