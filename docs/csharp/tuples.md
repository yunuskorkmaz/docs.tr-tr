---
title: Diziler - C# Kılavuzu
description: C# adsız ve adlandırılmış tanımlama grubu türleri hakkında bilgi edinin
keywords: .NET, .NET Core, C#
author: BillWagner
ms-author: wiwagn
ms.date: 11/23/2016
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.assetid: ee8bf7c3-aa3e-4c9e-a5c6-e05cc6138baa
ms.openlocfilehash: 1d1fc450503dc905e6b260a2b984e3ce2315fd45
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/26/2018
---
# <a name="c-tuple-types"></a><span data-ttu-id="78f74-104">C# tanımlama grubu türleri</span><span class="sxs-lookup"><span data-stu-id="78f74-104">C# Tuple types</span></span> #

<span data-ttu-id="78f74-105">C# diziler basit bir söz dizimi kullanılarak tanımlayan türleridir.</span><span class="sxs-lookup"><span data-stu-id="78f74-105">C# Tuples are types that you define using a lightweight syntax.</span></span> <span data-ttu-id="78f74-106">Avantajlar kuralları (kardinalite adlandırılır) sayısı ve türleri öğeleri ve kopyalar ve atamaları için tutarlı kurallarını temel dönüştürmeleri için daha basit bir sözdizimi vardır.</span><span class="sxs-lookup"><span data-stu-id="78f74-106">The advantages include a simpler syntax, rules for conversions based on number (referred to as cardinality) and types of elements, and consistent rules for copies and assignments.</span></span> <span data-ttu-id="78f74-107">Bir kolaylığını diziler devralma ile ilişkili nesne yönelimli deyimleri bazıları desteklemez.</span><span class="sxs-lookup"><span data-stu-id="78f74-107">As a tradeoff, Tuples do not support some of the object oriented idioms associated with inheritance.</span></span> <span data-ttu-id="78f74-108">Bir genel bakış bölümünde alma [C# 7. 0'yenilikler içindeki diziler](whats-new/csharp-7.md#tuples) konu.</span><span class="sxs-lookup"><span data-stu-id="78f74-108">You can get an overview in the section on [Tuples in the What's new in C# 7.0](whats-new/csharp-7.md#tuples) topic.</span></span>

<span data-ttu-id="78f74-109">Bu konuda, C# 7.0 ve daha sonra farklı yolları tanımlama grupları ile çalışma hakkında onları ve başlangıç kılavuzunu kullanın diziler yöneten dil kurallarına öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-109">In this topic, you'll learn the language rules governing Tuples in C# 7.0 and later, different ways to use them, and initial guidance on working with Tuples.</span></span>

> [!NOTE]
> <span data-ttu-id="78f74-110">Yeni diziler işlevleri <xref:System.ValueTuple> türleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-110">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="78f74-111">NuGet paketini eklemeniz gerekir [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) türleri dahil etmeyin platformlarda kullanmak için.</span><span class="sxs-lookup"><span data-stu-id="78f74-111">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="78f74-112">Bu framework teslim türlerine dayanan diğer dil özellikleri benzer.</span><span class="sxs-lookup"><span data-stu-id="78f74-112">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="78f74-113">Örnekler `async` ve `await` güvenmek `INotifyCompletion` arabirimi ve güvenmek LINQ `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="78f74-113">Examples include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="78f74-114">Ancak, daha fazla platform bağımsız .NET olma gibi teslim mekanizması değiştiriyor.</span><span class="sxs-lookup"><span data-stu-id="78f74-114">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="78f74-115">.NET Framework her zaman aynı tempoyla dil derleyici olarak yükleyeceğiniz değil.</span><span class="sxs-lookup"><span data-stu-id="78f74-115">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="78f74-116">Yeni dil özellikleri yeni türlerinde kullanır, dil özellikleri sevk ettiğinizde bu türlerde NuGet paketleri olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-116">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="78f74-117">Bu yeni tür .NET standart API'sine eklenir ve framework'ün bir parçası olarak teslim olarak NuGet paketi gereksinimi kaldırılacak.</span><span class="sxs-lookup"><span data-stu-id="78f74-117">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="78f74-118">Yeni tanımlama grubu desteği ekleme nedenleri ile başlayalım tıklatın.</span><span class="sxs-lookup"><span data-stu-id="78f74-118">Let's start with the reasons for adding new Tuple support.</span></span> <span data-ttu-id="78f74-119">Yöntemleri tek bir nesne döndürür.</span><span class="sxs-lookup"><span data-stu-id="78f74-119">Methods return a single object.</span></span> <span data-ttu-id="78f74-120">Diziler, söz konusu tek nesne birden fazla değer daha kolay paketini olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="78f74-120">Tuples enable you to package multiple values in that single object more easily.</span></span>

<span data-ttu-id="78f74-121">.NET Framework genel zaten `Tuple` sınıfları.</span><span class="sxs-lookup"><span data-stu-id="78f74-121">The .NET Framework already has generic `Tuple` classes.</span></span> <span data-ttu-id="78f74-122">Bu sınıfların ancak, iki önemli kısıtlama vardı.</span><span class="sxs-lookup"><span data-stu-id="78f74-122">These classes, however, had two major limitations.</span></span> <span data-ttu-id="78f74-123">Birisi, `Tuple` sınıfları, özellikleri adlı `Item1`, `Item2`ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="78f74-123">For one, the `Tuple` classes named their properties `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="78f74-124">Bu adları yok anlamsal bilgilerin taşır.</span><span class="sxs-lookup"><span data-stu-id="78f74-124">Those names carry no semantic information.</span></span> <span data-ttu-id="78f74-125">Bunlar kullanarak `Tuple` türleri özelliklerin her biri anlamını iletişim sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="78f74-125">Using these `Tuple` types does not enable communicating the meaning of each of the properties.</span></span> <span data-ttu-id="78f74-126">Yeni dil özellikleri bildirme ve bir dizi öğelerin anlamsal olarak anlamlı adlarını kullanma olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-126">The new language features enable you to declare and use semantically meaningful names for the elements in a tuple.</span></span>

<span data-ttu-id="78f74-127">Başka bir önemli olan `Tuple` sınıfları başvuru türleridir.</span><span class="sxs-lookup"><span data-stu-id="78f74-127">Another concern is that the `Tuple` classes are reference types.</span></span> <span data-ttu-id="78f74-128">Aşağıdakilerden birini kullanarak `Tuple` türleri nesneleri ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="78f74-128">Using one of the `Tuple` types means allocating objects.</span></span> <span data-ttu-id="78f74-129">Sık kullanılan yollarına göre bu, uygulamanızın performans üzerinde ölçülebilir bir etkisi olabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-129">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="78f74-130">Bu nedenle, tanımlama grupları için dil desteği yeni yararlanır `ValueTuple` yapılar.</span><span class="sxs-lookup"><span data-stu-id="78f74-130">Therefore, the language support for tuples leverages the new `ValueTuple` structs.</span></span>

<span data-ttu-id="78f74-131">Bu eksiklikler önlemek için oluşturabilirsiniz bir `class` veya `struct` birden çok öğe taşımak için.</span><span class="sxs-lookup"><span data-stu-id="78f74-131">To avoid those deficiencies, you could create a `class` or a `struct` to carry multiple elements.</span></span> <span data-ttu-id="78f74-132">Ne yazık ki, sizin için daha fazla iş ve tasarım hedefi gizler.</span><span class="sxs-lookup"><span data-stu-id="78f74-132">Unfortunately, that's more work for you, and it obscures your design intent.</span></span> <span data-ttu-id="78f74-133">Yaparak bir `struct` veya `class` hem veri hem de davranış türüyle tanımlama anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="78f74-133">Making a `struct` or `class` implies that you are defining a type with both data and behavior.</span></span> <span data-ttu-id="78f74-134">Çoğu zaman, yalnızca birden çok değeri tek bir nesnede depolamak istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-134">Many times, you simply want to store multiple values in a single object.</span></span>

<span data-ttu-id="78f74-135">Dil özellikleri ve `ValueTuple` genel yapılar bu kayıt türlerinin herhangi davranışları (yöntemleri) ekleyemezsiniz kural zorla.</span><span class="sxs-lookup"><span data-stu-id="78f74-135">The language features and the `ValueTuple` generic structs enforce the rule that you cannot add any behavior (methods) to these tuple types.</span></span>
<span data-ttu-id="78f74-136">Tüm `ValueTuple` türleri *değişebilir yapılar*.</span><span class="sxs-lookup"><span data-stu-id="78f74-136">All the `ValueTuple` types are *mutable structs*.</span></span> <span data-ttu-id="78f74-137">Her üye alan genel bir alandır.</span><span class="sxs-lookup"><span data-stu-id="78f74-137">Each member field is a public field.</span></span> <span data-ttu-id="78f74-138">Bunları çok basit hale getirir.</span><span class="sxs-lookup"><span data-stu-id="78f74-138">That makes them very lightweight.</span></span> <span data-ttu-id="78f74-139">Ancak, bu girişi önemli olduğu diziler kullanılmamalıdır anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="78f74-139">However, that means tuples should not be used where immutability is important.</span></span>

<span data-ttu-id="78f74-140">Diziler olan hem de daha basit ve daha esnek veri kapsayıcıları daha `class` ve `struct` türleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-140">Tuples are both simpler and more flexible data containers than `class` and `struct` types.</span></span> <span data-ttu-id="78f74-141">Bu farklılıklar inceleyelim.</span><span class="sxs-lookup"><span data-stu-id="78f74-141">Let's explore those differences.</span></span>

## <a name="named-and-unnamed-tuples"></a><span data-ttu-id="78f74-142">Adlandırılmış ve adlandırılmamış diziler</span><span class="sxs-lookup"><span data-stu-id="78f74-142">Named and unnamed tuples</span></span>

<span data-ttu-id="78f74-143">`ValueTuple` Yapısı sahip adlı alanları `Item1`, `Item2`, `Item3` ve vb., varolan tanımlanan özellikleri benzer `Tuple` türleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-143">The `ValueTuple` struct has fields named `Item1`, `Item2`, `Item3` and so on, similar to the properties defined in the existing `Tuple` types.</span></span>
<span data-ttu-id="78f74-144">Bu adları, için kullanabileceğiniz yalnızca adlarıdır *diziler adlandırılmamış*.</span><span class="sxs-lookup"><span data-stu-id="78f74-144">These names are the only names you can use for *unnamed tuples*.</span></span> <span data-ttu-id="78f74-145">Herhangi bir tanımlama grubu alternatif alan adlarını sağlamaz, adsız bir tanımlama grubu oluşturduğunuzu düşünün:</span><span class="sxs-lookup"><span data-stu-id="78f74-145">When you do not provide any alternative field names to a tuple, you've created an unnamed tuple:</span></span>

[!code-csharp[UnnamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#01_UnNamedTuple "Unnamed tuple")]

<span data-ttu-id="78f74-146">Önceki örnekte yer alan tanımlama grubu değişmez değer sabitleri kullanarak başlatıldı ve öğe adları kullanarak oluşturduğunuz olmaz *tanımlama grubu alan adı tahminleri* C# 7.1 içinde.</span><span class="sxs-lookup"><span data-stu-id="78f74-146">The tuple in the previous example was initialized using literal constants and won't have element names created using *Tuple field name projections* in C# 7.1.</span></span>

<span data-ttu-id="78f74-147">Ancak, bir tanımlama grubu başlattığınızda, her bir alan için daha iyi adlar verin yeni dil özellikleri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-147">However, when you initialize a tuple, you can use new language features that give better names to each field.</span></span> <span data-ttu-id="78f74-148">Bunun yapılması oluşturur bir *tanımlama grubu adlı*.</span><span class="sxs-lookup"><span data-stu-id="78f74-148">Doing so creates a *named tuple*.</span></span>
<span data-ttu-id="78f74-149">Adlandırılmış diziler adlı öğe çözümlenmedi `Item1`, `Item2`, `Item3` ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="78f74-149">Named tuples still have elements named `Item1`, `Item2`, `Item3` and so on.</span></span>
<span data-ttu-id="78f74-150">Ancak aynı zamanda herhangi bu öğelerin adlı için eş anlamlı sözcükleri sahiptirler.</span><span class="sxs-lookup"><span data-stu-id="78f74-150">But they also have synonyms for any of those elements that you have named.</span></span>
<span data-ttu-id="78f74-151">Her öğe için adları belirterek adlandırılmış bir tanımlama grubu oluşturun.</span><span class="sxs-lookup"><span data-stu-id="78f74-151">You create a named tuple by specifying the names for each element.</span></span> <span data-ttu-id="78f74-152">Tuple başlatma bir parçası olarak adlarını belirtmek için bir yolu şöyledir:</span><span class="sxs-lookup"><span data-stu-id="78f74-152">One way is to specify the names as part of the tuple initialization:</span></span>

[!code-csharp[NamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#02_NamedTuple "Named tuple")]

<span data-ttu-id="78f74-153">Bu eş anlamlıları derleyici tarafından işlenir ve dil etkili bir şekilde adlandırılmış'u diziler böylece kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-153">These synonyms are handled by the compiler and the language so that you can use named tuples effectively.</span></span> <span data-ttu-id="78f74-154">IDE ve düzenleyiciler Roslyn API'leri kullanılarak bu anlam adları okuyabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-154">IDEs and editors can read these semantic names using the Roslyn APIs.</span></span> <span data-ttu-id="78f74-155">Bu adlandırılmış bir tanımlama grubu öğelerini bu anlam adlarıyla aynı bütünleştirilmiş kodda başka bir yerindeki başvuru sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-155">This enables you to reference the elements of a named tuple by those semantic names anywhere in the same assembly.</span></span> <span data-ttu-id="78f74-156">Derleyici ile tanımlanan adlar değiştirir `Item*` derlenmiş çıkış oluştururken eşdeğerleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-156">The compiler replaces the names you've defined with `Item*` equivalents when generating the compiled output.</span></span> <span data-ttu-id="78f74-157">Derlenmiş Microsoft Ara dili (MSIL) bu öğelerin verdiniz adları içermez.</span><span class="sxs-lookup"><span data-stu-id="78f74-157">The compiled Microsoft Intermediate Language (MSIL) does not include the names you've given these elements.</span></span>

<span data-ttu-id="78f74-158">C# 7.1 ile başlayarak, bir tanımlama grubu için alan adlarını alan tanımlama grubu başlatmak için kullanılan değişkenler arasından sağlanabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-158">Beginning with C# 7.1, the field names for a tuple may be provided from the variables used to initialize the tuple.</span></span> <span data-ttu-id="78f74-159">Bu olarak adlandırılır  **[tanımlama grubu projeksiyon başlatıcıları](#tuple-projection-initializers)**.</span><span class="sxs-lookup"><span data-stu-id="78f74-159">This is referred to as **[tuple projection initializers](#tuple-projection-initializers)**.</span></span> <span data-ttu-id="78f74-160">Aşağıdaki kod adlı bir tanımlama grubu oluşturur `accumulation` öğelerle `count` (tamsayı) ve `sum` (bir double).</span><span class="sxs-lookup"><span data-stu-id="78f74-160">The following code creates a tuple named `accumulation` with elements `count` (an integer), and `sum` (a double).</span></span>

[!code-csharp[ProjectedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectedTupleNames "Named tuple")]

<span data-ttu-id="78f74-161">Derleyici ortak yöntemleri ya da özellikleri döndürülen diziler için oluşturulan bu adlar iletişim kurması gerekir.</span><span class="sxs-lookup"><span data-stu-id="78f74-161">The compiler must communicate those names you created for tuples that are returned from public methods or properties.</span></span> <span data-ttu-id="78f74-162">Bu gibi durumlarda derleyici ekler bir <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> yöntemi özniteliği.</span><span class="sxs-lookup"><span data-stu-id="78f74-162">In those cases, the compiler adds a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> attribute on the method.</span></span> <span data-ttu-id="78f74-163">Bu öznitelik içeren bir <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> tanımlama grubundaki öğelerin her biri için verilen adlarını içeren özellik listesi.</span><span class="sxs-lookup"><span data-stu-id="78f74-163">This attribute contains a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> list property that contains the names given to each of the elements in the Tuple.</span></span>

> [!NOTE]
> <span data-ttu-id="78f74-164">Visual Studio gibi geliştirme araçları Ayrıca bu meta verilerini okuma ve IntelliSense meta verileri alan adları kullanarak ve diğer özellikleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-164">Development Tools, such as Visual Studio, also read that metadata, and provide IntelliSense and other features using the metadata field names.</span></span>

<span data-ttu-id="78f74-165">Bu yeni başlıklar temelleri temel anlamak önemlidir ve `ValueTuple` atamak için kurallar anlamak için türünün adlı birbirlerine diziler.</span><span class="sxs-lookup"><span data-stu-id="78f74-165">It is important to understand these underlying fundamentals of the new tuples and the `ValueTuple` type in order to understand the rules for assigning named tuples to each other.</span></span>

## <a name="tuple-projection-initializers"></a><span data-ttu-id="78f74-166">Tuple projeksiyon başlatıcıları</span><span class="sxs-lookup"><span data-stu-id="78f74-166">Tuple projection initializers</span></span>

<span data-ttu-id="78f74-167">Genel olarak, bir tanımlama grubu başlatma ifadesinin sağ taraftaki değişken veya alan adları kullanarak tanımlama grubu projeksiyon başlatıcıları çalışır.</span><span class="sxs-lookup"><span data-stu-id="78f74-167">In general, tuple projection initializers work by using the variable or field names from the right-hand side of a tuple initialization statement.</span></span>
<span data-ttu-id="78f74-168">Açık bir ad belirtilmezse, tüm tahmini adın üzerine önceliklidir.</span><span class="sxs-lookup"><span data-stu-id="78f74-168">If an explicit name is given, that takes precedence over any projected name.</span></span> <span data-ttu-id="78f74-169">Örneğin, aşağıdaki Başlatıcı öğeleridir `explicitFieldOne` ve `explicitFieldTwo`değil `localVariableOne` ve `localVariableTwo`:</span><span class="sxs-lookup"><span data-stu-id="78f74-169">For example, in the following initializer, the elements are `explicitFieldOne` and `explicitFieldTwo`, not `localVariableOne` and `localVariableTwo`:</span></span>

[!code-csharp[ExplicitNamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectionExample_Explicit "Explicitly named tuple")]

<span data-ttu-id="78f74-170">Burada açık bir ad sağlanmayan herhangi bir alan için geçerli bir örtük adı yansıtılacak.</span><span class="sxs-lookup"><span data-stu-id="78f74-170">For any field where an explicit name is not provided, an applicable implicit name will be projected.</span></span> <span data-ttu-id="78f74-171">Anlam adları, açıkça veya örtük sağlamak için gereksinim olduğuna dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="78f74-171">Note that there is no requirement to provide semantic names, either explicitly or implicitly.</span></span> <span data-ttu-id="78f74-172">Aşağıdaki Başlatıcı alan adları olacaktır `Item1`, değeri olan `42` ve `StringContent`, "Her şeyi yanıt" değeri olan:</span><span class="sxs-lookup"><span data-stu-id="78f74-172">The following initializer will have field names `Item1`, whose value is `42` and `StringContent`, whose value is "The answer to everything":</span></span>

[!code-csharp[MixedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#MixedTuple "mixed tuple")]

<span data-ttu-id="78f74-173">Burada adayı alan adları tanımlama grubu alanın öngörülen olmayan iki koşul vardır:</span><span class="sxs-lookup"><span data-stu-id="78f74-173">There are two conditions where candidate field names are not projected onto the tuple field:</span></span>

1. <span data-ttu-id="78f74-174">Aday adı ayrılmış bir tanımlama grubu adı olduğunda.</span><span class="sxs-lookup"><span data-stu-id="78f74-174">When the candidate name is a reserved tuple name.</span></span> <span data-ttu-id="78f74-175">Örnekler `Item3`, `ToString` veya `Rest`.</span><span class="sxs-lookup"><span data-stu-id="78f74-175">Examples include `Item3`, `ToString` or `Rest`.</span></span>
1. <span data-ttu-id="78f74-176">Aday adı başka bir tanımlama grubu alan adı, açık veya örtülü tekrarı olduğunda.</span><span class="sxs-lookup"><span data-stu-id="78f74-176">When the candidate name is a duplicate of another tuple field name, either explicit or implicit.</span></span>

<span data-ttu-id="78f74-177">Bu koşullar belirsizlik kaçının.</span><span class="sxs-lookup"><span data-stu-id="78f74-177">These conditions avoid ambiguity.</span></span> <span data-ttu-id="78f74-178">Bir tanımlama grubu alanında alan adları olarak kullanıldıysa, bu adları bir belirsizlik neden olur.</span><span class="sxs-lookup"><span data-stu-id="78f74-178">These names would cause an ambiguity if they were used as the field names for a field in a tuple.</span></span> <span data-ttu-id="78f74-179">Bu koşullar neden derleme zamanı hataları hiçbiri.</span><span class="sxs-lookup"><span data-stu-id="78f74-179">Neither of these conditions cause compile time errors.</span></span> <span data-ttu-id="78f74-180">Bunun yerine, tahmini adları olmadan öğeleri kendileri için öngörülen anlamsal adları yok.</span><span class="sxs-lookup"><span data-stu-id="78f74-180">Instead, the elements without projected names do not have semantic names projected for them.</span></span>  <span data-ttu-id="78f74-181">Aşağıdaki örnekler, bu koşullar göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="78f74-181">The following examples demonstrate these conditions:</span></span>

[!code-csharp[Ambiguity](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectionAmbiguities "tuples where projections are not performed")]

<span data-ttu-id="78f74-182">Bu durumlarda, tanımlama grubu alan adı tahminleri kullanılamıyordu, C# 7.0 ile yazılan kod için önemli bir değişiklik olacağından derleyici hataları neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="78f74-182">These situations do not cause compiler errors because that would be a breaking change for code written with C# 7.0, when tuple field name projections were not available.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="78f74-183">Atama ve diziler</span><span class="sxs-lookup"><span data-stu-id="78f74-183">Assignment and tuples</span></span>

<span data-ttu-id="78f74-184">Dil aynı sayıda öğe olan tanımlama grubu türlerini örtük dönüşümler arasındaki atama türleri için her bu öğeler için destekler.</span><span class="sxs-lookup"><span data-stu-id="78f74-184">The language supports assignment between tuple types that have the same number of elements and implicit conversions for the types for each of those elements.</span></span> <span data-ttu-id="78f74-185">Diğer dönüştürme atamaları dikkate alınmaz.</span><span class="sxs-lookup"><span data-stu-id="78f74-185">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="78f74-186">Dizi türleri arasında izin atamalarını türlerini bakalım.</span><span class="sxs-lookup"><span data-stu-id="78f74-186">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="78f74-187">Aşağıdaki örneklerde kullanılan bu değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="78f74-187">Consider these variables used in the following examples:</span></span>

[!code-csharp[VariableCreation](../../samples/snippets/csharp/tuples/tuples/program.cs#03_VariableCreation "Variable creation")]

<span data-ttu-id="78f74-188">İlk iki değişkenleri `unnamed` ve `anonymous` sahip değil öğeleri için sağlanan anlamsal adları.</span><span class="sxs-lookup"><span data-stu-id="78f74-188">The first two variables, `unnamed` and `anonymous` do not have semantic names provided for the elements.</span></span> <span data-ttu-id="78f74-189">Alan adları `Item1` ve `Item2`.</span><span class="sxs-lookup"><span data-stu-id="78f74-189">The field names are `Item1` and `Item2`.</span></span>
<span data-ttu-id="78f74-190">Son iki değişken `named` ve `differentName` öğeleri için verilen anlamsal adlara sahip olması.</span><span class="sxs-lookup"><span data-stu-id="78f74-190">The last two variables, `named` and `differentName` have semantic names given for the elements.</span></span> <span data-ttu-id="78f74-191">Bu iki başlığın öğeleri için farklı adlar gerektiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="78f74-191">Note that these two tuples have different names for the elements.</span></span>

<span data-ttu-id="78f74-192">Bu başlık dört aynı sayıda öğe ('kardinalite' adlandırılır) varsa ve bu öğeleri türlerini aynıdır.</span><span class="sxs-lookup"><span data-stu-id="78f74-192">All four of these tuples have the same number of elements (referred to as 'cardinality') and the types of those elements are identical.</span></span> <span data-ttu-id="78f74-193">Bu nedenle, tüm bu atamaları çalışır:</span><span class="sxs-lookup"><span data-stu-id="78f74-193">Therefore, all of these assignments work:</span></span>

[!code-csharp[VariableAssignment](../../samples/snippets/csharp/tuples/tuples/program.cs#04_VariableAssignment "Variable assignment")]

<span data-ttu-id="78f74-194">Diziler adları atanmamıştır dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="78f74-194">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="78f74-195">Tanımlama grubundaki öğelerin sırasını aşağıdaki öğelerin değerleri atanır.</span><span class="sxs-lookup"><span data-stu-id="78f74-195">The values of the elements are assigned following the order of the elements in the tuple.</span></span>

<span data-ttu-id="78f74-196">Diziler farklı türlerin veya öğeleri sayıda atanabilir değil:</span><span class="sxs-lookup"><span data-stu-id="78f74-196">Tuples of different types or numbers of elements are not assignable:</span></span>

```csharp
// Does not compile.
// CS0029: Cannot assign Tuple(int,int,int) to Tuple(int, string)
var differentShape = (1, 2, 3);
named = differentShape;
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="78f74-197">Diziler yöntemi olarak dönüş değerleri</span><span class="sxs-lookup"><span data-stu-id="78f74-197">Tuples as method return values</span></span>

<span data-ttu-id="78f74-198">Diziler için en yaygın kullanımları yönteminin dönüş değeri olarak biridir.</span><span class="sxs-lookup"><span data-stu-id="78f74-198">One of the most common uses for Tuples is as a method return value.</span></span> <span data-ttu-id="78f74-199">Şimdi bir örnek yol.</span><span class="sxs-lookup"><span data-stu-id="78f74-199">Let's walk through one example.</span></span> <span data-ttu-id="78f74-200">Sayılardan oluşan bir dizi standart sapmayı hesaplar bu yöntem göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="78f74-200">Consider this method that computes the standard deviation for a sequence of numbers:</span></span>

[!code-csharp[StandardDeviation](../../samples/snippets/csharp/tuples/tuples/statistics.cs#05_StandardDeviation "Compute Standard Deviation")]

> [!NOTE]
> <span data-ttu-id="78f74-201">Bu örnekler düzeltilemeyen örnek standart sapma işlem.</span><span class="sxs-lookup"><span data-stu-id="78f74-201">These examples compute the uncorrected sample standard deviation.</span></span>
> <span data-ttu-id="78f74-202">Düzeltilmiş örnek standart sapma formülü (N-1) tarafından N yerine ortalaması arasındaki karesi alınmış farklar toplamı olarak bölmek `Average` genişletme yöntemi yapar.</span><span class="sxs-lookup"><span data-stu-id="78f74-202">The corrected sample standard deviation formula would divide the sum of the squared differences from the mean by (N-1) instead of N, as the `Average` extension method does.</span></span> <span data-ttu-id="78f74-203">Standart sapma için bu formüller arasındaki farklar hakkında daha fazla ayrıntı için istatistikleri metin başvurun.</span><span class="sxs-lookup"><span data-stu-id="78f74-203">Consult a statistics text for more details on the differences between these formulas for standard deviation.</span></span>

<span data-ttu-id="78f74-204">Standart sapma Ders Kitabı formülü izler.</span><span class="sxs-lookup"><span data-stu-id="78f74-204">This follows the textbook formula for the standard deviation.</span></span> <span data-ttu-id="78f74-205">Doğru yanıt oluşturur, ancak çok verimli bir uygulamasıdır.</span><span class="sxs-lookup"><span data-stu-id="78f74-205">It produces the correct answer, but it's a very inefficient implementation.</span></span> <span data-ttu-id="78f74-206">Bu yöntem dizisi iki kez numaralandırır: ortalama üretmek için bir kez ve ortalama farkı kare ortalaması üretmek için bir kez.</span><span class="sxs-lookup"><span data-stu-id="78f74-206">This method enumerates the sequence twice: Once to produce the average, and once to produce the average of the square of the difference of the average.</span></span>
<span data-ttu-id="78f74-207">(Yalnızca bir numaralandırma ortalaması arasındaki farklar hesaplama ve bu farklılıkları ortalaması yapar şekilde LINQ sorgularını gevşek, değerlendirildiğini unutmayın.)</span><span class="sxs-lookup"><span data-stu-id="78f74-207">(Remember that LINQ queries are evaluated lazily, so the computation of the differences from the mean and the average of those differences makes only one enumeration.)</span></span>

<span data-ttu-id="78f74-208">Yalnızca bir numaralandırma dizisi kullanarak alarak standart sapmayı hesaplar alternatif bir formül yoktur.</span><span class="sxs-lookup"><span data-stu-id="78f74-208">There is an alternative formula that computes standard deviation using only one enumeration of the sequence.</span></span>  <span data-ttu-id="78f74-209">Dizisini numaralandıran gibi iki değerleri bu hesaplama üreten: dizisindeki tüm öğelerin toplamı ve her bir değerin toplamını kare:</span><span class="sxs-lookup"><span data-stu-id="78f74-209">This computation produces two values as it enumerates the sequence: the sum of all items in the sequence, and the sum of the each value squared:</span></span>

[!code-csharp[SumOfSquaresFormula](../../samples/snippets/csharp/tuples/tuples/statistics.cs#06_SumOfSquaresFormula "Compute Standard Deviation using the sum of squares")]

<span data-ttu-id="78f74-210">Bu sürüm dizisi tam olarak bir kez numaralandırır.</span><span class="sxs-lookup"><span data-stu-id="78f74-210">This version enumerates the sequence exactly once.</span></span> <span data-ttu-id="78f74-211">Ancak, çok yeniden kullanılabilir kodu değil.</span><span class="sxs-lookup"><span data-stu-id="78f74-211">But, it's not very reusable code.</span></span> <span data-ttu-id="78f74-212">Çalışmaya devam gibi birçok farklı istatistiksel hesaplamalar sırası, sıra toplamı ve dizisi kareler toplamı öğelerin sayısı kullandığınız bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-212">As you keep working, you'll find that many different statistical computations use the number of items in the sequence, the sum of the sequence, and the sum of the squares of the sequence.</span></span> <span data-ttu-id="78f74-213">Şimdi bu yöntem yeniden düzenlemeniz ve bu değerlerin üçünü üreten bir yardımcı program yöntemi yazın.</span><span class="sxs-lookup"><span data-stu-id="78f74-213">Let's refactor this method and write a utility method that produces all three of those values.</span></span>

<span data-ttu-id="78f74-214">Diziler çok kullanışlı yeri budur.</span><span class="sxs-lookup"><span data-stu-id="78f74-214">This is where tuples come in very useful.</span></span> 

<span data-ttu-id="78f74-215">Hesaplanan numaralandırma sırasında üç değerden bir tanımlama grubu içinde depolanan şimdi bu yöntem güncelleştirin.</span><span class="sxs-lookup"><span data-stu-id="78f74-215">Let's update this method so the three values computed during the enumeration are stored in a tuple.</span></span> <span data-ttu-id="78f74-216">Bu sürüm oluşturur:</span><span class="sxs-lookup"><span data-stu-id="78f74-216">That creates this version:</span></span>

[!code-csharp[TupleVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#07_TupleVersion "Refactor to use tuples")]

<span data-ttu-id="78f74-217">Visual Studio'nun Refactoring destek işlevsellik çekirdek istatistikler için özel bir yöntem ayıklamak kolaylaşır.</span><span class="sxs-lookup"><span data-stu-id="78f74-217">Visual Studio's Refactoring support makes it easy to extract the functionality for the core statistics into a private method.</span></span> <span data-ttu-id="78f74-218">Gösteren bir `private static` üç değerlerini dizi türüyle döndürür yöntemi `Sum`, `SumOfSquares`, ve `Count`:</span><span class="sxs-lookup"><span data-stu-id="78f74-218">That gives you a `private static` method that returns the tuple type with the three values of `Sum`, `SumOfSquares`, and `Count`:</span></span>

[!code-csharp[TupleMethodVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#08_TupleMethodVersion "After extracting utility method")]
 
<span data-ttu-id="78f74-219">El ile birkaç Hızlı düzenlemeleri yapmak istiyorsanız dil kullanabilirsiniz, birkaç diğer seçenekler sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-219">The language enables a couple more options that you can use, if you want to make a few quick edits by hand.</span></span> <span data-ttu-id="78f74-220">İlk olarak, kullanabileceğiniz `var` tanımlama grubu sonucundan başlatmak için bildirimi `ComputeSumAndSumOfSquares` yöntem çağrısı.</span><span class="sxs-lookup"><span data-stu-id="78f74-220">First, you can use the `var` declaration to initialize the tuple result from the `ComputeSumAndSumOfSquares` method call.</span></span> <span data-ttu-id="78f74-221">İçinde üç ayrık değişkenleri oluşturabilirsiniz `ComputeSumAndSumOfSquares` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="78f74-221">You can also create three discrete variables inside the `ComputeSumAndSumOfSquares` method.</span></span> <span data-ttu-id="78f74-222">Son sürümü aşağıdadır:</span><span class="sxs-lookup"><span data-stu-id="78f74-222">The final version is below:</span></span>

[!code-csharp[CleanedTupleVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#09_CleanedTupleVersion "After final cleanup")]

<span data-ttu-id="78f74-223">Bu son sürümü bu üç değerleri veya herhangi bir kısmını gereken herhangi bir yöntemi için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-223">This final version can be used for any method that needs those three values, or any subset of them.</span></span>

<span data-ttu-id="78f74-224">Dili, bu tanımlama grubu döndüren yöntemler öğeleri adları yönetilmesindeki diğer seçenekleri destekler.</span><span class="sxs-lookup"><span data-stu-id="78f74-224">The language supports other options in managing the names of the elements in these tuple-returning methods.</span></span>

<span data-ttu-id="78f74-225">Dönüş değeri bildiriminden alan adlarını kaldırmak ve adsız bir tanımlama grubu döndürür:</span><span class="sxs-lookup"><span data-stu-id="78f74-225">You can remove the field names from the return value declaration and return an unnamed tuple:</span></span>

```csharp
private static (double, double, int) ComputeSumAndSumOfSquares(IEnumerable<double> sequence)
{
    double sum = 0;
    double sumOfSquares = 0;
    int count = 0;

    foreach (var item in sequence)
    {
        count++;
        sum += item;
        sumOfSquares += item * item;
    }

    return (sum, sumOfSquares, count);
}
```

<span data-ttu-id="78f74-226">Bu tanımlama grubu alanlarının adres `Item1`, `Item2`, ve `Item3`.</span><span class="sxs-lookup"><span data-stu-id="78f74-226">You must address the fields of this tuple as `Item1`, `Item2`, and `Item3`.</span></span>
<span data-ttu-id="78f74-227">Yöntemleri iade diziler öğelerine anlamsal adları sağladığınız önerilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-227">It's recommended that you provide semantic names to the elements of tuples returned from methods.</span></span>

<span data-ttu-id="78f74-228">Başka bir deyim burada diziler çok kullanışlı olabilir, nihai sonucu bazı içeren bir yansıtma olduğu LINQ sorgularını yazarken, ancak seçilmesini nesneleri özelliklerinin tümünü değil ' dir.</span><span class="sxs-lookup"><span data-stu-id="78f74-228">Another idiom where tuples can be very useful is when you are authoring LINQ queries where the final result is a projection that contains some, but not all, of the properties of the objects being selected.</span></span>

<span data-ttu-id="78f74-229">Geleneksel olarak anonim bir tür olan nesneler dizisi sorgu sonuçlarını proje.</span><span class="sxs-lookup"><span data-stu-id="78f74-229">You would traditionally project the results of the query into a sequence of objects that were an anonymous type.</span></span> <span data-ttu-id="78f74-230">Öncelikle anonim türler rahat dönüş türü için bir yöntem adlandırılabilir değil çünkü, çok sayıda sınırlamaya sunulmuştur.</span><span class="sxs-lookup"><span data-stu-id="78f74-230">That presented many limitations, primarily because anonymous types could not conveniently be named in the return type for a method.</span></span> <span data-ttu-id="78f74-231">Kullanarak alternatifleri `object` veya `dynamic` sonuç türü ile önemli performans maliyetleri gelen gibi.</span><span class="sxs-lookup"><span data-stu-id="78f74-231">Alternatives using `object` or `dynamic` as the type of the result came with significant performance costs.</span></span>

<span data-ttu-id="78f74-232">Bir tanımlama grubu bir dizi döndürme türü kolaydır ve adlarını ve öğeleri türlerini derleme zamanında ve IDE araçları aracılığıyla kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-232">Returning a sequence of a tuple type is easy, and the names and types of the elements are available at compile time and through IDE tools.</span></span>
<span data-ttu-id="78f74-233">Örneğin, bir Yapılacaklar uygulamasının göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="78f74-233">For example, consider a ToDo application.</span></span> <span data-ttu-id="78f74-234">Yapılacaklar listesindeki tek bir giriş temsil etmek için aşağıdakine benzer bir sınıf tanımlama:</span><span class="sxs-lookup"><span data-stu-id="78f74-234">You might define a class similar to the following to represent a single entry in the ToDo list:</span></span>

[!code-csharp[ToDoItem](../../samples/snippets/csharp/tuples/tuples/projectionsample.cs#14_ToDoItem "To Do Item")]

<span data-ttu-id="78f74-235">Mobil uygulamalarınız başlık yalnızca görüntüleyen bir compact biçiminde geçerli Yapılacaklar öğelerini desteklemiyor olabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-235">Your mobile applications may support a compact form of the current ToDo items that only displays the title.</span></span> <span data-ttu-id="78f74-236">LINQ Sorgu projeksiyon, yapacağınız, yalnızca kimliği ve başlık içerir.</span><span class="sxs-lookup"><span data-stu-id="78f74-236">That LINQ query would make a projection that includes only the ID and the title.</span></span> <span data-ttu-id="78f74-237">Diziler dizisi döndüren bir yöntem bu tasarımı çok iyi ifade eder:</span><span class="sxs-lookup"><span data-stu-id="78f74-237">A method that returns a sequence of tuples expresses that design very well:</span></span>

[!code-csharp[QueryReturningTuple](../../samples/snippets/csharp/tuples/tuples/projectionsample.cs#15_QueryReturningTuple "Query returning a tuple")]

> [!NOTE]
> <span data-ttu-id="78f74-238">C# içinde 7.1, tanımlama grubu tahminleri özelliği adlandırma anonim türlerinde benzer bir şekilde kullanan öğeler, adlandırılmış diziler oluşturmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-238">In C# 7.1, tuple projections enable you to create named tuples using elements, in a manner similar to the property naming in anonymous types.</span></span> <span data-ttu-id="78f74-239">Yukarıdaki kod `select` sorgu projeksiyon deyiminde öğesi içeren bir tanımlama grubu oluşturur `ID` ve `Title`.</span><span class="sxs-lookup"><span data-stu-id="78f74-239">In the above code, the `select` statement in the query projection creates a tuple that has elements `ID` and `Title`.</span></span>

<span data-ttu-id="78f74-240">Adlandırılmış tanımlama grubu imza parçası olabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-240">The named tuple can be part of the signature.</span></span> <span data-ttu-id="78f74-241">Derleyici sağlar ve IDE araçları statik sonucu doğru kullanıyorsanız denetimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="78f74-241">It lets the compiler and IDE tools provide static checking that you are using the result correctly.</span></span> <span data-ttu-id="78f74-242">Bu yüzden sonuçlarıyla çalışmaya yansıma ya da dinamik bağlama gibi pahalı çalışma zamanı özellikleri kullanmak için gerekli adlandırılmış tanımlama grubu statik türü bilgileri de taşır.</span><span class="sxs-lookup"><span data-stu-id="78f74-242">The named tuple also carries the static type information so there is no need to use expensive run time features like reflection or dynamic binding to work with the results.</span></span>

## <a name="deconstruction"></a><span data-ttu-id="78f74-243">Deconstruction</span><span class="sxs-lookup"><span data-stu-id="78f74-243">Deconstruction</span></span>

<span data-ttu-id="78f74-244">Bir tanımlama grubu tarafından tüm öğelerde gitmeniz *deconstructing* yöntemi tarafından döndürülen tanımlama grubu.</span><span class="sxs-lookup"><span data-stu-id="78f74-244">You can unpackage all the items in a tuple by *deconstructing* the tuple returned by a method.</span></span> <span data-ttu-id="78f74-245">Diziler deconstructing için üç farklı yaklaşım vardır.</span><span class="sxs-lookup"><span data-stu-id="78f74-245">There are three different approaches to deconstructing tuples.</span></span>  <span data-ttu-id="78f74-246">İlk olarak, her tanımlama grubu öğeleri ayrık değişkenleri oluşturmak üzere parantez içindeki her bir alan türü açıkça bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="78f74-246">First, you can explicitly declare the type of each field inside parentheses to create discrete variables for each of the elements in the tuple:</span></span>

[!code-csharp[Deconstruct](../../samples/snippets/csharp/tuples/tuples/statistics.cs#10_Deconstruct "Deconstruct")]

<span data-ttu-id="78f74-247">Kullanarak bir tanımlama grubu içindeki her alan için örtük olarak yazılan değişkenler de bildirebilirsiniz `var` anahtar sözcüğü parantez dışında:</span><span class="sxs-lookup"><span data-stu-id="78f74-247">You can also declare implicitly typed variables for each field in a tuple by using the `var` keyword outside the parentheses:</span></span>

[!code-csharp[DeconstructToVar](../../samples/snippets/csharp/tuples/tuples/statistics.cs#11_DeconstructToVar "Deconstruct to Var")]

<span data-ttu-id="78f74-248">Kullanmak için uygundur `var` , bir bölümünü veya tamamını parantez içinde değişken bildirimleri olan anahtar sözcük.</span><span class="sxs-lookup"><span data-stu-id="78f74-248">It is also legal to use the `var` keyword with any, or all of the variable declarations inside the parentheses.</span></span> 

```csharp
(double sum, var sumOfSquares, var count) = ComputeSumAndSumOfSquares(sequence);
```

<span data-ttu-id="78f74-249">Dizideki her alan aynı türde olsa bile belirli bir tür parantez dışında kullanamayacağınızı unutmayın.</span><span class="sxs-lookup"><span data-stu-id="78f74-249">Note that you cannot use a specific type outside the parentheses, even if every field in the tuple has the same type.</span></span>

<span data-ttu-id="78f74-250">Varolan bildirimleri de içeren başlık deconstruct:</span><span class="sxs-lookup"><span data-stu-id="78f74-250">You can deconstruct tuples with existing declarations as well:</span></span>

```csharp
public class Point
{
    public int X { get; set; }
    public int Y { get; set; }

    public Point(int x, int y) => (X, Y) = (x, y);
}
```

> [!WARNING]
>  <span data-ttu-id="78f74-251">Bildirimleri parantez içinde varolan bildirimlerle karıştıramazsınız.</span><span class="sxs-lookup"><span data-stu-id="78f74-251">You cannot mix existing declarations with declarations inside the parentheses.</span></span> <span data-ttu-id="78f74-252">Örneğin, aşağıdaki verilmez: `(var x, y) = MyMethod();`.</span><span class="sxs-lookup"><span data-stu-id="78f74-252">For instance, the following is not allowed: `(var x, y) = MyMethod();`.</span></span> <span data-ttu-id="78f74-253">Bu hata CS8184 çünkü oluşturur *x* parantez içinde bildirilen ve *y* daha önce başka bir yerde bildirilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-253">This produces error CS8184 because *x* is declared inside the parentheses and *y* is previously declared elsewhere.</span></span>

### <a name="deconstructing-user-defined-types"></a><span data-ttu-id="78f74-254">Deconstructing kullanıcı tanımlı türler</span><span class="sxs-lookup"><span data-stu-id="78f74-254">Deconstructing user defined types</span></span>

<span data-ttu-id="78f74-255">Herhangi bir tanımlama grubu türü yukarıda gösterildiği gibi deconstructed.</span><span class="sxs-lookup"><span data-stu-id="78f74-255">Any tuple type can be deconstructed as shown above.</span></span> <span data-ttu-id="78f74-256">Kullanıcı tanımlı türler (sınıflar, yapılar veya hatta arabirimleri) üzerinde deconstruction etkinleştirmek kolaydır.</span><span class="sxs-lookup"><span data-stu-id="78f74-256">It's also easy to enable deconstruction on any user defined type (classes, structs, or even interfaces).</span></span>

<span data-ttu-id="78f74-257">Bir veya daha fazla türü yazarı tanımlayabilirsiniz `Deconstruct` herhangi bir sayıda için değerler atayın yöntemleri `out` türü olun veri öğelerini temsil eden değişkenleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-257">The type author can define one or more `Deconstruct` methods that assign values to any number of `out` variables representing the data elements that make up the type.</span></span> <span data-ttu-id="78f74-258">Örneğin, aşağıdaki `Person` türünü tanımlayan bir `Deconstruct` bir kişi nesnesi ilk ad ve soyadı temsil eden elemanlara deconstructs yöntemi:</span><span class="sxs-lookup"><span data-stu-id="78f74-258">For example, the following `Person` type defines a `Deconstruct` method that deconstructs a person object into the elements representing the first name and last name:</span></span>

[!code-csharp[TypeWithDeconstructMethod](../../samples/snippets/csharp/tuples/tuples/person.cs#12_TypeWithDeconstructMethod "Type with a deconstruct method")]

<span data-ttu-id="78f74-259">Bir atama deconstruct yöntemi etkinleştirir bir `Person` temsil eden iki dizelere `FirstName` ve `LastName` özellikleri:</span><span class="sxs-lookup"><span data-stu-id="78f74-259">The deconstruct method enables assignment from a `Person` to two strings, representing the `FirstName` and `LastName` properties:</span></span>

[!code-csharp[Deconstruct Type](../../samples/snippets/csharp/tuples/tuples/program.cs#12A_DeconstructType "Deconstruct a class type")]

<span data-ttu-id="78f74-260">Deconstruction bile değil Yazar türleri için etkinleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="78f74-260">You can enable deconstruction even for types you did not author.</span></span>
<span data-ttu-id="78f74-261">`Deconstruct` Yöntemi, bir nesnenin erişilebilir veri üyelerine tutucusu paketten çıkarır bir genişletme yöntemi olabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-261">The `Deconstruct` method can be an extension method that unpackages the accessible data members of an object.</span></span> <span data-ttu-id="78f74-262">Gösterir aşağıdaki örnekte bir `Student` öğesinden türetilen tür `Person` türü ve deconstructs bir genişletme yöntemi bir `Student` temsil eden üç değişkenleri içine `FirstName`, `LastName` ve `GPA`:</span><span class="sxs-lookup"><span data-stu-id="78f74-262">The example below shows a `Student` type, derived from the `Person` type, and an extension method that deconstructs a `Student` into three variables, representing the `FirstName`, the `LastName` and the `GPA`:</span></span>

[!code-csharp[ExtensionDeconstructMethod](../../samples/snippets/csharp/tuples/tuples/person.cs#13_ExtensionDeconstructMethod "Type with a deconstruct extension method")]

<span data-ttu-id="78f74-263">A `Student` nesne artık sahiptir erişilebilir iki `Deconstruct` yöntemleri: uzantı yöntemi için bildirilen `Student` türleri ve üyesi `Person` türü.</span><span class="sxs-lookup"><span data-stu-id="78f74-263">A `Student` object now has two accessible `Deconstruct` methods: the extension method declared for `Student` types, and the member of the `Person` type.</span></span> <span data-ttu-id="78f74-264">Her ikisi de kapsamındaki ve sağlayan bir `Student` iki değişkeni veya üç deconstructed için.</span><span class="sxs-lookup"><span data-stu-id="78f74-264">Both are in scope, and that enables a `Student` to be deconstructed into either two variables or three.</span></span>
<span data-ttu-id="78f74-265">Üç değişkenlere öğrencinin atarsanız, tüm ad, son adı ve GPA döndürdü.</span><span class="sxs-lookup"><span data-stu-id="78f74-265">If you assign a student to three variables, the first name, last name, and GPA are all returned.</span></span> <span data-ttu-id="78f74-266">İki değişken öğrencinin atarsanız, yalnızca ilk ad ve Soyadı döndürülür.</span><span class="sxs-lookup"><span data-stu-id="78f74-266">If you assign a student to two variables, only the first name and the last name are returned.</span></span>

[!code-csharp[Deconstruct extension method](../../samples/snippets/csharp/tuples/tuples/program.cs#13A_DeconstructExtension "Deconstruct a class type using an extension method")]

<span data-ttu-id="78f74-267">Birden çok tanımlama çok dikkatli olmalıdır `Deconstruct` yöntemleri bir sınıf ya da bir sınıf hiyerarşisi.</span><span class="sxs-lookup"><span data-stu-id="78f74-267">You should be very careful defining multiple `Deconstruct` methods in a class or a class hierarchy.</span></span> <span data-ttu-id="78f74-268">Birden çok `Deconstruct` aynı sayıda yöntemleri `out` parametreleri hızla belirsizliğe neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="78f74-268">Multiple `Deconstruct` methods that have the same number of `out` parameters can quickly cause ambiguities.</span></span> <span data-ttu-id="78f74-269">Arayanlar kolayca istenen arayabilmesi için olmayabilir `Deconstruct` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="78f74-269">Callers may not be able to easily call the desired `Deconstruct` method.</span></span>

<span data-ttu-id="78f74-270">Bu örnekte, yoktur belirsiz bir çağrı için en az fırsat olmadığından `Deconstruct` yöntemi `Person` iki parametreleri çıktısı ve `Deconstruct` yöntemi `Student` üç sahiptir.</span><span class="sxs-lookup"><span data-stu-id="78f74-270">In this example, there is minimal chance for an ambiguous call because the `Deconstruct` method for `Person` has two output parameters, and the `Deconstruct` method for `Student` has three.</span></span>

## <a name="conclusion"></a><span data-ttu-id="78f74-271">Sonuç</span><span class="sxs-lookup"><span data-stu-id="78f74-271">Conclusion</span></span> 

<span data-ttu-id="78f74-272">Adlandırılmış diziler yeni dil ve kitaplık desteği sınıfları ve yapıları yaptığınız gibi birden çok öğe depolamak ancak davranışı tanımlamayın veri yapılarını kullanan tasarımlar ile çalışmak çok daha kolay hale getirir.</span><span class="sxs-lookup"><span data-stu-id="78f74-272">The new language and library support for named tuples makes it much easier to work with designs that use data structures that store multiple elements but do not define behavior, as classes and structs do.</span></span> <span data-ttu-id="78f74-273">Kolay ve Diziler bu türleri için kullanılacak kısa.</span><span class="sxs-lookup"><span data-stu-id="78f74-273">It's easy and concise to use tuples for those types.</span></span> <span data-ttu-id="78f74-274">Daha ayrıntılı kullanarak türleri yazmak zorunda kalmadan statik tür denetlemesi tüm avantajlarını elde `class` veya `struct` sözdizimi.</span><span class="sxs-lookup"><span data-stu-id="78f74-274">You get all the benefits of static type checking, without needing to author types using the more verbose `class` or `struct` syntax.</span></span> <span data-ttu-id="78f74-275">Buna rağmen yardımcı program yöntemleri için en kullanışlı `private`, veya `internal`.</span><span class="sxs-lookup"><span data-stu-id="78f74-275">Even so, they are most useful for utility methods that are `private`, or `internal`.</span></span> <span data-ttu-id="78f74-276">Kullanıcı tanımlı türler ya da oluşturma `class` veya `struct` ortak yöntemlerinizi birden çok öğeye sahip bir değer döndüğünüzde türleri.</span><span class="sxs-lookup"><span data-stu-id="78f74-276">Create user defined types, either `class` or `struct` types when your public methods return a value that has multiple elements.</span></span>
