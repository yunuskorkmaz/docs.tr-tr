---
title: C# 7,2 sürümündeki yenilikler
description: C# 7,2 sürümündeki yeni özelliklere genel bakış.
ms.date: 08/16/2017
ms.openlocfilehash: 7febefb81bbea6f24690adb05488ad6a18bbf552
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/07/2020
ms.locfileid: "75694601"
---
# <a name="whats-new-in-c-72"></a><span data-ttu-id="bbbde-103">C# 7,2 sürümündeki yenilikler</span><span class="sxs-lookup"><span data-stu-id="bbbde-103">What's new in C# 7.2</span></span>

<span data-ttu-id="bbbde-104">C#7,2, çok sayıda yararlı özellik ekleyen başka bir nokta sürümüdür.</span><span class="sxs-lookup"><span data-stu-id="bbbde-104">C# 7.2 is another point release that adds a number of useful features.</span></span>
<span data-ttu-id="bbbde-105">Bu yayın için bir tema gereksiz kopyaları veya ayırmaları önleyerek değer türleriyle daha verimli çalışıyor.</span><span class="sxs-lookup"><span data-stu-id="bbbde-105">One theme for this release is working more efficiently with value types by avoiding unnecessary copies or allocations.</span></span>

<span data-ttu-id="bbbde-106">Kalan özellikler küçük, tam özellikli özelliklerdir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-106">The remaining features are small, nice-to-have features.</span></span>

<span data-ttu-id="bbbde-107">C#7,2, derleyici dili sürümünü seçmek için [dil sürümü seçimi](../language-reference/configure-language-version.md) yapılandırma öğesini kullanır.</span><span class="sxs-lookup"><span data-stu-id="bbbde-107">C# 7.2 uses the [language version selection](../language-reference/configure-language-version.md) configuration element to select the compiler language version.</span></span>

<span data-ttu-id="bbbde-108">Bu sürümdeki yeni dil özellikleri şunlardır:</span><span class="sxs-lookup"><span data-stu-id="bbbde-108">The new language features in this release are:</span></span>

- [<span data-ttu-id="bbbde-109">Güvenli verimli kod yazma teknikleri</span><span class="sxs-lookup"><span data-stu-id="bbbde-109">Techniques for writing safe efficient code</span></span>](#safe-efficient-code-enhancements)
  - <span data-ttu-id="bbbde-110">Başvuru semantiğinin kullanıldığı değer türleriyle çalışmayı sağlayan sözdizimi geliştirmelerinden oluşan bir bileşim.</span><span class="sxs-lookup"><span data-stu-id="bbbde-110">A combination of syntax improvements that enable working with value types using reference semantics.</span></span>
- [<span data-ttu-id="bbbde-111">Girintili olmayan adlandırılmış bağımsız değişkenler</span><span class="sxs-lookup"><span data-stu-id="bbbde-111">Non-trailing named arguments</span></span>](#non-trailing-named-arguments)
  - <span data-ttu-id="bbbde-112">Adlandırılmış bağımsız değişkenlerin ardından konumsal bağımsız değişkenler gelebilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-112">Named arguments can be followed by positional arguments.</span></span>
- [<span data-ttu-id="bbbde-113">Sayısal sabit değerlerde önde gelen alt çizgiler</span><span class="sxs-lookup"><span data-stu-id="bbbde-113">Leading underscores in numeric literals</span></span>](#leading-underscores-in-numeric-literals)
  - <span data-ttu-id="bbbde-114">Sayısal değişmez değerler artık, yazdırılan rakamlardan önce önde gelen alt çizgileri olabilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-114">Numeric literals can now have leading underscores before any printed digits.</span></span>
- [<span data-ttu-id="bbbde-115">`private protected` erişim değiştiricisi</span><span class="sxs-lookup"><span data-stu-id="bbbde-115">`private protected` access modifier</span></span>](#private-protected-access-modifier)
  - <span data-ttu-id="bbbde-116">`private protected` erişim değiştiricisi aynı derlemede türetilmiş sınıfların erişimine izin vermez.</span><span class="sxs-lookup"><span data-stu-id="bbbde-116">The `private protected` access modifier enables access for derived classes in the same assembly.</span></span>
- [<span data-ttu-id="bbbde-117">Koşullu `ref` ifadeleri</span><span class="sxs-lookup"><span data-stu-id="bbbde-117">Conditional `ref` expressions</span></span>](#conditional-ref-expressions)
  - <span data-ttu-id="bbbde-118">Koşullu ifadenin sonucu (`?:`) artık bir başvuru olabilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-118">The result of a conditional expression (`?:`) can now be a reference.</span></span>

<span data-ttu-id="bbbde-119">Bu makalenin geri kalanında her özelliğe bir genel bakış sunulmaktadır.</span><span class="sxs-lookup"><span data-stu-id="bbbde-119">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="bbbde-120">Her bir özellik için, arkasında yatan bir düşünme olduğunu öğrenirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bbbde-120">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="bbbde-121">Söz dizimini öğrenirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bbbde-121">You'll learn the syntax.</span></span> <span data-ttu-id="bbbde-122">Ortamınızdaki bu özellikleri, `dotnet try` genel aracını kullanarak inceleyebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bbbde-122">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="bbbde-123">[DotNet-TRY](https://github.com/dotnet/try/blob/master/README.md#setup) küresel aracını yükler.</span><span class="sxs-lookup"><span data-stu-id="bbbde-123">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="bbbde-124">[DotNet/TRY-Samples](https://github.com/dotnet/try-samples) deposunu kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="bbbde-124">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="bbbde-125">*TRY-Samples* deposu için geçerli dizini *csharp7* alt dizinine ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="bbbde-125">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="bbbde-126">`dotnet try`'i çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="bbbde-126">Run `dotnet try`.</span></span>

## <a name="safe-efficient-code-enhancements"></a><span data-ttu-id="bbbde-127">Güvenli verimli kod geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="bbbde-127">Safe efficient code enhancements</span></span>

<span data-ttu-id="bbbde-128">7,2 ' de tanıtılan dil özellikleri, başvuru semantiğinin kullanıldığı sırada değer türleriyle çalışmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="bbbde-128">Language features introduced in 7.2 let you work with value types while using reference semantics.</span></span> <span data-ttu-id="bbbde-129">Başvuru türleri kullanılarak ilişkili bellek ayırmalarını oluşturmadan değer türlerini kopyalamayı en aza indirerek performansı artırmak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="bbbde-129">They are designed to increase performance by minimizing copying value types without incurring the memory allocations associated with using reference types.</span></span> <span data-ttu-id="bbbde-130">Özellikler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="bbbde-130">The features include:</span></span>

- <span data-ttu-id="bbbde-131">Bir bağımsız değişkenin başvuruya göre geçirilmesini, ancak çağrılan yöntem tarafından değiştirilmediğini belirtmek için parametrelerde `in` değiştirici.</span><span class="sxs-lookup"><span data-stu-id="bbbde-131">The `in` modifier on parameters, to specify that an argument is passed by reference but not modified by the called method.</span></span> <span data-ttu-id="bbbde-132">`in` değiştiricisini bir bağımsız değişkene eklemek, kaynak ile [uyumlu bir değişikdir](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="bbbde-132">Adding the `in` modifier to an argument is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span>
- <span data-ttu-id="bbbde-133">Yöntem üzerinde `ref readonly` değiştirici, bir yöntemin değerini başvuruya göre döndürdüğünü ancak bu nesneye yazma izni olmadığını belirtmek için döndürür.</span><span class="sxs-lookup"><span data-stu-id="bbbde-133">The `ref readonly` modifier on method returns, to indicate that a method returns its value by reference but doesn't allow writes to that object.</span></span> <span data-ttu-id="bbbde-134">`ref readonly` değiştiricisini eklemek, return değeri bir değere atanmışsa, [kaynak ile uyumlu bir değişiklik](version-update-considerations.md#source-compatible-changes)olur.</span><span class="sxs-lookup"><span data-stu-id="bbbde-134">Adding the `ref readonly` modifier is a [source compatible change](version-update-considerations.md#source-compatible-changes), if the return is assigned to a value.</span></span> <span data-ttu-id="bbbde-135">`readonly` değiştiricisini var olan bir `ref` Return ifadesine eklemek [uyumsuz bir değişiklik](version-update-considerations.md#incompatible-changes).</span><span class="sxs-lookup"><span data-stu-id="bbbde-135">Adding the `readonly` modifier to an existing `ref` return statement is an [incompatible change](version-update-considerations.md#incompatible-changes).</span></span> <span data-ttu-id="bbbde-136">Çağıranların `readonly` değiştiricisini içermesi için `ref` yerel değişkenlerin bildirimini güncelleştirmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-136">It requires callers to update the declaration of `ref` local variables to include the `readonly` modifier.</span></span>
- <span data-ttu-id="bbbde-137">Bir yapının sabit olduğunu ve onun üye yöntemlerine bir `in` parametresi olarak geçirilmesi gerektiğini göstermek için `readonly struct` bildirimi.</span><span class="sxs-lookup"><span data-stu-id="bbbde-137">The `readonly struct` declaration, to indicate that a struct is immutable and should be passed as an `in` parameter to its member methods.</span></span> <span data-ttu-id="bbbde-138">`readonly` değiştiricisini varolan bir struct bildirimine eklemek, [ikili uyumlu bir değişikdir](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="bbbde-138">Adding the `readonly` modifier to an existing struct declaration is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>
- <span data-ttu-id="bbbde-139">Bir struct türünün doğrudan yönetilen belleğe eriştiğini ve her zaman yığın ayrılması gerektiğini göstermek için `ref struct` bildirimi.</span><span class="sxs-lookup"><span data-stu-id="bbbde-139">The `ref struct` declaration, to indicate that a struct type accesses managed memory directly and must always be stack allocated.</span></span> <span data-ttu-id="bbbde-140">`ref` değiştiricisini mevcut bir `struct` bildirimine eklemek [uyumsuz bir değişiklik](version-update-considerations.md#incompatible-changes).</span><span class="sxs-lookup"><span data-stu-id="bbbde-140">Adding the `ref` modifier to an existing `struct` declaration is an [incompatible change](version-update-considerations.md#incompatible-changes).</span></span> <span data-ttu-id="bbbde-141">`ref struct`, bir sınıfın üyesi olamaz veya yığın üzerinde ayrılabileceği diğer konumlarda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-141">A `ref struct` cannot be a member of a class or used in other locations where it may be allocated on the heap.</span></span>

<span data-ttu-id="bbbde-142">Bu değişiklikler hakkında daha fazla bilgi için [yazma güvenli verimli kod](../write-safe-efficient-code.md)' a erişebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bbbde-142">You can read more about all these changes in [Write safe efficient code](../write-safe-efficient-code.md).</span></span>

## <a name="non-trailing-named-arguments"></a><span data-ttu-id="bbbde-143">Girintili olmayan adlandırılmış bağımsız değişkenler</span><span class="sxs-lookup"><span data-stu-id="bbbde-143">Non-trailing named arguments</span></span>

<span data-ttu-id="bbbde-144">Yöntem çağrıları artık, adlandırılmış bağımsız değişkenler doğru konumlarda olduğunda Konumsal bağımsız değişkenlerden önce gelen adlandırılmış bağımsız değişkenleri kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-144">Method calls may now use named arguments that precede positional arguments when those named arguments are in the correct positions.</span></span> <span data-ttu-id="bbbde-145">Daha fazla bilgi için bkz. [adlandırılmış ve isteğe bağlı bağımsız değişkenler](../programming-guide/classes-and-structs/named-and-optional-arguments.md).</span><span class="sxs-lookup"><span data-stu-id="bbbde-145">For more information see [Named and optional arguments](../programming-guide/classes-and-structs/named-and-optional-arguments.md).</span></span>

## <a name="leading-underscores-in-numeric-literals"></a><span data-ttu-id="bbbde-146">Sayısal sabit değerlerde önde gelen alt çizgiler</span><span class="sxs-lookup"><span data-stu-id="bbbde-146">Leading underscores in numeric literals</span></span>

<span data-ttu-id="bbbde-147">7,0 ' de C# basamak ayırıcıları için destek uygulanması, `_` değişmez değerin ilk karakteri olarak izin vermedi.</span><span class="sxs-lookup"><span data-stu-id="bbbde-147">The implementation of support for digit separators in C# 7.0 didn't allow the `_` to be the first character of the literal value.</span></span> <span data-ttu-id="bbbde-148">Onaltılık ve ikili sayısal değişmez değerler artık `_`başlayabilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-148">Hex and binary numeric literals may now begin with an `_`.</span></span>

<span data-ttu-id="bbbde-149">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="bbbde-149">For example:</span></span>

```csharp
int binaryValue = 0b_0101_0101;
```

## <a name="private-protected-access-modifier"></a><span data-ttu-id="bbbde-150">*özel korumalı* erişim değiştiricisi</span><span class="sxs-lookup"><span data-stu-id="bbbde-150">*private protected* access modifier</span></span>

<span data-ttu-id="bbbde-151">Yeni bir bileşik erişim değiştiricisi: `private protected`, bir üyenin aynı derlemede belirtilen sınıf veya türetilmiş sınıflar ile erişilebilir olabileceğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-151">A new compound access modifier: `private protected` indicates that a member may be accessed by containing class or derived classes that are declared in the same assembly.</span></span> <span data-ttu-id="bbbde-152">`protected internal` aynı derlemede bulunan türetilmiş sınıfların veya sınıfların erişimine izin verdiğinden, `private protected` aynı derlemede belirtilen türetilmiş türlere erişimi kısıtlar.</span><span class="sxs-lookup"><span data-stu-id="bbbde-152">While `protected internal` allows access by derived classes or classes that are in the same assembly, `private protected` limits access to derived types declared in the same assembly.</span></span>

<span data-ttu-id="bbbde-153">Daha fazla bilgi için bkz. dil başvurusunda [erişim değiştiriciler](../language-reference/keywords/access-modifiers.md) .</span><span class="sxs-lookup"><span data-stu-id="bbbde-153">For more information see [access modifiers](../language-reference/keywords/access-modifiers.md) in the language reference.</span></span>

## <a name="conditional-ref-expressions"></a><span data-ttu-id="bbbde-154">Koşullu `ref` ifadeleri</span><span class="sxs-lookup"><span data-stu-id="bbbde-154">Conditional `ref` expressions</span></span>

<span data-ttu-id="bbbde-155">Son olarak, koşullu ifade bir değer sonucu yerine bir başvuru sonucu üretebilir.</span><span class="sxs-lookup"><span data-stu-id="bbbde-155">Finally, the conditional expression may produce a ref result instead of a value result.</span></span> <span data-ttu-id="bbbde-156">Örneğin, iki diziden birindeki ilk öğeye başvuru almak için aşağıdakini yazın:</span><span class="sxs-lookup"><span data-stu-id="bbbde-156">For example, you would write the following to retrieve a reference to the first element in one of two arrays:</span></span>

```csharp
ref var r = ref (arr != null ? ref arr[0] : ref otherArr[0]);
```

<span data-ttu-id="bbbde-157">`r` değişkeni, `arr` veya `otherArr`ilk değerine bir başvurudur.</span><span class="sxs-lookup"><span data-stu-id="bbbde-157">The variable `r` is a reference to the first value in either `arr` or `otherArr`.</span></span>

<span data-ttu-id="bbbde-158">Daha fazla bilgi için bkz. dil başvurusunda [koşullu işleç (?:)](../language-reference/operators/conditional-operator.md) .</span><span class="sxs-lookup"><span data-stu-id="bbbde-158">For more information, see [conditional operator (?:)](../language-reference/operators/conditional-operator.md) in the language reference.</span></span>
