---
title: "C# 7 - C# Kılavuzu yenilikler nelerdir?"
description: "C# dili gelecek sürümünde 7 gelen yeni özelliklere genel bakış alın."
keywords: "Yenilikler C#, .NET, .NET Core, en son özellikleri"
author: BillWagner
ms.author: wiwagn
ms.date: 12/21/2016
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: f98039404789e8886154e04c4b97a21741c4d885
ms.sourcegitcommit: bbde43da655ae7bea1977f7af7345eb87bd7fd5f
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/21/2017
---
# <a name="whats-new-in-c-7"></a><span data-ttu-id="31ef3-104">C# 7'deki yenilikler</span><span class="sxs-lookup"><span data-stu-id="31ef3-104">What's new in C# 7</span></span>

<span data-ttu-id="31ef3-105">C# 7 C# dili için birtakım yeni özellikleri ekler:</span><span class="sxs-lookup"><span data-stu-id="31ef3-105">C# 7 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="31ef3-106">`out`değişkenleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-106">`out` variables</span></span>](#out-variables)
    - <span data-ttu-id="31ef3-107">Bildirebilirsiniz `out` bunlar burada kullanılır yöntemi bağımsız değişken olarak satır içi değerleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-107">You can declare `out` values inline as arguments to the method where they are used.</span></span>
* [<span data-ttu-id="31ef3-108">Diziler</span><span class="sxs-lookup"><span data-stu-id="31ef3-108">Tuples</span></span>](#tuples)
    - <span data-ttu-id="31ef3-109">Birden çok genel alanlar içeren basit, adlandırılmamış türleri oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-109">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="31ef3-110">Bu tür semantiği derleyicileri ve IDE araçları anlayın.</span><span class="sxs-lookup"><span data-stu-id="31ef3-110">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="31ef3-111">Atar</span><span class="sxs-lookup"><span data-stu-id="31ef3-111">Discards</span></span>](#discards)
    - <span data-ttu-id="31ef3-112">İptali atanan değer hakkında önemli değil atamalarını kullanılan geçici, yalnızca yazma değişkenlerdir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-112">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="31ef3-113">Tanımlama grupları ve kullanıcı tanımlı türler deconstructing zaman yanı sıra yöntemleri çağrılırken özellikle yararlı oldukları `out` parametreleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-113">They are particularly useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="31ef3-114">Desen eşleştirme</span><span class="sxs-lookup"><span data-stu-id="31ef3-114">Pattern Matching</span></span>](#pattern-matching)
    - <span data-ttu-id="31ef3-115">Rastgele türler ve bu türlerde üyelerinin değerlerine dayalı dallandırma mantığını oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-115">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="31ef3-116">`ref`Yerel öğeler ve döndürür</span><span class="sxs-lookup"><span data-stu-id="31ef3-116">`ref` locals and returns</span></span>](#ref-locals-and-returns)
    - <span data-ttu-id="31ef3-117">Yöntem bağımsız değişkenleri ve yerel değişkenler diğer depolama başvurular olabilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-117">Method arguments and local variables can be references to other storage.</span></span>
* [<span data-ttu-id="31ef3-118">Yerel İşlevler</span><span class="sxs-lookup"><span data-stu-id="31ef3-118">Local Functions</span></span>](#local-functions)
    - <span data-ttu-id="31ef3-119">Kendi kapsam ve görünürlük sınırlamak için diğer işlevleri iç işlevleri iç içe.</span><span class="sxs-lookup"><span data-stu-id="31ef3-119">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="31ef3-120">Daha fazla ifade bodied üyeleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-120">More expression-bodied members</span></span>](#more-expression-bodied-members)
    - <span data-ttu-id="31ef3-121">İfadeler kullanarak yazılabilir üyelerin listesi haline geldi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-121">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="31ef3-122">`throw`İfadeler</span><span class="sxs-lookup"><span data-stu-id="31ef3-122">`throw` Expressions</span></span>](#throw-expressions)
    - <span data-ttu-id="31ef3-123">Özel durumlar, daha önce olduğundan verilmez kod yapılardan throw `throw` bir ifadesi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-123">You can throw exceptions in code constructs that previously were not allowed because `throw` was a statement.</span></span> 
* [<span data-ttu-id="31ef3-124">Genelleştirilmiş zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-124">Generalized async return types</span></span>](#generalized-async-return-types)
    - <span data-ttu-id="31ef3-125">Yöntemleri bildirilen ile `async` değiştiricisi ek olarak diğer türleri dönebilirsiniz `Task` ve `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-125">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="31ef3-126">Değişmez sayısal sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-126">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
    - <span data-ttu-id="31ef3-127">Yeni belirteçleri Sayısal sabitler için okunabilirliğini artırmak.</span><span class="sxs-lookup"><span data-stu-id="31ef3-127">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="31ef3-128">Bu konunun geri kalanında özelliklerin her biri açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-128">The remainder of this topic discusses each of the features.</span></span> <span data-ttu-id="31ef3-129">Her bir özellik için bunun arkasındaki mantığı öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-129">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="31ef3-130">Sözdizimi öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-130">You'll learn the syntax.</span></span> <span data-ttu-id="31ef3-131">Burada yeni özelliği kullanarak, bir uygulama geliştiricisi olarak daha verimli hale getirir bazı örnek senaryolar görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-131">You'll see some sample scenarios where using the new feature will make you more productive as a developer.</span></span> 

## <a name="out-variables"></a><span data-ttu-id="31ef3-132">`out`değişkenleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-132">`out` variables</span></span>

<span data-ttu-id="31ef3-133">Destekleyen mevcut sözdizimi `out` parametreleri bu sürümde geliştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-133">The existing syntax that supports `out` parameters has been improved in this version.</span></span>  

<span data-ttu-id="31ef3-134">Daha önce out değişkeni ve onun başlatma iki farklı deyimleri içine bildirimi ayrı gerekir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-134">Previously, you would need to separate the declaration of the out variable and its initialization into two different statements:</span></span>

[!code-csharp[OutVariableOldStyle](../../../samples/snippets/csharp/new-in-7/program.cs#03_OutVariableOldStyle "classic out variable declaration")]

<span data-ttu-id="31ef3-135">Şimdi bildirebilir `out` ayrı bildirimi deyimi yazma yerine bir yöntem çağrısı bağımsız değişken listesinde değişkenleri:</span><span class="sxs-lookup"><span data-stu-id="31ef3-135">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#01_OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="31ef3-136">Türünü belirtmek isteyebilirsiniz `out` anlaşılması için değişken yukarıda gösterildiği gibi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-136">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="31ef3-137">Ancak, dil türü örtük olarak belirlenmiş yerel değişken kullanarak destekler:</span><span class="sxs-lookup"><span data-stu-id="31ef3-137">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#02_OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="31ef3-138">Kod okumak daha kolay olur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-138">The code is easier to read.</span></span> 
    - <span data-ttu-id="31ef3-139">Olmayan başka bir satıra yukarıdaki kullandığınız out değişken bildirin.</span><span class="sxs-lookup"><span data-stu-id="31ef3-139">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="31ef3-140">Bir başlangıç değeri atamak gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-140">No need to assign an initial value.</span></span>
    - <span data-ttu-id="31ef3-141">Bildirme tarafından `out` kendisine atanmış önce bir yöntem çağrısı kullanıldığı değişken, yanlışlıkla kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="31ef3-141">By declaring the `out` variable where it is used in a method call, you can't accidentally use it before it is assigned.</span></span>

<span data-ttu-id="31ef3-142">Bu özellik için en yaygın kullanımı olacaktır `Try` düzeni.</span><span class="sxs-lookup"><span data-stu-id="31ef3-142">The most common use for this feature will be the `Try` pattern.</span></span> <span data-ttu-id="31ef3-143">Bu modelinde bir yöntem bir `bool` başarı veya başarısızlık belirten ve bir `out` yöntem başarılı olursa, sonuç sağlayan değişkeni.</span><span class="sxs-lookup"><span data-stu-id="31ef3-143">In this pattern, a method returns a `bool` indicating success or failure and an `out` variable that provides the result if the method succeeds.</span></span>

<span data-ttu-id="31ef3-144">Kullanırken `out` değişken bildirimi bildirilen değişkeni "sızdırıyor" IF dış kapsamı içine deyimi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-144">When using the `out` variable declaration, the declared variable "leaks" into the outer scope of the if statement.</span></span> <span data-ttu-id="31ef3-145">Bu değişkeni sonradan kullanmanıza olanak sağlar:</span><span class="sxs-lookup"><span data-stu-id="31ef3-145">This allows you to use the variable afterwards:</span></span>

```csharp
if (!int.TryParse(input, out int result))
{    
    return null;
}

return result;
```

## <a name="tuples"></a><span data-ttu-id="31ef3-146">Demetler</span><span class="sxs-lookup"><span data-stu-id="31ef3-146">Tuples</span></span>

> [!NOTE]
> <span data-ttu-id="31ef3-147">Yeni diziler işlevleri <xref:System.ValueTuple> türleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-147">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="31ef3-148">NuGet paketini eklemeniz gerekir [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) türleri dahil etmeyin platformlarda kullanmak için.</span><span class="sxs-lookup"><span data-stu-id="31ef3-148">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="31ef3-149">Bu framework teslim türlerine dayanan diğer dil özellikleri benzer.</span><span class="sxs-lookup"><span data-stu-id="31ef3-149">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="31ef3-150">Örnek dahil `async` ve `await` güvenmek `INotifyCompletion` arabirimi ve güvenmek LINQ `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-150">Example include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="31ef3-151">Ancak, daha fazla platform bağımsız .NET olma gibi teslim mekanizması değiştiriyor.</span><span class="sxs-lookup"><span data-stu-id="31ef3-151">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="31ef3-152">.NET Framework her zaman aynı tempoyla dil derleyici olarak yükleyeceğiniz değil.</span><span class="sxs-lookup"><span data-stu-id="31ef3-152">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="31ef3-153">Yeni dil özellikleri yeni türlerinde kullanır, dil özellikleri sevk ettiğinizde bu türlerde NuGet paketleri olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-153">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="31ef3-154">Bu yeni tür .NET standart API'sine eklenir ve framework'ün bir parçası olarak teslim olarak NuGet paketi gereksinimi kaldırılacak.</span><span class="sxs-lookup"><span data-stu-id="31ef3-154">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="31ef3-155">C# sınıflar ve tasarım hedefi açıklamak için kullanılan yapılar için zengin bir sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-155">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="31ef3-156">Ancak bazen bu zengin sözdizimi en az avantajı ile ek iş gerektirir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-156">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="31ef3-157">Birden fazla veri öğesi içeren basit bir yapıya gereksinim yöntemleri genellikle yazabilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-157">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="31ef3-158">Bu senaryoları desteklemek için *diziler* C# eklendi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-158">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="31ef3-159">Diziler veri üyeleri göstermek için birden çok alan içeren basit veri yapılarını ' dir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-159">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="31ef3-160">Alanları doğrulanmazsa ve kendi yöntemleri tanımlanamıyor</span><span class="sxs-lookup"><span data-stu-id="31ef3-160">The fields are not validated, and you cannot define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="31ef3-161">Diziler C# 7 önce kullanılabilir, ancak verimsiz ve hiçbir dil desteği vardı.</span><span class="sxs-lookup"><span data-stu-id="31ef3-161">Tuples were available before C# 7, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="31ef3-162">Bu başlığın öğeleri yalnızca olarak başvurulan geliyordu `Item1`, `Item2` ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-162">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="31ef3-163">C# 7 yeni, daha verimli tanımlama grubu türlerini kullanarak bir tanımlama grubu alanlarının anlamsal adlarını etkinleştirir diziler için dil desteği sunar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-163">C# 7 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="31ef3-164">Her üye bir değere atayarak bir tanımlama grubu oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-164">You can create a tuple by assigning each member to a value:</span></span>

[!code-csharp[UnnamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#04_UnnamedTuple "Unnamed tuple")]

<span data-ttu-id="31ef3-165">Atama üyeleri olan bir tanımlama grubu oluşturur `Item1` ve `Item2`, aynı şekilde kullanabileceğiniz <xref:System.Tuple> her bir tanımlama grubu üyeleri anlamsal adları sağlayan bir tanımlama grubu oluşturmak için söz dizimi değiştirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-165">That assignment creates a tuple whose members are `Item1` and `Item2`, which you can use in the same way as <xref:System.Tuple> You can change the syntax to create a tuple that provides semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#05_NamedTuple "Named tuple")]

<span data-ttu-id="31ef3-166">`namedLetters` Tanımlama grubu olarak adlandırılan alanları içeren `Alpha` ve `Beta`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-166">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="31ef3-167">Bu adları yalnızca derleme zamanında mevcut ve örneğin, çalışma zamanında yansıma kullanarak tanımlama grubu incelerken korunmaz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-167">Those names exist only at compile time and are not preserved for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="31ef3-168">Bir tanımlama grubu ataması alanların adlarını atama sağ tarafta belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-168">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#06_ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="31ef3-169">Ad alanları için iki sol ve sağ tarafında atama belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-169">You can specify names for the fields on both the left and right-hand side of the assignment:</span></span>

[!code-csharp[NamedTupleConflict](../../../samples/snippets/csharp/new-in-7/program.cs#07_NamedTupleConflict "Named tuple conflict")]

<span data-ttu-id="31ef3-170">Yukarıdaki satır bir uyarı oluşturur `CS8123`, bunu söyleyen adları atama sağ tarafındaki `Alpha` ve `Beta` sol taraftaki adlarıyla çakıştığından göz ardı edilir `First` ve `Second`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-170">The line above generates a warning, `CS8123`, telling you that the names on the right side of the assignment, `Alpha` and `Beta` are ignored because they conflict with the names on the left side, `First` and `Second`.</span></span>

<span data-ttu-id="31ef3-171">Yukarıdaki örnekler diziler bildirmek için temel söz dizimini gösterir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-171">The examples above show the basic syntax to declare tuples.</span></span> <span data-ttu-id="31ef3-172">Diziler için dönüş türü olarak en yararlı `private` ve `internal` yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-172">Tuples are most useful as return types for `private` and `internal` methods.</span></span> <span data-ttu-id="31ef3-173">Tanımlama grupları birden çok ayrık değer döndürmek bu yöntemleri için basit bir sözdizimi sağlar: yazma çalışmalarınızı kaydedin bir `class` veya `struct` döndürülen türü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-173">Tuples provide a simple syntax for those methods to return multiple discrete values: You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> <span data-ttu-id="31ef3-174">Yeni bir tür oluşturmak için gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-174">There is no need for creating a new type.</span></span>

<span data-ttu-id="31ef3-175">Bir tanımlama grubu oluşturmak daha etkili ve daha verimli olur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-175">Creating a tuple is more efficient and more productive.</span></span>
<span data-ttu-id="31ef3-176">Birden fazla değer taşıyan bir veri yapısı tanımlamak için daha basit ve basit bir sözdizimi şeklindedir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-176">It is a simpler, lightweight syntax to define a data structure that carries more than one value.</span></span> <span data-ttu-id="31ef3-177">Aşağıdaki örnek yöntemi tamsayılar dizisinde bulunan minimum ve maksimum değerleri döndürür:</span><span class="sxs-lookup"><span data-stu-id="31ef3-177">The example method below returns the minimum and maximum values found in a sequence of integers:</span></span>

[!code-csharp[TupleReturningMethod](../../../samples/snippets/csharp/new-in-7/program.cs#08_TupleReturningMethod "Tuple returning method")]

<span data-ttu-id="31ef3-178">Bu şekilde tanımlama gruplarını kullanma birkaç avantaj sunar:</span><span class="sxs-lookup"><span data-stu-id="31ef3-178">Using tuples in this way offers several advantages:</span></span>

* <span data-ttu-id="31ef3-179">Yazma işlemlerini kaydettiğiniz bir `class` veya `struct` döndürülen türü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-179">You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> 
* <span data-ttu-id="31ef3-180">Yeni türü oluşturmak gerekmez.</span><span class="sxs-lookup"><span data-stu-id="31ef3-180">You do not need to create new type.</span></span>
* <span data-ttu-id="31ef3-181">Dil geliştirmeleri kaldırır çağırmak için gereken < xref:System.Tuple.Create``1(``0) > yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-181">The language enhancements removes the need to call the <xref:System.Tuple.Create``1(``0)> methods.</span></span>

<span data-ttu-id="31ef3-182">Yöntem bildirimi döndürülen tanımlama grubu alanlarının adları sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-182">The declaration for the method provides the names for the fields of the tuple that is returned.</span></span> <span data-ttu-id="31ef3-183">Yöntemini çağırdığınızda, dönüş değeri olan alanlar bir tanımlama grubu olan `Max` ve `Min`:</span><span class="sxs-lookup"><span data-stu-id="31ef3-183">When you call the method, the return value is a tuple whose fields are `Max` and `Min`:</span></span>

[!code-csharp[CallingTupleMethod](../../../samples/snippets/csharp/new-in-7/program.cs#09_CallingTupleMethod "Calling a tuple returning method")]

<span data-ttu-id="31ef3-184">Bir yönteminden döndürülen bir tanımlama grubu üyeleri gitmeniz istediğinizde zamanlar olabilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-184">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="31ef3-185">Tanımlama grubundaki değerlerin her biri için ayrı değişkenlerini bildirerek yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-185">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="31ef3-186">Bu adlı *deconstructing* tanımlama:</span><span class="sxs-lookup"><span data-stu-id="31ef3-186">This is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](../../../samples/snippets/csharp/new-in-7/program.cs#10_CallingWithDeconstructor "Deconstructing a tuple")]

<!-- Add wildcards here, if they are in C# 7
-->

<span data-ttu-id="31ef3-187">.NET içinde herhangi bir türü için benzer bir deconstruction sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-187">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="31ef3-188">Bu yazarak yapılır bir `Deconstruct` sınıf üyesi olarak yöntemi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-188">This is done by writing a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="31ef3-189">Olduğunu `Deconstruct` yöntemi sağlayan bir dizi `out` özelliklerin her biri için ayıklamak istediğiniz bağımsız değişkenleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-189">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="31ef3-190">Şunları göz önünde bulundurun `Point` ayıklar deconstructor yöntemi sağlayan sınıf `X` ve `Y` koordinatları:</span><span class="sxs-lookup"><span data-stu-id="31ef3-190">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](../../../samples/snippets/csharp/new-in-7/point.cs#11_PointWithDeconstruction "Point with deconstruction method")]
 
<span data-ttu-id="31ef3-191">Bir tanımlama grubu atayarak tek tek alanların ayıklayabilirsiniz bir `Point`:</span><span class="sxs-lookup"><span data-stu-id="31ef3-191">You can extract the individual fields by assigning a tuple to a `Point`:</span></span>

[!code-csharp[DeconstructPoint](../../../samples/snippets/csharp/new-in-7/program.cs#12_DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="31ef3-192">Tanımlanmış adlarıyla bağlı olmayan `Deconstruct` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-192">You are not bound by the names defined in the `Deconstruct` method.</span></span> <span data-ttu-id="31ef3-193">Extract değişkenleri atama bir parçası olarak yeniden adlandırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-193">You can rename the extract variables as part of the assignment:</span></span>  

[!code-csharp[DeconstructNames](../../../samples/snippets/csharp/new-in-7/program.cs#13_DeconstructNames "Deconstruct with new names")]

<span data-ttu-id="31ef3-194">Diziler hakkında daha fazla giriş derinliği öğrenebilirsiniz [diziler konu](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="31ef3-194">You can learn more in depth about tuples in the [tuples topic](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="31ef3-195">Atar</span><span class="sxs-lookup"><span data-stu-id="31ef3-195">Discards</span></span>

<span data-ttu-id="31ef3-196">Olduğunda, genellikle bir tanımlama grubu deconstructing veya sahip bir yöntemi çağırma `out` parametreleri hakkında önemli değil ve kullanmayı düşünmüyorsanız değeri bir değişkeni tanımlamak zorunda.</span><span class="sxs-lookup"><span data-stu-id="31ef3-196">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="31ef3-197">C# için destek ekler *atar* bu senaryo işlemek için.</span><span class="sxs-lookup"><span data-stu-id="31ef3-197">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="31ef3-198">Bir atma adı olan bir salt yazılır değişkendir `_` (alt çizgi karakteri); tüm tek değişkene atmak istiyorsanız değerleri atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-198">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="31ef3-199">Bir atma gibi atanmamış bir değişkenidir; atama deyimi dışında atma kod içinde kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-199">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="31ef3-200">İptali aşağıdaki senaryolarda desteklenir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-200">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="31ef3-201">Tanımlama grupları ve kullanıcı tanımlı türler deconstructing olduğunda.</span><span class="sxs-lookup"><span data-stu-id="31ef3-201">When deconstructing tuples or user-defined types.</span></span>

* <span data-ttu-id="31ef3-202">Yöntemleri çağrılırken [çıkışı](../language-reference/keywords/out.md) parametreleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-202">When calling methods with [out](../language-reference/keywords/out.md) parameters.</span></span>

* <span data-ttu-id="31ef3-203">İşlemi ile eşleşen bir düzende [olan](../language-reference/keywords/is.md) ve [geçiş](../language-reference/keywords/switch.md) deyimleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-203">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>

* <span data-ttu-id="31ef3-204">Açıkça istediğinizde, bir tek başına tanımlayıcı olarak bir atma olarak atama değerini belirleyin.</span><span class="sxs-lookup"><span data-stu-id="31ef3-204">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="31ef3-205">Aşağıdaki örnek tanımlayan bir `QueryCityDataForYears` 6-bir şehir için bir veri iki farklı yıl içeren tanımlama grubu döndürür yöntemi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-205">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains a data for a city for two different years.</span></span> <span data-ttu-id="31ef3-206">Örnekte yöntem çağrısı yalnızca yöntemi tarafından döndürülen iki doldurma değerleri ile ilgilidir ve böylece kalan değerler tanımlama grubu tanımlama grubu deconstructs zaman atar gibi davranır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-206">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](../../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="31ef3-207">Daha fazla bilgi için bkz: [atar](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="31ef3-207">For more information, see [Discards](../discards.md).</span></span>
 
## <a name="pattern-matching"></a><span data-ttu-id="31ef3-208">Desen eşleştirme</span><span class="sxs-lookup"><span data-stu-id="31ef3-208">Pattern matching</span></span>

<span data-ttu-id="31ef3-209">*Desen eşleştirme* yöntemi gönderme özellikleri bir nesne türünü dışında uygulamak olanak sağlayan bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-209">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="31ef3-210">Büyük olasılıkla zaten aşina olan bir nesne türüne göre yöntemi gönderme.</span><span class="sxs-lookup"><span data-stu-id="31ef3-210">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="31ef3-211">Nesne yönelimli, sanal programlama ve geçersiz kılma yöntemleri yöntemi nesnenin türüne göre göndermeyi uygulamak için dili sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-211">In Object Oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="31ef3-212">Temel ve türetilmiş sınıflarının farklı uygulamaları sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-212">Base and Derived classes provide different implementations.</span></span> <span data-ttu-id="31ef3-213">Böylece bir devralma hiyerarşisi aracılığıyla ilgili olmayan türleri ve veri öğeleri için benzer gönderme desenleri kolayca uygulayabilirsiniz desen eşleştirme ifadeleri bu kavramı genişletir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-213">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that are not related through an inheritance hierarchy.</span></span> 

<span data-ttu-id="31ef3-214">Desen eşleştirme destekler `is` ifadeleri ve `switch` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="31ef3-214">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="31ef3-215">Her bir nesne ve söz konusu nesne Aranan düzeni karşılayan varsa belirlemek üzere özelliklerini inceleme sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-215">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="31ef3-216">Kullandığınız `when` düzeni için ek kurallarını belirtmek için anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="31ef3-216">You use the `when` keyword to specify additional rules to the pattern.</span></span>

### <a name="is-expression"></a><span data-ttu-id="31ef3-217">`is`ifade</span><span class="sxs-lookup"><span data-stu-id="31ef3-217">`is` expression</span></span>

<span data-ttu-id="31ef3-218">`is` Düzeni ifade genişletir bilinen `is` bir nesne türünü ötesinde sorgu işleci.</span><span class="sxs-lookup"><span data-stu-id="31ef3-218">The `is` pattern expression extends the familiar `is` operator to query an object beyond its type.</span></span>

<span data-ttu-id="31ef3-219">Basit bir senaryoyla başlayalım.</span><span class="sxs-lookup"><span data-stu-id="31ef3-219">Let's start with a simple scenario.</span></span> <span data-ttu-id="31ef3-220">Desen eşleştirme ifadeleri kolay iş ile ilgisiz türleri algoritmaları nasıl oluşturduğunu gösteren bu senaryo için özellikleri ekleyeceğiz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-220">We'll add capabilities to this scenario that demonstrate how pattern matching expressions make algorithms that work with unrelated types easy.</span></span> <span data-ttu-id="31ef3-221">Dökme dökümünü sayısı toplamını hesaplar bir yöntem ile başlayacağız:</span><span class="sxs-lookup"><span data-stu-id="31ef3-221">We'll start with a method that computes the sum of a number of die rolls:</span></span>

[!code-csharp[SumDieRolls](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#14_SumDieRolls "Sum die rolls")]

<span data-ttu-id="31ef3-222">Hızlı bir şekilde dökümünü bazıları birden fazla dökme ile yapılan burada dökme toplamını yapar bulmak gerektiğini fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-222">You might quickly find that you need to find the sum of die rolls where some of the rolls are made with more than one die.</span></span> <span data-ttu-id="31ef3-223">Giriş dizisinin bir parçası, tek bir sayı yerine birden çok sonuç olabilir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-223">Part of the input sequence may be multiple results instead of a single number:</span></span>

[!code-csharp[SumDieRollsWithGroups](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#15_SumDieRollsWithGroups "Sum die rolls with groups")]

<span data-ttu-id="31ef3-224">`is` Deseni ifadesi bu senaryoda oldukça iyi çalışır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-224">The `is` pattern expression works quite well in this scenario.</span></span> <span data-ttu-id="31ef3-225">Tür denetleme bir parçası olarak bir değişken başlatma yazma.</span><span class="sxs-lookup"><span data-stu-id="31ef3-225">As part of checking the type, you write a variable initialization.</span></span> <span data-ttu-id="31ef3-226">Doğrulanmış çalışma zamanı türü yeni bir değişken oluşturur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-226">This creates a new variable of the validated runtime type.</span></span>

<span data-ttu-id="31ef3-227">Bu senaryolar genişletme tutmak gibi daha fazla yapı bulabilirsiniz `if` ve `else if` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-227">As you keep extending these scenarios, you may find that you build more `if` and `else if` statements.</span></span> <span data-ttu-id="31ef3-228">Yönetilmeleri olduktan sonra büyük olasılıkla geçiş yapmak istediğiniz `switch` ifadeleri desen.</span><span class="sxs-lookup"><span data-stu-id="31ef3-228">Once that becomes unwieldy, you'll likely want to switch to `switch` pattern expressions.</span></span>

### <a name="switch-statement-updates"></a><span data-ttu-id="31ef3-229">`switch`deyim</span><span class="sxs-lookup"><span data-stu-id="31ef3-229">`switch` statement updates</span></span>

<span data-ttu-id="31ef3-230">*İfade ile eşleşen* sahip dayalı bir bilinen sözdizimini `switch` deyimi C# dili zaten parçası.</span><span class="sxs-lookup"><span data-stu-id="31ef3-230">The *match expression* has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="31ef3-231">Şimdi yeni durumları eklemeden önce bir eşleşme ifadesi kullanmak için var olan kodu çevir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-231">Let's translate the existing code to use a match expression before adding new cases:</span></span> 

[!code-csharp[SumUsingSwitch](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#16_SumUsingSwitch "Sum using switch")]

<span data-ttu-id="31ef3-232">Eşleşme ifadeleri değerinden biraz farklı bir sözdizimi sahip `is` ifadeleri, burada bildirdiğiniz türü ve değişken başındaki `case` ifade.</span><span class="sxs-lookup"><span data-stu-id="31ef3-232">The match expressions have a slightly different syntax than the `is` expressions, where you declare the type and variable at the beginning of the `case` expression.</span></span>

<span data-ttu-id="31ef3-233">Eşleşme ifadeleri sabitleri de destekler.</span><span class="sxs-lookup"><span data-stu-id="31ef3-233">The match expressions also support constants.</span></span> <span data-ttu-id="31ef3-234">Basit durumlar Finansman tarafından bu zamandan tasarruf edebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="31ef3-234">This can save time by factoring out simple cases:</span></span>

[!code-csharp[SwitchWithConstants](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#17_SwitchWithConstants "Switch with constants")]

<span data-ttu-id="31ef3-235">Yukarıdaki kod durumlarda ekler `0` bir özel durum olarak `int`, ve `null` herhangi bir giriş olduğunda bir özel durum olarak.</span><span class="sxs-lookup"><span data-stu-id="31ef3-235">The code above adds cases for `0` as a special case of `int`, and `null` as a special case when there is no input.</span></span> <span data-ttu-id="31ef3-236">Bu anahtar düzeni ifadelerde yeni bir önemli özellik gösterir: sırasını `case` ifadeleri şimdi önemlidir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-236">This demonstrates one important new feature in switch pattern expressions: the order of the `case` expressions now matters.</span></span> <span data-ttu-id="31ef3-237">`0` Durumda önce genel görünür `int` durumda.</span><span class="sxs-lookup"><span data-stu-id="31ef3-237">The `0` case must appear before the general `int` case.</span></span> <span data-ttu-id="31ef3-238">Aksi durumda, eşleştirmek için ilk desen olacak `int` durumda değer olsa bile, `0`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-238">Otherwise, the first pattern to match would be the `int` case, even when the value is `0`.</span></span> <span data-ttu-id="31ef3-239">Bir sonraki servis talebi zaten işlenmiş şekilde eşleşme ifadeleri yanlışlıkla sipariş, derleyici, bayrak ve bir hata oluşturur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-239">If you accidentally order match expressions such that a later case has already been handled, the compiler will flag that and generate an error.</span></span>

<span data-ttu-id="31ef3-240">Bu aynı davranışı boş bir giriş sırası için özel durum sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-240">This same behavior enables the special case for an empty input sequence.</span></span>
<span data-ttu-id="31ef3-241">Görebilirsiniz bu durum bir `IEnumerable` öğeleri içeren öğe genel önce görünmelidir `IEnumerable` durumda.</span><span class="sxs-lookup"><span data-stu-id="31ef3-241">You can see that the case for an `IEnumerable` item that has elements must appear before the general `IEnumerable` case.</span></span>

<span data-ttu-id="31ef3-242">Bu sürüm ayrıca eklenmiş bir `default` durumda.</span><span class="sxs-lookup"><span data-stu-id="31ef3-242">This version has also added a `default` case.</span></span> <span data-ttu-id="31ef3-243">`default` Durumu her zaman değerlendirildiği son, sipariş bağımsız olarak kaynak olarak görünür.</span><span class="sxs-lookup"><span data-stu-id="31ef3-243">The `default` case is always evaluated last, regardless of the order it appears in the source.</span></span> <span data-ttu-id="31ef3-244">Bu nedenle, yerleştirilecek kuraldır `default` servis talebi son.</span><span class="sxs-lookup"><span data-stu-id="31ef3-244">For that reason, convention is to put the `default` case last.</span></span>

<span data-ttu-id="31ef3-245">Son olarak, bir son ekleyelim `case` dökme yeni stili için.</span><span class="sxs-lookup"><span data-stu-id="31ef3-245">Finally, let's add one last `case` for a new style of die.</span></span> <span data-ttu-id="31ef3-246">Bazı oyunlar yüzdebirlik bölmek numaraları büyük aralıklarına göstermek için kullanın.</span><span class="sxs-lookup"><span data-stu-id="31ef3-246">Some games use percentile dice to represent larger ranges of numbers.</span></span> 

> [!NOTE]
> <span data-ttu-id="31ef3-247">İki 10 taraflı yüzdebirlik bölmek 0 ile 99 arasındaki tüm sayıları temsil edebilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-247">Two 10-sided percentile dice can represent every number from 0 through 99.</span></span> <span data-ttu-id="31ef3-248">Bir özel etiketli kenara sahip `00`, `10`, `20`,... `90`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-248">One die has sides labelled `00`, `10`, `20`, ... `90`.</span></span> <span data-ttu-id="31ef3-249">Diğer özel etiketli kenarlara sahip `0`, `1`, `2`,... `9`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-249">The other die has sides labeled `0`, `1`, `2`, ... `9`.</span></span> <span data-ttu-id="31ef3-250">İki dökme değerlerin birlikte ekleyin ve her sayı 0 ile 99 arasında alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-250">Add the two die values together and you can get every number from 0 through 99.</span></span>

<span data-ttu-id="31ef3-251">Bu tür bir dökme koleksiyonunuza eklemek için öncelikle yüzdelik dökme temsil etmek için bir tür tanımlayın:</span><span class="sxs-lookup"><span data-stu-id="31ef3-251">To add this kind of die to your collection, first define a type to represent the percentile die:</span></span>

[!code-csharp[18_PercentileDie](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#18_PercentileDie "Percentile Die type")]

<span data-ttu-id="31ef3-252">Ardından, ekleyin bir `case` yeni türü için ifade ile eşleşen:</span><span class="sxs-lookup"><span data-stu-id="31ef3-252">Then, add a `case` match expression for the new type:</span></span>

[!code-csharp[SwitchWithNewTypes](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#19_SwitchWithNewTypes "Include Percentile Die type")]

<span data-ttu-id="31ef3-253">Desen eşleştirme ifadeleri yeni sözdizimi, nesnenin türü veya kısa ve öz sözdizimini kullanarak diğer özellikleri göre gönderme algoritmaları oluşturmak kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-253">The new syntax for pattern matching expressions makes it easier to create dispatch algorithms based on an object's type, or other properties, using a clear and concise syntax.</span></span> <span data-ttu-id="31ef3-254">Desen eşleştirme ifadeleri tarafından devralma ilişkisiz veri türleri üzerinde bu yapıları etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="31ef3-254">Pattern matching expressions enable these constructs on data types that are unrelated by inheritance.</span></span>

<span data-ttu-id="31ef3-255">Desen ayrılmış konudaki eşleştirme hakkında daha fazla bilgiyi [desen eşleştirme C# dilinde](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="31ef3-255">You can learn more about pattern matching in the topic dedicated to [pattern matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="31ef3-256">Ref Yereller ve döndürür</span><span class="sxs-lookup"><span data-stu-id="31ef3-256">Ref locals and returns</span></span>

<span data-ttu-id="31ef3-257">Bu özelliği kullanmak ve başka bir yerde tanımlanan değişkenler başvurular algoritmaları sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-257">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="31ef3-258">Örneğin, büyük matrisleri ile çalışma ve belirli özelliklere sahip tek bir konum bulma.</span><span class="sxs-lookup"><span data-stu-id="31ef3-258">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="31ef3-259">Bir yöntem tek bir konum için iki dizinlerini matrisinde döndürür:</span><span class="sxs-lookup"><span data-stu-id="31ef3-259">One method would return the two indices for a single location in the matrix:</span></span>

[!code-csharp[FindReturningIndices](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#20_FindReturningIndices "Find returning indices")]

<span data-ttu-id="31ef3-260">Bu kod birçok sorunları vardır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-260">There are many issues with this code.</span></span> <span data-ttu-id="31ef3-261">İlk olarak bir tanımlama grubu döndüren bir ortak yöntem olur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-261">First of all, it's a public method that's returning a tuple.</span></span> <span data-ttu-id="31ef3-262">Bu dil destekler, ancak kullanıcı tanımlı türler (sınıfları veya yapılar) için ortak API'ler tercih edilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-262">The language supports this, but user defined types (either classes or structs) are preferred for public APIs.</span></span>

<span data-ttu-id="31ef3-263">İkinci olarak, bu yöntem dizinlerini Matristeki öğesine döndürüyor.</span><span class="sxs-lookup"><span data-stu-id="31ef3-263">Second, this method is returning the indices to the item in the matrix.</span></span>
<span data-ttu-id="31ef3-264">Bu matris dereference ve tek bir öğe değiştirmek için bu dizinlerini kullanan kod yazmaya arayanlar yol açar:</span><span class="sxs-lookup"><span data-stu-id="31ef3-264">That leads callers to write code that uses those indices to dereference the matrix and modify a single element:</span></span>

[!code-csharp[UpdateItemFromIndices](../../../samples/snippets/csharp/new-in-7/program.cs#21_UpdateItemFromIndices "Update Item From Indices")]

<span data-ttu-id="31ef3-265">Bunun yerine döndüren bir yöntem yazmak istediğiniz bir *başvuru* değiştirmek istediğiniz matrisi öğesine.</span><span class="sxs-lookup"><span data-stu-id="31ef3-265">You'd rather write a method that returns a *reference* to the element of the matrix that you want to change.</span></span> <span data-ttu-id="31ef3-266">Yalnızca güvenli olmayan kod kullanarak ve bir işaretçi döndüren bunu bir `int` önceki sürümlerinde.</span><span class="sxs-lookup"><span data-stu-id="31ef3-266">You could only accomplish this by using unsafe code and returning a pointer to an `int` in previous versions.</span></span>

<span data-ttu-id="31ef3-267">Şimdi değişikliklerin ref yerel özellik göstermek ve dahili depolama başvuru döndüren bir yöntem oluşturma göstermek için bir dizi yol.</span><span class="sxs-lookup"><span data-stu-id="31ef3-267">Let's walk through a series of changes to demonstrate the ref local feature and show how to create a method that returns a reference to internal storage.</span></span>
<span data-ttu-id="31ef3-268">Yol boyunca ref dönüş ve onu kötüye yanlışlıkla korur ref yerel özellik kurallarına öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-268">Along the way, you'll learn the rules of the ref return and ref local feature that protects you from accidentally misusing it.</span></span>

<span data-ttu-id="31ef3-269">Başlangıç değiştirerek `Find` onun döndürecek şekilde yöntem bildirimi bir `ref int` yerine bir tanımlama grubu.</span><span class="sxs-lookup"><span data-stu-id="31ef3-269">Start by modifying the `Find` method declaration so that it returns a `ref int` instead of a tuple.</span></span> <span data-ttu-id="31ef3-270">Ardından, iki dizinlerini yerine Matristeki depolanan değeri döndürecek şekilde return deyimi değiştirin:</span><span class="sxs-lookup"><span data-stu-id="31ef3-270">Then, modify the return statement so it returns the value stored in the matrix instead of the two indices:</span></span>

```csharp
// Note that this won't compile. 
// Method declaration indicates ref return,
// but return statement specifies a value return.
public static ref int Find2(int[,] matrix, Func<int, bool> predicate)
{
    for (int i = 0; i < matrix.GetLength(0); i++)
        for (int j = 0; j < matrix.GetLength(1); j++)
            if (predicate(matrix[i, j]))
                return matrix[i, j];
    throw new InvalidOperationException("Not found");
}
```

<span data-ttu-id="31ef3-271">Ne zaman bildirdiğiniz yöntemi döndürür bir `ref` değişken, ayrıca eklemelisiniz `ref` anahtar sözcüğü her return deyimi için.</span><span class="sxs-lookup"><span data-stu-id="31ef3-271">When you declare that a method returns a `ref` variable, you must also add the `ref` keyword to each return statement.</span></span> <span data-ttu-id="31ef3-272">Return başvuruya göre gösterir ve yöntem başvuruya göre döndürür kodu daha sonra okuma yardımcı geliştiriciler unutmayın:</span><span class="sxs-lookup"><span data-stu-id="31ef3-272">That indicates return by reference, and helps developers reading the code later remember that the method returns by reference:</span></span>

[!code-csharp[FindReturningRef](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#22_FindReturningRef "Find returning by reference")]

<span data-ttu-id="31ef3-273">Yöntem matrisinde tamsayı değeri bir başvuru döndürür, burada adlı değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-273">Now that the method returns a reference to the integer value in the matrix, you need to modify where it's called.</span></span>  <span data-ttu-id="31ef3-274">`var` Bildirimi anlamına `valItem` artık bir `int` bir tanımlama grubu yerine:</span><span class="sxs-lookup"><span data-stu-id="31ef3-274">The `var` declaration means that `valItem` is now an `int` rather than a tuple:</span></span>

[!code-csharp[AssignRefReturnToValue](../../../samples/snippets/csharp/new-in-7/program.cs#23_AssignRefReturnToValue "Assign ref return to value")]

<span data-ttu-id="31ef3-275">İkinci `WriteLine` deyimi yukarıdaki örnekte yazdırır değerini `42`değil `24`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-275">The second `WriteLine` statement in the example above prints out the value `42`, not `24`.</span></span> <span data-ttu-id="31ef3-276">Değişkeni `valItem` olan bir `int`değil bir `ref int`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-276">The variable `valItem` is an `int`, not a `ref int`.</span></span> <span data-ttu-id="31ef3-277">`var` Anahtar sözcüğü türünü belirtmek derleyici sağlar, ancak örtük olarak eklemeyecek `ref` değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-277">The `var` keyword enables the compiler to specify the type, but will not implicitly add the `ref` modifier.</span></span> <span data-ttu-id="31ef3-278">Bunun yerine, değer tarafından başvurulan `ref return` olan *kopyalanan* değişkenine atama sol taraftaki.</span><span class="sxs-lookup"><span data-stu-id="31ef3-278">Instead, the value referred to by the `ref return` is *copied* to the variable on the left-hand side of the assignment.</span></span> <span data-ttu-id="31ef3-279">Değişken değil bir `ref` yerel.</span><span class="sxs-lookup"><span data-stu-id="31ef3-279">The variable is not a `ref` local.</span></span>

<span data-ttu-id="31ef3-280">İstediğiniz sonucu alabilmek için eklemeniz gerekir `ref` değiştiricisi dönüş değeri bir başvuru değişkeni bir başvuru yapmak için yerel değişken bildirimi için:</span><span class="sxs-lookup"><span data-stu-id="31ef3-280">In order to get the result you want, you need to add the `ref` modifier to the local variable declaration to make the variable a reference when the return value is a reference:</span></span>

[!code-csharp[AssignRefReturn](../../../samples/snippets/csharp/new-in-7/program.cs#24_AssignRefReturn "Assign ref return")]

<span data-ttu-id="31ef3-281">Şimdi, ikinci `WriteLine` deyimi yukarıdaki örnekte değerini yazdıracak `24`, Matristeki depolama değiştirildiğini belirten.</span><span class="sxs-lookup"><span data-stu-id="31ef3-281">Now, the second `WriteLine` statement in the example above will print out the value `24`, indicating that the storage in the matrix has been modified.</span></span> <span data-ttu-id="31ef3-282">Yerel değişken ile bildirilen `ref` değiştirici ve alacak bir `ref` döndürür.</span><span class="sxs-lookup"><span data-stu-id="31ef3-282">The local variable has been declared with the `ref` modifier, and it will take a `ref` return.</span></span> <span data-ttu-id="31ef3-283">Başlatması gerekir bir `ref` değişkeni bildirildiğinde, bildirim ve başlatma bölünemez.</span><span class="sxs-lookup"><span data-stu-id="31ef3-283">You must initialize a `ref` variable when it is declared, you cannot split the declaration and the initialization.</span></span>

<span data-ttu-id="31ef3-284">C# dili kötüye kullanmasının korumak diğer üç kurallarına sahiptir `ref` Yereller ve döndürür:</span><span class="sxs-lookup"><span data-stu-id="31ef3-284">The C# language has three other rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="31ef3-285">Bir standart yönteminin dönüş değeri atayamazsınız bir `ref` yerel değişken.</span><span class="sxs-lookup"><span data-stu-id="31ef3-285">You cannot assign a standard method return value to a `ref` local variable.</span></span>
    - <span data-ttu-id="31ef3-286">Aşağıdaki gibi ifadeler izin vermez`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="31ef3-286">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="31ef3-287">Geri dönemezsiniz bir `ref` bir değişkene, yaşam süresi yönteminin yürütülmesi genişletmek değil.</span><span class="sxs-lookup"><span data-stu-id="31ef3-287">You cannot return a `ref` to a variable whose lifetime does not extend beyond the execution of the method.</span></span>
    - <span data-ttu-id="31ef3-288">Yerel bir değişken ya da benzer bir kapsama sahip bir değişken başvuru döndüremez anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-288">That means you cannot return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="31ef3-289">`ref`Yerel öğeler ve döndürür zaman uyumsuz yöntemleri ile kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-289">`ref` locals and returns can't be used with async methods.</span></span>
    - <span data-ttu-id="31ef3-290">Derleyici, zaman uyumsuz yöntem döndüğünde başvurulan değişken son değerine ayarlanmış olmadığını bilemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-290">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="31ef3-291">Ref Yereller ve ref değerleri kopyalama veya birden çok kez bilgileri başvuru kaldırma işlemlerini gerçekleştirme kaçınarak daha verimlidir etkinleştir algoritmaları döndürür.</span><span class="sxs-lookup"><span data-stu-id="31ef3-291">The addition of ref locals and ref returns enable algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span> 

## <a name="local-functions"></a><span data-ttu-id="31ef3-292">Yerel İşlevler</span><span class="sxs-lookup"><span data-stu-id="31ef3-292">Local functions</span></span>

<span data-ttu-id="31ef3-293">Çoğu tasarımları sınıfları için yalnızca tek bir konumdan adlı yöntemleri içerir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-293">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="31ef3-294">Bu ek özel yöntemleri, küçük ve odaklanmış her yöntem tutun.</span><span class="sxs-lookup"><span data-stu-id="31ef3-294">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="31ef3-295">Ancak, bunlar, ilk kez okurken bir sınıf anlaşılması zor yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-295">However, they can make it harder to understand a class when reading it the first time.</span></span> <span data-ttu-id="31ef3-296">Bu yöntemler tek arama konumu bağlamı dışında anlaşılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-296">These methods must be understood outside of the context of the single calling location.</span></span>

<span data-ttu-id="31ef3-297">Bu tasarımları için *yerel işlevler* yöntemlerin başka bir yöntem bağlamı içinde bildirme olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="31ef3-297">For those designs, *local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="31ef3-298">Bu bağlamda olmasından bildirilen yerel yöntemi yalnızca çağrılır olduğunu görmek için sınıf okuyucular için kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-298">This makes it easier for readers of the class to see that the local method is only called from the context in which is it declared.</span></span>

<span data-ttu-id="31ef3-299">İki çok ortak kullanım durumları için yerel işlevler vardır: ortak Yineleyici ve ortak zaman uyumsuz yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-299">There are two very common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="31ef3-300">Her iki tür yöntemi hataları programcıları bekleyebilirsiniz daha sonra rapor kodu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-300">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="31ef3-301">Yineleyici metotları söz konusu olduğunda, yalnızca özel durumlar gözlenen döndürülen dizi numaralandırır kod çağrılırken.</span><span class="sxs-lookup"><span data-stu-id="31ef3-301">In the case of iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="31ef3-302">Zaman uyumsuz yöntemleri söz konusu olduğunda, tüm özel durumlar yalnızca zaman gözlemlenen döndürülen `Task` beklemenin.</span><span class="sxs-lookup"><span data-stu-id="31ef3-302">In the case of async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span>

<span data-ttu-id="31ef3-303">İterator yöntemi ile başlayalım:</span><span class="sxs-lookup"><span data-stu-id="31ef3-303">Let's start with an iterator method:</span></span>

[!code-csharp[IteratorMethod](../../../samples/snippets/csharp/new-in-7/Iterator.cs#25_IteratorMethod "Iterator method")]

<span data-ttu-id="31ef3-304">İterator yöntemi yanlış çağıran aşağıdaki kodu inceleyin:</span><span class="sxs-lookup"><span data-stu-id="31ef3-304">Examine the code below that calls the iterator method incorrectly:</span></span>

[!code-csharp[CallIteratorMethod](../../../samples/snippets/csharp/new-in-7/program.cs#26_CallIteratorMethod "Call iterator method")]

<span data-ttu-id="31ef3-305">Ne zaman özel durum `resultSet` , ne zaman yinelendiğinde değil `resultSet` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-305">The exception is thrown when `resultSet` is iterated, not when `resultSet` is created.</span></span>
<span data-ttu-id="31ef3-306">Çoğu geliştirici, içerilen bu örnekte, hızlı bir şekilde bu sorunu tanılamak.</span><span class="sxs-lookup"><span data-stu-id="31ef3-306">In this contained example, most developers could quickly diagnose the problem.</span></span> <span data-ttu-id="31ef3-307">Ancak, daha büyük olarak kullanılabilecek kod temeli, sonuç numaralandırır kodu gibi yakın genellikle yineleyici oluşturan kodu değil.</span><span class="sxs-lookup"><span data-stu-id="31ef3-307">However, in larger codebases, the code that creates an iterator often isn't as close to the code that enumerates the result.</span></span> <span data-ttu-id="31ef3-308">Böylece tüm bağımsız değişkenler genel yöntem doğrular ve özel bir yöntem numaralandırma oluşturur kodu yeniden düzenleme:</span><span class="sxs-lookup"><span data-stu-id="31ef3-308">You can refactor the code so that the public method validates all arguments, and a private method generates the enumeration:</span></span>

[!code-csharp[IteratorMethodRefactored](../../../samples/snippets/csharp/new-in-7/Iterator.cs#27_IteratorMethodRefactored "Iterator method refactored")]

<span data-ttu-id="31ef3-309">Bu işlenmiş sürüm özel durum atar hemen genel yöntem bir yineleyici yöntemi; olmadığından yalnızca bir özel yöntem `yield return` sözdizimi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-309">This refactored version will throw exceptions immediately because the public method is not an iterator method; only the private method uses the `yield return` syntax.</span></span> <span data-ttu-id="31ef3-310">Ancak, bu yeniden düzenleme olası sorunları vardır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-310">However, there are potential problems with this refactoring.</span></span> <span data-ttu-id="31ef3-311">Aksi takdirde tüm bağımsız değişkeni doğrulama atlandığından ortak arabirim yöntemi, yalnızca özel yöntemi çağrılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-311">The private method should only be called from the public interface method, because otherwise all argument validation is skipped.</span></span>
<span data-ttu-id="31ef3-312">Sınıfın okuyucuları gerekir Bul olgunun tüm sınıf okuma ve başka bir başvuru için arama yaparak `alphabetSubsetImplementation` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-312">Readers of the class must discover this fact by reading the entire class and searching for any other references to the `alphabetSubsetImplementation` method.</span></span>

<span data-ttu-id="31ef3-313">Bu tasarım hedefi daha temizleyin bildirerek yapabileceğiniz `alphabetSubsetImplementation` ortak API yöntemi içinde yerel bir işlev olarak:</span><span class="sxs-lookup"><span data-stu-id="31ef3-313">You can make that design intent more clear by declaring the `alphabetSubsetImplementation` as a local function inside the public API method:</span></span>

[!code-csharp[22_IteratorMethodLocal](../../../samples/snippets/csharp/new-in-7/Iterator.cs#28_IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="31ef3-314">Yukarıdaki sürüm temizleyin yerel yöntemi başvurulan yalnızca dış yöntem bağlamında kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-314">The version above makes it clear that the local method is referenced only in the context of the outer method.</span></span> <span data-ttu-id="31ef3-315">Yerel işlevler için kuralları ayrıca Geliştirici olamaz yanlışlıkla sınıfında başka bir konumdan yerel işlevini çağırın ve bağımsız değişkeni Doğrulamayı atla emin olun.</span><span class="sxs-lookup"><span data-stu-id="31ef3-315">The rules for local functions also ensure that a developer can't accidentally call the local function from another location in the class and bypass the argument validation.</span></span>

<span data-ttu-id="31ef3-316">İle aynı tekniği işe `async` yöntemleri zaman uyumsuz iş başlamadan önce bağımsız değişkeni doğrulama dışında doğan özel durumlar emin olun:</span><span class="sxs-lookup"><span data-stu-id="31ef3-316">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#29_TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="31ef3-317">Yerel işlevler tarafından desteklenen tasarımları bazıları da kullanarak bunu sağlayabilirsiniz *lambda ifadeleri*.</span><span class="sxs-lookup"><span data-stu-id="31ef3-317">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="31ef3-318">Bu ilgi yapabilirsiniz [farklar hakkında daha fazlasını okuyun](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="31ef3-318">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="31ef3-319">Daha fazla ifade bodied üyeleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-319">More expression-bodied members</span></span>

<span data-ttu-id="31ef3-320">C# 6 sunulan [ifade bodied üyeleri](csharp-6.md#expression-bodied-function-members) üye işlevleri ve salt okunur özellikler.</span><span class="sxs-lookup"><span data-stu-id="31ef3-320">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="31ef3-321">C# 7 ifadeleri olarak uygulanabilir izin verilen üyeleri genişletir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-321">C# 7 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="31ef3-322">C# 7'de, uygulayabileceğiniz *oluşturucular*, *sonlandırıcılar*, ve `get` ve `set` erişimciler üzerinde *özellikleri* ve *dizin oluşturucular* .</span><span class="sxs-lookup"><span data-stu-id="31ef3-322">In C# 7, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="31ef3-323">Aşağıdaki kod örnekleri her gösterir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-323">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](../../../samples/snippets/csharp/new-in-7/expressionmembers.cs#36_ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="31ef3-324">Bu örnek bir sonlandırıcı gerekmez, ancak sözdizimini göstermek için gösterilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-324">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="31ef3-325">Yönetilmeyen kaynakları serbest bırakmak gerekli olmadıkça bir sonlandırıcı sınıfınızda uygulamalıdır değil.</span><span class="sxs-lookup"><span data-stu-id="31ef3-325">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="31ef3-326">Kullanmayı düşünmelisiniz <xref:System.Runtime.InteropServices.SafeHandle> yönetilmeyen kaynakları doğrudan yönetmek yerine sınıfı.</span><span class="sxs-lookup"><span data-stu-id="31ef3-326">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="31ef3-327">Bu yeni konumlar ifade bodied üyeleri için C# dili için önemli bir kilometre taşını temsil eder: Bu özellikler üzerinde açık kaynak çalışma topluluk üyeleri tarafından uygulanan [Roslyn](https://github.com/dotnet/Roslyn) projesi.</span><span class="sxs-lookup"><span data-stu-id="31ef3-327">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="31ef3-328">Throw deyimleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-328">Throw expressions</span></span>

<span data-ttu-id="31ef3-329">C# ' ta, `throw` her zaman bir deyim olmuştur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-329">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="31ef3-330">Çünkü `throw` bir, olmayan bir ifade, var olan C# yapılarını burada kullandığınızda, bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-330">Because `throw` is a statement, not an expression, there were C# constructs where you could not use it.</span></span> <span data-ttu-id="31ef3-331">Bunlar, koşullu ifadeleri, null birleştirmesi ifadeleri ve bazı lambda ifadeleri dahil.</span><span class="sxs-lookup"><span data-stu-id="31ef3-331">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="31ef3-332">Daha fazla konumları ifade bodied üyelerinin eklenmesini ekler nerede `throw` ifadeleri yararlı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-332">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="31ef3-333">Bu yapıları hiçbirini yazma böylece C# 7 tanıtır *ifadeleri throw*.</span><span class="sxs-lookup"><span data-stu-id="31ef3-333">So that you can write any of these constructs, C# 7 introduces *throw expressions*.</span></span>

<span data-ttu-id="31ef3-334">Her zaman için kullandığınız sözdizimi aynıdır `throw` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="31ef3-334">The syntax is the same as you've always used for `throw` statements.</span></span> <span data-ttu-id="31ef3-335">Tek fark, bunları yeni konumlarda gibi bir koşullu ifade yerleştirebilirsiniz şimdi Bunun anlamı:</span><span class="sxs-lookup"><span data-stu-id="31ef3-335">The only difference is that now you can place them in new locations, such as in a conditional expression:</span></span>

[!code-csharp[Throw_ExpressionExample](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#37_Throw_ExpressionExample "conditional throw expressions")]

<span data-ttu-id="31ef3-336">Throw deyimleri başlatma ifadelerinde kullanarak etkinleştirir özellikleri:</span><span class="sxs-lookup"><span data-stu-id="31ef3-336">This features enables using throw expressions in initialization expressions:</span></span>

[!code-csharp[ThrowInInitialization](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#38_ThrowInInitialization "conditional throw expressions")]

<span data-ttu-id="31ef3-337">Daha önce bu başlatmaları Oluşturucusu gövdesinde throw deyimleri ile bir oluşturucuda olması gerekir:</span><span class="sxs-lookup"><span data-stu-id="31ef3-337">Previously, those initializations would need to be in a constructor, with the throw statements in the body of the constructor:</span></span>


[!code-csharp[ThrowInConstructor](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#39_ThrowInConstructor "throw statements")]

> [!NOTE]
> <span data-ttu-id="31ef3-338">Önceki yapıları her ikisinin bir nesne oluşturma sırasında durum için özel durumlar neden olur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-338">Both of the preceding constructs will cause exceptions to be thrown during the construction of an object.</span></span> <span data-ttu-id="31ef3-339">Bu genellikle kurtarılır zordur.</span><span class="sxs-lookup"><span data-stu-id="31ef3-339">Those are often difficult to recover from.</span></span>
> <span data-ttu-id="31ef3-340">Bu nedenle, oluşturma sırasında özel durumlar oluşturma tasarımları önerilmez.</span><span class="sxs-lookup"><span data-stu-id="31ef3-340">For that reason, designs that throw exceptions during construction are discouraged.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="31ef3-341">Genelleştirilmiş zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-341">Generalized async return types</span></span>

<span data-ttu-id="31ef3-342">Döndüren bir `Task` zaman uyumsuz yöntemleri bir nesneden performans sorunları belirli yollarda tanıtır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-342">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="31ef3-343">`Task`bir başvuru türü olduğundan, kullanmadan bir nesne ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-343">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="31ef3-344">Burada bir yöntem bildirilen ile durumlarda `async` değiştiricisi önbelleğe alınmış bir sonuç döndürür veya zaman uyumlu olarak tamamlar, fazladan ayırmaları önemli zaman Maliyet performans kritik kod bölümlerini hale gelebilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-344">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="31ef3-345">Bu ayırmaları sıkı Döngülerde oluşursa çok yüksek maliyetli hale gelebilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-345">It can become very costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="31ef3-346">Yeni dil zaman uyumsuz yöntemleri ek olarak diğer türleri döndürebilir özelliğin `Task`, `Task<T>` ve `void`.</span><span class="sxs-lookup"><span data-stu-id="31ef3-346">The new language feature means that async methods may return other types in addition to `Task`, `Task<T>` and `void`.</span></span> <span data-ttu-id="31ef3-347">Döndürülen türü zaman uyumsuz desen karşılaması gerekir yani bir `GetAwaiter` yöntemi erişilebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-347">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="31ef3-348">Somut bir örnek olarak `ValueTask` türü yapmak için .NET framework eklendi bu yeni dil özelliğini kullanın:</span><span class="sxs-lookup"><span data-stu-id="31ef3-348">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span> 

[!code-csharp[UsingValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#30_UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="31ef3-349">NuGet paketini eklemeniz [ `System.Threading.Tasks.Extensions` ](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) kullanmak için <xref:System.Threading.Tasks.ValueTask%601> türü.</span><span class="sxs-lookup"><span data-stu-id="31ef3-349">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="31ef3-350">Basit bir iyileştirme kullanmak olabilir `ValueTask` yerlerde nerede `Task` önce kullanılacaktır.</span><span class="sxs-lookup"><span data-stu-id="31ef3-350">A simple optimization would be to use `ValueTask` in places where `Task` would be used before.</span></span> <span data-ttu-id="31ef3-351">Ancak, ek iyileştirmeler el ile gerçekleştirmek istiyorsanız, zaman uyumsuz iş sonuçlarından önbelleğe ve sonraki çağrılar sonuç yeniden kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-351">However, if you want to perform extra optimizations by hand, you can cache results from async work and reuse the result in subsequent calls.</span></span> <span data-ttu-id="31ef3-352">`ValueTask` Yapısı bir oluşturucu sahip bir `Task` parametresi, oluşturabileceğiniz böylece bir `ValueTask` varolan herhangi bir zaman uyumsuz yöntemi dönüş değerinin:</span><span class="sxs-lookup"><span data-stu-id="31ef3-352">The `ValueTask` struct has a constructor with a `Task` parameter so that you can construct a `ValueTask` from the return value of any existing async method:</span></span>

[!code-csharp[AsyncOptimizedValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#31_AsyncOptimizedValueTask "Return async result or cached value")]
 
<span data-ttu-id="31ef3-353">Tüm performans önerileri, her iki sürümünü de büyük yapmadan önce Kıyaslama şekilde ölçek kodunuzu değiştirir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-353">As with all performance recommendations, you should benchmark both versions before making large scale changes to your code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="31ef3-354">Değişmez sayısal sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="31ef3-354">Numeric literal syntax improvements</span></span>

<span data-ttu-id="31ef3-355">Sayısal sabitleri misreading ilk kez okunurken kod anlaşılması zor yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-355">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="31ef3-356">Bu sayı bit maskesi veya diğer sembolik olarak kullanıldığında, bu genellikle oluşur. sayısal değerler yerine.</span><span class="sxs-lookup"><span data-stu-id="31ef3-356">This often occurs when those numbers are used as bit masks or other symbolic rather than numeric values.</span></span> <span data-ttu-id="31ef3-357">C# 7 kullanım için en okunabilir şekilde numaraları yazmayı kolaylaştırmak için iki yeni özellikler içerir: *ikili değişmez değerleri*, ve *basamak ayırıcıları*.</span><span class="sxs-lookup"><span data-stu-id="31ef3-357">C# 7 includes two new features to make it easier to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="31ef3-358">Bu kez için bu sayıyı ikili yazma en okunabilir kod bit maskesi oluştururken veya bir sayı ikili gösterimidir her yapar:</span><span class="sxs-lookup"><span data-stu-id="31ef3-358">For those times when you are creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[BinaryConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#32_BinaryConstants "Binary constants")]

<span data-ttu-id="31ef3-359">`0b` Sayı ikili bir sayı olarak yazılır sabiti başında gösterir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-359">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span>

<span data-ttu-id="31ef3-360">Genellikle sunarak bit desenleri görmek daha kolay olacak şekilde ikili sayılar çok uzun alabilirsiniz `_` basamak ayırıcı olarak:</span><span class="sxs-lookup"><span data-stu-id="31ef3-360">Binary numbers can get very long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator:</span></span>

[!code-csharp[ThousandSeparators](../../../samples/snippets/csharp/new-in-7/Program.cs#33_ThousandSeparators "Thousands separators")]

<span data-ttu-id="31ef3-361">Basamak ayırıcı sabitinde herhangi bir yerde görünebilir.</span><span class="sxs-lookup"><span data-stu-id="31ef3-361">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="31ef3-362">Temel 10 numaraları için binlerce kullanmak için ortak olacaktır ayırıcı:</span><span class="sxs-lookup"><span data-stu-id="31ef3-362">For base 10 numbers, it would be common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](../../../samples/snippets/csharp/new-in-7/Program.cs#34_LargeIntegers "Large integer")]

<span data-ttu-id="31ef3-363">Basamak ayırıcı kullanılabilir `decimal`, `float` ve `double` de türleri:</span><span class="sxs-lookup"><span data-stu-id="31ef3-363">The digit separator can be used with `decimal`, `float` and `double` types as well:</span></span>

[!code-csharp[OtherConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#35_OtherConstants "non-integral constants")]

<span data-ttu-id="31ef3-364">Birlikte ele alındığında sayısal sabit ile çok daha fazla okunabilirliği bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="31ef3-364">Taken together, you can declare numeric constants with much more readability.</span></span>
