---
title: C# 7,0 ' deki yenilikler-C# Kılavuzu
description: C# dilinin sürüm 7,0 ' deki yeni özelliklere genel bakış alın.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: e78d680e19709bf3dd854531d5d9f6b7d6464f49
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84392255"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="b0e73-103">C# 7.0 Yenilikleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-103">What's new in C# 7.0</span></span>

<span data-ttu-id="b0e73-104">C# 7,0, C# diline bir dizi yeni özellik ekler:</span><span class="sxs-lookup"><span data-stu-id="b0e73-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="b0e73-105">`out`değişkenlerinin</span><span class="sxs-lookup"><span data-stu-id="b0e73-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="b0e73-106">`out`Değerleri, kullanıldığı yönteme bağımsız değişken olarak satır içi olarak bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="b0e73-107">Demetler</span><span class="sxs-lookup"><span data-stu-id="b0e73-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="b0e73-108">Birden çok ortak alan içeren hafif, adlandırılmamış türler oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="b0e73-109">Derleyiciler ve IDE araçları bu türlerin semantiğini anlayın.</span><span class="sxs-lookup"><span data-stu-id="b0e73-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="b0e73-110">Atılanlar</span><span class="sxs-lookup"><span data-stu-id="b0e73-110">Discards</span></span>](#discards)
  - <span data-ttu-id="b0e73-111">Atama, atanan değer hakkında endişelenmezseniz atamalar içinde kullanılan geçici, salt yazılır değişkenlerdir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="b0e73-112">Bunlar, tanımlama grupları ve Kullanıcı tanımlı türler oluştururken ve parametreleri ile Yöntemler çağrılırken faydalıdır `out` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="b0e73-113">Desen Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="b0e73-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="b0e73-114">Bu türlerin üyelerinin rastgele türlerini ve değerlerini temel alarak dallanma mantığı oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="b0e73-115">`ref`Yereller ve döndürür</span><span class="sxs-lookup"><span data-stu-id="b0e73-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="b0e73-116">Yöntem yerel değişkenleri ve dönüş değerleri diğer depolamaya başvuru olabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="b0e73-117">Yerel Işlevler</span><span class="sxs-lookup"><span data-stu-id="b0e73-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="b0e73-118">Kendi kapsamını ve görünürlüğünü sınırlamak için işlevleri diğer işlevlerde iç içe geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="b0e73-119">Daha fazla ifade-Bodied Üyeler</span><span class="sxs-lookup"><span data-stu-id="b0e73-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="b0e73-120">İfadeler kullanılarak yazılabilir üyelerin listesi artmıştır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="b0e73-121">`throw`İfadelerde</span><span class="sxs-lookup"><span data-stu-id="b0e73-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="b0e73-122">Daha önce izin verilmeyen kod yapılarında özel durumlar oluşturabilir, çünkü `throw` bir deyimidir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="b0e73-123">Genelleştirilmiş zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="b0e73-124">Değiştiriciyle belirtilen Yöntemler, `async` ve ' a ek olarak başka türler `Task` döndürebilir `Task<T>` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="b0e73-125">Sayısal sabit değer sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="b0e73-126">Yeni belirteçler Sayısal sabitler için okunabilirliği geliştirir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="b0e73-127">Bu makalenin geri kalanında her özelliğe bir genel bakış sunulmaktadır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="b0e73-128">Her bir özellik için, arkasında yatan bir düşünme olduğunu öğrenirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="b0e73-129">Söz dizimini öğrenirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-129">You'll learn the syntax.</span></span> <span data-ttu-id="b0e73-130">Genel aracı kullanarak ortamınızdaki bu özellikleri keşfedebilirsiniz `dotnet try` :</span><span class="sxs-lookup"><span data-stu-id="b0e73-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="b0e73-131">[DotNet-TRY](https://github.com/dotnet/try/blob/master/README.md#setup) küresel aracını yükler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="b0e73-132">[DotNet/TRY-Samples](https://github.com/dotnet/try-samples) deposunu kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="b0e73-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="b0e73-133">*TRY-Samples* deposu için geçerli dizini *csharp7* alt dizinine ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="b0e73-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="b0e73-134">`dotnet try` öğesini çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="b0e73-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="b0e73-135">`out`değişkenlerinin</span><span class="sxs-lookup"><span data-stu-id="b0e73-135">`out` variables</span></span>

<span data-ttu-id="b0e73-136">`out`Bu sürümde, parametreleri destekleyen mevcut sözdizimi geliştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="b0e73-137">Artık `out` değişkenleri, ayrı bir bildirim bildirimi yazmak yerine bir yöntem çağrısının bağımsız değişken listesinde bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="b0e73-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="b0e73-138">`out`Yukarıda gösterildiği gibi, açıklık için değişkenin türünü belirtmek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="b0e73-139">Ancak, dil örtük olarak yazılmış bir yerel değişken kullanmayı destekler:</span><span class="sxs-lookup"><span data-stu-id="b0e73-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="b0e73-140">Kodu daha kolay okunabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-140">The code is easier to read.</span></span>
  - <span data-ttu-id="b0e73-141">Yukarıdaki diğer bir satırda değil, kullandığınız yerde çıkış değişkenini bildirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="b0e73-142">İlk değer atamaya gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="b0e73-143">`out`Yöntemi bir yöntem çağrısında kullanıldığı yerde bildirerek, yanlışlıkla onu atanmadan kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="b0e73-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="b0e73-144">Demetler</span><span class="sxs-lookup"><span data-stu-id="b0e73-144">Tuples</span></span>

<span data-ttu-id="b0e73-145">C#, tasarım amacını açıklamak için kullanılan sınıflar ve yapılar için zengin bir sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="b0e73-146">Ancak bazen zengin söz dizimi çok az avantajlı ek iş gerektirir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="b0e73-147">Genellikle birden fazla veri öğesi içeren basit bir yapıya ihtiyacı olan Yöntemler yazabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="b0e73-148">Bu *senaryoları desteklemek* için C# ' ye eklenmiştir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="b0e73-149">Tanımlama grupları, veri üyelerini temsil etmek için birden çok alan içeren hafif veri yapılarıdır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="b0e73-150">Alanlar doğrulanmaz ve kendi yöntemlerinizi tanımlayamazsınız</span><span class="sxs-lookup"><span data-stu-id="b0e73-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="b0e73-151">Tanımlama grupları C# 7,0 ' den önce kullanılabilir, ancak verimsiz ve dil desteği yoktu.</span><span class="sxs-lookup"><span data-stu-id="b0e73-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="b0e73-152">Bu, demet öğelerine yalnızca olarak başvurulabilir `Item1` , `Item2` vb. anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="b0e73-153">C# 7,0, tanımlama grupları için dil desteğini tanıtır ve bu, yeni, daha verimli demet türleri kullanarak bir kayıt düzeni alanları için anlamsal adlar sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="b0e73-154">Her üyeye bir değer atayarak ve isteğe bağlı olarak tanımlama grubu üyelerinin her birine anlamsal adlar sağlayarak bir tanımlama grubu oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="b0e73-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="b0e73-155">`namedLetters`Kayıt düzeni ve olarak adlandırılan alanları içerir `Alpha` `Beta` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="b0e73-156">Bu adlar yalnızca derleme sırasında bulunur ve korunmaz, örneğin, çalışma zamanında yansıma kullanarak tanımlama grubu incelenirken.</span><span class="sxs-lookup"><span data-stu-id="b0e73-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at run time.</span></span>

<span data-ttu-id="b0e73-157">Tanımlama grubu atamasında, atamanın sağ tarafındaki alanların adlarını da belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="b0e73-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="b0e73-158">Bir yöntemden döndürülen bir tanımlama grubunun üyelerinin paketini kaldırmak istediğiniz zamanlar olabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="b0e73-159">Bu, kayıt grubundaki her bir değer için ayrı değişkenler bildirerek yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="b0e73-160">Bu paketten *çıkarılması* , kayıt düzeninin kaldırılması olarak adlandırılır:</span><span class="sxs-lookup"><span data-stu-id="b0e73-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="b0e73-161">Ayrıca, .NET 'teki herhangi bir tür için benzer bir deme sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="b0e73-162">Bir `Deconstruct` yöntemi sınıfının bir üyesi olarak yazarsınız.</span><span class="sxs-lookup"><span data-stu-id="b0e73-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="b0e73-163">Bu `Deconstruct` Yöntem `out` , ayıklamak istediğiniz her bir özellik için bir dizi bağımsız değişken sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="b0e73-164">`Point`Ve koordinatlarını çıkaran bir Deconstructor yöntemi sağlayan bu sınıfı göz önünde `X` bulundurun `Y` :</span><span class="sxs-lookup"><span data-stu-id="b0e73-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="b0e73-165">Bir tanımlama grubu 'na atayarak tek tek alanları ayıklayabilirsiniz `Point` :</span><span class="sxs-lookup"><span data-stu-id="b0e73-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="b0e73-166">Tanımlama grupları [makalesindeki](../tuples.md)tanımlama grupları hakkında ayrıntılı bilgi edinebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="b0e73-167">Atılanlar</span><span class="sxs-lookup"><span data-stu-id="b0e73-167">Discards</span></span>

<span data-ttu-id="b0e73-168">Genellikle, bir tanımlama grubu oluştururken veya parametreler ile bir yöntemi çağırırken `out` , değeri ilgilenmediğiniz ve kullanmayı planlamadığınız bir değişken tanımlamaya zorlanır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="b0e73-169">C#, bu senaryoyu işlemek için *atma* desteği ekler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="b0e73-170">Bir atma, adı `_` (alt çizgi karakteri) olan salt yazılır bir değişkendir; atmayı planladığınız tüm değerleri tek bir değişkene atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="b0e73-171">Bir atma atanmamış değişken gibidir; atama ifadesinden ayrı olarak, atma kodda kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="b0e73-172">Atma, aşağıdaki senaryolarda desteklenir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="b0e73-173">Tanımlama grupları veya Kullanıcı tanımlı türler kaldırılıyor.</span><span class="sxs-lookup"><span data-stu-id="b0e73-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="b0e73-174">[Out](../language-reference/keywords/out-parameter-modifier.md) parametreleri ile Yöntemler çağrılırken.</span><span class="sxs-lookup"><span data-stu-id="b0e73-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="b0e73-175">WITH ve [Switch](../language-reference/keywords/switch.md) deyimleriyle bir kalıp [is](../language-reference/keywords/is.md) eşleştirme işleminde.</span><span class="sxs-lookup"><span data-stu-id="b0e73-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="b0e73-176">Bir atamanın değerini bir atma olarak açıkça tanımlamak istediğinizde tek başına tanımlayıcı olarak.</span><span class="sxs-lookup"><span data-stu-id="b0e73-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="b0e73-177">Aşağıdaki örnek, `QueryCityDataForYears` iki farklı yıl için şehir için veri içeren 6 demet döndüren bir yöntemi tanımlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="b0e73-178">Örnekteki yöntem çağrısı yalnızca yöntemi tarafından döndürülen iki popülasyon değeriyle ilgilidir ve bu nedenle, kayıt düzeni oluşturulduğunda, kayıt düzeninde kalan değerleri atma olarak değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="b0e73-179">Daha fazla bilgi için bkz. [atma](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="b0e73-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="b0e73-180">Desen eşleştirme</span><span class="sxs-lookup"><span data-stu-id="b0e73-180">Pattern matching</span></span>

<span data-ttu-id="b0e73-181">*Model eşleştirme* , nesne türünden farklı özelliklerde Yöntem gönderimi uygulamanıza olanak sağlayan bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="b0e73-182">Büyük olasılıkla nesne türüne göre Yöntem gönderimi hakkında zaten bilgi sahibisiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="b0e73-183">Nesne odaklı programlamada, sanal ve geçersiz kılma yöntemleri, nesne türünü temel alarak yöntem gönderme uygulamak için dil sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="b0e73-184">Temel ve türetilmiş sınıflar farklı uygulamalar sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="b0e73-185">Desen eşleştirme ifadeleri, devralma hiyerarşisi aracılığıyla ilgili olmayan türler ve veri öğeleri için benzer dağıtım düzenlerini kolayca uygulayabileceğiniz şekilde bu kavramı genişletir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="b0e73-186">Model eşleştirme `is` , ifadeleri ve `switch` ifadeleri destekler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="b0e73-187">Her biri, nesnenin aranan düzene karşılayıp karşılamadığını tespit etmek için bir nesne ve özelliklerini inceleyerek sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="b0e73-188">`when`Modele ek kurallar belirtmek için anahtar sözcüğünü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="b0e73-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="b0e73-189">`is`Model ifadesi, bir nesneyi türü hakkında sorgulamak ve sonucu bir yönergede atamak için tanıdık [ `is` işleci](../language-reference/keywords/is.md#pattern-matching-with-is) genişletir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="b0e73-190">Aşağıdaki kod, bir değişkenin bir olduğunu denetler `int` ve varsa, geçerli Sum 'a ekler:</span><span class="sxs-lookup"><span data-stu-id="b0e73-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="b0e73-191">Önceki küçük örnek, ifadeye yönelik geliştirmeleri gösterir `is` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="b0e73-192">Aynı zamanda değer türlerine ve başvuru türlerine karşı test edebilirsiniz ve başarılı sonucu doğru türdeki yeni bir değişkene atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="b0e73-193">Anahtar eşleşme ifadesi, `switch` C# dilinin zaten bir parçası olan bildirime göre tanıdık bir sözdizimine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="b0e73-194">Güncelleştirilmiş switch ifadesinin çeşitli yeni yapıları vardır:</span><span class="sxs-lookup"><span data-stu-id="b0e73-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="b0e73-195">Bir ifadenin yöneten türü `switch` artık, `Enum` `string` Bu türlerden birine karşılık gelen tamsayı türleri, türleri veya null olabilen bir türle sınırlı değildir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="b0e73-196">Herhangi bir tür kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-196">Any type may be used.</span></span>
- <span data-ttu-id="b0e73-197">`switch`Her etikette ifadenin türünü test edebilirsiniz `case` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="b0e73-198">İfadesinde olduğu gibi `is` , bu türe yeni bir değişken atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="b0e73-199">`when`O değişkende daha fazla test koşullarına bir yan tümce ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="b0e73-200">`case`Etiketlerin sırası artık önemlidir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="b0e73-201">Eşleştirilecek ilk dal yürütülür; diğerleri atlanır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="b0e73-202">Aşağıdaki kod bu yeni özellikleri göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="b0e73-203">`case 0:`tanıdık sabit bir örüntü.</span><span class="sxs-lookup"><span data-stu-id="b0e73-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="b0e73-204">`case IEnumerable<int> childSequence:`bir tür deseninin.</span><span class="sxs-lookup"><span data-stu-id="b0e73-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="b0e73-205">`case int n when n > 0:`, ek bir koşula sahip bir tür düzendir `when` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="b0e73-206">`case null:`null desenli bir değer.</span><span class="sxs-lookup"><span data-stu-id="b0e73-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="b0e73-207">`default:`tanıdık varsayılan durumdur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="b0e73-208">[C# ' de model eşleştirme](../pattern-matching.md)ile model eşleştirme hakkında daha fazla bilgi edinebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="b0e73-209">Ref Yereller ve geri dönüşler</span><span class="sxs-lookup"><span data-stu-id="b0e73-209">Ref locals and returns</span></span>

<span data-ttu-id="b0e73-210">Bu özellik, tarafından kullanılan ve başka bir yerde tanımlanan değişkenlere başvuru döndüren algoritmaların yapılmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="b0e73-211">Bir örnek büyük matrislerle çalışıyor ve belirli özelliklere sahip tek bir konum buluyor.</span><span class="sxs-lookup"><span data-stu-id="b0e73-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="b0e73-212">Aşağıdaki yöntem, matriste bu depolama için bir **başvuru** döndürür:</span><span class="sxs-lookup"><span data-stu-id="b0e73-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="b0e73-213">`ref`Aşağıdaki kodda gösterildiği gibi, dönüş değerini bir olarak bildirebilir ve matriste bu değeri değiştirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="b0e73-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="b0e73-214">C# dili, yerelleri yanlış kullanmanızı ve şunu döndürdüğünü koruyan çeşitli kurallara sahiptir `ref` :</span><span class="sxs-lookup"><span data-stu-id="b0e73-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="b0e73-215">`ref`Anahtar sözcüğünü Yöntem imzasına ve `return` bir yöntemindeki tüm deyimlere eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="b0e73-216">Bu, yöntem boyunca başvuruya göre döndürülen yöntemi temizler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="b0e73-217">Bir `ref return` değer değişkenine veya bir `ref` değişkene atanabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="b0e73-218">Çağıran, dönüş değerinin kopyalanıp kopyalanmadığını denetler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="b0e73-219">`ref`Dönüş değerini atarken değiştiricinin atlanması, çağıranın depolama için bir başvuru değil, değer kopyasının istediğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="b0e73-220">Bir yerel değişkene standart bir yöntem dönüş değeri atayamazsınız `ref` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="b0e73-221">Şunun gibi deyimler izin vermez`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="b0e73-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="b0e73-222">`ref`Ömrü, yönteminin yürütülmesinden daha fazla genişlemeyen bir değişkene geri dönemez.</span><span class="sxs-lookup"><span data-stu-id="b0e73-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="b0e73-223">Bu, yerel bir değişkene veya benzer kapsama sahip bir değişkene bir başvuru döndüremeyeceğiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="b0e73-224">`ref`Yereller ve geri dönüş, zaman uyumsuz yöntemlerle kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="b0e73-225">Zaman uyumsuz yöntem döndürüldüğünde, derleyici başvurulan değişkenin son değerine ayarlandığını bilemez.</span><span class="sxs-lookup"><span data-stu-id="b0e73-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="b0e73-226">Ref Yereller ve ref işlevinin eklenmesi, değerleri kopyalamayı önleyerek veya birden çok kez başvuru işlemleri gerçekleştirerek daha etkili olan algoritmaların kullanılmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="b0e73-227">`ref`Dönüş değerine ekleme, [kaynak ile uyumlu bir değişikdir](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="b0e73-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="b0e73-228">Varolan kod derlenir, ancak ref dönüş değeri atandığında kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="b0e73-229">Çağıranlar, döndürmeyi `ref` bir başvuru olarak depolamak için dönüş değeri için depolamayı yerel bir değişkene güncelleştirmelidir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="b0e73-230">Daha fazla bilgi için bkz. [ref anahtar sözcüğü](../language-reference/keywords/ref.md) makalesi.</span><span class="sxs-lookup"><span data-stu-id="b0e73-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="b0e73-231">Yerel işlevler</span><span class="sxs-lookup"><span data-stu-id="b0e73-231">Local functions</span></span>

<span data-ttu-id="b0e73-232">Sınıfların pek çok tasarımı yalnızca bir konumdan çağrılan yöntemleri içerir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="b0e73-233">Bu ek özel yöntemler her bir yöntemi küçük ve odaklanmış olarak tutar.</span><span class="sxs-lookup"><span data-stu-id="b0e73-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="b0e73-234">*Yerel işlevler* , yöntemleri başka bir yöntem bağlamı içinde bildirmenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="b0e73-235">Yerel işlevler, sınıfının okuyucularının, yerel yöntemin yalnızca bildirildiği bağlamdan çağrıldığını görmesini kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="b0e73-236">Yerel işlevler için iki yaygın kullanım durumu vardır: genel Yineleyici yöntemleri ve genel zaman uyumsuz yöntemler.</span><span class="sxs-lookup"><span data-stu-id="b0e73-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="b0e73-237">Her iki yöntem türü, programcılar tarafından daha sonra oluşabilecek hataları raporlayan kodu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="b0e73-238">Yineleyici metotlarda, tüm özel durumlar yalnızca döndürülen sırayı belirten kod çağrılırken izlenir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="b0e73-239">Zaman uyumsuz metotlarda, tüm özel durumlar yalnızca döndürülen geri beklendiğinde gözlemlenir `Task` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="b0e73-240">Aşağıdaki örnek, yerel bir işlev kullanarak Yineleyici uygulamasından parametre doğrulamayı ayırmayı gösterir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="b0e73-241">`async`Bağımsız değişken doğrulamasından doğan özel durumların, zaman uyumsuz iş başlamadan önce oluşturulması için yöntemler ile aynı yöntem kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="b0e73-242">Yerel işlevler tarafından desteklenen tasarımlardan bazıları *lambda ifadeleri*kullanılarak da gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-242">Some of the designs that are supported by local functions can also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="b0e73-243">Daha fazla bilgi için bkz [. yerel işlevler ve lambda ifadeleri](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span><span class="sxs-lookup"><span data-stu-id="b0e73-243">For more information, see [Local functions vs. lambda expressions](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="b0e73-244">Daha fazla ifade-Bodied Üyeler</span><span class="sxs-lookup"><span data-stu-id="b0e73-244">More expression-bodied members</span></span>

<span data-ttu-id="b0e73-245">C# 6 ifadesi-üye işlevleri için [Bodied Üyeler](csharp-6.md#expression-bodied-function-members) ve salt okunurdur özellikleri eklendi.</span><span class="sxs-lookup"><span data-stu-id="b0e73-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="b0e73-246">C# 7,0, ifade olarak uygulanabilecek izin verilen üyeleri genişletir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="b0e73-247">C# 7,0 ' de, *constructors* *finalizers* `get` `set` *Özellikler* ve *Dizin oluşturucular*üzerinde oluşturucular, sonlandırıcılar ve erişimciler uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="b0e73-248">Aşağıdaki kod, her birinin örneklerini gösterir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="b0e73-249">Bu örnekte sonlandırıcısı gerekmez, ancak söz dizimini göstermek için gösterilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="b0e73-250">Yönetilmeyen kaynakları serbest bırakmak gerekmedikçe, sınıfınıza sonlandırıcıyı uygulamamalısınız.</span><span class="sxs-lookup"><span data-stu-id="b0e73-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="b0e73-251">Ayrıca, <xref:System.Runtime.InteropServices.SafeHandle> yönetilmeyen kaynakları doğrudan yönetmek yerine sınıfını kullanmayı göz önünde bulundurmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="b0e73-252">İfade için bu yeni konumlar C# dili için önemli bir kilometre taşını temsil eder: Bu özellikler, açık kaynaklı [Roslyn](https://github.com/dotnet/Roslyn) projesinde çalışan topluluk üyeleri tarafından uygulanmıştır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="b0e73-253">Bir yöntemi bir ifade ile değiştirmek, [ikili uyumlu bir değişiklik](version-update-considerations.md#binary-compatible-changes)olur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="b0e73-254">Throw ifadeleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-254">Throw expressions</span></span>

<span data-ttu-id="b0e73-255">C# ' de `throw` her zaman bir ifade olmuştur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="b0e73-256">Bir `throw` ifade değil, bir deyim olduğundan, bunu kullanamadığı Için C# yapıları vardı.</span><span class="sxs-lookup"><span data-stu-id="b0e73-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="b0e73-257">Bu dahil edilen Koşullu ifadeler, null birleştirme ifadeleri ve bazı lambda ifadeleri.</span><span class="sxs-lookup"><span data-stu-id="b0e73-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="b0e73-258">İfade bululmuş üyelerin eklenmesi, ifadelerin yararlı olacağı daha fazla konum ekler `throw` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="b0e73-259">Bu yapıların herhangi birini yazmak için C# 7,0, [*throw ifadelerini*](../language-reference/keywords/throw.md#the-throw-expression)tanıtır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="b0e73-260">Bu ek, daha fazla ifade tabanlı kod yazmayı kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="b0e73-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="b0e73-261">Hata denetimi için ek deyimlere ihtiyacınız yoktur.</span><span class="sxs-lookup"><span data-stu-id="b0e73-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="b0e73-262">Genelleştirilmiş zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-262">Generalized async return types</span></span>

<span data-ttu-id="b0e73-263">`Task`Zaman uyumsuz metotlardan bir nesne döndürmek, belirli yollarda performans sorunlarını ortaya çıkarabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="b0e73-264">`Task`bir başvuru türüdür, bu nedenle bu bir nesne ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="b0e73-265">Değiştirici ile belirtilen bir yöntemin `async` önbelleğe alınmış bir sonuç döndürdüğü veya zaman uyumlu olarak tamamladığı durumlarda, ek ayırmalar kodun performans açısından kritik bölümlerinde önemli bir zaman maliyeti olabilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="b0e73-266">Bu ayırmalar sıkı Döngülerde gerçekleşirse maliyetli hale gelebilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="b0e73-267">Yeni dil özelliği, zaman uyumsuz yöntem dönüş türlerinin `Task` , ve ile sınırlı olmadığı anlamına gelir `Task<T>` `void` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="b0e73-268">Döndürülen türün zaman uyumsuz düzene uygun olması gerekir, yani bir `GetAwaiter` yönteme erişilebilir olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="b0e73-269">Tek bir somut örnek olarak, `ValueTask` Bu yeni dil özelliğinin kullanılması için tür .net 'e eklenmiştir:</span><span class="sxs-lookup"><span data-stu-id="b0e73-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="b0e73-270">Türü kullanabilmek için NuGet paketini eklemeniz gerekir [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) <xref:System.Threading.Tasks.ValueTask%601> .</span><span class="sxs-lookup"><span data-stu-id="b0e73-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="b0e73-271">Bu geliştirme, en çok bir performans kritik kodunda ayırmayı önlemek için kitaplık yazarları için yararlıdır `Task` .</span><span class="sxs-lookup"><span data-stu-id="b0e73-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="b0e73-272">Sayısal sabit değer sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="b0e73-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="b0e73-273">Hatalı okuma sayısal sabitleri, ilk kez okurken kodu daha zor hale getirir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="b0e73-274">Bit maskeleri veya diğer sembolik değerler yanlış anlama eğilimindedir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="b0e73-275">C# 7,0, tasarlanan kullanım için en okunabilir şekilde sayı yazmak üzere iki yeni özellik içerir: *ikili sabit değerler*ve *basamak ayırıcıları*.</span><span class="sxs-lookup"><span data-stu-id="b0e73-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="b0e73-276">Bit maskeleri oluştururken veya bir sayının ikili gösteriminin en çok okunabilen kodu yaptığı durumlarda bu süreler için bu sayıyı binary olarak yazın:</span><span class="sxs-lookup"><span data-stu-id="b0e73-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="b0e73-277">`0b`Sabitin başlangıcında, sayının bir ikili sayı olarak yazıldığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="b0e73-278">İkili sayılar uzun sürebilir, bu nedenle, `_` ikili Sabitte yukarıda gösterildiği gibi, bir rakam ayırıcısı olarak girerek bit düzenlerini görmeyi daha kolay hale getirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="b0e73-279">Sayı ayırıcısı, sabit içinde herhangi bir yerde görünebilir.</span><span class="sxs-lookup"><span data-stu-id="b0e73-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="b0e73-280">10 tabanında numara için bu, binlerce ayırıcı olarak kullanılması yaygındır:</span><span class="sxs-lookup"><span data-stu-id="b0e73-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="b0e73-281">Rakam ayırıcısı,, ve türleri ile birlikte kullanılabilir `decimal` `float` `double` :</span><span class="sxs-lookup"><span data-stu-id="b0e73-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="b0e73-282">Birlikte çalışarak, sayısal sabitleri çok daha okunaklı bir şekilde bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="b0e73-282">Taken together, you can declare numeric constants with much more readability.</span></span>
