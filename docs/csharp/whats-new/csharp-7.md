---
title: C# 7.0 - C# Kılavuzu yenilikler nelerdir?
description: C# dilinin gelecek sürümünde 7 gelecek yeni özelliklere genel bakış edinin.
ms.date: 12/21/2016
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 08e9b9d1a991c6dd18477214dec60fba95afc6c9
ms.sourcegitcommit: b56d59ad42140d277f2acbd003b74d655fdbc9f1
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/19/2019
ms.locfileid: "54415734"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="1dfa1-103">C# 7.0 yenilikleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-103">What's new in C# 7.0</span></span>

<span data-ttu-id="1dfa1-104">C# 7.0 C# dili için yeni özellikler ekler:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-104">C# 7.0 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="1dfa1-105">`out` Değişkenleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-105">`out` variables</span></span>](#out-variables)
    - <span data-ttu-id="1dfa1-106">Bildirebilirsiniz `out` kullanıldıkları yöntemi için bağımsız değişkeni olarak satır içi değerleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-106">You can declare `out` values inline as arguments to the method where they are used.</span></span>
* [<span data-ttu-id="1dfa1-107">Demetler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-107">Tuples</span></span>](#tuples)
    - <span data-ttu-id="1dfa1-108">Birden çok ortak alanları içeren basit, adsız türleri oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="1dfa1-109">Bu tür semantikleri, derleyiciler ve Araçlar IDE anlayın.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-109">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="1dfa1-110">Atılanlar</span><span class="sxs-lookup"><span data-stu-id="1dfa1-110">Discards</span></span>](#discards)
    - <span data-ttu-id="1dfa1-111">İptali atanan değer hakkında umursamaz atamalarını kullanılan geçici, salt yazılır değişkenlerdir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="1dfa1-112">Diziler ve kullanıcı tanımlı türler Ayrıştırma sırasında yanı sıra ile yöntemleri çağrılırken özellikle yararlı oldukları `out` parametreleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-112">They are particularly useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="1dfa1-113">Desen Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="1dfa1-113">Pattern Matching</span></span>](#pattern-matching)
    - <span data-ttu-id="1dfa1-114">Rastgele türler ve bu türlerin üyelerinin değerlerini göre dallanma mantığı oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="1dfa1-115">`ref` yerel değerleri ve dönüşleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
    - <span data-ttu-id="1dfa1-116">Diğer depolama başvuruları yöntemi yerel değişkenleri ve dönüş değerleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-116">Method local variables and return values can be references to other storage.</span></span>
* [<span data-ttu-id="1dfa1-117">Yerel İşlevler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-117">Local Functions</span></span>](#local-functions)
    - <span data-ttu-id="1dfa1-118">İşlevler, kapsam ve görünürlük sınırlamak için diğer işlevler içinde iç içe yerleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="1dfa1-119">Daha fazla ifade gövdeli üyeler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
    - <span data-ttu-id="1dfa1-120">İfadeleri kullanarak yazılabilir üye listesini geldi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-120">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="1dfa1-121">`throw` İfadeleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-121">`throw` Expressions</span></span>](#throw-expressions)
    - <span data-ttu-id="1dfa1-122">Daha önce olduğundan kullanılamaz kod yapıları özel durumlar oluşturabilecek `throw` bir ifadesi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-122">You can throw exceptions in code constructs that previously were not allowed because `throw` was a statement.</span></span> 
* [<span data-ttu-id="1dfa1-123">Genelleştirilmiş bir zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-123">Generalized async return types</span></span>](#generalized-async-return-types)
    - <span data-ttu-id="1dfa1-124">Yöntemleri ile bildirilmiş `async` değiştiricisi, ek olarak diğer türleri döndürebilir `Task` ve `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="1dfa1-125">Sayısal sabit değer sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
    - <span data-ttu-id="1dfa1-126">Yeni belirteç sayısal sabitlere okunabilirliği geliştirir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="1dfa1-127">Bu konunun geri kalanı özelliklerin her biri açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-127">The remainder of this topic discusses each of the features.</span></span> <span data-ttu-id="1dfa1-128">Her bir özellik için ardındaki mantık öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="1dfa1-129">Söz dizimi öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-129">You'll learn the syntax.</span></span> <span data-ttu-id="1dfa1-130">Burada yeni özelliği kullanarak daha üretken bir geliştirici olarak yapacağınız bazı örnek senaryolar görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-130">You'll see some sample scenarios where using the new feature will make you more productive as a developer.</span></span> 

## <a name="out-variables"></a><span data-ttu-id="1dfa1-131">`out` Değişkenleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-131">`out` variables</span></span>

<span data-ttu-id="1dfa1-132">Destekleyen varolan sözdizimi `out` parametreleri bu sürümde geliştirildi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-132">The existing syntax that supports `out` parameters has been improved in this version.</span></span>  

<span data-ttu-id="1dfa1-133">Daha önce out değişkeni bildirimi başlatılmasını iki farklı deyimleri içinde ayırmak gerekir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-133">Previously, you would need to separate the declaration of the out variable and its initialization into two different statements:</span></span>

[!code-csharp[OutVariableOldStyle](../../../samples/snippets/csharp/new-in-7/program.cs#03_OutVariableOldStyle "classic out variable declaration")]

<span data-ttu-id="1dfa1-134">Artık bildirebilirsiniz `out` ayrı bildirim deyimindeki yazmak yerine bir yöntem çağrısının bağımsız değişken listesinde değişkenleri:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-134">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#01_OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="1dfa1-135">Türünü belirtmek isteyebilirsiniz `out` , anlaşılması için değişken yukarıda da gösterildiği gibi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-135">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="1dfa1-136">Ancak bir türü örtük olarak belirlenmiş yerel değişken kullanarak dili destekler:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-136">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#02_OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="1dfa1-137">Kodu daha kolay.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-137">The code is easier to read.</span></span> 
    - <span data-ttu-id="1dfa1-138">Out değişkeni da değil başka bir satırda yukarıdaki kullandığınız bildirdiğiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-138">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="1dfa1-139">Bir başlangıç değeri atamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-139">No need to assign an initial value.</span></span>
    - <span data-ttu-id="1dfa1-140">Bildirmek `out` bunu atanmadan önce değişken bir yöntem çağrısında kullanıldığı yanlışlıkla kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-140">By declaring the `out` variable where it is used in a method call, you can't accidentally use it before it is assigned.</span></span>

<span data-ttu-id="1dfa1-141">Bu özelliğin en yaygın kullanımı olacaktır `Try` deseni.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-141">The most common use for this feature will be the `Try` pattern.</span></span> <span data-ttu-id="1dfa1-142">Bu düzende, bir yöntemi döndürür bir `bool` başarısı veya başarısızlığı gösteren ve bir `out` sonucunu yöntem başarılı olursa sağlayan değişken.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-142">In this pattern, a method returns a `bool` indicating success or failure and an `out` variable that provides the result if the method succeeds.</span></span>

<span data-ttu-id="1dfa1-143">Kullanırken `out` değişken bildirimi bildirilmiş bir değişken "sızıntıları" if dış kapsamına deyimi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-143">When using the `out` variable declaration, the declared variable "leaks" into the outer scope of the if statement.</span></span> <span data-ttu-id="1dfa1-144">Bu değişkeni sonradan kullanmanıza olanak tanır:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-144">This allows you to use the variable afterwards:</span></span>

```csharp
if (!int.TryParse(input, out int result))
{    
    return null;
}

return result;
```

## <a name="tuples"></a><span data-ttu-id="1dfa1-145">Demetler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-145">Tuples</span></span>

> [!NOTE]
> <span data-ttu-id="1dfa1-146">Yeni diziler işlevleri <xref:System.ValueTuple> türleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-146">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="1dfa1-147">NuGet paketini eklemeniz gerekir [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) türlerini içermez platformlarda kullanmak için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-147">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="1dfa1-148">Bu çerçevede teslim türleri kullanan diğer dil özellikleri benzer.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-148">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="1dfa1-149">Örnek dahil `async` ve `await` güvenmek `INotifyCompletion` arabirimi ve bağlı LINQ `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-149">Example include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="1dfa1-150">Ancak, .NET, daha fazla platform bağımsız olma gibi teslim mekanizması değişiyor.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-150">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="1dfa1-151">.NET Framework her zaman aynı temposu dil derleyicisi olarak yükleyeceğiniz değil.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-151">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="1dfa1-152">Yeni dil özellikleri üzerinde yeni türler kullanan, dil başlayamıyorsunuz türlerine NuGet paketleri olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-152">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="1dfa1-153">Bu yeni türleri ve .NET standart API için eklenen framework'ün bir parçası olarak sunulan NuGet paketini gereksinim kaldırılacak.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-153">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="1dfa1-154">C# sınıfları ve, tasarım amacı açıklamak için kullanılan yapıları için zengin bir söz dizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-154">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="1dfa1-155">Ancak bazen zengin Bu sözdizimi en az avantajı ile ek iş gerektirir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-155">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="1dfa1-156">Birden fazla veri öğesini içeren basit bir yapıya gereken yöntemler genellikle yazabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-156">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="1dfa1-157">Bu senaryoları desteklemek için *diziler* C# için eklendi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-157">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="1dfa1-158">Diziler veri üyelerini temsil etmek için birden fazla alan içeren basit veri yapılarıdır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-158">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="1dfa1-159">Alanlar yok doğrulandı ve kendi yöntem tanımlanamaz</span><span class="sxs-lookup"><span data-stu-id="1dfa1-159">The fields are not validated, and you cannot define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="1dfa1-160">Diziler C# 7.0 önce kullanılabilir, ancak verimsiz ve dil desteği yok vardı.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-160">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="1dfa1-161">Bu dizi öğeleri yalnızca olarak başvurulan geliyordu `Item1`, `Item2` ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-161">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="1dfa1-162">C# 7.0 alanlarını yeni ve daha verimli bir tanımlama grubu türleri kullanarak bir demet için anlamsal adları sağlayan diziler için dil desteği sunar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-162">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="1dfa1-163">Bir değer her üyesine atayarak bir tanımlama grubu oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-163">You can create a tuple by assigning a value to each member:</span></span>

[!code-csharp[UnnamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#04_UnnamedTuple "Unnamed tuple")]

<span data-ttu-id="1dfa1-164">Atama üyeleri olan bir dizi oluşturur `Item1` ve `Item2`, aynı şekilde kullanabileceğiniz <xref:System.Tuple> anlam adları her tanımlama grubu üyelerinin sağlayan bir kayıt düzeni oluşturmak için sözdizimi değiştirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-164">That assignment creates a tuple whose members are `Item1` and `Item2`, which you can use in the same way as <xref:System.Tuple> You can change the syntax to create a tuple that provides semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#05_NamedTuple "Named tuple")]

<span data-ttu-id="1dfa1-165">`namedLetters` Tanımlama grubu olarak adlandırılan alanları içeren `Alpha` ve `Beta`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-165">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="1dfa1-166">Bu adları, yalnızca derleme zamanında mevcut ve örnek için çalışma zamanında yansıma kullanarak demet incelerken korunmaz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-166">Those names exist only at compile time and are not preserved for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="1dfa1-167">Bir tanımlama grubu atamasını alanların adlarını atamada sağ tarafta da belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-167">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#06_ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="1dfa1-168">Her iki sol ve sağ tarafında atama alanların adlarını belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-168">You can specify names for the fields on both the left and right-hand side of the assignment:</span></span>

[!code-csharp[NamedTupleConflict](../../../samples/snippets/csharp/new-in-7/program.cs#07_NamedTupleConflict "Named tuple conflict")]

<span data-ttu-id="1dfa1-169">Yukarıdaki satırı bir uyarı üretir `CS8123`, şeklinde ataması sağ tarafında adları `Alpha` ve `Beta` sol tarafındaki adlarıyla çakıştığından göz ardı edilir `First` ve `Second`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-169">The line above generates a warning, `CS8123`, telling you that the names on the right side of the assignment, `Alpha` and `Beta` are ignored because they conflict with the names on the left side, `First` and `Second`.</span></span>

<span data-ttu-id="1dfa1-170">Yukarıdaki örnekler diziler bildirmek için temel söz dizimini gösterir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-170">The examples above show the basic syntax to declare tuples.</span></span> <span data-ttu-id="1dfa1-171">Diziler için dönüş türleri olarak faydalı `private` ve `internal` yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-171">Tuples are most useful as return types for `private` and `internal` methods.</span></span> <span data-ttu-id="1dfa1-172">Diziler, birden çok ayrı değer döndürmek bu yöntemleri için basit bir söz dizimi sağlar: Yazma iş kaydettiğiniz bir `class` veya `struct` döndürülen tür tanımlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-172">Tuples provide a simple syntax for those methods to return multiple discrete values: You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> <span data-ttu-id="1dfa1-173">Yeni tür oluşturma için gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-173">There is no need for creating a new type.</span></span>

<span data-ttu-id="1dfa1-174">Daha verimli ve daha üretken bir tanımlama grubu oluşturuluyor.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-174">Creating a tuple is more efficient and more productive.</span></span>
<span data-ttu-id="1dfa1-175">Bu, birden fazla değer taşıyan bir veri yapısını tanımlamak için daha basit ve basit bir söz dizimi olur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-175">It is a simpler, lightweight syntax to define a data structure that carries more than one value.</span></span> <span data-ttu-id="1dfa1-176">Aşağıdaki örnek yöntemi, bir tamsayı dizisinde bulunan minimum ve maksimum değerleri döndürür:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-176">The example method below returns the minimum and maximum values found in a sequence of integers:</span></span>

[!code-csharp[TupleReturningMethod](../../../samples/snippets/csharp/new-in-7/program.cs#08_TupleReturningMethod "Tuple returning method")]

<span data-ttu-id="1dfa1-177">Bu şekilde demetlerin kullanılmasını sağlayan çeşitli avantajlar sunar:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-177">Using tuples in this way offers several advantages:</span></span>

* <span data-ttu-id="1dfa1-178">Yazma iş kaydettiğiniz bir `class` veya `struct` döndürülen tür tanımlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-178">You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> 
* <span data-ttu-id="1dfa1-179">Yeni tür oluşturma gerekmez.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-179">You do not need to create new type.</span></span>
* <span data-ttu-id="1dfa1-180">Dil geliştirmelerini çağırma gereksinimini kaldırır <xref:System.Tuple.Create``1(``0)> yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-180">The language enhancements removes the need to call the <xref:System.Tuple.Create``1(``0)> methods.</span></span>

<span data-ttu-id="1dfa1-181">Yöntem bildiriminin döndürülen kayıt düzeninin alanların adlarını sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-181">The declaration for the method provides the names for the fields of the tuple that is returned.</span></span> <span data-ttu-id="1dfa1-182">Yöntemini çağırdığınızda, bir tanımlama grubu olan alanlar dönüş değeridir `Max` ve `Min`:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-182">When you call the method, the return value is a tuple whose fields are `Max` and `Min`:</span></span>

[!code-csharp[CallingTupleMethod](../../../samples/snippets/csharp/new-in-7/program.cs#09_CallingTupleMethod "Calling a tuple returning method")]

<span data-ttu-id="1dfa1-183">Yöntemden döndürülen bir tanımlama grubu üyelerinin açmak istediğinizde zamanlar olabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-183">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="1dfa1-184">Dizideki değerlerin her biri için ayrı değişkenleri bildirerek bunu yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-184">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="1dfa1-185">Bu adlandırılır *ayrıştırma* tanımlama grubu:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-185">This is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](../../../samples/snippets/csharp/new-in-7/program.cs#10_CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="1dfa1-186">. NET'te herhangi bir tür için benzer bir ayrıştırma de sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-186">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="1dfa1-187">Yazarak yapıldığını bir `Deconstruct` yöntemi sınıfının bir üyesi olarak.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-187">This is done by writing a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="1dfa1-188">Olduğunu `Deconstruct` yöntem kümesi sağlar `out` ayıklamak istediğiniz bağımsız değişkenleri özelliklerin her biri için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-188">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="1dfa1-189">Bunu göz önünde bulundurun `Point` ayıklayan deconstructor yöntemi sağlar sınıfını `X` ve `Y` koordinatları:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-189">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](../../../samples/snippets/csharp/new-in-7/point.cs#11_PointWithDeconstruction "Point with deconstruction method")]
 
<span data-ttu-id="1dfa1-190">Tek tek alanları atayarak ayıklayabileceğiniz bir `Point` bir demet için:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-190">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](../../../samples/snippets/csharp/new-in-7/program.cs#12_DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="1dfa1-191">Tanımlanan adlarına göre bağlı değildir `Deconstruct` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-191">You are not bound by the names defined in the `Deconstruct` method.</span></span> <span data-ttu-id="1dfa1-192">Extract değişkenleri atama bir parçası olarak yeniden adlandırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-192">You can rename the extract variables as part of the assignment:</span></span>  

[!code-csharp[DeconstructNames](../../../samples/snippets/csharp/new-in-7/program.cs#13_DeconstructNames "Deconstruct with new names")]

<span data-ttu-id="1dfa1-193">Diziler hakkında daha da ayrıntılı bilgi edinebilirsiniz [diziler konu](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-193">You can learn more in depth about tuples in the [tuples topic](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="1dfa1-194">Atar</span><span class="sxs-lookup"><span data-stu-id="1dfa1-194">Discards</span></span>

<span data-ttu-id="1dfa1-195">Olduğunda, genellikle bir demet ayrıştırma veya bir yöntemi çağırmak `out` parametreleri hakkında umursamaz ve kullanmayı düşünmüyorsanız değeri bir değişken tanımlayın zorunda.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-195">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="1dfa1-196">C# için destek ekler *atar* bu senaryonun işlenmesi için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-196">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="1dfa1-197">Bir atma adı olan bir salt yazılır değişkendir `_` (alt çizgi karakteri); tüm tek bir değişkene atmak düşündüğünüz değerleri atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-197">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="1dfa1-198">Bir atma gibi atanmamış bir değişkenidir; atama ifadesi dışında atma kodda kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-198">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="1dfa1-199">Atar, aşağıdaki senaryolarda desteklenir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-199">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="1dfa1-200">Tanımlama grubu veya kullanıcı tanımlı türler ayrıştırma olduğunda.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-200">When deconstructing tuples or user-defined types.</span></span>

* <span data-ttu-id="1dfa1-201">Yöntemleri çağrılırken [kullanıma](../language-reference/keywords/out-parameter-modifier.md) parametreleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-201">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>

* <span data-ttu-id="1dfa1-202">İşlemle eşleşen desende [olduğu](../language-reference/keywords/is.md) ve [geçiş](../language-reference/keywords/switch.md) deyimleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-202">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>

* <span data-ttu-id="1dfa1-203">Açıkça istediğinizde bir tek başına tanımlayıcı olarak bir atma atamadan değerini belirleyin.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-203">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="1dfa1-204">Aşağıdaki örnekte tanımlayan bir `QueryCityDataForYears` farklı iki yıllık bir şehir için bir veri içeren bir 6 bölütlü döndüren yöntem.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-204">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains a data for a city for two different years.</span></span> <span data-ttu-id="1dfa1-205">Örnek yöntem çağrısında yöntem tarafından döndürülen iki popülasyon değerleri ile ilgilidir ve bu nedenle kalan değerler demet tanımlama grubu deconstructs olduğunda atar gibi davranır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-205">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](../../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="1dfa1-206">Daha fazla bilgi için [atar](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-206">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="1dfa1-207">Desen eşleştirme</span><span class="sxs-lookup"><span data-stu-id="1dfa1-207">Pattern matching</span></span>

<span data-ttu-id="1dfa1-208">*Desen eşleştirme* yöntem gönderimine dışındaki bir nesne türü özellikleri uygulamak izin veren bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-208">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="1dfa1-209">Zaten bir nesne türüne göre yöntemi gönderme hakkında bilgi sahibi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-209">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="1dfa1-210">Yöntemi, bir nesnenin türüne göre gönderme uygulamak için dili sözdizimi, nesne yönelimli, sanal programlama ve geçersiz kılma yöntemleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-210">In Object Oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="1dfa1-211">Temel ve türetilmiş sınıfları farklı uygulamalar sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-211">Base and Derived classes provide different implementations.</span></span> <span data-ttu-id="1dfa1-212">Desen eşleştirme ifadeleri bu kavramı genişletmeniz Devralma Hiyerarşisi ilgili olmayan türler ve veri öğeleri için benzer gönderme desenleri kolayca uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-212">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that are not related through an inheritance hierarchy.</span></span> 

<span data-ttu-id="1dfa1-213">Desen eşleştirme destekler `is` ifadeleri ve `switch` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-213">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="1dfa1-214">Her bir nesne ve o nesnenin Aranan deseni karşılayıp karşılamadığını belirlemek için özelliklerini inceleme sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-214">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="1dfa1-215">Kullandığınız `when` düzeninin ek kuralları belirtmek için anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-215">You use the `when` keyword to specify additional rules to the pattern.</span></span>

### <a name="is-expression"></a><span data-ttu-id="1dfa1-216">`is` İfade</span><span class="sxs-lookup"><span data-stu-id="1dfa1-216">`is` expression</span></span>

<span data-ttu-id="1dfa1-217">`is` Desen ifadesi genişletir bilinen [ `is` işleci](../language-reference/keywords/is.md#pattern-matching-with-is) bir nesne türünü ötesinde sorgulamak için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-217">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object beyond its type.</span></span>

<span data-ttu-id="1dfa1-218">Basit bir senaryoyla başlayalım.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-218">Let's start with a simple scenario.</span></span> <span data-ttu-id="1dfa1-219">Desen eşleştirme ifadeleri iş ile ilgisiz türleri kolay algoritmalar nasıl oluşturduğunu gösteren bu senaryo özellikleri ekleyeceğiz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-219">We'll add capabilities to this scenario that demonstrate how pattern matching expressions make algorithms that work with unrelated types easy.</span></span> <span data-ttu-id="1dfa1-220">Zar pay sayısı toplamını hesaplar bir yöntem ile başlayacağız:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-220">We'll start with a method that computes the sum of a number of die rolls:</span></span>

[!code-csharp[SumDieRolls](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#14_SumDieRolls "Sum die rolls")]

<span data-ttu-id="1dfa1-221">Bazı pay ile birden çok dice yapılan burada zar pay toplamını bulmak gerektiğini hızla fark edebilirsiniz (zar, plural dice'dir).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-221">You might quickly find that you need to find the sum of die rolls where some of the rolls are made with multiple dice (dice is the plural of die).</span></span> <span data-ttu-id="1dfa1-222">Giriş dizisinin bir parçası birden çok sonuç yerine tek bir sayı olabilir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-222">Part of the input sequence may be multiple results instead of a single number:</span></span>

[!code-csharp[SumDieRollsWithGroups](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#15_SumDieRollsWithGroups "Sum die rolls with groups")]

<span data-ttu-id="1dfa1-223">`is` Desen ifadesi bu senaryoda oldukça iyi çalışır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-223">The `is` pattern expression works quite well in this scenario.</span></span> <span data-ttu-id="1dfa1-224">Tür denetleme bir parçası olarak bir değişken başlatma yazın.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-224">As part of checking the type, you write a variable initialization.</span></span> <span data-ttu-id="1dfa1-225">Bu, doğrulanmış çalışma zamanı türü yeni bir değişken oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-225">This creates a new variable of the validated runtime type.</span></span>

<span data-ttu-id="1dfa1-226">Bu senaryolar genişletme tutmak gibi daha fazla yapı bulabilirsiniz `if` ve `else if` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-226">As you keep extending these scenarios, you may find that you build more `if` and `else if` statements.</span></span> <span data-ttu-id="1dfa1-227">Zahmetli hale geldikten sonra büyük olasılıkla geçmek istersiniz `switch` ifadeleri desen.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-227">Once that becomes unwieldy, you'll likely want to switch to `switch` pattern expressions.</span></span>

### <a name="switch-statement-updates"></a><span data-ttu-id="1dfa1-228">`switch` deyim</span><span class="sxs-lookup"><span data-stu-id="1dfa1-228">`switch` statement updates</span></span>

<span data-ttu-id="1dfa1-229">*Eşleşen ifade* sahip temel tanıdık bir söz dizimi `switch` ifadesi C# dilinin zaten parçası.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-229">The *match expression* has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="1dfa1-230">Şimdi yeni çalışmaları eklemeden önce bir eşleme ifadesi kullanmak için mevcut kodu çevir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-230">Let's translate the existing code to use a match expression before adding new cases:</span></span> 

[!code-csharp[SumUsingSwitch](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#16_SumUsingSwitch "Sum using switch")]

<span data-ttu-id="1dfa1-231">Eşleşme ifadeleri değerinden biraz farklı bir sözdizimi sahip `is` ifadeleri, burada bildirdiğiniz tür ve değişken başındaki `case` ifade.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-231">The match expressions have a slightly different syntax than the `is` expressions, where you declare the type and variable at the beginning of the `case` expression.</span></span>

<span data-ttu-id="1dfa1-232">Eşleşme ifadeleri sabitleri de destekler.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-232">The match expressions also support constants.</span></span> <span data-ttu-id="1dfa1-233">Bu basit durumlar hesaba katacak şekilde tarafından zamandan tasarruf edebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-233">This can save time by factoring out simple cases:</span></span>

[!code-csharp[SwitchWithConstants](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#17_SwitchWithConstants "Switch with constants")]

<span data-ttu-id="1dfa1-234">Yukarıdaki kod çalışmaları için ekler `0` bir özel durum olarak `int`, ve `null` müdahalesi olduğunda özel bir durum olarak.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-234">The code above adds cases for `0` as a special case of `int`, and `null` as a special case when there is no input.</span></span> <span data-ttu-id="1dfa1-235">Bu önemli bir yeni özellik anahtarı düzeni ifadelerde gösterir: sırasını `case` ifadeleri şimdi önemlidir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-235">This demonstrates one important new feature in switch pattern expressions: the order of the `case` expressions now matters.</span></span> <span data-ttu-id="1dfa1-236">`0` Çalışması genel önce görünmelidir `int` çalışması.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-236">The `0` case must appear before the general `int` case.</span></span> <span data-ttu-id="1dfa1-237">Aksi durumda, ilk eşleşme gerçekleştirilecek desenin olacak `int` değer olsa bile çalışması `0`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-237">Otherwise, the first pattern to match would be the `int` case, even when the value is `0`.</span></span> <span data-ttu-id="1dfa1-238">Bir sonraki çalışması zaten işlenmiş şekilde eşleşme ifadeleri yanlışlıkla sipariş, derleyici, bayrak ve bir hata oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-238">If you accidentally order match expressions such that a later case has already been handled, the compiler will flag that and generate an error.</span></span>

<span data-ttu-id="1dfa1-239">Boş bir giriş sırası için özel durum bu aynı davranışı sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-239">This same behavior enables the special case for an empty input sequence.</span></span>
<span data-ttu-id="1dfa1-240">Gördüğünüz gibi bu durum bir `IEnumerable` öğelere sahip öğesi genel önce görünmelidir `IEnumerable` çalışması.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-240">You can see that the case for an `IEnumerable` item that has elements must appear before the general `IEnumerable` case.</span></span>

<span data-ttu-id="1dfa1-241">Bu sürüm ayrıca eklemiştir bir `default` çalışması.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-241">This version has also added a `default` case.</span></span> <span data-ttu-id="1dfa1-242">`default` Çalışması her zaman değerlendirilir son, kaynak sırasını bağımsız olarak görünür.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-242">The `default` case is always evaluated last, regardless of the order it appears in the source.</span></span> <span data-ttu-id="1dfa1-243">Bu nedenle, kuralı eklemektir `default` büyük/küçük harf son.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-243">For that reason, convention is to put the `default` case last.</span></span>

<span data-ttu-id="1dfa1-244">Son olarak, bir son ekleyelim `case` zar yeni stili için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-244">Finally, let's add one last `case` for a new style of die.</span></span> <span data-ttu-id="1dfa1-245">Bazı oyunlar yüzdebirlik dice daha büyük sayılar aralıklarını temsil etmek için kullanın.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-245">Some games use percentile dice to represent larger ranges of numbers.</span></span> 

> [!NOTE]
> <span data-ttu-id="1dfa1-246">0 ile 99 arasında iki 10 taraflı yüzdebirlik dice tüm sayıları temsil edebilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-246">Two 10-sided percentile dice can represent every number from 0 through 99.</span></span> <span data-ttu-id="1dfa1-247">Bir özel etiketli yüz sahip `00`, `10`, `20`,... `90`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-247">One die has sides labelled `00`, `10`, `20`, ... `90`.</span></span> <span data-ttu-id="1dfa1-248">Diğer özel etiketli kenarlara sahip `0`, `1`, `2`,... `9`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-248">The other die has sides labeled `0`, `1`, `2`, ... `9`.</span></span> <span data-ttu-id="1dfa1-249">İki zar değerlerini birleştirmek ve her sayı 0 ile 99 arasında alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-249">Add the two die values together and you can get every number from 0 through 99.</span></span>

<span data-ttu-id="1dfa1-250">Bu tür bir zar koleksiyonunuza eklemek için önce yüzdebirlik dice temsil edecek bir tür tanımlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-250">To add this kind of die to your collection, first define a type to represent the percentile dice.</span></span> <span data-ttu-id="1dfa1-251">`TensDigit` Özellik değerlerini depolar `0`, `10`, `20`, en fazla `90`:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-251">The `TensDigit` property stores values `0`, `10`, `20`, up to `90`:</span></span>

[!code-csharp[18_PercentileDice](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#18_PercentileDice "Percentile Die type")]

<span data-ttu-id="1dfa1-252">Ardından, ekleme bir `case` eşleşen yeni türü ifadesi:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-252">Then, add a `case` match expression for the new type:</span></span>

[!code-csharp[SwitchWithNewTypes](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#19_SwitchWithNewTypes "Include Percentile Die type")]

<span data-ttu-id="1dfa1-253">Desen eşleştirme ifadelerinde yeni sözdizimi, bir nesnenin türü ya da kısa ve anlaşılır bir söz dizimi kullanılarak diğer özellikleri de dayalı dağıtma algoritmaları oluşturmak kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-253">The new syntax for pattern matching expressions makes it easier to create dispatch algorithms based on an object's type, or other properties, using a clear and concise syntax.</span></span> <span data-ttu-id="1dfa1-254">Desen eşleştirme ifadeleri, bu yapıları tarafından devralma ilişkisiz veri türlerinde etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-254">Pattern matching expressions enable these constructs on data types that are unrelated by inheritance.</span></span>

<span data-ttu-id="1dfa1-255">Adanmış konusunda desen hakkında daha fazla bilgi [desen eşleştirme C# dilinde](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-255">You can learn more about pattern matching in the topic dedicated to [pattern matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="1dfa1-256">Ref yerel değerleri ve dönüşleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-256">Ref locals and returns</span></span>

<span data-ttu-id="1dfa1-257">Bu özelliği kullanın ve başka bir yerde tanımlanan değişkenleri başvuruları dönüş algoritmalar sağlar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-257">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="1dfa1-258">Örneğin, büyük matrisleri ile çalışma ve belirli özelliklere sahip tek bir konum bulma.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-258">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="1dfa1-259">Bir yöntem tek bir konum için iki indeks matriste döndürür:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-259">One method would return the two indices for a single location in the matrix:</span></span>

[!code-csharp[FindReturningIndices](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#20_FindReturningIndices "Find returning indices")]

<span data-ttu-id="1dfa1-260">Bu kod ile birçok sorun vardır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-260">There are many issues with this code.</span></span> <span data-ttu-id="1dfa1-261">İlk olarak bir dizi döndüren bir ortak yöntemidir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-261">First of all, it's a public method that's returning a tuple.</span></span> <span data-ttu-id="1dfa1-262">Bu dili destekler, ancak kullanıcı tanımlı türler (sınıflar veya yapılar) yönelik ortak API'ler tercih edilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-262">The language supports this, but user defined types (either classes or structs) are preferred for public APIs.</span></span>

<span data-ttu-id="1dfa1-263">İkinci olarak, bu yöntem, dizinleri matris öğeye döndürmektir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-263">Second, this method is returning the indices to the item in the matrix.</span></span>
<span data-ttu-id="1dfa1-264">Bu başvuru matrisi ve bir öğeyi değiştirmek için bu dizinleri kullanan kod yazmak için çağıranlar doğurur:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-264">That leads callers to write code that uses those indices to dereference the matrix and modify a single element:</span></span>

[!code-csharp[UpdateItemFromIndices](../../../samples/snippets/csharp/new-in-7/program.cs#21_UpdateItemFromIndices "Update Item From Indices")]

<span data-ttu-id="1dfa1-265">Döndüren bir yöntem yerine yazmak istediğiniz bir *başvuru* değiştirmek istediğiniz matris öğesine.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-265">You'd rather write a method that returns a *reference* to the element of the matrix that you want to change.</span></span> <span data-ttu-id="1dfa1-266">Bunu yalnızca güvenli olmayan kod kullanarak ve bir işaretçi döndüren gerçekleştirirsiniz bir `int` önceki sürümlerinde.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-266">You could only accomplish this by using unsafe code and returning a pointer to an `int` in previous versions.</span></span>

<span data-ttu-id="1dfa1-267">Şimdi bir dizi değişikliği ref yerel özelliği göstermek ve dahili depolama başvuru döndüren bir yöntem oluşturma işlemini göstermek için yol.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-267">Let's walk through a series of changes to demonstrate the ref local feature and show how to create a method that returns a reference to internal storage.</span></span>
<span data-ttu-id="1dfa1-268">Bu doğrultuda, ref dönüş ve bunu kötüye yanlışlıkla koruyan bir ref yerel özelliği kurallarını öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-268">Along the way, you'll learn the rules of the ref return and ref local feature that protects you from accidentally misusing it.</span></span>

<span data-ttu-id="1dfa1-269">Başlangıç değiştirerek `Find` BT'nin döndürecek şekilde yöntem bildiriminde bir `ref int` yerine bir tanımlama grubu.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-269">Start by modifying the `Find` method declaration so that it returns a `ref int` instead of a tuple.</span></span> <span data-ttu-id="1dfa1-270">Ardından, iki indeks yerine Matristeki depolanan değer döndürecek şekilde return deyimi değiştirin:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-270">Then, modify the return statement so it returns the value stored in the matrix instead of the two indices:</span></span>

```csharp
// Note that this won't compile. 
// Method declaration indicates ref return,
// but return statement specifies a value return.
public static ref int Find2(int[,] matrix, Func<int, bool> predicate)
{
    for (int i = 0; i < matrix.GetLength(0); i++)
        for (int j = 0; j < matrix.GetLength(1); j++)
            if (predicate(matrix[i, j]))
                return matrix[i, j];
    throw new InvalidOperationException("Not found");
}
```

<span data-ttu-id="1dfa1-271">Bir yöntemi döndürür bildirdiğinizde bir `ref` değişkeni de eklemeniz gerekir `ref` her iade bildirimde anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-271">When you declare that a method returns a `ref` variable, you must also add the `ref` keyword to each return statement.</span></span> <span data-ttu-id="1dfa1-272">Başvuru ile dönüş gösterir ve daha sonra kodu okuyan yardımcı geliştiriciler yöntemi başvuru ile döndürülen unutmayın:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-272">That indicates return by reference, and helps developers reading the code later remember that the method returns by reference:</span></span>

[!code-csharp[FindReturningRef](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#22_FindReturningRef "Find returning by reference")]

<span data-ttu-id="1dfa1-273">Yöntem matriste tamsayı değerine bir başvuru döndürür, burada çağrılır değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-273">Now that the method returns a reference to the integer value in the matrix, you need to modify where it's called.</span></span>  <span data-ttu-id="1dfa1-274">`var` Bildirimi anlamına `valItem` artık bir `int` yerine bir demet:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-274">The `var` declaration means that `valItem` is now an `int` rather than a tuple:</span></span>

[!code-csharp[AssignRefReturnToValue](../../../samples/snippets/csharp/new-in-7/program.cs#23_AssignRefReturnToValue "Assign ref return to value")]

<span data-ttu-id="1dfa1-275">İkinci `WriteLine` yukarıdaki örnekte deyimi yazdırır değerini `42`değil `24`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-275">The second `WriteLine` statement in the example above prints out the value `42`, not `24`.</span></span> <span data-ttu-id="1dfa1-276">Değişken `valItem` olduğu bir `int`değil bir `ref int`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-276">The variable `valItem` is an `int`, not a `ref int`.</span></span> <span data-ttu-id="1dfa1-277">`var` Anahtar sözcüğü türü belirtmek derleyiciyi etkinleştirir, ancak değil örtük olarak ekleyecek `ref` değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-277">The `var` keyword enables the compiler to specify the type, but will not implicitly add the `ref` modifier.</span></span> <span data-ttu-id="1dfa1-278">Bunun yerine, değeri adlandırılan tarafından `ref return` olduğu *kopyalanan* atamanın sol tarafı değişkenine.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-278">Instead, the value referred to by the `ref return` is *copied* to the variable on the left-hand side of the assignment.</span></span> <span data-ttu-id="1dfa1-279">Değişken değil bir `ref` yerel.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-279">The variable is not a `ref` local.</span></span>

<span data-ttu-id="1dfa1-280">Eklemenize gerek istediğiniz sonucu elde etmek için `ref` değiştirici dönüş değeri bir başvuru değişkeni başvuru yapmak için yerel değişken bildirimi için:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-280">In order to get the result you want, you need to add the `ref` modifier to the local variable declaration to make the variable a reference when the return value is a reference:</span></span>

[!code-csharp[AssignRefReturn](../../../samples/snippets/csharp/new-in-7/program.cs#24_AssignRefReturn "Assign ref return")]

<span data-ttu-id="1dfa1-281">Şimdi, ikinci `WriteLine` yukarıdaki örnekte deyimi şeyler değerini yazdırır `24`, Matristeki depolama değiştirilmiş olduğunu gösteren.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-281">Now, the second `WriteLine` statement in the example above will print out the value `24`, indicating that the storage in the matrix has been modified.</span></span> <span data-ttu-id="1dfa1-282">Yerel değişken ile bildirilen `ref` değiştiricisi ve alacağınız bir `ref` döndürür.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-282">The local variable has been declared with the `ref` modifier, and it will take a `ref` return.</span></span> <span data-ttu-id="1dfa1-283">Başlatması gerekir bir `ref` değişkeni bildirildiğinde, bildirim ve başlatma bölünemiyor.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-283">You must initialize a `ref` variable when it is declared, you cannot split the declaration and the initialization.</span></span>

<span data-ttu-id="1dfa1-284">C# dili kötüye kullanmasının korumak üç kuralları sahip `ref` yerel değerleri ve Dönüşleri:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-284">The C# language has three other rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="1dfa1-285">Standart bir yöntem dönüş değerine atanamaz bir `ref` yerel değişken.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-285">You cannot assign a standard method return value to a `ref` local variable.</span></span>
    - <span data-ttu-id="1dfa1-286">Aşağıdaki gibi ifadeler izin vermiyor `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="1dfa1-286">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="1dfa1-287">Döndüremezsiniz bir `ref` ömürlerinin yönteminin yürütülmesi genişletmiyoruz bir değişkene.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-287">You cannot return a `ref` to a variable whose lifetime does not extend beyond the execution of the method.</span></span>
    - <span data-ttu-id="1dfa1-288">Yerel bir değişken veya benzer bir kapsama sahip bir değişken başvuru döndüremez anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-288">That means you cannot return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="1dfa1-289">`ref` yerel değerleri ve dönüşleri zaman uyumsuz yöntemlerle kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-289">`ref` locals and returns can't be used with async methods.</span></span>
    - <span data-ttu-id="1dfa1-290">Derleyici, zaman uyumsuz yöntem döndürüldüğünde başvurulan değişken son değerine ayarlandı olmadığını bilemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-290">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="1dfa1-291">Ref yerel ayarlar ve ref değerleri kopyalama ya da birden çok kez başvurusunu kaldırma işlemlerini gerçekleştirme önleyerek daha verimlidir etkinleştir algoritmaları döndürür.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-291">The addition of ref locals and ref returns enable algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="1dfa1-292">Ekleme `ref` için dönüş değeri bir [kaynağı uyumlu değişiklik](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-292">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="1dfa1-293">Var olan kod derlenir, ancak başvuru dönüş değeri atandığında kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-293">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="1dfa1-294">Çağıranlar, depolama alanı için dönüş değeri için güncelleştirilmesi gerekir bir `ref` dönüş başvuru olarak saklamak için yerel değişken.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-294">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="1dfa1-295">Daha fazla bilgi için [ref anahtar sözcüğü](../language-reference/keywords/ref.md) makalesi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-295">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="1dfa1-296">Yerel İşlevler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-296">Local functions</span></span>

<span data-ttu-id="1dfa1-297">Sınıflar için birçok tasarım tek bir konumdan Aranan yöntemleri kapsar.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-297">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="1dfa1-298">Bu ek özel yöntemleri her yöntem, küçük ve odaklı tutun.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-298">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="1dfa1-299">Ancak, bunlar, bir sınıf ilk kez okurken anlamak zorlaştırabilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-299">However, they can make it harder to understand a class when reading it the first time.</span></span> <span data-ttu-id="1dfa1-300">Tek arama konumu bağlamı dışında bu yöntemlerin anlaşılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-300">These methods must be understood outside of the context of the single calling location.</span></span>

<span data-ttu-id="1dfa1-301">Bu tasarım için *yerel işlevler* , başka bir yöntem bağlamı içinde yöntemleri bildirmek etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-301">For those designs, *local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="1dfa1-302">Bu bağlamda olduğu bildirilen yerel yöntemi yalnızca çağrılır, görmek için sınıf okuyucular için kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-302">This makes it easier for readers of the class to see that the local method is only called from the context in which is it declared.</span></span>

<span data-ttu-id="1dfa1-303">Yerel işlevler için iki yaygın kullanım örnekleri vardır: Genel yineleyici yöntemleri ve genel zaman uyumsuz yöntemler.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-303">There are two very common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="1dfa1-304">Her iki yöntem tür hataları programcılar beklenebilir daha sonra rapor kod oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-304">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="1dfa1-305">Yineleyici metotları olması durumunda, yalnızca özel durumların gözlemlenen döndürülen dizi numaralandırır kod çağırırken.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-305">In the case of iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="1dfa1-306">Söz konusu olduğunda zaman uyumsuz yöntemler, özel durumların yalnızca zaman uyulması gereken döndürülen `Task` beklenir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-306">In the case of async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span>

<span data-ttu-id="1dfa1-307">Bir yineleyici yöntem ile başlayalım:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-307">Let's start with an iterator method:</span></span>

[!code-csharp[IteratorMethod](../../../samples/snippets/csharp/new-in-7/Iterator.cs#25_IteratorMethod "Iterator method")]

<span data-ttu-id="1dfa1-308">Yineleyici yöntem yanlış çağıran kodu aşağıdaki inceleyin:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-308">Examine the code below that calls the iterator method incorrectly:</span></span>

[!code-csharp[CallIteratorMethod](../../../samples/snippets/csharp/new-in-7/program.cs#26_CallIteratorMethod "Call iterator method")]

<span data-ttu-id="1dfa1-309">Özel durum oluşan `resultSet` , ne zaman yinelendiğinde değil `resultSet` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-309">The exception is thrown when `resultSet` is iterated, not when `resultSet` is created.</span></span>
<span data-ttu-id="1dfa1-310">Çoğu geliştirici, kapsanan bu örnekte, hızlı bir şekilde bu sorunu tanılamak.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-310">In this contained example, most developers could quickly diagnose the problem.</span></span> <span data-ttu-id="1dfa1-311">Ancak, büyük kod tabanlarında, sonuç listesini numaralandıran kod olarak yakın bir yineleyici genellikle oluşturan kodu değil.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-311">However, in larger codebases, the code that creates an iterator often isn't as close to the code that enumerates the result.</span></span> <span data-ttu-id="1dfa1-312">Tüm bağımsız değişkenler genel yöntem doğrular ve özel bir yöntem sabit listesi oluşturur. böylece kodu yeniden düzenleyebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-312">You can refactor the code so that the public method validates all arguments, and a private method generates the enumeration:</span></span>

[!code-csharp[IteratorMethodRefactored](../../../samples/snippets/csharp/new-in-7/Iterator.cs#27_IteratorMethodRefactored "Iterator method refactored")]

<span data-ttu-id="1dfa1-313">İşlenmiş bu sürümü, özel durumları oluşturmaz hemen genel yöntem yineleyici yöntemini; olmadığı için yalnızca özel yöntemi kullanan `yield return` söz dizimi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-313">This refactored version will throw exceptions immediately because the public method is not an iterator method; only the private method uses the `yield return` syntax.</span></span> <span data-ttu-id="1dfa1-314">Ancak, bu yeniden düzenleme ile olası sorunları vardır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-314">However, there are potential problems with this refactoring.</span></span> <span data-ttu-id="1dfa1-315">Aksi takdirde tüm bağımsız değişken doğrulama atlandığından ortak arabirim yöntemi yalnızca özel yöntemi çağrılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-315">The private method should only be called from the public interface method, because otherwise all argument validation is skipped.</span></span>
<span data-ttu-id="1dfa1-316">Okuyucular sınıfının gerekir Bul bu olgu sınıfın tamamı okuyarak ve diğer tüm başvurular aranıyor `alphabetSubsetImplementation` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-316">Readers of the class must discover this fact by reading the entire class and searching for any other references to the `alphabetSubsetImplementation` method.</span></span>

<span data-ttu-id="1dfa1-317">Bildirerek bu tasarım hedefi daha temiz yapabilirsiniz `alphabetSubsetImplementation` Genel API yöntemi içinde yerel bir işlev olarak:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-317">You can make that design intent more clear by declaring the `alphabetSubsetImplementation` as a local function inside the public API method:</span></span>

[!code-csharp[22_IteratorMethodLocal](../../../samples/snippets/csharp/new-in-7/Iterator.cs#28_IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="1dfa1-318">Yukarıdaki sürüm Temizle yerel yöntemi başvurulan yalnızca dış yöntem bağlamında kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-318">The version above makes it clear that the local method is referenced only in the context of the outer method.</span></span> <span data-ttu-id="1dfa1-319">Yerel işlevleri için kurallar ayrıca bir geliştirici yanlışlıkla sınıfında başka bir konumdan yerel işlev çağrısı veya bağımsız değişken doğrulama atlama emin emin olun.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-319">The rules for local functions also ensure that a developer can't accidentally call the local function from another location in the class and bypass the argument validation.</span></span>

<span data-ttu-id="1dfa1-320">Aynı yöntem ile işe `async` zaman uyumsuz işler başlamadan önce bağımsız değişken doğrulama doğan özel durumlar emin olmak için yöntemleri:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-320">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#29_TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="1dfa1-321">Yerel işlevleri tarafından desteklenen tasarımları bazılarını da kullanarak sağlayabilirsiniz *lambda ifadeleri*.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-321">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="1dfa1-322">Bu ilgi olabilir [farklar hakkında daha fazla bilgi](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="1dfa1-322">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="1dfa1-323">Daha fazla ifade gövdeli üyeler</span><span class="sxs-lookup"><span data-stu-id="1dfa1-323">More expression-bodied members</span></span>

<span data-ttu-id="1dfa1-324">C# 6 sunulan [ifade gövdeli üyeler](csharp-6.md#expression-bodied-function-members) üye işlevleri ve salt okunur özellikler için.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-324">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="1dfa1-325">C# 7.0 ifadeleri olarak uygulanabilir izin verilen üyeleri genişletir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-325">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="1dfa1-326">C# 7. 0 ', uygulayabileceğiniz *oluşturucular*, *sonlandırıcılar*, ve `get` ve `set` bulunan erişimciler *özellikleri* ve *dizin oluşturucular* .</span><span class="sxs-lookup"><span data-stu-id="1dfa1-326">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="1dfa1-327">Aşağıdaki kod her örneklerini gösterir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-327">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](../../../samples/snippets/csharp/new-in-7/expressionmembers.cs#36_ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="1dfa1-328">Bu örnek bir sonlandırıcıya gerek yoktur, ancak sözdizimini göstermek için gösterilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-328">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="1dfa1-329">Yönetilmeyen kaynakları serbest bırakmak gerekli olmadıkça, sınıfta bir sonlandırıcı uygulamamalıdır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-329">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="1dfa1-330">Kullanmayı da düşünmelisiniz <xref:System.Runtime.InteropServices.SafeHandle> yönetilmeyen kaynakları doğrudan yönetmek yerine sınıfı.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-330">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="1dfa1-331">İfade gövdeli üyeler için bu yeni konumlar için önemli bir kilometre temsil C# dil: Bu özellikler, açık kaynaklı çalışma topluluk üyeleri tarafından uygulanan [Roslyn](https://github.com/dotnet/Roslyn) proje.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-331">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="1dfa1-332">Bir ifade bodied üyesine bir yöntemi değiştirme bir [ikili uyumlu değişiklik](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-332">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="1dfa1-333">Throw ifadeleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-333">Throw expressions</span></span>

<span data-ttu-id="1dfa1-334">C# ' ta, `throw` her zaman bir deyim olmuştur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-334">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="1dfa1-335">Çünkü `throw` olan C# yapılarını değil kullanabileceğiniz, bir ifade değil bir ifade, var olan.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-335">Because `throw` is a statement, not an expression, there were C# constructs where you could not use it.</span></span> <span data-ttu-id="1dfa1-336">Bunlar, koşullu ifadeleri ve null birleşim ifadelerini bazı lambda ifadeleri dahil.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-336">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="1dfa1-337">İfade gövdeli üyeler eklenmesi için daha fazla konum ekler burada `throw` ifadeleri yararlı olacaktır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-337">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="1dfa1-338">Bu yapıları hiçbirini yazabilmesi amacıyla da, C# 7.0 tanıtır *throw ifadelerini*.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-338">So that you can write any of these constructs, C# 7.0 introduces *throw expressions*.</span></span>

<span data-ttu-id="1dfa1-339">Her zaman için kullandığınız sözdizimi aynıdır `throw` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-339">The syntax is the same as you've always used for `throw` statements.</span></span> <span data-ttu-id="1dfa1-340">Bunları yeni konumlara gibi koşullu ifadede yerleştirebilirsiniz artık tek fark,.:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-340">The only difference is that now you can place them in new locations, such as in a conditional expression:</span></span>

[!code-csharp[Throw_ExpressionExample](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#37_Throw_ExpressionExample "conditional throw expressions")]

<span data-ttu-id="1dfa1-341">Bu, throw ifadelerini başlatma ifadeleri kullanarak etkinleştirir özellikleri:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-341">This features enables using throw expressions in initialization expressions:</span></span>

[!code-csharp[ThrowInInitialization](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#38_ThrowInInitialization "conditional throw expressions")]

<span data-ttu-id="1dfa1-342">Daha önce bu başlatmalar Oluşturucu gövdesinde throw deyimleri ile bir oluşturucu içinde olması gerekir:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-342">Previously, those initializations would need to be in a constructor, with the throw statements in the body of the constructor:</span></span>


[!code-csharp[ThrowInConstructor](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#39_ThrowInConstructor "throw statements")]

> [!NOTE]
> <span data-ttu-id="1dfa1-343">Önceki yapıları her ikisi de, özel bir nesnenin yapımı sırasında durum oluşturulmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-343">Both of the preceding constructs will cause exceptions to be thrown during the construction of an object.</span></span> <span data-ttu-id="1dfa1-344">Bu genellikle kurtarma yapmak zordur.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-344">Those are often difficult to recover from.</span></span>
> <span data-ttu-id="1dfa1-345">Bu nedenle, yapım sırasında özel durumlar oluşturan tasarımları önerilmez.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-345">For that reason, designs that throw exceptions during construction are discouraged.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="1dfa1-346">Genelleştirilmiş bir zaman uyumsuz dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-346">Generalized async return types</span></span>

<span data-ttu-id="1dfa1-347">Döndüren bir `Task` zaman uyumsuz yöntemler bir nesneden performans sorunlarını Belirli yollardaki tanıtmak.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-347">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="1dfa1-348">`Task` bir başvuru türü olduğundan, onu kullanarak bir nesne ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-348">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="1dfa1-349">Burada bir yöntem ile bildirilmiş durumlarda `async` değiştiricisi önbelleğe alınmış bir sonuç döndürür ya da zaman uyumlu olarak tamamlanan, kodun önemli bölümleri performans ciddi zaman maliyetine ek ayırmaları hale gelebilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-349">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="1dfa1-350">Bu ayırmalar sıkı döngüleri oluşursa çok pahalı hale gelebilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-350">It can become very costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="1dfa1-351">Yeni dil özelliği zaman uyumsuz yöntemler diğer tür ek olarak döndürebilir anlamına gelir. `Task`, `Task<T>` ve `void`.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-351">The new language feature means that async methods may return other types in addition to `Task`, `Task<T>` and `void`.</span></span> <span data-ttu-id="1dfa1-352">Döndürülen tür yine de zaman uyumsuz desen karşılaması gerekir yani bir `GetAwaiter` yöntemi erişilebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-352">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="1dfa1-353">Somut bir örnek olarak `ValueTask` yapmak için .NET framework türü eklendi bu yeni dil özelliğini kullanın:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-353">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span> 

[!code-csharp[UsingValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#30_UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="1dfa1-354">NuGet paketini eklemeniz [ `System.Threading.Tasks.Extensions` ](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) kullanmak için <xref:System.Threading.Tasks.ValueTask%601> türü.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-354">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="1dfa1-355">Basit bir iyileştirme kullanmak olabilir `ValueTask` yerde nerede `Task` önce kullanılması.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-355">A simple optimization would be to use `ValueTask` in places where `Task` would be used before.</span></span> <span data-ttu-id="1dfa1-356">Ancak, ek iyileştirmeler el ile gerçekleştirmek istiyorsanız, zaman uyumsuz iş sonuçlarını önbelleğe alma ve sonraki çağrılar sonuç yeniden.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-356">However, if you want to perform extra optimizations by hand, you can cache results from async work and reuse the result in subsequent calls.</span></span> <span data-ttu-id="1dfa1-357">`ValueTask` Yapı sahip bir oluşturucuyla bir `Task` parametresi, oluşturabilirsiniz böylece bir `ValueTask` herhangi bir mevcut zaman uyumsuz yöntem dönüş değerinin:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-357">The `ValueTask` struct has a constructor with a `Task` parameter so that you can construct a `ValueTask` from the return value of any existing async method:</span></span>

[!code-csharp[AsyncOptimizedValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#31_AsyncOptimizedValueTask "Return async result or cached value")]

<span data-ttu-id="1dfa1-358">Tüm performans önerileri ile iki sürüm de büyük yapmadan önce Kıyaslama şekilde ölçek kodunuza değiştirir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-358">As with all performance recommendations, you should benchmark both versions before making large scale changes to your code.</span></span>

<span data-ttu-id="1dfa1-359">Dönüş değeri hedefinin olduğunda bir `await` API'den değiştirme deyimi, bir <xref:System.Threading.Tasks.Task%601> için bir <xref:System.Threading.Tasks.ValueTask%601> olduğu bir [kaynağı uyumlu değişiklik](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="1dfa1-359">When the return value is the target of an `await` statement, changing an API from a <xref:System.Threading.Tasks.Task%601> to a <xref:System.Threading.Tasks.ValueTask%601> is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="1dfa1-360">Genel olarak, değiştirerek `ValueTask` değil.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-360">In general, changing to `ValueTask` is not.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="1dfa1-361">Sayısal sabit değer sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="1dfa1-361">Numeric literal syntax improvements</span></span>

<span data-ttu-id="1dfa1-362">Sayısal sabitlere misreading kodu ilk kez okurken anlaşılması zor yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-362">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="1dfa1-363">Bu sayı, bit maskeleri veya diğer sembolik olarak kullanıldığında, bu genellikle oluşur. sayısal değerler yerine.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-363">This often occurs when those numbers are used as bit masks or other symbolic rather than numeric values.</span></span> <span data-ttu-id="1dfa1-364">C# 7.0 en okunabilir bir biçimde için kullanım sayıları yazma daha kolay hale getirmek için iki yeni özellikler içerir: *ikili sabit dizeler*, ve *rakam ayırıcıları*.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-364">C# 7.0 includes two new features to make it easier to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="1dfa1-365">İçin bu kez, bit maskeleri oluştururken ya da birkaç ikili gösterimini her ikili dosyada bu sayıyı yazma en okunabilir bir kod sağlar:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-365">For those times when you are creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[BinaryConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#32_BinaryConstants "Binary constants")]

<span data-ttu-id="1dfa1-366">`0b` Sayı ikili sayı olarak yazılır sabiti başında gösterir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-366">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span>

<span data-ttu-id="1dfa1-367">Genellikle bit düzenleri sunarak daha kolay olacak şekilde ikili sayı çok uzun alabilirsiniz `_` basamak ayırıcı olarak:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-367">Binary numbers can get very long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator:</span></span>

[!code-csharp[ThousandSeparators](../../../samples/snippets/csharp/new-in-7/Program.cs#33_ThousandSeparators "Thousands separators")]

<span data-ttu-id="1dfa1-368">Basamak ayırıcı sabiti her yerde görünebilir.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-368">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="1dfa1-369">Taban 10 numaraları için bir binler basamağı kullanmak yaygın olacaktır ayırıcı:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-369">For base 10 numbers, it would be common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](../../../samples/snippets/csharp/new-in-7/Program.cs#34_LargeIntegers "Large integer")]

<span data-ttu-id="1dfa1-370">Basamak ayırıcı ile birlikte kullanılabilir `decimal`, `float` ve `double` türleri de:</span><span class="sxs-lookup"><span data-stu-id="1dfa1-370">The digit separator can be used with `decimal`, `float` and `double` types as well:</span></span>

[!code-csharp[OtherConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#35_OtherConstants "non-integral constants")]

<span data-ttu-id="1dfa1-371">Birlikte ele alındığında Sayısal sabitler ile çok daha fazla okunabilirliği bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dfa1-371">Taken together, you can declare numeric constants with much more readability.</span></span>
