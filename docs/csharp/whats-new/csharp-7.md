---
title: C# 7.0'da Yenilikler - C# Rehberi
description: C# dilinin 7.0 sürümündeki yeni özellikler hakkında genel bir bakış alın.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: a6ac5c00ceb2ce8e5e56e2a86a8cde937d5108e2
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399695"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="bfe63-103">C# 7.0 Yenilikleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-103">What's new in C# 7.0</span></span>

<span data-ttu-id="bfe63-104">C# 7.0, C# diline bir dizi yeni özellik ekler:</span><span class="sxs-lookup"><span data-stu-id="bfe63-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="bfe63-105">`out`Değişken</span><span class="sxs-lookup"><span data-stu-id="bfe63-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="bfe63-106">Değerleri, `out` kullanıldıkları yönteme bağımsız değişken olarak sıralı olarak bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="bfe63-107">Dizilerini</span><span class="sxs-lookup"><span data-stu-id="bfe63-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="bfe63-108">Birden çok ortak alan içeren hafif, adsız türler oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="bfe63-109">Derleyiciler ve IDE araçları bu tür anlambilimini anlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="bfe63-110">Atılanlar</span><span class="sxs-lookup"><span data-stu-id="bfe63-110">Discards</span></span>](#discards)
  - <span data-ttu-id="bfe63-111">Atanan değer le ilgilenmediğiniz durumlarda atamalarda kullanılan geçici, yalnızca yazma değişkenleridir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="bfe63-112">Tuples ve kullanıcı tanımlı türleri deconstructing yanı sıra parametreleri ile `out` yöntemleri arama yaparken en yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="bfe63-113">Desen Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="bfe63-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="bfe63-114">Bu tür üyelerin rasgele türleri ve değerlerini temel alan dallanma mantığı oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="bfe63-115">`ref`yerliler ve iadeler</span><span class="sxs-lookup"><span data-stu-id="bfe63-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="bfe63-116">Yöntem yerel değişkenler ve iade değerleri diğer depolama referansları olabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="bfe63-117">Yerel Fonksiyonlar</span><span class="sxs-lookup"><span data-stu-id="bfe63-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="bfe63-118">Kapsamlarını ve görünürlüklerini sınırlamak için işlevleri diğer işlevlerin içine sokabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="bfe63-119">Daha fazla ifade gövdeli üye</span><span class="sxs-lookup"><span data-stu-id="bfe63-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="bfe63-120">İfadeler kullanılarak yazılabilir üye listesi büyüdü.</span><span class="sxs-lookup"><span data-stu-id="bfe63-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="bfe63-121">`throw`Ifa -de</span><span class="sxs-lookup"><span data-stu-id="bfe63-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="bfe63-122">Bir deyim `throw` olduğu için daha önce izin verilmeyen kod yapılarında özel durumlar atabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="bfe63-123">Genelleştirilmiş async dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="bfe63-124">Değiştirici ile bildirilen yöntemler ek olarak diğer `Task` `Task<T>`türleri döndürebilir ve . `async`</span><span class="sxs-lookup"><span data-stu-id="bfe63-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="bfe63-125">Sayısal gerçek sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="bfe63-126">Yeni belirteçler sayısal sabitler için okunabilirliği artırır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="bfe63-127">Bu makalenin geri kalanı her özelliğin genel bir özetini sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="bfe63-128">Her özellik için, bunun arkasındaki mantığı öğreneceksiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="bfe63-129">Sözdizimini öğreneceksin.</span><span class="sxs-lookup"><span data-stu-id="bfe63-129">You'll learn the syntax.</span></span> <span data-ttu-id="bfe63-130">`dotnet try` Bu özellikleri ortamınızda genel aracı kullanarak keşfedebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="bfe63-131">[dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global aracını yükleyin.</span><span class="sxs-lookup"><span data-stu-id="bfe63-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="bfe63-132">[Dotnet/try-samples](https://github.com/dotnet/try-samples) deposunu klonla.</span><span class="sxs-lookup"><span data-stu-id="bfe63-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="bfe63-133">*Deneme örnekleri* deposu için geçerli dizini *csharp7* alt dizinine ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="bfe63-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="bfe63-134">`dotnet try` öğesini çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="bfe63-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="bfe63-135">`out`Değişken</span><span class="sxs-lookup"><span data-stu-id="bfe63-135">`out` variables</span></span>

<span data-ttu-id="bfe63-136">Parametreleri destekleyen `out` varolan sözdizimi bu sürümde geliştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="bfe63-137">Artık ayrı `out` bir bildirim bildirimi yazmak yerine bir yöntem çağrısının bağımsız değişken listesinde değişkenleri bildirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="bfe63-138">Yukarıda gösterildiği gibi, netlik `out` için değişkenin türünü belirtmek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="bfe63-139">Ancak, dil örtülü olarak yazılan yerel değişkeni destekler:</span><span class="sxs-lookup"><span data-stu-id="bfe63-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="bfe63-140">Kodu okumak daha kolaydır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-140">The code is easier to read.</span></span>
  - <span data-ttu-id="bfe63-141">Dış değişkeni kullandığınız yerde bildirirsiniz, yukarıdaki başka bir satırda değil.</span><span class="sxs-lookup"><span data-stu-id="bfe63-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="bfe63-142">Başlangıç değeri atamaya gerek yok.</span><span class="sxs-lookup"><span data-stu-id="bfe63-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="bfe63-143">Değişkeni `out` bir yöntem çağrısında kullanıldığı yeri beyan ederek, atanmadan önce yanlışlıkla kullanamazsınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="bfe63-144">Demetler</span><span class="sxs-lookup"><span data-stu-id="bfe63-144">Tuples</span></span>

<span data-ttu-id="bfe63-145">C#, tasarım amacınızı açıklamak için kullanılan sınıflar ve strüktler için zengin bir sözdizimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="bfe63-146">Ama bazen bu zengin sözdizimi en az yararı ile ekstra çalışma gerektirir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="bfe63-147">Genellikle birden fazla veri öğesi içeren basit bir yapı gerektiren yöntemler yazabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="bfe63-148">Bu senaryoları desteklemek için *Tuples* C#'a eklendi.</span><span class="sxs-lookup"><span data-stu-id="bfe63-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="bfe63-149">Tuples, veri üyelerini temsil edecek birden çok alan içeren hafif veri yapılarıdır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="bfe63-150">Alanlar doğrulanmadı ve kendi yöntemlerinizi tanımlayamadığınız</span><span class="sxs-lookup"><span data-stu-id="bfe63-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="bfe63-151">Tuples C # 7.0 önce mevcut, ama verimsiz ve hiçbir dil desteği vardı.</span><span class="sxs-lookup"><span data-stu-id="bfe63-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="bfe63-152">Bu, tuple öğelerinin yalnızca `Item1`"ve `Item2` benzeri" olarak başvurulabileceği anlamına geliyordu.</span><span class="sxs-lookup"><span data-stu-id="bfe63-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="bfe63-153">C# 7.0, yeni, daha verimli tuple türleri kullanarak bir tuple alanları için anlamsal adlar sağlayan tuples için dil desteği sunar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="bfe63-154">Her üyeye bir değer atayarak ve isteğe bağlı olarak tuple üyelerinin her birine anlamsal adlar sağlayarak bir tuple oluşturabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="bfe63-155">Tuple `namedLetters` olarak `Alpha` adlandırılan alanları `Beta`içerir ve.</span><span class="sxs-lookup"><span data-stu-id="bfe63-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="bfe63-156">Bu adlar yalnızca derleme zamanında bulunur ve örneğin çalışma zamanında yansımayı kullanarak tuple'ı incelerken korunmaz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="bfe63-157">Bir tuple atamasında, atamanın sağ tarafındaki alanların adlarını da belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="bfe63-158">Bir yöntemden döndürülen bir tuple'ın üyelerini paketini açmak istediğiniz zamanlar olabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="bfe63-159">Bunu, tuple'daki değerlerin her biri için ayrı değişkenler beyan ederek yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="bfe63-160">Bu unpackaging tuple *deconstructing* denir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="bfe63-161">Ayrıca,.NET'teki herhangi bir tür için benzer bir yapısızlaştırma sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="bfe63-162">Sınıfın bir `Deconstruct` üyesi olarak bir yöntem yazarsınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="bfe63-163">Bu `Deconstruct` yöntem, ayıklamak istediğiniz özelliklerin her biri için bir `out` bağımsız değişken kümesi sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="bfe63-164">Bunları `Point` `X` ve `Y` koordinatları ayıklayan bir dekonstrüktör yöntemi sağlayan bu sınıfı düşünün:</span><span class="sxs-lookup"><span data-stu-id="bfe63-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="bfe63-165">Bir tuple'a bir `Point` a atayarak tek tek alanları ayıklayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="bfe63-166">[Tuples makalede tuples](../tuples.md)hakkında derinlemesine daha fazla bilgi edinebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="bfe63-167">Atılanlar</span><span class="sxs-lookup"><span data-stu-id="bfe63-167">Discards</span></span>

<span data-ttu-id="bfe63-168">Genellikle bir tuple'ı dekonstrükte ederken veya parametreleri olan `out` bir yöntemi ararken, değerini önemsemeyen ve kullanmak istemediğiniz bir değişkeni tanımlamak zorunda kalırsınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="bfe63-169">C# bu senaryoyu işlemek için *atılanlar* için destek ekler.</span><span class="sxs-lookup"><span data-stu-id="bfe63-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="bfe63-170">Bir atma, yalnızca yazma yla `_` ilgili bir değişkendir ve adı (alt karakter) dir; tek bir değişkene atmayı istediğiniz tüm değerleri atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="bfe63-171">Bir atma atanmamış bir değişken gibidir; atama deyimi dışında, atama kodunda kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="bfe63-172">Atılanlar aşağıdaki senaryolarda desteklenir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="bfe63-173">Tuples veya kullanıcı tanımlı türleri deconstructing zaman.</span><span class="sxs-lookup"><span data-stu-id="bfe63-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="bfe63-174">Parametreleri [ile](../language-reference/keywords/out-parameter-modifier.md) yöntemleri ararken.</span><span class="sxs-lookup"><span data-stu-id="bfe63-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="bfe63-175">Is [ve](../language-reference/keywords/is.md) [switch](../language-reference/keywords/switch.md) deyimleri ile eşleşen bir desen deseni.</span><span class="sxs-lookup"><span data-stu-id="bfe63-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="bfe63-176">Bir atamanın değerini bir atama olarak açıkça tanımlamak istediğinizde bağımsız bir tanımlayıcı olarak.</span><span class="sxs-lookup"><span data-stu-id="bfe63-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="bfe63-177">Aşağıdaki örnek, iki `QueryCityDataForYears` farklı yıl için bir şehir için veri içeren bir 6-tuple döndüren bir yöntem tanımlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="bfe63-178">Örnekteki yöntem çağrısı yalnızca yöntem tarafından döndürülen iki popülasyon değeriyle ilgilidir ve bu nedenle tuple'da kalan değerleri, tuple'ı yapısızleştirdiği zaman atar gibi davranır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="bfe63-179">Daha fazla bilgi için [bkz.](../discards.md)</span><span class="sxs-lookup"><span data-stu-id="bfe63-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="bfe63-180">Desen eşleştirme</span><span class="sxs-lookup"><span data-stu-id="bfe63-180">Pattern matching</span></span>

<span data-ttu-id="bfe63-181">*Desen eşleştirme,* bir nesnenin türü dışındaki özellikler üzerinde yöntem gönderme uygulamanızı sağlayan bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="bfe63-182">Büyük olasılıkla bir nesnenin türüne göre yöntem gönderimi hakkında zaten aşinasınızdır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="bfe63-183">Nesne yönelimli programlamada, sanal ve geçersiz kılma yöntemleri, nesnenin türüne göre yöntem gönderme yöntemini uygulamak için dil sözdizimini sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="bfe63-184">Taban ve Türetilmiş sınıflar farklı uygulamalar sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="bfe63-185">Desen eşleştirme ifadeleri, devralma hiyerarşisi aracılığıyla ilişkili olmayan türler ve veri öğeleri için benzer gönderme desenleri kolayca uygulayabilmeniz için bu kavramı genişletir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="bfe63-186">Desen eşleştirme `is` ifadeleri `switch` ve ifadeleri destekler.</span><span class="sxs-lookup"><span data-stu-id="bfe63-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="bfe63-187">Her nesne nin aranan deseni karşılayıp karşıladığını belirlemek için bir nesnenin ve özelliklerinin incelenmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="bfe63-188">Desene `when` ek kurallar belirtmek için anahtar sözcüğü kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="bfe63-189">`is` Desen ifadesi, tanıdık [ `is` işleci,](../language-reference/keywords/is.md#pattern-matching-with-is) bir nesneyi türü hakkında sorgulamak ve sonucu tek bir yönergeyle atamak için genişletir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="bfe63-190">Aşağıdaki kod, bir değişkenin `int`,, eğer öyleyse, geçerli toplama ekleyip ekleninbileni denetler:</span><span class="sxs-lookup"><span data-stu-id="bfe63-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="bfe63-191">Önceki küçük `is` örnek, ifadedeki geliştirmeleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="bfe63-192">Değer türlerinin yanı sıra başvuru türlerine karşı sınayabilir ve başarılı sonucu doğru türden yeni bir değişkene atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="bfe63-193">Anahtar eşleşmesi ifadesi, C# dilinin `switch` zaten bir parçası olan deyimi temel alan tanıdık bir sözdizimine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="bfe63-194">Güncelleştirilmiş anahtar deyimibirkaç yeni yapıya sahiptir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="bfe63-195">Bir `switch` ifadenin yönetim türü artık integral türleri, `Enum` türleri `string`veya bu türlerden birine karşılık gelen nullable türüyle sınırlı değildir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="bfe63-196">Herhangi bir tür kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-196">Any type may be used.</span></span>
- <span data-ttu-id="bfe63-197">Her `switch` `case` etiketteki ifade türünü sınayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="bfe63-198">İfadede `is` olduğu gibi, bu türe yeni bir değişken atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="bfe63-199">Bu değişkendeki `when` diğer test koşullarına bir yan tümce ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="bfe63-200">Etiketlerin `case` sırası artık önemlidir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="bfe63-201">Eşleşen ilk dal yürütülür; diğerleri atlanır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="bfe63-202">Aşağıdaki kod bu yeni özellikleri gösterir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="bfe63-203">`case 0:`tanıdık sabit desendir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="bfe63-204">`case IEnumerable<int> childSequence:`bir tür desenidir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="bfe63-205">`case int n when n > 0:`ek `when` bir koşul ile bir tür desendir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="bfe63-206">`case null:`null desendir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="bfe63-207">`default:`tanıdık varsayılan durumdur.</span><span class="sxs-lookup"><span data-stu-id="bfe63-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="bfe63-208">[C#'da Desen Eşleştirme'de](../pattern-matching.md)desen eşleştirme hakkında daha fazla bilgi edinebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="bfe63-209">Ref yerlileri ve döner</span><span class="sxs-lookup"><span data-stu-id="bfe63-209">Ref locals and returns</span></span>

<span data-ttu-id="bfe63-210">Bu özellik, başka bir yerde tanımlanan değişkenlere başvuru kullanan ve döndüren algoritmalar sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="bfe63-211">Bir örnek, büyük matrisler ile çalışıyor ve belirli özelliklere sahip tek bir konum bulmaktır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="bfe63-212">Aşağıdaki yöntem matristeki depolama alanına bir **başvuru** döndürür:</span><span class="sxs-lookup"><span data-stu-id="bfe63-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="bfe63-213">İade değerini a `ref` olarak bildirebilir ve aşağıdaki kodda gösterildiği gibi matristeki bu değeri değiştirebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bfe63-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="bfe63-214">C# dili, `ref` yerel halkı kötüye kullanmaktan koruyan çeşitli kurallara sahiptir ve geri döner:</span><span class="sxs-lookup"><span data-stu-id="bfe63-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="bfe63-215">Yöntem imzasına `ref` ve yöntemdeki tüm `return` ifadelere anahtar kelime eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="bfe63-216">Bu yöntem, yöntem boyunca referans yoluyla döner açıkça yapar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="bfe63-217">A `ref return` bir değer değişkenine veya `ref` bir değişkene atanabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="bfe63-218">Arayan, iade değerinin kopyalanıp kopyalanmadığını denetler.</span><span class="sxs-lookup"><span data-stu-id="bfe63-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="bfe63-219">İade değerini `ref` atarken değiştiriciyi atamak, arayanın depolamaya bir başvuru değil, değerin bir kopyasını istediğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="bfe63-220">`ref` Yerel bir değişkene standart yöntem iade değeri atayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="bfe63-221">Bu gibi ifadeler indasvesi`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="bfe63-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="bfe63-222">Ömrü metodun yürütülmesinden öteye uzanamayan bir değişkene bir değişkeni `ref` döndüremezsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="bfe63-223">Bu, yerel bir değişkene veya benzer kapsama sahip bir değişkene başvuru veremezsiniz anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="bfe63-224">`ref`yerel ve döner async yöntemleri ile kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="bfe63-225">Derleyici, async yöntemi döndüğünde başvurulan değişkenin son değerine ayarlandığını bilemiyor.</span><span class="sxs-lookup"><span data-stu-id="bfe63-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="bfe63-226">Ref locals ve ref returns eklenmesi, değerleri kopyalamaktan kaçınarak veya birden çok kez dereferencing işlemleri gerçekleştirerek daha verimli algoritmalar sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="bfe63-227">İade `ref` değerine [ekleme, kaynak uyumlu](version-update-considerations.md#source-compatible-changes)bir değişikliktir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="bfe63-228">Varolan kod derlenir, ancak atandığında ref iade değeri kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="bfe63-229">Arayanların, iadeyi referans olarak depolamak için iade değeri depolama alanını yerel bir `ref` değişkene güncelleştirmeleri gerekir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="bfe63-230">Daha fazla bilgi için [ref anahtar kelime](../language-reference/keywords/ref.md) makalesine bakın.</span><span class="sxs-lookup"><span data-stu-id="bfe63-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="bfe63-231">Yerel işlevler</span><span class="sxs-lookup"><span data-stu-id="bfe63-231">Local functions</span></span>

<span data-ttu-id="bfe63-232">Sınıflar için birçok tasarım, yalnızca bir konumdan çağrılan yöntemleri içerir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="bfe63-233">Bu ek özel yöntemler her yöntemi küçük ve odaklanmış tutar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="bfe63-234">*Yerel işlevler,* yöntemleri başka bir yöntemin bağlamında bildirmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="bfe63-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="bfe63-235">Yerel işlevler, sınıf okuyucularının yerel yöntemin yalnızca beyan edildiği bağlamdan çağrıldığını görmesini kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="bfe63-236">Yerel işlevler için iki yaygın kullanım nedeni vardır: genel yineleyici yöntemleri ve genel async yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="bfe63-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="bfe63-237">Her iki yöntem türü de, programcıların beklediğinden daha geç hata bildiren kod oluşturur.</span><span class="sxs-lookup"><span data-stu-id="bfe63-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="bfe63-238">Yineleme yöntemlerinde, yalnızca döndürülen sırayı sıralayan kodu ararken herhangi bir özel durum gözlenir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="bfe63-239">Async yöntemleri, herhangi bir özel durum `Task` yalnızca döndürülen bekleniyor gözlenir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="bfe63-240">Aşağıdaki örnek, parametre doğrulaması nın yerel bir işlev kullanılarak yineleyici uygulamasından ayırılmasını gösterir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="bfe63-241">Aynı teknik, bağımsız `async` değişken doğrulamasından kaynaklanan özel durumların asenkron çalışma başlamadan önce atılmasını sağlamak için yöntemlerle kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="bfe63-242">Yerel fonksiyonlar tarafından desteklenen bazı tasarımlar da *lambda ifadeleri*kullanılarak gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-242">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="bfe63-243">Daha fazla bilgi için, [yerel işlevler vs lambda ifadeler](../local-functions-vs-lambdas.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="bfe63-243">For more information, see [Local functions vs. lambda expressions](../local-functions-vs-lambdas.md).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="bfe63-244">Daha fazla ifade gövdeli üye</span><span class="sxs-lookup"><span data-stu-id="bfe63-244">More expression-bodied members</span></span>

<span data-ttu-id="bfe63-245">C# 6, üye işlevler ve salt okunur özellikler için [ifade gövdeli üyeleri](csharp-6.md#expression-bodied-function-members) tanıttı.</span><span class="sxs-lookup"><span data-stu-id="bfe63-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="bfe63-246">C# 7.0, ifade olarak uygulanabilen izin verilen üyeleri genişletir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="bfe63-247">C# 7.0'da, *özellikler* ve *dizin*oluşturucular, *sonlandırıcılar*ve `get` `set` erişimciler uygulayabilirsiniz. *constructors*</span><span class="sxs-lookup"><span data-stu-id="bfe63-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="bfe63-248">Aşağıdaki kod her birinin örneklerini gösterir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="bfe63-249">Bu örnek bir sonlandırıcı gerekmez, ancak sözdizimini göstermek için gösterilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="bfe63-250">Yönetilmeyen kaynakları serbest bırakmak gerekli olmadıkça sınıfınızda bir sonlandırıcı uygulamamalısınız.</span><span class="sxs-lookup"><span data-stu-id="bfe63-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="bfe63-251">Ayrıca, yönetilmeyen <xref:System.Runtime.InteropServices.SafeHandle> kaynakları doğrudan yönetmek yerine sınıfı kullanmayı da düşünmelisiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="bfe63-252">İfade gövdeli üyeler için bu yeni konumlar C# dili için önemli bir kilometre taşını temsil ediyor: Bu özellikler açık kaynak [roslyn](https://github.com/dotnet/Roslyn) projesi üzerinde çalışan topluluk üyeleri tarafından uygulanmıştır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="bfe63-253">Bir metodu ifade gövdeli üyeye değiştirmek ikili uyumlu bir [değişikliktir.](version-update-considerations.md#binary-compatible-changes)</span><span class="sxs-lookup"><span data-stu-id="bfe63-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="bfe63-254">Throw ifadeleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-254">Throw expressions</span></span>

<span data-ttu-id="bfe63-255">C#'da `throw` her zaman bir ifade olmuştur.</span><span class="sxs-lookup"><span data-stu-id="bfe63-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="bfe63-256">Çünkü `throw` bir ifade değil, bir ifadedir, kullanamadığınız C# yapıları vardı.</span><span class="sxs-lookup"><span data-stu-id="bfe63-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="bfe63-257">Bunlar koşullu ifadeler, null coalescing ifadeler ve bazı lambda ifadeler dahil.</span><span class="sxs-lookup"><span data-stu-id="bfe63-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="bfe63-258">İfade gövdeli üyelerin eklenmesi, `throw` ifadelerin yararlı olacağı daha fazla konum ekler.</span><span class="sxs-lookup"><span data-stu-id="bfe63-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="bfe63-259">Böylece bu yapılardan herhangi birini yazabilirsiniz, C# 7.0 [*ifadeleri atmak*](../language-reference/keywords/throw.md#the-throw-expression)tanıttı.</span><span class="sxs-lookup"><span data-stu-id="bfe63-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="bfe63-260">Bu ek, daha fazla ifade tabanlı kod yazmayı kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="bfe63-261">Hata denetimi için ek ifadelergerekmez.</span><span class="sxs-lookup"><span data-stu-id="bfe63-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="bfe63-262">Genelleştirilmiş async dönüş türleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-262">Generalized async return types</span></span>

<span data-ttu-id="bfe63-263">Nesneyi `Task` async yöntemlerinden döndürmek, belirli yollarda performans darboğazlarına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="bfe63-264">`Task`bir başvuru türüdür, bu nedenle kullanmak bir nesneyi ayırmak anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="bfe63-265">Değiştirici ile bildirilen bir `async` yöntemin önbelleğe alınmış bir sonucu döndürdüğü veya eşzamanlı olarak tamamlandığı durumlarda, ek ayırmalar kodun performans açısından kritik bölümlerinde önemli bir zaman maliyeti ne olabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="bfe63-266">Bu ayırmalar sıkı döngüler halinde gerçekleşirse pahalıya mal olabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="bfe63-267">Yeni dil özelliği, async yöntemi dönüş türlerinin `Task` `Task<T>`, `void`ve .</span><span class="sxs-lookup"><span data-stu-id="bfe63-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="bfe63-268">Döndürülen tür yine de async deseni karşılamalıdır, yani bir `GetAwaiter` yöntem erişilebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="bfe63-269">Somut bir örnek `ValueTask` olarak, bu yeni dil özelliğinden yararlanmak için .NET türü eklendi:</span><span class="sxs-lookup"><span data-stu-id="bfe63-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="bfe63-270">Türü kullanmak için NuGet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) paketini <xref:System.Threading.Tasks.ValueTask%601> eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="bfe63-271">Bu geliştirme, performans açısından kritik kod `Task` ayırmayı önlemek için kitaplık yazarları için en yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="bfe63-272">Sayısal gerçek sözdizimi geliştirmeleri</span><span class="sxs-lookup"><span data-stu-id="bfe63-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="bfe63-273">Sayısal sabitleri yanlış okumak, kodu ilk kez okurken kodu anlamayı zorlaştırabilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="bfe63-274">Bit maskeleri veya diğer sembolik değerler yanlış anlama yatkındır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="bfe63-275">C# 7.0 amaçlanan kullanım için en okunabilir şekilde sayılar yazmak için iki yeni özellik içerir: *ikili literals*, ve *basamak ayırıcılar*.</span><span class="sxs-lookup"><span data-stu-id="bfe63-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="bfe63-276">Bit maskeleri oluşturduğunuz veya bir sayının ikili gösteriminin en çok okunabilir kodu oluşturduğu zamanlar için, bu sayıyı ikili olarak yazın:</span><span class="sxs-lookup"><span data-stu-id="bfe63-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="bfe63-277">Sabitin `0b` başında, sayının ikili sayı olarak yazıldığı belirtilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="bfe63-278">İkili sayılar uzun olabilir, bu nedenle yukarıda ikili sabitte gösterildiği `_` gibi, bir basamak ayırıcısı olarak tanıtarak bit desenlerini görmek genellikle daha kolaydır.</span><span class="sxs-lookup"><span data-stu-id="bfe63-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="bfe63-279">Basamak ayırıcısı sabitin herhangi bir yerinde görünebilir.</span><span class="sxs-lookup"><span data-stu-id="bfe63-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="bfe63-280">Temel 10 sayı için, onu binlerce ayırıcı olarak kullanmak yaygındır:</span><span class="sxs-lookup"><span data-stu-id="bfe63-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="bfe63-281">Basamak ayırıcısı , `decimal`, `float`ve `double` türleri ile de kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="bfe63-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="bfe63-282">Birlikte ele alındığında, çok daha okunabilirlik ile sayısal sabitleri bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bfe63-282">Taken together, you can declare numeric constants with much more readability.</span></span>
