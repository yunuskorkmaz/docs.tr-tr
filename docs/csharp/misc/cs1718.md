---
title: Derleyici Uyarısı (seviye 3) CS1718
ms.date: 07/20/2015
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
ms.openlocfilehash: debbe75d3464a0b2e08ae088f6ce25648c9f29be
ms.sourcegitcommit: f87ad41b8e62622da126aa928f7640108c4eff98
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/07/2020
ms.locfileid: "80805880"
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="4be36-102">Derleyici Uyarısı (seviye 3) CS1718</span><span class="sxs-lookup"><span data-stu-id="4be36-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="4be36-103">Aynı değişkenle yapılan karşılaştırma; Başka bir şeyle karşılaştırmak mı demek istiyorsun?</span><span class="sxs-lookup"><span data-stu-id="4be36-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="4be36-104">Başka bir şeyle karşılaştırmak istediyseniz, ifadeyi düzeltin.</span><span class="sxs-lookup"><span data-stu-id="4be36-104">If you meant to compare to something else, then correct the statement.</span></span>  
  
 <span data-ttu-id="4be36-105">Ama başka bir olasılık, doğru veya yanlış için test edildi `if (a == a) (true)` ve `if (a < a) (false)`bu tür ifadeler tarafından yapıyor lardı ya .</span><span class="sxs-lookup"><span data-stu-id="4be36-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="4be36-106">Kullanmak `if (true)` daha iyidir `if (false)`veya, aşağıdaki iki nedenden dolayı:</span><span class="sxs-lookup"><span data-stu-id="4be36-106">It is better to use `if (true)` or `if (false)`, for the following two reasons:</span></span>  
  
- <span data-ttu-id="4be36-107">Bu daha basittir: her zaman sadece ne demek istediğinizi söylemek daha açıktır.</span><span class="sxs-lookup"><span data-stu-id="4be36-107">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
- <span data-ttu-id="4be36-108">Karışıklığı önlemeye yardımcı olur: C# 2.0'ın yeni bir özelliği, SQL `null` Server tarafından kullanılan programlama dili Olan T-SQL'deki değere benzeyen boş değer türleridir.</span><span class="sxs-lookup"><span data-stu-id="4be36-108">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="4be36-109">T-SQL'e aşina olan `if (a == a)`geliştiriciler, T-SQL'de üçüncül mantığın kullanılması nedeniyle, geçersiz değer türlerinin ifadeler üzerindeki etkisinden endişe duyabilirler.</span><span class="sxs-lookup"><span data-stu-id="4be36-109">Developers familiar with T-SQL might be concerned about the effect of nullable value types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="4be36-110">Kullanırsanız `true` veya `false`, Bu olası karışıklığı önlemek.</span><span class="sxs-lookup"><span data-stu-id="4be36-110">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="4be36-111">Örnek</span><span class="sxs-lookup"><span data-stu-id="4be36-111">Example</span></span>  
 <span data-ttu-id="4be36-112">Aşağıdaki kod CS1718 uyarı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="4be36-112">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester
{  
    public static void Main()
    {
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {
            i++;  
        }  
    }  
}  
```
