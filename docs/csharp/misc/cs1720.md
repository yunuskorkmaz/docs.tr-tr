---
title: Derleyici Uyarısı (düzey 1) CS1720
ms.date: 07/20/2015
f1_keywords:
- CS1720
helpviewer_keywords:
- CS1720
ms.assetid: 97adc294-3a4c-4418-a4ed-ccff43125b62
ms.openlocfilehash: 90d5bc971344bdc062360fa5cb461521d34c63f8
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91181914"
---
# <a name="compiler-warning-level-1-cs1720"></a><span data-ttu-id="dab73-102">Derleyici Uyarısı (düzey 1) CS1720</span><span class="sxs-lookup"><span data-stu-id="dab73-102">Compiler Warning (level 1) CS1720</span></span>

<span data-ttu-id="dab73-103">' Genel tür ' öğesinin varsayılan değeri null olduğundan, ifade her zaman System. NullReferenceException öğesine neden olacak</span><span class="sxs-lookup"><span data-stu-id="dab73-103">Expression will always cause a System.NullReferenceException because the default value of 'generic type' is null</span></span>  
  
 <span data-ttu-id="dab73-104">Bir başvuru türü (örneğin, bir sınıf) olan bir genel tür değişkeninin varsayılan değeri içeren bir ifade yazarsanız, bu hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="dab73-104">If you write an expression involving the default of a generic type variable that is a reference type (for example, a class), this error will occur.</span></span> <span data-ttu-id="dab73-105">Aşağıdaki ifadeyi göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="dab73-105">Consider the following expression:</span></span>  
  
```csharp  
default(T).ToString()  
```  
  
 <span data-ttu-id="dab73-106">`T`Bir başvuru türü olduğundan, varsayılan değeri null olur ve bu nedenle metodunu uygulamaya çalışılması, <xref:System.Object.ToString%2A> bir oluşturur <xref:System.NullReferenceException> .</span><span class="sxs-lookup"><span data-stu-id="dab73-106">Since `T` is a reference type, its default value is null, and so attempting to apply the <xref:System.Object.ToString%2A> method to it will throw a <xref:System.NullReferenceException>.</span></span>  
  
## <a name="example"></a><span data-ttu-id="dab73-107">Örnek</span><span class="sxs-lookup"><span data-stu-id="dab73-107">Example</span></span>  

 <span data-ttu-id="dab73-108">Türündeki sınıf başvuru kısıtlaması, `T` `T` bir başvuru türü olmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="dab73-108">The class reference constraint on type `T` ensures that `T` is a reference type.</span></span>  
  
 <span data-ttu-id="dab73-109">Aşağıdaki örnek CS1720 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dab73-109">The following sample generates CS1720.</span></span>  
  
```csharp  
// CS1720.cs  
using System;  
public class Tester
{  
    public static void GenericClass<T>(T t1) where T : class
    {  
        Console.WriteLine(default(T).ToString());  // CS1720  
    }  
    public static void Main() {}  
}  
```
