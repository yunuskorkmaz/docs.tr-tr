---
description: Derleyici Uyarısı (düzey 2) CS0279
title: Derleyici Uyarısı (düzey 2) CS0279
ms.date: 07/20/2015
f1_keywords:
- CS0279
helpviewer_keywords:
- CS0279
ms.assetid: 5e5faa8f-3d5b-4999-aa62-ff7f131a3e04
ms.openlocfilehash: d9109d0b8cd95728ef2ebcf27d068300a22519b0
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89143265"
---
# <a name="compiler-warning-level-2-cs0279"></a><span data-ttu-id="ee207-103">Derleyici Uyarısı (düzey 2) CS0279</span><span class="sxs-lookup"><span data-stu-id="ee207-103">Compiler Warning (level 2) CS0279</span></span>
<span data-ttu-id="ee207-104">' tür adı ', ' model adı ' modelini uygulamıyor.</span><span class="sxs-lookup"><span data-stu-id="ee207-104">'type name' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="ee207-105">' Yöntem adı ' statik veya genel değil.</span><span class="sxs-lookup"><span data-stu-id="ee207-105">'method name' is either static or not public.</span></span>  
  
 <span data-ttu-id="ee207-106">C# içinde ve gibi tanımlanmış desenleri kullanan birkaç deyim vardır `foreach` `using` .</span><span class="sxs-lookup"><span data-stu-id="ee207-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="ee207-107">Örneğin, `foreach` sıralanabilir desenler uygulayan koleksiyon sınıfına bağımlıdır.</span><span class="sxs-lookup"><span data-stu-id="ee207-107">For example, `foreach` relies on the collection class implementing the enumerable pattern.</span></span> <span data-ttu-id="ee207-108">Bu hata, derleyici, bildirildiği bir yöntem nedeniyle eşleşmeyi yapamadığında oluşur `static` `public` .</span><span class="sxs-lookup"><span data-stu-id="ee207-108">This error occurs when the compiler is unable to make the match due to a method being declared `static` or not `public`.</span></span> <span data-ttu-id="ee207-109">Desenlerdeki yöntemlerin sınıfların örnekleri olması ve genel olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="ee207-109">Methods in patterns are required to be instances of classes, and to be public.</span></span>  
  
## <a name="example"></a><span data-ttu-id="ee207-110">Örnek</span><span class="sxs-lookup"><span data-stu-id="ee207-110">Example</span></span>  
 <span data-ttu-id="ee207-111">Aşağıdaki örnek CS0279 oluşturur:</span><span class="sxs-lookup"><span data-stu-id="ee207-111">The following example generates CS0279:</span></span>  
  
```csharp  
// CS0279.cs  
  
using System;  
using System.Collections;  
  
public class myTest : IEnumerable  
{  
    IEnumerator IEnumerable.GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    internal IEnumerator GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    public static void Main()  
    {  
        foreach (int i in new myTest()) {}  // CS0279  
    }  
}  
```
