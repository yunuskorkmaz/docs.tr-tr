---
title: Derleyici Uyarısı (Düzey 2) CS0728
ms.date: 07/20/2015
f1_keywords:
- CS0728
helpviewer_keywords:
- CS0728
ms.assetid: ad6d860d-bac4-48f3-9eab-1efd2b6de6c0
ms.openlocfilehash: 2cd0d1f35074c254d3d061cd58602d16db8281d5
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61654996"
---
# <a name="compiler-warning-level-2-cs0728"></a><span data-ttu-id="1ea33-102">Derleyici Uyarısı (Düzey 2) CS0728</span><span class="sxs-lookup"><span data-stu-id="1ea33-102">Compiler Warning (level 2) CS0728</span></span>
<span data-ttu-id="1ea33-103">Büyük olasılıkla hatalı atama yapılmış 'using veya lock deyimine bağımsız değişken olan yerel değişken'.</span><span class="sxs-lookup"><span data-stu-id="1ea33-103">Possibly incorrect assignment to local 'variable' which is the argument to a using or lock statement.</span></span>  <span data-ttu-id="1ea33-104">Dispose çağrısı veya kilit açma gerçekleştirilecek yerel öğenin özgün değerinde.</span><span class="sxs-lookup"><span data-stu-id="1ea33-104">The Dispose call or unlocking will happen on the original value of the local.</span></span>  
  
 <span data-ttu-id="1ea33-105">Birkaç senaryo mevcuttur burada `using` veya `lock` blokları kaynakların geçici sızıntı neden olur.</span><span class="sxs-lookup"><span data-stu-id="1ea33-105">There are several scenarios where `using` or `lock` blocks will result in a temporary leak of resources.</span></span> <span data-ttu-id="1ea33-106">Bir örnek aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="1ea33-106">Here is one example:</span></span>  
  
 `thisType f = null;`  
  
 `using (f)`  
  
 `{`  
  
 `f = new thisType();`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="1ea33-107">Bu durumda, özgün değeri gibi null değişkenin `thisType` ne zaman elden `using` blok tamamlandıktan Yürütülüyor, ancak `thisType` blok içinde oluşturulan nesne olmayacak, çöp sonuç elde edersiniz ancak toplanmadı.</span><span class="sxs-lookup"><span data-stu-id="1ea33-107">In this case, the original value, such as null, of the variable `thisType` will be disposed of when the `using` block finishes executing, but the `thisType` object created inside the block will not be, although it will eventually get garbage collected.</span></span>  
  
 <span data-ttu-id="1ea33-108">Bu hatayı gidermek için aşağıdaki biçimi kullanın:</span><span class="sxs-lookup"><span data-stu-id="1ea33-108">To resolve this error, use the following form:</span></span>  
  
 `using (thisType f = new thisType())`  
  
 `{`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="1ea33-109">Bu durumda, yeni ayrılan `thisType` nesne elden biri.</span><span class="sxs-lookup"><span data-stu-id="1ea33-109">In this case, the newly allocated `thisType` object will be disposed of.</span></span>  
  
## <a name="example"></a><span data-ttu-id="1ea33-110">Örnek</span><span class="sxs-lookup"><span data-stu-id="1ea33-110">Example</span></span>  
 <span data-ttu-id="1ea33-111">Aşağıdaki kod, uyarı CS0728 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1ea33-111">The following code will generate warning CS0728.</span></span>  
  
```csharp 
// CS0728.cs  
  
using System;  
public class ValidBase : IDisposable  
{  
    public void Dispose() {  }  
}  
  
public class Logger  
{  
    public static void dummy()  
    {  
        ValidBase vb = null;  
        using (vb)   
        {  
            vb = null;  // CS0728  
        }  
        vb = null;  
    }  
    public static void Main() { }  
}  
```
