---
title: Derleyici Uyarısı (seviye 2) CS0728
ms.date: 07/20/2015
f1_keywords:
- CS0728
helpviewer_keywords:
- CS0728
ms.assetid: ad6d860d-bac4-48f3-9eab-1efd2b6de6c0
ms.openlocfilehash: 290c5e8a51da0b9dff6fd555a70079f340d30943
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79171917"
---
# <a name="compiler-warning-level-2-cs0728"></a><span data-ttu-id="3efbd-102">Derleyici Uyarısı (seviye 2) CS0728</span><span class="sxs-lookup"><span data-stu-id="3efbd-102">Compiler Warning (level 2) CS0728</span></span>
<span data-ttu-id="3efbd-103">Yerel 'değişken' için büyük olasılıkla yanlış atama olan bir kullanarak veya kilit deyimi için argüman.</span><span class="sxs-lookup"><span data-stu-id="3efbd-103">Possibly incorrect assignment to local 'variable' which is the argument to a using or lock statement.</span></span>  <span data-ttu-id="3efbd-104">Elden Çıkarma çağrısı veya kilidini açma, yerel in orijinal değeri üzerinde gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="3efbd-104">The Dispose call or unlocking will happen on the original value of the local.</span></span>  
  
 <span data-ttu-id="3efbd-105">Kaynakların geçici olarak `using` sızdırılmasıyla sonuçlanacak çeşitli senaryolar vardır. `lock`</span><span class="sxs-lookup"><span data-stu-id="3efbd-105">There are several scenarios where `using` or `lock` blocks will result in a temporary leak of resources.</span></span> <span data-ttu-id="3efbd-106">İşte bir örnek:</span><span class="sxs-lookup"><span data-stu-id="3efbd-106">Here is one example:</span></span>  
  
 `thisType f = null;`  
  
 `using (f)`  
  
 `{`  
  
 `f = new thisType();`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="3efbd-107">Bu durumda, `thisType` `using` bloğun yürütmesi bittiğinde değişkenin null gibi özgün değeri atılır, ancak blok `thisType` içinde oluşturulan nesne, sonunda çöp toplanacak olsa da olmayacaktır.</span><span class="sxs-lookup"><span data-stu-id="3efbd-107">In this case, the original value, such as null, of the variable `thisType` will be disposed of when the `using` block finishes executing, but the `thisType` object created inside the block will not be, although it will eventually get garbage collected.</span></span>  
  
 <span data-ttu-id="3efbd-108">Bu hatayı gidermek için aşağıdaki formu kullanın:</span><span class="sxs-lookup"><span data-stu-id="3efbd-108">To resolve this error, use the following form:</span></span>  
  
 `using (thisType f = new thisType())`  
  
 `{`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="3efbd-109">Bu durumda, yeni ayrılan `thisType` nesne imha edilecektir.</span><span class="sxs-lookup"><span data-stu-id="3efbd-109">In this case, the newly allocated `thisType` object will be disposed of.</span></span>  
  
## <a name="example"></a><span data-ttu-id="3efbd-110">Örnek</span><span class="sxs-lookup"><span data-stu-id="3efbd-110">Example</span></span>  
 <span data-ttu-id="3efbd-111">Aşağıdaki kod uyarı CS0728 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="3efbd-111">The following code will generate warning CS0728.</span></span>  
  
```csharp
// CS0728.cs  
  
using System;  
public class ValidBase : IDisposable  
{  
    public void Dispose() {  }  
}  
  
public class Logger  
{  
    public static void dummy()  
    {  
        ValidBase vb = null;  
        using (vb)
        {  
            vb = null;  // CS0728  
        }  
        vb = null;  
    }  
    public static void Main() { }  
}  
```
