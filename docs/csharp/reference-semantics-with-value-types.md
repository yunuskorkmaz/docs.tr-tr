---
title: Başvuru semantiği ile değer türleri
description: Kopyalama yapıları güvenle en aza dil özellikleri anlama
author: billwagner
ms.author: wiwagn
ms.date: 11/10/2017
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.custom: mvc
ms.openlocfilehash: 778897dc92f8a94178ebbbed7704c0dfe2397729
ms.sourcegitcommit: 935d5267c44f9bce801468ef95f44572f1417e8c
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/28/2018
---
# <a name="reference-semantics-with-value-types"></a><span data-ttu-id="7427a-103">Başvuru semantiği ile değer türleri</span><span class="sxs-lookup"><span data-stu-id="7427a-103">Reference semantics with value types</span></span>

<span data-ttu-id="7427a-104">Değer türleri kullanmanın bir avantajı, bunlar genellikle yığın ayırmaları kaçının ' dir.</span><span class="sxs-lookup"><span data-stu-id="7427a-104">An advantage to using value types is that they often avoid heap allocations.</span></span>
<span data-ttu-id="7427a-105">Olumsuz değeriyle kopyalanırlar ' dir.</span><span class="sxs-lookup"><span data-stu-id="7427a-105">The disadvantage is that they are copied by value.</span></span> <span data-ttu-id="7427a-106">Bu kolaylığını büyük miktarlarda verinin çalışması algoritmaları iyileştirmek daha zor hale getirir.</span><span class="sxs-lookup"><span data-stu-id="7427a-106">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="7427a-107">C# 7.2 içindeki yeni dil özellikleri pass-by-reference semantiği değer türleri ile etkinleştirmek mekanizmaları sağlar.</span><span class="sxs-lookup"><span data-stu-id="7427a-107">New language features in C# 7.2 provide mechanisms that enable pass-by-reference semantics with value types.</span></span> <span data-ttu-id="7427a-108">Bu özellikler akıllıca hem ayırmaları en aza indirmek ve işlemleri kopyalamak için kullanın.</span><span class="sxs-lookup"><span data-stu-id="7427a-108">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="7427a-109">Bu makalede, bu yeni özellikleri açıklar.</span><span class="sxs-lookup"><span data-stu-id="7427a-109">This article explores those new features.</span></span>

<span data-ttu-id="7427a-110">Bu makaledeki örnek kod çoğunu C# 7.2 eklenen özellikler gösterir.</span><span class="sxs-lookup"><span data-stu-id="7427a-110">Much of the sample code in this article demonstrates features added in C# 7.2.</span></span> <span data-ttu-id="7427a-111">Bu özellikleri kullanmak için projenizin C# 7,2 veya sonraki sürümünü kullanacak şekilde yapılandırmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="7427a-111">In order to use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="7427a-112">Visual Studio seçmek için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-112">You can use Visual Studio to select it.</span></span> <span data-ttu-id="7427a-113">Her proje için seçin **proje** menüsünde, ardından **özellikleri**.</span><span class="sxs-lookup"><span data-stu-id="7427a-113">For each project, select **Project** from the menu, then **Properties**.</span></span> <span data-ttu-id="7427a-114">Seçin **yapı** sekmesinde **Gelişmiş**.</span><span class="sxs-lookup"><span data-stu-id="7427a-114">Select the **Build** tab and click **Advanced**.</span></span> <span data-ttu-id="7427a-115">Buradan, dil sürümü yapılandırın.</span><span class="sxs-lookup"><span data-stu-id="7427a-115">From there, configure the language version.</span></span> <span data-ttu-id="7427a-116">"7.2" veya "en son" seçin.</span><span class="sxs-lookup"><span data-stu-id="7427a-116">Choose either "7.2", or "latest".</span></span>  <span data-ttu-id="7427a-117">Veya düzenleyebilirsiniz *csproj* dosya ve aşağıdaki düğüm ekleyin:</span><span class="sxs-lookup"><span data-stu-id="7427a-117">Or you can edit the *csproj* file and add the following node:</span></span>

```XML
  <PropertyGroup>
    <LangVersion>7.2</LangVersion>
  </PropertyGroup>
```

<span data-ttu-id="7427a-118">"7,2" veya "en son" değerini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-118">You can use either "7.2" or "latest" for the value.</span></span>

## <a name="passing-arguments-by-readonly-reference"></a><span data-ttu-id="7427a-119">Salt okunur başvuruya göre bağımsız değişkenleri geçirme</span><span class="sxs-lookup"><span data-stu-id="7427a-119">Passing arguments by readonly reference</span></span>

<span data-ttu-id="7427a-120">C# 7.2 ekler `in` varolan tamamlamak üzere anahtar sözcük `ref` ve `out` başvuruya göre bağımsız değişken geçirmek için anahtar sözcükler.</span><span class="sxs-lookup"><span data-stu-id="7427a-120">C# 7.2 adds the `in` keyword to complement the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="7427a-121">`in` Anahtar sözcüğü belirtir başvuruya göre bağımsız değişken geçirme, ancak çağrılan yöntemin değeri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="7427a-121">The `in` keyword specifies passing the argument by reference, but the called method does not modify the value.</span></span> 

<span data-ttu-id="7427a-122">Bu ek tasarım hedefi ifade etmek için tam bir sözlüğünü sağlar.</span><span class="sxs-lookup"><span data-stu-id="7427a-122">This addition provides a full vocabulary to express your design intent.</span></span> <span data-ttu-id="7427a-123">Değer türleri yöntemi imzada aşağıdaki değiştiricileri belirtmeyin olduğunda çağrılan yönteme geçirildiğinde kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="7427a-123">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="7427a-124">Bu değiştiricileri her bir değer türü başvurusu tarafından kopya önleme geçirilen belirtir.</span><span class="sxs-lookup"><span data-stu-id="7427a-124">Each of these modifiers specifies that a value type is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="7427a-125">Her değiştiricisi farklı bir hedefi ifade eder:</span><span class="sxs-lookup"><span data-stu-id="7427a-125">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="7427a-126">`out`: Bu yöntem, bu parametre olarak kullanılan bağımsız değişkeninin değerini ayarlar.</span><span class="sxs-lookup"><span data-stu-id="7427a-126">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="7427a-127">`ref`: Bu yöntem, bu parametre olarak kullanılan bağımsız değişkeninin değeri ayarlayabilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-127">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="7427a-128">`in`: Bu yöntem, bu parametre olarak kullanılan bağımsız değişkeninin değeri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="7427a-128">`in`: This method does not modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="7427a-129">Ekleme `in` başvuruya göre bağımsız değişken geçirmek ve gereksiz kopyalama önlemek için başvuruya göre bağımsız değişken geçirmek için tasarım hedefi bildirmek üzere değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-129">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="7427a-130">Bu bağımsız değişken olarak kullanılan nesne değiştirmek istiyorsanız değil.</span><span class="sxs-lookup"><span data-stu-id="7427a-130">You do not intend to modify the object used as that argument.</span></span> <span data-ttu-id="7427a-131">Aşağıdaki kod örneği 3B uzaydaki iki nokta arasındaki uzaklığı hesaplar bir yöntemin gösterir.</span><span class="sxs-lookup"><span data-stu-id="7427a-131">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span> 

[!code-csharp[InArgument](../../samples/csharp/reference-semantics/Program.cs#InArgument "Specifying an In argument")]

<span data-ttu-id="7427a-132">Her üç çiftleri içeren iki yapıları bağımsız değişkenler.</span><span class="sxs-lookup"><span data-stu-id="7427a-132">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="7427a-133">Bir çift 8 bayt olduğundan, her bir bağımsız değişkeni 24 bayttır.</span><span class="sxs-lookup"><span data-stu-id="7427a-133">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="7427a-134">Belirterek `in` değiştiricisi, geçirdiğiniz bu bağımsız değişkenlerden 4-bayt veya 8-bayt başvuru makine mimarisi bağlı olarak.</span><span class="sxs-lookup"><span data-stu-id="7427a-134">By specifying the `in` modifier, you pass a 4-byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="7427a-135">Boyutu fark küçüktür, ancak uygulamanız bu yöntem birçok farklı değerleri kullanarak sıkı bir döngüde çağırdığında, hızlı bir şekilde ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-135">The difference in size is small, but it can quickly add up when your application calls this method in a tight loop using many different values.</span></span>
 
<span data-ttu-id="7427a-136">`in` Değiştiricisi hazırlandı `out` ve `ref` başka yöntemler de.</span><span class="sxs-lookup"><span data-stu-id="7427a-136">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="7427a-137">Yalnızca içinde varken, farklı bir yöntem aşırı oluşturulamıyor `in`, `out`, veya `ref`.</span><span class="sxs-lookup"><span data-stu-id="7427a-137">You cannot create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="7427a-138">Bu yeni kurallar her zaman için tanımlanmış aynı davranışı genişletmek `out` ve `ref` parametreleri.</span><span class="sxs-lookup"><span data-stu-id="7427a-138">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span>

<span data-ttu-id="7427a-139">`in` Değiştiricisi parametreler isteyen herhangi bir üyesi için uygulanan: yöntemler, temsilciler, Lambda'lar, yerel İşlevler, dizin oluşturucular, işleçler.</span><span class="sxs-lookup"><span data-stu-id="7427a-139">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="7427a-140">Farklı `ref` ve `out` bağımsız değişken kullandığınız değişmez değerler veya sabitleri bağımsız değişkeni için bir `in` parametresi.</span><span class="sxs-lookup"><span data-stu-id="7427a-140">Unlike `ref` and `out` arguments, you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="7427a-141">Ayrıca, farklı bir `ref` veya `out` parametre, geçerli gerekmeyen `in` çağrısı sitede değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-141">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="7427a-142">Aşağıdaki kod iki örnek çağırma gösterir `CalculateDistance` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="7427a-142">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="7427a-143">İlk iki yerel değişkenleri başvuruya göre geçirilen kullanır.</span><span class="sxs-lookup"><span data-stu-id="7427a-143">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="7427a-144">İkinci yöntem çağrısının bir parçası olarak oluşturulan geçici bir değişken içerir.</span><span class="sxs-lookup"><span data-stu-id="7427a-144">The second includes a temporary variable created as part of the method call.</span></span> 

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="7427a-145">İçinde derleyici sağlar, salt okunur yapısına birkaç yolu vardır bir `in` bağımsız değişkeni zorlanır.</span><span class="sxs-lookup"><span data-stu-id="7427a-145">There are several ways in which the compiler ensures that the read-only nature of an `in` argument is enforced.</span></span>  <span data-ttu-id="7427a-146">Öncelikle, çağrılan yöntemin doğrudan atayamazsınız bir `in` parametresi.</span><span class="sxs-lookup"><span data-stu-id="7427a-146">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="7427a-147">Herhangi bir alan için doğrudan atayamazsınız bir `in` bu değer olduğunda parametresi bir `struct` türü.</span><span class="sxs-lookup"><span data-stu-id="7427a-147">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="7427a-148">Ayrıca, geçiremezsiniz bir `in` kullanarak herhangi bir yöntem için parametre `ref` veya `out` değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-148">In addition, you cannot pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="7427a-149">Bu kurallar herhangi bir alan için geçerli bir `in` parametre, sağlanan alan bir `struct` türü ve parametre değil de bir `struct` türü.</span><span class="sxs-lookup"><span data-stu-id="7427a-149">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="7427a-150">Üye erişimi tüm düzeylerinde türleri çözümsüz üye erişimi sağlanan bu kurallar aslında, uygulanmasını `structs`.</span><span class="sxs-lookup"><span data-stu-id="7427a-150">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span> <span data-ttu-id="7427a-151">Derleyici zorlar `struct` türleri geçirilen olarak `in` bağımsız değişkenleri ve bunların `struct` diğer yöntemleri bağımsız değişken olarak kullanıldığında salt okunur değişkenler üyeleridir.</span><span class="sxs-lookup"><span data-stu-id="7427a-151">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="7427a-152">Kullanımını `in` parametreleri kopyaların, olası performans maliyetlerini ortadan kaldırır.</span><span class="sxs-lookup"><span data-stu-id="7427a-152">The use of `in` parameters avoids the potential performance costs of making copies.</span></span> <span data-ttu-id="7427a-153">Herhangi bir yöntem çağrısı semantiği değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="7427a-153">It does not change the semantics of any method call.</span></span> <span data-ttu-id="7427a-154">Bu nedenle, belirtmeniz gerekmez `in` çağrısı sitede değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-154">Therefore, you do not need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="7427a-155">Ancak, atlama `in` çağrısı sitede değiştirici bağımsız değişkeni aşağıdaki nedenlerden dolayı bir kopyasını oluşturmak için izin verildiğini derleyici bildirir:</span><span class="sxs-lookup"><span data-stu-id="7427a-155">However, omitting the `in` modifier at the call site informs the compiler that it is allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="7427a-156">Örtük bir dönüştürme ancak olmayan bir kimlik dönüştürme bağımsız değişken türü için parametre türü yok.</span><span class="sxs-lookup"><span data-stu-id="7427a-156">There is an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="7427a-157">Bağımsız değişkeni bir ifade ancak bilinen depolama değişkeni yok.</span><span class="sxs-lookup"><span data-stu-id="7427a-157">The argument is an expression but does not have a known storage variable.</span></span>
- <span data-ttu-id="7427a-158">Varlığı veya yokluğuna göre tarafından farklı bir aşırı var. `in`.</span><span class="sxs-lookup"><span data-stu-id="7427a-158">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="7427a-159">Bu durumda, değeriyle aşırı daha iyi bir eşleşmedir.</span><span class="sxs-lookup"><span data-stu-id="7427a-159">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="7427a-160">Bu kurallar, salt okunur başvuru değişkenlerini kullanmak için var olan kodu güncelleştirme gibi faydalıdır.</span><span class="sxs-lookup"><span data-stu-id="7427a-160">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="7427a-161">Çağrılan yöntemin içinde değeri parametreyle kullanan herhangi bir örnek yöntemi çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-161">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="7427a-162">Bu durumlarda, bir kopyasını `in` parametresi oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="7427a-162">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="7427a-163">Derleyici herhangi için geçici bir değişken oluşturmak için `in` parametresi, ayrıca varsayılan değerleri için belirtebilirsiniz `in` parametresi.</span><span class="sxs-lookup"><span data-stu-id="7427a-163">Because the compiler can create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="7427a-164">Aşağıdaki kod, ikinci noktası için varsayılan değer olarak (noktası 0,0) kaynak belirtir:</span><span class="sxs-lookup"><span data-stu-id="7427a-164">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/reference-semantics/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="7427a-165">Okuma yalnızca bağımsız değişkenleri başvuruya göre geçirmek için derleyici zorlamak için belirtmek `in` modifer değişkenlerine çağrısı sitede aşağıdaki kodda gösterildiği gibi:</span><span class="sxs-lookup"><span data-stu-id="7427a-165">To force the compiler to pass read only arguments by reference, specify the `in` modifer on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="7427a-166">Bu davranış benimsemeyi kolaylaştırır `in` büyük zamanla parametreleri olarak kullanılabilecek kod temeli performans artışı mümkün olduğu.</span><span class="sxs-lookup"><span data-stu-id="7427a-166">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="7427a-167">Eklediğiniz `in` değiştirici yöntem imzaları için ilk.</span><span class="sxs-lookup"><span data-stu-id="7427a-167">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="7427a-168">Daha sonra ekleyebilirsiniz `in` callsites adresindeki değiştiricisi ve oluşturma `readonly struct` savunma kopyalarını oluşturmamak derleyici etkinleştirmek için türleri `in` daha fazla konum parametrelerinde.</span><span class="sxs-lookup"><span data-stu-id="7427a-168">Then, you can add the `in` modifier at callsites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="7427a-169">`in` Parametre ataması da başvuru türleri veya sayısal değerler ile kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-169">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="7427a-170">Bununla birlikte, her iki durumda da avantajları en az, varsa.</span><span class="sxs-lookup"><span data-stu-id="7427a-170">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="ref-readonly-returns"></a><span data-ttu-id="7427a-171">`ref readonly` döndürür</span><span class="sxs-lookup"><span data-stu-id="7427a-171">`ref readonly` returns</span></span>

<span data-ttu-id="7427a-172">Başvuruya göre dönüş değeri türü, ancak bu değeri değiştirmeden gelen arayan engellemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-172">You may also want to return a value type by reference, but disallow the caller from modifying that value.</span></span> <span data-ttu-id="7427a-173">Kullanım `ref readonly` bu tasarım hedefi express değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-173">Use the `ref readonly` modifier to express that design intent.</span></span> <span data-ttu-id="7427a-174">Okuyucular, var olan verilere bir başvuru döndürüyor, ancak değişiklik sağlanmıyor olduğunu bildirir.</span><span class="sxs-lookup"><span data-stu-id="7427a-174">It notifies readers that you are returning a reference to existing data, but not allowing modification.</span></span> 

<span data-ttu-id="7427a-175">Derleyici çağıran başvurusu değiştirilemez zorlar.</span><span class="sxs-lookup"><span data-stu-id="7427a-175">The compiler enforces that the caller cannot modify the reference.</span></span> <span data-ttu-id="7427a-176">Değeri doğrudan atamanız girişimleri bir derleme zamanı hatası oluşturur.</span><span class="sxs-lookup"><span data-stu-id="7427a-176">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="7427a-177">Ancak, derleyici herhangi bir üye yöntemini yapısı durumunu değiştirir olmadığını bilemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-177">However, the compiler cannot know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="7427a-178">Nesne değiştirilmeyen emin olmak için derleyici bir kopyasını oluşturur ve bu kopyayı kullanarak başvurular üye çağırır.</span><span class="sxs-lookup"><span data-stu-id="7427a-178">To ensure that the object is not modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="7427a-179">Herhangi bir değişiklik savunma bu kopyaya ' dir.</span><span class="sxs-lookup"><span data-stu-id="7427a-179">Any modifications are to that defensive copy.</span></span> 

<span data-ttu-id="7427a-180">Büyük olasılıkla, kitaplığını kullanarak `Point3D` genellikle kaynak kod genelindeki kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="7427a-180">It's likely that the library using `Point3D` would often use the origin throughout the code.</span></span> <span data-ttu-id="7427a-181">Her örneği yığında yeni bir nesne oluşturur.</span><span class="sxs-lookup"><span data-stu-id="7427a-181">Every instance creates a new object on the stack.</span></span> <span data-ttu-id="7427a-182">Bir sabit oluşturup başvuruya göre dönmek için yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-182">It may be advantageous to create a constant and return it by reference.</span></span> <span data-ttu-id="7427a-183">Ancak, iç depolama başvuru döndürürse, çağıran başvurulan depolama değiştirilemiyor zorlamak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-183">But, if you return a reference to internal storage, you may want to enforce that the caller cannot modify the referenced storage.</span></span> <span data-ttu-id="7427a-184">Aşağıdaki kod döndüren bir salt okunur özelliği tanımlayan bir `readonly ref` için bir `Point3D` kaynağını belirtir.</span><span class="sxs-lookup"><span data-stu-id="7427a-184">The following code defines a read-only property that returns a `readonly ref` to a `Point3D` that specifies the origin.</span></span>

[!code-csharp[OriginReference](../../samples/csharp/reference-semantics/Point3D.cs#OriginReference "Creating a readonly Origin reference")]

<span data-ttu-id="7427a-185">Ref salt okunur bir kopyasını dönüş oluşturmak kolaydır: yalnızca ile bildirilmemiş bir değişkene atayın `ref readonly` değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-185">Creating a copy of a ref readonly return is easy: Just assign it to a variable not declared with the `ref readonly` modifier.</span></span> <span data-ttu-id="7427a-186">Derleyici nesneyi atama bir parçası olarak kopyalamak için kod oluşturur.</span><span class="sxs-lookup"><span data-stu-id="7427a-186">The compiler generates code to copy the object as part of the assignment.</span></span> 

<span data-ttu-id="7427a-187">Bir değişkene atadığınızda bir `ref readonly return`, ya da belirtebilirsiniz bir `ref readonly` değişkeni veya salt okunur başvuru değeri tarafından kopyasını:</span><span class="sxs-lookup"><span data-stu-id="7427a-187">When you assign a variable to a `ref readonly return`, you can specify either a `ref readonly` variable, or a by-value copy of the read-only reference:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/reference-semantics/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="7427a-188">İlk atama önceki kodda bir kopyasını oluşturur `Origin` sabiti ve kopyalama atar.</span><span class="sxs-lookup"><span data-stu-id="7427a-188">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="7427a-189">İkinci bir başvuru atar.</span><span class="sxs-lookup"><span data-stu-id="7427a-189">The second assigns a reference.</span></span> <span data-ttu-id="7427a-190">Dikkat `readonly` değiştiricisi değişken bildirimi parçası olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="7427a-190">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="7427a-191">Başvurduğu başvurusu değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="7427a-191">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="7427a-192">Bunu yapmak için deneme, bir derleme zamanı hatasına neden.</span><span class="sxs-lookup"><span data-stu-id="7427a-192">Attempts to do so result in a compile-time error.</span></span>

## <a name="readonly-struct-type"></a><span data-ttu-id="7427a-193">`readonly struct` Türü</span><span class="sxs-lookup"><span data-stu-id="7427a-193">`readonly struct` type</span></span>

<span data-ttu-id="7427a-194">Uygulama `ref readonly` yapı yüksek trafik kullanımlar için yeterli olabilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-194">Applying `ref readonly` to high-traffic uses of a struct may be sufficient.</span></span>
<span data-ttu-id="7427a-195">Diğer durumlarda, sabit bir yapı oluşturmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-195">Other times, you may want to create an immutable struct.</span></span> <span data-ttu-id="7427a-196">Ardından salt okunur başvuru geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-196">Then you can always pass by read-only reference.</span></span> <span data-ttu-id="7427a-197">Alıştırma savunma kaldırır meydana yöntemleri olarak kullanılan bir yapı eriştiğinizde kopyalar bir `in` parametresi.</span><span class="sxs-lookup"><span data-stu-id="7427a-197">That practice removes the defensive copies that take place when you access methods of a struct used as an `in` parameter.</span></span>

<span data-ttu-id="7427a-198">Bunu oluşturarak yapabilirsiniz bir `readonly struct` türü.</span><span class="sxs-lookup"><span data-stu-id="7427a-198">You can do that by creating a `readonly struct` type.</span></span> <span data-ttu-id="7427a-199">Ekleyebileceğiniz `readonly` yapısı bildirimine değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="7427a-199">You can add the `readonly` modifier to a struct declaration.</span></span> <span data-ttu-id="7427a-200">Tüm örnek yapı üyesi olmalarını derleyici zorlar `readonly`; `struct` sabit olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="7427a-200">The compiler enforces that all instance members of the struct are `readonly`; the `struct` must be immutable.</span></span>

<span data-ttu-id="7427a-201">Diğer en iyi duruma getirme vardır bir `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="7427a-201">There are other optimizations for a `readonly struct`.</span></span> <span data-ttu-id="7427a-202">Kullanabileceğiniz `in` her konumda değiştiricisi burada bir `readonly struct` bir bağımsız değişken.</span><span class="sxs-lookup"><span data-stu-id="7427a-202">You can use the `in` modifier at every location where a `readonly struct` is an argument.</span></span> <span data-ttu-id="7427a-203">Ayrıca, döndürebilir bir `readonly struct` olarak bir `ref return` zaman iade nesneyi, yaşam süresi genişletir nesnesi döndüren yöntemi kapsamı dışındadır.</span><span class="sxs-lookup"><span data-stu-id="7427a-203">In addition, you can return a `readonly struct` as a `ref return` when you are returning an object whose lifetime extends beyond the scope of the method returning the object.</span></span>

<span data-ttu-id="7427a-204">Son olarak, üyeleri çağırdığınızda derleyici daha verimli kodunu oluşturur bir `readonly struct`: `this` alıcı kopyasını yerine başvurusunun olduğundan her zaman bir `in` parametresine geçirilen üye yöntemi başvuru.</span><span class="sxs-lookup"><span data-stu-id="7427a-204">Finally, the compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="7427a-205">Bu iyileştirme kullanırken daha fazla kopyalama kaydeder bir `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="7427a-205">This optimization saves more copying when you use a `readonly struct`.</span></span> <span data-ttu-id="7427a-206">`Point3D` Bu değişikliği mükemmel bir adaydır.</span><span class="sxs-lookup"><span data-stu-id="7427a-206">The `Point3D` is a great candidate for this change.</span></span> <span data-ttu-id="7427a-207">Aşağıdaki kod güncelleştirilmiş gösterir `ReadonlyPoint3D` yapısı:</span><span class="sxs-lookup"><span data-stu-id="7427a-207">The following code shows an updated `ReadonlyPoint3D` structure:</span></span>

[!code-csharp[ReadonlyOnlyPoint3D](../../samples/csharp/reference-semantics/Point3D.cs#ReadonlyOnlyPoint3D "Defining an immutable structure")]

## <a name="ref-struct-type"></a><span data-ttu-id="7427a-208">`ref struct` Türü</span><span class="sxs-lookup"><span data-stu-id="7427a-208">`ref struct` type</span></span>

<span data-ttu-id="7427a-209">Başka bir ilgili dil yığını ayrılmış olması gereken değer türünü bildirmesine olanağı özelliğidir.</span><span class="sxs-lookup"><span data-stu-id="7427a-209">Another related language feature is the ability to declare a value type that must be stack allocated.</span></span> <span data-ttu-id="7427a-210">Diğer bir deyişle, bu tür hiçbir zaman öbek üzerinde başka bir sınıf üyesi olarak oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-210">In other words, these types can never be created on the heap as a member of another class.</span></span> <span data-ttu-id="7427a-211">Bu özellik için birincil motivasyon oluştu <xref:System.Span%601> ve ilgili yapıları.</span><span class="sxs-lookup"><span data-stu-id="7427a-211">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="7427a-212"><xref:System.Span%601> yönetilen bir işaretçi bir grubun üyeleri ve diğeri aralık uzunluğu olan içerebilir.</span><span class="sxs-lookup"><span data-stu-id="7427a-212"><xref:System.Span%601> may contain a managed pointer as one of its members, the other being the length of the span.</span></span> <span data-ttu-id="7427a-213">C# güvenli olmayan bir bağlam dışında yönetilen bellek işaretçiler desteklemediği için biraz farklı uygulanır.</span><span class="sxs-lookup"><span data-stu-id="7427a-213">It's implemented a bit differently because C# doesn't support pointers to managed memory outside of an unsafe context.</span></span> <span data-ttu-id="7427a-214">İşaretçinin ve uzunluk değişiklikleri yazma atomik değil.</span><span class="sxs-lookup"><span data-stu-id="7427a-214">Any write that changes the pointer and the length is not atomic.</span></span> <span data-ttu-id="7427a-215">Yani bir <xref:System.Span%601> aralığı hataları dışında tabi olacaktır veya diğer tür güvenlik ihlallerini onu değil kısıtlı bir tek yığın çerçevesi olan.</span><span class="sxs-lookup"><span data-stu-id="7427a-215">That means a <xref:System.Span%601> would be subject to out of range errors or other type safety violations were it not constrained to a single stack frame.</span></span> <span data-ttu-id="7427a-216">Ayrıca, yönetilen bir işaretçi GC yığınında genellikle koyma JIT zamanında çöküyor.</span><span class="sxs-lookup"><span data-stu-id="7427a-216">In addition, putting a managed pointer on the GC heap typically crashes at JIT time.</span></span>

<span data-ttu-id="7427a-217">Kullanılarak oluşturulan bellekle çalışma benzer gereksinimleri olabilir [ `stackalloc` ](language-reference/keywords/stackalloc.md) veya birlikte çalışma API'leri bellekten kullanılırken.</span><span class="sxs-lookup"><span data-stu-id="7427a-217">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="7427a-218">Kendi tanımlayabilirsiniz `ref struct` türleri bu ihtiyaçları için.</span><span class="sxs-lookup"><span data-stu-id="7427a-218">You can define your own `ref struct` types for those needs.</span></span> <span data-ttu-id="7427a-219">Bu makalede, gördüğünüz kullanan örnekler `Span<T>` basitleştirmek için.</span><span class="sxs-lookup"><span data-stu-id="7427a-219">In this article, you see examples using `Span<T>` for simplicity.</span></span>

<span data-ttu-id="7427a-220">`ref struct` Bildirimi bildirir bu tür bir yapı yığında olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="7427a-220">The `ref struct` declaration declares a struct of this type must be on the stack.</span></span> <span data-ttu-id="7427a-221">Dil kuralları bu türlerinin güvenli kullanımını emin olun.</span><span class="sxs-lookup"><span data-stu-id="7427a-221">The language rules ensure the safe use of these types.</span></span> <span data-ttu-id="7427a-222">Diğer türleri olarak bildirilen `ref struct` dahil <xref:System.ReadOnlySpan%601>.</span><span class="sxs-lookup"><span data-stu-id="7427a-222">Other types declared as `ref struct` include <xref:System.ReadOnlySpan%601>.</span></span> 

<span data-ttu-id="7427a-223">Tutma amacı bir `ref struct` yazın yığın ayırma değişkeni derleyici tüm zorlar çeşitli kurallar tanıtır gibi `ref struct` türleri.</span><span class="sxs-lookup"><span data-stu-id="7427a-223">The goal of keeping a `ref struct` type as a stack-allocated variable introduces several rules that the compiler enforces for all `ref struct` types.</span></span>

- <span data-ttu-id="7427a-224">Kutu olamaz bir `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="7427a-224">You can't box a `ref struct`.</span></span> <span data-ttu-id="7427a-225">Nesnesine atanamaz bir `ref struct` türünde bir değişkene `object`, `dynamic`, veya herhangi bir arabirim türü.</span><span class="sxs-lookup"><span data-stu-id="7427a-225">You cannot assign a `ref struct` type to a variable of type `object`, `dynamic`, or any interface type.</span></span>
- <span data-ttu-id="7427a-226">Bildiremezsiniz bir `ref struct` bir sınıf veya normal yapı üyesi olarak.</span><span class="sxs-lookup"><span data-stu-id="7427a-226">You can't declare a `ref struct` as a member of a class or a normal struct.</span></span>
- <span data-ttu-id="7427a-227">Yerel değişkenler bildiremezsiniz `ref struct` zaman uyumsuz yöntemleri türlerinde.</span><span class="sxs-lookup"><span data-stu-id="7427a-227">You cannot declare local variables that are `ref struct` types in async methods.</span></span> <span data-ttu-id="7427a-228">Döndüren zaman uyumlu yöntemleri bildirebilir `Task`, `Task<T>` veya görev-like türleri.</span><span class="sxs-lookup"><span data-stu-id="7427a-228">You can declare them in synchronous methods that return `Task`, `Task<T>` or Task-like types.</span></span>
- <span data-ttu-id="7427a-229">Bildiremezsiniz `ref struct` yineleyiciler yerel değişkenler.</span><span class="sxs-lookup"><span data-stu-id="7427a-229">You cannot declare `ref struct` local variables in iterators.</span></span>
- <span data-ttu-id="7427a-230">Yakalama yapılamaz `ref struct` lambda ifadeleri veya yerel işlevler değişkenlerinin.</span><span class="sxs-lookup"><span data-stu-id="7427a-230">You cannot capture `ref struct` variables in lambda expressions or local functions.</span></span>

<span data-ttu-id="7427a-231">Yanlışlıkla kullandığınızda bu kısıtlamaları sağlamak bir `ref struct` Yönetilen yığın Yükselt bir şekilde.</span><span class="sxs-lookup"><span data-stu-id="7427a-231">These restrictions ensure you do not accidentally use a `ref struct` in a manner that could promote it to the managed heap.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="7427a-232">`readonly ref struct` Türü</span><span class="sxs-lookup"><span data-stu-id="7427a-232">`readonly ref struct` type</span></span>

<span data-ttu-id="7427a-233">Yapı olarak bildirme `readonly ref` yararları ve kısıtlamaları birleştirir `ref struct` ve `readonly struct` delcarations.</span><span class="sxs-lookup"><span data-stu-id="7427a-233">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` delcarations.</span></span> 

<span data-ttu-id="7427a-234">Aşağıdaki örnek, bildirimi gösterir `readonly ref struct`.</span><span class="sxs-lookup"><span data-stu-id="7427a-234">The following example demonstrates the declaration of `readonly ref struct`.</span></span>

```csharp
readonly ref struct ReadOnlyRefPoint2D
{
    public int X { get; }
    public int Y { get; }
    
    public ReadOnlyRefPoint2D(int x, int y) => (X, Y) = (x, y);
}
```

## <a name="conclusions"></a><span data-ttu-id="7427a-235">Sonuçları</span><span class="sxs-lookup"><span data-stu-id="7427a-235">Conclusions</span></span>

<span data-ttu-id="7427a-236">C# dili Bu geliştirmeler, burada bellek ayırmaları gerekli performansını elde için kritik olabilir performans kritik algoritmaları için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="7427a-236">These enhancements to the C# language are designed for performance critical algorithms where memory allocations can be critical to achieving the necessary performance.</span></span> <span data-ttu-id="7427a-237">Yazdığınız kodu bu özellikleri sık kullanmadığınız bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7427a-237">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="7427a-238">Ancak, bu geliştirmeler, .NET Framework'teki birçok konumdaki uyarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="7427a-238">However, these enhancements have been adopted in many locations in the .NET Framework.</span></span> <span data-ttu-id="7427a-239">Daha da fazla API'leri yaparken bu özellikleri kullanan, kendi uygulamaların performansını artırmak görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="7427a-239">As more and more APIs make use of these features, you'll see the performance of your own applications improve.</span></span>
