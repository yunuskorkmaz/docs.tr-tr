---
title: Derleyici Hatası CS0413
ms.date: 07/20/2015
f1_keywords:
- CS0413
helpviewer_keywords:
- CS0413
ms.assetid: a01bd1ec-015b-433b-be55-b91db268d6a5
ms.openlocfilehash: 4e4e291ecba7c98ddfeaa6806aef910a1a89b270
ms.sourcegitcommit: 29a9b29d8b7d07b9c59d46628da754a8bff57fa4
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/17/2019
ms.locfileid: "69566266"
---
# <a name="compiler-error-cs0413"></a><span data-ttu-id="6a038-102">Derleyici Hatası CS0413</span><span class="sxs-lookup"><span data-stu-id="6a038-102">Compiler Error CS0413</span></span>
<span data-ttu-id="6a038-103">' Type Parameter ' tür parametresi, bir sınıf türü kısıtlaması veya bir ' class ' kısıtlaması olmadığından ' As ' işleciyle kullanılamaz</span><span class="sxs-lookup"><span data-stu-id="6a038-103">The type parameter 'type parameter' cannot be used with the 'as' operator because it does not have a class type constraint nor a 'class' constraint</span></span>  
  
<span data-ttu-id="6a038-104">Genel bir tür [as](../operators/type-testing-and-cast.md#as-operator) işlecini kullanıyorsa, ancak bu genel türün bir sınıf türü kısıtlaması yoksa, bu hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="6a038-104">This error occurs if a generic type uses the [as](../operators/type-testing-and-cast.md#as-operator) operator, but that generic type does not have a class type constraint.</span></span> <span data-ttu-id="6a038-105">`as` İşlecine yalnızca başvuru ve null atanabilir değer türleriyle izin verilir, bu nedenle tür parametresinin değer türü olmadığından emin olmak için kısıtlamalı olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="6a038-105">The `as` operator is only allowed with reference and nullable value types, so the type parameter must be constrained to guarantee that it is not a value type.</span></span> <span data-ttu-id="6a038-106">Bu hatayı önlemek için bir sınıf türü kısıtlaması veya bir başvuru türü kısıtlaması kullanın.</span><span class="sxs-lookup"><span data-stu-id="6a038-106">To avoid this error, use a class type constraint or a reference type constraint.</span></span>
  
<span data-ttu-id="6a038-107">Bunun nedeni `as` , işlecin `null`bir değer türü için olası bir değer olmaması ve tür parametresinin bir sınıf türü kısıtlaması veya bir başvuru türü kısıtlaması olmadığı sürece değer türü olarak değerlendirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="6a038-107">This is because the `as` operator could return `null`, which is not a possible value for a value type, and the type parameter must be treated as a value type unless it is a class type constraint or a reference type constraint.</span></span>
  
## <a name="example"></a><span data-ttu-id="6a038-108">Örnek</span><span class="sxs-lookup"><span data-stu-id="6a038-108">Example</span></span>

<span data-ttu-id="6a038-109">Aşağıdaki örnek CS0413 oluşturur.</span><span class="sxs-lookup"><span data-stu-id="6a038-109">The following sample generates CS0413.</span></span>  
  
```csharp  
// CS0413.cs  
// compile with: /target:library  
class A {}  
class B : A {}  
  
class CMain  
{  
   A a = null;  
   public void G<T>()  
   {  
      a = new A();  
      System.Console.WriteLine (a as T);  // CS0413  
   }  
  
   // OK  
   public void H<T>() where T : A  
   {  
      a = new A();  
      System.Console.WriteLine (a as T);  
   }  
}  
```
