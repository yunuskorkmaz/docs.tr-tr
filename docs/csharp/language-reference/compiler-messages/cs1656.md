---
title: Derleyici Hatası CS1656
ms.date: 07/20/2015
f1_keywords:
- CS1656
helpviewer_keywords:
- CS1656
ms.assetid: b5463a12-d685-4dae-9f88-08383e271b7a
ms.openlocfilehash: 272bfe245e707129fe96e7515dbddaf8f7a81eaf
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/14/2020
ms.locfileid: "79173867"
---
# <a name="compiler-error-cs1656"></a>Derleyici Hatası CS1656
'Salt okunur değişken türü' olduğu için 'değişkene' atayamaz  
  
 Bu hata, bir atama için bir atama salt okunur bağlamında oluştuğunda oluşur. Salt okunur bağlamlar, değişkenleri [kullanarak](../keywords/using-statement.md) [her yineleme](../keywords/foreach-in.md) değişkenini ve [sabit](../keywords/fixed-statement.md) değişkenleri içerir. Bu hatayı gidermek için, bloklar, `using` `foreach` deyimler ve `fixed` deyimler içinde bir deyim değişkenine atamalar kaçının.  
  
## <a name="example"></a>Örnek  
 Aşağıdaki örnek, bir `foreach` döngü içinde bir koleksiyonun tüm öğelerini değiştirmeye çalıştığından CS1656 hatası oluşturur. Hatayı aşmanın bir yolu, `foreach` döngüyü [for](../keywords/for.md) döngüsüne değiştirmektir. Burada gösterilmeyen başka bir yol da varolan öğenin üyelerini değiştirmektir; bu sınıflar ile mümkündür, ancak yapılarla değil.  
  
```csharp  
using System;  
using System.Collections;  
using System.Collections.Generic;  
using System.Text;  
  
namespace CS1656_2  
{  
  
    class Book  
    {  
        public string Title;  
        public string Author;  
        public double Price;  
        public Book(string t, string a, double p)  
        {  
            Title=t;  
            Author=a;  
            Price=p;  
  
        }  
    }  
  
    class Program  
    {  
        private List<Book> list;  
        static void Main(string[] args)  
        {  
            Program prog = new Program();  
            prog.list = new List<Book>();  
            prog.list.Add(new Book ("The C# Programming Language",  
                                    "Hejlsberg, Wiltamuth, Golde",  
                                     29.95));  
            prog.list.Add(new Book ("The C++ Programming Language",  
                                    "Stroustrup",  
                                     29.95));  
            prog.list.Add(new Book ("The C Programming Language",  
                                    "Kernighan, Ritchie",  
                                    29.95));  
            foreach(Book b in prog.list)  
            {  
                // Cannot modify an entire element in a foreach loop
                // even with reference types.  
                // Use a for or while loop instead  
                if (b.Title == "The C Programming Language")  
                    // Cannot assign to 'b' because it is a 'foreach
                    // iteration variable'  
                    b = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95); //CS1656  
            }  
  
            //With a for loop you can modify elements  
            //for(int x = 0; x < prog.list.Count; x++)  
            //{  
            //    if(prog.list[x].Title== "The C Programming Language")  
            //        prog.list[x] = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95);  
            //}  
            //foreach(Book b in prog.list)  
            //    Console.WriteLine(b.Title);  
  
        }  
    }  
}  
```  
  
## <a name="example"></a>Örnek  
 Aşağıdaki örnek, CS1656'nın döngü `foreach` dışında diğer bağlamlarda nasıl oluşturulabileceğini göstermektedir:  
  
```csharp  
// CS1656.cs  
// compile with: /unsafe  
using System;  
  
class C : IDisposable  
{  
    public void Dispose() { }  
}  
  
class CMain  
{  
    unsafe public static void Main()  
    {  
        using (C c = new C())  
        {  
            // Cannot assign to 'c' because it is a 'using variable'  
            c = new C(); // CS1656  
        }  
  
        int[] ary = new int[] { 1, 2, 3, 4 };  
        fixed (int* p = ary)  
        {  
            // Cannot assign to 'p' because it is a 'fixed variable'  
            p = null; // CS1656  
        }  
    }  
}  
```
