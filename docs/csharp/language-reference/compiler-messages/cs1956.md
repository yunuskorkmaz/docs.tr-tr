---
title: Derleyici Uyarısı (düzey 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: f848cff8aaedc86cbb2b6abb4a9b856bde85741c
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/18/2019
ms.locfileid: "59771029"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="f4a28-102">Derleyici Uyarısı (düzey 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="f4a28-102">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="f4a28-103">Üye 'name' type 'türü 'name' arabirim üyesini uygular.</span><span class="sxs-lookup"><span data-stu-id="f4a28-103">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="f4a28-104">Çalışma zamanında arabirim üyesi için birden çok eşleşme vardır.</span><span class="sxs-lookup"><span data-stu-id="f4a28-104">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="f4a28-105">Hangi yöntemin çağrılacağı uygulamaya bağımlıdır var.</span><span class="sxs-lookup"><span data-stu-id="f4a28-105">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="f4a28-106">Bu uyarı iki arabirim yöntemleri, yalnızca belirli bir parametre ile işaretlenmiş olup olmadığını ayırt edilen zaman oluşturulabilir `ref` veya `out`.</span><span class="sxs-lookup"><span data-stu-id="f4a28-106">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="f4a28-107">Çalışma zamanında hangi yöntemin çağrıldığı garanti veya belirgin olmadığından bu uyarıyı önlemek için kodunuzu değiştirmeniz en iyisidir.</span><span class="sxs-lookup"><span data-stu-id="f4a28-107">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="f4a28-108">Ancak C# ayırt `out` ve `ref`, CLR bunları aynı görür.</span><span class="sxs-lookup"><span data-stu-id="f4a28-108">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="f4a28-109">Hangi yöntemin arabirimi uygulayan karar verirken, CLR yalnızca birini seçer.</span><span class="sxs-lookup"><span data-stu-id="f4a28-109">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="f4a28-110">Bu uyarıyı engellemek için</span><span class="sxs-lookup"><span data-stu-id="f4a28-110">To avoid this warning</span></span>  
  
1. <span data-ttu-id="f4a28-111">Derleyici yöntemleri ayırmak için bir yol sağlayın.</span><span class="sxs-lookup"><span data-stu-id="f4a28-111">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="f4a28-112">Örneğin, bunlara farklı adlar vermeniz veya ek bir parametre bunlardan birini sağlayın.</span><span class="sxs-lookup"><span data-stu-id="f4a28-112">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="f4a28-113">Örnek</span><span class="sxs-lookup"><span data-stu-id="f4a28-113">Example</span></span>  
 <span data-ttu-id="f4a28-114">Aşağıdaki kodu CS1956 oluşturur çünkü iki `Test` yöntemleri `Base` yalnızca farklı `ref` / `out` ilk parametre değiştiricisi.</span><span class="sxs-lookup"><span data-stu-id="f4a28-114">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
