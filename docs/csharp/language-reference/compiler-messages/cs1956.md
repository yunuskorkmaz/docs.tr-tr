---
description: Derleyici Uyarısı (düzey 1) CS1956
title: Derleyici Uyarısı (düzey 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: b59eb0b351f5e75feae270bf39c552d62d9dabae
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89126274"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="d9fcd-103">Derleyici Uyarısı (düzey 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="d9fcd-103">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="d9fcd-104">' Name ' üyesi ' Type ' türünde ' name ' arabirim üyesini uyguluyor.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-104">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="d9fcd-105">Çalışma zamanında arabirim üyesi için birden fazla eşleşme var.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-105">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="d9fcd-106">Uygulamaya bağımlı, hangi yöntemin çağrılacaktır.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-106">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="d9fcd-107">Bu uyarı, iki arabirim yöntemi yalnızca belirli bir parametrenin veya ile işaretli olup olmadığı fark edildiğinde oluşturulabilir `ref` `out` .</span><span class="sxs-lookup"><span data-stu-id="d9fcd-107">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="d9fcd-108">Bu uyarıyı önlemek için kodunuzun değiştirilmesi en iyisidir veya çalışma zamanında hangi yöntemin çağrıldığı garanti edilir.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-108">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="d9fcd-109">C# ve arasında ayrım olmasına rağmen `out` `ref` clr bunları aynı şekilde görür.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-109">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="d9fcd-110">Arabirimi hangi yöntemin uyguladığı saptarken, CLR yalnızca bir tane seçer.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-110">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="d9fcd-111">Bu uyarıyı önlemek için</span><span class="sxs-lookup"><span data-stu-id="d9fcd-111">To avoid this warning</span></span>  
  
1. <span data-ttu-id="d9fcd-112">Derleyiciye yöntemleri ayırt etmek için bir yol sağlayın.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-112">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="d9fcd-113">Örneğin, bunlara farklı adlar verebilir veya bunlardan birine ek bir parametre sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-113">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="d9fcd-114">Örnek</span><span class="sxs-lookup"><span data-stu-id="d9fcd-114">Example</span></span>  
 <span data-ttu-id="d9fcd-115">Aşağıdaki kod CS1956 oluşturur, çünkü içindeki iki `Test` Yöntem `Base` yalnızca `ref` / `out` ilk parametresindeki değiştiriciye göre farklılık gösterir.</span><span class="sxs-lookup"><span data-stu-id="d9fcd-115">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
