---
title: 'CA2000: kapsamı kaybetmeden önce nesneleri atma (kod analizi)'
description: 'Kod Analizi kuralıyla ilgili bilgi edinin CA2000: kapsamı kaybetmeden önce nesneleri atma'
ms.date: 05/14/2019
ms.topic: reference
f1_keywords:
- CA2000
- Dispose objects before losing scope
- DisposeObjectsBeforeLosingScope
helpviewer_keywords:
- CA2000
- DisposeObjectsBeforeLosingScope
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: f00710d8a0290bea833e33c4ff5a7ebd2642d23b
ms.sourcegitcommit: a6bd4cad438fe479cbd112eae10f2cd449f06e40
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/08/2020
ms.locfileid: "96589678"
---
# <a name="ca2000-dispose-objects-before-losing-scope"></a><span data-ttu-id="02a74-103">CA2000: Kapsamı kaybetmeden önce nesneleri bırakın</span><span class="sxs-lookup"><span data-stu-id="02a74-103">CA2000: Dispose objects before losing scope</span></span>

| | <span data-ttu-id="02a74-104">Değer</span><span class="sxs-lookup"><span data-stu-id="02a74-104">Value</span></span> |
|-|-|
| <span data-ttu-id="02a74-105">**Kural Kimliği**</span><span class="sxs-lookup"><span data-stu-id="02a74-105">**Rule ID**</span></span> |<span data-ttu-id="02a74-106">CA2000</span><span class="sxs-lookup"><span data-stu-id="02a74-106">CA2000</span></span>|
| <span data-ttu-id="02a74-107">**Kategori**</span><span class="sxs-lookup"><span data-stu-id="02a74-107">**Category**</span></span> |<span data-ttu-id="02a74-108">Microsoft. güvenilirliği</span><span class="sxs-lookup"><span data-stu-id="02a74-108">Microsoft.Reliability</span></span>|
| <span data-ttu-id="02a74-109">**Düzeltilme veya kırılmamış**</span><span class="sxs-lookup"><span data-stu-id="02a74-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="02a74-110">Kırılmamış</span><span class="sxs-lookup"><span data-stu-id="02a74-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="02a74-111">Nedeni</span><span class="sxs-lookup"><span data-stu-id="02a74-111">Cause</span></span>

<span data-ttu-id="02a74-112">Bir türün yerel nesnesi <xref:System.IDisposable> oluşturulur, ancak nesne tüm başvuruları kapsam dışı olmadan önce nesne atılamaz.</span><span class="sxs-lookup"><span data-stu-id="02a74-112">A local object of an <xref:System.IDisposable> type is created, but the object is not disposed before all references to the object are out of scope.</span></span>

<span data-ttu-id="02a74-113">Varsayılan olarak, bu kural codebase 'in tamamını analiz eder, ancak bu [yapılandırılabilir](#configurability).</span><span class="sxs-lookup"><span data-stu-id="02a74-113">By default, this rule analyzes the entire codebase, but this is [configurable](#configurability).</span></span>

## <a name="rule-description"></a><span data-ttu-id="02a74-114">Kural açıklaması</span><span class="sxs-lookup"><span data-stu-id="02a74-114">Rule description</span></span>

<span data-ttu-id="02a74-115">Bir atılabilir nesnesi, tüm başvuruları kapsam dışına çıkarılmadan önce açıkça atılmaz, çöp toplayıcı nesnenin sonlandırıcısını çalıştırdığında, nesne belirsiz bir zamanda silinir.</span><span class="sxs-lookup"><span data-stu-id="02a74-115">If a disposable object is not explicitly disposed before all references to it are out of scope, the object will be disposed at some indeterminate time when the garbage collector runs the finalizer of the object.</span></span> <span data-ttu-id="02a74-116">Nesnenin sonlandırıcısını çalışmasını engelleyecek olağanüstü bir olay ortaya çıkabilecek için, bunun yerine nesne açıkça atılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="02a74-116">Because an exceptional event might occur that will prevent the finalizer of the object from running, the object should be explicitly disposed instead.</span></span>

## <a name="special-cases"></a><span data-ttu-id="02a74-117">Özel durumlar</span><span class="sxs-lookup"><span data-stu-id="02a74-117">Special cases</span></span>

<span data-ttu-id="02a74-118">CA2000 kuralı, nesne atılmasa bile aşağıdaki türlerin yerel nesneleri için başlatılmıyor:</span><span class="sxs-lookup"><span data-stu-id="02a74-118">Rule CA2000 does not fire for local objects of the following types even if the object is not disposed:</span></span>

- <xref:System.IO.Stream?displayProperty=nameWithType>
- <xref:System.IO.StringReader?displayProperty=nameWithType>
- <xref:System.IO.TextReader?displayProperty=nameWithType>
- <xref:System.IO.TextWriter?displayProperty=nameWithType>
- <xref:System.Resources.IResourceReader?displayProperty=nameWithType>

<span data-ttu-id="02a74-119">Bu türlerden birine ait bir nesnenin bir oluşturucuya geçirilmesi ve sonra bir alana atanması, yeni oluşturulan türe bir *atma sahipliğinin aktarılmasını* gösterir.</span><span class="sxs-lookup"><span data-stu-id="02a74-119">Passing an object of one of these types to a constructor and then assigning it to a field indicates a *dispose ownership transfer* to the newly constructed type.</span></span> <span data-ttu-id="02a74-120">Diğer bir deyişle, yeni oluşturulan tür artık nesnenin elden atılırken sorumludur.</span><span class="sxs-lookup"><span data-stu-id="02a74-120">That is, the newly constructed type is now responsible for disposing of the object.</span></span> <span data-ttu-id="02a74-121">Kodunuz bu türlerden birinin bir nesnesini bir oluşturucuya geçirirse, nesne tüm başvuruları kapsam dışı olmadan önce atılmasa bile kural CA2000 hiçbir ihlalin gerçekleşmez.</span><span class="sxs-lookup"><span data-stu-id="02a74-121">If your code passes an object of one of these types to a constructor, no violation of rule CA2000 occurs even if the object is not disposed before all references to it are out of scope.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="02a74-122">İhlalleri çözme</span><span class="sxs-lookup"><span data-stu-id="02a74-122">How to fix violations</span></span>

<span data-ttu-id="02a74-123">Bu kuralın ihlalini onarmak için, <xref:System.IDisposable.Dispose%2A> tüm başvuruları kapsam dışı olmadan önce nesne üzerinde çağrı yapın.</span><span class="sxs-lookup"><span data-stu-id="02a74-123">To fix a violation of this rule, call <xref:System.IDisposable.Dispose%2A> on the object before all references to it are out of scope.</span></span>

<span data-ttu-id="02a74-124">[ `using` ](../../../csharp/language-reference/keywords/using-statement.md) [`Using`](../../../visual-basic/language-reference/statements/using-statement.md) Uygulayan nesneleri kaydırmak için (Visual Basic) ifadesini kullanabilirsiniz <xref:System.IDisposable> .</span><span class="sxs-lookup"><span data-stu-id="02a74-124">You can use the [`using` statement](../../../csharp/language-reference/keywords/using-statement.md) ([`Using`](../../../visual-basic/language-reference/statements/using-statement.md) in Visual Basic) to wrap objects that implement <xref:System.IDisposable>.</span></span> <span data-ttu-id="02a74-125">Bu şekilde Sarmalanan nesneler bloğun sonuna otomatik olarak silinir `using` .</span><span class="sxs-lookup"><span data-stu-id="02a74-125">Objects that are wrapped in this manner are automatically disposed at the end of the `using` block.</span></span> <span data-ttu-id="02a74-126">Ancak aşağıdaki durumlar, bir ifadesiyle birlikte işlenmemelidir `using` .</span><span class="sxs-lookup"><span data-stu-id="02a74-126">However, the following situations should not or cannot be handled with a `using` statement:</span></span>

- <span data-ttu-id="02a74-127">Bir atılabilir nesnesi döndürmek için, nesnesinin bir `try/finally` blok dışında bir blokta oluşturulması gerekir `using` .</span><span class="sxs-lookup"><span data-stu-id="02a74-127">To return a disposable object, the object must constructed in a `try/finally` block outside of a `using` block.</span></span>

- <span data-ttu-id="02a74-128">Bir deyimin oluşturucusunda bir atılabilir nesnesinin üyelerini başlatmayın `using` .</span><span class="sxs-lookup"><span data-stu-id="02a74-128">Do not initialize members of a disposable object in the constructor of a `using` statement.</span></span>

- <span data-ttu-id="02a74-129">Yalnızca bir özel durum işleyicisi tarafından korunan oluşturucular [bir `using` deyimin alım bölümünde](../../../csharp/language-reference/keywords/using-statement.md)iç içe geçmişse, dış oluşturucudaki bir hata, iç içe Oluşturucu tarafından oluşturulan nesnenin hiçbir zaman kapanmamış olması halinde oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="02a74-129">When constructors that are protected by only one exception handler are nested in the [acquisition part of a `using` statement](../../../csharp/language-reference/keywords/using-statement.md), a failure in the outer constructor can result in the object created by the nested constructor never being closed.</span></span> <span data-ttu-id="02a74-130">Aşağıdaki örnekte, <xref:System.IO.StreamReader> oluşturucudaki bir hata <xref:System.IO.FileStream> nesnenin hiçbir şekilde kapanmamış olması olabilir.</span><span class="sxs-lookup"><span data-stu-id="02a74-130">In the following example, a failure in the <xref:System.IO.StreamReader> constructor can result in the <xref:System.IO.FileStream> object never being closed.</span></span> <span data-ttu-id="02a74-131">CA2000 bayraklar bu durumda kural ihlaline neden oluyor.</span><span class="sxs-lookup"><span data-stu-id="02a74-131">CA2000 flags a violation of the rule in this case.</span></span>

   ```csharp
   using (StreamReader sr = new StreamReader(new FileStream("C:\myfile.txt", FileMode.Create)))
   { ... }
   ```

- <span data-ttu-id="02a74-132">Dinamik nesneler, nesnelerin Dispose modelini uygulamak için bir gölge nesnesi kullanmalıdır <xref:System.IDisposable> .</span><span class="sxs-lookup"><span data-stu-id="02a74-132">Dynamic objects should use a shadow object to implement the dispose pattern of <xref:System.IDisposable> objects.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="02a74-133">Uyarıların ne zaman bastırılamıyor</span><span class="sxs-lookup"><span data-stu-id="02a74-133">When to suppress warnings</span></span>

<span data-ttu-id="02a74-134">Bu kuraldan bir uyarıyı şu durumlar dışında gösterme:</span><span class="sxs-lookup"><span data-stu-id="02a74-134">Do not suppress a warning from this rule unless:</span></span>

- <span data-ttu-id="02a74-135">Nesneniz için Şunu çağıran bir yöntemi (örneğin `Dispose` , <xref:System.IO.Stream.Close%2A></span><span class="sxs-lookup"><span data-stu-id="02a74-135">You've called a method on your object that calls `Dispose`, such as <xref:System.IO.Stream.Close%2A></span></span>
- <span data-ttu-id="02a74-136">Uyarıyı tetikleyen Yöntem, <xref:System.IDisposable> nesnenizin sarmaladığı bir nesne döndürüyor</span><span class="sxs-lookup"><span data-stu-id="02a74-136">The method that raised the warning returns an <xref:System.IDisposable> object that wraps your object</span></span>
- <span data-ttu-id="02a74-137">Ayırma yöntemi Dispose sahipliğini içermez; diğer bir deyişle, nesneyi atılama sorumluluğu, yönteminde oluşturulan ve çağırana döndürülen başka bir nesne veya sarmalayıcı için aktarılır</span><span class="sxs-lookup"><span data-stu-id="02a74-137">The allocating method does not have dispose ownership; that is, the responsibility to dispose the object is transferred to another object or wrapper that's created in the method and returned to the caller</span></span>

## <a name="configurability"></a><span data-ttu-id="02a74-138">Yapılandırılabilirlik</span><span class="sxs-lookup"><span data-stu-id="02a74-138">Configurability</span></span>

<span data-ttu-id="02a74-139">Bu kural, aşağıdaki yapılandırılabilir seçeneklere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="02a74-139">This rule has the following configurable options.</span></span>

### <a name="excluded-symbol-names"></a><span data-ttu-id="02a74-140">Dışlanan sembol adları</span><span class="sxs-lookup"><span data-stu-id="02a74-140">Excluded symbol names</span></span>

<span data-ttu-id="02a74-141">Kod tabanınızın hangi bölümlerinin analizden dışlanacağını yapılandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="02a74-141">You can configure which parts of your codebase to exclude from analysis.</span></span> <span data-ttu-id="02a74-142">Örneğin, kuralın adlı tür içindeki herhangi bir kodda çalıştırılmamalıdır `MyType` , aşağıdaki anahtar-değer çiftini projenizdeki bir *. editorconfig* dosyasına ekleyin:</span><span class="sxs-lookup"><span data-stu-id="02a74-142">For example, to specify that the rule should not run on any code within types named `MyType`, add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA2000.excluded_symbol_names = MyType
```

<span data-ttu-id="02a74-143">Seçenek değerindeki izin verilen sembol adı biçimleri (ile ayrılmış `|` ):</span><span class="sxs-lookup"><span data-stu-id="02a74-143">Allowed symbol name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="02a74-144">Yalnızca sembol adı (kapsayan tür veya ad alanından bağımsız olarak ada sahip tüm semboller dahil)</span><span class="sxs-lookup"><span data-stu-id="02a74-144">Symbol name only (includes all symbols with the name, regardless of the containing type or namespace)</span></span>
- <span data-ttu-id="02a74-145">Simgenin [belge kimliği biçimindeki](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format)tam adlar.</span><span class="sxs-lookup"><span data-stu-id="02a74-145">Fully qualified names in the symbol's [documentation ID format](https://github.com/dotnet/csharplang/blob/master/spec/documentation-comments.md#id-string-format).</span></span> <span data-ttu-id="02a74-146">Her sembol adı, "d:" metotları için önek, `T:` türler için önek, "N:" ad alanları için önek vb. gibi bir sembol türü öneki gerektirir.</span><span class="sxs-lookup"><span data-stu-id="02a74-146">Each symbol name requires a symbol kind prefix, such as "M:" prefix for methods, `T:` prefix for types, "N:" prefix for namespaces, etc.</span></span>
- <span data-ttu-id="02a74-147">`.ctor` oluşturucular ve `.cctor` statik oluşturucular için</span><span class="sxs-lookup"><span data-stu-id="02a74-147">`.ctor` for constructors and `.cctor` for static constructors</span></span>

<span data-ttu-id="02a74-148">Örnekler:</span><span class="sxs-lookup"><span data-stu-id="02a74-148">Examples:</span></span>

| <span data-ttu-id="02a74-149">Seçenek değeri</span><span class="sxs-lookup"><span data-stu-id="02a74-149">Option Value</span></span> | <span data-ttu-id="02a74-150">Özet</span><span class="sxs-lookup"><span data-stu-id="02a74-150">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA2000.excluded_symbol_names = MyType` | <span data-ttu-id="02a74-151">Derlemede ' MyType ' adlı tüm simgeleri eşleştirir</span><span class="sxs-lookup"><span data-stu-id="02a74-151">Matches all symbols named 'MyType' in the compilation</span></span>
|`dotnet_code_quality.CA2000.excluded_symbol_names = MyType1|MyType2` | <span data-ttu-id="02a74-152">Derlemede ' MyType1 ' ya da ' MyType2 ' adlı tüm simgeleri eşleştirir</span><span class="sxs-lookup"><span data-stu-id="02a74-152">Matches all symbols named either 'MyType1' or 'MyType2' in the compilation</span></span>
|`dotnet_code_quality.CA2000.excluded_symbol_names = M:NS.MyType.MyMethod(ParamType)` | <span data-ttu-id="02a74-153">Verilen tam imzaya sahip ' MyMethod ' özel yöntemiyle eşleşir</span><span class="sxs-lookup"><span data-stu-id="02a74-153">Matches specific method 'MyMethod' with given fully qualified signature</span></span>
|`dotnet_code_quality.CA2000.excluded_symbol_names = M:NS1.MyType1.MyMethod1(ParamType)|M:NS2.MyType2.MyMethod2(ParamType)` | <span data-ttu-id="02a74-154">' MyMethod1 ' ve ' MyMethod2 ' belirli yöntemleriyle ilgili tam imzayla eşleşir</span><span class="sxs-lookup"><span data-stu-id="02a74-154">Matches specific methods 'MyMethod1' and 'MyMethod2' with respective fully qualified signature</span></span>

<span data-ttu-id="02a74-155">Bu seçeneklerin tümünü yalnızca bu kural için, tüm kurallar için veya bu kategorideki tüm kurallar (tasarım) için yapılandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="02a74-155">You can configure all of these options for just this rule, for all rules, or for all rules in this category (Design).</span></span> <span data-ttu-id="02a74-156">Daha fazla bilgi için bkz. [kod kalitesi kural yapılandırma seçenekleri](../code-quality-rule-options.md).</span><span class="sxs-lookup"><span data-stu-id="02a74-156">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

## <a name="related-rules"></a><span data-ttu-id="02a74-157">İlgili kurallar</span><span class="sxs-lookup"><span data-stu-id="02a74-157">Related rules</span></span>

- [<span data-ttu-id="02a74-158">CA2213: Atılabilen alanlar atılmalıdır</span><span class="sxs-lookup"><span data-stu-id="02a74-158">CA2213: Disposable fields should be disposed</span></span>](ca2213.md)

## <a name="example-1"></a><span data-ttu-id="02a74-159">Örnek 1</span><span class="sxs-lookup"><span data-stu-id="02a74-159">Example 1</span></span>

<span data-ttu-id="02a74-160">Atılabilir nesnesi döndüren bir yöntem uygularsanız, nesnenin atılmış olduğundan emin olmak için catch bloğu olmayan bir try/finally bloğu kullanın.</span><span class="sxs-lookup"><span data-stu-id="02a74-160">If you're implementing a method that returns a disposable object, use a try/finally block without a catch block to make sure that the object is disposed.</span></span> <span data-ttu-id="02a74-161">Bir try/finally bloğu kullanarak, özel durumların hata noktasında ortaya çıkmasına ve nesnenin atıldığından emin olmanızı sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="02a74-161">By using a try/finally block, you allow exceptions to be raised at the fault point and make sure that object is disposed.</span></span>

<span data-ttu-id="02a74-162">OpenPort1 yönteminde, ISerializable nesne SerialPort 'u açma çağrısı veya SomeMethod çağrısı başarısız olabilir.</span><span class="sxs-lookup"><span data-stu-id="02a74-162">In the OpenPort1 method, the call to open the ISerializable object SerialPort or the call to SomeMethod can fail.</span></span> <span data-ttu-id="02a74-163">Bu uygulamada bir CA2000 uyarısı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="02a74-163">A CA2000 warning is raised on this implementation.</span></span>

<span data-ttu-id="02a74-164">OpenPort2 yönteminde, iki SerialPort nesnesi bildirilmiştir ve null olarak ayarlanır:</span><span class="sxs-lookup"><span data-stu-id="02a74-164">In the OpenPort2 method, two SerialPort objects are declared and set to null:</span></span>

- <span data-ttu-id="02a74-165">`tempPort`, yöntem işlemlerinin başarılı olduğunu test etmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="02a74-165">`tempPort`, which is used to test that the method operations succeed.</span></span>

- <span data-ttu-id="02a74-166">`port`, yönteminin dönüş değeri için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="02a74-166">`port`, which is used for the return value of the method.</span></span>

<span data-ttu-id="02a74-167">`tempPort`Oluşturulur ve bir `try` blokta açılır ve diğer tüm gerekli işler aynı `try` blokta gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="02a74-167">The `tempPort` is constructed and opened in a `try` block, and any other required work is performed in the same `try` block.</span></span> <span data-ttu-id="02a74-168">`try`Bloğun sonunda, açılan bağlantı noktası `port` döndürülecek nesneye atanır ve `tempPort` nesne olarak ayarlanır `null` .</span><span class="sxs-lookup"><span data-stu-id="02a74-168">At the end of the `try` block, the opened port is assigned to the `port` object that will be returned and the `tempPort` object is set to `null`.</span></span>

<span data-ttu-id="02a74-169">`finally`Bloğu değerini denetler `tempPort` .</span><span class="sxs-lookup"><span data-stu-id="02a74-169">The `finally` block checks the value of `tempPort`.</span></span> <span data-ttu-id="02a74-170">Null değilse, yöntemindeki bir işlem başarısız olur ve `tempPort` tüm kaynakların serbest bırakılmış olduğundan emin olmak için kapalıdır.</span><span class="sxs-lookup"><span data-stu-id="02a74-170">If it is not null, an operation in the method has failed, and `tempPort` is closed to make sure that any resources are released.</span></span> <span data-ttu-id="02a74-171">Döndürülen bağlantı noktası nesnesi, metodun işlemleri başarılı olursa Açık SerialPort nesnesini içerir veya bir işlem başarısız olursa null olur.</span><span class="sxs-lookup"><span data-stu-id="02a74-171">The returned port object will contain the opened SerialPort object if the operations of the method succeeded, or it will be null if an operation failed.</span></span>

```csharp
public SerialPort OpenPort1(string portName)
{
   SerialPort port = new SerialPort(portName);
   port.Open();  //CA2000 fires because this might throw
   SomeMethod(); //Other method operations can fail
   return port;
}

public SerialPort OpenPort2(string portName)
{
   SerialPort tempPort = null;
   SerialPort port = null;
   try
   {
      tempPort = new SerialPort(portName);
      tempPort.Open();
      SomeMethod();
      //Add any other methods above this line
      port = tempPort;
      tempPort = null;

   }
   finally
   {
      if (tempPort != null)
      {
         tempPort.Close();
      }
   }
   return port;
}
```

```vb
Public Function OpenPort1(ByVal PortName As String) As SerialPort

   Dim port As New SerialPort(PortName)
   port.Open()    'CA2000 fires because this might throw
   SomeMethod()   'Other method operations can fail
   Return port

End Function

Public Function OpenPort2(ByVal PortName As String) As SerialPort

   Dim tempPort As SerialPort = Nothing
   Dim port As SerialPort = Nothing

   Try
      tempPort = New SerialPort(PortName)
      tempPort.Open()
      SomeMethod()
      'Add any other methods above this line
      port = tempPort
      tempPort = Nothing

   Finally
      If Not tempPort Is Nothing Then
         tempPort.Close()
      End If

   End Try

   Return port

End Function
```

## <a name="example-2"></a><span data-ttu-id="02a74-172">Örnek 2</span><span class="sxs-lookup"><span data-stu-id="02a74-172">Example 2</span></span>

<span data-ttu-id="02a74-173">Varsayılan olarak, Visual Basic derleyicisinde tüm aritmetik işleçler taşma için denetlenir.</span><span class="sxs-lookup"><span data-stu-id="02a74-173">By default, the Visual Basic compiler has all arithmetic operators check for overflow.</span></span> <span data-ttu-id="02a74-174">Bu nedenle, herhangi bir Visual Basic aritmetik işlemi oluşturabilir <xref:System.OverflowException> .</span><span class="sxs-lookup"><span data-stu-id="02a74-174">Therefore, any Visual Basic arithmetic operation might throw an <xref:System.OverflowException>.</span></span> <span data-ttu-id="02a74-175">Bu, CA2000 gibi kurallarda beklenmeyen ihlallere neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="02a74-175">This could lead to unexpected violations in rules such as CA2000.</span></span> <span data-ttu-id="02a74-176">Örneğin, Visual Basic derleyici, StreamReader atılmasına neden olacak bir özel durum oluşturabilecek ekleme için bir taşma denetimi yönergesi yaydığı için, aşağıdaki CreateReader1 işlevi bir CA2000 ihlaline neden olur.</span><span class="sxs-lookup"><span data-stu-id="02a74-176">For example, the following CreateReader1 function will produce a CA2000 violation because the Visual Basic compiler is emitting an overflow checking instruction for the addition that could throw an exception that would cause the StreamReader not to be disposed.</span></span>

<span data-ttu-id="02a74-177">Bunu yapmak için, projenizdeki Visual Basic derleyicisinden taşma denetimleri yaymayı devre dışı bırakabilir veya kodunuzu aşağıdaki CreateReader2 işlevinde olarak değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="02a74-177">To fix this, you can disable the emitting of overflow checks by the Visual Basic compiler in your project or you can modify your code as in the following CreateReader2 function.</span></span>

<span data-ttu-id="02a74-178">Taşma denetimlerinin yayışını devre dışı bırakmak için Çözüm Gezgini ' de proje adına sağ tıklayın ve ardından **Özellikler**' e tıklayın.</span><span class="sxs-lookup"><span data-stu-id="02a74-178">To disable the emitting of overflow checks, right-click the project name in Solution Explorer and then click **Properties**.</span></span> <span data-ttu-id="02a74-179">**Derle**' ye tıklayın, **Gelişmiş derleme seçenekleri**' ne tıklayın ve ardından **tamsayı taşma denetimlerini kaldır**' ı işaretleyin.</span><span class="sxs-lookup"><span data-stu-id="02a74-179">Click **Compile**, click **Advanced Compile Options**, and then check **Remove integer overflow checks**.</span></span>

:::code language="vb" source="snippets/vb/all-rules/ca2000-dispose-objects-before-losing-scope-vboverflow_1.vb":::

## <a name="see-also"></a><span data-ttu-id="02a74-180">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="02a74-180">See also</span></span>

- <xref:System.IDisposable>
- [<span data-ttu-id="02a74-181">Dispose Deseni</span><span class="sxs-lookup"><span data-stu-id="02a74-181">Dispose Pattern</span></span>](../../../standard/garbage-collection/implementing-dispose.md)
