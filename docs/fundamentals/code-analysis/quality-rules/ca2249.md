---
title: "CA2249: ' String. IndexOf ' yerine ' String. Contains ' kullanmayı düşünün (kod analizi)"
description: "Kod Analizi kuralı CA2249 hakkında bilgi edinin: ' String. IndexOf ' yerine ' String. Contains ' kullanmayı düşünün"
ms.date: 08/27/2020
ms.topic: reference
f1_keywords:
- CA2249
helpviewer_keywords:
- CA2249
author: pgovind
ms.author: prgovi
ms.openlocfilehash: c945de80523d256f49f574ad2d8aa465c47c6dfc
ms.sourcegitcommit: 05d0087dfca85aac9ca2960f86c5efd218bf833f
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/30/2021
ms.locfileid: "99714592"
---
# <a name="ca2249-consider-using-stringcontains-instead-of-stringindexof"></a><span data-ttu-id="5eb5d-103">CA2249: String.IndexOf yerine String.Contains kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="5eb5d-103">CA2249: Consider using String.Contains instead of String.IndexOf</span></span>

| | <span data-ttu-id="5eb5d-104">Değer</span><span class="sxs-lookup"><span data-stu-id="5eb5d-104">Value</span></span> |
|-|-|
| <span data-ttu-id="5eb5d-105">**Kural Kimliği**</span><span class="sxs-lookup"><span data-stu-id="5eb5d-105">**Rule ID**</span></span> |<span data-ttu-id="5eb5d-106">CA2249</span><span class="sxs-lookup"><span data-stu-id="5eb5d-106">CA2249</span></span>|
| <span data-ttu-id="5eb5d-107">**Kategori**</span><span class="sxs-lookup"><span data-stu-id="5eb5d-107">**Category**</span></span> |[<span data-ttu-id="5eb5d-108">Kullanım</span><span class="sxs-lookup"><span data-stu-id="5eb5d-108">Usage</span></span>](usage-warnings.md)|
| <span data-ttu-id="5eb5d-109">**Düzeltilme veya kırılmamış**</span><span class="sxs-lookup"><span data-stu-id="5eb5d-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="5eb5d-110">Kırılmamış</span><span class="sxs-lookup"><span data-stu-id="5eb5d-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="5eb5d-111">Nedeni</span><span class="sxs-lookup"><span data-stu-id="5eb5d-111">Cause</span></span>

<span data-ttu-id="5eb5d-112">Bu kural, bir alt <xref:System.String.IndexOf%2A> dizenin varlığını veya yokluğunu denetlemek için sonucun kullanıldığı yere çağrıları bulur ve <xref:System.String.Contains%2A> okunabilirliği artırmak için bunun yerine kullanmayı önerir.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-112">This rule locates calls to <xref:System.String.IndexOf%2A> where the result is used to check for the presence or absence of a substring, and suggests using <xref:System.String.Contains%2A> instead, to improve readability.</span></span>

## <a name="rule-description"></a><span data-ttu-id="5eb5d-113">Kural açıklaması</span><span class="sxs-lookup"><span data-stu-id="5eb5d-113">Rule description</span></span>

<span data-ttu-id="5eb5d-114"><xref:System.String.IndexOf%2A>Sonucun şuna eşit veya daha büyük ya da eşit olup olmadığını denetlemek için kullanıldığında `-1` , bu `0` çağrının performansı etkilemeden güvenle yerine kullanılabilir <xref:System.String.Contains%2A> .</span><span class="sxs-lookup"><span data-stu-id="5eb5d-114">When <xref:System.String.IndexOf%2A> is used to check if the result is equal to `-1` or greater or equal than `0`, the call can be safely substituted with <xref:System.String.Contains%2A> without an impact on performance.</span></span>

<span data-ttu-id="5eb5d-115">Kullanılan aşırı yüklemeye bağlı olarak <xref:System.String.IndexOf%2A> , önerilen `comparisonType` düzeltilme bir bağımsız değişken alabilir:</span><span class="sxs-lookup"><span data-stu-id="5eb5d-115">Depending on the <xref:System.String.IndexOf%2A> overload being used, the suggested fix could get a `comparisonType` argument added:</span></span>

| <span data-ttu-id="5eb5d-116">Yüklemek</span><span class="sxs-lookup"><span data-stu-id="5eb5d-116">Overload</span></span> | <span data-ttu-id="5eb5d-117">Önerilen çözüm</span><span class="sxs-lookup"><span data-stu-id="5eb5d-117">Suggested fix</span></span> |
|----|----|
| `String.IndexOf(char)` | `String.Contains(char)` |
| `String.IndexOf(string)` | `String.Contains(string, StringComparison.CurrentCulture)` |
| `String.IndexOf(char, StringComparison.Ordinal)` | `String.Contains(char)` |
| `String.IndexOf(string, StringComparison.Ordinal)` | `String.Contains(string)` |
| `String.IndexOf(char, NON StringComparison.Ordinal)`\* | `String.Contains(char, NON StringComparison.Ordinal)`\* |
| `String.IndexOf(string, NON StringComparison.Ordinal)`\* | `String.Contains(string, NON StringComparison.Ordinal)`\* |

<span data-ttu-id="5eb5d-118">\* Dışında herhangi bir `StringComparison` numaralandırma değeri `StringComparison.Ordinal` :</span><span class="sxs-lookup"><span data-stu-id="5eb5d-118">\* Any `StringComparison` enum value other than `StringComparison.Ordinal`:</span></span>

- <xref:System.StringComparison.CurrentCulture>
- <xref:System.StringComparison.CurrentCultureIgnoreCase>
- <xref:System.StringComparison.InvariantCulture>
- <xref:System.StringComparison.InvariantCultureIgnoreCase>
- <xref:System.StringComparison.OrdinalIgnoreCase>

## <a name="how-to-fix-violations"></a><span data-ttu-id="5eb5d-119">İhlalleri çözme</span><span class="sxs-lookup"><span data-stu-id="5eb5d-119">How to fix violations</span></span>

<span data-ttu-id="5eb5d-120">İhlalin el ile veya bazı durumlarda, Visual Studio 'daki kodu onarmak için hızlı eylemler kullanılarak düzeltilebilir.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-120">The violation can either be fixed manually, or, in some cases, using Quick Actions to fix code in Visual Studio.</span></span>

## <a name="examples"></a><span data-ttu-id="5eb5d-121">Örnekler</span><span class="sxs-lookup"><span data-stu-id="5eb5d-121">Examples</span></span>

<span data-ttu-id="5eb5d-122">Aşağıdaki iki kod parçacığı, kuralın C# ' deki tüm olası ihlallerini ve bunların nasıl düzeltileceğini gösterir:</span><span class="sxs-lookup"><span data-stu-id="5eb5d-122">The following two code snippets show all possible violations of the rule in C# and how to fix them:</span></span>

```csharp
using System;

class MyClass
{
    void MyMethod()
    {
        string str = "My text";
        bool found;

        // No comparisonType in char overload, so no comparisonType added in resulting fix
        found = str.IndexOf('x') == -1;
        found = str.IndexOf('x') >= 0;

        // No comparisonType in string overload, adds StringComparison.CurrentCulture to resulting fix
        found = str.IndexOf("text") == -1;
        found = str.IndexOf("text") >= 0;

        // comparisonType equal to StringComparison.Ordinal, removes the argument
        found = str.IndexOf('x', StringComparison.Ordinal) == -1;
        found = str.IndexOf('x', StringComparison.Ordinal) >= 0;

        found = str.IndexOf("text", StringComparison.Ordinal) == -1;
        found = str.IndexOf("text", StringComparison.Ordinal) >= 0;

        // comparisonType different than StringComparison.Ordinal, preserves the argument
        found = str.IndexOf('x', StringComparison.OrdinalIgnoreCase) == -1;
        found = str.IndexOf('x', StringComparison.CurrentCulture) >= 0;

        found = str.IndexOf("text", StringComparison.InvariantCultureIgnoreCase) == -1;
        found = str.IndexOf("text", StringComparison.InvariantCulture) >= 0;

        // Suggestion message provided, but no automatic fix offered, must be fixed manually
        int index = str.IndexOf("text");
        if (index == -1)
        {
            Console.WriteLine("'text' Not found.");
        }
    }
}
```

```csharp
using System;

class MyClass
{
    void MyMethod()
    {
        string str = "My text";
        bool found;

        // No comparisonType in char overload, so no comparisonType added in resulting fix
        found = !str.Contains('x');
        found = str.Contains('x');

        // No comparisonType in string overload, adds StringComparison.CurrentCulture to resulting fix
        found = !string.Contains("text", StringComparison.CurrentCulture);
        found = string.Contains("text", StringComparison.CurrentCulture);

        // comparisonType equal to StringComparison.Ordinal, removes the argument
        found = !string.Contains('x');
        found = string.Contains('x');

        found = !string.Contains("text");
        found = string.Contains("text");

        // comparisonType different than StringComparison.Ordinal, preserves the argument
        ;found = !string.Contains('x', StringComparison.OrdinalIgnoreCase)
        found = string.Contains('x', StringComparison.CurrentCulture);

        found = !string.Contains("text", StringComparison.InvariantCultureIgnoreCase);
        found = string.Contains("text", StringComparison.InvariantCulture);

        // This case had to be manually fixed
        if (!str.Contains("text"))
        {
            Console.WriteLine("'text' Not found.");
        }
    }
}
```

> [!TIP]
> <span data-ttu-id="5eb5d-123">Visual Studio 'da bu kural için bir kod onarımı vardır.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-123">A code fix is available for this rule in Visual Studio.</span></span> <span data-ttu-id="5eb5d-124">Kullanmak için imleci ihlalin üzerine konumlandırın ve **CTRL** tuşuna basın + **.**</span><span class="sxs-lookup"><span data-stu-id="5eb5d-124">To use it, position the cursor on the violation and press **Ctrl**+**.**</span></span> <span data-ttu-id="5eb5d-125">(nokta).</span><span class="sxs-lookup"><span data-stu-id="5eb5d-125">(period).</span></span> <span data-ttu-id="5eb5d-126">**' Dize kullanmayı düşünün ' ı seçin. ' Yerine ' dize içerir. IndexOf '** , sunulan seçenekler listesinden.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-126">Choose **Consider using 'string.Contains' instead of 'string.IndexOf'** from the list of options that's presented.</span></span>
>
> ![CA2249 için kod düzeltilme-' String ' kullanmayı düşünün. ' Yerine ' dize içerir. IndexOf](media/ca2249-codefix.png)

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="5eb5d-128">Uyarıların ne zaman bastırılamıyor</span><span class="sxs-lookup"><span data-stu-id="5eb5d-128">When to suppress warnings</span></span>

<span data-ttu-id="5eb5d-129">Kod okunabilirliğini iyileştirmek sorun değilse, bu kural ihlalinin görünmemesi güvenli bir hale gelir.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-129">It's safe to suppress a violation of this rule if improving code readability is not a concern.</span></span>

## <a name="see-also"></a><span data-ttu-id="5eb5d-130">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="5eb5d-130">See also</span></span>

- [<span data-ttu-id="5eb5d-131">Kullanım kuralları</span><span class="sxs-lookup"><span data-stu-id="5eb5d-131">Usage rules</span></span>](usage-warnings.md)
