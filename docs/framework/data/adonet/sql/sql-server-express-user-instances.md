---
title: SQL Server Express Kullanıcı Örnekleri
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 00c12376-cb26-4317-86ad-e6e9c089be57
ms.openlocfilehash: 2bd67a9315eda4161d4b76e1638f5c08f9598a52
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79174491"
---
# <a name="sql-server-express-user-instances"></a><span data-ttu-id="420ab-102">SQL Server Express Kullanıcı Örnekleri</span><span class="sxs-lookup"><span data-stu-id="420ab-102">SQL Server Express User Instances</span></span>
<span data-ttu-id="420ab-103">Microsoft SQL Server Express Edition (SQL Server Express), yalnızca SQL Server için .NET Framework Data`SqlClient`Provider () kullanılırken kullanılabilen kullanıcı örneği özelliğini destekler.</span><span class="sxs-lookup"><span data-stu-id="420ab-103">Microsoft SQL Server Express Edition (SQL Server Express) supports the user instance feature, which is only available when using the .NET Framework Data Provider for SQL Server (`SqlClient`).</span></span> <span data-ttu-id="420ab-104">Kullanıcı örneği, bir üst örnek tarafından oluşturulan SQL Server Express Veritabanı Altyapısının ayrı bir örneğidir.</span><span class="sxs-lookup"><span data-stu-id="420ab-104">A user instance is a separate instance of the SQL Server Express Database Engine that is generated by a parent instance.</span></span> <span data-ttu-id="420ab-105">Kullanıcı örnekleri, yerel bilgisayarlarında yönetici olmayan kullanıcıların SQL Server Express veritabanlarına eklemesine ve bunlara bağlanmasına olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-105">User instances allow users who are not administrators on their local computers to attach and connect to SQL Server Express databases.</span></span> <span data-ttu-id="420ab-106">Her örnek, kullanıcı başına bir örnek bazında, tek tek kullanıcının güvenlik bağlamı altında çalışır.</span><span class="sxs-lookup"><span data-stu-id="420ab-106">Each instance runs under the security context of the individual user, on a one-instance-per-user basis.</span></span>  
  
## <a name="user-instance-capabilities"></a><span data-ttu-id="420ab-107">Kullanıcı Örneği Özellikleri</span><span class="sxs-lookup"><span data-stu-id="420ab-107">User Instance Capabilities</span></span>  
 <span data-ttu-id="420ab-108">Kullanıcı örnekleri, Windows'u en az ayrıcalıklı bir kullanıcı hesabı (LUA) altında çalıştıran kullanıcılar için yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-108">User instances are useful for users who are running Windows under a least-privilege user account (LUA).</span></span> <span data-ttu-id="420ab-109">Her kullanıcı, Windows yöneticisi`sysadmin`olarak çalışmaya gerek kalmadan bilgisayarında çalışan örnek üzerinde SQL Server sistem yöneticisi ( ) ayrıcalıkları vardır.</span><span class="sxs-lookup"><span data-stu-id="420ab-109">Each user has SQL Server system administrator (`sysadmin`) privileges over the instance running on their computer without needing to run as a Windows administrator as well.</span></span> <span data-ttu-id="420ab-110">Sınırlı izinlere sahip bir kullanıcı örneğinde çalıştırılan yazılım, SQL Server Express örneği hizmet olarak değil, kullanıcının yönetici olmayan Windows hesabı altında çalıştığı için sistem genelinde değişiklik yapamaz.</span><span class="sxs-lookup"><span data-stu-id="420ab-110">Software executing on a user instance with limited permissions cannot make system-wide changes because the instance of SQL Server Express is running under the non-administrator Windows account of the user, not as a service.</span></span> <span data-ttu-id="420ab-111">Her kullanıcı örneği, üst örneğinden ve aynı bilgisayarda çalışan diğer kullanıcı örneklerinden yalıtılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-111">Each user instance is isolated from its parent instance and from any other user instances running on the same computer.</span></span> <span data-ttu-id="420ab-112">Kullanıcı örneğinde çalışan veritabanları yalnızca tek kullanıcı modunda açılır ve birden çok kullanıcının bir kullanıcı örneğinde çalışan veritabanlarına bağlanması mümkün değildir.</span><span class="sxs-lookup"><span data-stu-id="420ab-112">Databases running on a user instance are opened in single-user mode only, and it is not possible for multiple users to connect to databases running on a user instance.</span></span> <span data-ttu-id="420ab-113">Çoğaltma ve dağıtılmış sorgular da kullanıcı örnekleri için devre dışı bırakılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-113">Replication and distributed queries are also disabled for user instances.</span></span>
  
> [!NOTE]
> <span data-ttu-id="420ab-114">Kullanıcı örnekleri, kendi bilgisayarlarında zaten yönetici olan kullanıcılar veya birden çok veritabanı kullanıcılarını içeren senaryolar için gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="420ab-114">User instances are not needed for users who are already administrators on their own computers, or for scenarios involving multiple database users.</span></span>  
  
## <a name="enabling-user-instances"></a><span data-ttu-id="420ab-115">Kullanıcı Örneklerini Etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="420ab-115">Enabling User Instances</span></span>  
 <span data-ttu-id="420ab-116">Kullanıcı örnekleri oluşturmak için, SQL Server Express'in bir üst örneğinin çalışıyor olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="420ab-116">To generate user instances, a parent instance of SQL Server Express must be running.</span></span> <span data-ttu-id="420ab-117">SQL Server Express yüklendiğinde kullanıcı örnekleri varsayılan olarak etkinleştirilir ve üst örnekte **sp_configure** sistem depolanan yordamı yürüten bir sistem yöneticisi tarafından açıkça etkinleştirilebilir veya devre dışı bırakılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-117">User instances are enabled by default when SQL Server Express is installed, and they can be explicitly enabled or disabled by a system administrator executing the **sp_configure** system stored procedure on the parent instance.</span></span>  
  
```sql  
-- Enable user instances.  
sp_configure 'user instances enabled','1'
  
-- Disable user instances.  
sp_configure 'user instances enabled','0'  
```  
  
 <span data-ttu-id="420ab-118">Kullanıcı örnekleri için ağ protokolü yerel Adlandırılmış Borular olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-118">The network protocol for user instances must be local Named Pipes.</span></span> <span data-ttu-id="420ab-119">Bir kullanıcı örneği SQL Server'ın uzak bir örneğinde başlatılamaz ve SQL Server girişlerine izin verilmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-119">A user instance cannot be started on a remote instance of SQL Server, and SQL Server logins are not allowed.</span></span>  
  
## <a name="connecting-to-a-user-instance"></a><span data-ttu-id="420ab-120">Kullanıcı Örneğine Bağlanma</span><span class="sxs-lookup"><span data-stu-id="420ab-120">Connecting to a User Instance</span></span>  
 <span data-ttu-id="420ab-121">Anahtar `User Instance` `AttachDBFilename` <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A> kelimeler ve <xref:System.Data.SqlClient.SqlConnection> anahtar kelimeler, a'nın bir kullanıcı örneğine bağlanmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="420ab-121">The `User Instance` and `AttachDBFilename`<xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A> keywords allow a <xref:System.Data.SqlClient.SqlConnection> to connect to a user instance.</span></span> <span data-ttu-id="420ab-122">Kullanıcı örnekleri de ve <xref:System.Data.SqlClient.SqlConnectionStringBuilder> `UserInstance` `AttachDBFilename` özellikleri tarafından desteklenir.</span><span class="sxs-lookup"><span data-stu-id="420ab-122">User instances are also supported by the <xref:System.Data.SqlClient.SqlConnectionStringBuilder>`UserInstance` and `AttachDBFilename` properties.</span></span>  
  
 <span data-ttu-id="420ab-123">Aşağıda gösterilen örnek bağlantı dizesi hakkında aşağıdakileri not edin:</span><span class="sxs-lookup"><span data-stu-id="420ab-123">Note the following about the sample connection string shown below:</span></span>  
  
- <span data-ttu-id="420ab-124">Anahtar `Data Source` kelime, kullanıcı örneğini oluşturan SQL Server Express'in üst örneğine başvurur.</span><span class="sxs-lookup"><span data-stu-id="420ab-124">The `Data Source` keyword refers to the parent instance of SQL Server Express that is generating the user instance.</span></span> <span data-ttu-id="420ab-125">Varsayılan örnek .\sqlexpress'tir.</span><span class="sxs-lookup"><span data-stu-id="420ab-125">The default instance is .\sqlexpress.</span></span>  
  
- <span data-ttu-id="420ab-126">`Integrated Security`olarak `true`ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-126">`Integrated Security` is set to `true`.</span></span> <span data-ttu-id="420ab-127">Bir kullanıcı örneğine bağlanmak için Windows Kimlik Doğrulama'sı gereklidir; SQL Server girişleri desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-127">To connect to a user instance, Windows Authentication is required; SQL Server logins are not supported.</span></span>  
  
- <span data-ttu-id="420ab-128">Kullanıcı `User Instance` örneğini `true`çağıran , olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-128">The `User Instance` is set to `true`, which invokes a user instance.</span></span> <span data-ttu-id="420ab-129">(Varsayılan dır `false`.)</span><span class="sxs-lookup"><span data-stu-id="420ab-129">(The default is `false`.)</span></span>  
  
- <span data-ttu-id="420ab-130">`AttachDbFileName` Bağlantı dizesi anahtar kelimesi, tam yol adını içermesi gereken birincil veritabanı dosyasını (.mdf) eklemek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-130">The `AttachDbFileName` connection string keyword is used to attach the primary database file (.mdf), which must include the full path name.</span></span> <span data-ttu-id="420ab-131">`AttachDbFileName`ayrıca bağlantı <xref:System.Data.SqlClient.SqlConnection> dizesi içindeki "genişletilmiş özellikler" ve "ilk dosya adı" tuşlarına karşılık gelir.</span><span class="sxs-lookup"><span data-stu-id="420ab-131">`AttachDbFileName` also corresponds to the "extended properties" and "initial file name" keys within a <xref:System.Data.SqlClient.SqlConnection> connection string.</span></span>  
  
- <span data-ttu-id="420ab-132">Boru `|DataDirectory|` sembollerine ekteki ikame dizesi, bağlantıyı açan uygulamanın veri dizinini ifade eder ve .mdf ve .ldf veritabanı ve günlük dosyalarının konumunu gösteren göreli bir yol sağlar.</span><span class="sxs-lookup"><span data-stu-id="420ab-132">The `|DataDirectory|` substitution string enclosed in the pipe symbols refers to the data directory of the application opening the connection and provides a relative path indicating the location of the .mdf and .ldf database and log files.</span></span> <span data-ttu-id="420ab-133">Bu dosyaları başka bir yerde bulmak istiyorsanız, dosyalara tam yol sağlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="420ab-133">If you want to locate these files elsewhere, you must provide the full path to the files.</span></span>  
  
```text
Data Source=.\\SQLExpress;Integrated Security=true;  
User Instance=true;AttachDBFilename=|DataDirectory|\InstanceDB.mdf;  
Initial Catalog=InstanceDB;  
```  
  
> [!NOTE]
> <span data-ttu-id="420ab-134">Çalışma zamanında bir <xref:System.Data.SqlClient.SqlConnectionStringBuilder> <xref:System.Data.SqlClient.SqlConnectionStringBuilder.UserInstance%2A> <xref:System.Data.SqlClient.SqlConnectionStringBuilder.AttachDBFilename%2A> bağlantı dizesi oluşturmak için ve özellikleri de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="420ab-134">You can also use the <xref:System.Data.SqlClient.SqlConnectionStringBuilder><xref:System.Data.SqlClient.SqlConnectionStringBuilder.UserInstance%2A> and <xref:System.Data.SqlClient.SqlConnectionStringBuilder.AttachDBFilename%2A> properties to build a connection string at run time.</span></span>  
  
### <a name="using-the-124datadirectory124-substitution-string"></a><span data-ttu-id="420ab-135">&#124;DataDirectory&#124; Değiştirme Dizesini Kullanma</span><span class="sxs-lookup"><span data-stu-id="420ab-135">Using the &#124;DataDirectory&#124; Substitution String</span></span>  
 <span data-ttu-id="420ab-136">`AttachDbFileName`(boru sembolleriyle içlenmiş) ikame `|DataDirectory|` dizesi girişi ile 2.0 ADO.NET uzatıldı.</span><span class="sxs-lookup"><span data-stu-id="420ab-136">`AttachDbFileName` was extended in ADO.NET 2.0 with the introduction of the `|DataDirectory|` (enclosed in pipe symbols) substitution string.</span></span> <span data-ttu-id="420ab-137">`DataDirectory`bir veri dosyasına göreli bir yol belirtmek `AttachDbFileName` için birlikte kullanılır, geliştiricilerin tam bir yol belirtmek için gerekli olmak yerine veri kaynağına göreli bir yolu temel alan bağlantı dizeleri oluşturmak için izin.</span><span class="sxs-lookup"><span data-stu-id="420ab-137">`DataDirectory` is used in conjunction with `AttachDbFileName` to indicate a relative path to a data file, allowing developers to create connection strings that are based on a relative path to the data source instead of being required to specify a full path.</span></span>  
  
 <span data-ttu-id="420ab-138">Noktayı işaret `DataDirectory` eden fiziksel konum, uygulamanın türüne bağlıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-138">The physical location that `DataDirectory` points to depends on the type of application.</span></span> <span data-ttu-id="420ab-139">Bu örnekte, eklenecek Northwind.mdf dosyası uygulamanın \app_data klasöründe yer alır.</span><span class="sxs-lookup"><span data-stu-id="420ab-139">In this example, the Northwind.mdf file to be attached is located in the application's \app_data folder.</span></span>  
  
```text
Data Source=.\\SQLExpress;Integrated Security=true;  
User Instance=true;  
AttachDBFilename=|DataDirectory|\app_data\Northwind.mdf;  
Initial Catalog=Northwind;  
```  
  
 <span data-ttu-id="420ab-140">Kullanıldığında, `DataDirectory` elde edilen dosya yolu dizin yapısında ikame dizesinin işaret ettiğinden daha yüksek olamaz.</span><span class="sxs-lookup"><span data-stu-id="420ab-140">When `DataDirectory` is used, the resulting file path cannot be higher in the directory structure than the directory pointed to by the substitution string.</span></span> <span data-ttu-id="420ab-141">Örneğin, tamamen genişletilmiş `DataDirectory` C:\AppDirectory\app_data ise, yukarıda gösterilen örnek bağlantı dizesi çalışır çünkü c:\AppDirectory'nin altındadır.</span><span class="sxs-lookup"><span data-stu-id="420ab-141">For example, if the fully expanded `DataDirectory` is C:\AppDirectory\app_data, then the sample connection string shown above works because it is below c:\AppDirectory.</span></span> <span data-ttu-id="420ab-142">Ancak, \veri `DataDirectory` \AppDirectory bir alt dizini olmadığından bir hata neden olur olarak `|DataDirectory|\..\data` belirtmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="420ab-142">However, attempting to specify `DataDirectory` as `|DataDirectory|\..\data` will result in an error because \data is not a subdirectory of \AppDirectory.</span></span>  
  
 <span data-ttu-id="420ab-143">Bağlantı dizesi yanlış biçimlendirilmiş bir değiştirme dizesi varsa, bir <xref:System.ArgumentException> atılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-143">If the connection string has an improperly formatted substitution string, an <xref:System.ArgumentException> will be thrown.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="420ab-144"><xref:System.Data.SqlClient>ikame dizelerini yerel bilgisayar dosya sistemine karşı tam yollara giderir.</span><span class="sxs-lookup"><span data-stu-id="420ab-144"><xref:System.Data.SqlClient> resolves the substitution strings into full paths against the local computer file system.</span></span> <span data-ttu-id="420ab-145">Bu nedenle, uzak sunucu, HTTP ve UNC yol adları desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-145">Therefore, remote server, HTTP, and UNC path names are not supported.</span></span> <span data-ttu-id="420ab-146">Sunucu yerel bilgisayarda bulunmuyorsa bağlantı açıldığında bir özel durum atılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-146">An exception is thrown when the connection is opened if the server is not located on the local computer.</span></span>  
  
 <span data-ttu-id="420ab-147"><xref:System.Data.SqlClient.SqlConnection> Açıldığında, varsayılan SQL Server Express örneğinden, arayanın hesabı altında çalışan bir çalışma zamanı başlatılan örneğe yönlendirilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-147">When the <xref:System.Data.SqlClient.SqlConnection> is opened, it is redirected from the default SQL Server Express instance to a run-time initiated instance running under the caller's account.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="420ab-148">Kullanıcı örneklerinin <xref:System.Data.SqlClient.SqlConnection.ConnectionTimeout%2A> yüklenmesi normal örneklerden daha uzun sürebilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-148">It may be necessary to increase the <xref:System.Data.SqlClient.SqlConnection.ConnectionTimeout%2A> value since user instances may take longer to load than regular instances.</span></span>  
  
 <span data-ttu-id="420ab-149">Aşağıdaki kod parçası yeni `SqlConnection`bir açılır, konsol penceresinde bağlantı dizesini görüntüler ve `using` kod bloğundan çıkarken bağlantıyı kapatır.</span><span class="sxs-lookup"><span data-stu-id="420ab-149">The following code fragment opens a new `SqlConnection`, displays the connection string in the console window, and then closes the connection when exiting the `using` code block.</span></span>  
  
```vb  
Private Sub OpenSqlConnection()  
    ' Retrieve the connection string.  
    Dim connectionString As String = GetConnectionString()  
  
    Using connection As New SqlConnection(connectionString)  
        connection.Open()  
        Console.WriteLine("ConnectionString: {0}", _  
           connection.ConnectionString)  
    End Using  
End Sub  
```  
  
```csharp  
private static void OpenSqlConnection()  
{  
    // Retrieve the connection string.  
    string connectionString = GetConnectionString();  
  
    using (SqlConnection connection =
        new SqlConnection(connectionString))  
    {  
        connection.Open();  
        Console.WriteLine("ConnectionString: {0}",
             connection.ConnectionString);  
    }  
}  
```  
  
> [!NOTE]
> <span data-ttu-id="420ab-150">Kullanıcı örnekleri, SQL Server'ın içinde çalışan ortak dil çalışma süresi (CLR) kodunda desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-150">User instances are not supported in common language runtime (CLR) code that is running inside of SQL Server.</span></span> <span data-ttu-id="420ab-151">Bağlantı <xref:System.InvalidOperationException> dizesinde bulunan `Open` <xref:System.Data.SqlClient.SqlConnection> `User Instance=true` bir a'ya çağrılırsa bir atılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-151">An <xref:System.InvalidOperationException> is thrown if `Open` is called on a <xref:System.Data.SqlClient.SqlConnection> that has `User Instance=true` in the connection string.</span></span>  
  
## <a name="lifetime-of-a-user-instance-connection"></a><span data-ttu-id="420ab-152">Kullanıcı Örneği Bağlantısının Kullanım Ömrü</span><span class="sxs-lookup"><span data-stu-id="420ab-152">Lifetime of a User Instance Connection</span></span>  
 <span data-ttu-id="420ab-153">Hizmet olarak çalışan SQL Server sürümlerinin aksine, SQL Server Express örneklerinin el ile başlatılması ve durdurulması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-153">Unlike versions of SQL Server that run as a service, SQL Server Express instances do not need to be manually started and stopped.</span></span> <span data-ttu-id="420ab-154">Bir kullanıcı oturum açıp bir kullanıcı örneğine her bağkuruca, zaten çalışmıyorsa kullanıcı örneği başlatılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-154">Each time a user logs in and connects to a user instance, the user instance is started if it is not already running.</span></span> <span data-ttu-id="420ab-155">Kullanıcı örneği veritabanları, `AutoClose` veritabanının bir hareketsizlik döneminden sonra otomatik olarak kapatılması için seçenek kümesine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="420ab-155">User instance databases have the `AutoClose` option set so that the database is automatically shut down after a period of inactivity.</span></span> <span data-ttu-id="420ab-156">Başlatılan sqlservr.exe işlemi, örneğe son bağlantı kapatıldıktan sonra sınırlı bir zaman aşımı süresi boyunca çalışmaya devam eder, bu nedenle zaman aşımı süresi dolmadan başka bir bağlantı açılırsa yeniden başlatılması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="420ab-156">The sqlservr.exe process that is started is kept running for a limited time-out period after the last connection to the instance is closed, so it does not need to be restarted if another connection is opened before the time-out has expired.</span></span> <span data-ttu-id="420ab-157">Bu zaman aşımı süresi dolmadan önce yeni bir bağlantı açılmazsa kullanıcı örneği otomatik olarak kapanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-157">The user instance automatically shuts down if no new connection opens before that time-out period has expired.</span></span> <span data-ttu-id="420ab-158">Üst örnekteki bir sistem yöneticisi, **kullanıcı örneğinin zaman ayarı** seçeneğini değiştirmek için **sp_configure** kullanarak bir kullanıcı örneğinin zaman ödeme süresinin süresini ayarlayabilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-158">A system administrator on the parent instance can set the duration of the time-out period for a user instance by using **sp_configure** to change the **user instance timeout** option.</span></span> <span data-ttu-id="420ab-159">Varsayılan değer 60 dakikadır.</span><span class="sxs-lookup"><span data-stu-id="420ab-159">The default is 60 minutes.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="420ab-160">`Min Pool Size` Sıfırdan büyük bir değere sahip bağlantı dizesinde kullanılırsa, bağlantı havuzu her zaman birkaç açık bağlantı tutar ve kullanıcı örneği otomatik olarak kapanmaz.</span><span class="sxs-lookup"><span data-stu-id="420ab-160">If `Min Pool Size` is used in the connection string with a value greater than zero, the connection pooler will always maintain a few opened connections, and the user instance will not automatically shut down.</span></span>  
  
## <a name="how-user-instances-work"></a><span data-ttu-id="420ab-161">Kullanıcı Örnekleri Nasıl Çalışır?</span><span class="sxs-lookup"><span data-stu-id="420ab-161">How User Instances Work</span></span>  
 <span data-ttu-id="420ab-162">Her kullanıcı için ilk kez bir kullanıcı örneği oluşturulduğunda, **ana** ve **msdb** sistem veritabanları Şablon Veri klasöründen kullanıcı örneğinin özel kullanımı için kullanıcının yerel uygulama veri deposu dizininin altındaki bir yola kopyalanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-162">The first time a user instance is generated for each user, the **master** and **msdb** system databases are copied from the Template Data folder to a path under the user's local application data repository directory for exclusive use by the user instance.</span></span> <span data-ttu-id="420ab-163">Bu yol genellikle `C:\Documents and Settings\<UserName>\Local Settings\Application Data\Microsoft\Microsoft SQL Server Data\SQLEXPRESS`.</span><span class="sxs-lookup"><span data-stu-id="420ab-163">This path is typically `C:\Documents and Settings\<UserName>\Local Settings\Application Data\Microsoft\Microsoft SQL Server Data\SQLEXPRESS`.</span></span> <span data-ttu-id="420ab-164">Bir kullanıcı örneği başlatıldığında, **tempdb,** günlük ve izleme dosyaları da bu dizine yazılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-164">When a user instance starts up, the **tempdb**, log, and trace files are also written to this directory.</span></span> <span data-ttu-id="420ab-165">Örnek için bir ad oluşturulur ve bu ad her kullanıcı için benzersiz olduğu garanti edilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-165">A name is generated for the instance, which is guaranteed to be unique for each user.</span></span>  
  
 <span data-ttu-id="420ab-166">Varsayılan olarak, Windows Builtin\Users grubunun tüm üyelerine yerel örnekte bağlanma ve SQL Server ikililerinde okuma ve yürütme izinleri verilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-166">By default all members of the Windows Builtin\Users group are granted permissions to connect on the local instance as well as read and execute permissions on the SQL Server binaries.</span></span> <span data-ttu-id="420ab-167">Kullanıcı örneğini barındıran arama yapan kullanıcının kimlik bilgileri doğrulandıktan sonra, bu kullanıcı bu `sysadmin` örnekte olur.</span><span class="sxs-lookup"><span data-stu-id="420ab-167">Once the credentials of the calling user hosting the user instance have been verified, that user becomes the `sysadmin` on that instance.</span></span> <span data-ttu-id="420ab-168">Yalnızca paylaşılan bellek, kullanıcı örnekleri için etkinleştirilir, bu da yalnızca yerel makinedeki işlemlerin mümkün olduğu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="420ab-168">Only shared memory is enabled for user instances, which means that only operations on the local machine are possible.</span></span>  
  
 <span data-ttu-id="420ab-169">Kullanıcılara bağlantı dizesinde belirtilen .mdf ve .ldf dosyalarında okuma ve yazma izinleri verilmelidir.</span><span class="sxs-lookup"><span data-stu-id="420ab-169">Users must be granted both read and write permissions on the .mdf and .ldf files specified in the connection string.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="420ab-170">.mdf ve .ldf dosyaları sırasıyla veritabanını ve günlük dosyalarını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="420ab-170">The .mdf and .ldf files represent the database and log files, respectively.</span></span> <span data-ttu-id="420ab-171">Bu iki dosya eşleşen bir kümedir, bu nedenle yedekleme ve geri yükleme işlemleri sırasında dikkatli olunmalıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-171">These two files are a matched set, so care must be taken during backup and restore operations.</span></span> <span data-ttu-id="420ab-172">Veritabanı dosyası günlük dosyasının tam sürümü hakkında bilgi içerir ve yanlış günlük dosyası ile birleştiğinde veritabanı açılmaz.</span><span class="sxs-lookup"><span data-stu-id="420ab-172">The database file contains information about the exact version of the log file, and the database will not open if it is coupled with the wrong log file.</span></span>  
  
 <span data-ttu-id="420ab-173">Veri bozulmasını önlemek için, kullanıcı örneğinde özel erişimle bir veritabanı açılır.</span><span class="sxs-lookup"><span data-stu-id="420ab-173">To avoid data corruption, a database in the user instance is opened with exclusive access.</span></span> <span data-ttu-id="420ab-174">İki farklı kullanıcı örneği aynı bilgisayarda aynı veritabanını paylaşıyorsa, ikinci örnekte açılabilmesi için ilk örnekteki kullanıcının veritabanını kapatması gerekir.</span><span class="sxs-lookup"><span data-stu-id="420ab-174">If two different user instances share the same database on the same computer, the user on the first instance must close the database before it can be opened in a second instance.</span></span>  
  
## <a name="user-instance-scenarios"></a><span data-ttu-id="420ab-175">Kullanıcı Örneği Senaryoları</span><span class="sxs-lookup"><span data-stu-id="420ab-175">User Instance Scenarios</span></span>  
 <span data-ttu-id="420ab-176">Kullanıcı örnekleri, veritabanı uygulamalarının geliştiricileringeliştirme bilgisayarlarında yönetim hesapları olmasına bağlı olmayan bir SQL Server veri deposu sağlar.</span><span class="sxs-lookup"><span data-stu-id="420ab-176">User instances provide developers of database applications with a SQL Server data store that does not depend on developers having administrative accounts on their development computers.</span></span> <span data-ttu-id="420ab-177">Kullanıcı örnekleri, veritabanı uygulamasının yalnızca bir dosyaya bağlandığı Access/Jet modeline dayanır ve kullanıcı, izin vermek için bir sistem yöneticisinin müdahalesine gerek kalmadan tüm veritabanı nesnelerinde otomatik olarak tam izinlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="420ab-177">User instances are based on the Access/Jet model, where the database application simply connects to a file, and the user automatically has full permissions on all of the database objects without needing the intervention of a system administrator to grant permissions.</span></span> <span data-ttu-id="420ab-178">Kullanıcının en az ayrıcalıklı bir kullanıcı hesabı (LUA) altında çalıştığı ve sunucu veya yerel makinede yönetim ayrıcalıkları olmadığı, ancak veritabanı nesneleri ve uygulamaları oluşturması gereken durumlarda çalışması amaçlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="420ab-178">It is intended to work in situations where the user is running under a least-privilege user account (LUA) and does not have administrative privileges on the server or local machine, yet needs to create database objects and applications.</span></span> <span data-ttu-id="420ab-179">Kullanıcı örnekleri, kullanıcıların daha ayrıcalıklı bir sistem hizmetinin güvenlik bağlamında değil, kullanıcının kendi güvenlik bağlamı altında çalışan çalışma zamanında örnekler oluşturmasına olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="420ab-179">User instances allow users to create instances at run time that run under the user's own security context, and not in the security context of a more privileged system service.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="420ab-180">Kullanıcı örnekleri yalnızca onu kullanan tüm uygulamaların tam olarak güvenildiği senaryolarda kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-180">User instances should only be used in scenarios where all the applications using it are fully trusted.</span></span>  
  
 <span data-ttu-id="420ab-181">Kullanıcı örneği senaryoları şunlardır:</span><span class="sxs-lookup"><span data-stu-id="420ab-181">User instance scenarios include:</span></span>  
  
- <span data-ttu-id="420ab-182">Veri paylaşımının gerekli olmadığı tek kullanıcılı uygulamalar.</span><span class="sxs-lookup"><span data-stu-id="420ab-182">Any single-user application where sharing data is not required.</span></span>  
  
- <span data-ttu-id="420ab-183">ClickOnce dağıtım.</span><span class="sxs-lookup"><span data-stu-id="420ab-183">ClickOnce deployment.</span></span> <span data-ttu-id="420ab-184">Hedef bilgisayara .NET Framework 2.0 (veya daha sonra) ve SQL Server Express zaten yüklüyse, ClickOnce işlemi sonucunda indirilen yükleme paketi yönetici olmayan kullanıcılar tarafından yüklenebilir ve kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-184">If .NET Framework 2.0 (or later) and SQL Server Express are already installed on the target computer, the installation package downloaded as a result of a ClickOnce action can be installed and used by non-administrator users.</span></span> <span data-ttu-id="420ab-185">Kurulumun bir parçası ysa, bir yöneticinin SQL Server Express'i yüklemesi gerektiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="420ab-185">Note that an administrator must install SQL Server Express if that is part of the setup.</span></span> <span data-ttu-id="420ab-186">Daha fazla bilgi [için Windows Formlar için ClickOnce Dağıtım'a](../../../winforms/clickonce-deployment-for-windows-forms.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="420ab-186">For more information, see [ClickOnce Deployment for Windows Forms](../../../winforms/clickonce-deployment-for-windows-forms.md).</span></span>
  
- <span data-ttu-id="420ab-187">Windows Kimlik Doğrulaması kullanarak barındırmaya adanmış ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="420ab-187">Dedicated ASP.NET hosting using Windows Authentication.</span></span> <span data-ttu-id="420ab-188">Tek bir SQL Server Express örneği bir intranet üzerinde barındırılabilir.</span><span class="sxs-lookup"><span data-stu-id="420ab-188">A single SQL Server Express instance can be hosted on an intranet.</span></span> <span data-ttu-id="420ab-189">Uygulama, kimliğe bürünme kullanarak değil, ASPNET Windows hesabını kullanarak bağlanır.</span><span class="sxs-lookup"><span data-stu-id="420ab-189">The application connects using the ASPNET Windows account, not by using impersonation.</span></span> <span data-ttu-id="420ab-190">Kullanıcı örnekleri, tüm uygulamaların aynı kullanıcı örneğini paylaşacağı ve artık birbirinden izole edilmeyeceğini niçin üçüncü taraf veya paylaşılan barındırma senaryoları için kullanılmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="420ab-190">User instances should not be used for third-party or shared hosting scenarios where all applications would share the same user instance and would no longer remain isolated from each other.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="420ab-191">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="420ab-191">See also</span></span>

- [<span data-ttu-id="420ab-192">SQL Server ve ADO.NET</span><span class="sxs-lookup"><span data-stu-id="420ab-192">SQL Server and ADO.NET</span></span>](index.md)
- [<span data-ttu-id="420ab-193">Bağlantı Dizeleri</span><span class="sxs-lookup"><span data-stu-id="420ab-193">Connection Strings</span></span>](../connection-strings.md)
- [<span data-ttu-id="420ab-194">Veri Kaynağına Bağlanma</span><span class="sxs-lookup"><span data-stu-id="420ab-194">Connecting to a Data Source</span></span>](../connecting-to-a-data-source.md)
- [<span data-ttu-id="420ab-195">ADO.NET’e Genel Bakış</span><span class="sxs-lookup"><span data-stu-id="420ab-195">ADO.NET Overview</span></span>](../ado-net-overview.md)
