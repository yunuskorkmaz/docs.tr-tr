---
title: SQL Server'da Anlık Görüntü Yalıtımı
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 43ae5dd3-50f5-43a8-8d01-e37a61664176
ms.openlocfilehash: 8313ffc8eef70c1e5efc24b09a160edb7cec1595
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79174270"
---
# <a name="snapshot-isolation-in-sql-server"></a><span data-ttu-id="ce7f0-102">SQL Server'da Anlık Görüntü Yalıtımı</span><span class="sxs-lookup"><span data-stu-id="ce7f0-102">Snapshot Isolation in SQL Server</span></span>
<span data-ttu-id="ce7f0-103">Anlık görüntü yalıtımı OLTP uygulamaları için eşzamanlılığı artırır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-103">Snapshot isolation enhances concurrency for OLTP applications.</span></span>  
  
## <a name="understanding-snapshot-isolation-and-row-versioning"></a><span data-ttu-id="ce7f0-104">Anlık Görüntü Yalıtımı ve Satır Sürümünü Anlama</span><span class="sxs-lookup"><span data-stu-id="ce7f0-104">Understanding Snapshot Isolation and Row Versioning</span></span>  
 <span data-ttu-id="ce7f0-105">Anlık görüntü yalıtımı etkinleştirildikten sonra, her işlem için güncelleştirilmiş satır sürümleri korunmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-105">Once snapshot isolation is enabled, updated row versions for each transaction must be maintained.</span></span>  <span data-ttu-id="ce7f0-106">SQL Server 2019'dan önce bu sürümler **tempdb'de**depolandı.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-106">Prior to SQL Server 2019, these versions were stored in **tempdb**.</span></span> <span data-ttu-id="ce7f0-107">SQL Server 2019, kendi satır sürümlerini gerektiren hızlandırılmış Veritabanı Kurtarma (ADR) adlı yeni bir özelliği tanıttı.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-107">SQL Server 2019 introduces a new feature, Accelerated Database Recovery (ADR) which requires its own set of row versions.</span></span>  <span data-ttu-id="ce7f0-108">Bu nedenle, SQL Server 2019 itibariyle, ADR etkin değilse, satır sürümleri her zaman olduğu gibi **tempdb** tutulur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-108">So, as of SQL Server 2019, if ADR is not enabled, row versions are kept in **tempdb** as always.</span></span>  <span data-ttu-id="ce7f0-109">ADR etkinse, her ikisi de anlık görüntü yalıtımı ve ADR ile ilgili tüm satır sürümleri, kullanıcının belirttiği bir dosya grubunda kullanıcı veritabanında bulunan ADR'nin Kalıcı Sürüm Deposu'nda (PVS) tutulur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-109">If ADR is enabled, then all row versions, both related to snapshot isolation and ADR, are kept in ADR's Persistent Version Store (PVS), which is located in the user database in a filegroup which the user specifies.</span></span> <span data-ttu-id="ce7f0-110">Benzersiz bir işlem sırası numarası her hareketi tanımlar ve bu benzersiz sayılar her satır sürümü için kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-110">A unique transaction sequence number identifies each transaction, and these unique numbers are recorded for each row version.</span></span> <span data-ttu-id="ce7f0-111">Hareket, hareketin sıra numarasından önce sıra numarasına sahip en son satır sürümleriyle çalışır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-111">The transaction works with the most recent row versions having a sequence number before the sequence number of the transaction.</span></span> <span data-ttu-id="ce7f0-112">Hareket başladıktan sonra oluşturulan yeni satır sürümleri hareket tarafından yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-112">Newer row versions created after the transaction has begun are ignored by the transaction.</span></span>  
  
 <span data-ttu-id="ce7f0-113">"Anlık görüntü" terimi, işlemdeki tüm sorguların, işlemin başladığı anda veritabanının durumuna bağlı olarak veritabanının aynı sürümünü veya anlık görüntüsünü gördüğü gerçeğini yansıtır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-113">The term "snapshot" reflects the fact that all queries in the transaction see the same version, or snapshot, of the database, based on the state of the database at the moment in time when the transaction begins.</span></span> <span data-ttu-id="ce7f0-114">Anlık görüntü hareketindeki temel veri satırlarında veya veri sayfalarında kilit alınmaz ve bu da diğer işlemlerin daha önce tamamlanmamış bir işlem tarafından engellenmeden yürütülmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-114">No locks are acquired on the underlying data rows or data pages in a snapshot transaction, which permits other transactions to execute without being blocked by a prior uncompleted transaction.</span></span> <span data-ttu-id="ce7f0-115">Verileri değiştiren hareketler, verileri okuyan hareketleri engellemez ve verileri okuyan hareketler, normalde SQL Server'da varsayılan READ COMMITTED yalıtım düzeyi altında olacağı için veri yazan hareketleri engellemez.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-115">Transactions that modify data do not block transactions that read data, and transactions that read data do not block transactions that write data, as they normally would under the default READ COMMITTED isolation level in SQL Server.</span></span> <span data-ttu-id="ce7f0-116">Bu engellemeolmayan davranış, karmaşık hareketler için kilitlenme olasılığını da önemli ölçüde azaltır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-116">This non-blocking behavior also significantly reduces the likelihood of deadlocks for complex transactions.</span></span>  
  
 <span data-ttu-id="ce7f0-117">Anlık görüntü yalıtımı iyimser bir eşzamanlılık modeli kullanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-117">Snapshot isolation uses an optimistic concurrency model.</span></span> <span data-ttu-id="ce7f0-118">Anlık görüntü hareketi, işlem başladığından beri değişen verilerde değişiklik yapmaya çalışırsa, hareket geri döner ve bir hata yükselir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-118">If a snapshot transaction attempts to commit modifications to data that has changed since the transaction began, the transaction will roll back and an error will be raised.</span></span> <span data-ttu-id="ce7f0-119">Verilere erişim sağlayan SELECT ifadeleri için UPDLOCK ipuçlarını kullanarak bunu önleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-119">You can avoid this by using UPDLOCK hints for SELECT statements that access data to be modified.</span></span> <span data-ttu-id="ce7f0-120">Daha fazla bilgi için SQL Server Books Online'da "İpuçlarını Kilitleme" başlıklı bilgi için bkz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-120">See "Locking Hints" in SQL Server Books Online for more information.</span></span>  
  
 <span data-ttu-id="ce7f0-121">Anlık görüntü yalıtımı, işlemlerde kullanılmadan önce ALLOW_SNAPSHOT_ISOLATION ON veritabanı seçeneği ayarlayarak etkinleştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-121">Snapshot isolation must be enabled by setting the ALLOW_SNAPSHOT_ISOLATION ON database option before it is used in transactions.</span></span> <span data-ttu-id="ce7f0-122">Bu, satır sürümlerini geçici veritabanında depolama mekanizmasını etkinleştirir **(tempdb).**</span><span class="sxs-lookup"><span data-stu-id="ce7f0-122">This activates the mechanism for storing row versions in the temporary database (**tempdb**).</span></span> <span data-ttu-id="ce7f0-123">Transact-SQL ALTER DATABASE deyimiyle kullanan her veritabanında anlık görüntü yalıtımını etkinleştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-123">You must enable snapshot isolation in each database that uses it with the Transact-SQL ALTER DATABASE statement.</span></span> <span data-ttu-id="ce7f0-124">Bu bağlamda, anlık görüntü yalıtımı, yapılandırma gerektirmeyen READ COMMITTED, REPEATABLE READ, SERIALIZABLE ve READ UNCOMMITTED'un geleneksel yalıtım düzeylerinden farklıdır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-124">In this respect, snapshot isolation differs from the traditional isolation levels of READ COMMITTED, REPEATABLE READ, SERIALIZABLE, and READ UNCOMMITTED, which require no configuration.</span></span> <span data-ttu-id="ce7f0-125">Aşağıdaki ifadeler anlık görüntü yalıtımını etkinleştirir ve varsayılan READ COMMITTED davranışını SNAPSHOT ile değiştirir:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-125">The following statements activate snapshot isolation and replace the default READ COMMITTED behavior with SNAPSHOT:</span></span>  
  
```sql  
ALTER DATABASE MyDatabase  
SET ALLOW_SNAPSHOT_ISOLATION ON  
  
ALTER DATABASE MyDatabase  
SET READ_COMMITTED_SNAPSHOT ON  
```  
  
 <span data-ttu-id="ce7f0-126">READ_COMMITTED_SNAPSHOT ON seçeneğinin ayarlanması, varsayılan READ COMMITTED yalıtım düzeyi altında sürümlenmiş satırlara erişim sağlar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-126">Setting the READ_COMMITTED_SNAPSHOT ON option allows access to versioned rows under the default READ COMMITTED isolation level.</span></span> <span data-ttu-id="ce7f0-127">READ_COMMITTED_SNAPSHOT seçeneği KAPALI olarak ayarlanmışsa, sürümlü satırlara erişmek için her oturum için Anlık Görüntü yalıtım düzeyini açıkça ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-127">If the READ_COMMITTED_SNAPSHOT option is set to OFF, you must explicitly set the Snapshot isolation level for each session in order to access versioned rows.</span></span>  
  
## <a name="managing-concurrency-with-isolation-levels"></a><span data-ttu-id="ce7f0-128">Yalıtım Düzeyleri ile Eşzamanlılık Yönetimi</span><span class="sxs-lookup"><span data-stu-id="ce7f0-128">Managing Concurrency with Isolation Levels</span></span>  
 <span data-ttu-id="ce7f0-129">Transact-SQL deyiminin yürütüldettiği yalıtım düzeyi, kilitleme ve satır sürüm davranışını belirler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-129">The isolation level under which a Transact-SQL statement executes determines its locking and row versioning behavior.</span></span> <span data-ttu-id="ce7f0-130">İzolasyon düzeyi bağlantı geniş bir kapsama sahiptir ve SET Hareket İzOLASYON DÜZEYİ deyimiyle bağlantı için ayarlandıktan sonra, bağlantı kapanana veya başka bir yalıtım düzeyi ayarlanana kadar etkin kalır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-130">An isolation level has connection-wide scope, and once set for a connection with the SET TRANSACTION ISOLATION LEVEL statement, it remains in effect until the connection is closed or another isolation level is set.</span></span> <span data-ttu-id="ce7f0-131">Bir bağlantı kapatıldığında ve havuza döndürüldüğünde, son SET İşlem İzOLASYON DÜZEYİ deyiminden yalıtım düzeyi korunur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-131">When a connection is closed and returned to the pool, the isolation level from the last SET TRANSACTION ISOLATION LEVEL statement is retained.</span></span> <span data-ttu-id="ce7f0-132">Birleştirilmiş bağlantıyı yeniden kullanan sonraki bağlantılar, bağlantının biraraya bağlandığının etkisinde olan yalıtım düzeyini kullanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-132">Subsequent connections reusing a pooled connection use the isolation level that was in effect at the time the connection is pooled.</span></span>  
  
 <span data-ttu-id="ce7f0-133">Bağlantı içinde verilen tek tek sorgular, tek bir deyim veya hareket için yalıtımı değiştiren ancak bağlantının yalıtım düzeyini etkilemeyen kilit ipuçları içerebilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-133">Individual queries issued within a connection can contain lock hints that modify the isolation for a single statement or transaction but do not affect the isolation level of the connection.</span></span> <span data-ttu-id="ce7f0-134">Depolanan yordamlar veya işlevlerde ayarlanan yalıtım düzeyleri veya kilit ipuçları, onları çağıran bağlantının yalıtım düzeyini değiştirmez ve yalnızca depolanan yordam veya işlev çağrısı süresince etkindir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-134">Isolation levels or lock hints set in stored procedures or functions do not change the isolation level of the connection that calls them and are in effect only for the duration of the stored procedure or function call.</span></span>  
  
 <span data-ttu-id="ce7f0-135">SQL-92 standardında tanımlanan dört yalıtım düzeyi, SQL Server'ın ilk sürümlerinde desteklenmiştir:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-135">Four isolation levels defined in the SQL-92 standard were supported in early versions of SQL Server:</span></span>  
  
- <span data-ttu-id="ce7f0-136">READ UNCOMMITTED, diğer hareketler tarafından yerleştirilen kilitleri yoksaydığından en az kısıtlayıcı yalıtım düzeyidir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-136">READ UNCOMMITTED is the least restrictive isolation level because it ignores locks placed by other transactions.</span></span> <span data-ttu-id="ce7f0-137">READ UNCOMMITTED altında gerçekleştirilen işlemler, henüz diğer işlemler tarafından işlenmemiş değiştirilmiş veri değerlerini okuyabilir; bunlara "kirli" okur denir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-137">Transactions executing under READ UNCOMMITTED can read modified data values that have not yet been committed by other transactions; these are called "dirty" reads.</span></span>  
  
- <span data-ttu-id="ce7f0-138">READ COMMITTED, SQL Server için varsayılan yalıtım düzeyidir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-138">READ COMMITTED is the default isolation level for SQL Server.</span></span> <span data-ttu-id="ce7f0-139">Deyimlerin değiştirilen ancak diğer işlemler tarafından henüz işlenemeyen veri değerlerini okuyamadığını belirterek kirli okumaları önler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-139">It prevents dirty reads by specifying that statements cannot read data values that have been modified but not yet committed by other transactions.</span></span> <span data-ttu-id="ce7f0-140">Diğer işlemler, geçerli hareket içindeki tek tek ifadelerin yürütülmesi arasındaki verileri değiştirmeye, eklemeye veya silmeye devam ederek yinelenemeyen okumalarla veya "hayali" verilerle sonuçlanabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-140">Other transactions can still modify, insert, or delete data between executions of individual statements within the current transaction, resulting in non-repeatable reads, or "phantom" data.</span></span>  
  
- <span data-ttu-id="ce7f0-141">TEKRAR EDIleBilen OKUMA, READ COMMITTED'dan daha kısıtlayıcı bir izolasyon düzeyidir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-141">REPEATABLE READ is a more restrictive isolation level than READ COMMITTED.</span></span> <span data-ttu-id="ce7f0-142">READ COMMITTED'ı kapsar ve ayrıca geçerli işlem işleyene kadar geçerli hareket tarafından okunan verileri değiştiremez veya silemez.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-142">It encompasses READ COMMITTED and additionally specifies that no other transactions can modify or delete data that has been read by the current transaction until the current transaction commits.</span></span> <span data-ttu-id="ce7f0-143">Her bildirimin sonunda yayımlanacak işlem süresi boyunca paylaşılan kilitler tutulduğundan, eşzamanlılık READ COMMITTED'a göre daha düşüktür.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-143">Concurrency is lower than for READ COMMITTED because shared locks on read data are held for the duration of the transaction instead of being released at the end of each statement.</span></span>  
  
- <span data-ttu-id="ce7f0-144">SERIALIZABLE en kısıtlayıcı yalıtım düzeyidir, çünkü tüm anahtar aralıklarını kilitler ve işlem tamamlanana kadar kilitleri tutar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-144">SERIALIZABLE is the most restrictive isolation level, because it locks entire ranges of keys and holds the locks until the transaction is complete.</span></span> <span data-ttu-id="ce7f0-145">TEKRARLANABILIR OKUMA'yı kapsar ve hareket tamamlanana kadar diğer işlemlerin hareket tarafından okunan aralıklara yeni satırlar ekleyememesi kısıtlamasını ekler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-145">It encompasses REPEATABLE READ and adds the restriction that other transactions cannot insert new rows into ranges that have been read by the transaction until the transaction is complete.</span></span>  
  
 <span data-ttu-id="ce7f0-146">Daha fazla bilgi için [İşlem Kilitleme ve Satır Sürüm Kılavuzu'na](/sql/relational-databases/sql-server-transaction-locking-and-row-versioning-guide)bakın.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-146">For more information, refer to the [Transaction Locking and Row Versioning Guide](/sql/relational-databases/sql-server-transaction-locking-and-row-versioning-guide).</span></span>  
  
### <a name="snapshot-isolation-level-extensions"></a><span data-ttu-id="ce7f0-147">Anlık Görüntü Yalıtım Düzey Uzantıları</span><span class="sxs-lookup"><span data-stu-id="ce7f0-147">Snapshot Isolation Level Extensions</span></span>  
 <span data-ttu-id="ce7f0-148">SQL Server, SNAPSHOT yalıtım düzeyinin getirilmesi ve READ COMMITTED'ın ek bir uygulaması ile SQL-92 yalıtım düzeylerine uzantılar getirmiştir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-148">SQL Server introduced extensions to the SQL-92 isolation levels with the introduction of the SNAPSHOT isolation level and an additional implementation of READ COMMITTED.</span></span> <span data-ttu-id="ce7f0-149">READ_COMMITTED_SNAPSHOT yalıtım düzeyi, tüm işlemler için READ COMMITTED'ın yerini saydam olarak alabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-149">The READ_COMMITTED_SNAPSHOT isolation level can transparently replace READ COMMITTED for all transactions.</span></span>  
  
- <span data-ttu-id="ce7f0-150">SNAPSHOT yalıtımı, bir hareket içinde okunan verilerin diğer eşzamanlı hareketler tarafından yapılan değişiklikleri asla yansıtmayacağını belirtir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-150">SNAPSHOT isolation specifies that data read within a transaction will never reflect changes made by other simultaneous transactions.</span></span> <span data-ttu-id="ce7f0-151">Hareket, hareket başladığında var olan veri satırı sürümlerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-151">The transaction uses the data row versions that exist when the transaction begins.</span></span> <span data-ttu-id="ce7f0-152">Okunduğunda verilere kilit yerleştirilmez, bu nedenle SNAPSHOT hareketleri diğer işlemlerin veri yazılmasına engel olmaz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-152">No locks are placed on the data when it is read, so SNAPSHOT transactions do not block other transactions from writing data.</span></span> <span data-ttu-id="ce7f0-153">Veri yazan hareketler anlık görüntü hareketlerinin veri okumalarını engellemez.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-153">Transactions that write data do not block snapshot transactions from reading data.</span></span> <span data-ttu-id="ce7f0-154">Kullanmak için ALLOW_SNAPSHOT_ISOLATION veritabanı seçeneğini ayarlayarak anlık görüntü yalıtımını etkinleştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-154">You need to enable snapshot isolation by setting the ALLOW_SNAPSHOT_ISOLATION database option in order to use it.</span></span>  
  
- <span data-ttu-id="ce7f0-155">VeritabanıREAD_COMMITTED_SNAPSHOT seçeneği, veritabanında anlık görüntü yalıtımı etkinleştirildiğinde varsayılan READ COMMITTED yalıtım düzeyinin davranışını belirler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-155">The READ_COMMITTED_SNAPSHOT database option determines the behavior of the default READ COMMITTED isolation level when snapshot isolation is enabled in a database.</span></span> <span data-ttu-id="ce7f0-156">READ_COMMITTED_SNAPSHOT A'yı açıkça belirtmezseniz, READ COMMITTED tüm örtülü işlemlere uygulanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-156">If you do not explicitly specify READ_COMMITTED_SNAPSHOT ON, READ COMMITTED is applied to all implicit transactions.</span></span> <span data-ttu-id="ce7f0-157">Bu, READ_COMMITTED_SNAPSHOT OFF (varsayılan) olarak aynı davranışı üretir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-157">This produces the same behavior as setting READ_COMMITTED_SNAPSHOT OFF (the default).</span></span> <span data-ttu-id="ce7f0-158">READ_COMMITTED_SNAPSHOT KAPAİn yürürlüğe girdiğinde, Veritabanı Altyapısı varsayılan yalıtım düzeyini zorlamak için paylaşılan kilitleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-158">When READ_COMMITTED_SNAPSHOT OFF is in effect, the Database Engine uses shared locks to enforce the default isolation level.</span></span> <span data-ttu-id="ce7f0-159">READ_COMMITTED_SNAPSHOT veritabanı seçeneğini ON olarak ayarlarsanız, veritabanı altyapısı verileri korumak için kilitkullanmak yerine satır sürüm ve anlık görüntü yalıtımını varsayılan olarak kullanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-159">If you set the READ_COMMITTED_SNAPSHOT database option to ON, the database engine uses row versioning and snapshot isolation as the default, instead of using locks to protect the data.</span></span>  
  
## <a name="how-snapshot-isolation-and-row-versioning-work"></a><span data-ttu-id="ce7f0-160">Anlık Görüntü Yalıtımı ve Satır Sürümleme Nasıl Çalışır?</span><span class="sxs-lookup"><span data-stu-id="ce7f0-160">How Snapshot Isolation and Row Versioning Work</span></span>  
 <span data-ttu-id="ce7f0-161">Anlık görüntü yalıtım düzeyi etkinleştirildiğinde, her satır güncelleştirildiğinde, SQL Server Database Engine orijinal satırın bir kopyasını **tempdb'de**depolar ve satıra bir işlem sıra numarası ekler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-161">When the SNAPSHOT isolation level is enabled, each time a row is updated, the SQL Server Database Engine stores a copy of the original row in **tempdb**, and adds a transaction sequence number to the row.</span></span> <span data-ttu-id="ce7f0-162">Aşağıdaki olaylar dizisi oluşur:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-162">The following is the sequence of events that occurs:</span></span>  
  
- <span data-ttu-id="ce7f0-163">Yeni bir işlem başlatıldı ve bir hareket sıra numarası atanır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-163">A new transaction is initiated, and it is assigned a transaction sequence number.</span></span>  
  
- <span data-ttu-id="ce7f0-164">Veritabanı Altyapısı hareket içindeki bir satırı okur ve sıra numarası işlem sıra numarasına en yakın ve daha düşük olan **tempdb'den** satır sürümünü alır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-164">The Database Engine reads a row within the transaction and retrieves the row version from **tempdb** whose sequence number is closest to, and lower than, the transaction sequence number.</span></span>  
  
- <span data-ttu-id="ce7f0-165">Veritabanı Altyapısı, anlık görüntü hareketi başlatıldığında, işlem sırası numarasının, anlık görüntü hareketi başlatıldığında etkin olan işlem sıra numaraları listesinde olup olmadığını denetler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-165">The Database Engine checks to see if the transaction sequence number is not in the list of transaction sequence numbers of the uncommitted transactions active when the snapshot transaction started.</span></span>  
  
- <span data-ttu-id="ce7f0-166">Hareket, hareketin başlangıcında geçerli olan **tempdb'den** satırın sürümünü okur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-166">The transaction reads the version of the row from **tempdb** that was current as of the start of the transaction.</span></span> <span data-ttu-id="ce7f0-167">Bu sıra numarası değerleri hareket sıra numarasıdeğerinden daha yüksek olacağından, işlem başlatıldıktan sonra eklenen yeni satırları görmez.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-167">It will not see new rows inserted after the transaction was started because those sequence number values will be higher than the value of the transaction sequence number.</span></span>  
  
- <span data-ttu-id="ce7f0-168">**Tempdb'de** daha düşük sıra numarası değerine sahip bir satır sürümü olacağından, geçerli hareket, hareket başladıktan sonra silinen satırları görür.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-168">The current transaction will see rows that were deleted after the transaction began, because there will be a row version in **tempdb** with a lower sequence number value.</span></span>  
  
 <span data-ttu-id="ce7f0-169">Anlık görüntü yalıtımının net etkisi, işlemin tüm verileri işlemin başlangıcında var olduğu gibi, temel tablolara herhangi bir kilit koymadan görmesidir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-169">The net effect of snapshot isolation is that the transaction sees all of the data as it existed at the start of the transaction, without honoring or placing any locks on the underlying tables.</span></span> <span data-ttu-id="ce7f0-170">Bu, çekişmenin olduğu durumlarda performans iyileştirmelerine neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-170">This can result in performance improvements in situations where there is contention.</span></span>  
  
 <span data-ttu-id="ce7f0-171">Anlık görüntü hareketi her zaman iyimser eşzamanlılık denetimini kullanır ve diğer hareketlerin satırları güncelleştirmesini engelleyecek kilitleri stopajlar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-171">A snapshot transaction always uses optimistic concurrency control, withholding any locks that would prevent other transactions from updating rows.</span></span> <span data-ttu-id="ce7f0-172">Anlık görüntü hareketi, işlem başladıktan sonra değiştirilen bir satıra güncelleştirme ayırmaya çalışırsa, hareket geri alınır ve bir hata yükseltilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-172">If a snapshot transaction attempts to commit an update to a row that was changed after the transaction began, the transaction is rolled back, and an error is raised.</span></span>  
  
## <a name="working-with-snapshot-isolation-in-adonet"></a><span data-ttu-id="ce7f0-173">ADO.NET'da Anlık Görüntü Yalıtımı ile Çalışma</span><span class="sxs-lookup"><span data-stu-id="ce7f0-173">Working with Snapshot Isolation in ADO.NET</span></span>  
 <span data-ttu-id="ce7f0-174">Anlık görüntü yalıtımı <xref:System.Data.SqlClient.SqlTransaction> sınıf tarafından ADO.NET olarak desteklenir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-174">Snapshot isolation is supported in ADO.NET by the <xref:System.Data.SqlClient.SqlTransaction> class.</span></span> <span data-ttu-id="ce7f0-175">Anlık görüntü yalıtımı için bir veritabanı etkinleştirildiyse ancak READ_COMMITTED_SNAPSHOT A <xref:System.Data.SqlClient.SqlTransaction> için yapılandırılmamışsa, <xref:System.Data.SqlClient.SqlConnection.BeginTransaction%2A> yöntemi ararken **İzolasyon Düzeyi.Anlık Görüntü** numaralandırma değerini kullanarak başlatmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-175">If a database has been enabled for snapshot isolation but is not configured for READ_COMMITTED_SNAPSHOT ON, you must initiate a <xref:System.Data.SqlClient.SqlTransaction> using the **IsolationLevel.Snapshot** enumeration value when calling the <xref:System.Data.SqlClient.SqlConnection.BeginTransaction%2A> method.</span></span> <span data-ttu-id="ce7f0-176">Bu kod parçası, bağlantının <xref:System.Data.SqlClient.SqlConnection> açık bir nesne olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-176">This code fragment assumes that connection is an open <xref:System.Data.SqlClient.SqlConnection> object.</span></span>  
  
```vb  
Dim sqlTran As SqlTransaction = _  
  connection.BeginTransaction(IsolationLevel.Snapshot)  
```  
  
```csharp  
SqlTransaction sqlTran =
  connection.BeginTransaction(IsolationLevel.Snapshot);  
```  
  
### <a name="example"></a><span data-ttu-id="ce7f0-177">Örnek</span><span class="sxs-lookup"><span data-stu-id="ce7f0-177">Example</span></span>  
 <span data-ttu-id="ce7f0-178">Aşağıdaki örnek, kilitli verilere erişmeye çalışarak farklı yalıtım düzeylerinin nasıl çalıştığını gösterir ve üretim kodunda kullanılmak üzere tasarlanmamıştır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-178">The following example demonstrates how the different isolation levels behave by attempting to access locked data, and it is not intended to be used in production code.</span></span>  
  
 <span data-ttu-id="ce7f0-179">Kod, SQL Server'daki **AdventureWorks** örnek veritabanına bağlanır ve **TestSnapshot** adında bir tablo oluşturur ve bir veri satırı ekler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-179">The code connects to the **AdventureWorks** sample database in SQL Server and creates a table named **TestSnapshot** and inserts one row of data.</span></span> <span data-ttu-id="ce7f0-180">Kod, veritabanı için anlık görüntü yalıtımını açmak için ALTER DATABASE Transact-SQL deyimini kullanır, ancak varsayılan READ COMMITTED yalıtım düzeyi davranışını etkin bırakarak READ_COMMITTED_SNAPSHOT seçeneğini ayarlamaz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-180">The code uses the ALTER DATABASE Transact-SQL statement to turn on snapshot isolation for the database, but it does not set the READ_COMMITTED_SNAPSHOT option, leaving the default READ COMMITTED isolation-level behavior in effect.</span></span> <span data-ttu-id="ce7f0-181">Kod daha sonra aşağıdaki eylemleri gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-181">The code then performs the following actions:</span></span>  
  
- <span data-ttu-id="ce7f0-182">Bir güncelleştirme hareketi başlatmak için SERIALIZABLE yalıtım düzeyini kullanan sqlTransaction1 başlar, ancak tamamlanmaz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-182">It begins, but does not complete, sqlTransaction1, which uses the SERIALIZABLE isolation level to start an update transaction.</span></span> <span data-ttu-id="ce7f0-183">Bu, tabloyu kilitleme etkisine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-183">This has the effect of locking the table.</span></span>  
  
- <span data-ttu-id="ce7f0-184">İkinci bir bağlantı açar ve **TestSnapshot** tablosundaki verileri okumak için SNAPSHOT yalıtım düzeyini kullanarak ikinci bir işlem başlatır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-184">It opens a second connection and initiates a second transaction using the SNAPSHOT isolation level to read the data in the **TestSnapshot** table.</span></span> <span data-ttu-id="ce7f0-185">Anlık görüntü yalıtımı etkin olduğundan, bu işlem sqlTransaction1 başlamadan önce var olan verileri okuyabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-185">Because snapshot isolation is enabled, this transaction can read the data that existed before sqlTransaction1 started.</span></span>  
  
- <span data-ttu-id="ce7f0-186">Üçüncü bir bağlantı açar ve tablodaki verileri okumaya çalışmak için READ COMMITTED yalıtım düzeyini kullanarak bir işlem başlatır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-186">It opens a third connection and initiates a transaction using the READ COMMITTED isolation level to attempt to read the data in the table.</span></span> <span data-ttu-id="ce7f0-187">Bu durumda, ilk işlemde tabloya yerleştirilen kilitleri ve saatleri okuyamadığı için kod verileri okuyamaz. Bu yalıtım düzeyleri de ilk işlemde yerleştirilen kilitleri geçmiş okuyamaz çünkü TEKRAREDİlEBILEN READ ve SERIALIZABLE izolasyon düzeyleri kullanılırsa aynı sonuç oluşur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-187">In this case, the code cannot read the data because it cannot read past the locks placed on the table in the first transaction and times out. The same result would occur if the REPEATABLE READ and SERIALIZABLE isolation levels were used because these isolation levels also cannot read past the locks placed in the first transaction.</span></span>  
  
- <span data-ttu-id="ce7f0-188">Dördüncü bir bağlantı açar ve sqlTransaction1'de işlenmemiş değerin kirli bir okumasını gerçekleştiren READ UNCOMMITTED yalıtım düzeyini kullanarak bir işlem başlatır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-188">It opens a fourth connection and initiates a transaction using the READ UNCOMMITTED isolation level, which performs a dirty read of the uncommitted value in sqlTransaction1.</span></span> <span data-ttu-id="ce7f0-189">İlk işlem taahhüt edilmezse, bu değer veritabanında hiçbir zaman var olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-189">This value may never actually exist in the database if the first transaction is not committed.</span></span>  
  
- <span data-ttu-id="ce7f0-190">İlk işlemi geri alır ve **TestSnapshot** tablosunu silerek ve **AdventureWorks** veritabanı için anlık görüntü yalıtımını kapatarak temizler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-190">It rolls back the first transaction and cleans up by deleting the **TestSnapshot** table and turning off snapshot isolation for the **AdventureWorks** database.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="ce7f0-191">Aşağıdaki örneklerde bağlantı havuzu kapalı aynı bağlantı dizesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-191">The following examples use the same connection string with connection pooling turned off.</span></span> <span data-ttu-id="ce7f0-192">Bir bağlantı birleştirilmişse, yalıtım düzeyini sıfırlamak sunucudaki yalıtım düzeyini sıfırlamaz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-192">If a connection is pooled, resetting its isolation level does not reset the isolation level at the server.</span></span> <span data-ttu-id="ce7f0-193">Sonuç olarak, aynı havuzlu iç bağlantıyı kullanan sonraki bağlantılar, yalıtım düzeylerinin birleştirilmiş bağlantınınkine göre ayarlanmasıyla başlar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-193">As a result, subsequent connections that use the same pooled inner connection start with their isolation levels set to that of the pooled connection.</span></span> <span data-ttu-id="ce7f0-194">Bağlantı birleştirmeyi kapatmanın alternatifi, her bağlantı için yalıtım düzeyini açıkça ayarlamaktır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-194">An alternative to turning off connection pooling is to set the isolation level explicitly for each connection.</span></span>  
  
 [!code-csharp[DataWorks SnapshotIsolation.Demo#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks SnapshotIsolation.Demo/CS/source.cs#1)]
 [!code-vb[DataWorks SnapshotIsolation.Demo#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks SnapshotIsolation.Demo/VB/source.vb#1)]  
  
### <a name="example"></a><span data-ttu-id="ce7f0-195">Örnek</span><span class="sxs-lookup"><span data-stu-id="ce7f0-195">Example</span></span>  
 <span data-ttu-id="ce7f0-196">Aşağıdaki örnek, veriler değiştirilirken anlık görüntü yalıtımı davranışını gösterir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-196">The following example demonstrates the behavior of snapshot isolation when data is being modified.</span></span> <span data-ttu-id="ce7f0-197">Kod aşağıdaki eylemleri gerçekleştirir:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-197">The code performs the following actions:</span></span>  
  
- <span data-ttu-id="ce7f0-198">**AdventureWorks** örnek veritabanına bağlanır ve SNAPSHOT yalıtımı sağlar.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-198">Connects to the **AdventureWorks** sample database and enables SNAPSHOT isolation.</span></span>  
  
- <span data-ttu-id="ce7f0-199">**TestSnapshotUpdate** adında bir tablo oluşturur ve üç örnek veri satırı ekler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-199">Creates a table named **TestSnapshotUpdate** and inserts three rows of sample data.</span></span>  
  
- <span data-ttu-id="ce7f0-200">Snapshot yalıtımı kullanarak sqlTransaction1 başlar, ancak tamamlanmaz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-200">Begins, but does not complete, sqlTransaction1 using SNAPSHOT isolation.</span></span> <span data-ttu-id="ce7f0-201">Harekette üç veri satırı seçilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-201">Three rows of data are selected in the transaction.</span></span>  
  
- <span data-ttu-id="ce7f0-202">**AdventureWorks'e** ikinci bir **SqlConnection** oluşturur ve sqlTransaction1'de seçilen satırlardan birinde bir değeri güncelleştiren READ COMMITTED yalıtım düzeyini kullanarak ikinci bir işlem oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-202">Creates a second **SqlConnection** to **AdventureWorks** and creates a second transaction using the READ COMMITTED isolation level that updates a value in one of the rows selected in sqlTransaction1.</span></span>  
  
- <span data-ttu-id="ce7f0-203">sqlTransaction2'yi işler.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-203">Commits sqlTransaction2.</span></span>  
  
- <span data-ttu-id="ce7f0-204">sqlTransaction1'e döner ve sqlTransaction1'in zaten işlediği aynı satırı güncelleştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-204">Returns to sqlTransaction1 and attempts to update the same row that sqlTransaction1 already committed.</span></span> <span data-ttu-id="ce7f0-205">Hata 3960 yükseltilir ve sqlTransaction1 otomatik olarak geri alınır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-205">Error 3960 is raised, and sqlTransaction1 is rolled back automatically.</span></span> <span data-ttu-id="ce7f0-206">**SqlException.Number** ve **SqlException.Message** Konsol penceresinde görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-206">The **SqlException.Number** and **SqlException.Message** are displayed in the Console window.</span></span>  
  
- <span data-ttu-id="ce7f0-207">**AdventureWorks'te** anlık görüntü yalıtımını kapatmak ve **TestSnapshotUpdate** tablosunu silmek için temizleme kodunu yürütür.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-207">Executes clean-up code to turn off snapshot isolation in **AdventureWorks** and delete the **TestSnapshotUpdate** table.</span></span>  
  
 [!code-csharp[DataWorks SnapshotIsolation.DemoUpdate#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks SnapshotIsolation.DemoUpdate/CS/source.cs#1)]
 [!code-vb[DataWorks SnapshotIsolation.DemoUpdate#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks SnapshotIsolation.DemoUpdate/VB/source.vb#1)]  
  
### <a name="using-lock-hints-with-snapshot-isolation"></a><span data-ttu-id="ce7f0-208">Anlık Görüntü Yalıtımı ile Kilit İpuçları kullanma</span><span class="sxs-lookup"><span data-stu-id="ce7f0-208">Using Lock Hints with Snapshot Isolation</span></span>  
 <span data-ttu-id="ce7f0-209">Önceki örnekte, ilk işlem verileri seçer ve ilk işlem tamamlanmadan önce ikinci bir işlem verileri güncelleştirir ve ilk işlem aynı satırı güncelleştirmeye çalıştığında güncelleştirme çakışmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-209">In the previous example, the first transaction selects data, and a second transaction updates the data before the first transaction is able to complete, causing an update conflict when the first transaction tries to update the same row.</span></span> <span data-ttu-id="ce7f0-210">İşlemin başında kilit ipuçları sağlayarak uzun süren anlık görüntü işlemlerinde çakışmaları güncelleştirme olasılığını azaltabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-210">You can reduce the chance of update conflicts in long-running snapshot transactions by supplying lock hints at the beginning of the transaction.</span></span> <span data-ttu-id="ce7f0-211">Aşağıdaki SELECT deyimi, seçili satırları kilitlemek için UPDLOCK ipucunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="ce7f0-211">The following SELECT statement uses the UPDLOCK hint to lock the selected rows:</span></span>  
  
```sql  
SELECT * FROM TestSnapshotUpdate WITH (UPDLOCK)
  WHERE PriKey BETWEEN 1 AND 3  
```  
  
 <span data-ttu-id="ce7f0-212">UPDLOCK kilit ipucunu kullanarak, ilk işlem tamamlanmadan önce satırları güncelleştirmeye çalışan satırları engeller.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-212">Using the UPDLOCK lock hint blocks any rows attempting to update the rows before the first transaction completes.</span></span> <span data-ttu-id="ce7f0-213">Bu, seçili satırların daha sonra işlemde güncelleştirildiğinde çakışma olmamasını garanti eder.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-213">This guarantees that the selected rows have no conflicts when they are updated later in the transaction.</span></span> <span data-ttu-id="ce7f0-214">SQL Server Books Online'da "İpuçlarını Kilitleme" başlıklı bünyeye bakın.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-214">See "Locking Hints" in SQL Server Books Online.</span></span>  
  
 <span data-ttu-id="ce7f0-215">Uygulamanızda çok sayıda çakışma varsa, anlık görüntü yalıtımı en iyi seçim olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-215">If your application has many conflicts, snapshot isolation may not be the best choice.</span></span> <span data-ttu-id="ce7f0-216">İpuçları sadece gerçekten gerektiğinde kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-216">Hints should only be used when really needed.</span></span> <span data-ttu-id="ce7f0-217">Uygulamanız, sürekli olarak çalışması için kilit ipuçlarına dayanacak şekilde tasarlanmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-217">Your application should not be designed so that it constantly relies on lock hints for its operation.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ce7f0-218">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="ce7f0-218">See also</span></span>

- [<span data-ttu-id="ce7f0-219">SQL Server ve ADO.NET</span><span class="sxs-lookup"><span data-stu-id="ce7f0-219">SQL Server and ADO.NET</span></span>](index.md)
- [<span data-ttu-id="ce7f0-220">ADO.NET’e Genel Bakış</span><span class="sxs-lookup"><span data-stu-id="ce7f0-220">ADO.NET Overview</span></span>](../ado-net-overview.md)
- [<span data-ttu-id="ce7f0-221">İşlem Kilitleme ve Satır Sürüm Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="ce7f0-221">Transaction Locking and Row Versioning Guide</span></span>](/sql/relational-databases/sql-server-transaction-locking-and-row-versioning-guide)
