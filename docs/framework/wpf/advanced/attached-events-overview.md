---
title: Ekli Olaylara Genel Bakış
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- handling attached events [WPF]
- defining attached events as routed events [WPF]
- attached events [WPF], scenarios for
- attached events vs. routed events [WPF]
- backing attached events with routed events [WPF]
- attached events [WPF], definition
ms.assetid: 2c40eae3-80e4-4a45-ae09-df6c9ab4d91e
ms.openlocfilehash: 76ff60cfe26f9105d4504164802987115fc2a7e2
ms.sourcegitcommit: 944ddc52b7f2632f30c668815f92b378efd38eea
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/03/2019
ms.locfileid: "73455465"
---
# <a name="attached-events-overview"></a><span data-ttu-id="819ce-102">Ekli Olaylara Genel Bakış</span><span class="sxs-lookup"><span data-stu-id="819ce-102">Attached Events Overview</span></span>

<span data-ttu-id="819ce-103">Extensible Application Markup Language (XAML), bir dil bileşeni ve *ekli olay*olarak adlandırılan olay türünü tanımlar.</span><span class="sxs-lookup"><span data-stu-id="819ce-103">Extensible Application Markup Language (XAML) defines a language component and type of event called an *attached event*.</span></span> <span data-ttu-id="819ce-104">Ekli bir olay kavramı, olayı gerçekten tanımlayan veya devralan bir öğe yerine, belirli bir olaya yönelik bir işleyiciyi rastgele bir öğeye eklemenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="819ce-104">The concept of an attached event enables you to add a handler for a particular event to an arbitrary element rather than to an element that actually defines or inherits the event.</span></span> <span data-ttu-id="819ce-105">Bu durumda, nesne potansiyel olarak ne tür bir şekilde tetiklemez ve hedef işleme örneği, olayı tanımlar veya başka bir şekilde "sahip değildir".</span><span class="sxs-lookup"><span data-stu-id="819ce-105">In this case, neither the object potentially raising the event nor the destination handling instance defines or otherwise "owns" the event.</span></span>  

<a name="prerequisites"></a>   
## <a name="prerequisites"></a><span data-ttu-id="819ce-106">Prerequisites</span><span class="sxs-lookup"><span data-stu-id="819ce-106">Prerequisites</span></span>  
 <span data-ttu-id="819ce-107">Bu konuda, [yönlendirilmiş olaylara genel bakış](routed-events-overview.md) ve [xaml 'ye Genel Bakış (WPF)](../../../desktop-wpf/fundamentals/xaml.md)okumanız varsayılmaktadır.</span><span class="sxs-lookup"><span data-stu-id="819ce-107">This topic assumes that you have read [Routed Events Overview](routed-events-overview.md) and [XAML Overview (WPF)](../../../desktop-wpf/fundamentals/xaml.md).</span></span>  
  
<a name="Syntax"></a>   
## <a name="attached-event-syntax"></a><span data-ttu-id="819ce-108">Ekli olay sözdizimi</span><span class="sxs-lookup"><span data-stu-id="819ce-108">Attached Event Syntax</span></span>  
 <span data-ttu-id="819ce-109">Ekli olaylarda, ekli olay kullanımını desteklemek için, yedekleme kodu tarafından kullanılması gereken bir XAML sözdizimi ve bir kodlama düzeni vardır.</span><span class="sxs-lookup"><span data-stu-id="819ce-109">Attached events have a XAML syntax and a coding pattern that must be used by the backing code in order to support the attached event usage.</span></span>  
  
 <span data-ttu-id="819ce-110">XAML sözdiziminde, ekli olay yalnızca kendi olay adı tarafından değil, sahip türü ve bir noktayla (.) ayrılmış olan olay adı ile belirtilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-110">In XAML syntax, the attached event is specified not just by its event name, but by its owning type plus the event name, separated by a dot (.).</span></span> <span data-ttu-id="819ce-111">Olay adı, sahip olduğu türün adıyla nitelenbildiğinden, ekli olay sözdizimi, hiçbir ekli olayın, örneklenebilir herhangi bir öğeye eklenmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="819ce-111">Because the event name is qualified with the name of its owning type, the attached event syntax allows any attached event to be attached to any element that can be instantiated.</span></span>  
  
 <span data-ttu-id="819ce-112">Örneğin, özel `NeedsCleaning` ekli bir olay için bir işleyici eklemek için XAML sözdizimi aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="819ce-112">For example, the following is the XAML syntax for attaching a handler for a custom `NeedsCleaning` attached event:</span></span>  
  
 [!code-xaml[WPFAquariumSln#AE](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFAquariumSln/CSharp/WPFAquarium/Window1.xaml#ae)]  
  
 <span data-ttu-id="819ce-113">`aqua:` ön ekine göz önüne alın. Bu durumda, ekli olay özel eşlenmiş bir xmlns 'den gelen özel bir olay olduğundan, ön ek gereklidir.</span><span class="sxs-lookup"><span data-stu-id="819ce-113">Note the `aqua:` prefix; the prefix is necessary in this case because the attached event is a custom event that comes from a custom mapped xmlns.</span></span>  
  
<a name="WPFImplements"></a>   
## <a name="how-wpf-implements-attached-events"></a><span data-ttu-id="819ce-114">WPF ekli olayları nasıl uygular</span><span class="sxs-lookup"><span data-stu-id="819ce-114">How WPF Implements Attached Events</span></span>

<span data-ttu-id="819ce-115">WPF 'de, ekli olaylar bir <xref:System.Windows.RoutedEvent> alanı tarafından desteklenir ve oluşturulduktan sonra ağaç üzerinden yönlendirilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-115">In WPF, attached events are backed by a <xref:System.Windows.RoutedEvent> field and are routed through the tree after they are raised.</span></span> <span data-ttu-id="819ce-116">Genellikle, ekli olayın kaynağı (olayı oluşturan nesnesi) bir sistem veya hizmet kaynağıdır ve olayı oluşturan kodu çalıştıran nesne bu nedenle öğe ağacının doğrudan bir parçası değildir.</span><span class="sxs-lookup"><span data-stu-id="819ce-116">Typically, the source of the attached event (the object that raises the event) is a system or service source, and the object that runs the code that raises the event is therefore not a direct part of the element tree.</span></span>  
  
<a name="Scenarios"></a>   
## <a name="scenarios-for-attached-events"></a><span data-ttu-id="819ce-117">Ekli olaylara yönelik senaryolar</span><span class="sxs-lookup"><span data-stu-id="819ce-117">Scenarios for Attached Events</span></span>  
 <span data-ttu-id="819ce-118">WPF 'de, ekli olaylar, statik <xref:System.Windows.Input.Mouse> sınıfı veya <xref:System.Windows.Controls.Validation> sınıfı tarafından etkinleştirilen olaylar gibi hizmet düzeyi soyutlama olan belirli özellik alanlarında mevcuttur.</span><span class="sxs-lookup"><span data-stu-id="819ce-118">In WPF, attached events are present in certain feature areas where there is service-level abstraction, such as for the events enabled by the static <xref:System.Windows.Input.Mouse> class or the <xref:System.Windows.Controls.Validation> class.</span></span> <span data-ttu-id="819ce-119">Ya da hizmetini kullanan sınıflar, ekli olay söz diziminde olayını kullanabilir ya da ekli olayı sınıfın hizmetin yeteneklerini nasıl tümleştirdiğini gösteren yönlendirilmiş bir olay olarak yüzey seçebilirler.</span><span class="sxs-lookup"><span data-stu-id="819ce-119">Classes that interact with or use the service can either use the event in the attached event syntax, or they can choose to surface the attached event as a routed event that is part of how the class integrates the capabilities of the service.</span></span>  
  
 <span data-ttu-id="819ce-120">WPF bir dizi ekli olayı tanımlasa da, ekli olayı doğrudan kullanacağınız veya işleyebileceğiniz senaryolar çok sınırlı olur.</span><span class="sxs-lookup"><span data-stu-id="819ce-120">Although WPF defines a number of attached events, the scenarios where you will either use or handle the attached event directly are very limited.</span></span> <span data-ttu-id="819ce-121">Genellikle, ekli olay bir mimari amaca hizmet eder, ancak daha sonra eklenmemiş (CLR olayı "sarmalayıcı") yönlendirilmiş bir olayla iletilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-121">Generally, the attached event serves an architecture purpose, but is then forwarded to a non-attached (backed with a CLR event "wrapper") routed event.</span></span>  
  
 <span data-ttu-id="819ce-122">Örneğin, temel alınan ekli olay <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType>, XAML veya koddaki ekli olay söz dizimi ile ilgilenmektense, söz konusu <xref:System.Windows.UIElement> <xref:System.Windows.UIElement.MouseDown> kullanılarak, belirli bir <xref:System.Windows.UIElement> üzerinde daha kolay işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-122">For instance, the underlying attached event <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> can more easily be handled on any given <xref:System.Windows.UIElement> by using <xref:System.Windows.UIElement.MouseDown> on that <xref:System.Windows.UIElement> rather than dealing with attached event syntax either in XAML or code.</span></span> <span data-ttu-id="819ce-123">Eklenen olay, giriş cihazlarının gelecekteki genişlemesine izin verdiğinden, mimaride bir amaca hizmet eder.</span><span class="sxs-lookup"><span data-stu-id="819ce-123">The attached event serves a purpose in the architecture because it allows for future expansion of input devices.</span></span> <span data-ttu-id="819ce-124">Kuramsal cihazın, fare girişinin benzetimini yapmak için yalnızca <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> oluşturması ve <xref:System.Windows.Input.Mouse> türetmesinin gerekli olmaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="819ce-124">The hypothetical device would only need to raise <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> in order to simulate mouse input, and would not need to derive from <xref:System.Windows.Input.Mouse> to do so.</span></span> <span data-ttu-id="819ce-125">Ancak, bu senaryo olayların kod işlemesini içerir ve ekli etkinliğin XAML işleme Bu senaryoyla ilgili değildir.</span><span class="sxs-lookup"><span data-stu-id="819ce-125">However, this scenario involves code handling of the events, and XAML handling of the attached event is not relevant to this scenario.</span></span>  
  
<a name="Handling"></a>   
## <a name="handling-an-attached-event-in-wpf"></a><span data-ttu-id="819ce-126">WPF 'de ekli olay işleme</span><span class="sxs-lookup"><span data-stu-id="819ce-126">Handling an Attached Event in WPF</span></span>  
 <span data-ttu-id="819ce-127">Ekli olayı işleme işlemi ve yazacağınız işleyici kodu, bir yönlendirilmiş olayla aynı şekilde temelde aynıdır.</span><span class="sxs-lookup"><span data-stu-id="819ce-127">The process for handling an attached event, and the handler code that you will write, is basically the same as for a routed event.</span></span>  
  
 <span data-ttu-id="819ce-128">Genel olarak, WPF ekli olayı WPF yönlendirilmiş olayından çok farklı değildir.</span><span class="sxs-lookup"><span data-stu-id="819ce-128">In general, a WPF attached event is not very different from a WPF routed event.</span></span> <span data-ttu-id="819ce-129">Farklar olayın kaynağını belirleme ve bir sınıf tarafından bir üye olarak nasıl açığa çıkarılacağıdır (Ayrıca XAML işleyicisi söz dizimini da etkiler).</span><span class="sxs-lookup"><span data-stu-id="819ce-129">The differences are how the event is sourced and how it is exposed by a class as a member (which also affects the XAML handler syntax).</span></span>  
  
 <span data-ttu-id="819ce-130">Ancak, daha önce belirtildiği gibi, mevcut WPF ekli olayları özellikle WPF 'de işlemeye yöneliktir.</span><span class="sxs-lookup"><span data-stu-id="819ce-130">However, as noted earlier, the existing WPF attached events are not particularly intended for handling in WPF.</span></span> <span data-ttu-id="819ce-131">Daha sık, olayın amacı, bir örneği birleştirme sırasında bir üst öğeye raporlamak için bir Oluşturucu bir öğe sağlamaktır, bu durumda olay genellikle kodda tetiklenir ve ayrıca ilgili üst sınıfta sınıf işlemeye dayanır.</span><span class="sxs-lookup"><span data-stu-id="819ce-131">More often, the purpose of the event is to enable a composited element to report a state to a parent element in compositing, in which case the event is usually raised in code and also relies on class handling in the relevant parent class.</span></span> <span data-ttu-id="819ce-132">Örneğin, bir <xref:System.Windows.Controls.Primitives.Selector> içindeki öğelerin ekli <xref:System.Windows.Controls.Primitives.Selector.Selected> olayını oluşturması beklenir. Bu, daha sonra <xref:System.Windows.Controls.Primitives.Selector> sınıfı tarafından işlenen ve daha sonra büyük olasılıkla <xref:System.Windows.Controls.Primitives.Selector> sınıfı tarafından farklı bir yönlendirilmiş <xref:System.Windows.Controls.Primitives.Selector.SelectionChanged>olaya dönüştürüldü.</span><span class="sxs-lookup"><span data-stu-id="819ce-132">For instance, items within a <xref:System.Windows.Controls.Primitives.Selector> are expected to raise the attached <xref:System.Windows.Controls.Primitives.Selector.Selected> event, which is then class handled by the <xref:System.Windows.Controls.Primitives.Selector> class and then potentially converted by the <xref:System.Windows.Controls.Primitives.Selector> class into a different routed event, <xref:System.Windows.Controls.Primitives.Selector.SelectionChanged>.</span></span> <span data-ttu-id="819ce-133">Yönlendirilmiş olaylar ve sınıf işlemesi hakkında daha fazla bilgi için bkz. [yönlendirilmiş olayları işlenmiş olarak işaretleme ve sınıf işleme](marking-routed-events-as-handled-and-class-handling.md).</span><span class="sxs-lookup"><span data-stu-id="819ce-133">For more information on routed events and class handling, see [Marking Routed Events as Handled, and Class Handling](marking-routed-events-as-handled-and-class-handling.md).</span></span>  
  
<a name="Custom"></a>   
## <a name="defining-your-own-attached-events-as-routed-events"></a><span data-ttu-id="819ce-134">Kendi ekli olaylarınızı yönlendirilmiş olaylar olarak tanımlama</span><span class="sxs-lookup"><span data-stu-id="819ce-134">Defining Your Own Attached Events as Routed Events</span></span>  
 <span data-ttu-id="819ce-135">Ortak WPF temel sınıflarından türetmeniz durumunda, sınıfınıza belirli model yöntemlerini ekleyerek ve temel sınıflarda zaten mevcut olan yardımcı program yöntemlerini kullanarak kendi ekli olaylarınızı uygulayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="819ce-135">If you are deriving from common WPF base classes, you can implement your own attached events by including certain pattern methods in your class and by using utility methods that are already present on the base classes.</span></span>  
  
 <span data-ttu-id="819ce-136">Bu model aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="819ce-136">The pattern is as follows:</span></span>  
  
- <span data-ttu-id="819ce-137">Bir yöntem, iki parametreli bir __*EventName*işleyicisi ekler__ .</span><span class="sxs-lookup"><span data-stu-id="819ce-137">A method __Add*EventName*Handler__ with two parameters.</span></span> <span data-ttu-id="819ce-138">İlk parametre olay işleyicisinin eklendiği örneğidir.</span><span class="sxs-lookup"><span data-stu-id="819ce-138">The first parameter is the instance to which the event handler is added.</span></span> <span data-ttu-id="819ce-139">İkinci parametre, eklenecek olay işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="819ce-139">The second parameter is the event handler to add.</span></span> <span data-ttu-id="819ce-140">Yöntem, dönüş değeri olmadan `public` ve `static`olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="819ce-140">The method must be `public` and `static`, with no return value.</span></span>  
  
- <span data-ttu-id="819ce-141">Bir yöntem __,*EventName*Işleyicisini__ iki parametreyle kaldırır.</span><span class="sxs-lookup"><span data-stu-id="819ce-141">A method __Remove*EventName*Handler__ with two parameters.</span></span> <span data-ttu-id="819ce-142">İlk parametre olay işleyicisinin kaldırıldığı örneğidir.</span><span class="sxs-lookup"><span data-stu-id="819ce-142">The first parameter is the instance from which the event handler is removed.</span></span> <span data-ttu-id="819ce-143">İkinci parametre, kaldırılacak olay işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="819ce-143">The second parameter is the event handler to remove.</span></span> <span data-ttu-id="819ce-144">Yöntem, dönüş değeri olmadan `public` ve `static`olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="819ce-144">The method must be `public` and `static`, with no return value.</span></span>  
  
 <span data-ttu-id="819ce-145">__*EventName*işleyicisi ekleme__ erişimcisi yöntemi, ekli olay işleyicisi ÖZNITELIKLERI bir öğede bildirildiğinde xaml işlemesini kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="819ce-145">The __Add*EventName*Handler__ accessor method facilitates XAML processing when attached event handler attributes are declared on an element.</span></span> <span data-ttu-id="819ce-146">__*EventName*Handler__ ve __Remove*EventName*Handler__ yöntemleri, ekli olay için olay işleyicisi deposuna kod erişimini de etkinleştirir.</span><span class="sxs-lookup"><span data-stu-id="819ce-146">The __Add*EventName*Handler__ and __Remove*EventName*Handler__ methods also enable code access to the event handler store for the attached event.</span></span>  
  
 <span data-ttu-id="819ce-147">Bu genel düzen bir çerçevede pratik uygulama için yeterince kesin değildir, çünkü herhangi bir XAML okuyucu uygulamasının destekleyici dilde ve mimaride temel olayları tanımlamak için farklı şemaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-147">This general pattern is not yet precise enough for practical implementation in a framework, because any given XAML reader implementation might have different schemes for identifying underlying events in the supporting language and architecture.</span></span> <span data-ttu-id="819ce-148">Bu, WPF 'nin ekli olayları yönlendirilmiş olaylar olarak uyguladığı nedenlerinden biridir; bir olay (<xref:System.Windows.RoutedEvent>) için kullanılacak tanımlayıcı, WPF olay sistemi tarafından zaten tanımlanmış.</span><span class="sxs-lookup"><span data-stu-id="819ce-148">This is one of the reasons that WPF implements attached events as routed events; the identifier to use for an event (<xref:System.Windows.RoutedEvent>) is already defined by the WPF event system.</span></span> <span data-ttu-id="819ce-149">Ayrıca, bir olayı yönlendirme, ekli bir olayın XAML dil düzeyi kavramında doğal bir uygulama uzantısıdır.</span><span class="sxs-lookup"><span data-stu-id="819ce-149">Also, routing an event is a natural implementation extension on the XAML language-level concept of an attached event.</span></span>  
  
 <span data-ttu-id="819ce-150">WPF ekli olayı için __*EventName*işleyici ekleme__ , bağımsız değişken olarak yönlendirilmiş olay ve işleyiciyle <xref:System.Windows.UIElement.AddHandler%2A> çağırmaktan oluşur.</span><span class="sxs-lookup"><span data-stu-id="819ce-150">The __Add*EventName*Handler__ implementation for a WPF attached event consists of calling the <xref:System.Windows.UIElement.AddHandler%2A> with the routed event and handler as arguments.</span></span>  
  
 <span data-ttu-id="819ce-151">Bu uygulama stratejisi ve yönlendirilmiş olay sistemi genel olarak, yalnızca bu sınıfların <xref:System.Windows.UIElement.AddHandler%2A> uygulamalarına sahip olduğu için, ekli olayları <xref:System.Windows.UIElement> türetilmiş sınıflara veya <xref:System.Windows.ContentElement> türetilmiş sınıflara göre işlemeyi kısıtlar.</span><span class="sxs-lookup"><span data-stu-id="819ce-151">This implementation strategy and the routed event system in general restrict handling for attached events to either <xref:System.Windows.UIElement> derived classes or <xref:System.Windows.ContentElement> derived classes, because only those classes have <xref:System.Windows.UIElement.AddHandler%2A> implementations.</span></span>  
  
 <span data-ttu-id="819ce-152">Örneğin, aşağıdaki kod, ekli olayı yönlendirilmiş olay olarak bildirmek için WPF Ekli olay stratejisi kullanarak, `NeedsCleaning` ekli olayını sahip sınıfında `Aquarium`tanımlar.</span><span class="sxs-lookup"><span data-stu-id="819ce-152">For example, the following code defines the `NeedsCleaning` attached event on the owner class `Aquarium`, using the WPF attached event strategy of declaring the attached event as a routed event.</span></span>  
  
 [!code-csharp[WPFAquariumSln#AECode](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFAquariumSln/CSharp/WPFAquariumObjects/Class1.cs#aecode)]
 [!code-vb[WPFAquariumSln#AECode](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFAquariumSln/visualbasic/wpfaquariumobjects/class1.vb#aecode)]  
  
 <span data-ttu-id="819ce-153"><xref:System.Windows.EventManager.RegisterRoutedEvent%2A>ekli olay tanımlayıcı alanını oluşturmak için kullanılan yöntemin gerçekten, ekli olmayan bir yönlendirilmiş olayı kaydetmek için kullanılan yöntem olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="819ce-153">Note that the method used to establish the attached event identifier field, <xref:System.Windows.EventManager.RegisterRoutedEvent%2A>, is actually the same method that is used to register a non-attached routed event.</span></span> <span data-ttu-id="819ce-154">Ekli olaylar ve yönlendirilmiş olaylar, merkezi bir dahili depoya kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-154">Attached events and routed events all are registered to a centralized internal store.</span></span> <span data-ttu-id="819ce-155">Bu olay depolama uygulama, [yönlendirilmiş olaylara genel bakış](routed-events-overview.md)bölümünde ele alınan "arabirim olarak olaylar" kavramsal değerlendirmesi sunar.</span><span class="sxs-lookup"><span data-stu-id="819ce-155">This event store implementation enables the "events as an interface" conceptual consideration that is discussed in [Routed Events Overview](routed-events-overview.md).</span></span>  
  
<a name="Raising"></a>   
## <a name="raising-a-wpf-attached-event"></a><span data-ttu-id="819ce-156">WPF ekli olayını yükseltme</span><span class="sxs-lookup"><span data-stu-id="819ce-156">Raising a WPF Attached Event</span></span>  
 <span data-ttu-id="819ce-157">Genellikle, kodunuzda mevcut WPF tanımlı ekli olayları uygulamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="819ce-157">You do not typically need to raise existing WPF-defined attached events from your code.</span></span> <span data-ttu-id="819ce-158">Bu olaylar genel "hizmet" kavramsal modelini izler ve <xref:System.Windows.Input.InputManager> gibi hizmet sınıfları olayları oluşturmaktan sorumludur.</span><span class="sxs-lookup"><span data-stu-id="819ce-158">These events follow the general "service" conceptual model, and service classes such as <xref:System.Windows.Input.InputManager> are responsible for raising the events.</span></span>  
  
 <span data-ttu-id="819ce-159">Ancak, <xref:System.Windows.RoutedEvent>ekli olayları temel alan WPF modeline dayalı özel bir ekli olay tanımlıyorsanız, herhangi bir <xref:System.Windows.UIElement> veya <xref:System.Windows.ContentElement>eklenen bir olayı açmak için <xref:System.Windows.UIElement.RaiseEvent%2A> kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="819ce-159">However, if you are defining a custom attached event based on the WPF model of basing attached events on <xref:System.Windows.RoutedEvent>, you can use <xref:System.Windows.UIElement.RaiseEvent%2A> to raise an attached event from any <xref:System.Windows.UIElement> or <xref:System.Windows.ContentElement>.</span></span> <span data-ttu-id="819ce-160">Yönlendirilmiş bir olayı (ekli veya değil) yükseltmek için, öğe ağacında olay kaynağı olarak belirli bir öğeyi bildirmeniz gerekir; Bu kaynak <xref:System.Windows.UIElement.RaiseEvent%2A> arayan olarak bildirilir.</span><span class="sxs-lookup"><span data-stu-id="819ce-160">Raising a routed event (attached or not) requires that you declare a particular element in the element tree as the event source; that source is reported as the <xref:System.Windows.UIElement.RaiseEvent%2A> caller.</span></span> <span data-ttu-id="819ce-161">Ağaçta kaynak olarak hangi öğenin bildirileceğini belirleme hizmetinizin sorumluluğu</span><span class="sxs-lookup"><span data-stu-id="819ce-161">Determining which element is reported as the source in the tree is your service's responsibility</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="819ce-162">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="819ce-162">See also</span></span>

- [<span data-ttu-id="819ce-163">Yönlendirilmiş Olaylara Genel Bakış</span><span class="sxs-lookup"><span data-stu-id="819ce-163">Routed Events Overview</span></span>](routed-events-overview.md)
- [<span data-ttu-id="819ce-164">Ayrıntılı XAML Sözdizimi</span><span class="sxs-lookup"><span data-stu-id="819ce-164">XAML Syntax In Detail</span></span>](xaml-syntax-in-detail.md)
- [<span data-ttu-id="819ce-165">WPF için XAML ve Özel Sınıflar</span><span class="sxs-lookup"><span data-stu-id="819ce-165">XAML and Custom Classes for WPF</span></span>](xaml-and-custom-classes-for-wpf.md)
