---
title: Profil Oluşturma Ortamını Ayarlama
ms.date: 03/30/2017
helpviewer_keywords:
- environment variables, profiling API
- profiling API [.NET Framework], setting up environment
- COR_PROFILER environment variable
- Windows Service applications, profiling
- profiling API [.NET Framework], Windows Service applications
- COR_ENABLE_PROFILING environment variable
- profiling API [.NET Framework], enabling
ms.assetid: fefca07f-7555-4e77-be86-3c542e928312
author: mairaw
ms.author: mairaw
ms.openlocfilehash: bfa11083fad7a3ccc6a208f5f0e4b68e9e1bc18c
ms.sourcegitcommit: 5b6d778ebb269ee6684fb57ad69a8c28b06235b9
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/08/2019
ms.locfileid: "59098188"
---
# <a name="setting-up-a-profiling-environment"></a><span data-ttu-id="e0947-102">Profil Oluşturma Ortamını Ayarlama</span><span class="sxs-lookup"><span data-stu-id="e0947-102">Setting Up a Profiling Environment</span></span>
> [!NOTE]
>  <span data-ttu-id="e0947-103">Da profillere önemli değişiklikler olmuştur [!INCLUDE[net_v40_long](../../../../includes/net-v40-long-md.md)].</span><span class="sxs-lookup"><span data-stu-id="e0947-103">There have been substantial changes to profiling in the [!INCLUDE[net_v40_long](../../../../includes/net-v40-long-md.md)].</span></span>  
  
 <span data-ttu-id="e0947-104">Yönetilen bir işlem (uygulama veya hizmet) başlatıldığında, ortak dil çalışma zamanı (CLR) yükler.</span><span class="sxs-lookup"><span data-stu-id="e0947-104">When a managed process (application or service) starts, it loads the common language runtime (CLR).</span></span> <span data-ttu-id="e0947-105">CLR başlatılırken, işlemin bir profil oluşturucuya bağlanması gerekip gerekmediğine karar vermek için aşağıdaki iki ortam değişkenini değerlendirir:</span><span class="sxs-lookup"><span data-stu-id="e0947-105">When the CLR is initialized, it evaluates the following two environmental variables to decide whether the process should connect to a profiler:</span></span>  
  
-   <span data-ttu-id="e0947-106">COR_ENABLE_PROFILING: Bu ortam değişkeni varsa ve 1 olarak ayarlanmış CLR Profil oluşturucuya bağlanır.</span><span class="sxs-lookup"><span data-stu-id="e0947-106">COR_ENABLE_PROFILING: The CLR connects to a profiler only if this environment variable exists and is set to 1.</span></span>  
  
-   <span data-ttu-id="e0947-107">COR_PROFILER: Cor_enable_profılıng geçişleri işaretlerseniz, CLR, bu CLSID veya önceden kayıt defterinde depolanmış olması gereken ProgID sahip profil oluşturucuya bağlanır.</span><span class="sxs-lookup"><span data-stu-id="e0947-107">COR_PROFILER: If the COR_ENABLE_PROFILING check passes, the CLR connects to the profiler that has this CLSID or ProgID, which must have been stored previously in the registry.</span></span> <span data-ttu-id="e0947-108">Cor_profıler ortam değişkeni, aşağıdaki iki örnekte gösterildiği gibi bir dize olarak tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="e0947-108">The COR_PROFILER environment variable is defined as a string, as shown in the following two examples.</span></span>  
  
    ```  
    set COR_PROFILER={32E2F4DA-1BEA-47ea-88F9-C5DAF691C94A}  
    set COR_PROFILER="MyProfiler"  
    ```  
  
 <span data-ttu-id="e0947-109">Bir CLR uygulamasının profilini çıkarmak için uygulamayı çalıştırmadan önce cor_enable_profılıng ve cor_profıler ortam değişkenleri ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-109">To profile a CLR application, you must set the COR_ENABLE_PROFILING and COR_PROFILER environment variables before you run the application.</span></span> <span data-ttu-id="e0947-110">Ayrıca Profil Oluşturucu DLL'SİNİN kayıtlı olduğundan emin olmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-110">You must also make sure that the profiler DLL is registered.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="e0947-111">İle başlayarak [!INCLUDE[net_v40_short](../../../../includes/net-v40-short-md.md)], profil oluşturucular kaydedilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="e0947-111">Starting with the [!INCLUDE[net_v40_short](../../../../includes/net-v40-short-md.md)], profilers do not have to be registered.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="e0947-112">.NET Framework 2.0, 3.0 ve 3.5 sürümlerini kullanmak için [!INCLUDE[net_v40_short](../../../../includes/net-v40-short-md.md)] ve sonraki sürümlerinde, complus_profapı_profilercompatibilitysetting ortam değişkenini ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-112">To use .NET Framework versions 2.0, 3.0, and 3.5 profilers in the [!INCLUDE[net_v40_short](../../../../includes/net-v40-short-md.md)] and later versions, you must set the COMPLUS_ProfAPI_ProfilerCompatibilitySetting environment variable.</span></span>  
  
## <a name="environment-variable-scope"></a><span data-ttu-id="e0947-113">Ortam değişken kapsamı</span><span class="sxs-lookup"><span data-stu-id="e0947-113">Environment Variable Scope</span></span>  
 <span data-ttu-id="e0947-114">Cor_enable_profılıng ve cor_profıler ortam değişkenlerini nasıl ayarladığınız etki kapsamlarını belirler.</span><span class="sxs-lookup"><span data-stu-id="e0947-114">How you set the COR_ENABLE_PROFILING and COR_PROFILER environment variables will determine their scope of influence.</span></span> <span data-ttu-id="e0947-115">Bu değişkenleri aşağıdaki yollardan biriyle ayarlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="e0947-115">You can set these variables in one of the following ways:</span></span>  
  
-   <span data-ttu-id="e0947-116">Değişkenleri ayarlarsanız bir [Icordebug::CreateProcess](../../../../docs/framework/unmanaged-api/debugging/icordebug-createprocess-method.md) çağrı, bunlar geçerli anda çalışmakta olan uygulama.</span><span class="sxs-lookup"><span data-stu-id="e0947-116">If you set the variables in an [ICorDebug::CreateProcess](../../../../docs/framework/unmanaged-api/debugging/icordebug-createprocess-method.md) call, they will apply only to the application that you are running at the time.</span></span> <span data-ttu-id="e0947-117">(Ayrıca ortamı devralan bu uygulama tarafından başlatılmış diğer uygulamalara uygulanacaklardır.)</span><span class="sxs-lookup"><span data-stu-id="e0947-117">(They will also apply to other applications started by that application that inherit the environment.)</span></span>  
  
-   <span data-ttu-id="e0947-118">Değişkenleri komut istemi penceresinde ayarlarsanız, bu pencereden başlatılan tüm uygulamalara uygulanacaklardır.</span><span class="sxs-lookup"><span data-stu-id="e0947-118">If you set the variables in a Command Prompt window, they will apply to all applications that are started from that window.</span></span>  
  
-   <span data-ttu-id="e0947-119">Değişkenleri kullanıcı düzeyinde ayarlarsanız, dosya Gezgini ile başlattığınız tüm uygulamalara uygulanacaklardır.</span><span class="sxs-lookup"><span data-stu-id="e0947-119">If you set the variables at the user level, they will apply to all applications that you start with File Explorer.</span></span> <span data-ttu-id="e0947-120">Bir komut istemi penceresi bu ortam ayarlarını değişkenleri ayarladıktan sonra açtığınız sahiptir ve bu pencereden başlayacağınız uygulamaları içerecektir.</span><span class="sxs-lookup"><span data-stu-id="e0947-120">A Command Prompt window that you open after you set the variables will have these environment settings, and so will any application that you start from that window.</span></span> <span data-ttu-id="e0947-121">Kullanıcı düzeyinde ortam değişkenlerini ayarlamak için sağ **Bilgisayarım**, tıklayın **özellikleri**, tıklayın **Gelişmiş** sekmesini tıklatın, **ortamı Değişkenleri**ve değişkenleri ekleyip **kullanıcı değişkenleri** listesi.</span><span class="sxs-lookup"><span data-stu-id="e0947-121">To set environment variables at the user level, right-click **My Computer**, click **Properties**, click the **Advanced** tab, click **Environment Variables**, and add the variables to the **User variables** list.</span></span>  
  
-   <span data-ttu-id="e0947-122">Değişkenleri bilgisayar düzeyinde ayarlarsanız, bu bilgisayarda başlatılan tüm uygulamalara uygulanacaklardır.</span><span class="sxs-lookup"><span data-stu-id="e0947-122">If you set the variables at the computer level, they will apply to all applications that are started on that computer.</span></span> <span data-ttu-id="e0947-123">Bu bilgisayarda açtığınız komut istemi penceresi bu ortam ayarlarını ve bu pencereden başlayacağınız uygulamaları içerecektir.</span><span class="sxs-lookup"><span data-stu-id="e0947-123">A Command Prompt window that you open on that computer will have these environment settings, and so will any application that you start from that window.</span></span> <span data-ttu-id="e0947-124">Bu, o bilgisayardaki her yönetilen her işlemin oluşturucunuz ile başlayacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="e0947-124">This means that every managed process on that computer will start with your profiler.</span></span> <span data-ttu-id="e0947-125">Bilgisayar düzeyinde ortam değişkenlerini ayarlamak için sağ **Bilgisayarım**, tıklayın **özellikleri**, tıklayın **Gelişmiş** sekmesini tıklatın, **ortamı Değişkenleri**, değişkenleri ekleyip **sistem değişkenlerini** listelemek ve bilgisayarınızı yeniden başlatın.</span><span class="sxs-lookup"><span data-stu-id="e0947-125">To set environment variables at the computer level, right-click **My Computer**, click **Properties**, click the **Advanced** tab, click **Environment Variables**, add the variables to the **System variables** list, and then restart your computer.</span></span> <span data-ttu-id="e0947-126">Yeniden başlattıktan sonra değişkenler sistem çapında olacaktır.</span><span class="sxs-lookup"><span data-stu-id="e0947-126">After restarting, the variables will be available system-wide.</span></span>  
  
 <span data-ttu-id="e0947-127">Bir Windows hizmeti profili oluşturuyorsanız, ortam değişkenlerini ayarladıktan ve profil oluşturucu DLL'yi kaydettikten sonra bilgisayarınızı yeniden başlatmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-127">If you are profiling a Windows Service, you must restart your computer after you set the environment variables and register the profiler DLL.</span></span> <span data-ttu-id="e0947-128">Bu değerlendirmeler hakkında daha fazla bilgi için bkz [bir Windows hizmeti profili](#windows_service).</span><span class="sxs-lookup"><span data-stu-id="e0947-128">For more information about these considerations, see the section [Profiling a Windows Service](#windows_service).</span></span>  
  
## <a name="additional-considerations"></a><span data-ttu-id="e0947-129">Dikkat edilecek diğer noktalar</span><span class="sxs-lookup"><span data-stu-id="e0947-129">Additional Considerations</span></span>  
  
-   <span data-ttu-id="e0947-130">Profil Oluşturucu sınıfının Implements [Icorprofilercallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) ve [Icorprofilercallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md) arabirimleri.</span><span class="sxs-lookup"><span data-stu-id="e0947-130">The profiler class implements the [ICorProfilerCallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) and [ICorProfilerCallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md) interfaces.</span></span> <span data-ttu-id="e0947-131">.NET Framework sürüm 2. 0'da, bir profil oluşturucu uygulamalıdır `ICorProfilerCallback2`.</span><span class="sxs-lookup"><span data-stu-id="e0947-131">In the .NET Framework version 2.0, a profiler must implement `ICorProfilerCallback2`.</span></span> <span data-ttu-id="e0947-132">Bu, eğer okumuyorsa `ICorProfilerCallback2` yüklenmeyecek.</span><span class="sxs-lookup"><span data-stu-id="e0947-132">If it does not, `ICorProfilerCallback2` will not be loaded.</span></span>  
  
-   <span data-ttu-id="e0947-133">İşlem verili bir ortamda tek seferde yalnızca bir profil oluşturucu profil oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="e0947-133">Only one profiler can profile a process at one time in a given environment.</span></span> <span data-ttu-id="e0947-134">İki farklı profil oluşturucuyu farklı ortamlarda kaydedebilirsiniz, ancak her ayrı işlemlerin profilini oluşturmalıdır.</span><span class="sxs-lookup"><span data-stu-id="e0947-134">You can register two different profilers in different environments, but each must profile separate processes.</span></span> <span data-ttu-id="e0947-135">Profil Oluşturucu, işlemde COM sunucusu, profili oluşturulan işlem olarak aynı adres alanına eşlenmiş bir DLL olarak uygulanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="e0947-135">The profiler must be implemented as an in-process COM server DLL, which is mapped into the same address space as the process that is being profiled.</span></span> <span data-ttu-id="e0947-136">Bu, profil oluşturucunun işlemde çalıştığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="e0947-136">This means that the profiler runs in-process.</span></span> <span data-ttu-id="e0947-137">.NET Framework, herhangi bir COM sunucusu türünü desteklemez.</span><span class="sxs-lookup"><span data-stu-id="e0947-137">The .NET Framework does not support any other type of COM server.</span></span> <span data-ttu-id="e0947-138">Örneğin, bir profil oluşturucu uzak bir bilgisayardan uygulamaları izlemek isterse, bunu her bilgisayarda Toplayıcı aracılarını uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="e0947-138">For example, if a profiler wants to monitor applications from a remote computer, it must implement collector agents on each computer.</span></span> <span data-ttu-id="e0947-139">Bu aracılar, sonuçları toplar ve onları merkezi veri toplama bilgisayarına iletir.</span><span class="sxs-lookup"><span data-stu-id="e0947-139">These agents will batch results and communicate them to the central data collection computer.</span></span>  
  
-   <span data-ttu-id="e0947-140">Profil Oluşturucu, işlemde oluşturulan bir COM nesnesi olduğundan, profilli her uygulama kendi profil oluşturucu kopyasına sahip.</span><span class="sxs-lookup"><span data-stu-id="e0947-140">Because the profiler is a COM object that is instantiated in-process, each profiled application will have its own copy of the profiler.</span></span> <span data-ttu-id="e0947-141">Bu nedenle, birden çok uygulamadan veri işlemek tek bir profil oluşturucu örneği yok.</span><span class="sxs-lookup"><span data-stu-id="e0947-141">Therefore, a single profiler instance does not have to handle data from multiple applications.</span></span> <span data-ttu-id="e0947-142">Ancak, günlük dosyası önlemek için profil oluşturucu günlüğe kaydetme koduna mantık diğer profilli uygulamalardan üzerine yazmasını eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-142">However, you will have to add logic to the profiler's logging code to prevent log file overwrites from other profiled applications.</span></span>  
  
## <a name="initializing-the-profiler"></a><span data-ttu-id="e0947-143">Profiler'ı başlatma</span><span class="sxs-lookup"><span data-stu-id="e0947-143">Initializing the Profiler</span></span>  
 <span data-ttu-id="e0947-144">Her iki ortam değişkeni denetimi başarılı olduğunda, CLR Profil Oluşturucu bir örneğini com benzer şekilde oluşturur. `CoCreateInstance` işlevi.</span><span class="sxs-lookup"><span data-stu-id="e0947-144">When both environment variable checks pass, the CLR creates an instance of the profiler in a similar manner to the COM `CoCreateInstance` function.</span></span> <span data-ttu-id="e0947-145">Profil Oluşturucu doğrudan çağrı aracılığıyla yüklenmediğinden `CoCreateInstance`.</span><span class="sxs-lookup"><span data-stu-id="e0947-145">The profiler is not loaded through a direct call to `CoCreateInstance`.</span></span> <span data-ttu-id="e0947-146">Bu nedenle, bir çağrı `CoInitialize`, iş parçacığı modelinin ayarlanmasını gerektiren engellenir.</span><span class="sxs-lookup"><span data-stu-id="e0947-146">Therefore, a call to `CoInitialize`, which requires setting the threading model, is avoided.</span></span> <span data-ttu-id="e0947-147">CLR sonra çağıran [Icorprofilercallback::Initialize](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-initialize-method.md) yönteminde profil oluşturucu.</span><span class="sxs-lookup"><span data-stu-id="e0947-147">The CLR then calls the [ICorProfilerCallback::Initialize](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-initialize-method.md) method in the profiler.</span></span> <span data-ttu-id="e0947-148">Bu yöntemin imzası aşağıdaki gibidir.</span><span class="sxs-lookup"><span data-stu-id="e0947-148">The signature of this method is as follows.</span></span>  
  
```  
HRESULT Initialize(IUnknown *pICorProfilerInfoUnk)  
```  
  
 <span data-ttu-id="e0947-149">Profil Oluşturucu faydalanacaksa `pICorProfilerInfoUnk` için bir [Icorprofilerınfo](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-interface.md) veya [Icorprofilerınfo2](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo2-interface.md) arabirim işaretçisi ve böylece daha sonra profil oluşturma sırasında daha fazla bilgi talep edebilir.</span><span class="sxs-lookup"><span data-stu-id="e0947-149">The profiler must query `pICorProfilerInfoUnk` for an [ICorProfilerInfo](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-interface.md) or [ICorProfilerInfo2](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo2-interface.md) interface pointer and save it so that it can request more information later during profiling.</span></span>  
  
## <a name="setting-event-notifications"></a><span data-ttu-id="e0947-150">Olay bildirimlerini ayarlama</span><span class="sxs-lookup"><span data-stu-id="e0947-150">Setting Event Notifications</span></span>  
 <span data-ttu-id="e0947-151">Daha sonra profil oluşturucuyu çağırır [Icorprofilerınfo::seteventmask](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-seteventmask-method.md) , ilgileniyor olan hangi bildirim kategorilerini belirlemek için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="e0947-151">The profiler then calls the [ICorProfilerInfo::SetEventMask](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-seteventmask-method.md) method to specify which categories of notifications it is interested in.</span></span> <span data-ttu-id="e0947-152">Örneğin, profil oluşturucu yalnızca işlev girişi ve bildirim ve çöp toplama bildirimleri bırakın, aşağıdakileri belirtir.</span><span class="sxs-lookup"><span data-stu-id="e0947-152">For example, if the profiler is interested only in function enter and leave notifications and garbage collection notifications, it specifies the following.</span></span>  
  
```  
ICorProfilerInfo* pInfo;  
pICorProfilerInfoUnk->QueryInterface(IID_ICorProfilerInfo, (void**)&pInfo);  
pInfo->SetEventMask(COR_PRF_MONITOR_ENTERLEAVE | COR_PRF_MONITOR_GC)  
```  
  
 <span data-ttu-id="e0947-153">Bu şekilde bildirim maskesini ayarlayarak, profil oluşturucu hangi bildirimlerin alınacağını sınırlandırabilir.</span><span class="sxs-lookup"><span data-stu-id="e0947-153">By setting the notifications mask in this manner, the profiler can limit which notifications it receives.</span></span> <span data-ttu-id="e0947-154">Bu yaklaşım, kullanıcının basit ya da özel amaçlı profil oluşturucu oluşturmasına yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="e0947-154">This approach helps the user build a simple or special-purpose profiler.</span></span> <span data-ttu-id="e0947-155">Ayrıca, profil oluşturucu yok bildirimleri gönderme boşa gidecektir CPU süresini azaltır.</span><span class="sxs-lookup"><span data-stu-id="e0947-155">It also reduces CPU time that would be wasted sending notifications that the profiler would just ignore.</span></span>  
  
 <span data-ttu-id="e0947-156">Belirli profil oluşturucu olayları sabittir.</span><span class="sxs-lookup"><span data-stu-id="e0947-156">Certain profiler events are immutable.</span></span> <span data-ttu-id="e0947-157">Bu olaylar kümesinde hemen sonra buna `ICorProfilerCallback::Initialize` geri çağırma, bunlar devre dışı bırakılamaz ve yeni olayların açılamayacağı.</span><span class="sxs-lookup"><span data-stu-id="e0947-157">This means that as soon as these events are set in the `ICorProfilerCallback::Initialize` callback, they cannot be turned off and new events cannot be turned on.</span></span> <span data-ttu-id="e0947-158">Değişmez bir olayı değiştirme girişimleri sonuçlanır `ICorProfilerInfo::SetEventMask` başarısız HRESULT döndüren bir.</span><span class="sxs-lookup"><span data-stu-id="e0947-158">Attempts to change an immutable event will result in `ICorProfilerInfo::SetEventMask` returning a failed HRESULT.</span></span>  
  
<a name="windows_service"></a>   
## <a name="profiling-a-windows-service"></a><span data-ttu-id="e0947-159">Bir Windows hizmeti profili oluşturma</span><span class="sxs-lookup"><span data-stu-id="e0947-159">Profiling a Windows Service</span></span>  
 <span data-ttu-id="e0947-160">Bir Windows hizmeti profili, bir ortak dil çalışma zamanı uygulamasının profilini oluşturmaya benzer olur.</span><span class="sxs-lookup"><span data-stu-id="e0947-160">Profiling a Windows Service is like profiling a common language runtime application.</span></span> <span data-ttu-id="e0947-161">Her iki profil oluşturma işlemi ortam değişkenleri ile etkinleştirilir.</span><span class="sxs-lookup"><span data-stu-id="e0947-161">Both profiling operations are enabled through environment variables.</span></span> <span data-ttu-id="e0947-162">İşletim sistemi başlatıldığında, bu konuda daha önce tartışılan ortam değişikliklerinin zaten olmalıdır, bir Windows hizmeti de başlatıldığı için sunmak ve sistem başlatılmadan önce gereken değerlere ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="e0947-162">Because a Windows Service is started when the operating system starts, the environment variables discussed previously in this topic must already be present and set to the required values before the system starts.</span></span> <span data-ttu-id="e0947-163">Ayrıca, profil oluşturma DLL'SİNİN sistemde zaten kayıtlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="e0947-163">In addition, the profiling DLL must already be registered on the system.</span></span>  
  
 <span data-ttu-id="e0947-164">Cor_enable_profılıng ve cor_profıler ortam değişkenlerini ayarladıktan ve profil oluşturucu DLL'yi kaydettikten sonra Windows hizmeti bu değişiklikleri algılayabilmesi hedef bilgisayarı yeniden başlatmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-164">After you set the COR_ENABLE_PROFILING and COR_PROFILER environment variables and register the profiler DLL, you should restart the target computer so that the Windows Service can detect those changes.</span></span>  
  
 <span data-ttu-id="e0947-165">Bu değişikliklerin sistem genelinde profil oluşturmayı unutmayın.</span><span class="sxs-lookup"><span data-stu-id="e0947-165">Note that these changes will enable profiling on a system-wide basis.</span></span> <span data-ttu-id="e0947-166">Profili oluşturulan daha sonra çalışan her yönetilen uygulamanın önlemek için hedef bilgisayarı yeniden başlattıktan sonra sistem ortam değişkenlerini silmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="e0947-166">To prevent every managed application that subsequently runs from being profiled, you should delete the system environment variables after you restart the target computer.</span></span>  
  
 <span data-ttu-id="e0947-167">Bu teknik, her CLR işleminin profilinin oluşturulmasına da yol açar.</span><span class="sxs-lookup"><span data-stu-id="e0947-167">This technique also leads to every CLR process getting profiled.</span></span> <span data-ttu-id="e0947-168">Profil Oluşturucu mantık eklemelidir kendi [Icorprofilercallback::Initialize](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-initialize-method.md) geçerli işlemin ilgili olup olmadığını algılamak için geri çağırma.</span><span class="sxs-lookup"><span data-stu-id="e0947-168">The profiler should add logic to its [ICorProfilerCallback::Initialize](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-initialize-method.md) callback to detect whether the current process is of interest.</span></span> <span data-ttu-id="e0947-169">Yüklü değilse, profil oluşturucu başlatmayı gerçekleştirmeden geri çağırma başarısız olabilir.</span><span class="sxs-lookup"><span data-stu-id="e0947-169">If it is not, the profiler can fail the callback without performing the initialization.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e0947-170">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="e0947-170">See also</span></span>

- [<span data-ttu-id="e0947-171">Profil Oluşturmaya Genel Bakış</span><span class="sxs-lookup"><span data-stu-id="e0947-171">Profiling Overview</span></span>](../../../../docs/framework/unmanaged-api/profiling/profiling-overview.md)
