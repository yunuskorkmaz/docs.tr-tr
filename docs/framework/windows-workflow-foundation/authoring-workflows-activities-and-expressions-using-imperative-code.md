---
title: Kesin Kod Kullanarak İş Akışları, Etkinlikler ve İfadeler Yazma
ms.date: 03/30/2017
ms.assetid: cefc9cfc-2882-4eb9-8c94-7a6da957f2b2
ms.openlocfilehash: 97f57067e72be2ed2fb6b3846e2ab876c13e049f
ms.sourcegitcommit: 32a575bf4adccc901f00e264f92b759ced633379
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 12/04/2019
ms.locfileid: "74802706"
---
# <a name="authoring-workflows-activities-and-expressions-using-imperative-code"></a><span data-ttu-id="80899-102">Kesin Kod Kullanarak İş Akışları, Etkinlikler ve İfadeler Yazma</span><span class="sxs-lookup"><span data-stu-id="80899-102">Authoring Workflows, Activities, and Expressions Using Imperative Code</span></span>
<span data-ttu-id="80899-103">İş akışı tanımı, yapılandırılmış etkinlik nesnelerinin bir ağacıdır.</span><span class="sxs-lookup"><span data-stu-id="80899-103">A workflow definition is a tree of configured activity objects.</span></span> <span data-ttu-id="80899-104">Bu etkinlik ağacı, el ile düzenlenen XAML veya XAML oluşturmak için İş Akışı Tasarımcısı kullanılarak dahil olmak üzere birçok şekilde tanımlanabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-104">This tree of activities can be defined many ways, including by hand-editing XAML or by using the Workflow Designer to produce XAML.</span></span> <span data-ttu-id="80899-105">Ancak XAML kullanımı bir gereklilik değildir.</span><span class="sxs-lookup"><span data-stu-id="80899-105">Use of XAML, however, is not a requirement.</span></span> <span data-ttu-id="80899-106">Ayrıca, iş akışı tanımları programlama yoluyla da oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-106">Workflow definitions can also be created programmatically.</span></span> <span data-ttu-id="80899-107">Bu konu, kod kullanarak iş akışı tanımları, etkinlikler ve deyimler oluşturmaya genel bir bakış sağlar.</span><span class="sxs-lookup"><span data-stu-id="80899-107">This topic provides an overview of creating workflow definitions, activities, and expressions by using code.</span></span> <span data-ttu-id="80899-108">Kodu kullanarak XAML iş akışlarıyla çalışma örnekleri için bkz. [xaml 'de ve xaml 'de Iş akışlarını ve etkinlikleri seri hale getirme](serializing-workflows-and-activities-to-and-from-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="80899-108">For examples of working with XAML workflows using code, see [Serializing Workflows and Activities to and from XAML](serializing-workflows-and-activities-to-and-from-xaml.md).</span></span>  
  
## <a name="creating-workflow-definitions"></a><span data-ttu-id="80899-109">Iş akışı tanımları oluşturma</span><span class="sxs-lookup"><span data-stu-id="80899-109">Creating Workflow Definitions</span></span>  
 <span data-ttu-id="80899-110">Etkinlik türünün bir örneğini oluşturarak ve etkinlik nesnesinin özellikleri yapılandırılarak bir iş akışı tanımı oluşturulabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-110">A workflow definition can be created by instantiating an instance of an activity type and configuring the activity object’s properties.</span></span> <span data-ttu-id="80899-111">Alt etkinlik içermeyen etkinlikler için, bu, birkaç satır kod kullanılarak gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-111">For activities that do not contain child activities, this can be accomplished using a few lines of code.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#47](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#47)]  
  
> [!NOTE]
> <span data-ttu-id="80899-112">Bu konudaki örnekler, örnek iş akışlarını çalıştırmak için <xref:System.Activities.WorkflowInvoker> kullanır.</span><span class="sxs-lookup"><span data-stu-id="80899-112">The examples in this topic use <xref:System.Activities.WorkflowInvoker> to run the sample workflows.</span></span> <span data-ttu-id="80899-113">İş akışlarını çağırma, bağımsız değişkenleri geçirme ve kullanılabilir farklı barındırma seçeneklerini kullanma hakkında daha fazla bilgi için bkz. [Workflowwınvoker ve WorkflowApplication kullanma](using-workflowinvoker-and-workflowapplication.md).</span><span class="sxs-lookup"><span data-stu-id="80899-113">For more information about invoking workflows, passing arguments, and the different hosting choices that are available, see [Using WorkflowInvoker and WorkflowApplication](using-workflowinvoker-and-workflowapplication.md).</span></span>  
  
 <span data-ttu-id="80899-114">Bu örnekte, tek bir <xref:System.Activities.Statements.WriteLine> etkinliğinden oluşan bir iş akışı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="80899-114">In this example, a workflow that consists of a single <xref:System.Activities.Statements.WriteLine> activity is created.</span></span> <span data-ttu-id="80899-115"><xref:System.Activities.Statements.WriteLine> etkinliğin <xref:System.Activities.Statements.WriteLine.Text%2A> bağımsız değişkeni ayarlanır ve iş akışı çağrılır.</span><span class="sxs-lookup"><span data-stu-id="80899-115">The <xref:System.Activities.Statements.WriteLine> activity’s <xref:System.Activities.Statements.WriteLine.Text%2A> argument is set, and the workflow is invoked.</span></span> <span data-ttu-id="80899-116">Bir etkinlik alt etkinlikler içeriyorsa, oluşturma yöntemi benzerdir.</span><span class="sxs-lookup"><span data-stu-id="80899-116">If an activity contains child activities, the method of construction is similar.</span></span> <span data-ttu-id="80899-117">Aşağıdaki örnek, iki <xref:System.Activities.Statements.WriteLine> etkinliği içeren <xref:System.Activities.Statements.Sequence> etkinliğini kullanır.</span><span class="sxs-lookup"><span data-stu-id="80899-117">The following example uses a <xref:System.Activities.Statements.Sequence> activity that contains two <xref:System.Activities.Statements.WriteLine> activities.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#48](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#48)]  
  
### <a name="using-object-initializers"></a><span data-ttu-id="80899-118">Nesne başlatıcılarının kullanımı</span><span class="sxs-lookup"><span data-stu-id="80899-118">Using Object Initializers</span></span>  
 <span data-ttu-id="80899-119">Bu konudaki örnekler, nesne başlatma sözdizimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="80899-119">The examples in this topic use object initialization syntax.</span></span> <span data-ttu-id="80899-120">Nesne başlatma sözdizimi, iş akışındaki etkinliklerin hiyerarşik bir görünümünü sağladığından koddaki iş akışı tanımlarını oluşturmak için kullanışlı bir yol olabilir ve etkinlikler arasındaki ilişkiyi gösterir.</span><span class="sxs-lookup"><span data-stu-id="80899-120">Object initialization syntax can be a useful way to create workflow definitions in code because it provides a hierarchical view of the activities in the workflow and shows the relationship between the activities.</span></span> <span data-ttu-id="80899-121">Program aracılığıyla iş akışları oluştururken nesne başlatma söz dizimini kullanma gereksinimi yoktur.</span><span class="sxs-lookup"><span data-stu-id="80899-121">There is no requirement to use object initialization syntax when you programmatically create workflows.</span></span> <span data-ttu-id="80899-122">Aşağıdaki örnek, önceki örneğe işlevsel olarak eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="80899-122">The following example is functionally equivalent to the previous example.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#49](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#49)]  
  
 <span data-ttu-id="80899-123">Nesne başlatıcıları hakkında daha fazla bilgi için bkz. [nasıl yapılır: bir Oluşturucu çağırılmadan nesneleri başlatmaC# (Programlama Kılavuzu)](../../csharp/programming-guide/classes-and-structs/how-to-initialize-objects-by-using-an-object-initializer.md) ve [nasıl yapılır: nesne Başlatıcısı kullanarak nesne bildirme](../../visual-basic/programming-guide/language-features/objects-and-classes/how-to-declare-an-object-by-using-an-object-initializer.md).</span><span class="sxs-lookup"><span data-stu-id="80899-123">For more information about object initializers, see [How to: Initialize Objects without Calling a Constructor (C# Programming Guide)](../../csharp/programming-guide/classes-and-structs/how-to-initialize-objects-by-using-an-object-initializer.md) and [How to: Declare an Object by Using an Object Initializer](../../visual-basic/programming-guide/language-features/objects-and-classes/how-to-declare-an-object-by-using-an-object-initializer.md).</span></span>  
  
### <a name="working-with-variables-literal-values-and-expressions"></a><span data-ttu-id="80899-124">Değişkenler, değişmez değerler ve Ifadelerle çalışma</span><span class="sxs-lookup"><span data-stu-id="80899-124">Working with Variables, Literal Values, and Expressions</span></span>  
 <span data-ttu-id="80899-125">Kodu kullanarak bir iş akışı tanımı oluştururken, iş akışı tanımı oluşturmanın bir parçası olarak hangi kodun yürütüldüğünü ve bu iş akışının bir örneğini yürütmenin bir parçası olarak hangi kodun yürütüldüğünü unutmayın.</span><span class="sxs-lookup"><span data-stu-id="80899-125">When creating a workflow definition using code, be aware of what code executes as part of the creation of the workflow definition and what code executes as part of the execution of an instance of that workflow.</span></span> <span data-ttu-id="80899-126">Örneğin, aşağıdaki iş akışı rastgele bir sayı oluşturmak ve konsola yazmak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="80899-126">For example, the following workflow is intended to generate a random number and write it to the console.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#50](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#50)]  
  
 <span data-ttu-id="80899-127">Bu iş akışı Tanım kodu yürütüldüğünde, `Random.Next` çağrısı yapılır ve sonuç iş akışı tanımında değişmez değer olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="80899-127">When this workflow definition code is executed, the call to `Random.Next` is made and the result is stored in the workflow definition as a literal value.</span></span> <span data-ttu-id="80899-128">Bu iş akışının birçok örneği çağrılabilir ve hepsi aynı numarayı görüntüler.</span><span class="sxs-lookup"><span data-stu-id="80899-128">Many instances of this workflow can be invoked, and all would display the same number.</span></span> <span data-ttu-id="80899-129">İş akışı yürütmesi sırasında rastgele sayının oluşturulması durumunda, iş akışı her çalıştığında değerlendirilen bir ifade kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="80899-129">To have the random number generation occur during workflow execution, an expression must be used that is evaluated each time the workflow runs.</span></span> <span data-ttu-id="80899-130">Aşağıdaki örnekte, bir Visual Basic ifadesi <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601>ile kullanılır.</span><span class="sxs-lookup"><span data-stu-id="80899-130">In the following example, a Visual Basic expression is used with a <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601>.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#51](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#51)]  
  
 <span data-ttu-id="80899-131">Önceki örnekteki ifade bir <xref:Microsoft.CSharp.Activities.CSharpValue%601> ve bir C# ifade kullanılarak da uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-131">The expression in the previous example could also be implemented using a <xref:Microsoft.CSharp.Activities.CSharpValue%601> and a C# expression.</span></span>  
  
```csharp  
new Assign<int>  
{  
    To = n,  
    Value = new CSharpValue<int>("new Random().Next(1, 101)")  
}  
```  
  
 <span data-ttu-id="80899-132">C#ifadeleri içeren iş akışı çağrılmadan önce ifadeler derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="80899-132">C# expressions must be compiled before the workflow containing them is invoked.</span></span> <span data-ttu-id="80899-133">C# İfadeler derlenmeyecekse, iş akışı aşağıdakine benzer bir iletiyle çağrıldığında bir <xref:System.NotSupportedException> oluşturulur ``Expression Activity type 'CSharpValue`1' requires compilation in order to run.  Please ensure that the workflow has been compiled.``: Visual Studio 'da oluşturulan iş akışlarını içeren çoğu senaryoda, C# ifadeler otomatik olarak derlenir, ancak kod iş akışları gibi bazı senaryolarda C# ifadeler el ile derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="80899-133">If the C# expressions are not compiled, a <xref:System.NotSupportedException> is thrown when the workflow is invoked with a message similar to the following: ``Expression Activity type 'CSharpValue`1' requires compilation in order to run.  Please ensure that the workflow has been compiled.`` In most scenarios involving workflows created in Visual Studio the C# expressions are compiled automatically, but in some scenarios, such as code workflows, the C# expressions must be manually compiled.</span></span> <span data-ttu-id="80899-134">İfadelerin nasıl derlenmeye C# ilişkin bir örnek için, [ C# ifadeler](csharp-expressions.md) konusunun [kod C# iş akışlarında ifadeleri kullanma](csharp-expressions.md#CodeWorkflows) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="80899-134">For an example of how to compile C# expressions, see the [Using C# expressions in code workflows](csharp-expressions.md#CodeWorkflows) section of the [C# Expressions](csharp-expressions.md) topic.</span></span>  
  
 <span data-ttu-id="80899-135"><xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601>, bir ifadede r değeri olarak kullanılabilecek Visual Basic sözdiziminde bir ifadeyi temsil eder ve bir <xref:Microsoft.CSharp.Activities.CSharpValue%601> bir ifadede r-değeri olarak kullanılabilecek bir ifadeyi C# temsil eder.</span><span class="sxs-lookup"><span data-stu-id="80899-135">A <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> represents an expression in Visual Basic syntax that can be used as an r-value in an expression, and a <xref:Microsoft.CSharp.Activities.CSharpValue%601> represents an expression in C# syntax that can be used as an r-value in an expression.</span></span> <span data-ttu-id="80899-136">Bu ifadeler, kapsayan etkinliğin her yürütüldüğü her seferinde değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="80899-136">These expressions are evaluated each time the containing activity is executed.</span></span> <span data-ttu-id="80899-137">İfadenin sonucu `n`iş akışı değişkenine atanır ve bu sonuçlar iş akışındaki sonraki etkinlik tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="80899-137">The result of the expression is assigned to the workflow variable `n`, and these results are used by the next activity in the workflow.</span></span> <span data-ttu-id="80899-138">Çalışma zamanında `n` iş akışı değişkeninin değerine erişmek için <xref:System.Activities.ActivityContext> gereklidir.</span><span class="sxs-lookup"><span data-stu-id="80899-138">To access the value of the workflow variable `n` at runtime, the <xref:System.Activities.ActivityContext> is required.</span></span> <span data-ttu-id="80899-139">Bu, aşağıdaki lambda ifadesi kullanılarak erişilebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-139">This can be accessed by using the following lambda expression.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="80899-140">Bu kodların her ikisi de programlama dili C# olarak kullanılmıştır, ancak biri <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> kullanır ve bir <xref:Microsoft.CSharp.Activities.CSharpValue%601>kullanır.</span><span class="sxs-lookup"><span data-stu-id="80899-140">Note that both of these code are examples are using C# as the programming language, but one uses a <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> and one uses a <xref:Microsoft.CSharp.Activities.CSharpValue%601>.</span></span> <span data-ttu-id="80899-141"><xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> ve <xref:Microsoft.CSharp.Activities.CSharpValue%601>, hem Visual Basic hem C# de projelerinde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-141"><xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> and <xref:Microsoft.CSharp.Activities.CSharpValue%601> can be used in both Visual Basic and C# projects.</span></span> <span data-ttu-id="80899-142">Varsayılan olarak, iş akışı tasarımcısında oluşturulan ifadeler barındırma projesinin diliyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="80899-142">By default, expressions created in the workflow designer match the language of the hosting project.</span></span> <span data-ttu-id="80899-143">Kodda iş akışları oluştururken, istenen dil iş akışı yazarının kararına göre belirlenir.</span><span class="sxs-lookup"><span data-stu-id="80899-143">When creating workflows in code, the desired language is at the discretion of the workflow author.</span></span>  
  
 <span data-ttu-id="80899-144">Bu örneklerde, ifadenin sonucu `n`iş akışı değişkenine atanır ve bu sonuçlar iş akışındaki sonraki etkinlik tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="80899-144">In these examples the result of the expression is assigned to the workflow variable `n`, and these results are used by the next activity in the workflow.</span></span> <span data-ttu-id="80899-145">Çalışma zamanında `n` iş akışı değişkeninin değerine erişmek için <xref:System.Activities.ActivityContext> gereklidir.</span><span class="sxs-lookup"><span data-stu-id="80899-145">To access the value of the workflow variable `n` at runtime, the <xref:System.Activities.ActivityContext> is required.</span></span> <span data-ttu-id="80899-146">Bu, aşağıdaki lambda ifadesi kullanılarak erişilebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-146">This can be accessed by using the following lambda expression.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#52](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#52)]  
  
 <span data-ttu-id="80899-147">Lambda ifadeleri hakkında daha fazla bilgi için bkz. [lambda ifadeleriC# (Programlama Kılavuzu)](../../csharp/programming-guide/statements-expressions-operators/lambda-expressions.md) veya [lambda ifadeleri (Visual Basic)](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="80899-147">For more information about lambda expressions, see [Lambda Expressions (C# Programming Guide)](../../csharp/programming-guide/statements-expressions-operators/lambda-expressions.md) or [Lambda Expressions (Visual Basic)](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md).</span></span>  
  
 <span data-ttu-id="80899-148">Lambda ifadeleri XAML biçimine serileştirilebilir değildir.</span><span class="sxs-lookup"><span data-stu-id="80899-148">Lambda expressions are not serializable to XAML format.</span></span> <span data-ttu-id="80899-149">Lambda ifadeleriyle bir iş akışını serileştirme girişimi yapılırsa, aşağıdaki iletiyle bir <xref:System.Activities.Expressions.LambdaSerializationException> oluşturulur: "Bu iş akışı kodda belirtilen Lambda ifadelerini içerir.</span><span class="sxs-lookup"><span data-stu-id="80899-149">If an attempt to serialize a workflow with lambda expressions is made, a <xref:System.Activities.Expressions.LambdaSerializationException> is thrown with the following message: "This workflow contains lambda expressions specified in code.</span></span> <span data-ttu-id="80899-150">Bu ifadeler XAML serileştirilebilir değildir.</span><span class="sxs-lookup"><span data-stu-id="80899-150">These expressions are not XAML serializable.</span></span> <span data-ttu-id="80899-151">İş akışınızı XAML-serileştirilebilir yapmak için VisualBasicValue/VisualBasicReference veya ExpressionServices. Convert (lambda) kullanın.</span><span class="sxs-lookup"><span data-stu-id="80899-151">In order to make your workflow XAML-serializable, either use VisualBasicValue/VisualBasicReference or ExpressionServices.Convert(lambda).</span></span> <span data-ttu-id="80899-152">Bu işlem lambda ifadelerinizi ifade etkinliklerine dönüştürür. "</span><span class="sxs-lookup"><span data-stu-id="80899-152">This will convert your lambda expressions into expression activities."</span></span> <span data-ttu-id="80899-153">Bu ifadeyi XAML ile uyumlu hale getirmek için, aşağıdaki örnekte gösterildiği gibi <xref:System.Activities.Expressions.ExpressionServices> ve <xref:System.Activities.Expressions.ExpressionServices.Convert%2A>kullanın.</span><span class="sxs-lookup"><span data-stu-id="80899-153">To make this expression compatible with XAML, use <xref:System.Activities.Expressions.ExpressionServices> and <xref:System.Activities.Expressions.ExpressionServices.Convert%2A>, as shown in the following example.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#53](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#53)]  
  
 <span data-ttu-id="80899-154"><xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> de kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-154">A <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> could also be used.</span></span> <span data-ttu-id="80899-155">Visual Basic ifadesi kullanılırken hiçbir lambda ifadesinin gerekli olmadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="80899-155">Note that no lambda expression is required when using a Visual Basic expression.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#54](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#54)]  
  
 <span data-ttu-id="80899-156">Çalışma zamanında, Visual Basic ifadeleri LINQ ifadelerine derlenir.</span><span class="sxs-lookup"><span data-stu-id="80899-156">At run time, Visual Basic expressions are compiled into LINQ expressions.</span></span> <span data-ttu-id="80899-157">Önceki örneklerin her ikisi de XAML 'ye serileştirilebilir, ancak serileştirilmiş XAML iş akışı tasarımcısında görüntülenmek ve düzenlenilmesi amaçlanıyorsa deyimleriniz için <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> kullanın.</span><span class="sxs-lookup"><span data-stu-id="80899-157">Both of the previous examples are serializable to XAML, but if the serialized XAML is intended to be viewed and edited in the workflow designer, use <xref:Microsoft.VisualBasic.Activities.VisualBasicValue%601> for your expressions.</span></span> <span data-ttu-id="80899-158">`ExpressionServices.Convert` kullanan serileştirilmiş iş akışları tasarımcıda açılabilir, ancak ifadenin değeri boş olur.</span><span class="sxs-lookup"><span data-stu-id="80899-158">Serialized workflows that use `ExpressionServices.Convert` can be opened in the designer, but the value of the expression will be blank.</span></span> <span data-ttu-id="80899-159">XAML 'de iş akışlarını serileştirme hakkında daha fazla bilgi için bkz. [xaml 'de ve xaml 'de Iş akışlarını ve etkinlikleri serileştirme](serializing-workflows-and-activities-to-and-from-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="80899-159">For more information about serializing workflows to XAML, see [Serializing Workflows and Activities to and from XAML](serializing-workflows-and-activities-to-and-from-xaml.md).</span></span>  
  
#### <a name="literal-expressions-and-reference-types"></a><span data-ttu-id="80899-160">Değişmez değer Ifadeleri ve başvuru türleri</span><span class="sxs-lookup"><span data-stu-id="80899-160">Literal Expressions and Reference Types</span></span>  
 <span data-ttu-id="80899-161">Değişmez değer ifadeleri <xref:System.Activities.Expressions.Literal%601> etkinliği tarafından iş akışlarında temsil edilir.</span><span class="sxs-lookup"><span data-stu-id="80899-161">Literal expressions are represented in workflows by the <xref:System.Activities.Expressions.Literal%601> activity.</span></span> <span data-ttu-id="80899-162">Aşağıdaki <xref:System.Activities.Statements.WriteLine> etkinlikleri işlevsel olarak eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="80899-162">The following <xref:System.Activities.Statements.WriteLine> activities are functionally equivalent.</span></span>  
  
```csharp  
new WriteLine  
{  
    Text = "Hello World."  
},  
new WriteLine  
{  
    Text = new Literal<string>("Hello World.")  
}  
```  
  
 <span data-ttu-id="80899-163"><xref:System.String>dışında herhangi bir başvuru türü ile değişmez bir ifade başlatmak geçersizdir.</span><span class="sxs-lookup"><span data-stu-id="80899-163">It is invalid to initialize a literal expression with any reference type except <xref:System.String>.</span></span> <span data-ttu-id="80899-164">Aşağıdaki örnekte, bir <xref:System.Activities.Statements.Assign> etkinliğinin <xref:System.Activities.Statements.Assign.Value%2A> özelliği bir `List<string>`kullanılarak değişmez değer ifadesiyle başlatılır.</span><span class="sxs-lookup"><span data-stu-id="80899-164">In the following example, an <xref:System.Activities.Statements.Assign> activity's <xref:System.Activities.Statements.Assign.Value%2A> property is initialized with a literal expression using a `List<string>`.</span></span>  
  
```csharp  
new Assign  
{  
    To = new OutArgument<List<string>>(items),  
    Value = new InArgument<List<string>>(new List<string>())  
},  
```  
  
 <span data-ttu-id="80899-165">Bu etkinliği içeren iş akışı doğrulanırsa, aşağıdaki doğrulama hatası döndürülür: "değişmez değer yalnızca değer türlerini ve sabit tür System. String ' i destekler.</span><span class="sxs-lookup"><span data-stu-id="80899-165">When the workflow containing this activity is validated, the following validation error is returned: "Literal only supports value types and the immutable type System.String.</span></span> <span data-ttu-id="80899-166">System. Collections. Generic. List ' 1 [System. String] türü bir sabit değer olarak kullanılamaz. "</span><span class="sxs-lookup"><span data-stu-id="80899-166">The type System.Collections.Generic.List\`1[System.String] cannot be used as a literal."</span></span> <span data-ttu-id="80899-167">İş akışı çağrılırsa, doğrulama hatasının metnini içeren bir <xref:System.Activities.InvalidWorkflowException> oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="80899-167">If the workflow is invoked, an <xref:System.Activities.InvalidWorkflowException> is thrown that contains the text of the validation error.</span></span> <span data-ttu-id="80899-168">Bu bir doğrulama hatasıdır çünkü başvuru türü ile değişmez değer ifadesi oluşturma, iş akışının her bir örneği için başvuru türünün yeni bir örneğini oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="80899-168">This is a validation error because creating a literal expression with a reference type does not create a new instance of the reference type for each instance of the workflow.</span></span> <span data-ttu-id="80899-169">Bu sorunu çözmek için, değişmez değer ifadesini oluşturan ve başvuru türünün yeni bir örneğini döndüren bir ifade ile değiştirin.</span><span class="sxs-lookup"><span data-stu-id="80899-169">To resolve this, replace the literal expression with one that creates and returns a new instance of the reference type.</span></span>  
  
```csharp  
new Assign  
{  
    To = new OutArgument<List<string>>(items),  
    Value = new InArgument<List<string>>(new VisualBasicValue<List<string>>("New List(Of String)"))  
},  
```  
  
 <span data-ttu-id="80899-170">İfadeler hakkında daha fazla bilgi için bkz. [ifadeler](expressions.md).</span><span class="sxs-lookup"><span data-stu-id="80899-170">For more information about expressions, see [Expressions](expressions.md).</span></span>  
  
#### <a name="invoking-methods-on-objects-using-expressions-and-the-invokemethod-activity"></a><span data-ttu-id="80899-171">Ifadeleri ve InvokeMethod etkinliğini kullanarak nesnelerde Yöntemler çağırma</span><span class="sxs-lookup"><span data-stu-id="80899-171">Invoking Methods on Objects using Expressions and the InvokeMethod Activity</span></span>  
 <span data-ttu-id="80899-172"><xref:System.Activities.Expressions.InvokeMethod%601> etkinliği, .NET Framework sınıfların statik ve örnek yöntemlerini çağırmak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-172">The <xref:System.Activities.Expressions.InvokeMethod%601> activity can be used to invoke static and instance methods of classes in the .NET Framework.</span></span> <span data-ttu-id="80899-173">Bu konunun önceki bir örneğinde, <xref:System.Random> sınıfı kullanılarak rastgele bir sayı oluşturulmuştur.</span><span class="sxs-lookup"><span data-stu-id="80899-173">In a previous example in this topic, a random number was generated using the <xref:System.Random> class.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#51](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#51)]  
  
 <span data-ttu-id="80899-174"><xref:System.Activities.Expressions.InvokeMethod%601> etkinliği, <xref:System.Random> sınıfının <xref:System.Random.Next%2A> yöntemini çağırmak için de kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-174">The <xref:System.Activities.Expressions.InvokeMethod%601> activity could also have been used to call the <xref:System.Random.Next%2A> method of the <xref:System.Random> class.</span></span>  
  
```csharp  
new InvokeMethod<int>  
{  
    TargetObject = new InArgument<Random>(new VisualBasicValue<Random>("New Random()")),  
    MethodName = "Next",  
    Parameters =   
    {  
        new InArgument<int>(1),  
        new InArgument<int>(101)  
    },  
    Result = n  
}  
```  
  
 <span data-ttu-id="80899-175"><xref:System.Random.Next%2A> statik bir yöntem olmadığından, <xref:System.Activities.Expressions.InvokeMethod%601.TargetObject%2A> özelliği için <xref:System.Random> sınıfının bir örneği sağlanır.</span><span class="sxs-lookup"><span data-stu-id="80899-175">Since <xref:System.Random.Next%2A> is not a static method, an instance of the <xref:System.Random> class is supplied for the <xref:System.Activities.Expressions.InvokeMethod%601.TargetObject%2A> property.</span></span> <span data-ttu-id="80899-176">Bu örnekte, bir Visual Basic ifadesi kullanılarak yeni bir örnek oluşturulur, ancak daha önce oluşturulmuş ve bir iş akışı değişkeninde depolanmış olabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-176">In this example a new instance is created using a Visual Basic expression, but it could also have been created previously and stored in a workflow variable.</span></span> <span data-ttu-id="80899-177">Bu örnekte, <xref:System.Activities.Expressions.InvokeMethod%601> etkinliği yerine <xref:System.Activities.Statements.Assign%601> etkinliğinin kullanılması daha kolay olacaktır.</span><span class="sxs-lookup"><span data-stu-id="80899-177">In this example, it would be simpler to use the <xref:System.Activities.Statements.Assign%601> activity instead of the <xref:System.Activities.Expressions.InvokeMethod%601> activity.</span></span> <span data-ttu-id="80899-178">Son olarak <xref:System.Activities.Statements.Assign%601> veya <xref:System.Activities.Expressions.InvokeMethod%601> etkinlikleri tarafından çağrılan yöntem çağrısı uzun süre çalışıyorsa, <xref:System.Activities.Expressions.InvokeMethod%601> bir <xref:System.Activities.Expressions.InvokeMethod%601.RunAsynchronously%2A> özelliği olduğundan avantajı vardır.</span><span class="sxs-lookup"><span data-stu-id="80899-178">If the method call ultimately invoked by either the <xref:System.Activities.Statements.Assign%601> or <xref:System.Activities.Expressions.InvokeMethod%601> activities is long running, <xref:System.Activities.Expressions.InvokeMethod%601> has an advantage since it has a <xref:System.Activities.Expressions.InvokeMethod%601.RunAsynchronously%2A> property.</span></span> <span data-ttu-id="80899-179">Bu özellik `true`olarak ayarlandığında çağrılan yöntem iş akışı ile ilgili olarak zaman uyumsuz olarak çalışır.</span><span class="sxs-lookup"><span data-stu-id="80899-179">When this property is set to `true`, the invoked method will run asynchronously with regard to the workflow.</span></span> <span data-ttu-id="80899-180">Diğer etkinlikler paralel ise, yöntem zaman uyumsuz olarak yürütülerek bunlar engellenmeyecektir.</span><span class="sxs-lookup"><span data-stu-id="80899-180">If other activities are in parallel, they will not be blocked while the method is asynchronously executing.</span></span> <span data-ttu-id="80899-181">Ayrıca, çağrılacak yöntemin dönüş değeri yoksa <xref:System.Activities.Expressions.InvokeMethod%601> yöntemi çağırmak için uygun bir yoldur.</span><span class="sxs-lookup"><span data-stu-id="80899-181">Also, if the method to be invoked has no return value, then <xref:System.Activities.Expressions.InvokeMethod%601> is the appropriate way to invoke the method.</span></span>  
  
## <a name="arguments-and-dynamic-activities"></a><span data-ttu-id="80899-182">Bağımsız değişkenler ve dinamik etkinlikler</span><span class="sxs-lookup"><span data-stu-id="80899-182">Arguments and Dynamic Activities</span></span>  
 <span data-ttu-id="80899-183">Bir etkinlik ağacına etkinlikler bağlayarak ve tüm özellikleri ve bağımsız değişkenleri yapılandırarak kodda bir iş akışı tanımı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="80899-183">A workflow definition is created in code by assembling activities into an activity tree and configuring any properties and arguments.</span></span> <span data-ttu-id="80899-184">Var olan bağımsız değişkenler bağlanabilir, ancak yeni bağımsız değişkenler etkinliklere eklenemez.</span><span class="sxs-lookup"><span data-stu-id="80899-184">Existing arguments can be bound, but new arguments cannot be added to activities.</span></span> <span data-ttu-id="80899-185">Bu, kök etkinliğe geçirilen iş akışı bağımsız değişkenlerini içerir.</span><span class="sxs-lookup"><span data-stu-id="80899-185">This includes workflow arguments passed to the root activity.</span></span> <span data-ttu-id="80899-186">Kesinlik temelli kodda, iş akışı bağımsız değişkenleri yeni bir CLR türünde özellikler olarak belirtilir ve XAML 'de `x:Class` ve `x:Member`kullanılarak bildirilenler.</span><span class="sxs-lookup"><span data-stu-id="80899-186">In imperative code, workflow arguments are specified as properties on a new CLR type, and in XAML they are declared by using `x:Class` and `x:Member`.</span></span> <span data-ttu-id="80899-187">Bir iş akışı tanımı, bellek içi nesneler ağacı olarak oluşturulduğunda oluşturulan yeni bir CLR türü olmadığından, bağımsız değişkenler eklenemez.</span><span class="sxs-lookup"><span data-stu-id="80899-187">Because there is no new CLR type created when a workflow definition is created as a tree of in-memory objects, arguments cannot be added.</span></span> <span data-ttu-id="80899-188">Ancak, <xref:System.Activities.DynamicActivity>bağımsız değişkenler eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-188">However, arguments can be added to a <xref:System.Activities.DynamicActivity>.</span></span> <span data-ttu-id="80899-189">Bu örnekte, iki tamsayı bağımsız değişkeni alan bir <xref:System.Activities.DynamicActivity%601> oluşturulur, bunları birlikte ekler ve sonucu döndürür.</span><span class="sxs-lookup"><span data-stu-id="80899-189">In this example, a <xref:System.Activities.DynamicActivity%601> is created that takes two integer arguments, adds them together, and returns the result.</span></span> <span data-ttu-id="80899-190">Her bağımsız değişken için bir <xref:System.Activities.DynamicActivityProperty> oluşturulur ve işlemin sonucu <xref:System.Activities.DynamicActivity%601><xref:System.Activities.Activity%601.Result%2A> bağımsız değişkenine atanır.</span><span class="sxs-lookup"><span data-stu-id="80899-190">A <xref:System.Activities.DynamicActivityProperty> is created for each argument, and the result of the operation is assigned to the <xref:System.Activities.Activity%601.Result%2A> argument of the <xref:System.Activities.DynamicActivity%601>.</span></span>  
  
 [!code-csharp[CFX_WorkflowApplicationExample#55](~/samples/snippets/csharp/VS_Snippets_CFX/cfx_workflowapplicationexample/cs/program.cs#55)]  
  
 <span data-ttu-id="80899-191">Dinamik etkinlikler hakkında daha fazla bilgi için bkz. [çalışma zamanında etkinlik oluşturma](creating-an-activity-at-runtime-with-dynamicactivity.md).</span><span class="sxs-lookup"><span data-stu-id="80899-191">For more information about dynamic activities, see [Creating an Activity at Runtime](creating-an-activity-at-runtime-with-dynamicactivity.md).</span></span>  
  
## <a name="compiled-activities"></a><span data-ttu-id="80899-192">Derlenen etkinlikler</span><span class="sxs-lookup"><span data-stu-id="80899-192">Compiled Activities</span></span>  
 <span data-ttu-id="80899-193">Dinamik etkinlikler, kod kullanan bağımsız değişkenler içeren bir etkinlik tanımlamanın bir yoludur, ancak etkinlikler kodda oluşturulabilir ve türlere derlenmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-193">Dynamic activities are one way to define an activity that contains arguments using code, but activities can also be created in code and compiled into types.</span></span> <span data-ttu-id="80899-194"><xref:System.Activities.CodeActivity>türetilen basit etkinlikler ve <xref:System.Activities.AsyncCodeActivity>türetilen zaman uyumsuz etkinliklerden elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-194">Simple activities can be created that derive from <xref:System.Activities.CodeActivity>, and asynchronous activities that derive from <xref:System.Activities.AsyncCodeActivity>.</span></span> <span data-ttu-id="80899-195">Bu etkinlikler bağımsız değişkenlere, dönüş değerlerine sahip olabilir ve zorunlu kod kullanarak mantığını tanımlayabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-195">These activities can have arguments, return values, and define their logic using imperative code.</span></span> <span data-ttu-id="80899-196">Bu tür etkinlikleri oluşturma örnekleri için bkz. [CodeActivity temel sınıfı](workflow-activity-authoring-using-the-codeactivity-class.md) ve [zaman uyumsuz etkinlikler oluşturma](creating-asynchronous-activities-in-wf.md).</span><span class="sxs-lookup"><span data-stu-id="80899-196">For examples of creating these types of activities, see [CodeActivity Base Class](workflow-activity-authoring-using-the-codeactivity-class.md) and [Creating Asynchronous Activities](creating-asynchronous-activities-in-wf.md).</span></span>  
  
 <span data-ttu-id="80899-197"><xref:System.Activities.NativeActivity> türetilen etkinlikler, zorunlu kod kullanarak mantığını tanımlayabilir ve ayrıca mantığı tanımlayan alt etkinlikleri de içerebilir.</span><span class="sxs-lookup"><span data-stu-id="80899-197">Activities that derive from <xref:System.Activities.NativeActivity> can define their logic using imperative code and they can also contain child activities that define the logic.</span></span> <span data-ttu-id="80899-198">Ayrıca, çalışma zamanının yer işaretlerini oluşturma gibi özelliklerine tam erişimi de vardır.</span><span class="sxs-lookup"><span data-stu-id="80899-198">They also have full access to the features of the runtime such as creating bookmarks.</span></span> <span data-ttu-id="80899-199"><xref:System.Activities.NativeActivity>tabanlı etkinlik oluşturma örnekleri için bkz. [NativeActivity temel sınıfı](nativeactivity-base-class.md), [nasıl yapılır: etkinlik oluşturma](how-to-create-an-activity.md)ve [özel bileşik etkinlik örneği kullanımı](./samples/custom-composite-using-native-activity.md) .</span><span class="sxs-lookup"><span data-stu-id="80899-199">For examples of creating a <xref:System.Activities.NativeActivity>-based activity, see [NativeActivity Base Class](nativeactivity-base-class.md), [How to: Create an Activity](how-to-create-an-activity.md), and the [Custom Composite using Native Activity](./samples/custom-composite-using-native-activity.md) sample.</span></span>  
  
 <span data-ttu-id="80899-200"><xref:System.Activities.Activity> türetilen etkinlikler mantığını yalnızca alt etkinliklerin kullanımı üzerinden tanımlar.</span><span class="sxs-lookup"><span data-stu-id="80899-200">Activities that derive from <xref:System.Activities.Activity> define their logic solely through the use of child activities.</span></span> <span data-ttu-id="80899-201">Bu etkinlikler genellikle iş akışı Tasarımcısı kullanılarak oluşturulur, ancak kod kullanılarak da tanımlanabilir.</span><span class="sxs-lookup"><span data-stu-id="80899-201">These activities are typically created by using the workflow designer, but can also be defined by using code.</span></span> <span data-ttu-id="80899-202">Aşağıdaki örnekte, `Activity<int>`türetilen `Square` bir etkinlik tanımlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="80899-202">In the following example, a `Square` activity is defined that derives from `Activity<int>`.</span></span> <span data-ttu-id="80899-203">`Square` etkinliği `Value`adlı tek bir <xref:System.Activities.InArgument%601> sahiptir ve <xref:System.Activities.Activity.Implementation%2A> özelliğini kullanarak bir <xref:System.Activities.Statements.Sequence> etkinliği belirterek mantığını tanımlar.</span><span class="sxs-lookup"><span data-stu-id="80899-203">The `Square` activity has a single <xref:System.Activities.InArgument%601> named `Value`, and defines its logic by specifying a <xref:System.Activities.Statements.Sequence> activity using the <xref:System.Activities.Activity.Implementation%2A> property.</span></span> <span data-ttu-id="80899-204"><xref:System.Activities.Statements.Sequence> etkinliği bir <xref:System.Activities.Statements.WriteLine> etkinliği ve bir <xref:System.Activities.Statements.Assign%601> etkinliği içerir.</span><span class="sxs-lookup"><span data-stu-id="80899-204">The <xref:System.Activities.Statements.Sequence> activity contains a <xref:System.Activities.Statements.WriteLine> activity and an <xref:System.Activities.Statements.Assign%601> activity.</span></span> <span data-ttu-id="80899-205">Birlikte, bu üç etkinlik `Square` etkinliğinin mantığını uygular.</span><span class="sxs-lookup"><span data-stu-id="80899-205">Together, these three activities implement the logic of the `Square` activity.</span></span>  
  
```csharp  
class Square : Activity<int>  
{  
    [RequiredArgument]  
    public InArgument<int> Value { get; set; }  
  
    public Square()  
    {  
        this.Implementation = () => new Sequence  
        {  
            Activities =  
            {  
                new WriteLine  
                {  
                    Text = new InArgument<string>((env) => "Squaring the value: " + this.Value.Get(env))  
                },  
                new Assign<int>  
                {  
                    To = new OutArgument<int>((env) => this.Result.Get(env)),  
                    Value = new InArgument<int>((env) => this.Value.Get(env) * this.Value.Get(env))  
                }  
            }  
        };  
    }  
}  
```  
  
 <span data-ttu-id="80899-206">Aşağıdaki örnekte, tek bir `Square` etkinliğinden oluşan bir iş akışı tanımı <xref:System.Activities.WorkflowInvoker>kullanılarak çağrılır.</span><span class="sxs-lookup"><span data-stu-id="80899-206">In the following example, a workflow definition consisting of a single `Square` activity is invoked using <xref:System.Activities.WorkflowInvoker>.</span></span>  
  
```csharp  
Dictionary<string, object> inputs = new Dictionary<string, object> {{ "Value", 5}};  
int result = WorkflowInvoker.Invoke(new Square(), inputs);  
Console.WriteLine("Result: {0}", result);  
```  
  
 <span data-ttu-id="80899-207">İş akışı çağrıldığında, konsola aşağıdaki çıktı görüntülenir:</span><span class="sxs-lookup"><span data-stu-id="80899-207">When the workflow is invoked, the following output is displayed to the console:</span></span>  
  
 <span data-ttu-id="80899-208">**Karelerini alıp değeri: 5**</span><span class="sxs-lookup"><span data-stu-id="80899-208">**Squaring the value: 5**</span></span>  
<span data-ttu-id="80899-209">**Sonuç: 25**</span><span class="sxs-lookup"><span data-stu-id="80899-209">**Result: 25**</span></span>
