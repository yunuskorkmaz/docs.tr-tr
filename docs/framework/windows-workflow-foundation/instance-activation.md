---
title: Örnek Etkinleştirme
ms.date: 03/30/2017
ms.assetid: 134c3f70-5d4e-46d0-9d49-469a6643edd8
ms.openlocfilehash: 088722ba19a1f38e8a341e34a8344963021f1113
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/28/2019
ms.locfileid: "64584913"
---
# <a name="instance-activation"></a><span data-ttu-id="51f9b-102">Örnek Etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="51f9b-102">Instance Activation</span></span>
<span data-ttu-id="51f9b-103">SQL iş akışı örneği Store düzenli olarak çalıştırılabilir veya etkinleştirilebilir iş akışı örnekleri Kalıcılık veritabanında algılar ve uyanır bir iç görev çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-103">The SQL Workflow Instance Store runs an internal task that periodically wakes up and detects runnable or activatable workflow instances in the persistence database.</span></span> <span data-ttu-id="51f9b-104">Bir çalıştırılabilir iş akışı örneği bulursa örneğin etkinleştirme özelliğine sahip olan iş akışı ana bilgisayarı bildirir.</span><span class="sxs-lookup"><span data-stu-id="51f9b-104">If it finds a runnable workflow instance, it notifies the workflow host that is capable of activating the instance.</span></span> <span data-ttu-id="51f9b-105">Örnek deposuna etkinleştirilebilir iş akışı örneği bulursa, sırayla iş akışı örneği çalıştıran bir iş akışı ana etkinleştiren genel bir ana bilgisayar bildirir.</span><span class="sxs-lookup"><span data-stu-id="51f9b-105">If the instance store finds an activatable workflow instance, it notifies a generic host that activates a workflow host, which in turn runs the workflow instance.</span></span> <span data-ttu-id="51f9b-106">Bu konunun aşağıdaki bölümlerinde ayrıntılı örnek etkinleştirme işlemi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-106">The following sections in this topic explain the instance activation process in detail.</span></span>  
  
## <a name="RunnableSection"></a> <span data-ttu-id="51f9b-107">Algılama ve çalıştırılabilir iş akışı örnekleri etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="51f9b-107">Detecting and Activating Runnable Workflow Instances</span></span>  
 <span data-ttu-id="51f9b-108">SQL iş akışı örneği Store iş akışı örneği göz önünde bulundurur *çalıştırılabilir* örneği askıya alınma durumunda veya tamamlandı durumuna değilse ve aşağıdaki koşulları karşılayan:</span><span class="sxs-lookup"><span data-stu-id="51f9b-108">The SQL Workflow Instance Store considers a workflow instance *runnable* if the instance is not in the suspended state or the completed state and satisfies the following conditions:</span></span>  
  
- <span data-ttu-id="51f9b-109">Örnek kilitli olup süresi doldu bekleyen bir zamanlayıcı içerir.</span><span class="sxs-lookup"><span data-stu-id="51f9b-109">The instance is unlocked and has a pending timer that has expired.</span></span>  
  
- <span data-ttu-id="51f9b-110">Örneğinin süresi dolmuş bir kilit üzerinde vardır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-110">The instance has an expired lock on it.</span></span>  
  
- <span data-ttu-id="51f9b-111">Örnek kilidi açılmış ve durumunun **Executing**.</span><span class="sxs-lookup"><span data-stu-id="51f9b-111">The instance is unlocked and its status is **Executing**.</span></span>  
  
 <span data-ttu-id="51f9b-112">SQL iş akışı örneği Store başlatır <xref:System.Activities.DurableInstancing.HasRunnableWorkflowEvent> çalıştırılabilir bir örneği bulduğunda.</span><span class="sxs-lookup"><span data-stu-id="51f9b-112">The SQL Workflow Instance Store raises the <xref:System.Activities.DurableInstancing.HasRunnableWorkflowEvent> when it finds a runnable instance.</span></span> <span data-ttu-id="51f9b-113">Bundan sonra SqlWorkflowInstanceStore durdurur kadar izleme <xref:System.Activities.DurableInstancing.TryLoadRunnableWorkflowCommand> mağaza kez çağrılır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-113">After this, the SqlWorkflowInstanceStore stops monitoring until the <xref:System.Activities.DurableInstancing.TryLoadRunnableWorkflowCommand> is called once on the store.</span></span>  
  
 <span data-ttu-id="51f9b-114">Abone bir iş akışı ana bilgisayarı <xref:System.Activities.DurableInstancing.HasRunnableWorkflowEvent> ve yükleme özellikli örneği yürütür <xref:System.Activities.DurableInstancing.TryLoadRunnableWorkflowCommand> belleğe örneği yüklemek için örnek deposu ile karşılaştırarak.</span><span class="sxs-lookup"><span data-stu-id="51f9b-114">A workflow host that has subscribed for the <xref:System.Activities.DurableInstancing.HasRunnableWorkflowEvent> and capable of loading the instance executes the <xref:System.Activities.DurableInstancing.TryLoadRunnableWorkflowCommand> against the instance store to load the instance into memory.</span></span> <span data-ttu-id="51f9b-115">Bir iş akışı ana bilgisayarı ana bilgisayar ve örnek meta veri özelliği varsa, bir iş akışı örneği yükleniyor uyumlu olarak kabul edilir **WorkflowServiceType** aynı değere ayarlar.</span><span class="sxs-lookup"><span data-stu-id="51f9b-115">A workflow host is considered capable of loading a workflow instance if the host and the instance have metadata property **WorkflowServiceType** set to the same value.</span></span>  
  
## <a name="detecting-and-activating-activatable-workflow-instances"></a><span data-ttu-id="51f9b-116">Algılama ve etkinleştirilebilir iş akışı örnekleri etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="51f9b-116">Detecting and Activating Activatable Workflow Instances</span></span>  
 <span data-ttu-id="51f9b-117">Bir iş akışı örneği kabul *etkinleştirilebilir* örneği çalıştırılabilir ve varsa örneği yüklerken birini özelliğine sahip olan hiçbir iş akışı ana bilgisayarda çalışan.</span><span class="sxs-lookup"><span data-stu-id="51f9b-117">A workflow instance is considered *activatable* if the instance is runnable and there is no workflow host that is capable of loading the instance is running on the computer.</span></span> <span data-ttu-id="51f9b-118">Algılama ve etkinleştirme çalıştırılabilir iş akışı örnekleri üzerinde çalıştırılabilir bir iş akışı örneğinin tanımını için bkz.</span><span class="sxs-lookup"><span data-stu-id="51f9b-118">See Detecting and Activating Runnable Workflow Instances above for the definition of a runnable workflow instance.</span></span>  
  
 <span data-ttu-id="51f9b-119">SQL iş akışı örneği Store başlatır <xref:System.Activities.DurableInstancing.HasActivatableWorkflowEvent> veritabanında etkinleştirilebilir iş akışı örneği bulduğunda bu.</span><span class="sxs-lookup"><span data-stu-id="51f9b-119">The SQL Workflow Instance Store raises the <xref:System.Activities.DurableInstancing.HasActivatableWorkflowEvent> when it finds an activatable workflow instance in the database.</span></span> <span data-ttu-id="51f9b-120">Bundan sonra SqlWorkflowInstanceStore durdurur kadar izleme <xref:System.Activities.DurableInstancing.QueryActivatableWorkflowsCommand> mağaza kez çağrılır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-120">After this, the SqlWorkflowInstanceStore stops monitoring until the <xref:System.Activities.DurableInstancing.QueryActivatableWorkflowsCommand> is called once on the store.</span></span>  
  
 <span data-ttu-id="51f9b-121">Bir genel ana bilgisayar için abone olduğunda <xref:System.Activities.DurableInstancing.HasActivatableWorkflowEvent> olay aldığında yürütülmeden <xref:System.Activities.DurableInstancing.QueryActivatableWorkflowsCommand> etkinleştirme parametresi bir iş akışı ana bilgisayar oluşturmak için gereken almak için örnek deposu ile karşılaştırarak.</span><span class="sxs-lookup"><span data-stu-id="51f9b-121">When a generic host that has subscribed for the <xref:System.Activities.DurableInstancing.HasActivatableWorkflowEvent> receives the event, it executes the <xref:System.Activities.DurableInstancing.QueryActivatableWorkflowsCommand> against the instance store to obtain activation parameters required to create a workflow host.</span></span> <span data-ttu-id="51f9b-122">Genel konak hangi sırayla yükler ve çalıştırılabilir hizmet örneği çalıştıran bir iş akışı ana bilgisayar oluşturmak için bu etkinleştirme parametrelerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="51f9b-122">The generic host uses these activation parameters to create a workflow host, which in turn loads and runs the runnable service instance.</span></span>  
  
## <a name="generic-hosts"></a><span data-ttu-id="51f9b-123">Genel konakları</span><span class="sxs-lookup"><span data-stu-id="51f9b-123">Generic Hosts</span></span>  
 <span data-ttu-id="51f9b-124">Meta veri özelliğinin değeri olan bir konak genel yöneticisidir **WorkflowServiceType** genel konakları için ayarlanmış **WorkflowServiceType.Any** herhangi bir iş akışı türü işleyebileceğini belirtmek için.</span><span class="sxs-lookup"><span data-stu-id="51f9b-124">A generic host is a host with the value of the metadata property **WorkflowServiceType** for generic hosts is set to **WorkflowServiceType.Any** to indicate that it can handle any workflow type.</span></span> <span data-ttu-id="51f9b-125">Genel bir ana bilgisayar adında bir XName parametresinin **ActivationType**.</span><span class="sxs-lookup"><span data-stu-id="51f9b-125">A generic host has an XName parameter named **ActivationType**.</span></span>  
  
 <span data-ttu-id="51f9b-126">Şu anda SQL iş akışı örneği Store kümesine ActivationType parametresinin değeri genel konaklarla destekler **WAS**.</span><span class="sxs-lookup"><span data-stu-id="51f9b-126">Currently, the SQL Workflow Instance Store supports generic hosts with value of the ActivationType parameter set to **WAS**.</span></span> <span data-ttu-id="51f9b-127">SQL iş akışı örneği Store ActivationType WAS için ayarlanmazsa, oluşturur bir <xref:System.Runtime.DurableInstancing.InstancePersistenceException>.</span><span class="sxs-lookup"><span data-stu-id="51f9b-127">If the ActivationType is not set to WAS, the SQL Workflow Instance Store throws an <xref:System.Runtime.DurableInstancing.InstancePersistenceException>.</span></span> <span data-ttu-id="51f9b-128">İş akışı yönetimi hizmeti ile birlikte gelen [!INCLUDE[dublin](../../../includes/dublin-md.md)] kümesine etkinleştirme türü olan genel bir ana bilgisayar **WAS**.</span><span class="sxs-lookup"><span data-stu-id="51f9b-128">The Workflow Management Service that ships with the [!INCLUDE[dublin](../../../includes/dublin-md.md)] is a generic host that has the activation type set to **WAS**.</span></span>  
  
 <span data-ttu-id="51f9b-129">WAS etkinleştirme için genel bir konağa bir dizi yeni konakları etkinleştirilebilmesi için uç nokta adresini türetmek için etkinleştirme parametresi gerektirir.</span><span class="sxs-lookup"><span data-stu-id="51f9b-129">For WAS activation, a generic host requires a set of activation parameters to derive the endpoint address at which new hosts can be activated.</span></span> <span data-ttu-id="51f9b-130">WAS etkinleştirme için etkinleştirme site, uygulamanın site göreli yolunu ve hizmetinin uygulama göreli yolunu adını parametrelerdir.</span><span class="sxs-lookup"><span data-stu-id="51f9b-130">The activation parameters for WAS activation are name of the site, path to the application relative to the site, and path to the service relative to the application.</span></span> <span data-ttu-id="51f9b-131">SQL iş akışı örneği Store yürütülmesi sırasında bu etkinleştirme parametreleri depolayan <xref:System.Activities.DurableInstancing.SaveWorkflowCommand>.</span><span class="sxs-lookup"><span data-stu-id="51f9b-131">The SQL Workflow Instance Store stores these activation parameters during the execution of the <xref:System.Activities.DurableInstancing.SaveWorkflowCommand>.</span></span>  
  
## <a name="runnable-instances-detection-period"></a><span data-ttu-id="51f9b-132">Çalıştırılabilir Örnekleri Algılama Dönemi</span><span class="sxs-lookup"><span data-stu-id="51f9b-132">Runnable Instances Detection Period</span></span>  
 <span data-ttu-id="51f9b-133">**Çalıştırılabilir örnekleri algılama dönemi** SQL iş akışı örneği Store özelliği SQL iş akışı örneği Store sonra çalıştırılabilir veya etkinleştirilebilir akışlarınızın algılamak için algılama görev çalıştırır süreyi belirtir Önceki saptama döngüsünden sonra Kalıcılık veritabanı örnekleri.</span><span class="sxs-lookup"><span data-stu-id="51f9b-133">The **Runnable Instances Detection Period** property of the SQL Workflow Instance Store specifies the time period after which the SQL Workflow Instance Store runs a detection task to detect any runnable or activatable workflow instances in the persistence database after the previous detection cycle.</span></span> <span data-ttu-id="51f9b-134">Bkz: [çalıştırılabilir örnekleri algılama dönemi](runnable-instances-detection-period.md) bu özellik hakkında daha fazla bilgi.</span><span class="sxs-lookup"><span data-stu-id="51f9b-134">See [Runnable Instances Detection Period](runnable-instances-detection-period.md) for more details on this property.</span></span>
