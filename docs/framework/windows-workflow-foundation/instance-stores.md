---
title: Örnek depolar
ms.custom: ''
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
ms.assetid: f2629668-0923-4987-b943-67477131c1e0
caps.latest.revision: 14
author: dotnet-bot
ms.author: dotnetcontent
manager: wpickett
ms.workload:
- dotnet
ms.openlocfilehash: a41c25dc3c664715bd9e811d6a21a6e3600aa8a5
ms.sourcegitcommit: 03ee570f6f528a7d23a4221dcb26a9498edbdf8c
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/28/2018
---
# <a name="instance-stores"></a><span data-ttu-id="eb134-102">Örnek depolar</span><span class="sxs-lookup"><span data-stu-id="eb134-102">Instance Stores</span></span>
<span data-ttu-id="eb134-103">Bir örnek deposuna örneklerinin mantıksal bir kapsayıcısıdır.</span><span class="sxs-lookup"><span data-stu-id="eb134-103">An instance store is a logical container of instances.</span></span> <span data-ttu-id="eb134-104">Örnek veri ve meta veri depolandığı yerdir.</span><span class="sxs-lookup"><span data-stu-id="eb134-104">It is the place where the instance data and metadata is stored.</span></span> <span data-ttu-id="eb134-105">Bir örnek deposuna ayrılmış bir fiziksel depolama göstermez.</span><span class="sxs-lookup"><span data-stu-id="eb134-105">An instance store does not imply dedicated physical storage.</span></span> <span data-ttu-id="eb134-106">Bir örnek deposuna dayanıklı bir SQL Server veritabanında veya dayanıklı olmayan durum bilgileri bellek içerebilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-106">An instance store could contain durable information in a SQL Server database or non-durable state information in a memory.</span></span> <span data-ttu-id="eb134-107">[!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] Örnek verileri ve meta verileri bir SQL Server 2005 veya SQL Server 2008 veritabanına kalıcı hale getirmek iş akışları sağlayan bir örnek deposuna somut bir uygulamasıdır SQL iş akışı örneği deposuna birlikte verilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-107">The [!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] ships with the SQL Workflow Instance Store, which is a concrete implementation of an instance store that allows workflows to persist instance data and metadata into a SQL Server 2005 or SQL Server 2008 database.</span></span> <span data-ttu-id="eb134-108">Ayrıca Windows Server App Fabric ayrıca bir örnek deposuna somut bir uygulamasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="eb134-108">In addition Windows Server App Fabric also provides a concrete implementation of an instance store.</span></span> <span data-ttu-id="eb134-109">Daha fazla bilgi için bkz: [Windows Server App Fabric örnek deposu, sorgu ve denetim sağlayıcıları](http://go.microsoft.com/fwlink/?LinkID=201201&clcid=0x409).</span><span class="sxs-lookup"><span data-stu-id="eb134-109">For more information, see [Windows Server App Fabric Instance Store, Query, and Control Providers](http://go.microsoft.com/fwlink/?LinkID=201201&clcid=0x409).</span></span>  
  
 <span data-ttu-id="eb134-110">API kalıcı bir ana bilgisayar ve komut istekleri göndermek ana bilgisayar tanır bir örnek deposuna arasındaki arabirimdir (örneğin, <xref:System.Activities.DurableInstancing.LoadWorkflowCommand> ve <xref:System.Activities.DurableInstancing.SaveWorkflowCommand>) örnek deposuna.</span><span class="sxs-lookup"><span data-stu-id="eb134-110">The persistence API is the interface between a host and an instance store that allows the host to send command requests (for example, <xref:System.Activities.DurableInstancing.LoadWorkflowCommand> and <xref:System.Activities.DurableInstancing.SaveWorkflowCommand>) to the instance store.</span></span> <span data-ttu-id="eb134-111">Bu API somut uyarlamasını kalıcı bir sağlayıcı adı verilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-111">The concrete implementation of this API is called a persistence provider.</span></span> <span data-ttu-id="eb134-112">Kalıcılık sağlayıcı bir ana bilgisayardan isteklerini alır ve örnek deposuna değiştirir.</span><span class="sxs-lookup"><span data-stu-id="eb134-112">The persistence provider receives requests from a host and modifies the instance store.</span></span>  
  
 <span data-ttu-id="eb134-113">Konaklar ve örnek depolar; böylece bir ana bilgisayar birçok örnek depoları ile kullanılabilir ve bir örnek deposuna birçok konaklarla kullanılabilir eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-113">Hosts and instance stores are pluggable so that a host can be used with many instance stores, and an instance store can be used with many hosts.</span></span> <span data-ttu-id="eb134-114">Örnek deposuna ve ana bilgisayar üzerinde bağımsız yaşam döngüsü gelişmesi ancak bir örnek deposuna genellikle belirli bir konağa kullanım desenlerini için optimize edilmiştir.</span><span class="sxs-lookup"><span data-stu-id="eb134-114">An instance store is typically optimized for the usage patterns of a particular host, although the instance store and host may evolve on independent life cycles.</span></span> <span data-ttu-id="eb134-115">Örneğin, **WorkflowServiceHost** ve **SqlWorkflowInstanceStore** birlikte çalışmak üzere tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="eb134-115">For example, the **WorkflowServiceHost** and the **SqlWorkflowInstanceStore** are designed to work well together.</span></span> <span data-ttu-id="eb134-116">Veri ve meta veri akışı hizmet örnekleri devam etmek ve bu örnek deposuna ile kullanmak için kendi örnek deposuna oluşturabilirsiniz **WorkflowServiceHost**.</span><span class="sxs-lookup"><span data-stu-id="eb134-116">You can create your own instance store to persist data and metadata of workflow service instances and use that instance store with the **WorkflowServiceHost**.</span></span> <span data-ttu-id="eb134-117">Örneğin, bir SQL Server veritabanına kaydetme yerine bir Oracle veritabanına bilgilerinin sürdürülmesi iş akışları sağlayan bir OracleWorkflowInstanceStore oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="eb134-117">For example, you can create an OracleWorkflowInstanceStore that lets workflows persist information into an Oracle database instead of saving them into a SQL Server database.</span></span>  
  
 <span data-ttu-id="eb134-118">Kalıcı nesneler değiştirir ek işlevsellik ile genişletilmesi konaklar için yaygın bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="eb134-118">It is common for hosts to be extended with additional functionality that modifies the persisted objects.</span></span> <span data-ttu-id="eb134-119">Örneğin, bir örnek Kalıcılık sistemi bir iş akışı ana bilgisayar "Askıda" işlem ve bir SQL örneği deposu destekleyen uzantı olabilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-119">For example, an instance persistence system may have a workflow host, an extension that supports the "Suspend" operation, and an SQL instance store.</span></span>  <span data-ttu-id="eb134-120">İş akışı ana standart bir komut gönderebilir gibi kaydetmek veya bir iş akışı örneği deposundan yüklemek veya kaydetmek veya bir iş akışı bir örnek deposuna kaydetmek için yükleyin.</span><span class="sxs-lookup"><span data-stu-id="eb134-120">The workflow host might send a standard command such as Save or Load to save or load a workflow from an instance store or to save a workflow into an instance store.</span></span> <span data-ttu-id="eb134-121">Askıya alma uzantısı kaydetme ve askıya alınmış iş akışı örneği yüklenemiyor böylece iş akışı örnekleri yükleme komutları ek semantiği eklemek.</span><span class="sxs-lookup"><span data-stu-id="eb134-121">The suspend extension might add additional semantics to the commands for saving and loading workflow instances so that a suspended workflow instance cannot be loaded.</span></span> <span data-ttu-id="eb134-122">SQL örneği depolama için Kalıcılık sağlayıcısı kaydetme ve iş akışı örnekleri yükleme komutları bilir ve bir SQL Server veritabanında kalıcı nesne tablolarını değiştirme yordamları Implements uygun çağırarak komutları depolanır.</span><span class="sxs-lookup"><span data-stu-id="eb134-122">The persistence provider for the SQL instance store understands the commands for saving and loading workflow instances, and implements the commands by calling appropriate stored procedures that change the tables of persistent objects in an SQL Server database.</span></span>  
  
 <span data-ttu-id="eb134-123">Bir ana bilgisayar örneği deposundaki örneği sahibi olarak görev yapar.</span><span class="sxs-lookup"><span data-stu-id="eb134-123">A host acts as an instance owner within an instance store.</span></span> <span data-ttu-id="eb134-124">Bir ana bilgisayar aynı anda birden fazla örnek deposuna sahip birden fazla örneği sahibi olarak çalışabilir.</span><span class="sxs-lookup"><span data-stu-id="eb134-124">A host may act as more than one instance owner with more than one instance store at the same time.</span></span> <span data-ttu-id="eb134-125">Ana bilgisayar GUID'ler örneği için örnekleriyle ilişkili tuşlarını sağlar.</span><span class="sxs-lookup"><span data-stu-id="eb134-125">The host provides GUIDs for instance keys associated with the instances.</span></span> <span data-ttu-id="eb134-126">Bir örneği anahtarı örneğini tanımlayan benzersiz bir diğer ad değil.</span><span class="sxs-lookup"><span data-stu-id="eb134-126">An instance key is a unique alias that identifies an instance.</span></span> <span data-ttu-id="eb134-127">Kalıcılık sistem oluşturur, güncelleştirir ve ana bilgisayarı tarafından istenen komutları yürütür gibi örneği sahibi bilgileri siler.</span><span class="sxs-lookup"><span data-stu-id="eb134-127">The persistence system creates, updates, and deletes instance owner information as it executes commands requested by hosts.</span></span>  
  
 <span data-ttu-id="eb134-128">Aşağıdaki listede örnek deposuna ana bilgisayarın etkileşim dahil edilen önemli adımları içerir:</span><span class="sxs-lookup"><span data-stu-id="eb134-128">The following list contains the important steps involved in the host’s interaction with the instance store:</span></span>  
  
1.  <span data-ttu-id="eb134-129">Elde bir **InstanceStore** Kalıcılık sağlayıcısından.</span><span class="sxs-lookup"><span data-stu-id="eb134-129">Obtain an **InstanceStore** from a persistence provider.</span></span>  

2.  <span data-ttu-id="eb134-130">Bir örneği için tanıtıcı çağırarak elde <xref:System.Runtime.DurableInstancing.InstanceStore.CreateInstanceHandle%2A> yöntemi **InstanceStore**.</span><span class="sxs-lookup"><span data-stu-id="eb134-130">Obtain the handle to an instance by calling the <xref:System.Runtime.DurableInstancing.InstanceStore.CreateInstanceHandle%2A> method on the **InstanceStore**.</span></span>  
  
3.  <span data-ttu-id="eb134-131">Örnek tanıtıcı karşı komutları çağırarak çağırma <xref:System.Runtime.DurableInstancing.InstanceStore.Execute%2A> yöntemi **InstanceStore**.</span><span class="sxs-lookup"><span data-stu-id="eb134-131">Invoke commands against the instance handle by calling the <xref:System.Runtime.DurableInstancing.InstanceStore.Execute%2A> method on the **InstanceStore**.</span></span>  
  
4.  <span data-ttu-id="eb134-132">İncelemek <xref:System.Runtime.DurableInstancing.InstanceView> tarafından döndürülen **InstanceStore.Execute** komutları sonuçlarını belirlemek için.</span><span class="sxs-lookup"><span data-stu-id="eb134-132">Examine the <xref:System.Runtime.DurableInstancing.InstanceView> returned by **InstanceStore.Execute** to determine the results of the commands.</span></span>
