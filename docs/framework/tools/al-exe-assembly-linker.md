---
title: Al.exe (Derleme Bağlayıcı)
ms.date: 03/30/2017
helpviewer_keywords:
- Al.exe
- Assembly Linker
- modules, Assembly Linker
- assembly manifest, Assembly Linker
ms.assetid: b5382965-0053-47cf-b92f-862860275a01
ms.openlocfilehash: b7081f00e31dfa8ac46909e61c7d389ba7954878
ms.sourcegitcommit: 27db07ffb26f76912feefba7b884313547410db5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 05/19/2020
ms.locfileid: "83611412"
---
# <a name="alexe-assembly-linker"></a>Al.exe (Derleme Bağlayıcı)

Assembly Linker, modül veya kaynak dosyalar olan bir veya daha fazla dosyadan, derleme bildirimi içeren bir dosya oluşturur. Modül, derleme bildirimi içermeyen bir ara dil (IL) dosyasıdır.

> [!NOTE]
> Visual Studio 2008 ile başlayarak, C# ve Visual Basic derleyicileri otomatik olarak derlemeye bir Win32 bildirimi katıştırır. Daha fazla bilgi için bkz. [-win32manifest (C# derleyici seçenekleri)](../../csharp/language-reference/compiler-options/win32manifest-compiler-option.md).

Bu araç, Visual Studio ile birlikte otomatik olarak yüklenir. Aracı çalıştırmak için, Visual Studio için Geliştirici Komut İstemi (veya Windows 7 ' de Visual Studio komut Istemi) kullanın. Daha fazla bilgi için bkz. [komut istemleri](developer-command-prompt-for-vs.md).

Komut satırına şunu yazın:

## <a name="syntax"></a>Söz dizimi

```console
al sources options
```

## <a name="parameters"></a>Parametreler

Aşağıdakilerden birini veya birkaçını belirtebilirsiniz `sources` .

| Kaynak | Açıklama |
| ------ | ----------- |
|`file`[,`target`]|`file`(Bir modül) içeriğini tarafından belirtilen dosya adına kopyalar `target` . Kopyaladıktan sonra, *al. exe* `target` bir derlemeye derlenir.|
|**/embed [kaynak]:** `file` [,`name` [,`private`]]|Tarafından belirtilen kaynağı, `file` derleme bildirimini içeren görüntüde katıştırır; *Al. exe* , içeriğini `file` TAŞINABILIR çalıştırılabilir (PE) görüntüsüne kopyalar.<br /><br /> `name`Parametresi, kaynak için bir iç tanıtıcıdır. Varsayılan olarak, kaynaklar derleme içinde geneldir (başka derlemeler tarafından görülür). Belirtme `private` , kaynağı diğer derlemelere görünmez hale getirir.<br /><br /> `file`Örneğin, [kaynak dosya Oluşturucu (*Resgen. exe*)](resgen-exe-resource-file-generator.md) veya geliştirme ortamında oluşturulmuş bir .NET Framework kaynak dosyası ise, içindeki üyeleriyle erişilebilir <xref:System.Resources> . Daha fazla bilgi için bkz. <xref:System.Resources.ResourceManager>. Diğer tüm kaynaklar için, `GetManifestResource*` <xref:System.Reflection.Assembly> çalışma zamanında kaynağa erişmek için içindeki yöntemleri kullanın.<br /><br /> *Al. exe*' ye yalnızca kaynak dosyaları geçirilirse, çıkış dosyası bir uydu kaynak derlemesidir.|
|**/Link [kaynak]:** `file` [,`name` [,`target` [,`private`]]]|Kaynak dosyayı bir derlemeye bağlar. Tarafından belirtilen kaynak `file` derlemenin bir parçası haline gelir; dosya kopyalanmaz. `file`Parametresi herhangi bir dosya biçiminde olabilir. Örneğin, parametresi olarak bir yerel DLL belirtebilirsiniz `file` . Bu, yerel DLL'yi derlemenin parçası haline getirir, böylece genel derleme önbelleğine yüklenebilir ve derlemedeki yönetilen koddan erişilebilir. Bunu, **/linkresource** derleyici seçeneğini kullanarak da yapabilirsiniz. Daha fazla bilgi için bkz. [-linkresource (C# derleyici seçenekleri)](../../csharp/language-reference/compiler-options/linkresource-compiler-option.md).<br /><br /> `name`Parametresi, kaynak için bir iç tanıtıcıdır. `target`Parametresi, *al. exe* ' nin kopyaladığı yolu ve dosya adını belirtir `file` *.* Kopyaladıktan sonra, *al. exe* `target` bir derlemeye derlenir. Varsayılan olarak, kaynaklar derleme içinde geneldir (başka derlemeler tarafından görülür). Belirtme `private` , kaynağı diğer derlemelere görünmez hale getirir.<br /><br /> `file`Örneğin, kaynak dosya Oluşturucu (*Resgen. exe*) veya geliştirme ortamında oluşturulmuş bir .NET Framework kaynak dosyası ise, <xref:System.Resources> ad alanındaki üyelerle erişilebilir. Daha fazla bilgi için bkz. <xref:System.Resources.ResourceManager>. Diğer tüm kaynaklar için, `GetManifestResource*` <xref:System.Reflection.Assembly> çalışma zamanında kaynağa erişmek üzere sınıfındaki yöntemleri kullanın.<br /><br /> *Al. exe*' ye yalnızca kaynak dosyaları geçirilirse, çıkış dosyası bir uydu kaynak derlemesidir.|

Aşağıdakileri belirtebilirsiniz `options` ; **/Out**belirtmeniz gerekir.

| Seçenek | Açıklama |
| ------ | ----------- |
|**/AlgId:**`id`|Derleme bildirimini içeren dosya hariç, çok dosyalı bir derlemede tüm dosyaları karma yapmak için bir algoritma belirtir. Varsayılan algoritma CALG_SHA1'dir. Diğer algoritmalar için Platform SDK belgelerindeki ALG_ID'ye bakın. .NET Framework'ün ilk sürümü için yalnızca CALG_SHA1 ve CALG_MD5 geçerlidir.<br /><br /> Karma değerleri derleme bildiriminin dosya tablosunda depolanır. Kurulum ve yükleme zamanında derleme dosyaları kendi karmaları ile denetlenir.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyAlgorithmIdAttribute> herhangi bir modülün kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/Base [Adres]:**`addr`|Çalışma zamanında kullanıcının bilgisayarına DLL yüklenecek adresi belirtir. İşlem alanında DLL'lerin yerini işletim sisteminin yeniden bulmasına izin vermektense, DLL'lerin temel adresini belirtirseniz, uygulamalar daha hızlı yüklenir.|
|**/bugreport**:`filename`|`filename`Hataları raporlamak için bilgi içeren bir dosya () oluşturur.|
|**/comp [any]:**`text`|Derlemedeki Şirket alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, dosya `text` Gezgini 'nde `Company` dosyanın özelliği olarak görünür. **/Win32res**belirtirseniz, belirtilen kaynak dosyasındaki şirket bilgileri `Company` Dosya Gezgini 'nde özellik olarak görüntülenir.<br /><br /> Metin boş bir dize ("") ise, Win32 `Company` kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/Company** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyCompanyAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/config [uration]:**`text`|Derlemedeki Yapılandırma alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> Metin boş bir dize ise, Win32 Yapılandırma kaynağı tek bir boşluk olarak görünür.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyConfigurationAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/copy [sağ]:**`text`|Derlemedeki Telif Hakkı alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/telif hakkı** Dosya Gezgini 'nde Win32 telif hakkı kaynağı olarak görünür.<br /><br /> Metin boş bir dize ise, Win32 Telif Hakkı kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/telif hakkı** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyCopyrightAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/c [yer]:**`text`|Derleme ile ilişkilendirilecek için kültür dizeyini belirtir. Kültür için geçerli değerler "Dillerin Tanımlanması İle İlgili Etiketler" başlıklı Internet Requests for Comments (RFC) 1766 belgesinde tanımlanan değerlerdir.<br /><br /> Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Varsayılan bir kültür dizesi yoktur. Bu dize, yansıma ile görüntülemek için kullanılabilir.<br /><br /> Geçerli dizeler hakkında daha fazla bilgi için `text` bkz <xref:System.Globalization.CultureInfo> ..<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyCultureAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/Delay [oturum açma] [+&#124;-]**|Derlemenin tamamen veya kısmen imzalanacağını belirtir. Tam olarak imzalanan bir derleme istiyorsanız **/delaysign-** kullanın. Yalnızca derlemeye ortak anahtar eklemek istiyorsanız **/delaysign +** kullanın.<br /><br /> Tam olarak imzalanan bir derleme istediğinizde, *al. exe* , bildirimi (derleme meta verileri) içeren dosyayı karma hale getirir ve bu karmayı özel anahtarla imzalar. Elde edilen dijital imza, bildirimi içeren dosyada depolanır. Bir bütünleştirilmiş kod gecikmeli imzalanmışsa, *al. exe* imzayı hesaplamaz ve depolamaz, ancak imzanın daha sonra eklenebilmesi için yalnızca dosyadaki alanı ayırır.<br /><br /> Varsayılan değer **/delaysign-**' dır.<br /><br /> **/Delaysign** seçeneği, **/keyfile** veya **/KeyName**ile kullanılmadığı müddetçe hiçbir etkiye sahip değildir.<br /><br /> Örneğin, **/delaysign +** kullanılması, bir sınayıcı 'ın derlemeyi genel önbellekte yerleştirmesine olanak sağlar. Sınamanın ardından, özel anahtarı derlemeye ekleyip derlemeyi tam olarak imzalayabilirsiniz.<br /><br /> Note: bir gecikmeli imzalanmış derlemeyi genel önbelleğe koymak için [ *Gacutil. exe* ' yi (genel bütünleştirilmiş kod önbelleği aracı)](gacutil-exe-gac-tool.md) kullanmadan önce, doğrulama atlama Için derlemeyi kaydetmek üzere [ *sn. exe* (tanımlayıcı ad aracı)](sn-exe-strong-name-tool.md) kullanın. Örneğin, `Sn.exe –Vr delaySignedAssembly`. Bunu yalnızca geliştirme için kullanın.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyDelaySignAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/DESCR [iption]:**`text`|Derlemedeki alan için bir dize belirtir <xref:System.Reflection.AssemblyDescriptionAttribute.Description%2A> . Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/Description** Dosya Gezgini 'nde Win32 **Comments** kaynağı olarak görünür.<br /><br /> Metin boş bir dize ise, Win32 **açıklamaları** kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/Description** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyDescriptionAttribute.Description%2A> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/e [vidence]:**`file`|`file`Derlemeyi Security. kanıt kaynak adıyla katıştırır.<br /><br /> Security.Evidence öğesini normal kaynaklar için kullanamazsınız.|
|**/fileversion:**`version`|Derlemedeki **dosya sürümü** alanı için bir dize belirtir. Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/fileversion** Win32 **dosya sürümü** kaynağı olarak kullanılacaktır. **/Fileversion**belirtmezseniz, Win32 **dosya sürümü** kaynağı Win32 **derleme sürümü** kaynağı tarafından doldurulur.<br /><br /> **/Win32res** belirtilirse, **/fileversion** Win32 kaynağını etkilemez.<br /><br /> Ayrıca bu seçeneği, herhangi bir MSIL modülü için kaynak kodunda özel bir öznitelik (AssemblyFileVersionAttribute) olarak da belirtebilirsiniz.|
|**/Flags:**`flags`|Derlemedeki alan için bir değer belirtir `Flags` . İçin olası değerler `flags` :<br /><br /> 0x0000<br /> Derleme, yan yana uyumludur.<br /><br /> 0x0010<br /> Derleme, aynı uygulama etki alanında çalıştırıldığında diğer sürümlerle yürütülemez.<br /><br /> 0x0020<br /> Derleme, aynı işlemde çalıştırıldığında diğer sürümlerle yürütülemez.<br /><br /> 0x0030<br /> Derleme, aynı bilgisayarda çalıştırıldığında diğer sürümlerle yürütülemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyFlagsAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/fullpaths**|*Al. exe* ' nin bir hata iletisinde bildirilen tüm dosyalar için mutlak yolu kullanmasına neden olur.|
|**/Help**|Araç için komut sözdizimini ve seçenekleri görüntüler.|
|**/keyf [ile]:**`filename`|Bir `filename` Derlemeyi imzalamak için bir anahtar çifti veya yalnızca ortak anahtar içeren bir dosyayı () belirtir. Derleyici ortak anahtarı derleme bildirimine ekler ve ardından son derlemeyi özel anahtarla imzalar. Anahtar dosyaları oluşturma ve anahtar çiftlerini anahtar kapsayıcılarına yükleme hakkında bilgi için bkz. [tanımlayıcı ad Aracı (*sn. exe*)](sn-exe-strong-name-tool.md) .<br /><br /> Gecikmeli imzalama kullanıyorsanız, bu dosyada genellikle ortak anahtar vardır, ancak özel anahtar bulunmaz.<br /><br /> Ortak anahtar (anahtar çiftine ait) derlemenin .publickey alanında görüntülenir.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyKeyFileAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.<br /><br /> Aynı derlemede hem **/keyfile** hem de **/KeyName** belirtilirse (komut satırı seçeneği ya da özel öznitelik tarafından), *al. exe* öncelikle **/KeyName**ile belirtilen kapsayıcıyı dener. Bu başarılı olursa, derleme anahtar kapsayıcısındaki bilgilerle imzalanır. *Al. exe* anahtar kapsayıcısını bulamazsa, **/keyfile**ile belirtilen dosyayı dener. Bu başarılı olursa, derleme anahtar dosyasındaki bilgilerle imzalanır ve anahtar bilgisi anahtar kapsayıcısına yüklenir ( [*sn. exe*](sn-exe-strong-name-tool.md)' de-i seçeneğine benzer), sonraki derlemede **/KeyName** seçeneği geçerli olacaktır.|
|**/keyn [AME]:**`text`|Anahtar çifti içeren bir kapsayıcıyı belirtir. Bu, derleme bildirimine ortak anahtar ekleyerek derlemeyi imzalar (ona tanımlayıcı ad verir). *Al. exe* , son derlemeyi özel anahtarla imzalayacaktır.<br /><br /> Bir anahtar çifti oluşturmak için *sn. exe* kullanın.<br /><br /> Anahtar bilgileri derlemenin .publickey alanında görüntülenir.<br /><br /> `text`Gömülü bir boşluk varsa çift tırnak işareti ("") koyun.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyKeyNameAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/Main:**`method`|`class` `method` Bir modülü yürütülebilir bir dosyaya dönüştürürken giriş noktası olarak kullanılacak yöntemin tam adını (.) belirtir.|
|**/nologo**|*Al. exe*' yi çağırdığınızda komut satırında görünen başlığı veya logoyu göstermez.|
|**/Out:**`filename`|*Al. exe*tarafından oluşturulan dosyanın adını belirtir. Bu gerekli bir seçenektir.|
|**/Platform:**`text`|Bu kodun çalıştırılabileceği platformları sınırlar; x86, Itanium, x64, anycpu (varsayılan) veya anycpu32bitpreferredx86 olmalıdır.|
|**/prod [UCT]:**`text`|Derlemedeki **ürün** alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/Product** Dosya Gezgini 'nde Win32 **ürün adı** kaynağı olarak görünür.<br /><br /> Metin boş bir dize ise, Win32 **ürün adı** kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/Product** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyProductAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/productv [Erma]:**`text`|Derlemedeki **ürün sürümü** alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/ProductVersion&lt** Win32 **ürün sürümü** kaynağı olarak kullanılacaktır. **/ProductVersion&lt**belirtmezseniz, Win32 **ürün sürümü** kaynağı Win32 **dosya sürümü** kaynağı tarafından doldurulur.<br /><br /> **/Win32res**belirtirseniz, **/ProductVersion&lt** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyInformationalVersionAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/t [arget]:** `lib[rary]` &#124; `exe` &#124;`win[exe]`|Çıkış dosyasının dosya biçimini belirtir: `lib[rary]` (kod kitaplığı), `exe` (konsol uygulaması) veya `win[exe]` (Windows tabanlı uygulama). Varsayılan değer: `lib[rary]`.|
|**/Template:**`filename`|`filename`Kültür alanı dışında tüm derleme meta verilerinin devraldığı derlemeyi belirtir.<br /><br /> **/Template** ile oluşturduğunuz bir derleme uydu bütünleştirilmiş kodu olacaktır.|
|**/title:**`text`|Derlemedeki **başlık** alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, bir uygulamanın kolay adı olarak kabuk tarafından kullanılan Win32 **Açıklama** kaynağı olarak dosya Gezgini 'nde **/title** görüntülenir. Ayrıca, birden çok destekleyici uygulamanın bulunduğu bir dosya türü için kısayol menüsünün **birlikte Aç** alt menüsünde de görüntülenir.<br /><br /> Metin boş bir dize ise, Win32 **Açıklama** kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/title** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyTitleAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/Trade [işaret]:**`text`|Derlemedeki **ticari marka** alanı için bir dize belirtir. Dizeyi, bir boşluk içeriyorsa çift tırnak işaretleri ("") içine koyun `text` . Bu dize, derleme üzerinde özel bir özniteliktir ve yansıma ile görüntülemek için kullanılabilir.<br /><br /> **/Win32res**belirtmezseniz, **/ticari** marka dosya Gezgini 'nde Win32 **ticari marka** kaynağı olarak görünür.<br /><br /> Metin boş bir dize ise, Win32 **ticari marka** kaynağı tek bir boşluk olarak görünür.<br /><br /> **/Win32res**belirtirseniz, **/ticari marka** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyTrademarkAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/v [Erma]:**`version`|Bu derlemenin sürüm bilgilerini belirtir. Sürüm dizesinin biçimi `major` . `minor` .. `build` `revision` . Varsayılan değer 0 ' dır.<br /><br /> **/Version**belirtirseniz, öğesini belirtmeniz gerekir `major` . `major`Ve belirtirseniz `minor` , için bir yıldız işareti ( \* ) belirtebilirsiniz `build` . Bu, `build` 1 ocak 2000 ' den beri geçen gün sayısına eşit olmasına ve `revision` geçerli günün gece yarısından (yerel saat) itibaren geçen saniye sayısına eşit olmasına neden olur.<br /><br /> , Ve belirtirseniz `major` , `minor` `build` için bir yıldız işareti belirtebilirsiniz `revision` . Bu, `revision` geçerli günün gece yarısından beri geçen saniye sayısına eşit olmasına neden olur.<br /><br /> Özetlemek gerekirse, geçerli sürüm dizeleri aşağıda belirtilmiştir:<br /><br /> X<br /><br /> X.X<br /><br /> X.X.\*<br /><br /> X.X.X<br /><br /> X.X.X.\*<br /><br /> X.X.X.X<br /><br /> burada X, 65535 (0-65534) dışında işaretsiz kısa bir sabit sayıdır.<br /><br /> **/Win32res**belirtmezseniz, **/Version** Win32 **derleme sürümü** kaynağı olarak kullanılacaktır.<br /><br /> **/Win32res**, **/ProductVersion&lt**ve **/fileversion**belirtmezseniz, **derleme sürümü**, dosya sürümü ve **ürün sürümü** Win32 kaynakları için **/Version** kullanılır.<br /><br /> **/Win32res**belirtirseniz, **/Version** Win32 kaynak bilgilerini etkilemez.<br /><br /> Bu seçeneği, <xref:System.Reflection.AssemblyVersionAttribute> herhangi BIR MSIL Modülü için kaynak kodunda özel bir öznitelik () olarak da belirtebilirsiniz.|
|**/Win32Icon:**`filename`|Derlemeye bir .ico simge dosyası ekler. .ico dosyası, çıktı dosyasına Dosya Gezgini'nde istenen görünümü verir.|
|**/win32res:**`filename`|Çıktı dosyasına bir Win32 kaynağı (.res dosyası) ekler. Kaynak Derleyicisi kullanılarak bir Win32 kaynak dosyası oluşturulabilir. Kaynak Derleyicisi, bir Visual C++ programını derlerken çağrılır; .rc dosyasından bir .res dosyası oluşturulur.|
|`@filename`|*Al. exe* komutlarını içeren bir yanıt dosyası belirtir.<br /><br /> Yanıt dosyasındaki komutlar her satırda birer birer veya aynı satırda bir veya daha fazla boşlukla ayrılmış şekilde görünebilir.|
|**/?**|Araç için komut sözdizimini ve seçenekleri görüntüler.|

## <a name="remarks"></a>Açıklamalar

Tüm Visual Studio derleyicileri derlemeler üretir. Ancak, bir veya daha fazla modülünüz (bildirim olmadan meta veriler) varsa, ayrı bir dosyada bildirime sahip bir derleme oluşturmak için *al. exe* ' yi kullanabilirsiniz.

Derlemeleri önbellekte yüklemek, önbellekteki derlemeleri kaldırmak veya önbelleğin içeriğini listelemek için [genel bütünleştirilmiş kod önbelleği aracı (*Gacutil. exe*)](gacutil-exe-gac-tool.md)kullanın.

## <a name="errors-and-warnings"></a>Hatalar ve Uyarılar

Aşağıdaki tabloda *al. exe*tarafından oluşturulan hatalar listelenmektedir.

| Hata | Açıklama |
| ----- | ----------- |
|al1001|İç derleyici hatası<br /><br /> *Al. exe* ' nin beklenmeyen sözdizimini ayrıştıramadığı için başarısız olup olmadığını belirlemeyi deneyin. Ardından, Microsoft Ürün Destek Hizmetleri 'ne başvurun.|
|al1002|Yetersiz bellek<br /><br /> *Al. exe* belleği tükendi ve durduruldu. Kullanılabilir bellek miktarını artırın.|
|al1003|' Option ' derleyici seçeneğinin arkasından bir bağımsız değişken gelmelidir<br /><br /> *Al. exe* bir bağımsız değişkenin bir komut satırı seçeneğine geçirilmesini bekliyordu. Örneğin, **/AlgId:** belirtirseniz, bir algoritma tanımlayıcısı geçirmeniz gerekir.|
|al1004|Beklenmeyen ortak dil çalışma zamanı başlatma hatası — ' Reason '<br /><br /> *Al. exe* , Visual Studio yüklemesinde veya belirtilen nedenden dolayı ortak dil çalışma zamanının bir hata bildirdi.|
|al1005|' Dosya ' dosyasının açılması çok büyük<br /><br /> *Al. exe* tarafından açılan tüm dosyalar 4 GIGABAYTTAN (GB) küçük olmalıdır.|
|al1006|' Dosya ' yanıt dosyası zaten eklendi<br /><br /> Aynı yanıt dosyası, `@file` komut satırında birden çok kez belirtildi (). Yanıt dosyası yalnızca bir kez dahil edilebilir.|
|al1007|' Dosya ' yanıt dosyasını açma hatası — ' Reason '<br /><br /> *Al. exe* belirtilen nedenden dolayı belirtilen yanıt dosyasını açamıyor.|
|al1008|' Option ' komut satırı seçeneği için dosya belirtimi eksik<br /><br /> *Al. exe* bir dosyanın komut satırı seçeneğine geçirilmesini bekliyordu. Örneğin, **/Out** seçeneğini belirtirseniz bir dosya belirtmeniz gerekir.|
|al1009|' Dosya ' yazma için açılamıyor<br /><br /> *Al. exe* , çıkış derleme dosyası gibi bir dosyaya yazamadı. Disk dolu olabilir, dosya salt okunurdur veya dosya için izinleriniz olmayabilir.|
|al1010|Komut satırı sözdizimi hatası: ' Option ' seçeneği için ': Text ' eksik<br /><br /> *Al. exe* bir bağımsız değişkenin bir komut satırı seçeneğine geçirilmesini bekliyordu. Örneğin, **/title** seçeneğini belirtirseniz, bir dize geçirmeniz gerekir.|
|al1011|' Dosya ' dosyası yürütülebilir bir dosyadır ve bir metin dosyası olarak açılamaz<br /><br /> Bir metin dosyasının beklenildiği bir ikili dosya belirtildi. Örneğin, bir ikili dosya komut satırına yanıt dosyası olarak geçirilirse bu hata oluşur.|
|al1012|' Value ', ' Option ' seçeneği için geçerli bir ayar değildir<br /><br /> Komut satırı seçeneğine beklenmeyen bir değer geçirildi. Örneğin, **/target** seçeneğine geçersiz bir değer belirtirseniz bu hata oluşur.|
|al1013|Tanınmayan komut satırı seçeneği: ' Option '<br /><br /> Geçersiz bir komut satırı seçeneği belirtildi.|
|al1014|Beklenmeyen başlatma hatası — ' Reason '<br /><br /> *Al. exe* bir com başlatma hatası algıladı. Bu, belleğin yetersizliğinden kaynaklanıyor olabilir, ancak olası bir nedeni sistem DLL dosyalarıdır. Microsoft Visual Studio gibi Otomasyon kullanan veya COM kullanan bir programı çalıştırırsanız benzer bir hata görmeniz gerekir.<br /><br /> İşletim sistemini yeniden yükleyin.|
|al1015|' Alınkuı. dll ' ileti dosyası bulunamıyor<br /><br /> *Al. exe* , *alınkuı. dll*gerektirir. Bu dosyanın yolunuzda olduğundan emin olun. Gerekirse, ürün CD 'sinden kopyalayın.|
|al1016|Geçerli bir giriş dosyası belirtilmedi<br /><br /> *Al. exe* , derleme bilgilerine sahip olmayan bir veya daha fazla giriş dosyası gerektirir.|
|al1017|Hedef dosya adı belirtilmedi<br /><br /> Hedef dosya adını belirten gerekli **/Out** seçeneği eksikti.|
|al1018|Gerekli ' dosya ' dosyası yüklenemedi<br /><br /> Belirli DLL dosyaları yüklenemiyor. Visual Studio 'Yu veya Windows SDK yeniden yükleyin.|
|al1019|Derleme oluştururken meta veri hatası — neden<br /><br /> Derlemenin oluşturulması belirtilen nedenden dolayı kesildi. Örneğin, **/win32res** seçeneğiyle belirttiğiniz bir dosya bulunamazsa bu hata oluşur.|
|al1020|İçerilen ' File ' derlemesi yoksayılıyor<br /><br /> Bir derlemeyi içeren bir giriş dosyası belirtildi. *Al. exe* giriş dosyaları derlemeler içeremez.|
|al1021|' ayar ': önceki ayarı geçersiz kılma<br /><br /> Bir modülün belirli bir ayar için bir değeri vardı, büyük olasılıkla bir *al. exe* komut satırı seçeneği kullanılarak geçilen bir değerle geçersiz kılınan özel öznitelikler aracılığıyla atanır.|
|al1022|Katıştırılmış kaynak ' file' okunurken hata; neden<br /><br /> *Al. exe* , belirtilen nedenden dolayı **/embedresource** seçeneğine geçirilen dosyayı okuyamıyor.|
|al1023|' File' kaynağını katıştırma hatası — neden<br /><br /> İşletim sistemi, kaynak dosyasını belirtilen nedenden dolayı derlemeye katıştıramaz.|
|al1025|ComType kaydı ' Record ', geçersiz bir ' Record ' dosya kaydına işaret ediyor<br /><br /> Giriş modülündeki meta veriler geçersiz. Modülü üreten araç düzeltilmelidir.|
|al1026|Belirtilen ' version ' sürümü geçersiz<br /><br /> Geçerli biçimler için **/Version** seçeneği hakkında bilgi için bkz..|
|al1028|' File ' anahtar dosyasında imzalama için gereken özel anahtar eksik<br /><br /> **/Keyfile** seçeneğine yalnızca ortak anahtarı içeren bir anahtar dosyası geçirildi. Aşağıdaki komutta gösterildiği gibi, hem ortak hem de özel anahtara sahip bir dosya oluşturmak için [tanımlayıcı ad aracı 'nı (*sn. exe*)](sn-exe-strong-name-tool.md) kullanın.<br /><br /> `sn -k keypair.snk.`|
|al1029|Anahtar kapsayıcı adı ' Container ' yok<br /><br /> **/KeyName** seçeneğine geçirilen değer geçerli bir kapsayıcı değil. Bir kapsayıcı oluşturmak için [tanımlayıcı ad aracı 'nı (*sn. exe*)](sn-exe-strong-name-tool.md) kullanın.|
|al1030|Şifreleme hizmeti düzgün yüklenmemiş veya uygun bir anahtar sağlayıcısına sahip değil<br /><br /> İşletim sistemini yeniden yüklemeniz veya anahtarı oluşturmak için kullanılan bir şifreleme yardımcı programını yüklemeniz gerekebilir.|
|al1031|' File' simgesi okunurken hata oluştu — neden<br /><br /> *Al. exe* , belirtilen nedenden dolayı **/Win32Icon** seçeneğine geçirilen dosyayı okuyamıyor|
|al1032|' File' için kaynaklar oluşturulurken hata oluştu — neden<br /><br /> *Al. exe* yetersiz disk alanı veya başka bir hata nedeniyle dosya oluşturamaz. Bu hata, **/Win32Icon** seçeneğini belirttiğinizde (bir. ico dosyası oluşturur) veya **/win32res** seçeneğini belirtmediğinde (kaynak bilgilerine sahip bir dosya oluşturan) oluşur.<br /><br /> Dosya oluşturma sorununu çözemezseniz, sürüm veya bit eşlem (simge) bilgilerini içerebilen bir dosyayı belirten **/win32res**komutunu kullanın.|
|al1033|' Attribute ' derleme özel özniteliği farklı değerlerle birden çok kez belirtildi<br /><br /> *Al. exe*' ye girdi olarak belirtilen kaynak modüllerinde aynı özel özniteliğin iki tekrarine farklı değerler geçirildi.|
|al1034|' File ' bütünleştirilmiş kodu kopyalanamıyor veya yeniden adlandırılamaz<br /><br /> Hem bir giriş dosyası belirtmenizi hem de kopyalamanızı sağlayan *al. exe* söz dizimini kullanırken, derleyiciyi durduran bir ad çakışması vardır. Örneğin, bu hata belirtirseniz oluşur `input.dll,somename.dll /out:somename.dll` .|
|al1035|Kitaplıkların giriş noktası olamaz<br /><br /> **/Target: lib** seçeneğini (varsayılan) ve **/Main** seçeneğini birlikte belirtemezsiniz.|
|al1036|Çalıştırılabilir uygulamalar için giriş noktası gerekiyor<br /><br /> **/Target: exe** veya **/target: Win** seçeneğini kullanırken, **/Main** seçeneğini de belirtmeniz gerekir.|
|al1037|' Main ' giriş noktası yöntemi bulunamıyor<br /><br /> *Al. exe* `Main` , **/Main** seçeneği tarafından belirtilen konumda bir yöntem bulamıyor.|
|al1039|Genel bütünleştirilmiş kod önbelleği Yöneticisi başlatılamadı; nedeni<br /><br /> Visual Studio 'Yu veya Windows SDK yeniden yükleyin.|
|al1040|Bütünleştirilmiş kod önbelleğe yüklenemedi — neden<br /><br /> Önbelleğe yalnızca imzalı derlemeler bağlanabilir. Daha fazla bilgi için bkz. [genel derleme önbelleği](../app-domains/gac.md).|
|al1041|' Method ': imza veya görünürlük yanlış olduğundan veya genel olduğundan giriş noktası olamaz<br /><br /> **/Main** seçeneğiyle bir yöntem belirtildi, ancak bu yöntem statik değil, döndürmüyor `int` veya `void` , genel değildi veya geçersiz bağımsız değişkenlere sahip.|
|al1042|' exe ': EXEs, modüller eklenemiyor<br /><br /> Derlemesi olmayan bir *. exe* dosyası, *al. exe*için bir giriş dosyası olarak belirtilmiştir. *Al. exe* yalnızca derleme içermeyen *DLL* dosyalarını giriş dosyaları olarak alabilir.|
|al1043|' Name ' bildirim dosyası adı herhangi bir modülle aynı olamaz<br /><br /> **/Out** seçeneğiyle belirtilen ad, *al. exe*' ye girdi olarak belirtilen dosya adlarından biriyle aynı olamaz.|
|al1044|' File' anahtar dosyası okunurken hata oluştu — neden<br /><br /> **/Keyfile** veya ile belirtilen bir dosya açılırken veya okunurken bir hata oluştu <xref:System.Reflection.AssemblyKeyFileAttribute> .|
|al1045|' File ' dosya adı çok uzun veya geçersiz<br /><br /> *Al. exe*' ye 260 karakterden daha uzun bir dosya adı geçirildi. Daha az karakter veya daha kısa bir yol içeren bir dosya adı seçin veya dosyayı yeniden adlandırın.|
|al1046|' KIMLIK ' kaynak tanımlayıcısı bu derlemede zaten kullanılmış<br /><br /> Gömülü veya bağlantılı iki kaynak aynı tanımlayıcıya veya ada (ikinci bağımsız değişken) sahiptir. Çakışan kaynaklardan birini kaldırın veya yeniden adlandırın.|
|al1047|' File' dosyası içeri aktarılırken hata oluştu — neden<br /><br /> Belirtilen nedenden dolayı bir modül dosyası açılamıyor.|
|al1048|' Assembly' derlemesinin ' Module ' modülü içeri aktarılırken hata oluştu — neden<br /><br /> Çok dosyalı bir derlemenin bildirim olmayan dosyası açılırken bir hata oluştu. Bu hata doğrudan *al. exe*tarafından yayınlanmaz, ancak *al. exe*kullanan bir işleme programlı olarak geçirilebilir.|
|al1049|1 Ocak 2000 ' den önceki tarihler için derleme ve düzeltme sürüm numaraları otomatik olarak üretilemiyor<br /><br /> Bilgisayarınızdaki sistem saati 1 Ocak 2000 ' den önceki bir tarihe ayarlanır.|
|al1050|' Eski özellik ' kullandığınız özellik artık desteklenmiyor; Lütfen bunun yerine ' yeni özellik ' kullanın<br /><br /> *Al. exe* tarafından daha önce desteklenen bir özellik artık kullanımdan kalkmıştır. Bunun yerine önerilen özelliği kullanın.|
|al1051|' Attribute ' özniteliği verilirken hata oluştu — neden<br /><br /> Derleme özel özniteliği, belirtilen nedenden dolayı *al. exe* tarafından işlenmedi.|
|al1052|' Filename ' dosyası bir derleme değil<br /><br /> **/Template** ile belirtilen dosya bütünleştirilmiş kod meta verisi içermelidir. Bu hata, **/Template** tarafından belirtilen dosyanın bir derleme içermediğini gösterir.|
|al1053|' Option ' için belirtilen ' version ' sürümü normal ' ana. ikincil. derleme. düzeltme ' biçiminde değil<br /><br /> *Al. exe* , **/fileversion** veya **/ProductVersion&lt** seçenekleriyle belirtilen hatalı oluşturulmuş sürüm bilgileri algıladı.|
|al1054|' Option ' için belirtilen ' version ' sürümü normal ' ana. ikincil. derleme. düzeltme ' biçiminde değil<br /><br /> *Al. exe* , ile belirtilen hatalı oluşturulmuş sürüm bilgileri algıladı <xref:System.Resources.SatelliteContractVersionAttribute> .|
|al1055|Başvurulan ' filename ' derlemesinin tanımlayıcı adı yok<br /><br /> Bu hata, güçlü bir ada sahip bir derleme oluştururken ve tanımlayıcı adı olmayan bir derlemeye başvuru yaptığınızda verilir. Bunu yapmak için derlemenizi tanımlayıcı bir adla yeniden oluşturmanız ya da *sn. exe* kullanarak derlemeye tanımlayıcı bir ad eklemeniz gerekir ( [*sn. exe*](sn-exe-strong-name-tool.md)belgelerine bakın).<br /><br /> Bu hatanın yaygın bir şekilde oluşması, COM nesnelerini, Visual Studio IDE aracılığıyla bir C# projesine bir başvuru eklediğinizde olduğu gibi sarmalayıcı derlemeler aracılığıyla kullanırken kullanmaktır. Hatayı önlemek için, "sarmalayıcı derleme anahtarı dosyası/adı" proje özelliğindeki COM sarmalayıcı derlemeleri için tanımlayıcı ad anahtar dosyasını belirtebilirsiniz<br /><br /> Sarmalayıcı derlemesini Tlbimp aracılığıyla oluşturuyorsanız, sarmalayıcı derlemesine tanımlayıcı bir ad atama hakkında bilgi için [Tlbimp](tlbimp-exe-type-library-importer.md) belgelerine bakın.<br /><br /> Bir derlemenin tanımlayıcı adı varsa, bu, genel derleme önbelleğine yüklenebilir. Sonuç olarak, başvurulan derlemeler da genel derleme önbelleğine gider. Yalnızca tanımlayıcı adlara sahip derlemeler genel derleme önbelleğine gidebilir.|
|al1056|Başvurulan ' filename ' bütünleştirilmiş kodu yerelleştirilmiş bir uydu bütünleştirilmiş kodu<br /><br /> <xref:System.Reflection.AssemblyCultureAttribute>Geçerli derleme oluşturulurken özniteliği kullanılarak oluşturulan bir derlemeye başvuruldu. <xref:System.Reflection.AssemblyCultureAttribute>Öznitelik, dosyanın yerelleştirilmiş bir uydu derlemesi olduğunu ve bir uydu derlemesine başvurmak için uygun olmadığını gösterir. Bunun yerine ana üst derlemeye başvurun.|
|al1057|Yürütülebilir dosyalar yerelleştirilemez, kültür her zaman boş olmalıdır<br /><br /> Bir bütünleştirilmiş kod **/target: exe** kullanılarak oluşturuluyor, ancak **/Culture** belirtildi. *. Exe* Içindeki derlemeler kültür alanında bilgi alamaz.|
|al1058|' File ' bir derlemedir ve modül olarak eklenemez<br /><br /> C++ derlemesinde **/ASSEMBLYMODULE** (bağlayıcı seçeneği) bir derleme içeren bir dosya geçirdi.|
|al1059|Bilinmeyen hata (kod)<br /><br /> *Al. exe* bilinmeyen bir hata kodu aldı ( `code` ).<br /><br /> Olası çözümler şunlardır:<br /><br /> Visual Studio'yu yeniden yükleyin.<br /><br /> Windows SDK yeniden yükleyin.<br /><br /> Eksik dosyaları denetle.<br /><br /> Yeterli disk alanı olup olmadığını denetleyin.<br /><br /> Yeterli bellek olup olmadığını denetleyin.<br /><br /> Dosyalara erişebilecek olabilecek diğer süreçler durdurulur.<br /><br /> Bilgisayarınızı yeniden başlatın.|
|al1060|Karmalar oluşturulurken şifreleme hatası oluştu — neden<br /><br /> Çok dosyalı bir derleme için dosya karmaları oluşturulurken hata oluştu.|
|al1061|' Reason ' seçeneği ayarlanamıyor çünkü ' Reason '<br /><br /> Bu seçenek için belirtilen değer, belirtilen nedenden dolayı geçersiz.|
|al1062|' Module ' modülü birden çok kez belirtildi; yalnızca bir kez dahil edilecek<br /><br /> Bu uyarı, komut satırında aynı kaynak, giriş veya modül dosyası birden çok kez belirtildiğinde oluşturulur. Dosya adını yalnızca bir kez belirttiğinizden emin olun.|
|al1063|' Type ' ortak türü bu derlemede birden çok konumda tanımlanmış: ' FILE1 ' ve ' dosya2 '<br /><br /> Aynı tür, derlemede birden fazla modülde bulundu. Bir derlemede her bir türün yalnızca bir sürümü bulunabilir.|
|al1064|Birden çok/bugreport seçeneği belirtilemiyor.<br /><br /> Yalnızca bir **/bugreport** seçeneğine izin verilir.|
|al1065|' Dosya adı ' dosya adı çok uzun veya geçersiz<br /><br /> Belirtilen dosya adı izin verilen üst sınırdan daha uzun.|
|al1066|' Character ' karakterine komut satırında veya yanıt dosyalarında izin verilmiyor<br /><br /> Komut satırında veya bir dosyada geçersiz bir karakter bulundu.|
|al1067|' filename ' bir metin dosyası yerine bir ikili dosyadır<br /><br /> Dosya yerine ikili biçimde.|
|al1068|' ModuleName ' modülü bu derlemede zaten tanımlanmış. Her bağlantılı kaynak ve modülün benzersiz bir dosya adı olmalıdır.<br /><br /> Modül Bu derlemede birden çok kez geçiyor.|
|al1069|Aynı kısa dosya adına sahip uzun bir dosya adı zaten varken ' filename ' kısa dosya adı oluşturulamıyor<br /><br /> Geçerli dosya, zaten var olan bir dosya adının kısa sürümü olan bir ada sahip. Örneğin, LongFileName.cs derleniyor ve sonra LongFi ~ 1. cs yeniden derlenerek buna benzer bir derleyici hatasına neden olur. Uzun adlara sahip derleyici çıkış dosyaları silinmişse, ancak benzer bağlayıcı dosyaları kaldığı takdirde bu hata oluşabilir.|
|al1070|Agnostic derlemesinde işlemciye özgü bir modül ' Module Name ' olamaz<br /><br /> **/Platform: agnostic** (veya **/Platform**belirtmezseniz) kullanarak oluşturuyorsanız, belirsiz olmayan bir modül ( **/addmodule**kullanarak) eklemeyi denerseniz bir hata oluşturulur. Bu, bir i386 obj dosyasını ia64 obj 'e bağlamayı deniyor gibidir.<br /><br /> Agkıtik olmayan modüllerin ana kaynağı C++ ' dır. Bir C++ modülüyle **/addmodule** kullanıyorsanız, uygun **/Platform** ayarını belirtmek için derleme betiklerinizi değiştirmeniz gerekebilir.|
|al1072|' Module Name ' bütünleştirilmiş kodu ve modülü farklı işlemcileri hedefleyemez<br /><br /> Bir derlemeyi ve farklı işlemciler için hedeflenen modülü bağlayamazsınız çünkü sonucun tek bir işlemcide çalışması gerekir.|
|al1073|Başvurulan ' Assembly ' derlemesi farklı bir işlemciyi hedefliyor<br /><br /> Farklı işlemciler için hedeflenen derlemeleri bağlayamazsınız çünkü sonucun tek bir işlemci üzerinde çalışması gerekir.|
|al1074|' Dosya adı ' içinde depolanan modül adı ' modül adı ' dosya adı ile eşleşmelidir<br /><br /> Bağlayıcı için bu gereklidir. Bu sorunu çözmek için iki adı aynı yapın.|
|al1075|Gecikmeli imzalama istendi, ancak anahtar sağlanmadı<br /><br /> Bir derlemenin gecikmesi gecikmeli kaydolduğunda, derleyici imzayı hesaplamaz ve depolamaz, ancak imzanın daha sonra eklenebilmesi için dosyada alanı ayırır.<br /><br /> Örneğin, **/delaysign +** kullanılması, bir sınayıcı 'ın derlemeyi genel önbellekte yerleştirmesine olanak sağlar. Test ettikten sonra derleme bağlayıcı yardımcı programını kullanarak derlemeye özel anahtar ekleyerek derlemeyi tamamen imzalayabilirsiniz.|
|al1076|' Type ' türü birden çok derlemeye iletilir: ' Assembly ' ve ' Assembly '.<br /><br /> Bir tür yalnızca bir derlemeye iletilebilir.|
|al1077|' Type ' ortak türü ' Assembly ' içinde tanımlanmış ve ' Assembly ' öğesine iletildi.<br /><br /> Oluşturulan derlemede yinelenen bir ortak tür var. Biri geçerli bir tür tanımıdır ve diğeri bir tür ileticidir.|

## <a name="example"></a>Örnek

Aşağıdaki komut, modülünden bir bütünleştirilmiş kod ile *T2A. exe* yürütülebilir dosyası oluşturur `t2.netmodule` . Giriş noktası, `Main` içindeki yöntemidir `MyClass` .

```console
al t2.netmodule /target:exe /out:t2a.exe /main:MyClass.Main
```

## <a name="see-also"></a>Ayrıca bkz.

- [Araçlar](index.md)
- [*Sn. exe* (tanımlayıcı ad aracı)](sn-exe-strong-name-tool.md)
- [*Gacutil. exe* (genel bütünleştirilmiş kod önbelleği aracı)](gacutil-exe-gac-tool.md)
- [Derlemelerle Programlama](../../standard/assembly/index.md)
- [Komut Istemleri](developer-command-prompt-for-vs.md)
