---
title: Derleme Yükleme için En İyi Yöntemler
description: .NET ' te derleme yükleme için en iyi uygulamaları araştırma. Geçersiz yayınlarına, eksik yöntemlere ve diğer özel durumlara yol açabilecek kimlik tür sorunlarından kaçının.
ms.date: 03/30/2017
helpviewer_keywords:
- assemblies,binding
- LoadFrom method
- default load context
- TypeLoadException class,causes
- assembly loading errors
- load contexts
- assemblies,loading
- LoadWithPartialName method
- load-from context
ms.assetid: 68d1c539-6a47-4614-ab59-4b071c9d4b4c
ms.openlocfilehash: 11ac4277081392b1e7ac79147f621ff67d699fe5
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/15/2020
ms.locfileid: "90555308"
---
# <a name="best-practices-for-assembly-loading"></a><span data-ttu-id="08508-104">Derleme Yükleme için En İyi Yöntemler</span><span class="sxs-lookup"><span data-stu-id="08508-104">Best Practices for Assembly Loading</span></span>
<span data-ttu-id="08508-105">Bu makalede <xref:System.InvalidCastException> ,, ve diğer hatalara yol açabilecek kimlik tür sorunlarından kaçınmanın yolları anlatılmaktadır <xref:System.MissingMethodException> .</span><span class="sxs-lookup"><span data-stu-id="08508-105">This article discusses ways to avoid problems of type identity that can lead to <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, and other errors.</span></span> <span data-ttu-id="08508-106">Makalede aşağıdaki öneriler ele alınmaktadır:</span><span class="sxs-lookup"><span data-stu-id="08508-106">The article discusses the following recommendations:</span></span>  
  
- [<span data-ttu-id="08508-107">Yük bağlamlarının olumlu ve olumsuz yönlerini anlayın</span><span class="sxs-lookup"><span data-stu-id="08508-107">Understand the advantages and disadvantages of load contexts</span></span>](#load_contexts)  
  
- [<span data-ttu-id="08508-108">Kısmi derleme adlarında bağlamayı önleyin</span><span class="sxs-lookup"><span data-stu-id="08508-108">Avoid binding on partial assembly names</span></span>](#avoid_partial_names)  
  
- [<span data-ttu-id="08508-109">Bir derlemeyi birden çok bağlama yüklemeden kaçının</span><span class="sxs-lookup"><span data-stu-id="08508-109">Avoid loading an assembly into multiple contexts</span></span>](#avoid_loading_into_multiple_contexts)  
  
- [<span data-ttu-id="08508-110">Bir derlemenin birden çok sürümünü aynı içeriğe yüklemeden kaçının</span><span class="sxs-lookup"><span data-stu-id="08508-110">Avoid loading multiple versions of an assembly into the same context</span></span>](#avoid_loading_multiple_versions)  
  
- [<span data-ttu-id="08508-111">Varsayılan yükleme bağlamına geçmeyi düşünün</span><span class="sxs-lookup"><span data-stu-id="08508-111">Consider switching to the default load context</span></span>](#switch_to_default)  
  
 <span data-ttu-id="08508-112">İlk öneri, [Yük bağlamlarının olumlu ve olumsuz yönlerini anlamak](#load_contexts)için, bunların hepsi yük bağlamlarına bağlı olduğundan diğer önerilere yönelik arka plan bilgileri sağlar.</span><span class="sxs-lookup"><span data-stu-id="08508-112">The first recommendation, [understand the advantages and disadvantages of load contexts](#load_contexts), provides background information for the other recommendations, because they all depend on a knowledge of load contexts.</span></span>  
  
<a name="load_contexts"></a>
## <a name="understand-the-advantages-and-disadvantages-of-load-contexts"></a><span data-ttu-id="08508-113">Yük bağlamlarının olumlu ve olumsuz yönlerini anlayın</span><span class="sxs-lookup"><span data-stu-id="08508-113">Understand the Advantages and Disadvantages of Load Contexts</span></span>  
 <span data-ttu-id="08508-114">Bir uygulama etki alanı içinde, derlemeler üç bağlamdan birine yüklenebilir ya da bağlam olmadan yüklenebilirler:</span><span class="sxs-lookup"><span data-stu-id="08508-114">Within an application domain, assemblies can be loaded into one of three contexts, or they can be loaded without context:</span></span>  
  
- <span data-ttu-id="08508-115">Varsayılan yükleme bağlamı, genel derleme önbelleğini yoklaerek bulunan derlemeleri, çalışma zamanının barındırıldığı ana bilgisayar derleme deposunu (örneğin, SQL Server) ve <xref:System.AppDomainSetup.ApplicationBase%2A> <xref:System.AppDomainSetup.PrivateBinPath%2A> uygulama etki alanını içerir.</span><span class="sxs-lookup"><span data-stu-id="08508-115">The default load context contains assemblies found by probing the global assembly cache, the host assembly store if the runtime is hosted (for example, in SQL Server), and the <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A> of the application domain.</span></span> <span data-ttu-id="08508-116">Metodun çoğu aşırı yüklemesi <xref:System.Reflection.Assembly.Load%2A> derlemeleri bu bağlamda yükler.</span><span class="sxs-lookup"><span data-stu-id="08508-116">Most overloads of the <xref:System.Reflection.Assembly.Load%2A> method load assemblies into this context.</span></span>  
  
- <span data-ttu-id="08508-117">Yük-from bağlamı, yükleyici tarafından aranmayan konumlardan yüklenen derlemeleri içerir.</span><span class="sxs-lookup"><span data-stu-id="08508-117">The load-from context contains assemblies that are loaded from locations that are not searched by the loader.</span></span> <span data-ttu-id="08508-118">Örneğin, eklentiler uygulama yolunda olmayan bir dizine yüklenmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-118">For example, add-ins might be installed in a directory that is not under the application path.</span></span> <span data-ttu-id="08508-119"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType> ve, <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> yolu tarafından yüklenen yöntemlerin örnekleridir.</span><span class="sxs-lookup"><span data-stu-id="08508-119"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, and <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> are examples of methods that load by path.</span></span>  
  
- <span data-ttu-id="08508-120">Yalnızca yansıma bağlamı ve yöntemleriyle yüklenen derlemeleri içerir <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> .</span><span class="sxs-lookup"><span data-stu-id="08508-120">The reflection-only context contains assemblies loaded with the <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> and <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> methods.</span></span> <span data-ttu-id="08508-121">Bu bağlamdaki kod yürütülemiyor, bu nedenle burada açıklanmıyor.</span><span class="sxs-lookup"><span data-stu-id="08508-121">Code in this context cannot be executed, so it is not discussed here.</span></span> <span data-ttu-id="08508-122">Daha fazla bilgi için bkz. [nasıl yapılır: derlemeleri yalnızca yansıma bağlamına yükleme](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span><span class="sxs-lookup"><span data-stu-id="08508-122">For more information, see [How to: Load Assemblies into the Reflection-Only Context](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span></span>  
  
- <span data-ttu-id="08508-123">Yansıma yayma kullanarak geçici bir dinamik derleme oluşturduysanız, derleme hiçbir bağlamda değildir.</span><span class="sxs-lookup"><span data-stu-id="08508-123">If you generated a transient dynamic assembly by using reflection emit, the assembly is not in any context.</span></span> <span data-ttu-id="08508-124">Ayrıca, yöntemi kullanılarak yüklenen çoğu derleme <xref:System.Reflection.Assembly.LoadFile%2A> Bağlam olmadan yüklenir ve kimlik (ilke uygulandıktan sonra), kimlikleri genel derleme önbelleğinde olduklarından, bayt dizilerden yüklenen derlemeler bağlam olmadan yüklenir.</span><span class="sxs-lookup"><span data-stu-id="08508-124">In addition, most assemblies that are loaded by using the <xref:System.Reflection.Assembly.LoadFile%2A> method are loaded without context, and assemblies that are loaded from byte arrays are loaded without context unless their identity (after policy is applied) establishes that they are in the global assembly cache.</span></span>  
  
 <span data-ttu-id="08508-125">Yürütme bağlamlarının, aşağıdaki bölümlerde ele alındığı gibi avantajları ve dezavantajları vardır.</span><span class="sxs-lookup"><span data-stu-id="08508-125">The execution contexts have advantages and disadvantages, as discussed in the following sections.</span></span>  
  
### <a name="default-load-context"></a><span data-ttu-id="08508-126">Varsayılan yükleme bağlamı</span><span class="sxs-lookup"><span data-stu-id="08508-126">Default Load Context</span></span>  
 <span data-ttu-id="08508-127">Derlemeler varsayılan yükleme bağlamına yüklendiğinde, bağımlılıkları otomatik olarak yüklenir.</span><span class="sxs-lookup"><span data-stu-id="08508-127">When assemblies are loaded into the default load context, their dependencies are loaded automatically.</span></span> <span data-ttu-id="08508-128">Varsayılan yükleme bağlamına yüklenen bağımlılıklar, varsayılan yükleme bağlamındaki derlemeler veya yük-from bağlamı için otomatik olarak bulunur.</span><span class="sxs-lookup"><span data-stu-id="08508-128">Dependencies that are loaded into the default load context are found automatically for assemblies in the default load context or the load-from context.</span></span> <span data-ttu-id="08508-129">Derleme kimliğine göre yükleme, derlemelerin bilinmeyen sürümlerinin kullanılmadığından emin olarak uygulamaların kararlılığını artırır ( [kısmi derleme adlarında bağlamaktan kaçının](#avoid_partial_names) bölümünde).</span><span class="sxs-lookup"><span data-stu-id="08508-129">Loading by assembly identity increases the stability of applications by ensuring that unknown versions of assemblies are not used (see the [Avoid Binding on Partial Assembly Names](#avoid_partial_names) section).</span></span>  
  
 <span data-ttu-id="08508-130">Varsayılan yükleme bağlamının kullanılması aşağıdaki dezavantajlara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="08508-130">Using the default load context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="08508-131">Diğer bağlamlara yüklenen bağımlılıklar kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="08508-131">Dependencies that are loaded into other contexts are not available.</span></span>  
  
- <span data-ttu-id="08508-132">Yoklama yolu dışındaki konumlardan derlemeleri varsayılan yükleme bağlamına yükleyemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-132">You cannot load assemblies from locations outside the probing path into the default load context.</span></span>  
  
### <a name="load-from-context"></a><span data-ttu-id="08508-133">Yük-bağlam</span><span class="sxs-lookup"><span data-stu-id="08508-133">Load-From Context</span></span>  
 <span data-ttu-id="08508-134">Yük-from bağlamı, uygulama yolu altında olmayan bir yoldan bir derleme yüklemenize olanak sağlar ve bu nedenle, yoklama içine dahil değildir.</span><span class="sxs-lookup"><span data-stu-id="08508-134">The load-from context lets you load an assembly from a path that is not under the application path, and therefore is not included in probing.</span></span> <span data-ttu-id="08508-135">Yol bilgileri bağlam tarafından korunduğundan, bağımlılıkların bu yoldan bulunmasını ve yüklenmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="08508-135">It enables dependencies to be located and loaded from that path, because the path information is maintained by the context.</span></span> <span data-ttu-id="08508-136">Ayrıca, bu bağlamdaki derlemeler varsayılan yükleme bağlamına yüklenen bağımlılıkları kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-136">In addition, assemblies in this context can use dependencies that are loaded into the default load context.</span></span>  
  
 <span data-ttu-id="08508-137"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>Metodu veya yol tarafından yüklenen diğer yöntemlerden birini kullanarak derlemeleri yüklemek aşağıdaki dezavantajlara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="08508-137">Loading assemblies by using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, or one of the other methods that load by path, has the following disadvantages:</span></span>  
  
- <span data-ttu-id="08508-138">Aynı kimliğe sahip bir derleme yük-from bağlamına zaten yüklenmişse, <xref:System.Reflection.Assembly.LoadFrom%2A> farklı bir yol belirtilmiş olsa bile yüklenen derlemeyi döndürür.</span><span class="sxs-lookup"><span data-stu-id="08508-138">If an assembly with the same identity is already loaded in the load-from context, <xref:System.Reflection.Assembly.LoadFrom%2A> returns the loaded assembly even if a different path was specified.</span></span>  
  
- <span data-ttu-id="08508-139">Bir derleme ile yüklenmişse <xref:System.Reflection.Assembly.LoadFrom%2A> ve daha sonra varsayılan yükleme bağlamındaki bir derleme, görünen ad ile aynı derlemeyi yüklemeye çalışırsa, yükleme girişimi başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="08508-139">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and later an assembly in the default load context tries to load the same assembly by display name, the load attempt fails.</span></span> <span data-ttu-id="08508-140">Bu, bir derlemenin serisi kaldırıldığında ortaya çıkar.</span><span class="sxs-lookup"><span data-stu-id="08508-140">This can occur when an assembly is deserialized.</span></span>  
  
- <span data-ttu-id="08508-141">Bir bütünleştirilmiş kod ile yüklenmişse <xref:System.Reflection.Assembly.LoadFrom%2A> ve yoklama yolu aynı kimliğe sahip bir derleme içeriyorsa, ancak farklı bir konumda,, <xref:System.InvalidCastException> <xref:System.MissingMethodException> , veya diğer beklenmeyen davranışlar oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-141">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and the probing path includes an assembly with the same identity but in a different location, an <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, or other unexpected behavior can occur.</span></span>  
  
- <span data-ttu-id="08508-142"><xref:System.Reflection.Assembly.LoadFrom%2A><xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType> belirtilen yolda talepler ve, veya <xref:System.Net.WebPermission> .</span><span class="sxs-lookup"><span data-stu-id="08508-142"><xref:System.Reflection.Assembly.LoadFrom%2A> demands <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> and <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, or <xref:System.Net.WebPermission>, on the specified path.</span></span>  
  
- <span data-ttu-id="08508-143">Derleme için yerel bir görüntü varsa, bu kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="08508-143">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="08508-144">Derleme, etki alanı nötr olarak yüklenemez.</span><span class="sxs-lookup"><span data-stu-id="08508-144">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="08508-145">1,0 ve 1,1 .NET Framework sürümlerinde, ilke uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="08508-145">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
### <a name="no-context"></a><span data-ttu-id="08508-146">Bağlam yok</span><span class="sxs-lookup"><span data-stu-id="08508-146">No Context</span></span>  
 <span data-ttu-id="08508-147">Bağlam olmadan yükleme, yansıma yayma ile oluşturulan geçici derlemeler için tek seçenektir.</span><span class="sxs-lookup"><span data-stu-id="08508-147">Loading without context is the only option for transient assemblies that are generated with reflection emit.</span></span> <span data-ttu-id="08508-148">Bağlam olmadan yükleme, aynı kimliğe sahip birden fazla derlemeyi tek bir uygulama etki alanında yüklemenin tek yoludur.</span><span class="sxs-lookup"><span data-stu-id="08508-148">Loading without context is the only way to load multiple assemblies that have the same identity into one application domain.</span></span> <span data-ttu-id="08508-149">Yoklama maliyeti kaçınılıyor.</span><span class="sxs-lookup"><span data-stu-id="08508-149">The cost of probing is avoided.</span></span>  
  
 <span data-ttu-id="08508-150">Bayt dizilerinizden yüklenen derlemeler, ilke uygulandığında oluşturulan derleme kimliği, genel derleme önbelleğindeki bir derlemenin kimliğiyle eşleştiğinde bağlam olmadan yüklenir; Bu durumda, derleme genel derleme önbelleğinden yüklenir.</span><span class="sxs-lookup"><span data-stu-id="08508-150">Assemblies that are loaded from byte arrays are loaded without context unless the identity of the assembly, which is established when policy is applied, matches the identity of an assembly in the global assembly cache; in that case, the assembly is loaded from the global assembly cache.</span></span>  
  
 <span data-ttu-id="08508-151">Derlemeleri bağlam olmadan yüklemek aşağıdaki dezavantajlara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="08508-151">Loading assemblies without context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="08508-152">Diğer derlemeler, olayı işlemediğiniz müddetçe bağlam olmadan yüklenen derlemelere bağlanamaz <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="08508-152">Other assemblies cannot bind to assemblies that are loaded without context, unless you handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="08508-153">Bağımlılıklar otomatik olarak yüklenmez.</span><span class="sxs-lookup"><span data-stu-id="08508-153">Dependencies are not loaded automatically.</span></span> <span data-ttu-id="08508-154">Bunları bağlam olmadan önyükleyebilir, varsayılan yükleme bağlamına önceden yükleyebilir veya olayı işleyerek yükleyebilirsiniz <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="08508-154">You can preload them without context, preload them into the default load context, or load them by handling the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="08508-155">Aynı kimliğe sahip birden fazla derlemeyi bağlama olmadan yüklemek, aynı kimliğe sahip derlemeleri birden çok bağlamla yüklerken oluşan tür kimlik sorunlarına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-155">Loading multiple assemblies with the same identity without context can cause type identity problems similar to those caused by loading assemblies with the same identity into multiple contexts.</span></span> <span data-ttu-id="08508-156">Bkz. [bir derlemeyi birden çok bağlama yüklemeden kaçının](#avoid_loading_into_multiple_contexts).</span><span class="sxs-lookup"><span data-stu-id="08508-156">See [Avoid Loading an Assembly into Multiple Contexts](#avoid_loading_into_multiple_contexts).</span></span>  
  
- <span data-ttu-id="08508-157">Derleme için yerel bir görüntü varsa, bu kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="08508-157">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="08508-158">Derleme, etki alanı nötr olarak yüklenemez.</span><span class="sxs-lookup"><span data-stu-id="08508-158">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="08508-159">1,0 ve 1,1 .NET Framework sürümlerinde, ilke uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="08508-159">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
<a name="avoid_partial_names"></a>
## <a name="avoid-binding-on-partial-assembly-names"></a><span data-ttu-id="08508-160">Kısmi derleme adlarında bağlamayı önleyin</span><span class="sxs-lookup"><span data-stu-id="08508-160">Avoid Binding on Partial Assembly Names</span></span>  
 <span data-ttu-id="08508-161">Bir derlemeyi yüklerken derleme görünen adının () yalnızca bir kısmını belirttiğinizde Kısmi ad bağlama gerçekleşir <xref:System.Reflection.Assembly.FullName%2A> .</span><span class="sxs-lookup"><span data-stu-id="08508-161">Partial name binding occurs when you specify only part of the assembly display name (<xref:System.Reflection.Assembly.FullName%2A>) when you load an assembly.</span></span> <span data-ttu-id="08508-162">Örneğin, <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemi yalnızca derlemenin basit adıyla, sürüm, kültür ve ortak anahtar belirtecini atlayarak çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-162">For example, you might call the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method with only the simple name of the assembly, omitting the version, culture, and public key token.</span></span> <span data-ttu-id="08508-163">Ya da <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> ilk olarak <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemini çağıran ve derlemeyi bulamazsa, genel derleme önbelleğinde arama yapan ve derlemenin en son kullanılabilir sürümünü yükleyen yöntemini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-163">Or you might call the <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method, which first calls the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and, if that fails to locate the assembly, searches the global assembly cache and loads the latest available version of the assembly.</span></span>  
  
 <span data-ttu-id="08508-164">Kısmi ad bağlama, aşağıdakiler de dahil olmak üzere birçok soruna neden olabilir:</span><span class="sxs-lookup"><span data-stu-id="08508-164">Partial name binding can cause many problems, including the following:</span></span>  
  
- <span data-ttu-id="08508-165"><xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType>Yöntemi, aynı basit ada sahip farklı bir derleme yükleyebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-165">The <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method might load a different assembly with the same simple name.</span></span> <span data-ttu-id="08508-166">Örneğin, iki uygulama, genel derleme önbelleğinde basit ada sahip iki tamamen farklı derleme kurabilir `GraphicsLibrary` .</span><span class="sxs-lookup"><span data-stu-id="08508-166">For example, two applications might install two completely different assemblies that both have the simple name `GraphicsLibrary` into the global assembly cache.</span></span>  
  
- <span data-ttu-id="08508-167">Gerçekten yüklenen derleme geriye dönük olarak uyumlu olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-167">The assembly that is actually loaded might not be backward-compatible.</span></span> <span data-ttu-id="08508-168">Örneğin, sürümü belirtmediğinde programınızın ilk olarak kullanılmak üzere yazıldığı sürümden daha sonraki bir sürümün yüklenmesine yol açabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-168">For example, not specifying the version might result in the loading of a much later version than the version your program was originally written to use.</span></span> <span data-ttu-id="08508-169">Sonraki sürümdeki değişiklikler uygulamanızda hatalara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-169">Changes in the later version might cause errors in your application.</span></span>  
  
- <span data-ttu-id="08508-170">Gerçekten yüklenen derleme ileri uyumlu olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-170">The assembly that is actually loaded might not be forward-compatible.</span></span> <span data-ttu-id="08508-171">Örneğin, uygulamanızı derlemenin en son sürümüyle oluşturup test edebilir, ancak kısmi bağlama uygulamanızın kullandığı özellikleri olmayan daha önceki bir sürümü yükleyebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-171">For example, you might have built and tested your application with the latest version of an assembly, but partial binding might load a much earlier version that lacks features your application uses.</span></span>  
  
- <span data-ttu-id="08508-172">Yeni uygulamaların yüklenmesi, mevcut uygulamaları bozabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-172">Installing new applications can break existing applications.</span></span> <span data-ttu-id="08508-173">Yöntemini kullanan bir uygulama <xref:System.Reflection.Assembly.LoadWithPartialName%2A> , paylaşılan bir derlemenin daha yeni, uyumsuz bir sürümü yüklenerek bozulabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-173">An application that uses the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method can be broken by installing a newer, incompatible version of a shared assembly.</span></span>  
  
- <span data-ttu-id="08508-174">Beklenmeyen bağımlılık yüklemesi meydana gelebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-174">Unexpected dependency loading can occur.</span></span> <span data-ttu-id="08508-175">Bir bağımlılığı paylaşan iki derleme yüklersiniz, bunları kısmi bağlama ile yüklemek, derlenmiş veya test edilmemiş bir bileşeni kullanarak bir derlemenin oluşmasına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-175">It you load two assemblies that share a dependency, loading them with partial binding might result in one assembly using a component that it was not built or tested with.</span></span>  
  
 <span data-ttu-id="08508-176">Neden olabileceği sorunlar nedeniyle, <xref:System.Reflection.Assembly.LoadWithPartialName%2A> yöntemi kullanılmıyor olarak işaretlendi.</span><span class="sxs-lookup"><span data-stu-id="08508-176">Because of the problems it can cause, the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method has been marked obsolete.</span></span> <span data-ttu-id="08508-177"><xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>Bunun yerine yöntemini kullanmanızı ve tam derleme görünen adlarını belirtmenizi öneririz.</span><span class="sxs-lookup"><span data-stu-id="08508-177">We recommend that you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method instead, and specify full assembly display names.</span></span> <span data-ttu-id="08508-178">Bkz. [Yük bağlamlarının olumlu ve olumsuz yönlerini anlayın](#load_contexts) ve [varsayılan yükleme bağlamına geçmeyi düşünün](#switch_to_default).</span><span class="sxs-lookup"><span data-stu-id="08508-178">See [Understand the Advantages and Disadvantages of Load Contexts](#load_contexts) and [Consider Switching to the Default Load Context](#switch_to_default).</span></span>  
  
 <span data-ttu-id="08508-179"><xref:System.Reflection.Assembly.LoadWithPartialName%2A>Derlemeyi daha kolay hale getiren yöntemi kullanmak istiyorsanız, uygulamanızın eksik derlemeyi tanımlayan bir hata iletisiyle başarısız olduğunu, derlemenin bilinmeyen bir sürümünü kullanarak otomatik olarak daha iyi bir kullanıcı deneyimi sağlayabileceğini göz önünde bulundurun. Bu, öngörülemeyen davranış ve güvenlik delikleri oluşmasına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-179">If you want to use the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method because it makes assembly loading easy, consider that having your application fail with an error message that identifies the missing assembly is likely to provide a better user experience than automatically using an unknown version of the assembly, which might cause unpredictable behavior and security holes.</span></span>  
  
<a name="avoid_loading_into_multiple_contexts"></a>
## <a name="avoid-loading-an-assembly-into-multiple-contexts"></a><span data-ttu-id="08508-180">Bir derlemeyi birden çok bağlama yüklemeden kaçının</span><span class="sxs-lookup"><span data-stu-id="08508-180">Avoid Loading an Assembly into Multiple Contexts</span></span>  
 <span data-ttu-id="08508-181">Bir derlemeyi birden çok bağlama yüklemek tür kimlik sorunlarına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-181">Loading an assembly into multiple contexts can cause type identity problems.</span></span> <span data-ttu-id="08508-182">Aynı türden iki farklı bağlamda aynı tür yüklüyse, aynı ada sahip iki farklı tür yüklenmiş olur.</span><span class="sxs-lookup"><span data-stu-id="08508-182">If the same type is loaded from the same assembly into two different contexts, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="08508-183">Bir <xref:System.InvalidCastException> türü diğer bir türe dönüştürmeye çalışırsanız, bu tür bir tür türüne yayınlanamaz bir ileti ile oluşturulur `MyType` `MyType` .</span><span class="sxs-lookup"><span data-stu-id="08508-183">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="08508-184">Örneğin, `ICommunicate` arabirimin adlı bir derlemede bildirildiği, bu da `Utility` programınızın ve programınızın yüklediği diğer derlemelerin başvurduğu bir derlemede olduğunu varsayalım.</span><span class="sxs-lookup"><span data-stu-id="08508-184">For example, suppose that the `ICommunicate` interface is declared in an assembly named `Utility`, which is referenced by your program and also by other assemblies that your program loads.</span></span> <span data-ttu-id="08508-185">Bu diğer derlemeler `ICommunicate` , bu arabirimi uygulayan ve programınızın bunları kullanmasına izin veren türler içerir.</span><span class="sxs-lookup"><span data-stu-id="08508-185">These other assemblies contain types that implement the `ICommunicate` interface, allowing your program to use them.</span></span>  
  
 <span data-ttu-id="08508-186">Artık programınız çalıştırıldığında ne olacağını düşünün.</span><span class="sxs-lookup"><span data-stu-id="08508-186">Now consider what happens when your program is run.</span></span> <span data-ttu-id="08508-187">Programınızın başvurduğu derlemeler varsayılan yükleme bağlamına yüklenir.</span><span class="sxs-lookup"><span data-stu-id="08508-187">Assemblies that are referenced by your program are loaded into the default load context.</span></span> <span data-ttu-id="08508-188">Yöntemini kullanarak bir hedef derlemeyi kimliğine göre yüklerseniz, <xref:System.Reflection.Assembly.Load%2A> Bu, varsayılan yükleme bağlamında olur ve bu nedenle bağımlılıklarını kullanacaktır.</span><span class="sxs-lookup"><span data-stu-id="08508-188">If you load a target assembly by its identity, using the <xref:System.Reflection.Assembly.Load%2A> method, it will be in the default load context, and so will its dependencies.</span></span> <span data-ttu-id="08508-189">Hem programınızın hem de hedef derlemenin aynı derlemeyi kullanması gerekir `Utility` .</span><span class="sxs-lookup"><span data-stu-id="08508-189">Both your program and the target assembly will use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="08508-190">Ancak, yöntemini kullanarak hedef derlemeyi dosya yolu ile yüklediğinizi varsayalım <xref:System.Reflection.Assembly.LoadFile%2A> .</span><span class="sxs-lookup"><span data-stu-id="08508-190">However, suppose you load the target assembly by its file path, using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="08508-191">Derleme herhangi bir bağlam olmadan yüklenir, bu nedenle bağımlılıkları otomatik olarak yüklenmez.</span><span class="sxs-lookup"><span data-stu-id="08508-191">The assembly is loaded without any context, so its dependencies are not automatically loaded.</span></span> <span data-ttu-id="08508-192">Bağımlılığı sağlamak için olaya yönelik bir işleyiciniz olabilir <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> ve `Utility` yöntemi kullanılarak bir bağlam olmadan derlemeyi yükleyebilir <xref:System.Reflection.Assembly.LoadFile%2A> .</span><span class="sxs-lookup"><span data-stu-id="08508-192">You might have a handler for the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to supply the dependency, and it might load the `Utility` assembly with no context by using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="08508-193">Artık hedef derlemede yer alan bir türün bir örneğini oluşturduğunuzda ve onu türünde bir değişkene atamayı denerseniz `ICommunicate` , <xref:System.InvalidCastException> çalışma zamanı `ICommunicate` derlemenin iki kopyasında bulunan arabirimleri farklı türler olarak düşündüğü için bir oluşturulur `Utility` .</span><span class="sxs-lookup"><span data-stu-id="08508-193">Now when you create an instance of a type that is contained in the target assembly and try to assign it to a variable of type `ICommunicate`, an <xref:System.InvalidCastException> is thrown because the runtime considers the `ICommunicate` interfaces in the two copies of the `Utility` assembly to be different types.</span></span>  
  
 <span data-ttu-id="08508-194">Bir derlemenin birden çok bağlamına yüklenebileceği birçok farklı senaryo vardır.</span><span class="sxs-lookup"><span data-stu-id="08508-194">There are many other scenarios in which an assembly can be loaded into multiple contexts.</span></span> <span data-ttu-id="08508-195">En iyi yaklaşım, hedef derlemenin uygulama yolunuzda yerini alarak ve <xref:System.Reflection.Assembly.Load%2A> yöntemi tam ekran adıyla kullanarak çakışmaları önlemelidir.</span><span class="sxs-lookup"><span data-stu-id="08508-195">The best approach is to avoid conflicts by relocating the target assembly in your application path and using the <xref:System.Reflection.Assembly.Load%2A> method with the full display name.</span></span> <span data-ttu-id="08508-196">Derleme daha sonra varsayılan yükleme bağlamına yüklenir ve her iki derleme de aynı `Utility` derlemeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="08508-196">The assembly is then loaded into the default load context, and both assemblies use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="08508-197">Hedef derlemenin uygulama yolunun dışında kalması gerekiyorsa, <xref:System.Reflection.Assembly.LoadFrom%2A> bunu yük-from bağlamına yüklemek için yöntemini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-197">If the target assembly must remain outside your application path, you can use the <xref:System.Reflection.Assembly.LoadFrom%2A> method to load it into the load-from context.</span></span> <span data-ttu-id="08508-198">Hedef derleme, uygulamanızın derlemesine bir başvuru ile derlenmişse `Utility` , `Utility` Uygulamanızın varsayılan yükleme bağlamına yüklediği derlemeyi kullanacaktır.</span><span class="sxs-lookup"><span data-stu-id="08508-198">If the target assembly was compiled with a reference to your application's `Utility` assembly, it will use the `Utility` assembly that your application has loaded into the default load context.</span></span> <span data-ttu-id="08508-199">Hedef derlemenin, `Utility` uygulama yolunuz dışında bulunan derlemenin bir kopyasına bağımlılığı varsa, sorunların gerçekleşebileceğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="08508-199">Note that problems can occur if the target assembly has a dependency on a copy of the `Utility` assembly located outside your application path.</span></span> <span data-ttu-id="08508-200">Uygulamanız derlemeyi yüklemeden önce bu derleme yük devretme bağlamına yüklenirse `Utility` , uygulamanızın yükü başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="08508-200">If that assembly is loaded into the load-from context before your application loads the `Utility` assembly, your application's load will fail.</span></span>  
  
 <span data-ttu-id="08508-201">[Varsayılan yükleme bağlamı bölümüne geçmeyi düşünün](#switch_to_default) , ve gibi dosya yolu yüklemelerinin kullanımına ilişkin alternatifleri açıklar <xref:System.Reflection.Assembly.LoadFile%2A> <xref:System.Reflection.Assembly.LoadFrom%2A> .</span><span class="sxs-lookup"><span data-stu-id="08508-201">The [Consider Switching to the Default Load Context](#switch_to_default) section discusses alternatives to using file path loads such as <xref:System.Reflection.Assembly.LoadFile%2A> and <xref:System.Reflection.Assembly.LoadFrom%2A>.</span></span>  
  
<a name="avoid_loading_multiple_versions"></a>
## <a name="avoid-loading-multiple-versions-of-an-assembly-into-the-same-context"></a><span data-ttu-id="08508-202">Bir derlemenin birden çok sürümünü aynı Içeriğe yüklemeden kaçının</span><span class="sxs-lookup"><span data-stu-id="08508-202">Avoid Loading Multiple Versions of an Assembly into the Same Context</span></span>  
 <span data-ttu-id="08508-203">Bir derlemenin birden çok sürümünü tek bir yük bağlamına yüklemek tür kimlik sorunlarına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-203">Loading multiple versions of an assembly into one load context can cause type identity problems.</span></span> <span data-ttu-id="08508-204">Aynı tür aynı derlemenin iki sürümünden yüklüyse, aynı ada sahip iki farklı tür yüklenmiş olur.</span><span class="sxs-lookup"><span data-stu-id="08508-204">If the same type is loaded from two versions of the same assembly, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="08508-205">Bir <xref:System.InvalidCastException> türü diğer bir türe dönüştürmeye çalışırsanız, bu tür bir tür türüne yayınlanamaz bir ileti ile oluşturulur `MyType` `MyType` .</span><span class="sxs-lookup"><span data-stu-id="08508-205">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="08508-206">Örneğin, programınız derlemenin bir sürümünü `Utility` doğrudan yükleyebilir ve daha sonra derlemenin farklı bir sürümünü yükleyen başka bir derlemeyi yükleyebilir `Utility` .</span><span class="sxs-lookup"><span data-stu-id="08508-206">For example, your program might load one version of the `Utility` assembly directly, and later it might load another assembly that loads a different version of the `Utility` assembly.</span></span> <span data-ttu-id="08508-207">Ya da bir kodlama hatası, uygulamanızdaki iki farklı kod yolunun bir derlemenin farklı sürümlerini yüklemesine neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-207">Or a coding error might cause two different code paths in your application to load different versions of an assembly.</span></span>  
  
 <span data-ttu-id="08508-208">Varsayılan yükleme bağlamında, <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemini kullandığınızda ve farklı sürüm numaraları içeren tüm derleme görünen adlarını belirttiğinizde bu sorun oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-208">In the default load context, this problem can occur when you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and specify complete assembly display names that include different version numbers.</span></span> <span data-ttu-id="08508-209">Bağlamı olmadan yüklenen derlemeler için, bu soruna <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> farklı yollardan aynı derlemeyi yüklemek için yöntemi kullanılarak neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-209">For assemblies that are loaded without context, the problem can be caused by using the <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> method to load the same assembly from different paths.</span></span> <span data-ttu-id="08508-210">Çalışma zamanı, kimlikleri aynı olsa bile farklı yollardan yüklenen iki derlemeyi farklı derlemeler olacak şekilde değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="08508-210">The runtime considers two assemblies that are loaded from different paths to be different assemblies, even if their identities are the same.</span></span>  
  
 <span data-ttu-id="08508-211">Tür kimlik sorunlarına ek olarak, bir derlemenin birden çok sürümü, bir <xref:System.MissingMethodException> derlemenin bir sürümünden yüklenen bir tür, bu türü farklı bir sürümden bekleyen koda geçirilmezse bir derlemeye neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="08508-211">In addition to type identity problems, multiple versions of an assembly can cause a <xref:System.MissingMethodException> if a type that is loaded from one version of the assembly is passed to code that expects that type from a different version.</span></span> <span data-ttu-id="08508-212">Örneğin, kod sonraki sürüme eklenmiş bir yöntemi bekleyebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-212">For example, the code might expect a method that was added to the later version.</span></span>  
  
 <span data-ttu-id="08508-213">Sürümler arasında tür davranışı değiştiyse daha hafif hatalar meydana gelebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-213">More subtle errors can occur if the behavior of the type changed between versions.</span></span> <span data-ttu-id="08508-214">Örneğin, bir yöntem beklenmeyen bir özel durum oluşturabilir veya beklenmeyen bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="08508-214">For example, a method might throw an unexpected exception or return an unexpected value.</span></span>  
  
 <span data-ttu-id="08508-215">Yalnızca bir derlemenin yalnızca bir sürümünün yüklendiğinden emin olmak için kodunuzu dikkatle gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="08508-215">Carefully review your code to ensure that only one version of an assembly is loaded.</span></span> <span data-ttu-id="08508-216"><xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType>Belirli bir zamanda hangi derlemelerin yüklendiğini öğrenmek için yöntemini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-216">You can use the <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> method to determine which assemblies are loaded at any given time.</span></span>  
  
<a name="switch_to_default"></a>
## <a name="consider-switching-to-the-default-load-context"></a><span data-ttu-id="08508-217">Varsayılan yükleme bağlamına geçmeyi düşünün</span><span class="sxs-lookup"><span data-stu-id="08508-217">Consider Switching to the Default Load Context</span></span>  
 <span data-ttu-id="08508-218">Uygulamanızın derleme yükleme ve dağıtım düzenlerini inceleyin.</span><span class="sxs-lookup"><span data-stu-id="08508-218">Examine your application's assembly loading and deployment patterns.</span></span> <span data-ttu-id="08508-219">Bayt dizilerden yüklenen derlemeleri ortadan kaldırabilir misiniz?</span><span class="sxs-lookup"><span data-stu-id="08508-219">Can you eliminate assemblies that are loaded from byte arrays?</span></span> <span data-ttu-id="08508-220">Derlemeleri yoklama yoluna taşıyabilir miyim?</span><span class="sxs-lookup"><span data-stu-id="08508-220">Can you move assemblies into the probing path?</span></span> <span data-ttu-id="08508-221">Derlemeler genel derleme önbelleğinde veya uygulama etki alanının yoklama yolunda bulunuyorsa (yani, <xref:System.AppDomainSetup.ApplicationBase%2A> ve <xref:System.AppDomainSetup.PrivateBinPath%2A> ), derlemeyi kimliğine göre yükleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-221">If assemblies are located in the global assembly cache or in the application domain's probing path (that is, its <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A>), you can load the assembly by its identity.</span></span>  
  
 <span data-ttu-id="08508-222">Tüm derlemelerinizi yoklama yoluna koymak mümkün değilse, .NET Framework eklenti modelini kullanma, derlemeleri genel derleme önbelleğine yerleştirme veya uygulama etki alanları oluşturma gibi alternatifleri göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="08508-222">If it is not possible to put all your assemblies in the probing path, consider alternatives such as using the .NET Framework add-in model, placing assemblies into the global assembly cache, or creating application domains.</span></span>  
  
### <a name="consider-using-the-net-framework-add-in-model"></a><span data-ttu-id="08508-223">.NET Framework eklentisi modelini kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="08508-223">Consider Using the .NET Framework Add-In Model</span></span>  
 <span data-ttu-id="08508-224">Genellikle uygulama tabanında yüklenmeyen eklentileri uygulamak için Load-from bağlamını kullanıyorsanız, .NET Framework eklenti modelini kullanın.</span><span class="sxs-lookup"><span data-stu-id="08508-224">If you are using the load-from context to implement add-ins, which typically are not installed in the application base, use the .NET Framework add-in model.</span></span> <span data-ttu-id="08508-225">Bu model uygulama etki alanı veya işlem düzeyinde yalıtım sağlar, böylece uygulama etki alanlarını kendiniz yönetmenizi zorunlu değildir.</span><span class="sxs-lookup"><span data-stu-id="08508-225">This model provides isolation at the application domain or process level, without requiring you to manage application domains yourself.</span></span> <span data-ttu-id="08508-226">Eklenti modeli hakkında daha fazla bilgi için bkz. eklentiler [ve genişletilebilirlik](/previous-versions/dotnet/netframework-4.0/bb384200(v=vs.100)).</span><span class="sxs-lookup"><span data-stu-id="08508-226">For information about the add-in model, see [Add-ins and Extensibility](/previous-versions/dotnet/netframework-4.0/bb384200(v=vs.100)).</span></span>  
  
### <a name="consider-using-the-global-assembly-cache"></a><span data-ttu-id="08508-227">Genel derleme önbelleğini kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="08508-227">Consider Using the Global Assembly Cache</span></span>  
 <span data-ttu-id="08508-228">Varsayılan Yük bağlamının avantajlarını kaybetmeden veya diğer bağlamların dezavantajlarını kaybetmeksizin, uygulama tabanının dışında paylaşılan bir derleme yolunun avantajlarından yararlanmak için bütünleştirilmiş kodları genel derleme önbelleğine yerleştirin.</span><span class="sxs-lookup"><span data-stu-id="08508-228">Place assemblies in the global assembly cache to get the benefit of a shared assembly path that is outside the application base, without losing the advantages of the default load context or taking on the disadvantages of the other contexts.</span></span>  
  
### <a name="consider-using-application-domains"></a><span data-ttu-id="08508-229">Uygulama etki alanlarını kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="08508-229">Consider Using Application Domains</span></span>  
 <span data-ttu-id="08508-230">Derlemelerin bazılarının uygulamanın yoklama yolunda dağıtılamadığını belirlerseniz, bu derlemeler için yeni bir uygulama etki alanı oluşturmayı düşünün.</span><span class="sxs-lookup"><span data-stu-id="08508-230">If you determine that some of your assemblies cannot be deployed in the application's probing path, consider creating a new application domain for those assemblies.</span></span> <span data-ttu-id="08508-231"><xref:System.AppDomainSetup>Yeni uygulama etki alanını oluşturmak için bir kullanın ve <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> yüklemek istediğiniz derlemeleri içeren yolu belirtmek için özelliğini kullanın.</span><span class="sxs-lookup"><span data-stu-id="08508-231">Use an <xref:System.AppDomainSetup> to create the new application domain, and use the <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> property to specify the path that contains the assemblies you want to load.</span></span> <span data-ttu-id="08508-232">Araştırmanız için birden çok dizininiz varsa, öğesini <xref:System.AppDomainSetup.ApplicationBase%2A> bir kök dizine ayarlayabilir ve <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> araştırmanın alt dizinlerini tanımlamak için özelliğini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-232">If you have multiple directories to probe, you can set the <xref:System.AppDomainSetup.ApplicationBase%2A> to a root directory and use the <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> property to identify the subdirectories to probe.</span></span> <span data-ttu-id="08508-233">Alternatif olarak, birden çok uygulama etki alanı oluşturabilir ve <xref:System.AppDomainSetup.ApplicationBase%2A> her bir uygulama etki alanını derlemeleri için uygun yola ayarlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="08508-233">Alternatively, you can create multiple application domains and set the <xref:System.AppDomainSetup.ApplicationBase%2A> of each application domain to the appropriate path for its assemblies.</span></span>  
  
 <span data-ttu-id="08508-234"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>Bu derlemeleri yüklemek için yöntemini kullanabileceğinizi unutmayın.</span><span class="sxs-lookup"><span data-stu-id="08508-234">Note that you can use the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method to load these assemblies.</span></span> <span data-ttu-id="08508-235">Bunlar artık yoklama yolunda olduklarından, yük-from bağlamı yerine varsayılan yükleme bağlamına yüklenecektir.</span><span class="sxs-lookup"><span data-stu-id="08508-235">Because they are now in the probing path, they will be loaded into the default load context instead of the load-from context.</span></span> <span data-ttu-id="08508-236">Ancak, yönteme geçiş yapmanızı <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> ve doğru sürümlerin her zaman kullanıldığından emin olmak için tam derleme görünen adları vermenizi öneririz.</span><span class="sxs-lookup"><span data-stu-id="08508-236">However, we recommend that you switch to the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and supply full assembly display names to ensure that correct versions are always used.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="08508-237">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="08508-237">See also</span></span>

- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType>
- <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>
