---
title: "Derleme Yükleme için En İyi Yöntemler"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- assemblies,binding
- LoadFrom method
- default load context
- TypeLoadException class,causes
- assembly loading errors
- load contexts
- assemblies,loading
- LoadWithPartialName method
- load-from context
ms.assetid: 68d1c539-6a47-4614-ab59-4b071c9d4b4c
caps.latest.revision: "10"
author: mairaw
ms.author: mairaw
manager: wpickett
ms.openlocfilehash: 302be9cafc0fd2ef327767cbf1178d4927757d2d
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/21/2017
---
# <a name="best-practices-for-assembly-loading"></a><span data-ttu-id="d6bfd-102">Derleme Yükleme için En İyi Yöntemler</span><span class="sxs-lookup"><span data-stu-id="d6bfd-102">Best Practices for Assembly Loading</span></span>
<span data-ttu-id="d6bfd-103">Yol açabilir türü kimliğinin sorunları önlemek için bu makalede ele <xref:System.InvalidCastException>, <xref:System.MissingMethodException>ve diğer hataları.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-103">This article discusses ways to avoid problems of type identity that can lead to <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, and other errors.</span></span> <span data-ttu-id="d6bfd-104">Makaleyi aşağıdaki öneriler ele alınmıştır:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-104">The article discusses the following recommendations:</span></span>  
  
-   [<span data-ttu-id="d6bfd-105">Olumlu ve olumsuz yönlerini yükleme bağlamları anlama</span><span class="sxs-lookup"><span data-stu-id="d6bfd-105">Understand the advantages and disadvantages of load contexts</span></span>](#load_contexts)  
  
-   [<span data-ttu-id="d6bfd-106">Kısmi derleme adları bağlama kaçının</span><span class="sxs-lookup"><span data-stu-id="d6bfd-106">Avoid binding on partial assembly names</span></span>](#avoid_partial_names)  
  
-   [<span data-ttu-id="d6bfd-107">Birden çok bağlamları bir derlemesi yüklenirken kaçının</span><span class="sxs-lookup"><span data-stu-id="d6bfd-107">Avoid loading an assembly into multiple contexts</span></span>](#avoid_loading_into_multiple_contexts)  
  
-   [<span data-ttu-id="d6bfd-108">Derleme birden fazla sürümünü aynı bağlamına yüklenmesini önlemek</span><span class="sxs-lookup"><span data-stu-id="d6bfd-108">Avoid loading multiple versions of an assembly into the same context</span></span>](#avoid_loading_multiple_versions)  
  
-   [<span data-ttu-id="d6bfd-109">Varsayılan yükleme bağlamı geçmeniz önerilir</span><span class="sxs-lookup"><span data-stu-id="d6bfd-109">Consider switching to the default load context</span></span>](#switch_to_default)  
  
 <span data-ttu-id="d6bfd-110">İlk öneri [olumlu ve olumsuz yönlerini yükleme bağlamları anlamak](#load_contexts), hepsi bir yükleme bağlamları bilgi üzerinde bağımlı olduğu için diğer öneriler için arka plan bilgileri sağlar.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-110">The first recommendation, [understand the advantages and disadvantages of load contexts](#load_contexts), provides background information for the other recommendations, because they all depend on a knowledge of load contexts.</span></span>  
  
<a name="load_contexts"></a>   
## <a name="understand-the-advantages-and-disadvantages-of-load-contexts"></a><span data-ttu-id="d6bfd-111">Olumlu ve olumsuz yönlerini yükleme bağlamları anlama</span><span class="sxs-lookup"><span data-stu-id="d6bfd-111">Understand the Advantages and Disadvantages of Load Contexts</span></span>  
 <span data-ttu-id="d6bfd-112">Bir uygulama etki alanı içinde derlemeleri üç bağlamları birine yüklenebilir veya bir bağlam olmadan yüklenmiş olabilir:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-112">Within an application domain, assemblies can be loaded into one of three contexts, or they can be loaded without context:</span></span>  
  
-   <span data-ttu-id="d6bfd-113">Derlemeleri genel derleme önbelleği, çalışma zamanı (örneğin, SQL Server), barındırılıyorsa, ana bilgisayar derleme deposu yoklama tarafından bulunan varsayılan yükleme bağlamı içerir ve <xref:System.AppDomainSetup.ApplicationBase%2A> ve <xref:System.AppDomainSetup.PrivateBinPath%2A> uygulama etki alanı.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-113">The default load context contains assemblies found by probing the global assembly cache, the host assembly store if the runtime is hosted (for example, in SQL Server), and the <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A> of the application domain.</span></span> <span data-ttu-id="d6bfd-114">Çoğu aşırı <xref:System.Reflection.Assembly.Load%2A> yöntemi yük derlemelerine bu bağlamı.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-114">Most overloads of the <xref:System.Reflection.Assembly.Load%2A> method load assemblies into this context.</span></span>  
  
-   <span data-ttu-id="d6bfd-115">Bağlamdan yükleme yükleyicisi tarafından aranmaz konumlardan yüklenmiş derlemeleri içerir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-115">The load-from context contains assemblies that are loaded from locations that are not searched by the loader.</span></span> <span data-ttu-id="d6bfd-116">Örneğin, eklentiler uygulama yolu altında olmayan bir dizin yüklenmemiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-116">For example, add-ins might be installed in a directory that is not under the application path.</span></span> <span data-ttu-id="d6bfd-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, ve <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> yoluyla yük yöntemler örnekleridir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, and <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> are examples of methods that load by path.</span></span>  
  
-   <span data-ttu-id="d6bfd-118">Yalnızca yansıma bağlamına derlemeleri ile yüklenen içerir <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> ve <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-118">The reflection-only context contains assemblies loaded with the <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> and <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> methods.</span></span> <span data-ttu-id="d6bfd-119">Bunu burada ele alınmamıştır şekilde kodu bu bağlamda yürütülemiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-119">Code in this context cannot be executed, so it is not discussed here.</span></span> <span data-ttu-id="d6bfd-120">Daha fazla bilgi için bkz: [nasıl yapılır: yük derlemelerine Reflection-Only bağlamı](../../../docs/framework/reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span><span class="sxs-lookup"><span data-stu-id="d6bfd-120">For more information, see [How to: Load Assemblies into the Reflection-Only Context](../../../docs/framework/reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span></span>  
  
-   <span data-ttu-id="d6bfd-121">Yansıma kullanarak geçici bir dinamik derleme oluşturulan yayma, derleme her bağlamda değildir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-121">If you generated a transient dynamic assembly by using reflection emit, the assembly is not in any context.</span></span> <span data-ttu-id="d6bfd-122">Ayrıca, kullanılarak yüklenen çoğu derlemeleri <xref:System.Reflection.Assembly.LoadFile%2A> yöntemi bağlamı olmadan yüklenir ve içinde bulundukları (ilke uygulandıktan sonra) kimliğini kurar sürece bayt dizileri yüklenen derlemeler bağlamı olmadan yüklü Genel Derleme Önbelleği.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-122">In addition, most assemblies that are loaded by using the <xref:System.Reflection.Assembly.LoadFile%2A> method are loaded without context, and assemblies that are loaded from byte arrays are loaded without context unless their identity (after policy is applied) establishes that they are in the global assembly cache.</span></span>  
  
 <span data-ttu-id="d6bfd-123">Yürütme bağlamı olumlu ve olumsuz yönleri, aşağıdaki bölümlerde açıklandığı gibi sahip.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-123">The execution contexts have advantages and disadvantages, as discussed in the following sections.</span></span>  
  
### <a name="default-load-context"></a><span data-ttu-id="d6bfd-124">Varsayılan yükleme bağlamı</span><span class="sxs-lookup"><span data-stu-id="d6bfd-124">Default Load Context</span></span>  
 <span data-ttu-id="d6bfd-125">Varsayılan yükleme bağlamına derlemeleri yüklenir, bağımlılıklarını otomatik olarak yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-125">When assemblies are loaded into the default load context, their dependencies are loaded automatically.</span></span> <span data-ttu-id="d6bfd-126">Varsayılan yükleme bağlamına yüklenen bağımlılıkları varsayılan yükleme bağlamı veya bağlamdan yükleme derlemeler için otomatik olarak bulunamadı.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-126">Dependencies that are loaded into the default load context are found automatically for assemblies in the default load context or the load-from context.</span></span> <span data-ttu-id="d6bfd-127">Derleme kimliğine göre yükleniyor derlemeleri bilinmeyen sürümleri kullanılmayan sağlayarak uygulamaları kararlılığını artırır (bkz [kaçının bağlama kısmi derleme adları](#avoid_partial_names) bölümü).</span><span class="sxs-lookup"><span data-stu-id="d6bfd-127">Loading by assembly identity increases the stability of applications by ensuring that unknown versions of assemblies are not used (see the [Avoid Binding on Partial Assembly Names](#avoid_partial_names) section).</span></span>  
  
 <span data-ttu-id="d6bfd-128">Varsayılan yükleme bağlamı kullanarak aşağıdaki dezavantajları bulunur:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-128">Using the default load context has the following disadvantages:</span></span>  
  
-   <span data-ttu-id="d6bfd-129">Diğer bağlamlarda yüklenen bağımlılıkları kullanılabilir değil.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-129">Dependencies that are loaded into other contexts are not available.</span></span>  
  
-   <span data-ttu-id="d6bfd-130">Varsayılan yükleme bağlamı içine algılama yolu dışında konumlardan derlemeler yüklenemiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-130">You cannot load assemblies from locations outside the probing path into the default load context.</span></span>  
  
### <a name="load-from-context"></a><span data-ttu-id="d6bfd-131">Bağlamdan yükleme</span><span class="sxs-lookup"><span data-stu-id="d6bfd-131">Load-From Context</span></span>  
 <span data-ttu-id="d6bfd-132">Bağlamdan yükleme uygulama yolu altında değil ve bu nedenle yoklama içinde bulunmayan bir yoldan derleme yük olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-132">The load-from context lets you load an assembly from a path that is not under the application path, and therefore is not included in probing.</span></span> <span data-ttu-id="d6bfd-133">Yol bilgileri bağlam tarafından korunduğundan bulunan ve bu yolundan yüklenen bağımlılıkları sağlar.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-133">It enables dependencies to be located and loaded from that path, because the path information is maintained by the context.</span></span> <span data-ttu-id="d6bfd-134">Ayrıca, bu bağlamda derlemeleri varsayılan yük bağlamına yüklenen bağımlılıkları kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-134">In addition, assemblies in this context can use dependencies that are loaded into the default load context.</span></span>  
  
 <span data-ttu-id="d6bfd-135">Kullanarak derlemeleri yükleme <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> yöntemi veya yoluyla yük diğer yöntemlerden birini aşağıdaki dezavantajları vardır:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-135">Loading assemblies by using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, or one of the other methods that load by path, has the following disadvantages:</span></span>  
  
-   <span data-ttu-id="d6bfd-136">Aynı kimliğe sahip bir derleme zaten yüklü, ise <xref:System.Reflection.Assembly.LoadFrom%2A> farklı bir yol belirtilmiş olsa bile yüklenen derleme döndürür.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-136">If an assembly with the same identity is already loaded, <xref:System.Reflection.Assembly.LoadFrom%2A> returns the loaded assembly even if a different path was specified.</span></span>  
  
-   <span data-ttu-id="d6bfd-137">Bir derlemeyi ile yüklenen ise <xref:System.Reflection.Assembly.LoadFrom%2A>, daha sonra aynı bütünleştirilmiş kodda görünen ada göre yüklemek bir varsayılan yükleme bağlamı derlemede çalışır, yükleme denemesi başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-137">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and later an assembly in the default load context tries to load the same assembly by display name, the load attempt fails.</span></span> <span data-ttu-id="d6bfd-138">Bir derlemeyi seri olduğunda bu durum oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-138">This can occur when an assembly is deserialized.</span></span>  
  
-   <span data-ttu-id="d6bfd-139">Bütünleştirilmiş ile yüklenmiş ise <xref:System.Reflection.Assembly.LoadFrom%2A>, ve aynı kimliğe sahip ancak farklı bir konumda bir derleme yoklama yolunu içeren bir <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, veya diğer beklenmeyen davranışları ortaya çıkabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-139">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and the probing path includes an assembly with the same identity but in a different location, an <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, or other unexpected behavior can occur.</span></span>  
  
-   <span data-ttu-id="d6bfd-140"><xref:System.Reflection.Assembly.LoadFrom%2A>taleplerin <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> ve <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, veya <xref:System.Net.WebPermission>, belirtilen yol üzerinde.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-140"><xref:System.Reflection.Assembly.LoadFrom%2A> demands <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> and <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, or <xref:System.Net.WebPermission>, on the specified path.</span></span>  
  
-   <span data-ttu-id="d6bfd-141">Yerel görüntü derleme için zaten varsa, kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-141">If a native image exists for the assembly, it is not used.</span></span>  
  
-   <span data-ttu-id="d6bfd-142">Etki alanı bağımsız derlemesi yüklenemiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-142">The assembly cannot be loaded as domain-neutral.</span></span>  
  
-   <span data-ttu-id="d6bfd-143">.NET Framework sürüm 1.0 ve 1.1, ilke uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-143">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
### <a name="no-context"></a><span data-ttu-id="d6bfd-144">Bağlam yok</span><span class="sxs-lookup"><span data-stu-id="d6bfd-144">No Context</span></span>  
 <span data-ttu-id="d6bfd-145">Bağlam yükleme olduğu yansıma ile oluşturulan geçici derlemeler için tek seçenek yayma.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-145">Loading without context is the only option for transient assemblies that are generated with reflection emit.</span></span> <span data-ttu-id="d6bfd-146">Yükleme bağlamı olmadan, bir uygulama etki alanına aynı kimliğe sahip birden çok derleme yüklemek için tek yoludur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-146">Loading without context is the only way to load multiple assemblies that have the same identity into one application domain.</span></span> <span data-ttu-id="d6bfd-147">Yoklama maliyetini önlenmiş olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-147">The cost of probing is avoided.</span></span>  
  
 <span data-ttu-id="d6bfd-148">Bir derlemeyi genel derleme önbelleğinde kimliğini ilke uygulandığında kurulduğunda, derleme kimliğini eşleşen sürece bayt dizileri yüklenen derlemeler bağlamı olmadan yüklenir; Bu durumda genel derleme önbelleğinde derleme yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-148">Assemblies that are loaded from byte arrays are loaded without context unless the identity of the assembly, which is established when policy is applied, matches the identity of an assembly in the global assembly cache; in that case, the assembly is loaded from the global assembly cache.</span></span>  
  
 <span data-ttu-id="d6bfd-149">Bağlam olmadan derlemeleri yükleme aşağıdaki dezavantajları bulunur:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-149">Loading assemblies without context has the following disadvantages:</span></span>  
  
-   <span data-ttu-id="d6bfd-150">Tanıtıcı sürece diğer derlemelerden bağlamı olmadan yüklenen derlemeler bağlanılamıyor <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> olay.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-150">Other assemblies cannot bind to assemblies that are loaded without context, unless you handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
-   <span data-ttu-id="d6bfd-151">Bağımlılıklar otomatik olarak yüklenmez.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-151">Dependencies are not loaded automatically.</span></span> <span data-ttu-id="d6bfd-152">Bir bağlam olmadan dağıtılacak, varsayılan yükleme bağlamına önceden veya işleyerek yük <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> olay.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-152">You can preload them without context, preload them into the default load context, or load them by handling the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
-   <span data-ttu-id="d6bfd-153">Bağlam olmadan aynı kimliğe sahip birden çok derlemeleri yükleme türü kimlik sorunları birden çok bağlamları aynı kimliğe sahip derlemeler yükleyerek neden benzer neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-153">Loading multiple assemblies with the same identity without context can cause type identity problems similar to those caused by loading assemblies with the same identity into multiple contexts.</span></span> <span data-ttu-id="d6bfd-154">Bkz: [bir derleme birden çok bağlamları yüklenmesini önlemek](#avoid_loading_into_multiple_contexts).</span><span class="sxs-lookup"><span data-stu-id="d6bfd-154">See [Avoid Loading an Assembly into Multiple Contexts](#avoid_loading_into_multiple_contexts).</span></span>  
  
-   <span data-ttu-id="d6bfd-155">Yerel görüntü derleme için zaten varsa, kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-155">If a native image exists for the assembly, it is not used.</span></span>  
  
-   <span data-ttu-id="d6bfd-156">Etki alanı bağımsız derlemesi yüklenemiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-156">The assembly cannot be loaded as domain-neutral.</span></span>  
  
-   <span data-ttu-id="d6bfd-157">.NET Framework sürüm 1.0 ve 1.1, ilke uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-157">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
<a name="avoid_partial_names"></a>   
## <a name="avoid-binding-on-partial-assembly-names"></a><span data-ttu-id="d6bfd-158">Kısmi derleme adları bağlama kaçının</span><span class="sxs-lookup"><span data-stu-id="d6bfd-158">Avoid Binding on Partial Assembly Names</span></span>  
 <span data-ttu-id="d6bfd-159">Kısmi adı bağlama derleme görünen adı yalnızca parçası belirttiğinizde oluşur (<xref:System.Reflection.Assembly.FullName%2A>) bir derleme yükleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-159">Partial name binding occurs when you specify only part of the assembly display name (<xref:System.Reflection.Assembly.FullName%2A>) when you load an assembly.</span></span> <span data-ttu-id="d6bfd-160">Örneğin, çağırabilirsiniz <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> adıyla yalnızca basit derlemenin sürüm, kültür ve ortak anahtar belirteci atlama yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-160">For example, you might call the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method with only the simple name of the assembly, omitting the version, culture, and public key token.</span></span> <span data-ttu-id="d6bfd-161">Veya deniyor olabilir <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> yöntemi, hangi ilk çağıran <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemi ve derleme bulmak başarısız olursa, Genel Derleme Önbelleği arar ve derlemenin en son sürümünü yükler.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-161">Or you might call the <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method, which first calls the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and, if that fails to locate the assembly, searches the global assembly cache and loads the latest available version of the assembly.</span></span>  
  
 <span data-ttu-id="d6bfd-162">Kısmi ad bağlama, aşağıdakiler de dahil olmak üzere birçok sorunlara neden olabilir:</span><span class="sxs-lookup"><span data-stu-id="d6bfd-162">Partial name binding can cause many problems, including the following:</span></span>  
  
-   <span data-ttu-id="d6bfd-163"><xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> Yöntemi basit adında farklı bir derleme yük.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-163">The <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method might load a different assembly with the same simple name.</span></span> <span data-ttu-id="d6bfd-164">Örneğin, iki uygulama hem basit adına sahip iki tamamen farklı derlemelerinin `GraphicsLibrary` genel derleme önbelleğine.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-164">For example, two applications might install two completely different assemblies that both have the simple name `GraphicsLibrary` into the global assembly cache.</span></span>  
  
-   <span data-ttu-id="d6bfd-165">Gerçekte yüklü olduğu derleme geriye dönük olarak uyumlu olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-165">The assembly that is actually loaded might not be backward-compatible.</span></span> <span data-ttu-id="d6bfd-166">Örneğin, sürüm belirtmeden programınızı ilk olarak kullanmak üzere yazılmış kadar sürümden daha sonraki bir sürümü yükleme de neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-166">For example, not specifying the version might result in the loading of a much later version than the version your program was originally written to use.</span></span> <span data-ttu-id="d6bfd-167">Sonraki bir sürümünü değişiklikleri uygulamanızda hatalara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-167">Changes in the later version might cause errors in your application.</span></span>  
  
-   <span data-ttu-id="d6bfd-168">Gerçekte yüklü olduğu derleme İleri uyumlu olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-168">The assembly that is actually loaded might not be forward-compatible.</span></span> <span data-ttu-id="d6bfd-169">Örneğin, yerleşik ve bir derlemenin en son sürümü ile uygulamanızı test, ancak kısmi bağlama uygulamanızın kullandığı özellikler eksik daha önceki bir sürümünü yükleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-169">For example, you might have built and tested your application with the latest version of an assembly, but partial binding might load a much earlier version that lacks features your application uses.</span></span>  
  
-   <span data-ttu-id="d6bfd-170">Yeni uygulamaları yüklemek, var olan uygulamaları bozulabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-170">Installing new applications can break existing applications.</span></span> <span data-ttu-id="d6bfd-171">Kullanan bir uygulamayı <xref:System.Reflection.Assembly.LoadWithPartialName%2A> yöntemi daha yeni bir uyumsuz bir paylaşılan derleme sürümü yükleyerek kilidi kırılmamış.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-171">An application that uses the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method can be broken by installing a newer, incompatible version of a shared assembly.</span></span>  
  
-   <span data-ttu-id="d6bfd-172">Beklenmeyen bağımlılık yükleniyor ortaya çıkabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-172">Unexpected dependency loading can occur.</span></span> <span data-ttu-id="d6bfd-173">Bu iletiyi bileşenini kullanarak bir derlemede kısmi bağlama ile yüklenirken bir bağımlılık sonuçlanabilir paylaşan iki derleme yük yerleşik veya ile test.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-173">It you load two assemblies that share a dependency, loading them with partial binding might result in one assembly using a component that it was not built or tested with.</span></span>  
  
 <span data-ttu-id="d6bfd-174">Bu neden olabilir, sorunları nedeniyle <xref:System.Reflection.Assembly.LoadWithPartialName%2A> yöntemi olarak işaretlendi artık kullanılmıyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-174">Because of the problems it can cause, the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method has been marked obsolete.</span></span> <span data-ttu-id="d6bfd-175">Kullanmanızı öneririz <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemi bunun yerine ve tam derleme görünen adları belirtin.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-175">We recommend that you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method instead, and specify full assembly display names.</span></span> <span data-ttu-id="d6bfd-176">Bkz: [olumlu ve olumsuz yönlerini yükleme bağlamları anlamak](#load_contexts) ve [için varsayılan yükleme bağlamı değiştirmeyi göz önünde bulundurun](#switch_to_default).</span><span class="sxs-lookup"><span data-stu-id="d6bfd-176">See [Understand the Advantages and Disadvantages of Load Contexts](#load_contexts) and [Consider Switching to the Default Load Context](#switch_to_default).</span></span>  
  
 <span data-ttu-id="d6bfd-177">Kullanmak istiyorsanız, <xref:System.Reflection.Assembly.LoadWithPartialName%2A> yöntemi kolay, yükleme derleme yaptığından düşünün uygulamanızı eksik derleme tanımlayan bir hata iletisi ile başarısız olan'otomatik olarak kullanmaktan daha iyi bir kullanıcı deneyimi sağlamak büyük olasılıkla bir öngörülemeyen davranışı ve güvenlik açıklarını neden olabilecek derleme sürümü bilinmiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-177">If you want to use the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method because it makes assembly loading easy, consider that having your application fail with an error message that identifies the missing assembly is likely to provide a better user experience than automatically using an unknown version of the assembly, which might cause unpredictable behavior and security holes.</span></span>  
  
<a name="avoid_loading_into_multiple_contexts"></a>   
## <a name="avoid-loading-an-assembly-into-multiple-contexts"></a><span data-ttu-id="d6bfd-178">Birden çok bağlamları bir derlemesi yüklenirken kaçının</span><span class="sxs-lookup"><span data-stu-id="d6bfd-178">Avoid Loading an Assembly into Multiple Contexts</span></span>  
 <span data-ttu-id="d6bfd-179">Birden çok bağlamları bir derlemesi yüklenirken türü kimlik sorunlara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-179">Loading an assembly into multiple contexts can cause type identity problems.</span></span> <span data-ttu-id="d6bfd-180">Aynı türde iki farklı bağlamdan içine aynı derlemeden yüklenir, aynı ada sahip iki farklı tür yüklenmiş gibi olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-180">If the same type is loaded from the same assembly into two different contexts, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="d6bfd-181">Bir <xref:System.InvalidCastException> bir diğerini yazın kafa karıştırıcı iletiyle türe çalışırsanız durum `MyType` türüne yayınlanamıyor `MyType`.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-181">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="d6bfd-182">Örneğin, varsayın `ICommunicate` arabirimi adlı bir derleme bildirilen `Utility`, programınızı ve ayrıca programınızı yükler diğer derlemelerden tarafından başvurulan.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-182">For example, suppose that the `ICommunicate` interface is declared in an assembly named `Utility`, which is referenced by your program and also by other assemblies that your program loads.</span></span> <span data-ttu-id="d6bfd-183">Bu diğer derlemeler uygulama türlerini içeren `ICommunicate` arabirimi, bunları kullanmak, program izin verme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-183">These other assemblies contain types that implement the `ICommunicate` interface, allowing your program to use them.</span></span>  
  
 <span data-ttu-id="d6bfd-184">Şimdi, program çalıştırıldığında ne olacağını düşünün.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-184">Now consider what happens when your program is run.</span></span> <span data-ttu-id="d6bfd-185">Varsayılan yükleme bağlamına programınız tarafından başvurulan bir derleme yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-185">Assemblies that are referenced by your program are loaded into the default load context.</span></span> <span data-ttu-id="d6bfd-186">Hedef derleme kimliğini tarafından yüklerseniz kullanarak <xref:System.Reflection.Assembly.Load%2A> yöntemi, varsayılan yükleme bağlamı olacaktır ve bu nedenle bağımlılıklarını olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-186">If you load a target assembly by its identity, using the <xref:System.Reflection.Assembly.Load%2A> method, it will be in the default load context, and so will its dependencies.</span></span> <span data-ttu-id="d6bfd-187">Programınız ve hedef derleme aynı kullanacağı `Utility` derleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-187">Both your program and the target assembly will use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="d6bfd-188">Ancak, dosya yoluna göre hedef derleme yük varsayalım kullanarak <xref:System.Reflection.Assembly.LoadFile%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-188">However, suppose you load the target assembly by its file path, using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="d6bfd-189">Derleme herhangi bir bağlam olmadan yüklü olduğundan bağımlılıklarını otomatik olarak yüklenmez.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-189">The assembly is loaded without any context, so its dependencies are not automatically loaded.</span></span> <span data-ttu-id="d6bfd-190">İçin bir işleyici olabilir <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> bağımlılık ve bunu sağlamak için olay yük `Utility` kullanarak bir bağlam yok derleme <xref:System.Reflection.Assembly.LoadFile%2A> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-190">You might have a handler for the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to supply the dependency, and it might load the `Utility` assembly with no context by using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="d6bfd-191">Şimdi bir türünün bir örneği oluşturduğunuzda türü değişkenine atamak için deneyin ve hedef derleme bulunan `ICommunicate`, bir <xref:System.InvalidCastException> çalışma zamanı algıladığından durum `ICommunicate` iki kopyasını arabirimlerde `Utility` farklı tür derleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-191">Now when you create an instance of a type that is contained in the target assembly and try to assign it to a variable of type `ICommunicate`, an <xref:System.InvalidCastException> is thrown because the runtime considers the `ICommunicate` interfaces in the two copies of the `Utility` assembly to be different types.</span></span>  
  
 <span data-ttu-id="d6bfd-192">Bir derleme birden çok bağlamları yüklenen olabilir birçok senaryo vardır.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-192">There are many other scenarios in which an assembly can be loaded into multiple contexts.</span></span> <span data-ttu-id="d6bfd-193">Uygulama yolu hedef derlemesinde yerini değiştirme ve kullanarak çakışmaları önlemek için en iyi yaklaşımdır <xref:System.Reflection.Assembly.Load%2A> yöntemi tam görünen adı.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-193">The best approach is to avoid conflicts by relocating the target assembly in your application path and using the <xref:System.Reflection.Assembly.Load%2A> method with the full display name.</span></span> <span data-ttu-id="d6bfd-194">Derleme sonra varsayılan yük bağlamına yüklenir ve her iki derlemeleri aynı kullanma `Utility` derleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-194">The assembly is then loaded into the default load context, and both assemblies use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="d6bfd-195">Hedef derleme uygulama yolunuz dışında kalırsa, bu gerekir kullanabileceğiniz <xref:System.Reflection.Assembly.LoadFrom%2A> bağlamdan yükleme yüklemek için yöntem.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-195">If the target assembly must remain outside your application path, you can use the <xref:System.Reflection.Assembly.LoadFrom%2A> method to load it into the load-from context.</span></span> <span data-ttu-id="d6bfd-196">Hedef derleme uygulamanızın başvuru derlenen varsa `Utility` derleme, bu kullanacağınız `Utility` uygulamanız varsayılan yük bağlamına yükledi derleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-196">If the target assembly was compiled with a reference to your application's `Utility` assembly, it will use the `Utility` assembly that your application has loaded into the default load context.</span></span> <span data-ttu-id="d6bfd-197">Hedef derleme bir kopyası üzerinde bir bağımlılığa sahipse sorunlar oluşabilir Not `Utility` derleme dışında Uygulama yolu bulunur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-197">Note that problems can occur if the target assembly has a dependency on a copy of the `Utility` assembly located outside your application path.</span></span> <span data-ttu-id="d6bfd-198">Bu derleme bağlamdan yükleme, uygulama yükünü önce halinde yüklenmiş ise `Utility` derleme, uygulamanızın yükleme başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-198">If that assembly is loaded into the load-from context before your application loads the `Utility` assembly, your application's load will fail.</span></span>  
  
 <span data-ttu-id="d6bfd-199">[Göz önünde bulundurun geçiş varsayılan yükleme bağlamı için](#switch_to_default) bölümde açıklanmıştır dosya yolu yükleri gibi kullanılarak alternatifleri <xref:System.Reflection.Assembly.LoadFile%2A> ve <xref:System.Reflection.Assembly.LoadFrom%2A>.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-199">The [Consider Switching to the Default Load Context](#switch_to_default) section discusses alternatives to using file path loads such as <xref:System.Reflection.Assembly.LoadFile%2A> and <xref:System.Reflection.Assembly.LoadFrom%2A>.</span></span>  
  
<a name="avoid_loading_multiple_versions"></a>   
## <a name="avoid-loading-multiple-versions-of-an-assembly-into-the-same-context"></a><span data-ttu-id="d6bfd-200">Derleme birden fazla sürümünü aynı bağlamına yüklenmesini önlemek</span><span class="sxs-lookup"><span data-stu-id="d6bfd-200">Avoid Loading Multiple Versions of an Assembly into the Same Context</span></span>  
 <span data-ttu-id="d6bfd-201">Bir yük bağlamına derleme birden fazla sürümünü yükleme türü kimlik sorunlara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-201">Loading multiple versions of an assembly into one load context can cause type identity problems.</span></span> <span data-ttu-id="d6bfd-202">Aynı türde iki aynı bütünleştirilmiş kodda sürümlerinden yüklenir, aynı ada sahip iki farklı tür yüklenmiş gibi olur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-202">If the same type is loaded from two versions of the same assembly, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="d6bfd-203">Bir <xref:System.InvalidCastException> bir diğerini yazın kafa karıştırıcı iletiyle türe çalışırsanız durum `MyType` türüne yayınlanamıyor `MyType`.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-203">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="d6bfd-204">Örneğin, programınızı bir sürümünü yükleyebilirsiniz `Utility` derleme doğrudan ve daha sonra farklı bir sürümünü yükler başka bir derleme yükleyebilir `Utility` derleme.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-204">For example, your program might load one version of the `Utility` assembly directly, and later it might load another assembly that loads a different version of the `Utility` assembly.</span></span> <span data-ttu-id="d6bfd-205">Veya bir kodlama hatası iki farklı kod yolları bir derlemenin farklı sürümleri yüklemek için uygulamanızda neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-205">Or a coding error might cause two different code paths in your application to load different versions of an assembly.</span></span>  
  
 <span data-ttu-id="d6bfd-206">Varsayılan yükleme bağlamı kullandığınızda bu sorun oluşabilir <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemi ve farklı sürüm numaralarını içeren tam derleme görünen adları belirtin.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-206">In the default load context, this problem can occur when you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and specify complete assembly display names that include different version numbers.</span></span> <span data-ttu-id="d6bfd-207">Bağlam olmadan yüklenen derlemeler için kullanarak sorunu kaynaklanabilir <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> aynı bütünleştirilmiş kodda farklı yolları yükleme yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-207">For assemblies that are loaded without context, the problem can be caused by using the <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> method to load the same assembly from different paths.</span></span> <span data-ttu-id="d6bfd-208">Çalışma zamanı kimliklerini aynı olsa bile, farklı derlemeler olması için farklı yollar yüklenen iki derleme göz önünde bulundurur.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-208">The runtime considers two assemblies that are loaded from different paths to be different assemblies, even if their identities are the same.</span></span>  
  
 <span data-ttu-id="d6bfd-209">Tür kimliği ek olarak, bir derleme birden fazla sürümünü sorunlara yol açabilir bir <xref:System.MissingMethodException> farklı bir sürümü bu türden bekliyor koduna bir derleme sürümünden yüklenen bir türü aktarılırsa.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-209">In addition to type identity problems, multiple versions of an assembly can cause a <xref:System.MissingMethodException> if a type that is loaded from one version of the assembly is passed to code that expects that type from a different version.</span></span> <span data-ttu-id="d6bfd-210">Örneğin, kod daha yeni sürümüne eklenmiş olan bir yöntem bekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-210">For example, the code might expect a method that was added to the later version.</span></span>  
  
 <span data-ttu-id="d6bfd-211">Sürümler arasında türünün davranışını değiştirdiyseniz, daha hafif hatalar oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-211">More subtle errors can occur if the behavior of the type changed between versions.</span></span> <span data-ttu-id="d6bfd-212">Örneğin, bir yöntem bir beklenmeyen bir özel durum oluşturduğunda veya beklenmeyen bir değer döndürür.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-212">For example, a method might throw an unexpected exception or return an unexpected value.</span></span>  
  
 <span data-ttu-id="d6bfd-213">Yalnızca bir emin olmak için kodunuzu dikkatlice inceleyin derleme sürümü yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-213">Carefully review your code to ensure that only one version of an assembly is loaded.</span></span> <span data-ttu-id="d6bfd-214">Kullanabileceğiniz <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> hangi derlemelerin herhangi bir zamanda yüklenen belirlemek amacıyla yöntemi.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-214">You can use the <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> method to determine which assemblies are loaded at any given time.</span></span>  
  
<a name="switch_to_default"></a>   
## <a name="consider-switching-to-the-default-load-context"></a><span data-ttu-id="d6bfd-215">Varsayılan yükleme bağlamı geçmeniz önerilir</span><span class="sxs-lookup"><span data-stu-id="d6bfd-215">Consider Switching to the Default Load Context</span></span>  
 <span data-ttu-id="d6bfd-216">Uygulamanızın derleme yükleme ve dağıtım desenleri inceleyin.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-216">Examine your application's assembly loading and deployment patterns.</span></span> <span data-ttu-id="d6bfd-217">Bayt dizileri yüklenen derlemeler ortadan kaldırabilir?</span><span class="sxs-lookup"><span data-stu-id="d6bfd-217">Can you eliminate assemblies that are loaded from byte arrays?</span></span> <span data-ttu-id="d6bfd-218">Yoklama yola derlemeleri taşıyabilir miyim?</span><span class="sxs-lookup"><span data-stu-id="d6bfd-218">Can you move assemblies into the probing path?</span></span> <span data-ttu-id="d6bfd-219">Derlemeleri genel derleme önbelleğinde veya uygulama etki alanında bulunuyorsa yol yoklama (diğer bir deyişle, kendi <xref:System.AppDomainSetup.ApplicationBase%2A> ve <xref:System.AppDomainSetup.PrivateBinPath%2A>), derleme kimliğini tarafından yükleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-219">If assemblies are located in the global assembly cache or in the application domain's probing path (that is, its <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A>), you can load the assembly by its identity.</span></span>  
  
 <span data-ttu-id="d6bfd-220">Tüm derlemeleriniz algılama yolu yerleştirilecek mümkün değilse, eklenti .NET Framework modelini kullanarak, derlemeleri genel derleme önbelleğine yerleştirme veya uygulama etki alanları oluşturma gibi alternatifleri göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-220">If it is not possible to put all your assemblies in the probing path, consider alternatives such as using the .NET Framework add-in model, placing assemblies into the global assembly cache, or creating application domains.</span></span>  
  
### <a name="consider-using-the-net-framework-add-in-model"></a><span data-ttu-id="d6bfd-221">Eklenti .NET Framework modeli kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="d6bfd-221">Consider Using the .NET Framework Add-In Model</span></span>  
 <span data-ttu-id="d6bfd-222">Genellikle uygulama Bankası'nda yüklü değil, eklentiler, uygulamak için bağlamdan yükleme kullanıyorsanız eklenti .NET Framework modelini kullanır.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-222">If you are using the load-from context to implement add-ins, which typically are not installed in the application base, use the .NET Framework add-in model.</span></span> <span data-ttu-id="d6bfd-223">Bu model, uygulama etki alanları kendiniz yönetmeye gerek kalmadan uygulama düzeyinde etki alanı ya da işlem, yalıtım sağlar.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-223">This model provides isolation at the application domain or process level, without requiring you to manage application domains yourself.</span></span> <span data-ttu-id="d6bfd-224">Eklenti modeli hakkında daha fazla bilgi için bkz: [eklentiler ve genişletilebilirlik](../../../docs/framework/add-ins/index.md).</span><span class="sxs-lookup"><span data-stu-id="d6bfd-224">For information about the add-in model, see [Add-ins and Extensibility](../../../docs/framework/add-ins/index.md).</span></span>  
  
### <a name="consider-using-the-global-assembly-cache"></a><span data-ttu-id="d6bfd-225">Genel Derleme Önbelleği kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="d6bfd-225">Consider Using the Global Assembly Cache</span></span>  
 <span data-ttu-id="d6bfd-226">Bir paylaşılan derleme yolu elde etmek için yer derlemeleri genel derleme önbelleğinde temel, uygulama dışında diğer bağlamlarda dezavantajları sürüyor veya varsayılan yükleme bağlamı avantajını kaybetmeden olmasıdır.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-226">Place assemblies in the global assembly cache to get the benefit of a shared assembly path that is outside the application base, without losing the advantages of the default load context or taking on the disadvantages of the other contexts.</span></span>  
  
### <a name="consider-using-application-domains"></a><span data-ttu-id="d6bfd-227">Uygulama etki alanları kullanmayı düşünün</span><span class="sxs-lookup"><span data-stu-id="d6bfd-227">Consider Using Application Domains</span></span>  
 <span data-ttu-id="d6bfd-228">Bazı derlemeleriniz uygulamanın yoklama yolunda dağıtılamıyor karar verirseniz, bu derlemeler için yeni bir uygulama etki alanı oluşturmayı düşünün.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-228">If you determine that some of your assemblies cannot be deployed in the application's probing path, consider creating a new application domain for those assemblies.</span></span> <span data-ttu-id="d6bfd-229">Kullanan bir <xref:System.AppDomainSetup> yeni uygulama etki alanı oluşturma ve kullanma <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> özelliği yüklemek istediğiniz derlemeleri içeren yolu belirtin.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-229">Use an <xref:System.AppDomainSetup> to create the new application domain, and use the <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> property to specify the path that contains the assemblies you want to load.</span></span> <span data-ttu-id="d6bfd-230">Yoklama için birden fazla dizine sahipseniz, ayarlayabileceğiniz <xref:System.AppDomainSetup.ApplicationBase%2A> kök dizini ve kullanımına <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> araştırma için alt dizinler tanımlamak için özellik.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-230">If you have multiple directories to probe, you can set the <xref:System.AppDomainSetup.ApplicationBase%2A> to a root directory and use the <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> property to identify the subdirectories to probe.</span></span> <span data-ttu-id="d6bfd-231">Alternatif olarak, birden çok uygulama etki alanları oluşturun ve ayarlayın <xref:System.AppDomainSetup.ApplicationBase%2A> her uygulama etki alanı kendi derlemeler için uygun yolu.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-231">Alternatively, you can create multiple application domains and set the <xref:System.AppDomainSetup.ApplicationBase%2A> of each application domain to the appropriate path for its assemblies.</span></span>  
  
 <span data-ttu-id="d6bfd-232">Kullanabileceğiniz Not <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> yöntemi bu derlemeler yüklenemiyor.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-232">Note that you can use the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method to load these assemblies.</span></span> <span data-ttu-id="d6bfd-233">Şimdi algılama yolu olduklarından, bunlar bağlamdan yükleme yerine varsayılan yükleme bağlamı içine yüklenir.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-233">Because they are now in the probing path, they will be loaded into the default load context instead of the load-from context.</span></span> <span data-ttu-id="d6bfd-234">Ancak, için geçiş öneririz <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> yöntemi ve doğru sürümlerini her zaman kullanılmasını sağlamak için derleme görünen adları tam sağlayın.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-234">However, we recommend that you switch to the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and supply full assembly display names to ensure that correct versions are always used.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d6bfd-235">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="d6bfd-235">See Also</span></span>  
 <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>  
 <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>  
 <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType>  
 <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>  
 [<span data-ttu-id="d6bfd-236">Eklentiler ve genişletilebilirlik</span><span class="sxs-lookup"><span data-stu-id="d6bfd-236">Add-ins and Extensibility</span></span>](../../../docs/framework/add-ins/index.md)
