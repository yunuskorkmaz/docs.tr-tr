---
title: x:Shared Özniteliği
ms.date: 03/30/2017
helpviewer_keywords:
- XAML [XAML Services], x:Shared attribute
- x:Shared attribute [XAML Services]
- Shared attribute in XAML [XAML Services]
ms.assetid: c8cff434-2785-405f-9f95-16deb34c9e64
ms.openlocfilehash: 42de341d59e3e70103db765faf3160b5fe3250d3
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2019
ms.locfileid: "58039398"
---
# <a name="xshared-attribute"></a><span data-ttu-id="27e8c-102">x:Shared Özniteliği</span><span class="sxs-lookup"><span data-stu-id="27e8c-102">x:Shared Attribute</span></span>
<span data-ttu-id="27e8c-103">Ayarlandığında `false`, böylece tüm istekler için aynı örneğini paylaşmak yerine, her istek için yeni bir örneği oluşturma isteği öznitelikli kaynağı için WPF kaynak alma davranışını değiştirir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-103">When set to `false`, modifies WPF resource-retrieval behavior so that requests for the attributed resource create a new instance for each request instead of sharing the same instance for all requests.</span></span>  
  
## <a name="xaml-attribute-usage"></a><span data-ttu-id="27e8c-104">XAML Öznitelik Kullanımı</span><span class="sxs-lookup"><span data-stu-id="27e8c-104">XAML Attribute Usage</span></span>  
  
```xaml  
<ResourceDictionary>  
  <object x:Shared="false".../>  
</ResourceDictionary>  
```  
  
## <a name="remarks"></a><span data-ttu-id="27e8c-105">Açıklamalar</span><span class="sxs-lookup"><span data-stu-id="27e8c-105">Remarks</span></span>  
 <span data-ttu-id="27e8c-106">`x:Shared` XAML dil XAML ad alanına eşlenen ve geçerli bir XAML dil öğesi .NET Framework XAML hizmetlerinde ve kendi XAML okuyucular tarafından değerlendirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-106">`x:Shared` is mapped to the XAML language XAML namespace and is recognized as a valid XAML language element by .NET Framework XAML Services and its XAML readers.</span></span> <span data-ttu-id="27e8c-107">Ancak, belirtilen yeteneklerini `x:Shared` yalnızca ve WPF XAML ayrıştırıcı WPF uygulamaları için uygundur.</span><span class="sxs-lookup"><span data-stu-id="27e8c-107">However, the stated capabilities of `x:Shared` are only relevant for WPF applications and for the WPF XAML parser.</span></span> <span data-ttu-id="27e8c-108">WPF, `x:Shared` WPF içinde var olan bir nesneye uygulanan yalnızca bir özniteliği olarak kullanışlıdır <xref:System.Windows.ResourceDictionary>.</span><span class="sxs-lookup"><span data-stu-id="27e8c-108">In WPF, `x:Shared` is only useful as an attribute when it is applied to an object that exists within a WPF <xref:System.Windows.ResourceDictionary>.</span></span> <span data-ttu-id="27e8c-109">Diğer kullanımları ayrıştırma özel durumlar veya diğer hatalar oluşturması beklenmiyor ancak bunlar herhangi bir etkisi yoktur.</span><span class="sxs-lookup"><span data-stu-id="27e8c-109">Other usages do not throw parse exceptions or other errors, but they have no effect.</span></span>  
  
 <span data-ttu-id="27e8c-110">Anlamını `x:Shared` XAML dil belirtimi belirtilmedi.</span><span class="sxs-lookup"><span data-stu-id="27e8c-110">The meaning of `x:Shared` is not specified in the XAML language specification.</span></span> <span data-ttu-id="27e8c-111">.NET Framework XAML hizmetlerinde yapı olanlar gibi diğer XAML uygulamaları kaynak paylaşma desteği sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="27e8c-111">Other XAML implementations, such as those that build on .NET Framework XAML Services, do not necessarily provide resource-sharing support.</span></span> <span data-ttu-id="27e8c-112">Bu tür XAML uygulamaları da kullanılan destekleyici Framework benzer bir davranış sağlayabilir `x:Shared` değerleri.</span><span class="sxs-lookup"><span data-stu-id="27e8c-112">Such XAML implementations could provide similar behavior in the supporting framework that also used `x:Shared` values.</span></span>  
  
 <span data-ttu-id="27e8c-113">WPF, varsayılan `x:Shared` kaynaklar için koşul `true`.</span><span class="sxs-lookup"><span data-stu-id="27e8c-113">In WPF, the default `x:Shared` condition for resources is `true`.</span></span> <span data-ttu-id="27e8c-114">Bu durum, belirtilen kaynak isteği gönderirken her zaman aynı örneğini döndürür anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-114">This condition means that any given resource request always returns the same instance.</span></span>  
  
 <span data-ttu-id="27e8c-115">Bir kaynağı API gibi döndürülen bir nesne değiştirme <xref:System.Windows.FrameworkElement.FindResource%2A>, veya bir nesne doğrudan içinde değiştirerek bir <xref:System.Windows.ResourceDictionary>, özgün kaynak değiştirir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-115">Modifying an object that is returned through a resource API, such as <xref:System.Windows.FrameworkElement.FindResource%2A>, or modifying an object directly within a <xref:System.Windows.ResourceDictionary>, changes the original resource.</span></span> <span data-ttu-id="27e8c-116">Referansları bu kaynağa dinamik kaynak başvurularını varsa, bu kaynağa tüketicilerinin değiştirilen kaynak alın.</span><span class="sxs-lookup"><span data-stu-id="27e8c-116">If references to that resource were dynamic resource references, the consumers of that resource get the changed resource.</span></span>  
  
 <span data-ttu-id="27e8c-117">Kaynak başvurularını statik kaynak başvurularını olsaydı, sonra kaynak değişikliklerini [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] ilgisiz işleme süresi.</span><span class="sxs-lookup"><span data-stu-id="27e8c-117">If references to the resource were static resource references, changes to the resource after [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] processing time are irrelevant.</span></span> <span data-ttu-id="27e8c-118">Statik ve dinamik kaynak başvuruları hakkında daha fazla bilgi için bkz: [XAML kaynakları](../wpf/advanced/xaml-resources.md).</span><span class="sxs-lookup"><span data-stu-id="27e8c-118">For more information about static versus dynamic resource references, see [XAML Resources](../wpf/advanced/xaml-resources.md).</span></span>  
  
 <span data-ttu-id="27e8c-119">Açıkça belirtilmesi `x:Shared="true"` , zaten varsayılan olduğundan nadiren, gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-119">Explicitly specifying `x:Shared="true"` is rarely done, because that is already the default.</span></span> <span data-ttu-id="27e8c-120">Doğrudan kod için eşdeğer `x:Shared` WPF içinde nesne modeli; yalnızca bir XAML kullanımı belirtilebilir ve varsayılan WPF davranış veya yükleme yolundaki Ara bir XAML düğüm akış kullanarak .NET Framework XAML Se işlediğinde işlenmesi gerekir rvices ve kendi XAML okuyucular.</span><span class="sxs-lookup"><span data-stu-id="27e8c-120">There is no direct code equivalent for `x:Shared` in the WPF object model; it can only be specified in a XAML usage and must be processed either by the default WPF behavior or in an intermediate XAML node stream on the load path if processed using .NET Framework XAML Services and its XAML readers.</span></span>  
  
 <span data-ttu-id="27e8c-121">Bir senaryo için `x:Shared="false"` tanımlarsınız, bir <xref:System.Windows.FrameworkElement> veya <xref:System.Windows.FrameworkContentElement> türetilmiş bir sınıf kaynak olarak ve size bir içerik modeline öğesi kaynak tanıtır.</span><span class="sxs-lookup"><span data-stu-id="27e8c-121">A scenario for `x:Shared="false"` is if you define a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> derived class as a resource and then you introduce the element resource into a content model.</span></span> <span data-ttu-id="27e8c-122">`x:Shared="false"` birden çok kez aynı koleksiyonda tanıtılmak üzere bir öğe kaynak sağlar (gibi bir <xref:System.Windows.Controls.UIElementCollection>).</span><span class="sxs-lookup"><span data-stu-id="27e8c-122">`x:Shared="false"` enables an element resource to be introduced multiple times in the same collection (such as a <xref:System.Windows.Controls.UIElementCollection>).</span></span> <span data-ttu-id="27e8c-123">Olmadan `x:Shared="false"` koleksiyonun içeriğini benzersizlik zorladığından bu geçersizdir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-123">Without `x:Shared="false"` this is invalid because the collection enforces uniqueness of its contents.</span></span> <span data-ttu-id="27e8c-124">Ancak, `x:Shared="false"` davranışı aynı örneğini döndürmek yerine kaynak aynı başka bir örneğini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="27e8c-124">However, the `x:Shared="false"` behavior creates another identical instance of the resource instead of returning the same instance.</span></span>  
  
 <span data-ttu-id="27e8c-125">Başka bir senaryo için `x:Shared="false"` kullanıyorsanız olduğu bir <xref:System.Windows.Freezable> animasyon değerlerini ancak kaynak animasyon başına temelinde değiştirmek istediğiniz kaynak.</span><span class="sxs-lookup"><span data-stu-id="27e8c-125">Another scenario for `x:Shared="false"` is if you use a <xref:System.Windows.Freezable> resource for animation values but want to modify the resource on a per animation basis.</span></span>  
  
 <span data-ttu-id="27e8c-126">Dize işleme `false` büyük/küçük harfe duyarlı değildir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-126">The string handling of `false` is not case sensitive.</span></span>  
  
 <span data-ttu-id="27e8c-127">WPF, `x:Shared` yalnızca aşağıdaki koşullarda geçerli olur:</span><span class="sxs-lookup"><span data-stu-id="27e8c-127">In WPF, `x:Shared` is only valid under the following conditions:</span></span>  
  
-   <span data-ttu-id="27e8c-128"><xref:System.Windows.ResourceDictionary> İle öğeleri içeren `x:Shared` derlenmelidir.</span><span class="sxs-lookup"><span data-stu-id="27e8c-128">The <xref:System.Windows.ResourceDictionary> that contains the items with `x:Shared` must be compiled.</span></span> <span data-ttu-id="27e8c-129"><xref:System.Windows.ResourceDictionary> Gevşek XAML içinde olamaz ya da temaları için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="27e8c-129">The <xref:System.Windows.ResourceDictionary> cannot be within loose XAML or used for themes.</span></span>  
  
-   <span data-ttu-id="27e8c-130"><xref:System.Windows.ResourceDictionary> Öğelerini içeren başka içinde iç içe olmamalıdır değil <xref:System.Windows.ResourceDictionary>.</span><span class="sxs-lookup"><span data-stu-id="27e8c-130">The <xref:System.Windows.ResourceDictionary> that contains the items must not be nested within another <xref:System.Windows.ResourceDictionary>.</span></span> <span data-ttu-id="27e8c-131">Örneğin, kullanamazsınız `x:Shared` öğeleri için bir <xref:System.Windows.ResourceDictionary> içinde olan bir <xref:System.Windows.Style> zaten olan bir <xref:System.Windows.ResourceDictionary> öğesi.</span><span class="sxs-lookup"><span data-stu-id="27e8c-131">For example, you cannot use `x:Shared` for items in a <xref:System.Windows.ResourceDictionary> that is within a <xref:System.Windows.Style> that is already a <xref:System.Windows.ResourceDictionary> item.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="27e8c-132">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="27e8c-132">See also</span></span>
- <xref:System.Windows.ResourceDictionary>
- [<span data-ttu-id="27e8c-133">XAML Kaynakları</span><span class="sxs-lookup"><span data-stu-id="27e8c-133">XAML Resources</span></span>](../wpf/advanced/xaml-resources.md)
- [<span data-ttu-id="27e8c-134">Temel Öğeler</span><span class="sxs-lookup"><span data-stu-id="27e8c-134">Base Elements</span></span>](../wpf/advanced/base-elements.md)
