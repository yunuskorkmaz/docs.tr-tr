---
title: XAML Düğüm Akış Yapılarını ve Kavramlarını Anlama
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: 2c8093c3ef497bd836427f71098e62626f228e24
ms.sourcegitcommit: 22be09204266253d45ece46f51cc6f080f2b3fd6
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/07/2019
ms.locfileid: "73733384"
---
# <a name="understanding-xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="5babd-102">XAML Düğüm Akış Yapılarını ve Kavramlarını Anlama</span><span class="sxs-lookup"><span data-stu-id="5babd-102">Understanding XAML Node Stream Structures and Concepts</span></span>

<span data-ttu-id="5babd-103">.NET Framework XAML hizmetlerinde uygulanan xaml okuyucuları ve XAML yazarları, XAML düğüm akışının tasarım kavramını temel alır.</span><span class="sxs-lookup"><span data-stu-id="5babd-103">XAML readers and XAML writers as implemented in .NET Framework XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="5babd-104">XAML düğüm akışı, bir dizi XAML düğümü için bir dizi kavramsalization.</span><span class="sxs-lookup"><span data-stu-id="5babd-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="5babd-105">Bu conceptuselte, bir XAML işlemcisi XAML içindeki düğüm ilişkilerinin yapısını birer birer gösterir.</span><span class="sxs-lookup"><span data-stu-id="5babd-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="5babd-106">Herhangi bir zamanda, açık bir XAML düğüm akışında yalnızca bir adet geçerli kayıt veya geçerli konum bulunur ve API 'nin birçok yönü yalnızca ilgili konumdan kullanılabilen bilgileri rapor ediyor.</span><span class="sxs-lookup"><span data-stu-id="5babd-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="5babd-107">Bir XAML düğüm akışındaki geçerli düğüm bir nesne, üye veya değer olarak açıklanabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="5babd-108">Xaml okuyucular bir XAML düğüm akışı olarak davranarak XAML yazarları ile iletişim kurabilir ve bir programın bir yük yolu ya da XAML içeren bir yol kaydetme işlemi sırasında XAML düğüm akışının içeriğini görüntülemesini, bunlarla etkileşime geçmesini veya değiştirmesini sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="5babd-109">XAML okuyucu ve yazıcı API 'SI tasarımı ve XAML düğüm akışı kavramı, XML Belge Nesne Modeli (DOM) ve <xref:System.Xml.XmlReader> ve <xref:System.Xml.XmlWriter> sınıfları gibi önceki ilgili okuyucu ve yazıcı tasarımlarına ve kavramlarına benzerdir.</span><span class="sxs-lookup"><span data-stu-id="5babd-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the XML Document Object Model (DOM) and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="5babd-110">Bu konu XAML düğüm akışı kavramlarını ve xaml düğüm düzeyinde xaml temsilleri ile etkileşime geçen yordamları nasıl yazacağınız açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="5babd-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

<a name="loading_into_a_xaml_reader"></a>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="5babd-111">Xaml 'yi XAML okuyucusuna yükleme</span><span class="sxs-lookup"><span data-stu-id="5babd-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="5babd-112">Temel <xref:System.Xaml.XamlReader> sınıfı, ilk XAML 'yi bir XAML okuyucusuna yüklemek için belirli bir teknik bildirmiyor.</span><span class="sxs-lookup"><span data-stu-id="5babd-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="5babd-113">Bunun yerine, türetilmiş bir sınıf, XAML için giriş kaynağının genel özellikleri ve kısıtlamaları dahil olmak üzere yükleme tekniğini bildirir ve uygular.</span><span class="sxs-lookup"><span data-stu-id="5babd-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="5babd-114">Örneğin, bir <xref:System.Xaml.XamlObjectReader>, kökü veya tabanı temsil eden tek bir nesnenin giriş kaynağından başlayarak bir nesne grafiğini okur.</span><span class="sxs-lookup"><span data-stu-id="5babd-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="5babd-115"><xref:System.Xaml.XamlObjectReader> ardından nesne grafiğinden bir XAML düğüm akışı üretir.</span><span class="sxs-lookup"><span data-stu-id="5babd-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="5babd-116">En belirgin .NET Framework XAML Hizmetleri – tanımlanmış <xref:System.Xaml.XamlReader> alt sınıfı <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="5babd-116">The most prominent .NET Framework XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="5babd-117"><xref:System.Xaml.XamlXmlReader> ilk XAML 'yi, doğrudan bir akış veya dosya yoluyla bir metin dosyası yükleyerek ya da <xref:System.IO.TextReader>gibi ilgili bir okuyucu sınıfı aracılığıyla dolaylı olarak yükler.</span><span class="sxs-lookup"><span data-stu-id="5babd-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="5babd-118"><xref:System.Xaml.XamlReader>, yüklendikten sonra XAML giriş kaynağının tamamını içeren olarak düşünülebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="5babd-119">Ancak <xref:System.Xaml.XamlReader> temel API, okuyucunun tek bir XAML düğümüyle etkileşimde bulunmak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="5babd-120">İlk yüklendiğinde, karşılaştığınız ilk tek düğüm XAML 'in ve başlangıç nesnesinin köküdür.</span><span class="sxs-lookup"><span data-stu-id="5babd-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="5babd-121">XAML düğüm akışı kavramı</span><span class="sxs-lookup"><span data-stu-id="5babd-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="5babd-122">XML tabanlı teknolojilerle ilgili bir DOM, ağaç metaphor veya sorgu tabanlı yaklaşım hakkında genel olarak daha fazla bilginiz varsa, XAML düğüm akışının kavramaya yönelik faydalı bir yolu aşağıdaki gibidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-122">If you are generally more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="5babd-123">Yüklenen XAML 'nin bir DOM veya her olası düğümün her şekilde genişletildiği bir ağaç olduğunu düşünün ve ardından ilk olarak sunulur.</span><span class="sxs-lookup"><span data-stu-id="5babd-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="5babd-124">Düğümlerde ilerlettikçe, bir DOM ile ilgili olacak düzeylerin "içinde" veya "Out" şeklinde geçiş yapabilirsiniz, ancak bu düzey kavramlar bir düğüm akışıyla ilgili olmadığından XAML düğüm akışı açıkça izlenmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="5babd-125">Düğüm akışı "geçerli" bir konuma sahiptir, ancak akışın diğer bölümlerini başvuru olarak depolamadığınız sürece, düğüm akışının geçerli düğüm konumu dışındaki her yönü görünüm dışında olur.</span><span class="sxs-lookup"><span data-stu-id="5babd-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="5babd-126">XAML düğüm akışı kavramı, tüm düğüm akışının tamamına giderseniz, tüm XAML gösterimini işlemenize emin olduğunuz için önemli bir avantajdır. bir sorgunun, DOM işleminin veya işleme bilgileri ile ilgili başka bir doğrusal yaklaşımın, tüm XAML gösteriminin bir parçasını kaçırmadığından endişelenmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="5babd-127">Bu nedenle, xaml düğüm akışı temsili hem XAML okuyucuları hem XAML yazıcılarını bağlamak için idealdir ve bir XAML işleme işleminin okuma ve yazma aşamaları arasında davranan kendi işleminizi ekleyebileceğiniz bir sistem sağlamak için idealdir.</span><span class="sxs-lookup"><span data-stu-id="5babd-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="5babd-128">Çoğu durumda, XAML düğüm akışındaki düğümlerin sıralaması, XAML okuyucular tarafından kasıtlı olarak iyileştirilir veya yeniden sıralanabilir ve bu da sıranın kaynak metin, ikili veya nesne grafiğinde nasıl görünebileceğini de gösterebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="5babd-129">Bu davranış, XAML yazıcılarının düğüm akışında "geri" gitmesi gereken bir konumda yer aldığı bir XAML işleme mimarisini zorlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="5babd-130">İdeal olarak, tüm XAML yazma işlemleri şema bağlamına ve düğüm akışının geçerli konumuna göre işlem yapabilmelidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

<a name="a_basic_reading_node_loop"></a>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="5babd-131">Temel bir okuma düğümü döngüsü</span><span class="sxs-lookup"><span data-stu-id="5babd-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="5babd-132">XAML düğüm akışını incelemek için temel bir okuma düğümü döngüsü aşağıdaki kavramlardan oluşur.</span><span class="sxs-lookup"><span data-stu-id="5babd-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="5babd-133">Bu konuda anlatıldığı gibi düğüm döngülerinin amaçları doğrultusunda, <xref:System.Xaml.XamlXmlReader>kullanarak metin tabanlı, okunabilir XAML dosyasını okudığınızı varsayalım.</span><span class="sxs-lookup"><span data-stu-id="5babd-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="5babd-134">Bu bölümdeki bağlantılar, <xref:System.Xaml.XamlXmlReader>tarafından uygulanan belirli XAML düğüm döngüsü API 'sine başvurur.</span><span class="sxs-lookup"><span data-stu-id="5babd-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="5babd-135">XAML düğüm akışının sonunda olmadığından emin olun (<xref:System.Xaml.XamlXmlReader.IsEof%2A>denetleyin veya <xref:System.Xaml.XamlXmlReader.Read%2A> dönüş değerini kullanın).</span><span class="sxs-lookup"><span data-stu-id="5babd-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="5babd-136">Akışın sonunda yer alıyorsa, geçerli bir düğüm yoktur ve çıkış yapmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="5babd-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="5babd-137"><xref:System.Xaml.XamlXmlReader.NodeType%2A>çağırarak XAML düğüm akışının ne tür düğüm tarafından kullanıma sunulduğunu denetleyin.</span><span class="sxs-lookup"><span data-stu-id="5babd-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="5babd-138">Doğrudan bağlanmış ilişkili bir XAML nesne yazıcınız varsa, genellikle bu noktada <xref:System.Xaml.XamlWriter.WriteNode%2A> çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="5babd-139">Geçerli düğüm veya geçerli kayıt olarak bildirilen <xref:System.Xaml.XamlNodeType> göre, düğüm içerikleri hakkında bilgi almak için aşağıdakilerden birini çağırın:</span><span class="sxs-lookup"><span data-stu-id="5babd-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

  - <span data-ttu-id="5babd-140"><xref:System.Xaml.XamlNodeType.StartMember> veya <xref:System.Xaml.XamlNodeType.EndMember><xref:System.Xaml.XamlXmlReader.NodeType%2A> için, bir üye hakkında <xref:System.Xaml.XamlMember> bilgileri almak üzere <xref:System.Xaml.XamlXmlReader.Member%2A> çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="5babd-141">Üyenin bir <xref:System.Xaml.XamlDirective>olabileceğini ve bu nedenle, önceki nesnenin geleneksel tür tanımlı bir üyesi olması gerekmediğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="5babd-141">Note that the member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="5babd-142">Örneğin, bir nesneye uygulanan `x:Name`, <xref:System.Xaml.XamlMember.IsDirective%2A> true olduğu ve üyenin <xref:System.Xaml.XamlMember.Name%2A>, bu yönergenin XAML Language XAML ad alanı altında olduğunu belirten diğer özelliklerle birlikte `Name`olduğu bir XAML üyesi olarak görünür.</span><span class="sxs-lookup"><span data-stu-id="5babd-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

  - <span data-ttu-id="5babd-143"><xref:System.Xaml.XamlNodeType.StartObject> veya <xref:System.Xaml.XamlNodeType.EndObject><xref:System.Xaml.XamlXmlReader.NodeType%2A> için, bir nesnesi hakkında <xref:System.Xaml.XamlType> bilgileri almak üzere <xref:System.Xaml.XamlXmlReader.Type%2A> çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

  - <span data-ttu-id="5babd-144"><xref:System.Xaml.XamlNodeType.Value><xref:System.Xaml.XamlXmlReader.NodeType%2A> için <xref:System.Xaml.XamlXmlReader.Value%2A>çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="5babd-145">Düğüm yalnızca bir üye için bir değerin en basit ifadesi veya bir nesnenin başlatma metni ise bir değerdir (ancak, bu konunun yakında yaklaşan bölümünde belgelenen tür dönüştürme davranışının farkında olmanız gerekir).</span><span class="sxs-lookup"><span data-stu-id="5babd-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

  - <span data-ttu-id="5babd-146"><xref:System.Xaml.XamlNodeType.NamespaceDeclaration><xref:System.Xaml.XamlXmlReader.NodeType%2A> için, bir ad alanı düğümü için ad alanı bilgilerini almak üzere <xref:System.Xaml.XamlXmlReader.Namespace%2A> çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="5babd-147">Xaml okuyucuyu XAML düğüm akışındaki sonraki düğüme ilerletmek için <xref:System.Xaml.XamlXmlReader.Read%2A> çağırın ve adımları tekrar tekrarlayın.</span><span class="sxs-lookup"><span data-stu-id="5babd-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="5babd-148">.NET Framework XAML Hizmetleri XAML okuyucuları tarafından sağlanan XAML düğüm akışı her zaman tüm olası düğümlerin tam ve derin bir çapraz geçişini sağlar.</span><span class="sxs-lookup"><span data-stu-id="5babd-148">The XAML node stream provided by .NET Framework XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="5babd-149">XAML düğüm döngüsü için tipik akış denetimi teknikleri, `while (reader.Read())`içinde bir gövde tanımlamayı ve düğüm döngüsündeki her bir düğüm noktasında <xref:System.Xaml.XamlXmlReader.NodeType%2A> değiştirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="5babd-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="5babd-150">Düğüm akışı dosyanın sonunda ise, geçerli düğüm null olur.</span><span class="sxs-lookup"><span data-stu-id="5babd-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="5babd-151">Bir okuyucu ve yazıcı kullanan en basit döngü aşağıdaki örneğe benzer.</span><span class="sxs-lookup"><span data-stu-id="5babd-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="5babd-152">Bir load path XAML düğümü döngüsünün bu temel örneği, XAML okuyucuyu ve XAML yazıcısını saydam şekilde bağlar ve <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>kullanmışsınız gibi başka hiçbir şey yapmaz.</span><span class="sxs-lookup"><span data-stu-id="5babd-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="5babd-153">Ancak bu temel yapı daha sonra okuma veya yazma senaryonuz için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="5babd-154">Olası bazı senaryolar aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="5babd-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="5babd-155"><xref:System.Xaml.XamlXmlReader.NodeType%2A>üzerinde geçiş yapın.</span><span class="sxs-lookup"><span data-stu-id="5babd-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="5babd-156">Hangi düğüm türünün okunmakta olduğuna bağlı olarak farklı eylemler gerçekleştirin.</span><span class="sxs-lookup"><span data-stu-id="5babd-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="5babd-157">Her durumda <xref:System.Xaml.XamlWriter.WriteNode%2A> çağırmayın.</span><span class="sxs-lookup"><span data-stu-id="5babd-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="5babd-158">Yalnızca bazı <xref:System.Xaml.XamlXmlReader.NodeType%2A> durumlarda <xref:System.Xaml.XamlWriter.WriteNode%2A> çağırın.</span><span class="sxs-lookup"><span data-stu-id="5babd-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="5babd-159">Belirli bir düğüm türünün mantığı içinde, bu düğümün özelliklerini çözümleyin ve üzerinde işlem yapın.</span><span class="sxs-lookup"><span data-stu-id="5babd-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="5babd-160">Örneğin, yalnızca belirli bir XAML ad alanından gelen nesneleri yazabilir ve ardından bu XAML ad alanından olmayan nesneleri bırakabilir veya erteleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-160">For example, you could write only objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="5babd-161">Ya da XAML sisteminizin üye işlemenin bir parçası olarak desteklemediği XAML yönergelerini bırakabilir veya başka bir şekilde yeniden işleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="5babd-162">Muhtemelen XAML şeması bağlamını atlayan tür eşlemesi gerçekleştirerek, `Write*` yöntemleri geçersiz kılan özel bir <xref:System.Xaml.XamlObjectWriter> tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="5babd-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="5babd-163">Yalnızca, XAML davranışında özelleştirilmiş farkların hem okuyucu hem de yazıcı tarafından kullanıldığı şekilde, varsayılan olarak kullanılan XAML şema bağlamını kullanmak için <xref:System.Xaml.XamlXmlReader> oluşturun.</span><span class="sxs-lookup"><span data-stu-id="5babd-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="5babd-164">Düğüm döngüsü kavramının ötesinde XAML 'e erişme</span><span class="sxs-lookup"><span data-stu-id="5babd-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="5babd-165">XAML düğüm döngüsü dışında bir XAML temsili ile çalışmanın diğer olası yolları vardır.</span><span class="sxs-lookup"><span data-stu-id="5babd-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="5babd-166">Örneğin, dizini oluşturulmuş bir düğümü okuyabilen bir XAML okuyucusu veya belirli erişim düğümlerine doğrudan `x:Name`, `x:Uid`veya diğer tanımlayıcılar aracılığıyla erişebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="5babd-167">.NET Framework XAML Hizmetleri tam uygulama sağlamaz, ancak hizmetler ve destek türleri aracılığıyla önerilen bir model sağlar.</span><span class="sxs-lookup"><span data-stu-id="5babd-167">.NET Framework XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="5babd-168">Daha fazla bilgi için bkz. <xref:System.Xaml.IXamlIndexingReader> ve <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="5babd-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

<a name="working_with_the_current_node"></a>

## <a name="working-with-the-current-node"></a><span data-ttu-id="5babd-169">Geçerli düğümle çalışma</span><span class="sxs-lookup"><span data-stu-id="5babd-169">Working with the Current Node</span></span>

<span data-ttu-id="5babd-170">XAML düğümü döngüsü kullanan çoğu senaryo yalnızca düğümleri okummaz.</span><span class="sxs-lookup"><span data-stu-id="5babd-170">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="5babd-171">Çoğu senaryo geçerli düğümleri işler ve her bir düğümü tek seferde bir <xref:System.Xaml.XamlWriter>uygulamasına iletir.</span><span class="sxs-lookup"><span data-stu-id="5babd-171">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="5babd-172">Tipik yükleme yolu senaryosunda, bir <xref:System.Xaml.XamlXmlReader> XAML düğüm akışı oluşturur; XAML düğümleri Logic ve XAML şema bağlamına göre işlenir; ve düğümleri bir <xref:System.Xaml.XamlObjectWriter>geçirilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-172">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="5babd-173">Daha sonra elde edilen nesne grafiğini uygulamanız veya çatısı ile tümleştirin.</span><span class="sxs-lookup"><span data-stu-id="5babd-173">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="5babd-174">Tipik bir kayıt yolu senaryosunda, bir <xref:System.Xaml.XamlObjectReader> nesne grafiğini okur, tek XAML düğümleri işlenir ve bir <xref:System.Xaml.XamlXmlWriter> serileştirilmiş sonucu bir XAML metin dosyası olarak verir.</span><span class="sxs-lookup"><span data-stu-id="5babd-174">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="5babd-175">Anahtar, hem yolların hem de senaryoların tek seferde tam olarak bir XAML düğümü ile çalışmasını içerir ve XAML düğümleri XAML türü sistem ve the.NET Framework XAML Hizmetleri API 'Leri tarafından tanımlanan standartlaştırılmış bir şekilde kullanıma sunulmuştur.</span><span class="sxs-lookup"><span data-stu-id="5babd-175">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET Framework XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="5babd-176">Çerçeveler ve kapsam</span><span class="sxs-lookup"><span data-stu-id="5babd-176">Frames and Scope</span></span>

<span data-ttu-id="5babd-177">XAML düğüm döngüsü, XAML düğüm akışını doğrusal bir şekilde gösterir.</span><span class="sxs-lookup"><span data-stu-id="5babd-177">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="5babd-178">Düğüm akışı nesnelere, diğer nesneleri içeren üyelere ve bu şekilde devam eder.</span><span class="sxs-lookup"><span data-stu-id="5babd-178">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="5babd-179">Bir çerçeve ve yığın kavramı uygulayarak XAML düğüm akışı içindeki kapsamı izlemek genellikle yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="5babd-179">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="5babd-180">Bu, özellikle düğüm akışını etkin olarak ayarlıyorsanız geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-180">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="5babd-181">Düğüm döngüsü mantığınızın bir parçası olarak uyguladığınız çerçeve ve yığın desteği, yapı bir DOM perspektifinden düşünülse de bir XAML düğüm yapısına son bakış halinde `StartObject` (veya `GetObject`) ve `EndObject` kapsamlarını saymanız olabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-181">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

<a name="traversing_and_entering_object_nodes"></a>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="5babd-182">Nesne düğümlerini geçme ve girme</span><span class="sxs-lookup"><span data-stu-id="5babd-182">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="5babd-183">Bir XAML okuyucusu tarafından açıldığında düğüm akışındaki ilk düğüm, kök nesnenin başlangıç nesnesi düğümüdür.</span><span class="sxs-lookup"><span data-stu-id="5babd-183">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="5babd-184">Tanım olarak, bu nesne her zaman tek bir nesne düğümüdür ve bir eşi yoktur.</span><span class="sxs-lookup"><span data-stu-id="5babd-184">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="5babd-185">Herhangi bir gerçek dünyada XAML örneğinde, kök nesnesi daha fazla nesne tutan bir veya daha fazla özelliğe sahip olacak şekilde tanımlanır ve bu özelliklerde üye düğümleri vardır.</span><span class="sxs-lookup"><span data-stu-id="5babd-185">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="5babd-186">Daha sonra üye düğümleri bir veya daha fazla nesne düğümüne sahiptir veya bunun yerine bir değer düğümünde da sonlandırılabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-186">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="5babd-187">Kök nesnesi genellikle xaml metin biçimlendirmesinde öznitelik olarak atanan, ancak XAML düğüm akışı gösteriminde bir `Namescope` node türüyle eşlenen XAML namescopes 'yi tanımlar.</span><span class="sxs-lookup"><span data-stu-id="5babd-187">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="5babd-188">Aşağıdaki XAML örneğini göz önünde bulundurun (.NET Framework, bu rastgele XAML 'dir, bu, var olan türler tarafından yedeklenmez).</span><span class="sxs-lookup"><span data-stu-id="5babd-188">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in the .NET Framework).</span></span> <span data-ttu-id="5babd-189">Bu nesne modelinde, `FavorCollection` `Favor``List<T>` `Balloon` ve `NoiseMaker` `Favor`atanabilir olduğunu varsayın, `Balloon.Color` özelliği WPF 'in bilinen renk adları olarak renkleri nasıl tanımladığına benzer bir `Color` nesne tarafından desteklenir ve `Color` öznitelik sözdizimi için tür dönüştürücüsünü destekler.</span><span class="sxs-lookup"><span data-stu-id="5babd-189">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="5babd-190">XAML işaretleme</span><span class="sxs-lookup"><span data-stu-id="5babd-190">XAML markup</span></span>|<span data-ttu-id="5babd-191">Sonuçta elde edilen XAML düğüm akışı</span><span class="sxs-lookup"><span data-stu-id="5babd-191">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="5babd-192">`Party` için `Namespace` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-192">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="5babd-193">`Party` için `StartObject` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-193">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="5babd-194">`Party.Favors` için `StartMember` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-194">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="5babd-195">örtük `FavorCollection` için `StartObject` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-195">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="5babd-196">örtük `FavorCollection` Items özelliği için `StartMember` düğümü.</span><span class="sxs-lookup"><span data-stu-id="5babd-196">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="5babd-197">`Balloon` için `StartObject` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-197">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="5babd-198">`Color` için `StartMember` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-198">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="5babd-199">öznitelik değeri dizesinin `Value` düğümü `"Red"`</span><span class="sxs-lookup"><span data-stu-id="5babd-199">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="5babd-200">`Color` için `EndMember`</span><span class="sxs-lookup"><span data-stu-id="5babd-200">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="5babd-201">`HasHelium` için `StartMember` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-201">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="5babd-202">öznitelik değeri dizesinin `Value` düğümü `"True"`</span><span class="sxs-lookup"><span data-stu-id="5babd-202">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="5babd-203">`HasHelium` için `EndMember`</span><span class="sxs-lookup"><span data-stu-id="5babd-203">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="5babd-204">`Balloon` için `EndObject`</span><span class="sxs-lookup"><span data-stu-id="5babd-204">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="5babd-205">`NoiseMaker` için `StartObject` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-205">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="5babd-206">`_Initialization` için `StartMember` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-206">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="5babd-207">başlatma değeri dizesi için düğüm `Value` `"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="5babd-207">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="5babd-208">`_Initialization` için `EndMember` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-208">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="5babd-209">`NoiseMaker` için `EndObject`</span><span class="sxs-lookup"><span data-stu-id="5babd-209">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="5babd-210">örtük `FavorCollection` Items özelliği için `EndMember` düğümü.</span><span class="sxs-lookup"><span data-stu-id="5babd-210">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="5babd-211">örtük `FavorCollection` için `EndObject` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-211">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="5babd-212">`Favors` için `EndMember`</span><span class="sxs-lookup"><span data-stu-id="5babd-212">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="5babd-213">`Party` için `EndObject`</span><span class="sxs-lookup"><span data-stu-id="5babd-213">`EndObject` for `Party`</span></span>|

<span data-ttu-id="5babd-214">XAML düğüm akışında aşağıdaki davranışa güvenebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="5babd-214">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="5babd-215">Bir `Namespace` düğümü varsa, bu, `xmlns`ile XAML ad alanını bildirdikleri `StartObject` hemen öncesine akışa eklenir.</span><span class="sxs-lookup"><span data-stu-id="5babd-215">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="5babd-216">XAML ve örnek düğüm akışının bulunduğu önceki tabloya bakın.</span><span class="sxs-lookup"><span data-stu-id="5babd-216">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="5babd-217">`StartObject` ve `Namespace` düğümlerinin, metin biçimlendirmesinde bildirim konumlarına karşı nasıl konumlandırdığı gibi göründüğünü fark edin.</span><span class="sxs-lookup"><span data-stu-id="5babd-217">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="5babd-218">Bu, ad alanı düğümlerinin düğüm akışında için uygulanan düğümden önce her zaman göründüğü davranış temsilcisidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-218">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="5babd-219">Bu tasarımın amacı, ad alanı bilgilerinin nesne yazarları için hayati öneme sahip olması ve nesne yazıcısının tür eşlemesi gerçekleştirmeyi denemesi ya da nesneyi işlemesini denemeden önce bilinmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="5babd-219">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="5babd-220">XAML ad alanı bilgilerini akıştaki uygulama kapsamının önüne koymak, her zaman düğüm akışının sunulma sırasında işlemesini daha kolay hale getirir.</span><span class="sxs-lookup"><span data-stu-id="5babd-220">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="5babd-221">Yukarıdaki durumlar nedeniyle, kökün `StartObject` değil, başlangıçtan itibaren düğümlerden geçiş yaparken en çok gerçek dünya biçimlendirme durumlarında ilk olarak okuduğunuzdan bir veya daha fazla `Namespace` düğümdür.</span><span class="sxs-lookup"><span data-stu-id="5babd-221">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="5babd-222">`StartObject` bir düğümün ardından `StartMember`, `Value`veya hemen bir `EndObject`gelebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-222">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="5babd-223">Artık başka bir `StartObject`tarafından hemen izlenmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-223">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="5babd-224">Bir `StartMember` `StartObject`, `Value`veya anında `EndMember`gelebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-224">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="5babd-225">Değerin, yeni bir değer örneklenebilecek bir `StartObject` yerine üst nesnenin mevcut bir değerinden gelmesi beklenen Üyeler için `GetObject`tarafından izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-225">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="5babd-226">Ayrıca, yaklaşan bir `StartObject`için geçerli olan bir `Namespace` düğümü de gelebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-226">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="5babd-227">Artık başka bir `StartMember`tarafından hemen izlenmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-227">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="5babd-228">`Value` düğüm değerin kendisini temsil eder; "EndValue" yok.</span><span class="sxs-lookup"><span data-stu-id="5babd-228">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="5babd-229">Bunun ardından yalnızca bir `EndMember`olabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-229">It can be followed only by an `EndMember`.</span></span>

  - <span data-ttu-id="5babd-230">Nesnenin XAML başlatma metni, oluşturma tarafından kullanılabilecek şekilde, nesne değeri yapısına neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="5babd-230">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="5babd-231">Bunun yerine, `_Initialization` adlı üye için ayrılmış bir üye düğümü oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="5babd-231">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="5babd-232">ve bu üye düğümü başlatma değeri dizesini içerir.</span><span class="sxs-lookup"><span data-stu-id="5babd-232">and that member node contains the initialization value string.</span></span> <span data-ttu-id="5babd-233">Varsa, `_Initialization` her zaman ilk `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="5babd-233">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="5babd-234">`_Initialization`, XAML dili XAML namescope ile bazı XAML Hizmetleri temsillerine uygun olabilir, bu da `_Initialization`, yedekleme türlerinde tanımlı bir özellik değildir.</span><span class="sxs-lookup"><span data-stu-id="5babd-234">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

  - <span data-ttu-id="5babd-235">Üye-değer birleşimi değerin bir öznitelik ayarını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="5babd-235">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="5babd-236">Sonuç olarak bu değeri işlemeye dahil bir değer Dönüştürücüsü olabilir ve değer düz bir dizedir.</span><span class="sxs-lookup"><span data-stu-id="5babd-236">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="5babd-237">Ancak, bir XAML nesne yazıcısı bu düğüm akışını işleene kadar değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-237">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="5babd-238">XAML nesne yazarı, gerekli XAML şeması bağlamı, tür sistemi eşlemesi ve değer dönüştürmeleri için gereken diğer desteğe sahiptir.</span><span class="sxs-lookup"><span data-stu-id="5babd-238">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="5babd-239">Bir `EndMember` düğümü, sonraki bir üye için bir `StartMember` düğümü veya üye sahibi için bir `EndObject` düğümü tarafından izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-239">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="5babd-240">Bir `EndObject` düğümü, `EndMember` bir düğüm tarafından izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-240">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="5babd-241">Ayrıca, nesnelerin bir koleksiyonun öğelerinde eşler olduğu durumlarda bir `StartObject` düğümü de görüntülenebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-241">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="5babd-242">Veya yaklaşan bir `StartObject`için geçerli olan bir `Namespace` düğümü gelebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-242">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

  - <span data-ttu-id="5babd-243">Tüm düğüm akışını kapatmakla ilgili benzersiz durumda, kök `EndObject` hiçbir şey tarafından izlenmez; okuyucu artık dosya sonu ' dir ve <xref:System.Xaml.XamlReader.Read%2A> `false`döndürür.</span><span class="sxs-lookup"><span data-stu-id="5babd-243">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

<a name="value_converters_and_the_xaml_node_stream"></a>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="5babd-244">Değer dönüştürücüler ve XAML düğüm akışı</span><span class="sxs-lookup"><span data-stu-id="5babd-244">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="5babd-245">Değer Dönüştürücüsü, bir işaretleme uzantısı için genel bir terimdir, bir tür Dönüştürücüsü (değer serileştiriciler dahil) veya XAML tür sistemi aracılığıyla bir değer Dönüştürücüsü olarak bildirilen başka bir ayrılmış sınıf.</span><span class="sxs-lookup"><span data-stu-id="5babd-245">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="5babd-246">XAML düğüm akışında, tür dönüştürücüsü kullanımı ve biçimlendirme uzantısı kullanımı çok farklı temsiller de vardır.</span><span class="sxs-lookup"><span data-stu-id="5babd-246">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="5babd-247">XAML düğüm akışında tür dönüştürücüler</span><span class="sxs-lookup"><span data-stu-id="5babd-247">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="5babd-248">Sonunda bir tür dönüştürücüsü kullanımıyla sonuçlanan bir öznitelik kümesi, XAML düğüm akışında bir üyenin değeri olarak raporlanır.</span><span class="sxs-lookup"><span data-stu-id="5babd-248">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="5babd-249">XAML düğüm akışı, bir tür dönüştürücü örnek nesnesi üretmeyi ve değeri geçirmeye çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="5babd-249">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="5babd-250">Tür dönüştürücünün dönüştürme uygulamasının kullanılması için XAML şema bağlamını çağırma ve tür eşleme için kullanılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="5babd-250">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="5babd-251">Değeri işlemek için hangi tür dönüştürücü sınıfının kullanılması gerektiğini belirlemek bile, XAML şeması bağlamını dolaylı olarak gerektirir.</span><span class="sxs-lookup"><span data-stu-id="5babd-251">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="5babd-252">Varsayılan XAML şema bağlamını kullandığınızda, bu bilgiler XAML tür sisteminde kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-252">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="5babd-253">XAML yazıcı bağlantısı yapmadan önce XAML düğüm akışı düzeyinde tür dönüştürücü sınıfı bilgisine ihtiyacınız varsa, bunu ayarlanan üyenin <xref:System.Xaml.XamlMember> bilgilerden elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-253">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="5babd-254">Ancak Aksi takdirde, tür dönüştürme sistemi ve XAML şeması bağlamı gerektiren işlemlerin geri kalanı, örneğin bir XAML nesne yazıcısı tarafından oluşturulması gibi bir değer olarak XAML düğüm akışında, tür dönüştürücüsü girişi korunmalıdır.</span><span class="sxs-lookup"><span data-stu-id="5babd-254">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="5babd-255">Örneğin, aşağıdaki sınıf tanımı ana hattını ve XAML kullanımını göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="5babd-255">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="5babd-256">Bu kullanım için XAML düğüm akışının metin gösterimi aşağıdakiler olarak ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="5babd-256">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="5babd-257">`GameBoard` temsil eden <xref:System.Xaml.XamlType> `StartObject`</span><span class="sxs-lookup"><span data-stu-id="5babd-257">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="5babd-258">`BoardSize` temsil eden <xref:System.Xaml.XamlMember> `StartMember`</span><span class="sxs-lookup"><span data-stu-id="5babd-258">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="5babd-259">"`8x8`" metin dizesiyle `Value` düğümü</span><span class="sxs-lookup"><span data-stu-id="5babd-259">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="5babd-260">`EndMember` eşleşiyor `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="5babd-260">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="5babd-261">`EndObject` eşleşiyor `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="5babd-261">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="5babd-262">Bu düğüm akışında tür dönüştürücüsü örneği olmadığına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="5babd-262">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="5babd-263">Ancak `BoardSize`için <xref:System.Xaml.XamlMember> <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> çağırarak tür dönüştürücüsü bilgilerini alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-263">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="5babd-264">Geçerli bir XAML şeması bağlamına sahipseniz, <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>bir örnek alarak dönüştürücü yöntemlerini de çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-264">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="5babd-265">XAML düğüm akışındaki biçimlendirme uzantıları</span><span class="sxs-lookup"><span data-stu-id="5babd-265">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="5babd-266">Biçimlendirme uzantısı kullanımı, XAML düğüm akışında, nesnenin bir biçimlendirme uzantısı örneğini temsil ettiği bir üye içindeki nesne düğümü olarak raporlanır.</span><span class="sxs-lookup"><span data-stu-id="5babd-266">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="5babd-267">Bu nedenle, bir biçimlendirme uzantısı kullanımı, düğüm akışı gösteriminde tür dönüştürücüsü kullanımının daha açık bir şekilde sunulur ve daha fazla bilgi taşır.</span><span class="sxs-lookup"><span data-stu-id="5babd-267">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="5babd-268"><xref:System.Xaml.XamlMember> bilgiler, kullanım durumsal olduğu ve olası her biçimlendirme durumunda değişiklik gösterdiği için biçimlendirme uzantısıyla ilgili hiçbir şey söylemedi; tür dönüştürücülerinde olduğu gibi tür veya üye başına adanmış ve örtük değildir.</span><span class="sxs-lookup"><span data-stu-id="5babd-268"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="5babd-269">Biçimlendirme uzantılarının nesne düğümleri olarak düğüm akışı temsili, XAML metin biçimlendirmesinde (genellikle büyük/küçük harf) biçimlendirme uzantısı kullanımı öznitelik biçiminde yapılmış olsa bile durumdur.</span><span class="sxs-lookup"><span data-stu-id="5babd-269">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="5babd-270">Açık nesne öğesi formu kullanan biçimlendirme uzantısı kullanımları aynı şekilde işlenir.</span><span class="sxs-lookup"><span data-stu-id="5babd-270">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="5babd-271">Biçimlendirme uzantısı nesne düğümü içinde, bu biçimlendirme uzantısının üyeleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-271">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="5babd-272">XAML düğüm akışı temsili, bu biçimlendirme uzantısının kullanımını, Konumsal parametre kullanımı veya açık adlandırılmış parametrelerle kullanım gibi korur.</span><span class="sxs-lookup"><span data-stu-id="5babd-272">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="5babd-273">Konumsal parametre kullanımı için XAML düğüm akışı, kullanımı kaydeden XAML dil tanımlı bir özellik `_PositionalParameters` içerir.</span><span class="sxs-lookup"><span data-stu-id="5babd-273">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="5babd-274">Bu özellik <xref:System.Object> kısıtlaması olan genel bir <xref:System.Collections.Generic.List%601>.</span><span class="sxs-lookup"><span data-stu-id="5babd-274">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="5babd-275">Çalıp a Konumsal parametre kullanımı, içinde iç içe biçimlendirme uzantısı kullanımları içerebildiğinden kısıtlama Object ve String değildir.</span><span class="sxs-lookup"><span data-stu-id="5babd-275">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="5babd-276">Kullanımdan konumsal parametrelere erişmek için, listede yineleyebilir ve tek liste değerleri için Dizin oluşturuculardan yararlanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-276">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="5babd-277">Adlandırılmış bir parametre kullanımı için, her adlandırılmış parametre, düğüm akışındaki bu adın üye düğümü olarak temsil edilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-277">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="5babd-278">İç içe geçmiş biçimlendirme uzantısı kullanımı olabileceğinden üye değerleri dize değildir.</span><span class="sxs-lookup"><span data-stu-id="5babd-278">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="5babd-279">biçimlendirme uzantısından `ProvideValue` henüz çağrılmamıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-279">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="5babd-280">Ancak, bir XAML okuyucuyu ve XAML yazıcısını bağladığınızda çağrılır. böylece, düğüm akışında inceleyebileceğiniz biçimlendirme uzantısı düğümünde `WriteEndObject` çağrılır.</span><span class="sxs-lookup"><span data-stu-id="5babd-280">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="5babd-281">Bu nedenle, genellikle yükleme yolundaki nesne grafiğini biçimlendirmek için kullanılan aynı XAML şeması bağlamına ihtiyacınız vardır.</span><span class="sxs-lookup"><span data-stu-id="5babd-281">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="5babd-282">Aksi takdirde, herhangi bir biçimlendirme uzantısının `ProvideValue`, beklenen hizmetleri kullanılabilir olmadığı için burada özel durumlar oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-282">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

<a name="xaml_and_xml_languagedefined_members_in_the_xaml_node_stream"></a>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="5babd-283">Xaml düğüm akışındaki XAML ve XML dil tanımlı Üyeler</span><span class="sxs-lookup"><span data-stu-id="5babd-283">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="5babd-284">Belirli Üyeler, açık bir <xref:System.Xaml.XamlMember> arama veya oluşturma yerine bir XAML okuyucusunun yorumlamalar ve kuralları nedeniyle XAML düğüm akışına tanıtılmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-284">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="5babd-285">Genellikle, bu Üyeler XAML yönergelerinden kaynaklanır.</span><span class="sxs-lookup"><span data-stu-id="5babd-285">Often, these members are XAML directives.</span></span> <span data-ttu-id="5babd-286">Bazı durumlarda, XAML düğüm akışına yönergesini tanıtan XAML okuma işlemidir.</span><span class="sxs-lookup"><span data-stu-id="5babd-286">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="5babd-287">Diğer bir deyişle, özgün giriş XAML metni üye yönergesini açıkça belirtmedi, ancak XAML okuyucu, bir yapısal XAML kuralını ve bu bilgiler kaybedilmeden önce XAML düğüm akışındaki rapor bilgilerini karşılamak için yönergesini ekler.</span><span class="sxs-lookup"><span data-stu-id="5babd-287">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="5babd-288">Aşağıdaki liste, bir XAML okuyucusunun bir yönerge XAML üye düğümü tanıtması beklenen ve bu üye düğümünün .NET Framework XAML Hizmetleri uygulamalarında nasıl belirlenebileceği tüm durumları not.</span><span class="sxs-lookup"><span data-stu-id="5babd-288">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in the .NET Framework XAML Services implementations.</span></span>

- <span data-ttu-id="5babd-289">**Nesne düğümü Için başlatma metni:** Bu üye düğümün adı `_Initialization`, bir XAML yönergesini temsil eder ve XAML Language XAML ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="5babd-289">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="5babd-290"><xref:System.Xaml.XamlLanguage.Initialization%2A>için bir statik varlık alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-290">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="5babd-291">**Biçimlendirme Uzantısı Için Konumsal Parametreler:** Bu üye düğümün adı `_PositionalParameters`ve XAML Language XAML ad alanında tanımlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-291">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="5babd-292">Her biri her biri, giriş XAML 'de sağlanan `,` sınırlayıcı karakter üzerinde bölünerek önceden ayrılmış konumsal bir parametre olan bir konum parametresi olan genel bir nesne listesi içerir.</span><span class="sxs-lookup"><span data-stu-id="5babd-292">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="5babd-293"><xref:System.Xaml.XamlLanguage.PositionalParameters%2A>konumsal Parameters yönergesine yönelik statik bir varlık alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-293">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="5babd-294">**Bilinmeyen içerik:** Bu üye düğümün adı `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="5babd-294">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="5babd-295">Kesinlikle konuşuyor, bir <xref:System.Xaml.XamlDirective>ve XAML Language XAML ad alanında tanımlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-295">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="5babd-296">Bu yönerge, bir XAML nesne öğesinin kaynak XAML 'de içerik içerdiği ancak şu anda kullanılabilir olan XAML şema bağlamı altında hiçbir içerik özelliği belirlenebileceği durumlar için Sentinel olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="5babd-296">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="5babd-297">`_UnknownContent`adlı üyeleri denetleyerek bu durumu bir XAML düğüm akışında tespit edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-297">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="5babd-298">Bir yükleme yolu XAML düğüm akışında başka bir işlem alınıyorsa, varsayılan <xref:System.Xaml.XamlObjectWriter> herhangi bir nesne üzerinde `_UnknownContent` üyesiyle karşılaştığında `WriteEndObject` yapılmaya çalışıldı.</span><span class="sxs-lookup"><span data-stu-id="5babd-298">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="5babd-299">Varsayılan <xref:System.Xaml.XamlXmlWriter> oluşturmaz ve üyeyi örtük olarak kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="5babd-299">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="5babd-300"><xref:System.Xaml.XamlLanguage.UnknownContent%2A>`_UnknownContent` için statik bir varlık alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="5babd-300">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="5babd-301">**Bir koleksiyonun Collection özelliği:** XAML için kullanılan bir koleksiyon sınıfının yedekleme CLR türünün genellikle koleksiyon öğelerini tutan ayrılmış bir adlandırılmış özelliği olsa da, bu özellik tür çözümlemesini yedeklemeden önce bir XAML tür sistemi olarak bilinir.</span><span class="sxs-lookup"><span data-stu-id="5babd-301">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="5babd-302">Bunun yerine, XAML düğüm akışı, koleksiyon XAML türünün bir üyesi olarak bir `Items` yer tutucusu tanıtır.</span><span class="sxs-lookup"><span data-stu-id="5babd-302">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="5babd-303">.NET Framework XAML Hizmetleri uygulamasında, düğüm akışındaki bu yönergenin/üyenin adı `_Items`.</span><span class="sxs-lookup"><span data-stu-id="5babd-303">In the .NET Framework XAML Services implementation the name of this directive / member in the node stream is `_Items`.</span></span> <span data-ttu-id="5babd-304">Bu yönerge için bir sabit <xref:System.Xaml.XamlLanguage.Items%2A>adresinden elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-304">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="5babd-305">XAML düğüm akışı, yedekleme türü çözümleme ve XAML şema bağlamı temelinde ayrıştırılamayan bir öğe özelliği içerebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-305">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="5babd-306">Örneğin,</span><span class="sxs-lookup"><span data-stu-id="5babd-306">For example,</span></span>

- <span data-ttu-id="5babd-307">**XML tanımlı Üyeler:** XML tanımlı `xml:base`, `xml:lang` ve `xml:space` üyeleri, `lang`XAML Hizmetleri uygulamalarında `base`, `space` ve .NET Framework adlı XAML yönergeleri olarak raporlanır.</span><span class="sxs-lookup"><span data-stu-id="5babd-307">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in the .NET Framework XAML Services implementations.</span></span> <span data-ttu-id="5babd-308">Bu ad alanı, `http://www.w3.org/XML/1998/namespace`XML ad alanıdır.</span><span class="sxs-lookup"><span data-stu-id="5babd-308">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="5babd-309">Bunların her biri için sabitler <xref:System.Xaml.XamlLanguage>elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="5babd-309">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="5babd-310">Düğüm sırası</span><span class="sxs-lookup"><span data-stu-id="5babd-310">Node Order</span></span>

<span data-ttu-id="5babd-311">Bazı durumlarda <xref:System.Xaml.XamlXmlReader> XAML düğüm akışındaki XAML düğümlerinin sırasını, İşaretlemede görüntüleniyorsa veya XML olarak işlenirse, düğümlerin görünmesini ve bu şekilde değişdikleri sıraya göre değiştirir.</span><span class="sxs-lookup"><span data-stu-id="5babd-311">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="5babd-312">Bu, düğümleri bir <xref:System.Xaml.XamlObjectWriter> düğüm akışını yalnızca ileri bir şekilde işleyebileceği şekilde sıralamak için yapılır.</span><span class="sxs-lookup"><span data-stu-id="5babd-312">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="5babd-313">.NET Framework XAML hizmetlerinde XAML okuyucusu, düğüm akışının XAML nesne yazıcısı tüketicileri için performans iyileştirmesi olarak bu görevi XAML yazıcısına bırakmak yerine düğümleri yeniden sıralar.</span><span class="sxs-lookup"><span data-stu-id="5babd-313">In .NET Framework XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="5babd-314">Belirli yönergeler özellikle bir nesne öğesinden bir nesne oluşturulmasına yönelik daha fazla bilgi sağlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="5babd-314">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="5babd-315">Bu yönergeler şunlardır: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span><span class="sxs-lookup"><span data-stu-id="5babd-315">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="5babd-316">.NET Framework XAML Hizmetleri XAML okuyucuları, sonraki bölümde açıklanan nedenlerden dolayı, bu yönergeleri nesnenin `StartObject`takip eden düğüm akışına ilk üye olarak yerleştirmeyi dener.</span><span class="sxs-lookup"><span data-stu-id="5babd-316">The .NET Framework XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="5babd-317">XamlObjectWriter davranışı ve düğüm sırası</span><span class="sxs-lookup"><span data-stu-id="5babd-317">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="5babd-318">bir <xref:System.Xaml.XamlObjectWriter> `StartObject`, nesne örneğini hemen oluşturmak için XAML nesne yazıcısında bir sinyal değildir.</span><span class="sxs-lookup"><span data-stu-id="5babd-318">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="5babd-319">XAML, ek girişi olan bir nesneyi başlatmayı mümkün kılan ve tamamen ilk nesneyi oluşturmak için parametresiz bir Oluşturucu çağırma ve yalnızca özellikleri ayarlama gibi birçok dil özelliği içerir.</span><span class="sxs-lookup"><span data-stu-id="5babd-319">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a parameterless constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="5babd-320">Bu özellikler şunları içerir: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; başlatma metni; [X:TypeArguments](x-typearguments-directive.md); biçimlendirme uzantısının konumsal parametreleri; Factory yöntemleri ve ilişkili [X:Arguments](x-arguments-directive.md) DÜĞÜMLERI (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="5babd-320">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](x-typearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](x-arguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="5babd-321">Bu durumların her biri gerçek nesne oluşumunu geciktirecek ve düğüm akışı yeniden sınıflandırıldığı için XAML nesne yazıcısı, özellikle bir oluşturma olmayan bir başlangıç üyesine rastlana her seferinde örneği oluşturan bir davranışa güvenebilirler Bu nesne türü için yönerge.</span><span class="sxs-lookup"><span data-stu-id="5babd-321">Each of these cases delay the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="5babd-322">GetObject</span><span class="sxs-lookup"><span data-stu-id="5babd-322">GetObject</span></span>

<span data-ttu-id="5babd-323">`GetObject`, yeni bir nesne oluşturmak yerine bir xaml düğümünü temsil eder, bunun yerine nesnenin içeren özelliğin değerini alır.</span><span class="sxs-lookup"><span data-stu-id="5babd-323">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="5babd-324">Bir XAML düğüm akışında bir `GetObject` düğümünün karşılaştığı tipik bir durum, bir koleksiyon nesnesi veya sözlük nesnesi içindir; Bu özellik, kendisini yedekleme türünün nesne modelinde kasıtlı olarak salt okunurdur.</span><span class="sxs-lookup"><span data-stu-id="5babd-324">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="5babd-325">Bu senaryoda, koleksiyon veya sözlük genellikle sahip olan bir türün başlatma mantığı tarafından oluşturulur ve başlatılır (genellikle boştur).</span><span class="sxs-lookup"><span data-stu-id="5babd-325">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="5babd-326">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="5babd-326">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="5babd-327">XAML Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="5babd-327">XAML Services</span></span>](index.md)
- [<span data-ttu-id="5babd-328">XAML Ad Alanları</span><span class="sxs-lookup"><span data-stu-id="5babd-328">XAML Namespaces</span></span>](xaml-namespaces-for-net-framework-xaml-services.md)
