---
title: XAML Düğüm Akış Yapılarını ve Kavramlarını Anlama
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: babf98b7dd30cd60e72e310ae8ba8c9a42d9125f
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/18/2019
ms.locfileid: "58824435"
---
# <a name="understanding-xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="f5aa4-102">XAML Düğüm Akış Yapılarını ve Kavramlarını Anlama</span><span class="sxs-lookup"><span data-stu-id="f5aa4-102">Understanding XAML Node Stream Structures and Concepts</span></span>

<span data-ttu-id="f5aa4-103">XAML okuyucular ve .NET Framework XAML hizmetlerinde uygulandığı şekilde XAML yazarları XAML düğümü akışı tasarım kavramını temel alır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-103">XAML readers and XAML writers as implemented in .NET Framework XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="f5aa4-104">XAML düğümü akışı XAML düğüm kümesinin bir kavramsallaştırılması ' dir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="f5aa4-105">Bu kavramsallaştırılması XAML işlemci XAML düğümü ilişkileri yapısını teker teker kılavuzluk eder.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="f5aa4-106">Herhangi bir zamanda yalnızca tek bir geçerli kayıt ya da geçerli konum bir açık XAML düğümü akışı var ve yalnızca bilgileri kullanılabilir o konumdan API birçok yönden rapor.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="f5aa4-107">XAML düğümü akışı geçerli düğüm, bir nesne, bir üyesi veya bir değer olacak şekilde açıklanabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="f5aa4-108">XAML düğümü akışı XAML düşünerek XAML okuyucular XAML yazıcıları ile iletişim kurmak ve görüntülemek, etkileşim veya bir yükleme yolu veya kaydetme sırasında bir XAML düğüm akış içeriğini değiştirmek bir programı etkinleştirmek XAML içeren yolu işlemi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="f5aa4-109">XAML okuyucu ve yazıcı API tasarımı ve XAML düğüm akış kavram benzerdir önceki ilgili okuyucu ve yazıcı tasarımları ve kavramlarını gibi [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] ve <xref:System.Xml.XmlReader> ve <xref:System.Xml.XmlWriter> sınıfları.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="f5aa4-110">Bu konu, XAML düğüm akış kavramları açıklar ve XAML gösterimleri XAML düğüm düzeyinde etkileşim yordamlarını nasıl yazabileceğiniz açıklar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

<a name="loading_into_a_xaml_reader"></a>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="f5aa4-111">XAML XAML okuyucuya yükleniyor</span><span class="sxs-lookup"><span data-stu-id="f5aa4-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="f5aa4-112">Temel <xref:System.Xaml.XamlReader> sınıfı bir XAML okuyucuya ilk XAML yüklemek için belirli bir teknik bildirmiyor.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="f5aa4-113">Bunun yerine, türetilmiş bir sınıf bildirir ve yükleme teknik genel özelliklerini ve giriş kaynağına kısıtlamaları için XAML dahil olmak üzere, uygular.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="f5aa4-114">Örneğin, bir <xref:System.Xaml.XamlObjectReader> kök veya temel temsil eden tek bir nesne ise giriş kaynağından başlayan bir nesne grafiğinin okur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="f5aa4-115"><xref:System.Xaml.XamlObjectReader> Ardından XAML düğümü akıştan Nesne grafiği oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="f5aa4-116">En önemli .NET Framework XAML hizmetlerinde tanımlı <xref:System.Xaml.XamlReader> sınıfıdır <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-116">The most prominent .NET Framework XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="f5aa4-117"><xref:System.Xaml.XamlXmlReader> ilk XAML, bir metin dosyası bir akış veya dosya yolu üzerinden doğrudan veya dolaylı olarak ilgili okuyucusu sınıf aracılığıyla gibi yükleniyor ya da yükler <xref:System.IO.TextReader>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="f5aa4-118"><xref:System.Xaml.XamlReader> Yüklendikten sonra tamamen XAML giriş kaynağının içerecek şekilde zorlayıcı olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="f5aa4-119">Ancak, <xref:System.Xaml.XamlReader> temel API okuyucu XAML tek bir düğüm ile etkileşimde olacak şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="f5aa4-120">İlk yüklendiğinde, karşılaştığınız ilk düğümü XAML ve kendi başlangıç nesnesi köküdür.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="f5aa4-121">XAML düğümü Stream kavramı</span><span class="sxs-lookup"><span data-stu-id="f5aa4-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="f5aa4-122">Genellikle daha tanıdık bir DOM, ağaç benzetimini veya XML tabanlı teknolojiler erişme doğrultusunda sorgu temelli yaklaşım ile, XAML düğümü akışı kavramsallaştırmanın kullanışlı bir yol aşağıdaki gibidir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-122">If you are generally more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="f5aa4-123">Yüklenen XAML bir DOM ya da burada olası her düğüm tüm genişletilmiş ve doğrusal olarak sunulan bir ağaç olduğunu hayal edin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="f5aa4-124">Düğümleri aracılığıyla geliştikçe, "içinde" veya "dışarı" için bir DOM ilgilendirebilecek düzeylerinde geçiş, ancak bu düzey kavramlarla ilgili bir düğüm akışa olmadığından XAML düğümü akışı izleme açıkça korumaz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="f5aa4-125">Düğümü akışı "geçerli" konuma var, ancak kendiniz akış diğer bölümlerini başvuru olarak depoladığınız sürece düğümü akışı geçerli düğüm konumu dışında her yönüyle dışında görüntüle.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="f5aa4-126">XAML düğüm akış kavramı tüm düğüm akışı giderseniz, tüm XAML gösterimi işlenen garanti önemli avantajı vardır; bir sorgu, bir DOM işlemi veya bazı diğer doğrusal yaklaşım işleme bilgiler için tam XAML gösterimi kısmı eksik olduğunu endişelenmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="f5aa4-127">Bu nedenle, XAML düğümü akışı temsili bir XAML işleme işlemi okuma ve yazma aşamalar arasında davranan kendi işlemi burada ekleyebilirsiniz hem XAML okuyucular ve yazıcılar XAML bağlanma ve bir sistem sağlamak için idealdir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="f5aa4-128">Çoğu durumda, kasıtlı olarak en iyi duruma getirilmiş veya sırasını kaynak metni, ikili veya nesne grafiğinin nasıl görünebilir ve XAML okuyucular tarafından yeniden düğümleri XAML düğüm akış sıralama.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="f5aa4-129">Bu davranışı ile XAML yazıcılarının, hiçbir zaman, "geri" düğümü stream'de dönmek için sahip olduğu bir konumda olduğundan bir XAML işleme mimarisi zorlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="f5aa4-130">İdeal olarak, tüm XAML yazma işlemlerini yapmasını mümkün şema içeriği yanı sıra düğümü akışı geçerli konumunu de bağlı olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

<a name="a_basic_reading_node_loop"></a>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="f5aa4-131">Bir temel okuma düğüm döngüsü</span><span class="sxs-lookup"><span data-stu-id="f5aa4-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="f5aa4-132">Temel bir okuma düğüm döngü XAML düğümü akışı incelemek için aşağıdaki kavramları oluşur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="f5aa4-133">Döngüler, bu konuda açıklandığı gibi varsayar metin tabanlı, okunabilir XAML okuma düğüm amacıyla dosya kullanarak <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="f5aa4-134">Bu bölümdeki bağlantılar belirli XAML düğümü döngüsü tarafından uygulanan API bakın <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="f5aa4-135">XAML düğümü akışı sonunda olmadığından emin olun (denetleyin <xref:System.Xaml.XamlXmlReader.IsEof%2A>, veya <xref:System.Xaml.XamlXmlReader.Read%2A> dönüş değeri).</span><span class="sxs-lookup"><span data-stu-id="f5aa4-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="f5aa4-136">Akışın sonunda ise hiçbir geçerli düğüm ve çıkılması gerekiyor.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="f5aa4-137">Ne tür bir düğüm çağırarak XAML düğüm akış şu anda sunar. kontrol <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="f5aa4-138">Doğrudan bağlı bir ilişkili XAML nesne yazıcısı varsa, genellikle arama <xref:System.Xaml.XamlWriter.WriteNode%2A> bu noktada.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="f5aa4-139">Temel <xref:System.Xaml.XamlNodeType> geçerli düğüm veya geçerli kayıt çağrısı olarak düğüm içeriği hakkında bilgi edinmek için aşağıdakilerden birini bildirilir:</span><span class="sxs-lookup"><span data-stu-id="f5aa4-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

    - <span data-ttu-id="f5aa4-140">İçin bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> , <xref:System.Xaml.XamlNodeType.StartMember> veya <xref:System.Xaml.XamlNodeType.EndMember>, çağrı <xref:System.Xaml.XamlXmlReader.Member%2A> edinme <xref:System.Xaml.XamlMember> üyesi hakkında bilgi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="f5aa4-141">Üye olabilecek bir not bir <xref:System.Xaml.XamlDirective>, ve bu nedenle yukarıdaki nesne türü tanımlı geleneksel üyesi mutlaka olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-141">Note that the member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="f5aa4-142">Örneğin, `x:Name` uygulanan bir nesneyi bir XAML üyesi olarak görünür burada <xref:System.Xaml.XamlMember.IsDirective%2A> geçerlidir ve <xref:System.Xaml.XamlMember.Name%2A> üyesidir `Name`, bu yönerge, XAML dili XAML ad alanı altında olduğunu gösteren diğer özelliklere sahip.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

    - <span data-ttu-id="f5aa4-143">İçin bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> , <xref:System.Xaml.XamlNodeType.StartObject> veya <xref:System.Xaml.XamlNodeType.EndObject>, çağrı <xref:System.Xaml.XamlXmlReader.Type%2A> edinme <xref:System.Xaml.XamlType> bir nesneyle ilgili bilgileri.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

    - <span data-ttu-id="f5aa4-144">İçin bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> , <xref:System.Xaml.XamlNodeType.Value>, çağrı <xref:System.Xaml.XamlXmlReader.Value%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="f5aa4-145">Yalnızca basit ifadesi bir değerin bir üyesi veya başlatma metni (ancak, bu konunun ilerideki bölümde açıklandığı gibi tür dönüştürme davranışı bilmeniz) bir nesne için ise bir düğüm bir değerdir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

    - <span data-ttu-id="f5aa4-146">İçin bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> , <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, çağrı <xref:System.Xaml.XamlXmlReader.Namespace%2A> bir ad alanı düğümü için ad alanı bilgileri elde etmek için.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="f5aa4-147">Çağrı <xref:System.Xaml.XamlXmlReader.Read%2A> XAML okuyucu XAML düğüm akış sonraki düğümü geçin ve yeniden adımları tekrarlayın.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="f5aa4-148">.NET Framework XAML Hizmetleri XAML okuyucular tarafından her zaman sağlanan XAML düğümü akışı, tüm olası düğümlerinin tam ve kapsamlı bir geçişi sağlar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-148">The XAML node stream provided by .NET Framework XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="f5aa4-149">XAML düğümü Döngü için tipik bir akış denetimi teknikler kullanılabilir gövdesi içinde tanımlama `while (reader.Read())`ve geçme konusunda <xref:System.Xaml.XamlXmlReader.NodeType%2A> her düğümde düğüm döngüde gelin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="f5aa4-150">Düğümü akışı dosya sonunda ise, geçerli düğüm null olur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="f5aa4-151">Okuyucu ve yazıcı kullanan basit döngüsünü aşağıdaki örneğe benzer.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="f5aa4-152">Bu temel örnek, bir yükleme yolu XAML düğüm döngünün XAML okuyucu ve yazıcı XAML şeffaf bir şekilde bağlanır, eğer farklı bir şey yapmak, kullanmışsınız <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="f5aa4-153">Ancak, bu temel yapı okuma veya yazma senaryoyu uygulamak için genişletilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="f5aa4-154">Bazı olası senaryolar aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="f5aa4-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="f5aa4-155">Anahtarı'nda <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="f5aa4-156">Bağlı olarak hangi düğümün türü okunan farklı eylemleri gerçekleştirin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="f5aa4-157">Çağırmayın <xref:System.Xaml.XamlWriter.WriteNode%2A> tüm durumlarda.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="f5aa4-158">Yalnızca çağrı <xref:System.Xaml.XamlWriter.WriteNode%2A> bazı <xref:System.Xaml.XamlXmlReader.NodeType%2A> durumları.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="f5aa4-159">Mantık belirli düğüm türü için içinde o düğümün ayrıntılarını analiz edin ve bunlar üzerinde harekete.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="f5aa4-160">Örneğin, belirli bir XAML ad alanından gelen ve ardından bırakın ya da herhangi bir nesne değil, XAML ad alanından erteleme nesneleri yazabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-160">For example, you could write only objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="f5aa4-161">Veya bırakın veya aksi halde XAML sisteminizi desteklemediği bir XAML yönergeleri işleme üyelik bir parçası olarak yeniden işleme.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="f5aa4-162">Özel tanımlama <xref:System.Xaml.XamlObjectWriter> , geçersiz kılmalar `Write*` yöntemleri, büyük olasılıkla XAML şema içeriği atlar tür eşlemesi gerçekleştirme.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="f5aa4-163">Oluşturmak <xref:System.Xaml.XamlXmlReader> özelleştirilmiş XAML davranış farklılıkları hem okuyucu ve yazıcı tarafından kullanılan varsayılan XAML şema içeriği kullanılacak.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="f5aa4-164">XAML düğümü Döngü kavramı ötesinde erişme</span><span class="sxs-lookup"><span data-stu-id="f5aa4-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="f5aa4-165">XAML düğümü döngü olarak dışındaki bir XAML gösterimi ile çalışmak için büyük olasılıkla farklı yöntemleri vardır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="f5aa4-166">Örneğin, var dizinli bir düğüm okuyabilir veya özellikle erişen düğümleri ile doğrudan bir XAML okuyucu `x:Name`tarafından `x:Uid`, ya da diğer tanımlayıcılarla aracılığıyla.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="f5aa4-167">.NET framework XAML hizmetlerinde tam bir uygulamayı sağlamaz, ancak önerilen bir modeli Hizmetleri ve Destek türleri aracılığıyla sağlar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-167">.NET Framework XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="f5aa4-168">Daha fazla bilgi için bkz. <xref:System.Xaml.IXamlIndexingReader> ve <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

> [!TIP]
> <span data-ttu-id="f5aa4-169">Microsoft ayrıca Microsoft XAML Araç Seti bilinen bir bant dışı yayın oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-169">Microsoft also produces an out-of-band release known as the Microsoft XAML Toolkit.</span></span> <span data-ttu-id="f5aa4-170">Yayın öncesi aşamalı olarak yine de bu bant dışı sürümdür.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-170">This out-of-band release is still in its pre-release stages.</span></span> <span data-ttu-id="f5aa4-171">Ancak, yayın öncesi bileşenleri ile çalışmak istekliyse Microsoft XAML Araç Seti bazı ilginç kaynaklar XAML araçları ve XAML statik analizini sağlar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-171">However, if you are willing to work with pre-release components, the Microsoft XAML Toolkit provides some interesting resources for XAML tooling and static analysis of XAML.</span></span> <span data-ttu-id="f5aa4-172">Bir XAML DOM API'yi Microsoft XAML Araç Seti içeren, FxCop analizi ve XAML şema içeriği için Silverlight için destek.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-172">The Microsoft XAML Toolkit includes a XAML DOM API, support for FxCop analysis, and a XAML schema context for Silverlight.</span></span> <span data-ttu-id="f5aa4-173">Daha fazla bilgi için [Microsoft XAML Araç Seti](https://code.msdn.microsoft.com/XAML).</span><span class="sxs-lookup"><span data-stu-id="f5aa4-173">For more information, see [Microsoft XAML Toolkit](https://code.msdn.microsoft.com/XAML).</span></span>

<a name="working_with_the_current_node"></a>

## <a name="working-with-the-current-node"></a><span data-ttu-id="f5aa4-174">Geçerli düğüm ile çalışma</span><span class="sxs-lookup"><span data-stu-id="f5aa4-174">Working with the Current Node</span></span>

<span data-ttu-id="f5aa4-175">XAML düğümü Döngü kullanan Çoğu senaryoda, yalnızca düğümlerin okunmaz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-175">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="f5aa4-176">Çoğu senaryoda geçerli düğümleri işlemek ve her düğüm uygulaması için teker teker geçirmek <xref:System.Xaml.XamlWriter>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-176">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="f5aa4-177">Tipik bir yükleme yolu senaryosunda bir <xref:System.Xaml.XamlXmlReader> bir XAML düğüm akış üretir; XAML düğümleri, mantıksal ve XAML şema içeriği; göre işlenir ve düğümleri geçirilen bir <xref:System.Xaml.XamlObjectWriter>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-177">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="f5aa4-178">Uygulamanızı veya framework sonra elde edilen Nesne grafiği tümleştirin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-178">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="f5aa4-179">Tipik bir kaydetme yolu senaryosu, bir <xref:System.Xaml.XamlObjectReader> Nesne grafiğini okur, tek tek XAML düğümleri işlenir ve bir <xref:System.Xaml.XamlXmlWriter> XAML metin dosyası olarak seri hale getirilmiş sonuç verir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-179">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="f5aa4-180">Yolları hem senaryoları ile tam olarak bir XAML düğümü aynı anda ve XAML düğümleri XAML tür sistemi ve.NET Framework XAML Hizmetleri API tarafından tanımlanan standartlaştırılmış bir şekilde işlenmesi için kullanılabilir çalışma içeren anahtardır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-180">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET Framework XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="f5aa4-181">Çerçeve ve kapsamı</span><span class="sxs-lookup"><span data-stu-id="f5aa4-181">Frames and Scope</span></span>

<span data-ttu-id="f5aa4-182">XAML düğümü Döngü doğrusal bir şekilde XAML düğümü akışı gösterilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-182">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="f5aa4-183">Düğümü akışı nesneleri, diğer nesneleri kapsamak ve benzeri üye olarak erişir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-183">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="f5aa4-184">Genellikle, XAML düğümü akışı kapsamında bir çerçeve ve yığın kavramı uygulayarak izlemek yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-184">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="f5aa4-185">İçinde çalışırken düğümü akışı ayarlama etkin, bu özellikle doğrudur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-185">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="f5aa4-186">Çerçeve ve yığın düğümü Döngü mantığınızın bir parçası sayısı gibi uygulama destek `StartObject` (veya `GetObject`) ve `EndObject` kapsamları yapısı bir DOM açısından zorlayıcı, XAML düğümü yapısına düzen gibi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-186">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

<a name="traversing_and_entering_object_nodes"></a>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="f5aa4-187">Geçiş yapma ve nesne düğümleri girme</span><span class="sxs-lookup"><span data-stu-id="f5aa4-187">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="f5aa4-188">XAML okuyucu tarafından açıldığında bir düğümü akışı ilk düğümü kök nesnesinin Başlangıç nesnesi düğümüdür.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-188">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="f5aa4-189">Tanımına göre bu nesne bir tek bir nesne düğümü her zaman olduğu ve hiç eşlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-189">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="f5aa4-190">Herhangi bir gerçek XAML örnek kök nesne, daha fazla nesne içeren bir veya daha fazla özelliklerine sahip olmayı tanımlanır ve üye düğümleri bu özelliklere sahip.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-190">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="f5aa4-191">Üye düğümlerinin düğümleri için bir veya daha fazla nesne veya bir değer düğüm bunun yerine aynı zamanda sonlandırabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-191">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="f5aa4-192">Kök nesnenin, genellikle XAML metin biçimlendirme öznitelikleri olarak sözdizimsel olarak atanmış, ancak eşlemek, XAML ad kapsamları tanımlar bir `Namescope` XAML düğüm akış gösteriminde düğüm türü.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-192">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="f5aa4-193">(Bu, .NET Framework'teki varolan türleri tarafından yedeklenmez rastgele XAML) aşağıdaki XAML örneği göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-193">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in the .NET Framework).</span></span> <span data-ttu-id="f5aa4-194">Bu nesne modelinde varsayımında `FavorCollection` olduğu `List<T>` , `Favor`, `Balloon` ve `NoiseMaker` öğesine atanamaz olduğundan `Favor`, `Balloon.Color` özelliği destekli bir `Color` WPF nasıl tanımladığını benzer nesnesi bilinen rengi adları olarak renkleri ve `Color` öznitelik sözdizimi için bir tür dönüştürücüsü destekler.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-194">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="f5aa4-195">XAML biçimlendirme</span><span class="sxs-lookup"><span data-stu-id="f5aa4-195">XAML markup</span></span>|<span data-ttu-id="f5aa4-196">Sonuçta elde edilen XAML düğümü akışı</span><span class="sxs-lookup"><span data-stu-id="f5aa4-196">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="f5aa4-197">`Namespace` düğüm için `Party`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-197">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="f5aa4-198">`StartObject` düğüm için `Party`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-198">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="f5aa4-199">`StartMember` düğüm için `Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-199">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="f5aa4-200">`StartObject` düğüm için örtük `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-200">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="f5aa4-201">`StartMember` düğüm için örtük `FavorCollection` öğelerini özelliği.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-201">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="f5aa4-202">`StartObject` düğüm için `Balloon`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-202">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="f5aa4-203">`StartMember` düğüm için `Color`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-203">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="f5aa4-204">`Value` öznitelik değeri dize düğümü `"Red"`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-204">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="f5aa4-205">`EndMember` için `Color`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-205">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="f5aa4-206">`StartMember` düğüm için `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-206">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="f5aa4-207">`Value` öznitelik değeri dize düğümü `"True"`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-207">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="f5aa4-208">`EndMember` için `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-208">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="f5aa4-209">`EndObject` için `Balloon`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-209">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="f5aa4-210">`StartObject` düğüm için `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-210">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="f5aa4-211">`StartMember` düğüm için `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-211">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="f5aa4-212">`Value` başlatma değeri dize düğümü `"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-212">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="f5aa4-213">`EndMember` düğüm için `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-213">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="f5aa4-214">`EndObject` için `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-214">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="f5aa4-215">`EndMember` düğüm için örtük `FavorCollection` öğelerini özelliği.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-215">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="f5aa4-216">`EndObject` düğüm için örtük `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-216">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="f5aa4-217">`EndMember` için `Favors`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-217">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="f5aa4-218">`EndObject` için `Party`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-218">`EndObject` for `Party`</span></span>|

<span data-ttu-id="f5aa4-219">XAML düğüm akış üzerinde aşağıdaki davranış güvenebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="f5aa4-219">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="f5aa4-220">Varsa bir `Namespace` düğüm yoksa, bu akışa hemen önce eklenir `StartObject` XAML ad alanı ile bildirilen `xmlns`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-220">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="f5aa4-221">Önceki tabloya ile XAML ve örnek düğümü akışı yeniden bakın.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-221">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="f5aa4-222">Bildirim nasıl `StartObject` ve `Namespace` düğümleri görünen metni işaretleme bildirimi konumlarını karşı yorumlanabileceğinden için.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-222">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="f5aa4-223">Bu temsilci, ad alanı düğümleri her zaman göründüğü önüne düğüm davranış düğüm akışında geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-223">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="f5aa4-224">Bu tasarım amacı, ad alanı bilgisi nesnesi yazıcılar için önemlidir ve nesne türü eşleştirme veya aksi halde gerçekleştirmek için nesne yazıcı denemeleri işlemeden önce bilinmesi gerekir ' dir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-224">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="f5aa4-225">Uygulama kapsamı stream'de önüne XAML ad alanı bilgi verme, her zaman kendi sunulan sırayla düğüm akışa işlemek daha basit kılar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-225">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="f5aa4-226">Yukarıdaki nedeniyle, onu bir veya daha fazla husustur `Namespace` , ilk genellikle gerçek biçimlendirme düğümleri başından geçiş değil okuma düğümleri `StartObject` kök.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-226">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="f5aa4-227">A `StartObject` düğüm arkasından `StartMember`, `Value`, ya da hemen `EndObject`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-227">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="f5aa4-228">Hiçbir zaman hemen bir başkası tarafından sonrasında `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-228">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="f5aa4-229">A `StartMember` tarafından izlenen bir `StartObject`, `Value`, ya da hemen `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-229">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="f5aa4-230">Tarafından izlenebilir `GetObject`, nerede olduğunu değeri beklenen üst nesne var olan bir değerden gelmesini üyeleri için yerine `StartObject` yeni bir değer örneği.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-230">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="f5aa4-231">Tarafından da izlenebilir bir `Namespace` yaklaşan bir geçerli düğüm `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-231">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="f5aa4-232">Hiçbir zaman hemen bir başkası tarafından sonrasında `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-232">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="f5aa4-233">A `Value` düğümü değeri temsil eder; yok "EndValue" yoktur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-233">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="f5aa4-234">Yalnızca izlenebilir bir `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-234">It can be followed only by an `EndMember`.</span></span>

    - <span data-ttu-id="f5aa4-235">XAML başlatma metin nesnesinin oluşturma tarafından kullanılan bir nesne değeri yapısında sonuçlanmaz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-235">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="f5aa4-236">Bunun yerine bir üye için bir özel üye düğüm adlı `_Initialization` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-236">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="f5aa4-237">ve bu üye düğümünü başlatma değer dizesi içerir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-237">and that member node contains the initialization value string.</span></span> <span data-ttu-id="f5aa4-238">Varsa, `_Initialization` her zaman ilk olan `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-238">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="f5aa4-239">`_Initialization` XAML dil XAML namescope ile bazı XAML Hizmetleri gösterimleri, açıklamak için nitelenebilir `_Initialization` yedekleme türleri içinde tanımlanan bir özellik değil.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-239">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

    - <span data-ttu-id="f5aa4-240">Üyenin değeri değerinin bir özniteliğini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-240">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="f5aa4-241">Sonunda olabilir bir değer dönüştürücü bu değer işleme dahil olan ve düz bir dize değeridir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-241">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="f5aa4-242">Ancak, bu düğümü akışı XAML nesne yazıcısı işler kadar değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-242">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="f5aa4-243">XAML nesne yazıcısı gerekli XAML şema içeriği ve tür sistemi eşlemesi değer dönüştürmeleri için gerekli olan diğer destek sahiptir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-243">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="f5aa4-244">Bir `EndMember` düğüm arkasından bir `StartMember` düğümü tarafından veya sonraki üyesi için bir `EndObject` düğümü üyesi sahibi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-244">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="f5aa4-245">Bir `EndObject` düğüm arkasından bir `EndMember` düğümü.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-245">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="f5aa4-246">Tarafından da izlenebilir bir `StartObject` düğüm nesneleri, bir koleksiyonun öğeleri meslektaşlarınız olduğu durumlar için.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-246">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="f5aa4-247">Ya da tarafından izlenebilir bir `Namespace` yaklaşan bir geçerli düğüm `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-247">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

    - <span data-ttu-id="f5aa4-248">Tüm düğüm akış kapatılırken benzersiz kullanım durumu için `EndObject` , kök herhangi bir şey tarafından izlenmiyor; okuyucu artık bitiş dosya, ve <xref:System.Xaml.XamlReader.Read%2A> döndürür `false`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-248">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

<a name="value_converters_and_the_xaml_node_stream"></a>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="f5aa4-249">Değer dönüştürücüler ve XAML düğümü Stream</span><span class="sxs-lookup"><span data-stu-id="f5aa4-249">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="f5aa4-250">Bir değer dönüştürücü (değeri seri hale getiricileri genişletme dahil) bir tür dönüştürücüsü veya XAML tür sistemi üzerinden bir değer dönüştürücü olarak bildirilen özel başka bir sınıf, bir işaretleme uzantısı için genel bir terimdir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-250">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="f5aa4-251">XAML düğümü akışı bir tür dönüştürücüsü kullanımı ve biçimlendirme uzantısı kullanımı çok farklı temsilleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-251">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="f5aa4-252">XAML düğümü Stream içinde tür dönüştürücüleri</span><span class="sxs-lookup"><span data-stu-id="f5aa4-252">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="f5aa4-253">Bir öznitelik, sonunda bir tür dönüştürücüsü kullanımı sonuçları raporlanır, XAML düğüm akış içinde bir üye değeri olarak ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-253">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="f5aa4-254">XAML düğümü akışı bir tür dönüştürücüsü örneği nesnesi üretir ve değeri geçirin denemez.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-254">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="f5aa4-255">Bir tür dönüştürücüsü 's dönüştürme uygulamasını kullanarak çağırma XAML şema içeriği ve tür eşlemesi için kullanarak gerektirir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-255">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="f5aa4-256">Hangi tür dönüştürücü sınıfı değeri işlemek için kullanılması gereken bile belirleme XAML şema içeriği dolaylı olarak gerektirir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-256">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="f5aa4-257">Varsayılan XAML şema içeriği kullandığınızda, bu bilgileri XAML tür sisteminden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-257">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="f5aa4-258">XAML yazıcı bağlantı önce XAML düğüm akış düzeyinde türü dönüştürücü sınıf bilgileri gerekiyorsa, buradan edinebilirsiniz <xref:System.Xaml.XamlMember> ayarlanan üye bilgileri.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-258">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="f5aa4-259">Ancak Aksi takdirde, XAML şema içeriği ve tür eşlemesi sistemi gerektiren işlemler geri kalanında gerçekleştirilinceye kadar türü dönüştürücü giriş XAML düğüm akış içinde düz bir değer olarak korunması, örneğin bir XAML ile nesne oluşturma nesne yazıcı.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-259">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="f5aa4-260">Örneğin, aşağıdaki sınıf tanımı anahat ve XAML kullanım için göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="f5aa4-260">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="f5aa4-261">Bu kullanım için XAML düğümü akışı metin temsili aşağıdaki ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="f5aa4-261">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="f5aa4-262">`StartObject` ile <xref:System.Xaml.XamlType> temsil eden `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-262">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="f5aa4-263">`StartMember` ile <xref:System.Xaml.XamlMember> temsil eden `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-263">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="f5aa4-264">`Value` düğümle metin dizesi "`8x8`"</span><span class="sxs-lookup"><span data-stu-id="f5aa4-264">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="f5aa4-265">`EndMember` Eşleşme `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-265">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="f5aa4-266">`EndObject` Eşleşme `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="f5aa4-266">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="f5aa4-267">Bu düğüm stream'de hiçbir tür dönüştürücü örnek olduğuna dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-267">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="f5aa4-268">Ancak çağırarak türü dönüştürücü bilgi edinebilirsiniz <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> üzerinde <xref:System.Xaml.XamlMember> için `BoardSize`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-268">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="f5aa4-269">Geçerli bir XAML şema içeriği varsa, ayrıca dönüştürücü yöntemleri bir örneği elde ederek çağırabilirsiniz <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-269">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="f5aa4-270">XAML düğümü Stream biçimlendirme uzantıları</span><span class="sxs-lookup"><span data-stu-id="f5aa4-270">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="f5aa4-271">Biçimlendirme uzantısı kullanımı XAML düğüm akış nesnenin bir işaretleme uzantısı örneğinin temsil ettiği, bir üye içerisindeki bir nesne düğümü olarak bildirilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-271">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="f5aa4-272">Bu nedenle bir tür dönüştürücüsü kullanımı, daha fazla bilgi taşıyan değerinden ve biçimlendirme uzantısı kullanımı daha açık düğüm stream gösteriminde sunulur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-272">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="f5aa4-273"><xref:System.Xaml.XamlMember> Kullanım durumsal ve her olası biçimlendirme durumda değişir olduğundan bilgileri, her şeyi işaretleme uzantısı hakkında bir uyarıyla değil; tür dönüştürücüleri olduğu gibi adanmış ve örtük tür veya üye başına değil.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-273"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="f5aa4-274">Biçimlendirme uzantısı kullanımı (genellikle söz konusu olduğu) XAML metni işaretleme öznitelik formunda yapılan bile biçimlendirme uzantıları düğümü akışı temsili nesne düğümleri olarak durumdur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-274">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="f5aa4-275">Aynı şekilde kullanılan bir açık nesne öğesi biçiminde biçimlendirme uzantısı kullanımı kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-275">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="f5aa4-276">Bir işaretleme uzantısı nesne düğümü içinde bu işaretleme uzantısı üyeleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-276">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="f5aa4-277">XAML düğümü akışı temsili, biçimlendirme uzantısı kullanımı, konumsal parametre kullanımı ya da açıkça adlandırılmış parametrelerle bir kullanım oluşmasından korur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-277">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="f5aa4-278">Konumsal parametresi kullanım için XAML düğümü akışı XAML dili tarafından tanımlanan bir özellik içeren `_PositionalParameters` kullanım kayıtları.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-278">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="f5aa4-279">Bu özellik, genel <xref:System.Collections.Generic.List%601> ile <xref:System.Object> kısıtlaması.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-279">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="f5aa4-280">Kısıtlama nesnesi ve dize değil çünkü kısıtlanmamışsa konumsal parametre kullanımı içindeki iç içe geçmiş biçimlendirme uzantısı kullanımı içerebilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-280">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="f5aa4-281">Konumsal parametreler kullanımdan erişmek için listede yinelemek ve dizin oluşturucular için tek tek liste değerleri kullanın.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-281">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="f5aa4-282">Adlandırılmış parametre kullanımı için her adlandırılmış parametre düğüm stream'de bu ada sahip bir üye düğümü olarak temsil edilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-282">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="f5aa4-283">İç içe geçmiş biçimlendirme uzantısı kullanımı olabileceğinden üye değerlerinin, dizeler desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-283">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="f5aa4-284">`ProvideValue` biçimlendirmeden uzantısı henüz çağrılmaz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-284">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="f5aa4-285">Ancak, XAML okuyucu ve yazıcı XAML bağlanmanız durumunda çağrılan böylece `WriteEndObject` düğüm stream'de incelediğinizde işaretleme uzantısı düğüm üzerinde çağrılır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-285">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="f5aa4-286">Bu nedenle, genellikle aynı XAML şema içeriği yükleme yolunda Nesne grafiği oluşturmak için kullanılan kullanılabilir gerekir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-286">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="f5aa4-287">Aksi takdirde, `ProvideValue` beklenen Hizmetleri kullanılabilir olmadığından hiçbir biçimlendirmeden özel durumlar, burada uzantı oluşturabilecek.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-287">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

<a name="xaml_and_xml_languagedefined_members_in_the_xaml_node_stream"></a>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="f5aa4-288">XAML ve XML dili tarafından tanımlanan üyeleri XAML düğümü Stream</span><span class="sxs-lookup"><span data-stu-id="f5aa4-288">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="f5aa4-289">Belirli üyeleri bir XAML düğümü akışı yorumlar ve bir XAML okuyucu kuralları nedeniyle yerine açık bir sunulan <xref:System.Xaml.XamlMember> arama veya oluşturma.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-289">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="f5aa4-290">Genellikle, bu üyeler XAML yönergeleridir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-290">Often, these members are XAML directives.</span></span> <span data-ttu-id="f5aa4-291">Bazı durumlarda, XAML düğümü akışı yönergesi tanıtan XAML okuma işlemidir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-291">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="f5aa4-292">Diğer bir deyişle, özgün XAML metni member yönergesi açıkça belirtilmedi, ancak XAML okuyucu bu bilgileri kaybolur önce bir yapısal XAML kuralı ve rapor bilgilerinde XAML düğümü akışı karşılamak için yönergesi ekler girin.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-292">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="f5aa4-293">Aşağıdaki liste notları tüm durumlarda bir yönerge XAML üye düğümünü tanıtmak için bir XAML okuyucu burada beklenir ve bu üye düğümünü .NET Framework XAML hizmetlerinde uygulamalarında nasıl tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-293">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in the .NET Framework XAML Services implementations.</span></span>

- <span data-ttu-id="f5aa4-294">**Bir nesne düğümü başlatma metni:** Bu üye düğümün adı `_Initialization`temsil ettiği bir XAML yönerge ve XAML dil XAML ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-294">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="f5aa4-295">Buradan için statik bir varlığı alabilirsiniz <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-295">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="f5aa4-296">**İşaretleme uzantısı için konumsal Parametreler:** Bu üye düğümün adı `_PositionalParameters`, XAML dili XAML ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-296">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="f5aa4-297">Her zaman her biri üzerinde ayırarak, önceden ayrılmış konumsal bir parametresi olan bir genel liste nesnelerin içerdiği `,` XAML girişinde sağlanan olarak sınırlayıcı karakter.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-297">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="f5aa4-298">Konumsal parametreler yönergesinden kaynaklandığından için statik bir varlığı alabilirsiniz <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-298">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="f5aa4-299">**Bilinmeyen içeriği:** Bu üye düğümün adı `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-299">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="f5aa4-300">NET olarak söylemek gerekirse, olan bir <xref:System.Xaml.XamlDirective>, XAML dili XAML ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-300">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="f5aa4-301">Bu yönerge, burada kaynak XAML İçerik XAML nesne öğesi içeriyor, ancak hiçbir içerik özelliği şu anda kullanılabilir XAML şema içeriği altında belirlenebilir durumlarda bir sentinel kullanılır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-301">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="f5aa4-302">XAML düğümü akışı bu durumda adlı üye için kontrol ederek algılayabilir `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-302">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="f5aa4-303">Bir yükleme yolu XAML düğüm akış varsayılan başka hiçbir işlem yapılmazsa <xref:System.Xaml.XamlObjectWriter> denenen oluşturur `WriteEndObject` karşılaştığında `_UnknownContent` herhangi bir nesne üzerinde üyesi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-303">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="f5aa4-304">Varsayılan <xref:System.Xaml.XamlXmlWriter> oluşturmaz ve üyeyi örtük olarak değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-304">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="f5aa4-305">Statik bir varlık için alabilirsiniz `_UnknownContent` gelen <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-305">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="f5aa4-306">**Bir koleksiyonun koleksiyon özelliği:** Destekleyen CLR türünü XAML için genellikle kullanılan bir koleksiyon sınıfının özel koleksiyon öğelerini tutan adlı olsa da, bu özellik için tür çözümlemesi yedekleme önce bir XAML tür sistemi bilinmiyor.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-306">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="f5aa4-307">Bunun yerine, XAML düğümü akışı tanıtır bir `Items` yer tutucu XAML türü koleksiyonunun bir üyesi olarak.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-307">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="f5aa4-308">Bu yönerge, .NET Framework XAML hizmetlerinde uygulamasında adı / düğüm stream'de üyesidir `_Items`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-308">In the .NET Framework XAML Services implementation the name of this directive / member in the node stream is `_Items`.</span></span> <span data-ttu-id="f5aa4-309">Bu yönerge için bir sabit örneğinden alınabilen <xref:System.Xaml.XamlLanguage.Items%2A>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-309">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="f5aa4-310">XAML düğümü akışı ayrıştırılabilir olmaması kapatma öğeleri içeren bir Items özelliğini içerebilir yedekleme tür çözümlemesi ve XAML şema içeriği göre unutmayın.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-310">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="f5aa4-311">Örneğin,</span><span class="sxs-lookup"><span data-stu-id="f5aa4-311">For example,</span></span>

- <span data-ttu-id="f5aa4-312">**XML tarafından tanımlanan Üyeler:** XML tanımlı `xml:base`, `xml:lang` ve `xml:space` üyeleri adlı XAML yönergeleri olarak bildirilen `base`, `lang`, ve `space` .NET Framework XAML hizmetlerinde uygulamalarında.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-312">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in the .NET Framework XAML Services implementations.</span></span> <span data-ttu-id="f5aa4-313">Bu ad alanı XML ad alanı `http://www.w3.org/XML/1998/namespace`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-313">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="f5aa4-314">Bunların her biri için sabitler elde edilebilir gelen <xref:System.Xaml.XamlLanguage>.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-314">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="f5aa4-315">Düğüm sırası</span><span class="sxs-lookup"><span data-stu-id="f5aa4-315">Node Order</span></span>

<span data-ttu-id="f5aa4-316">Bazı durumlarda, <xref:System.Xaml.XamlXmlReader> XAML düğümlerinin düğümleri işaretlemede görüntülerse veya XML olarak işlenen görüntülenme sırasını karşı XAML düğüm akış sırasını değiştirir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-316">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="f5aa4-317">Bu düğümler sipariş için gerçekleştirilir gibi bir <xref:System.Xaml.XamlObjectWriter> düğümü akışı yalnızca ileriye doğru bir şekilde işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-317">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="f5aa4-318">.NET Framework XAML hizmetlerinde XAML okuyucu düğümleri bu görevi, performans iyileştirme düğümü akışı XAML nesne yazıcı Tüketiciler için XAML yazıcı için bırakmak yerine yeniden sıralar.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-318">In .NET Framework XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="f5aa4-319">Belirli yönergeleri, özellikle daha fazla bilgi için bir nesneden bir nesne öğesi oluşturulmasını sağlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-319">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="f5aa4-320">Bu yönergeler şunlardır: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-320">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="f5aa4-321">Bu yönergeler, bir nesnenin aşağıdaki düğüm stream'de ilk üye olarak yerleştirmek .NET Framework XAML Hizmetleri XAML okuyucular denemesi `StartObject`, sonraki bölümde açıklanan nedenlerle.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-321">The .NET Framework XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="f5aa4-322">XamlObjectWriter davranışı ve düğüm sırası</span><span class="sxs-lookup"><span data-stu-id="f5aa4-322">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="f5aa4-323">`StartObject` için bir <xref:System.Xaml.XamlObjectWriter> mutlaka hemen nesne örneği oluşturmak için bir sinyal için XAML nesne yazıcısı değil.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-323">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="f5aa4-324">XAML ek Giriş bir nesne başlatmak ve ilk nesnesi ve ardından yalnızca ayarı özellikleri oluşturmak için varsayılan bir oluşturucu tamamen çağırmaya dayanması değil olanaklı kılan birçok dil özellikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-324">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a default constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="f5aa4-325">Bu özellikler şunları içerir: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; başlatma metin; [x: TypeArguments](x-typearguments-directive.md); konumsal bir işaretleme uzantısı parametre; Fabrika yöntemleri ve ilişkili [x: Arguments](x-arguments-directive.md) düğümleri (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="f5aa4-325">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](x-typearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](x-arguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="f5aa4-326">Her durumda gerçek nesnenin yapımı gecikme ve XAML nesne yazıcısı düğümü akışı yeniden olduğundan, özellikle bir yapım olmayan bir başlangıç üyesi karşılaşıldığında, aslında örneği oluşturma davranışı güvenebilirsiniz nesne türüne yönergesi.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-326">Each of these cases delay the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="f5aa4-327">GetObject</span><span class="sxs-lookup"><span data-stu-id="f5aa4-327">GetObject</span></span>

<span data-ttu-id="f5aa4-328">`GetObject` Yeni bir nesne oluşturmak yerine bir XAML nesne yazıcısı bunun yerine nesnenin içeren özelliğinin değeri nereden bir XAML düğümünü temsil eder.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-328">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="f5aa4-329">Tipik bir servis talebi burada bir `GetObject` düğüm karşılaşıldığında içeren özellik yedekleme türün nesne modelinde salt okunur kasıtlı olarak bir XAML düğüm akış koleksiyon nesnesi veya bir sözlük nesnesi olur.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-329">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="f5aa4-330">Bu senaryoda, koleksiyona veya sözlüğe genellikle oluşturulur ve başlatıldı (genellikle boş) sahip olan bir türü başlatma mantığı tarafından.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-330">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="f5aa4-331">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="f5aa4-331">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="f5aa4-332">XAML Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="f5aa4-332">XAML Services</span></span>](index.md)
- [<span data-ttu-id="f5aa4-333">XAML Ad Alanları</span><span class="sxs-lookup"><span data-stu-id="f5aa4-333">XAML Namespaces</span></span>](xaml-namespaces-for-net-framework-xaml-services.md)
