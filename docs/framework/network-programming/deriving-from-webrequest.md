---
title: "WebRequest türetme"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
caps.latest.revision: "9"
author: mcleblanc
ms.author: markl
manager: markl
ms.workload: dotnet
ms.openlocfilehash: 83579c25c154462cb21488acf9fcf84999b9a2d4
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 12/22/2017
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="9ab70-102">WebRequest türetme</span><span class="sxs-lookup"><span data-stu-id="9ab70-102">Deriving from WebRequest</span></span>
<span data-ttu-id="9ab70-103"><xref:System.Net.WebRequest> Sınıfı, .NET Framework takılabilir Protokolü modeli uygun bir protokole özgü istek işleyicisi oluşturmak için temel yöntemleri ve özellikleri sağlayan bir Özet temel sınıf.</span><span class="sxs-lookup"><span data-stu-id="9ab70-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="9ab70-104">Kullanan uygulamalar **WebRequest** sınıfı, kullanılan protokolü belirtmek için gerek kalmadan herhangi bir desteklenen protokolünü kullanarak veri isteyebilir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="9ab70-105">İki ölçütleri karşılar, takılabilir bir protokol olarak kullanılacak bir protokole özgü sınıf için sırayla: sınıf uygulamalıdır <xref:System.Net.IWebRequestCreate> arabirimi ve kaydetmeniz gerekir ile <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="9ab70-106">Sınıfı, tüm soyut yöntemler ve özelliklerini geçersiz kılmanız gerekir **WebRequest** takılabilir arabirimi sağlamak için.</span><span class="sxs-lookup"><span data-stu-id="9ab70-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="9ab70-107">**WebRequest** örnekleri tek seferlik kullanılmak üzere tasarlanmıştır; başka bir istek yapmak istiyorsanız Yeni Oluştur **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="9ab70-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="9ab70-108">**WebRequest** destekleyen <xref:System.Runtime.Serialization.ISerializable> şablon seri hale getirmek, geliştiricilerin arabirimi **WebRequest** ve ek istekler için şablonu yeniden yapılandırma.</span><span class="sxs-lookup"><span data-stu-id="9ab70-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="9ab70-109">IWebRequest oluşturma yöntemi</span><span class="sxs-lookup"><span data-stu-id="9ab70-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="9ab70-110"><xref:System.Net.IWebRequestCreate.Create%2A> Yöntemdir protokole özgü sınıfının yeni bir örneğini başlatmak için sorumlu.</span><span class="sxs-lookup"><span data-stu-id="9ab70-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="9ab70-111">Yeni bir **WebRequest** oluşturulur, <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> yöntemi ile kayıtlı URI öneklerle istenen URI'yi eşleşen **RegisterPrefix** yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="9ab70-112">**Oluşturma** uygun protokole özgü alt öğesi yöntemi döndürmelidir descendant başlatılmış örneği herhangi gerek kalmadan protokol için standart istek/yanıt işlem gerçekleştirme yeteneğine protokole özgü alanları değiştirdi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="9ab70-113">ConnectionGroupName özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="9ab70-114"><xref:System.Net.WebRequest.ConnectionGroupName%2A> Özelliği, böylece birden fazla isteği tek bir bağlantı üzerinden yapılabilir bağlantı için bir kaynak grubunun adı için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="9ab70-115">Bağlantı Paylaşımı uygulamak için havuzu ve bağlantıları atama bir protokole özgü yöntemini kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="9ab70-116">Örneğin, sağlanan <xref:System.Net.ServicePointManager> sınıfı uygulayan paylaşımı için bağlantı <xref:System.Net.HttpWebRequest> sınıfı.</span><span class="sxs-lookup"><span data-stu-id="9ab70-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="9ab70-117">**ServicePointManager** sınıfı oluşturur bir <xref:System.Net.ServicePoint> , her bağlantı grubu için belirli bir sunucuya bağlantı sağlar.</span><span class="sxs-lookup"><span data-stu-id="9ab70-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="9ab70-118">ContentLength özelliğinin</span><span class="sxs-lookup"><span data-stu-id="9ab70-118">ContentLength Property</span></span>  
 <span data-ttu-id="9ab70-119"><xref:System.Net.WebRequest.ContentLength%2A> Özelliği, sunucuya gönderilir, verileri karşıya yüklenirken veri bayt sayısını belirtir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="9ab70-120">Genellikle <xref:System.Net.WebRequest.Method%2A> özelliğini ayarlayın, bir yükleme sürüyor belirtmek için ne zaman yerleştirin **ContentLength** özelliği ayarlanmış bir değer sıfırdan büyük.</span><span class="sxs-lookup"><span data-stu-id="9ab70-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="9ab70-121">ContentType özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-121">ContentType Property</span></span>  
 <span data-ttu-id="9ab70-122"><xref:System.Net.WebRequest.ContentType%2A> Özelliği, protokol, göndermekte olduğunuz içerik türünü tanımlamak için sunucuya göndermenizi gerektirdiği herhangi bir özel bilgiyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="9ab70-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="9ab70-123">Genellikle bu MIME içerik karşıya herhangi bir veri türüdür.</span><span class="sxs-lookup"><span data-stu-id="9ab70-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="9ab70-124">Kimlik bilgileri özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-124">Credentials Property</span></span>  
 <span data-ttu-id="9ab70-125"><xref:System.Net.WebRequest.Credentials%2A> Özelliği, sunucu ile istek kimliğini doğrulamak için gereken bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="9ab70-126">Kimlik doğrulama işleminin ayrıntılarını protokolü için uygulamalıdır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="9ab70-127"><xref:System.Net.AuthenticationManager> Sınıftır istekleri kimlik doğrulaması ve kimlik doğrulama belirtecini sağlama sorumlu.</span><span class="sxs-lookup"><span data-stu-id="9ab70-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="9ab70-128">Protokolü tarafından kullanılan kimlik bilgilerini sağlayan sınıf uygulamalıdır <xref:System.Net.ICredentials> arabirimi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="9ab70-129">Üstbilgiler özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-129">Headers Property</span></span>  
 <span data-ttu-id="9ab70-130"><xref:System.Net.WebRequest.Headers%2A> Özelliği, istekle ilişkili meta verileri ad/değer çiftlerini rastgele bir koleksiyonunu içerir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="9ab70-131">Ad/değer çifti eklenebilir olarak ifade edilebilir protokol için gerekli herhangi bir meta veri **üstbilgileri** özelliği.</span><span class="sxs-lookup"><span data-stu-id="9ab70-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="9ab70-132">Genellikle bu bilgileri çağırmadan önce ayarlamalısınız <xref:System.Net.WebRequest.GetRequestStream%2A> veya <xref:System.Net.WebRequest.GetResponse%2A> yöntemleri; istek gerçekleştirildikten sonra meta veriler salt okunur olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="9ab70-133">Kullanmak için gerekli **üstbilgileri** üstbilgi meta veriyi kullanmak için özelliği.</span><span class="sxs-lookup"><span data-stu-id="9ab70-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="9ab70-134">Protokol özgü meta veriler özellikleri olarak gösterilebilir; Örneğin, <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> özelliği çıkarır **User-Agent** HTTP üstbilgisi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="9ab70-135">Üstbilgi meta veri özelliği olarak kullanıma sunmak, kullanarak ayarlamak aynı özelliğe izin vermemelisiniz **üstbilgileri** özelliği.</span><span class="sxs-lookup"><span data-stu-id="9ab70-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="9ab70-136">Yöntem özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-136">Method Property</span></span>  
 <span data-ttu-id="9ab70-137"><xref:System.Net.WebRequest.Method%2A> Fiil veya isteği gerçekleştirmek için sunucu isteyen eylem özelliği içerir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="9ab70-138">İçin varsayılan **yöntemi** özelliği, bir standart istek/yanıt eylemi ayarlanacak herhangi bir protokole özgü özellik gerek kalmadan etkinleştirmelisiniz.</span><span class="sxs-lookup"><span data-stu-id="9ab70-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="9ab70-139">Örneğin, <xref:System.Net.HttpWebResponse.Method%2A> yöntemi varsayılan olarak, bir kaynak Web sunucusundan gelen istekleri ve yanıt döndüren GET.</span><span class="sxs-lookup"><span data-stu-id="9ab70-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="9ab70-140">Genellikle **ContentLength** özelliği için bir değer sıfır olduğunda büyük ayarlanmalıdır **yöntemi** bir fiil veya karşıya yükleme yer aldığını gösteren işlem özelliğini ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="9ab70-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="9ab70-141">Özellik preAuthenticate</span><span class="sxs-lookup"><span data-stu-id="9ab70-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="9ab70-142">Uygulamaları kümesi <xref:System.Net.WebRequest.PreAuthenticate%2A> bu kimlik doğrulama bilgilerini belirtmek için özelliği ilk istekle gönderilmesi gerektiğini yerine bir kimlik doğrulaması sınaması için bekleniyor.</span><span class="sxs-lookup"><span data-stu-id="9ab70-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="9ab70-143">**PreAuthenticate** özelliği Protokolü kimlik doğrulama bilgilerini ilk istekle birlikte gönderilen destekliyorsa anlamlı yalnızca.</span><span class="sxs-lookup"><span data-stu-id="9ab70-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="9ab70-144">Proxy özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-144">Proxy Property</span></span>  
 <span data-ttu-id="9ab70-145"><xref:System.Net.WebRequest.Proxy%2A> Özelliği içeren bir <xref:System.Net.IWebProxy> istenen kaynağa erişmek için kullanılan arabirim.</span><span class="sxs-lookup"><span data-stu-id="9ab70-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="9ab70-146">**Proxy** özelliği yalnızca, Protokolü yönlendirilirken istekleri destekliyorsa anlamlı.</span><span class="sxs-lookup"><span data-stu-id="9ab70-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="9ab70-147">Bir protokolü tarafından gerekirse varsayılan proxy ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="9ab70-148">Bazı ortamlarda gibi kurumsal bir güvenlik duvarı, protokolü bir proxy sunucu kullanmak için gerekli olabilir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="9ab70-149">Bu durumda, uygulamanız gereken **IWebProxy** protokolü için çalışacak bir proxy sınıfı oluşturmak için arabirimi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="9ab70-150">RequestUri özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-150">RequestUri Property</span></span>  
 <span data-ttu-id="9ab70-151"><xref:System.Net.WebRequest.RequestUri%2A> Özelliği için geçirilen URI içeren **WebRequest.Create** yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="9ab70-152">Salt okunur olduğundan ve bir kez değiştirilemez **WebRequest** oluşturuldu.</span><span class="sxs-lookup"><span data-stu-id="9ab70-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="9ab70-153">Yeniden yönlendirme, Protokolü destekliyorsa, yanıt farklı bir URI tarafından tanımlanan bir kaynaktan gelebilir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="9ab70-154">Yanıt URI'si erişim sağlamak gerekiyorsa, bu URI içeren bir ek özellik sağlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="9ab70-155">Zaman aşımı özelliği</span><span class="sxs-lookup"><span data-stu-id="9ab70-155">Timeout Property</span></span>  
 <span data-ttu-id="9ab70-156"><xref:System.Net.WebRequest.Timeout%2A> Özelliği, milisaniye cinsinden istek zaman aşımına uğradı ve bir özel durum oluşturur önce beklenecek süreyi içerir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="9ab70-157">**Zaman aşımı** ile yapılan yalnızca çok zaman uyumlu istekleri geçerlidir <xref:System.Net.WebRequest.GetResponse%2A> yöntemi; zaman uyumsuz istekleri kullanmalıdır <xref:System.Net.WebRequest.Abort%2A> bekleyen isteği iptal etmek için yöntem.</span><span class="sxs-lookup"><span data-stu-id="9ab70-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="9ab70-158">Ayarı **zaman aşımı** özelliği yalnızca protokole özgü sınıfı bir zaman aşımı işlem uyguluyorsa anlamlı.</span><span class="sxs-lookup"><span data-stu-id="9ab70-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="9ab70-159">Abort Yöntemi</span><span class="sxs-lookup"><span data-stu-id="9ab70-159">Abort Method</span></span>  
 <span data-ttu-id="9ab70-160"><xref:System.Net.WebRequest.Abort%2A> Yöntemi bir sunucu için bekleyen bir zaman uyumsuz isteği iptal eder.</span><span class="sxs-lookup"><span data-stu-id="9ab70-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="9ab70-161">İstek iptal edildikten sonra çağırma **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**,  **BeginGetRequestStream**, veya **EndGetRequestStream** özel durum oluşturacak bir <xref:System.Net.WebException> ile <xref:System.Net.WebException.Status%2A> özelliğini <xref:System.Net.WebExceptionStatus>.</span><span class="sxs-lookup"><span data-stu-id="9ab70-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="9ab70-162">BeginGetRequestStream ve EndGetRequestStream yöntemleri</span><span class="sxs-lookup"><span data-stu-id="9ab70-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="9ab70-163"><xref:System.Net.WebRequest.BeginGetRequestStream%2A> Yöntemi verileri sunucuya yüklemek için kullanılan akış için zaman uyumsuz isteği başlatır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="9ab70-164"><xref:System.Net.WebRequest.EndGetRequestStream%2A> Yöntemi zaman uyumsuz istek tamamlandıktan ve istenen akışı döndürür.</span><span class="sxs-lookup"><span data-stu-id="9ab70-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="9ab70-165">Bu yöntemleri uygulamak **GetRequestStream** standart .NET Framework zaman uyumsuz desen kullanma yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="9ab70-166">BeginGetResponse ve EndGetResponse yöntemleri</span><span class="sxs-lookup"><span data-stu-id="9ab70-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="9ab70-167"><xref:System.Net.WebRequest.BeginGetResponse%2A> Yöntemi bir sunucu için zaman uyumsuz isteği başlatır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="9ab70-168"><xref:System.Net.WebRequest.EndGetResponse%2A> Yöntemi zaman uyumsuz istek tamamlandıktan ve istenen yanıtı döndürür.</span><span class="sxs-lookup"><span data-stu-id="9ab70-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="9ab70-169">Bu yöntemleri uygulamak **GetResponse** standart .NET Framework zaman uyumsuz desen kullanma yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9ab70-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="9ab70-170">GetRequestStream yöntemi</span><span class="sxs-lookup"><span data-stu-id="9ab70-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="9ab70-171"><xref:System.Net.WebRequest.GetRequestStream%2A> Yöntemi, istenen sunucuya veri yazmak için kullanılan bir akış döndürür.</span><span class="sxs-lookup"><span data-stu-id="9ab70-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="9ab70-172">Döndürülen akış arama değil yalnızca yazma akışı olmalıdır; tek yönlü bir sunucuya yazılan veri akışı olarak tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="9ab70-173">Akış için false döndürür <xref:System.IO.Stream.CanRead%2A> ve <xref:System.IO.Stream.CanSeek%2A> özellikleri ve için true <xref:System.IO.Stream.CanWrite%2A> özelliği.</span><span class="sxs-lookup"><span data-stu-id="9ab70-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="9ab70-174">**GetRequestStream** yöntemi genellikle sunucunun bir bağlantı açar ve akış göndermeden önce bu verileri gösterir gönderir üst bilgileri sunucuya gönderiliyor.</span><span class="sxs-lookup"><span data-stu-id="9ab70-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="9ab70-175">Çünkü **GetRequestStream** herhangi bir ayarı isteğini başlatır **üstbilgi** özellikleri veya **ContentLength** özelliği genellikle izin verilmeyen çağrıldıktansonra**GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="9ab70-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="9ab70-176">GetResponse yöntemi</span><span class="sxs-lookup"><span data-stu-id="9ab70-176">GetResponse Method</span></span>  
 <span data-ttu-id="9ab70-177"><xref:System.Net.WebRequest.GetResponse%2A> Yöntemi döndürür protokole özgü alt öğesi <xref:System.Net.WebResponse> sunucudan gelen yanıtı temsil eden sınıf.</span><span class="sxs-lookup"><span data-stu-id="9ab70-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="9ab70-178">İstek tarafından zaten başlatıldı sürece **GetRequestStream** yöntemi, **GetResponse** yöntemi, bir bağlantı tarafından tanımlanan kaynağa oluşturur **RequestUri**yapılan bir istek türünü belirten üst bilgileri gönderir ve ardından kaynak yanıtı alır.</span><span class="sxs-lookup"><span data-stu-id="9ab70-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="9ab70-179">Bir kez **GetResponse** yöntemi çağrıldığında, tüm özellikleri salt okunur olarak düşünülmelidir.</span><span class="sxs-lookup"><span data-stu-id="9ab70-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="9ab70-180">**WebRequest** örnekleri tek seferlik kullanıma yöneliktir; başka bir istek yapmak istiyorsanız, yeni bir oluşturmalısınız **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="9ab70-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="9ab70-181">**GetResponse** yöntemdir uygun bir oluşturmaktan sorumlu **WebResponse** gelen yanıtı içeren alt.</span><span class="sxs-lookup"><span data-stu-id="9ab70-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9ab70-182">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="9ab70-182">See Also</span></span>  
 <xref:System.Net.WebRequest>  
 <xref:System.Net.HttpWebRequest>  
 <xref:System.Net.FileWebRequest>  
 [<span data-ttu-id="9ab70-183">Takılabilir Protokoller Programlama</span><span class="sxs-lookup"><span data-stu-id="9ab70-183">Programming Pluggable Protocols</span></span>](../../../docs/framework/network-programming/programming-pluggable-protocols.md)  
 [<span data-ttu-id="9ab70-184">WebResponse’tan Türetme</span><span class="sxs-lookup"><span data-stu-id="9ab70-184">Deriving from WebResponse</span></span>](../../../docs/framework/network-programming/deriving-from-webresponse.md)
