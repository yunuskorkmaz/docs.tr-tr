---
title: WebRequest’ten Türetme
ms.date: 03/30/2017
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
ms.openlocfilehash: 6bee864f8d24076d16f226c29d61801e856739d9
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/15/2020
ms.locfileid: "71048611"
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="371b8-102">WebRequest’ten Türetme</span><span class="sxs-lookup"><span data-stu-id="371b8-102">Deriving from WebRequest</span></span>
<span data-ttu-id="371b8-103">Sınıf, <xref:System.Net.WebRequest> .NET Framework takılabilir protokol modeline uyan protokole özgü bir istek işleyicisi oluşturmak için temel yöntemleri ve özellikleri sağlayan soyut bir taban sınıfıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="371b8-104">**Webİstek** sınıfını kullanan uygulamalar, kullanılan protokolü belirtmeye gerek kalmadan desteklenen herhangi bir protokolü kullanarak veri isteyebilir.</span><span class="sxs-lookup"><span data-stu-id="371b8-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="371b8-105">Protokole özgü bir sınıfın takılabilir bir protokol olarak kullanılabilmesi için iki ölçüt <xref:System.Net.IWebRequestCreate> karşılanmalıdır: Sınıf arabirimi uygulamalı ve <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> yöntemle kaydolmalıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="371b8-106">Sınıf takılabilir arabirimi sağlamak için **WebRequest'in** tüm soyut yöntemlerini ve özelliklerini geçersiz kılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="371b8-107">**WebRequest** örnekleri tek seferlik kullanım için tasarlanmıştır; başka bir istekte bulunmak istiyorsanız, yeni bir **Webİstek**oluşturun.</span><span class="sxs-lookup"><span data-stu-id="371b8-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="371b8-108">**WebRequest,** <xref:System.Runtime.Serialization.ISerializable> geliştiricilerin bir şablon **UYR'u** seri hale getirmek ve ardından ek istekler için şablonu yeniden oluşturmak için arabirimi destekler.</span><span class="sxs-lookup"><span data-stu-id="371b8-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="371b8-109">iWebRequest Oluşturma Yöntemi</span><span class="sxs-lookup"><span data-stu-id="371b8-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="371b8-110">Yöntem, <xref:System.Net.IWebRequestCreate.Create%2A> protokole özgü sınıfın yeni bir örneğini başlatmadan sorumludur.</span><span class="sxs-lookup"><span data-stu-id="371b8-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="371b8-111">Yeni bir **WebRequest** oluşturulduğunda, <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> yöntem istenen URI ile **RegisterPrefix** yöntemine kayıtlı URI önekleri ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="371b8-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="371b8-112">Uygun protokole özgü soyundan gelen in oluşturma **yöntemi,** protokol için protokol için standart bir istek/yanıt hareketi gerçekleştirme yeteneğine sahip soyundan gelenin, protokole özgü alanlara gerek kalmadan başlatılması gereken bir örneğini döndürmelidir.</span><span class="sxs-lookup"><span data-stu-id="371b8-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="371b8-113">BağlantıGroupName Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="371b8-114">Özellik, <xref:System.Net.WebRequest.ConnectionGroupName%2A> tek bir bağlantı üzerinden birden çok istek yapIlebilmek için bir kaynağa bağlantı grubu adlandırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="371b8-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="371b8-115">Bağlantı paylaşımını uygulamak için, bağlantıları birleştirme ve atama protokolüne özgü bir yöntem kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="371b8-116">Örneğin, sağlanan <xref:System.Net.ServicePointManager> sınıf <xref:System.Net.HttpWebRequest> sınıf için bağlantı paylaşımı uygular.</span><span class="sxs-lookup"><span data-stu-id="371b8-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="371b8-117">**ServicePointManager** sınıfı, her <xref:System.Net.ServicePoint> bağlantı grubu için belirli bir sunucuya bağlantı sağlayan bir bağlantı oluşturur.</span><span class="sxs-lookup"><span data-stu-id="371b8-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="371b8-118">İçerik Uzunluğu Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-118">ContentLength Property</span></span>  
 <span data-ttu-id="371b8-119">Özellik, <xref:System.Net.WebRequest.ContentLength%2A> veri yüklerken sunucuya gönderilecek bayt veri sayısını belirtir.</span><span class="sxs-lookup"><span data-stu-id="371b8-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="371b8-120">Genellikle <xref:System.Net.WebRequest.Method%2A> **özellik, ContentLength** özelliği sıfırdan büyük bir değere ayarlandığında bir yüklemenin gerçekleştiğini gösterecek şekilde ayarlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="371b8-121">İçerikTürü Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-121">ContentType Property</span></span>  
 <span data-ttu-id="371b8-122">Özellik, <xref:System.Net.WebRequest.ContentType%2A> protokolünüzün gönderdiğiniz içerik türünü belirlemek için sunucuya göndermenizi gerektirdiği özel bilgileri sağlar.</span><span class="sxs-lookup"><span data-stu-id="371b8-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="371b8-123">Genellikle bu yüklenen herhangi bir verinin MIME içerik türüdür.</span><span class="sxs-lookup"><span data-stu-id="371b8-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="371b8-124">Kimlik Bilgileri Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-124">Credentials Property</span></span>  
 <span data-ttu-id="371b8-125">Özellik, <xref:System.Net.WebRequest.Credentials%2A> isteğin sunucuyla doğruluğunu doğrulamak için gereken bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="371b8-126">Protokolünüz için kimlik doğrulama işleminin ayrıntılarını uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="371b8-127">Sınıf, <xref:System.Net.AuthenticationManager> isteklerin doğrulanmasından ve kimlik doğrulama belirteci nin sağlanmasından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="371b8-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="371b8-128">Protokolünüz tarafından kullanılan kimlik bilgilerini sağlayan <xref:System.Net.ICredentials> sınıfın arabirimi uygulaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="371b8-129">Üstbilgi Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-129">Headers Property</span></span>  
 <span data-ttu-id="371b8-130">Özellik, <xref:System.Net.WebRequest.Headers%2A> istekle ilişkili meta verilerin ad/değer çiftleri rasgele bir koleksiyon içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="371b8-131">Protokol tarafından ad/değer çifti olarak ifade edilebilen meta veriler **Üstbilgi** özelliğine dahil edilebilir.</span><span class="sxs-lookup"><span data-stu-id="371b8-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="371b8-132">Genellikle bu bilgiler veya <xref:System.Net.WebRequest.GetRequestStream%2A> <xref:System.Net.WebRequest.GetResponse%2A> yöntemleri çağırmadan önce ayarlanmalıdır; istek yapıldıktan sonra, meta veriler salt okunur olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="371b8-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="371b8-133">Üstbilgi meta verilerini kullanmak için **Üstbilgiler** özelliğini kullanmanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="371b8-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="371b8-134">Protokole özgü meta veriler özellik olarak ortaya çıkabilir; örneğin, <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> özellik **Kullanıcı-Aracı** HTTP üstbilgisini ortaya çıkarır.</span><span class="sxs-lookup"><span data-stu-id="371b8-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="371b8-135">Üstbilgi meta verilerini bir özellik olarak ortaya çıkarırken, aynı özelliğin **Üstbilgi** özelliğikullanılarak ayarlanabilmesine izin vermemelisiniz.</span><span class="sxs-lookup"><span data-stu-id="371b8-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="371b8-136">Yöntem Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-136">Method Property</span></span>  
 <span data-ttu-id="371b8-137">Özellik, <xref:System.Net.WebRequest.Method%2A> isteğin sunucudan gerçekleştirmesini istediği fiil veya eylemi içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="371b8-138">**Yöntem** özelliği için varsayılan, protokole özgü özelliklerin ayarlanmasına gerek kalmadan standart bir istek/yanıt eylemini etkinleştirmelidir.</span><span class="sxs-lookup"><span data-stu-id="371b8-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="371b8-139">Örneğin, <xref:System.Net.HttpWebResponse.Method%2A> yöntem, bir Web sunucusundan kaynak isteyen ve yanıtı döndüren GET varsayılan olarak karşılar.</span><span class="sxs-lookup"><span data-stu-id="371b8-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="371b8-140">Genellikle **ContentLength** özelliği, **Yöntem** özelliği bir yüklemenin gerçekleştiğini gösteren bir fiil veya eyleme ayarlandığında sıfırdan büyük bir değere ayarlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="371b8-141">PreAuthenticate Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="371b8-142"><xref:System.Net.WebRequest.PreAuthenticate%2A> Uygulamalar, kimlik doğrulama itirazı beklemek yerine kimlik doğrulama bilgilerinin ilk istekle gönderilmesi gerektiğini belirtmek için özelliği ayarlar.</span><span class="sxs-lookup"><span data-stu-id="371b8-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="371b8-143">**PreAuthenticate** özelliği yalnızca protokol ilk istekle birlikte gönderilen kimlik doğrulama kimlik bilgilerini destekliyorsa anlamlıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="371b8-144">Proxy Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-144">Proxy Property</span></span>  
 <span data-ttu-id="371b8-145">Özellik, <xref:System.Net.WebRequest.Proxy%2A> istenen <xref:System.Net.IWebProxy> kaynağa erişmek için kullanılan bir arabirim içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="371b8-146">**Proxy** özelliği yalnızca protokolünüz yakın istekleri destekliyorsa anlamlıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="371b8-147">Protokolünüz tarafından gerekli yse varsayılan proxy'yi ayarlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="371b8-148">Kurumsal güvenlik duvarının arkasında olduğu gibi bazı ortamlarda, iletişim kuralınızın proxy kullanması gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="371b8-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="371b8-149">Bu durumda, protokolünüz için çalışacak bir proxy sınıfı oluşturmak için **IWebProxy** arabirimini uygulamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="371b8-150">Requesturi Mülkiyet</span><span class="sxs-lookup"><span data-stu-id="371b8-150">RequestUri Property</span></span>  
 <span data-ttu-id="371b8-151">Özellik, <xref:System.Net.WebRequest.RequestUri%2A> **WebRequest.Create** yöntemine geçirilen URI'yi içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="371b8-152">Salt okunur ve **Web İsteği** oluşturulduktan sonra değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="371b8-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="371b8-153">Protokolünüz yeniden yönlendirmeyi destekliyorsa, yanıt farklı bir URI tarafından tanımlanan bir kaynaktan gelebilir.</span><span class="sxs-lookup"><span data-stu-id="371b8-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="371b8-154">Yanıtlayan URI'ye erişim sağlamanız gerekiyorsa, bu URI'yi içeren ek bir özellik sağlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="371b8-155">Zaman Ara Özelliği</span><span class="sxs-lookup"><span data-stu-id="371b8-155">Timeout Property</span></span>  
 <span data-ttu-id="371b8-156">Özellik, <xref:System.Net.WebRequest.Timeout%2A> istek zamanları dolmadan önce beklemek ve bir özel durum atar milisaniye cinsinden, süreyi içerir.</span><span class="sxs-lookup"><span data-stu-id="371b8-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="371b8-157">**Zaman alameti** yalnızca <xref:System.Net.WebRequest.GetResponse%2A> yöntemle yapılan eşzamanlı istekler için geçerlidir; asynchronous istekleri bekleyen <xref:System.Net.WebRequest.Abort%2A> bir isteği iptal etmek için yöntemi kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="371b8-158">Zaman **Ayırma** özelliğini ayarlamak, yalnızca protokole özgü sınıf bir zaman ayırma işlemi uygularsa anlamlıdır.</span><span class="sxs-lookup"><span data-stu-id="371b8-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="371b8-159">Abort Yöntemi</span><span class="sxs-lookup"><span data-stu-id="371b8-159">Abort Method</span></span>  
 <span data-ttu-id="371b8-160">Yöntem, <xref:System.Net.WebRequest.Abort%2A> sunucuya bekleyen bir eşzamanlı isteği iptal eder.</span><span class="sxs-lookup"><span data-stu-id="371b8-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="371b8-161">İstek iptal edildikten sonra **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, BeginGetRequestStream <xref:System.Net.WebException> , <xref:System.Net.WebException.Status%2A> veya <xref:System.Net.WebExceptionStatus> **EndGetRequestStream**arama özelliği ile bir atar . **EndGetRequestStream**</span><span class="sxs-lookup"><span data-stu-id="371b8-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="371b8-162">BeginGetRequestStream ve EndGetRequestStream Yöntemleri</span><span class="sxs-lookup"><span data-stu-id="371b8-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="371b8-163">Yöntem, <xref:System.Net.WebRequest.BeginGetRequestStream%2A> sunucuya veri yüklemek için kullanılan akış için bir eşzamanlı istek başlatır.</span><span class="sxs-lookup"><span data-stu-id="371b8-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="371b8-164">Yöntem, <xref:System.Net.WebRequest.EndGetRequestStream%2A> eşzamanlı isteği tamamlar ve istenen akışı döndürür.</span><span class="sxs-lookup"><span data-stu-id="371b8-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="371b8-165">Bu yöntemler, standart .NET Framework asynchronous deseni kullanarak **GetRequestStream** yöntemini uygular.</span><span class="sxs-lookup"><span data-stu-id="371b8-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="371b8-166">BeginGetResponse ve EndGetResponse Yöntemleri</span><span class="sxs-lookup"><span data-stu-id="371b8-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="371b8-167">Yöntem, <xref:System.Net.WebRequest.BeginGetResponse%2A> bir sunucuya bir eşzamanlı istek başlatır.</span><span class="sxs-lookup"><span data-stu-id="371b8-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="371b8-168">Yöntem, <xref:System.Net.WebRequest.EndGetResponse%2A> eşzamanlı isteği tamamlar ve istenen yanıtı döndürür.</span><span class="sxs-lookup"><span data-stu-id="371b8-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="371b8-169">Bu yöntemler, standart .NET Framework asynchronous deseni kullanarak **GetResponse** yöntemini uygular.</span><span class="sxs-lookup"><span data-stu-id="371b8-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="371b8-170">GetRequestStream Yöntemi</span><span class="sxs-lookup"><span data-stu-id="371b8-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="371b8-171">Yöntem, <xref:System.Net.WebRequest.GetRequestStream%2A> istenen sunucuya veri yazmak için kullanılan bir akışı döndürür.</span><span class="sxs-lookup"><span data-stu-id="371b8-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="371b8-172">Döndürülen akış, aramayan yalnızca yazma akışı olmalıdır; sunucuya yazılmış tek yönlü veri akışı olarak tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="371b8-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="371b8-173">Akış, özellikler <xref:System.IO.Stream.CanRead%2A> ve <xref:System.IO.Stream.CanSeek%2A> özellikler için yanlış <xref:System.IO.Stream.CanWrite%2A> ve özellik için doğru döndürür.</span><span class="sxs-lookup"><span data-stu-id="371b8-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="371b8-174">**GetRequestStream** yöntemi genellikle sunucuya bir bağlantı açar ve akışı döndürmeden önce, verilerin sunucuya gönderildiğini gösteren üstbilgi bilgileri gönderir.</span><span class="sxs-lookup"><span data-stu-id="371b8-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="371b8-175">**GetRequestStream** isteği başladığından, **GetRequestStream'i**aradıktan sonra genellikle **üstbilgi** özelliklerini veya **ContentLength** özelliğini ayarlamaya izin verilmez.</span><span class="sxs-lookup"><span data-stu-id="371b8-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="371b8-176">GetResponse Yöntemi</span><span class="sxs-lookup"><span data-stu-id="371b8-176">GetResponse Method</span></span>  
 <span data-ttu-id="371b8-177">Yöntem, <xref:System.Net.WebRequest.GetResponse%2A> sunucudan gelen yanıtı <xref:System.Net.WebResponse> temsil eden sınıfın protokole özgü bir soyundan gelenleri döndürür.</span><span class="sxs-lookup"><span data-stu-id="371b8-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="371b8-178">İstek **GetRequestStream** yöntemi tarafından başlatılmamışsa, **GetResponse** yöntemi **RequestUri**tarafından tanımlanan kaynağa bir bağlantı oluşturur, yapılan istek türünü belirten üstbilgi bilgileri gönderir ve ardından yanıtı kaynaktan alır.</span><span class="sxs-lookup"><span data-stu-id="371b8-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="371b8-179">**GetResponse** yöntemi çağrıldıktan sonra, tüm özellikler salt okunur olarak kabul edilmelidir.</span><span class="sxs-lookup"><span data-stu-id="371b8-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="371b8-180">**WebRequest** örnekleri tek seferlik kullanım için tasarlanmıştır; başka bir istekte bulunmak istiyorsanız, yeni bir **Webİsteği**oluşturmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="371b8-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="371b8-181">**GetResponse** yöntemi, gelen yanıtı içerecek şekilde uygun bir **WebResponse** soyundan gelen oluşturmaktan sorumludur.</span><span class="sxs-lookup"><span data-stu-id="371b8-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="371b8-182">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="371b8-182">See also</span></span>

- <xref:System.Net.WebRequest>
- <xref:System.Net.HttpWebRequest>
- <xref:System.Net.FileWebRequest>
- [<span data-ttu-id="371b8-183">Takılabilir Protokoller Programlama</span><span class="sxs-lookup"><span data-stu-id="371b8-183">Programming Pluggable Protocols</span></span>](programming-pluggable-protocols.md)
- [<span data-ttu-id="371b8-184">WebResponse’tan Türetme</span><span class="sxs-lookup"><span data-stu-id="371b8-184">Deriving from WebResponse</span></span>](deriving-from-webresponse.md)
