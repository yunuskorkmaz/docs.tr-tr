---
title: Bir Hizmetin Kimliğini Kimlik Doğrulama için Geçersiz Kılma
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: d613a22b-07d7-41a4-bada-1adc653b9b5d
ms.openlocfilehash: 3ac2d48962dd96535e1a08310570212121eca986
ms.sourcegitcommit: 27a15a55019f6b5f2733961738babe94aec0def3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/15/2020
ms.locfileid: "90555425"
---
# <a name="override-the-identity-of-a-service-for-authentication"></a><span data-ttu-id="560e6-102">Kimlik doğrulaması için bir hizmetin kimliğini geçersiz kılma</span><span class="sxs-lookup"><span data-stu-id="560e6-102">Override the identity of a service for authentication</span></span>

<span data-ttu-id="560e6-103">Genellikle, bir hizmetin kimliğini ayarlamanız gerekmez, çünkü bir istemci kimlik bilgisi türü seçimi, hizmet meta verilerinde sunulan kimlik türünü belirler.</span><span class="sxs-lookup"><span data-stu-id="560e6-103">Typically, you do not have to set the identity on a service because the selection of a client credential type dictates the type of identity exposed in the service metadata.</span></span> <span data-ttu-id="560e6-104">Örneğin, aşağıdaki yapılandırma kodu [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) öğesini kullanır ve `clientCredentialType` özniteliğini Windows 'a ayarlar.</span><span class="sxs-lookup"><span data-stu-id="560e6-104">For example, the following configuration code uses the [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) element and sets the `clientCredentialType` attribute to Windows.</span></span>  

 <span data-ttu-id="560e6-105">Aşağıdaki Web Hizmetleri Açıklama Dili (WSDL) parçası, daha önce tanımlanan uç noktanın kimliğini gösterir.</span><span class="sxs-lookup"><span data-stu-id="560e6-105">The following Web Services Description Language (WSDL) fragment shows the identity for the endpoint previously defined.</span></span> <span data-ttu-id="560e6-106">Bu örnekte, hizmet belirli bir kullanıcı hesabı () altında kendi kendine barındırılan bir hizmet olarak çalışıyor username@contoso.com ve bu nedenle Kullanıcı asıl adı (UPN) kimliği hesap adını içerir.</span><span class="sxs-lookup"><span data-stu-id="560e6-106">In this example, the service is running as a self-hosted service under a particular user account (username@contoso.com) and therefore the user principal name (UPN) identity contains the account name.</span></span> <span data-ttu-id="560e6-107">UPN, bir Windows etki alanında Kullanıcı oturum açma adı olarak da bilinir.</span><span class="sxs-lookup"><span data-stu-id="560e6-107">The UPN is also known as the user sign-in name in a Windows domain.</span></span>  

 <span data-ttu-id="560e6-108">Kimlik ayarını gösteren örnek bir uygulama için bkz. [hizmet kimliği örneği](../samples/service-identity-sample.md).</span><span class="sxs-lookup"><span data-stu-id="560e6-108">For a sample application that demonstrates identity setting, see [Service Identity Sample](../samples/service-identity-sample.md).</span></span> <span data-ttu-id="560e6-109">Hizmet kimliği hakkında daha fazla bilgi için bkz. [hizmet kimliği ve kimlik doğrulaması](../feature-details/service-identity-and-authentication.md).</span><span class="sxs-lookup"><span data-stu-id="560e6-109">For more information about service identity, see [Service Identity and Authentication](../feature-details/service-identity-and-authentication.md).</span></span>  
  
## <a name="kerberos-authentication-and-identity"></a><span data-ttu-id="560e6-110">Kerberos kimlik doğrulaması ve kimliği</span><span class="sxs-lookup"><span data-stu-id="560e6-110">Kerberos Authentication and Identity</span></span>  
 <span data-ttu-id="560e6-111">Varsayılan olarak, bir hizmet Windows kimlik bilgisi kullanacak şekilde yapılandırıldığında, [\<identity>](../../configure-apps/file-schema/wcf/identity.md) [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) WSDL 'de veya öğesi içeren bir öğe [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="560e6-111">By default, when a service is configured to use a Windows credential, an [\<identity>](../../configure-apps/file-schema/wcf/identity.md) element that contains a [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) or [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element is generated in the WSDL.</span></span> <span data-ttu-id="560e6-112">Hizmet,, veya hesabı altında çalışıyorsa `LocalSystem` , `LocalService` `NetworkService` `host/` \<*hostname*> Bu hesapların bilgisayarın SPN verilerine erişimi olduğundan, varsayılan olarak BIR hizmet asıl adı (SPN) oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="560e6-112">If the service is running under the `LocalSystem`, `LocalService`, or `NetworkService` account, a service principal name (SPN) is generated by default in the form of `host/`\<*hostname*> because those accounts have access to the computer's SPN data.</span></span> <span data-ttu-id="560e6-113">Hizmet farklı bir hesap altında çalışıyorsa, Windows Communication Foundation (WCF) domainName biçiminde bir UPN oluşturur \<*username*> @< *domainName* `>` .</span><span class="sxs-lookup"><span data-stu-id="560e6-113">If the service is running under a different account, Windows Communication Foundation (WCF) generates a UPN in the form of \<*username*>@<*domainName*`>`.</span></span> <span data-ttu-id="560e6-114">Bu durum, Kerberos kimlik doğrulamasının hizmetin kimliğini doğrulamak için bir UPN veya SPN 'nin istemciye sağlanması gerektiğinden oluşur.</span><span class="sxs-lookup"><span data-stu-id="560e6-114">This occurs because Kerberos authentication requires that a UPN or SPN be supplied to the client to authenticate the service.</span></span>  
  
 <span data-ttu-id="560e6-115">[Setspn](/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)) aracını Ayrıca, bir etki alanındaki hizmet hesabına sahip ek bir SPN kaydetmek için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="560e6-115">You can also use the [Setspn](/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/cc731241(v=ws.10)) tool to register an additional SPN with a service's account in a domain.</span></span> <span data-ttu-id="560e6-116">Daha sonra SPN 'YI hizmetin kimliği olarak kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="560e6-116">You can then use the SPN as the identity of the service.</span></span> <span data-ttu-id="560e6-117">Araç hakkında daha fazla bilgi için bkz. [Setspn Overview](/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="560e6-117">For more information about the tool, see [Setspn Overview](/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="560e6-118">Windows kimlik bilgisi türünü anlaşma olmadan kullanmak için, hizmetin Kullanıcı hesabının Active Directory etki alanına kayıtlı SPN 'ye erişimi olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="560e6-118">To use the Windows credential type without negotiation, the service's user account must have access to the SPN that is registered with the Active Directory domain.</span></span> <span data-ttu-id="560e6-119">Bunu aşağıdaki yöntemlerle yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="560e6-119">You can do this in the following ways:</span></span>  
  
- <span data-ttu-id="560e6-120">Hizmetinizi çalıştırmak için NetworkService veya LocalSystem hesabını kullanın.</span><span class="sxs-lookup"><span data-stu-id="560e6-120">Use the NetworkService or LocalSystem account to run your service.</span></span> <span data-ttu-id="560e6-121">Makine Active Directory etki alanına katıldığında, bu hesapların bir makine SPN 'sine erişimi olduğundan, WCF hizmetin meta verilerinde (WSDL) otomatik olarak hizmetin uç noktasındaki uygun SPN öğesini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="560e6-121">Because those accounts have access to the machine SPN that is established when the machine joins the Active Directory domain, WCF automatically generates the proper SPN element inside the service's endpoint in the service's metadata (WSDL).</span></span>  
  
- <span data-ttu-id="560e6-122">Hizmetinizi çalıştırmak için rastgele bir Active Directory etki alanı hesabı kullanın.</span><span class="sxs-lookup"><span data-stu-id="560e6-122">Use an arbitrary Active Directory domain account to run your service.</span></span> <span data-ttu-id="560e6-123">Bu durumda, bu etki alanı hesabı için bir SPN oluşturun ve bu, Setspn.exe yardımcı program aracını kullanarak yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="560e6-123">In this case, establish an SPN for that domain account, which you can do by using the Setspn.exe utility tool.</span></span> <span data-ttu-id="560e6-124">Hizmet hesabı için SPN 'YI oluşturduktan sonra, bu SPN 'YI, meta verileri (WSDL) aracılığıyla hizmetin istemcilerine yayımlamak üzere WCF 'yi yapılandırın.</span><span class="sxs-lookup"><span data-stu-id="560e6-124">Once you create the SPN for the service's account, configure WCF to publish that SPN to the service's clients through its metadata (WSDL).</span></span> <span data-ttu-id="560e6-125">Bu, bir uygulama yapılandırma dosyası ya da kod aracılığıyla, gösterilen uç nokta için uç nokta kimliği ayarlanarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="560e6-125">This is done by setting the endpoint identity for the exposed endpoint, either through an application configuration file or code.</span></span>  
  
 <span data-ttu-id="560e6-126">SPN 'Ler, Kerberos protokolü ve Active Directory hakkında daha fazla bilgi için bkz. [Windows Için Kerberos teknik eki](/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span><span class="sxs-lookup"><span data-stu-id="560e6-126">For more information about SPNs, the Kerberos protocol, and Active Directory, see [Kerberos Technical Supplement for Windows](/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span></span>  
  
### <a name="when-spn-or-upn-equals-the-empty-string"></a><span data-ttu-id="560e6-127">SPN veya UPN boş dizeye eşitse</span><span class="sxs-lookup"><span data-stu-id="560e6-127">When SPN or UPN Equals the Empty String</span></span>  
 <span data-ttu-id="560e6-128">SPN veya UPN 'yi boş bir dizeye eşit olarak ayarlarsanız, kullanılan güvenlik düzeyi ve kimlik doğrulama moduna bağlı olarak bir dizi farklı işlem gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="560e6-128">If you set the SPN or UPN equal to an empty string, a number of different things happen, depending on the security level and authentication mode being used:</span></span>  
  
- <span data-ttu-id="560e6-129">Aktarım düzeyi güvenliği kullanıyorsanız, NT LanMan (NTLM) kimlik doğrulaması seçilir.</span><span class="sxs-lookup"><span data-stu-id="560e6-129">If you are using transport level security, NT LanMan (NTLM) authentication is chosen.</span></span>  
  
- <span data-ttu-id="560e6-130">İleti düzeyi güvenliği kullanıyorsanız kimlik doğrulama moduna bağlı olarak kimlik doğrulaması başarısız olabilir:</span><span class="sxs-lookup"><span data-stu-id="560e6-130">If you are using message level security, authentication may fail, depending on the authentication mode:</span></span>  
  
- <span data-ttu-id="560e6-131">`spnego`Modunu kullanıyorsanız ve `AllowNtlm` özniteliği olarak ayarlandıysa `false` , kimlik doğrulaması başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="560e6-131">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `false`, authentication fails.</span></span>  
  
- <span data-ttu-id="560e6-132">`spnego`Modunu kullanıyorsanız ve `AllowNtlm` özniteliği olarak AYARLANDıYSA `true` , UPN boş ise kimlik doğrulaması başarısız olur ancak SPN boşsa başarılı olur.</span><span class="sxs-lookup"><span data-stu-id="560e6-132">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `true`, authentication fails if the UPN is empty but succeeds if the SPN is empty.</span></span>  
  
- <span data-ttu-id="560e6-133">Kerberos doğrudan ("tek görüntü" olarak da bilinir) kullanıyorsanız, kimlik doğrulaması başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="560e6-133">If you are using Kerberos direct (also known as "one-shot"), authentication fails.</span></span>  
  
### <a name="use-the-identity-element-in-configuration"></a><span data-ttu-id="560e6-134">\<identity>Yapılandırmada öğesini kullanın</span><span class="sxs-lookup"><span data-stu-id="560e6-134">Use the \<identity> Element in Configuration</span></span>  
 <span data-ttu-id="560e6-135">Daha önce olarak gösterilen bağlamadaki istemci kimlik bilgisi türünü değiştirirseniz `Certificate` oluşturulan wsdl, kimlik değeri için aşağıdaki kodda gösterildiği gibi Base64 serileştirilmiş X. 509.440 sertifikası içerir.</span><span class="sxs-lookup"><span data-stu-id="560e6-135">If you change the client credential type in the binding previously shown to `Certificate`, then the generated WSDL contains a Base64 serialized X.509 certificate for the identity value as shown in the following code.</span></span> <span data-ttu-id="560e6-136">Bu, Windows dışındaki tüm istemci kimlik bilgisi türleri için varsayılandır.</span><span class="sxs-lookup"><span data-stu-id="560e6-136">This is the default for all client credential types other than Windows.</span></span>  

 <span data-ttu-id="560e6-137">`identity`Yapılandırma içindeki <> öğesini kullanarak veya koddaki kimliği ayarlayarak, varsayılan hizmet kimliğinin değerini değiştirebilir veya kimliğin türünü değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="560e6-137">You can change the value of the default service identity or change the type of the identity by using the <`identity`> element in configuration or by setting the identity in code.</span></span> <span data-ttu-id="560e6-138">Aşağıdaki yapılandırma kodu, bir etki alanı adı sistemi (DNS) kimliğini değeriyle ayarlar `contoso.com` .</span><span class="sxs-lookup"><span data-stu-id="560e6-138">The following configuration code sets a domain name system (DNS) identity with the value `contoso.com`.</span></span>  

### <a name="set-identity-programmatically"></a><span data-ttu-id="560e6-139">Kimliği programlı olarak ayarla</span><span class="sxs-lookup"><span data-stu-id="560e6-139">Set Identity Programmatically</span></span>  
 <span data-ttu-id="560e6-140">WCF tarafından otomatik olarak belirlediği için hizmetiniz bir kimlik belirtmek zorunda değildir.</span><span class="sxs-lookup"><span data-stu-id="560e6-140">Your service does not have to explicitly specify an identity, because WCF automatically determines it.</span></span> <span data-ttu-id="560e6-141">Ancak, WCF, gerekirse bir uç nokta üzerinde kimlik belirtmenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="560e6-141">However, WCF allows you to specify an identity on an endpoint, if necessary.</span></span> <span data-ttu-id="560e6-142">Aşağıdaki kod, belirli bir DNS kimliğine sahip yeni bir hizmet uç noktası ekler.</span><span class="sxs-lookup"><span data-stu-id="560e6-142">The following code adds a new service endpoint with a specific DNS identity.</span></span>  
  
 [!code-csharp[C_Identity#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_identity/cs/source.cs#5)]
 [!code-vb[C_Identity#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_identity/vb/source.vb#5)]  
  
## <a name="see-also"></a><span data-ttu-id="560e6-143">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="560e6-143">See also</span></span>

- [<span data-ttu-id="560e6-144">Nasıl yapılır: Özel İstemci Kimliği Doğrulayıcı Oluşturma</span><span class="sxs-lookup"><span data-stu-id="560e6-144">How to: Create a Custom Client Identity Verifier</span></span>](how-to-create-a-custom-client-identity-verifier.md)
- [<span data-ttu-id="560e6-145">Kimlik Doğrulama ile Hizmet Kimliği</span><span class="sxs-lookup"><span data-stu-id="560e6-145">Service Identity and Authentication</span></span>](../feature-details/service-identity-and-authentication.md)
