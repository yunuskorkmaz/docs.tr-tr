---
title: Bir ListenUri için Birden Fazla Belirteç
ms.date: 03/30/2017
ms.assetid: 911ffad4-4d47-4430-b7c2-79192ce6bcbd
ms.openlocfilehash: 8e26cc18ed35c446dda120c678dd7e879c756c0f
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79183480"
---
# <a name="multiple-endpoints-at-a-single-listenuri"></a><span data-ttu-id="43498-102">Bir ListenUri için Birden Fazla Belirteç</span><span class="sxs-lookup"><span data-stu-id="43498-102">Multiple Endpoints at a Single ListenUri</span></span>
<span data-ttu-id="43498-103">Bu örnek, tek `ListenUri`bir noktada birden çok uç noktayı barındıran bir hizmeti gösterir.</span><span class="sxs-lookup"><span data-stu-id="43498-103">This sample demonstrates a service that hosts multiple endpoints at a single `ListenUri`.</span></span> <span data-ttu-id="43498-104">Bu örnek, bir hesap makinesi hizmeti uygulayan [Başlarken'e](../../../../docs/framework/wcf/samples/getting-started-sample.md) dayanır.</span><span class="sxs-lookup"><span data-stu-id="43498-104">This sample is based on the [Getting Started](../../../../docs/framework/wcf/samples/getting-started-sample.md) that implements a calculator service.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="43498-105">Bu örnek için kurulum yordamı ve yapı yönergeleri bu konunun sonunda yer alır.</span><span class="sxs-lookup"><span data-stu-id="43498-105">The setup procedure and build instructions for this sample are located at the end of this topic.</span></span>  
  
 <span data-ttu-id="43498-106">[Birden çok Uç Nokta](../../../../docs/framework/wcf/samples/multiple-endpoints.md) örneğinde gösterildiği gibi, bir hizmet her biri farklı adreslere ve büyük olasılıkla farklı bağlamalara sahip birden çok uç nokta barındırabilir.</span><span class="sxs-lookup"><span data-stu-id="43498-106">As demonstrated in the [Multiple Endpoints](../../../../docs/framework/wcf/samples/multiple-endpoints.md) sample, a service can host multiple endpoints, each with different addresses and possibly also different bindings.</span></span> <span data-ttu-id="43498-107">Bu örnek, aynı adreste birden çok uç nokta barındırmanın mümkün olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="43498-107">This sample shows that it is possible to host multiple endpoints at the same address.</span></span> <span data-ttu-id="43498-108">Bu örnek, bir hizmet bitiş noktasının sahip olduğu iki tür `EndpointAddress` adres `ListenUri`arasındaki farkları da gösterir: ve .</span><span class="sxs-lookup"><span data-stu-id="43498-108">This sample also demonstrates the differences between the two kinds of addresses that a service endpoint has: `EndpointAddress` and `ListenUri`.</span></span>  
  
 <span data-ttu-id="43498-109">Bir `EndpointAddress` hizmetin mantıksal adresidir.</span><span class="sxs-lookup"><span data-stu-id="43498-109">The `EndpointAddress` is the logical address of a service.</span></span> <span data-ttu-id="43498-110">SOAP mesajlarının adresolduğu adrestir.</span><span class="sxs-lookup"><span data-stu-id="43498-110">It is the address that SOAP messages are addressed to.</span></span> <span data-ttu-id="43498-111">Hizmetin `ListenUri` fiziksel adresidir.</span><span class="sxs-lookup"><span data-stu-id="43498-111">The `ListenUri` is the physical address of the service.</span></span> <span data-ttu-id="43498-112">Hizmet bitiş noktasının geçerli makinedeki iletileri gerçekten dinlediği bağlantı noktası ve adres bilgilerine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="43498-112">It has the port and address information where the service endpoint actually listens for messages on the current machine.</span></span> <span data-ttu-id="43498-113">Çoğu durumda, bu adreslerin farklı olmasına gerek yoktur; a `ListenUri` açıkça belirtilmemişse, bitiş noktasının `EndpointAddress` URI'sine varsayılan olarak</span><span class="sxs-lookup"><span data-stu-id="43498-113">In most cases, there is no need for these addresses to differ; when a `ListenUri` is not explicitly specified, it defaults to the URI of the `EndpointAddress` of the endpoint.</span></span> <span data-ttu-id="43498-114">Birkaç durumda, bir yönlendirici, farklı hizmetler bir dizi adresli iletileri kabul edebilir yapılandırma gibi bunları ayırt etmek yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="43498-114">In a few cases, it is useful to distinguish them, such as when configuring a router, which might accept messages addressed to a number of different services.</span></span>  
  
## <a name="service"></a><span data-ttu-id="43498-115">Hizmet</span><span class="sxs-lookup"><span data-stu-id="43498-115">Service</span></span>  
 <span data-ttu-id="43498-116">Bu örnekteki hizmetin iki `ICalculator` `IEcho`sözleşmesi vardır ve .</span><span class="sxs-lookup"><span data-stu-id="43498-116">The service in this sample has two contracts, `ICalculator` and `IEcho`.</span></span> <span data-ttu-id="43498-117">Alışılmış `IMetadataExchange` bitiş noktasına ek olarak, aşağıdaki kodda gösterildiği gibi üç uygulama bitiş noktası vardır.</span><span class="sxs-lookup"><span data-stu-id="43498-117">In addition to the customary `IMetadataExchange` endpoint, there are three application endpoints, as shown in the following code.</span></span>  
  
```xml  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.ICalculator"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:OtherEcho"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
```  
  
 <span data-ttu-id="43498-118">Her üç uç nokta da `ListenUri` aynı şekilde `binding` barındırılır ve `ListenUri` aynı uç noktaları kullanır - aynı uç noktalar aynı bağlamaya sahip olmalıdır, çünkü makinedeki fiziksel adresteki iletileri dinleyen tek bir kanal yığınını paylaşıyorlar.</span><span class="sxs-lookup"><span data-stu-id="43498-118">All three endpoints are hosted at the same `ListenUri` and use the same `binding` - endpoints at the same `ListenUri` must have the same binding, because they are sharing a single channel stack that listens for messages at that physical address on the machine.</span></span> <span data-ttu-id="43498-119">Her `address` bitiş noktasının bir URN'si; genellikle adresler fiziksel konumları temsil etse de, adres bu örnekte gösterildiği gibi eşleştirme ve filtreleme amacıyla kullanıldığından, adres her türlü URI olabilir.</span><span class="sxs-lookup"><span data-stu-id="43498-119">The `address` of each endpoint is a URN; though typically addresses represent physical locations, in fact the address can be any kind of URI, because the address is used for matching and filtering purposes as is demonstrated in this sample.</span></span>  
  
 <span data-ttu-id="43498-120">Üç uç nokta da `ListenUri`aynı olduğundan , bir ileti geldiğinde, Windows Communication Foundation (WCF) iletinin kaderinde olan bitiş noktasına karar vermelidir.</span><span class="sxs-lookup"><span data-stu-id="43498-120">Because all three endpoints share the same `ListenUri`, when a message arrives there, Windows Communication Foundation (WCF) must decide which endpoint the message is destined for.</span></span> <span data-ttu-id="43498-121">Her uç noktanın iki bölümden oluşan bir ileti filtresi vardır: adres filtresi ve sözleşme filtresi.</span><span class="sxs-lookup"><span data-stu-id="43498-121">Each endpoint has a message filter that is comprised of two parts: the address filter and the contract filter.</span></span> <span data-ttu-id="43498-122">Adres filtresi, `To` SOAP iletisinin hizmet bitiş noktasının adresiyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="43498-122">The address filter matches the `To` of the SOAP message to the address of the service endpoint.</span></span> <span data-ttu-id="43498-123">Örneğin, yalnızca bu hizmetin üçüncü bitiş noktası için adaylar yalnızca adreslenir. `To "Urn:OtherEcho"`</span><span class="sxs-lookup"><span data-stu-id="43498-123">For example, only messages addressed `To "Urn:OtherEcho"` are candidates for the third endpoint of this service.</span></span> <span data-ttu-id="43498-124">Sözleşme filtresi, belirli bir sözleşmenin işlemleriyle ilişkili Eylemlerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="43498-124">The contract filter matches the Actions associated with the operations of a particular contract.</span></span> <span data-ttu-id="43498-125">Örneğin, eylemi ile iletileri `IEcho`.</span><span class="sxs-lookup"><span data-stu-id="43498-125">For example, messages with the action of `IEcho`.</span></span> <span data-ttu-id="43498-126">`Echo`bu uç noktaların her ikisi de sözleşmeyi barındırdığından, bu hizmetin `IEcho` hem ikinci hem de üçüncü uç noktalarının sözleşme filtrelerini eşleşir.</span><span class="sxs-lookup"><span data-stu-id="43498-126">`Echo` matches the contract filters of both the second and third endpoints of this service, because both of those endpoints host the `IEcho` contract.</span></span>  
  
 <span data-ttu-id="43498-127">Böylece adres filtresi ve sözleşme filtresi nin birleşimi, bu hizmete `ListenUri` gelen her iletiyi doğru bitiş noktasına yönlendirmeyi mümkün kılar.</span><span class="sxs-lookup"><span data-stu-id="43498-127">Thus the combination of address filter and contract filter makes it possible to route each message that arrives at this service's `ListenUri` to the correct endpoint.</span></span> <span data-ttu-id="43498-128">Üçüncü uç nokta, diğer uç noktalardan farklı bir adrese gönderilen iletileri kabul ettiği için diğer ikisinden ayrılır.</span><span class="sxs-lookup"><span data-stu-id="43498-128">The third endpoint is differentiated from the other two because it accepts messages sent to a different address from the other endpoints.</span></span> <span data-ttu-id="43498-129">Birinci ve ikinci uç noktalar, sözleşmelerine (gelen iletinin Eylemi) göre birbirinden ayrılır.</span><span class="sxs-lookup"><span data-stu-id="43498-129">The first and second endpoints are differentiated from each other based on their contracts (the Action of the incoming message).</span></span>  
  
## <a name="client"></a><span data-ttu-id="43498-130">İstemci</span><span class="sxs-lookup"><span data-stu-id="43498-130">Client</span></span>  
 <span data-ttu-id="43498-131">Sunucudaki uç noktaların iki farklı adresi olduğu gibi, istemci uç noktalarının da iki adresi vardır.</span><span class="sxs-lookup"><span data-stu-id="43498-131">Just as endpoints on the server have two different addresses, client endpoints also have two addresses.</span></span> <span data-ttu-id="43498-132">Hem sunucuda hem de istemcide, `EndpointAddress`mantıksal adres .</span><span class="sxs-lookup"><span data-stu-id="43498-132">On both server and client, the logical address is called the `EndpointAddress`.</span></span> <span data-ttu-id="43498-133">Ancak fiziksel adres `ListenUri` sunucuda, istemcide ise fiziksel adrese . `Via`</span><span class="sxs-lookup"><span data-stu-id="43498-133">But whereas the physical address is called the `ListenUri` on the server, on the client, the physical address is called the `Via`.</span></span>  
  
 <span data-ttu-id="43498-134">Sunucuda olduğu gibi, varsayılan olarak, bu iki adres aynıdır.</span><span class="sxs-lookup"><span data-stu-id="43498-134">As on the server, by default, these two addresses are the same.</span></span> <span data-ttu-id="43498-135">Uç noktanın adresinden `Via` `ClientViaBehavior` farklı bir istemci üzerinde bir belirtmek için kullanılır:</span><span class="sxs-lookup"><span data-stu-id="43498-135">To specify a `Via` on the client that is different from the endpoint's address, `ClientViaBehavior` is used:</span></span>  
  
```csharp  
Uri via = new Uri("http://localhost/ServiceModelSamples/service.svc");  
CalculatorClient calcClient = new CalculatorClient();  
calcClient.ChannelFactory.Endpoint.Behaviors.Add(  
        new ClientViaBehavior(via));  
```  
  
 <span data-ttu-id="43498-136">Her zamanki gibi, adres Svcutil.exe tarafından oluşturulan istemci yapılandırma dosyasından gelir.</span><span class="sxs-lookup"><span data-stu-id="43498-136">As usual, the address comes from the client configuration file, which was generated by Svcutil.exe.</span></span> <span data-ttu-id="43498-137">`Via` (Hizmetin `ListenUri` karşısına karşılık gelen) hizmetin meta verilerinde görünmez ve bu nedenle bu bilgilerin bant dışı istemciye iletilmesi gerekir (hizmetin meta veri adresi gibi).</span><span class="sxs-lookup"><span data-stu-id="43498-137">The `Via` (which corresponds to the `ListenUri` of the service) does not appear in the service's metadata and so this information must be communicated to the client out-of-band (just like the service's metadata address).</span></span>  
  
 <span data-ttu-id="43498-138">Bu örnekteki istemci, sunucunun üç uygulama uç noktasının her birine, hepsi aynı `Via`olmasına rağmen üç uç noktayla da iletişim kurabileceğini (ve ayırt edebildiği) iletiler gönderir.</span><span class="sxs-lookup"><span data-stu-id="43498-138">The client in this sample sends messages to each of the server's three application endpoints, to demonstrate that it can communicate with (and differentiate) all three endpoints, even though they all have the same `Via`.</span></span>  
  
#### <a name="to-set-up-build-and-run-the-sample"></a><span data-ttu-id="43498-139">Örneği ayarlamak, oluşturmak ve çalıştırmak için</span><span class="sxs-lookup"><span data-stu-id="43498-139">To set up, build, and run the sample</span></span>  
  
1. <span data-ttu-id="43498-140">Windows Communication Foundation [Samples için Tek Seferlik Kurulum Yordamı'nı](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md)gerçekleştirdiğinizi emin olun.</span><span class="sxs-lookup"><span data-stu-id="43498-140">Ensure that you have performed the [One-Time Setup Procedure for the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md).</span></span>  
  
2. <span data-ttu-id="43498-141">Çözümün C# veya Visual Basic .NET sürümünü oluşturmak [için, Windows Communication Foundation Samples'i oluştururken](../../../../docs/framework/wcf/samples/building-the-samples.md)yönergeleri izleyin.</span><span class="sxs-lookup"><span data-stu-id="43498-141">To build the C# or Visual Basic .NET edition of the solution, follow the instructions in [Building the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/building-the-samples.md).</span></span>  
  
3. <span data-ttu-id="43498-142">Örneği tek veya çapraz makine yapılandırmasında çalıştırmak için, [Windows Communication Foundation Samples'ı çalıştıran](../../../../docs/framework/wcf/samples/running-the-samples.md)yönergeleri izleyin.</span><span class="sxs-lookup"><span data-stu-id="43498-142">To run the sample in a single- or cross-machine configuration, follow the instructions in [Running the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/running-the-samples.md).</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="43498-143">Çapraz makine için, Client.cs dosyasındaki localhost'u servis makinesinin adı ile değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="43498-143">For cross-machine, you must replace localhost in the Client.cs file with the name of the service machine.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="43498-144">Numuneler makinenize zaten yüklenmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="43498-144">The samples may already be installed on your machine.</span></span> <span data-ttu-id="43498-145">Devam etmeden önce aşağıdaki (varsayılan) dizini denetleyin.</span><span class="sxs-lookup"><span data-stu-id="43498-145">Check for the following (default) directory before continuing.</span></span>  
>
> `<InstallDrive>:\WF_WCF_Samples`  
>
> <span data-ttu-id="43498-146">Bu dizin yoksa, tüm Windows Communication Foundation (WCF) ve örneklerini indirmek için .NET Framework 4 için Windows Communication [!INCLUDE[wf1](../../../../includes/wf1-md.md)] Foundation [(WCF) ve Windows İş Akışı Temeli (WF) Örneklerine](https://www.microsoft.com/download/details.aspx?id=21459) gidin.</span><span class="sxs-lookup"><span data-stu-id="43498-146">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](https://www.microsoft.com/download/details.aspx?id=21459) to download all Windows Communication Foundation (WCF) and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="43498-147">Bu örnek aşağıdaki dizinde yer almaktadır.</span><span class="sxs-lookup"><span data-stu-id="43498-147">This sample is located in the following directory.</span></span>  
>
> `<InstallDrive>:\WF_WCF_Samples\WCF\Basic\Services\MultipleEndpointsSingleUri`  
