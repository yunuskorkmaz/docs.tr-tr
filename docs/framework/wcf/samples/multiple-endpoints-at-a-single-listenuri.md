---
title: Bir ListenUri için Birden Fazla Belirteç
ms.date: 03/30/2017
ms.assetid: 911ffad4-4d47-4430-b7c2-79192ce6bcbd
ms.openlocfilehash: 6249690b7fdc95affd21eee13e0c6e2af1c4f8a0
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/09/2019
ms.locfileid: "59312364"
---
# <a name="multiple-endpoints-at-a-single-listenuri"></a><span data-ttu-id="0440e-102">Bir ListenUri için Birden Fazla Belirteç</span><span class="sxs-lookup"><span data-stu-id="0440e-102">Multiple Endpoints at a Single ListenUri</span></span>
<span data-ttu-id="0440e-103">Bu örnek, birden çok uç tek bir noktada barındıran bir hizmeti gösterir `ListenUri`.</span><span class="sxs-lookup"><span data-stu-id="0440e-103">This sample demonstrates a service that hosts multiple endpoints at a single `ListenUri`.</span></span> <span data-ttu-id="0440e-104">Bu örnek dayanır [Başlarken](../../../../docs/framework/wcf/samples/getting-started-sample.md) hesaplayıcı hizmet uygulayan.</span><span class="sxs-lookup"><span data-stu-id="0440e-104">This sample is based on the [Getting Started](../../../../docs/framework/wcf/samples/getting-started-sample.md) that implements a calculator service.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="0440e-105">Bu örnek için Kurulum yordamı ve derleme yönergelerini, bu konunun sonunda yer alır.</span><span class="sxs-lookup"><span data-stu-id="0440e-105">The setup procedure and build instructions for this sample are located at the end of this topic.</span></span>  
  
 <span data-ttu-id="0440e-106">Örnekte gösterildiği şekilde [birden fazla uç nokta](../../../../docs/framework/wcf/samples/multiple-endpoints.md) örnek, birden fazla uç noktası, her farklı adresleri ve belki de farklı bağlamaları ile bir hizmeti barındırabilir.</span><span class="sxs-lookup"><span data-stu-id="0440e-106">As demonstrated in the [Multiple Endpoints](../../../../docs/framework/wcf/samples/multiple-endpoints.md) sample, a service can host multiple endpoints, each with different addresses and possibly also different bindings.</span></span> <span data-ttu-id="0440e-107">Bu örnek, birden fazla uç nokta aynı adresten barındırmak mümkün olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="0440e-107">This sample shows that it is possible to host multiple endpoints at the same address.</span></span> <span data-ttu-id="0440e-108">Bu örnek ayrıca iki tür hizmet uç noktası olan adresler arasındaki farklar gösterir: `EndpointAddress` ve `ListenUri`.</span><span class="sxs-lookup"><span data-stu-id="0440e-108">This sample also demonstrates the differences between the two kinds of addresses that a service endpoint has: `EndpointAddress` and `ListenUri`.</span></span>  
  
 <span data-ttu-id="0440e-109">`EndpointAddress` Hizmet mantıksal adresidir.</span><span class="sxs-lookup"><span data-stu-id="0440e-109">The `EndpointAddress` is the logical address of a service.</span></span> <span data-ttu-id="0440e-110">SOAP iletilerini ele alınır adresidir.</span><span class="sxs-lookup"><span data-stu-id="0440e-110">It is the address that SOAP messages are addressed to.</span></span> <span data-ttu-id="0440e-111">`ListenUri` Hizmet fiziksel adresidir.</span><span class="sxs-lookup"><span data-stu-id="0440e-111">The `ListenUri` is the physical address of the service.</span></span> <span data-ttu-id="0440e-112">Bağlantı noktası ve adres bilgilerini, burada hizmet uç noktası gerçekten geçerli makine iletileri dinler sahiptir.</span><span class="sxs-lookup"><span data-stu-id="0440e-112">It has the port and address information where the service endpoint actually listens for messages on the current machine.</span></span> <span data-ttu-id="0440e-113">Çoğu durumda, farklı bu adresler için gerek yoktur; olduğunda bir `ListenUri` açıkça belirtilmezse, URI değerini varsayılan olarak `EndpointAddress` uç nokta.</span><span class="sxs-lookup"><span data-stu-id="0440e-113">In most cases, there is no need for these addresses to differ; when a `ListenUri` is not explicitly specified, it defaults to the URI of the `EndpointAddress` of the endpoint.</span></span> <span data-ttu-id="0440e-114">Bazı durumlarda, iletileri kabul edebilirsiniz bir yönlendirici yapılandırma birtakım farklı hizmetler ele gibi bunları ayırt etmek kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="0440e-114">In a few cases, it is useful to distinguish them, such as when configuring a router, which might accept messages addressed to a number of different services.</span></span>  
  
## <a name="service"></a><span data-ttu-id="0440e-115">Hizmet</span><span class="sxs-lookup"><span data-stu-id="0440e-115">Service</span></span>  
 <span data-ttu-id="0440e-116">Bu hizmette iki sözleşmeleri olduğunu `ICalculator` ve `IEcho`.</span><span class="sxs-lookup"><span data-stu-id="0440e-116">The service in this sample has two contracts, `ICalculator` and `IEcho`.</span></span> <span data-ttu-id="0440e-117">Ek olarak her zamanki `IMetadataExchange` uç noktası, aşağıdaki kodda gösterildiği gibi üç uygulama uç noktaları olan.</span><span class="sxs-lookup"><span data-stu-id="0440e-117">In addition to the customary `IMetadataExchange` endpoint, there are three application endpoints, as shown in the following code.</span></span>  
  
```xml  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.ICalculator"   
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:Stuff"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"   
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
<endpoint address="urn:OtherEcho"  
        binding="wsHttpBinding"  
        contract="Microsoft.ServiceModel.Samples.IEcho"   
        listenUri="http://localhost/servicemodelsamples/service.svc" />  
```  
  
 <span data-ttu-id="0440e-118">Üç tüm uç noktalar aynı anda barındırılan `ListenUri` ve aynı `binding` -bitiş noktaları aynı `ListenUri` fiziksel Bu adresteki iletileri için dinlediği bir tek kanal yığını paylaşan çünkü aynı bağlama sahip olmalıdır Makine.</span><span class="sxs-lookup"><span data-stu-id="0440e-118">All three endpoints are hosted at the same `ListenUri` and use the same `binding` - endpoints at the same `ListenUri` must have the same binding, because they are sharing a single channel stack that listens for messages at that physical address on the machine.</span></span> <span data-ttu-id="0440e-119">`address` Her uç noktasını bir URN; genellikle fiziksel konumlara adresleri temsil eder, ancak eşleştirme ve bu örnekte gösterildiği gibi filtreleme amaçlı adresi kullanıldığından aslında adresi URI, herhangi bir türden olabilir.</span><span class="sxs-lookup"><span data-stu-id="0440e-119">The `address` of each endpoint is a URN; though typically addresses represent physical locations, in fact the address can be any kind of URI, because the address is used for matching and filtering purposes as is demonstrated in this sample.</span></span>  
  
 <span data-ttu-id="0440e-120">Üç tüm uç noktalar aynı paylaştığından `ListenUri`, ileti geldiğinde Windows Communication Foundation (WCF) ileti için atanmış hangi uç noktaya, karar vermeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="0440e-120">Because all three endpoints share the same `ListenUri`, when a message arrives there, Windows Communication Foundation (WCF) must decide which endpoint the message is destined for.</span></span> <span data-ttu-id="0440e-121">İki bölümden oluşan bir ileti filtresi her uç nokta vardır: adres filtresi ve sözleşme Filtresi.</span><span class="sxs-lookup"><span data-stu-id="0440e-121">Each endpoint has a message filter that is comprised of two parts: the address filter and the contract filter.</span></span> <span data-ttu-id="0440e-122">Eşleşen adresi filtre `To` hizmet uç noktası adresine SOAP iletisi.</span><span class="sxs-lookup"><span data-stu-id="0440e-122">The address filter matches the `To` of the SOAP message to the address of the service endpoint.</span></span> <span data-ttu-id="0440e-123">Örneğin, yalnızca iletileri ele `To "Urn:OtherEcho"` bu hizmetin üçüncü uç noktası için adaylardır.</span><span class="sxs-lookup"><span data-stu-id="0440e-123">For example, only messages addressed `To "Urn:OtherEcho"` are candidates for the third endpoint of this service.</span></span> <span data-ttu-id="0440e-124">Sözleşme filtre, belirli bir sözleşme işlemleriyle ilişkili eylemler eşleşir.</span><span class="sxs-lookup"><span data-stu-id="0440e-124">The contract filter matches the Actions associated with the operations of a particular contract.</span></span> <span data-ttu-id="0440e-125">Örneğin, eylemi ile iletileri `IEcho`.</span><span class="sxs-lookup"><span data-stu-id="0440e-125">For example, messages with the action of `IEcho`.</span></span> `Echo` <span data-ttu-id="0440e-126">çünkü bu hizmet uç noktaları ikinci ve üçüncü sözleşme filtrelerle eşleşen her ikisi de bu uç noktaları konak `IEcho` sözleşme.</span><span class="sxs-lookup"><span data-stu-id="0440e-126">matches the contract filters of both the second and third endpoints of this service, because both of those endpoints host the `IEcho` contract.</span></span>  
  
 <span data-ttu-id="0440e-127">Bu nedenle adresi filtresi ve sözleşme filtresi birleşimi, bu hizmetin ulaşan her ileti yolu mümkün kılar `ListenUri` doğru uç noktaya.</span><span class="sxs-lookup"><span data-stu-id="0440e-127">Thus the combination of address filter and contract filter makes it possible to route each message that arrives at this service's `ListenUri` to the correct endpoint.</span></span> <span data-ttu-id="0440e-128">Diğer uç noktalardan gelen farklı bir adrese gönderilen iletileri kabul ettiğinden üçüncü uç noktası diğer iki Ayrıştırılan.</span><span class="sxs-lookup"><span data-stu-id="0440e-128">The third endpoint is differentiated from the other two because it accepts messages sent to a different address from the other endpoints.</span></span> <span data-ttu-id="0440e-129">Birinci ve ikinci uç noktalarını birbirinden sözleşmelerine (gelen ileti eylem) göre ayrılır.</span><span class="sxs-lookup"><span data-stu-id="0440e-129">The first and second endpoints are differentiated from each other based on their contracts (the Action of the incoming message).</span></span>  
  
## <a name="client"></a><span data-ttu-id="0440e-130">İstemci</span><span class="sxs-lookup"><span data-stu-id="0440e-130">Client</span></span>  
 <span data-ttu-id="0440e-131">Yalnızca sunucu uç noktalarda iki farklı adres olduğuna göre istemci uç noktalarını da iki adreslerine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="0440e-131">Just as endpoints on the server have two different addresses, client endpoints also have two addresses.</span></span> <span data-ttu-id="0440e-132">Hem sunucu hem de istemci mantıksal adresi çağrılır `EndpointAddress`.</span><span class="sxs-lookup"><span data-stu-id="0440e-132">On both server and client, the logical address is called the `EndpointAddress`.</span></span> <span data-ttu-id="0440e-133">Ancak fiziksel adresiyle çağrılır ancak `ListenUri` fiziksel adresiyle sunucuda, istemcide çağrılır `Via`.</span><span class="sxs-lookup"><span data-stu-id="0440e-133">But whereas the physical address is called the `ListenUri` on the server, on the client, the physical address is called the `Via`.</span></span>  
  
 <span data-ttu-id="0440e-134">Sunucuda gibi varsayılan olarak, bu iki adres aynıdır.</span><span class="sxs-lookup"><span data-stu-id="0440e-134">As on the server, by default, these two addresses are the same.</span></span> <span data-ttu-id="0440e-135">Belirtmek için bir `Via` uç noktanın adresinden farklı istemcide `ClientViaBehavior` kullanılır:</span><span class="sxs-lookup"><span data-stu-id="0440e-135">To specify a `Via` on the client that is different from the endpoint's address, `ClientViaBehavior` is used:</span></span>  
  
```csharp  
Uri via = new Uri("http://localhost/ServiceModelSamples/service.svc");  
CalculatorClient calcClient = new CalculatorClient();  
calcClient.ChannelFactory.Endpoint.Behaviors.Add(  
        new ClientViaBehavior(via));  
```  
  
 <span data-ttu-id="0440e-136">Her zamanki şekilde svcutil.exe'yi oluşturulan istemci yapılandırma dosyasından adresi gelir.</span><span class="sxs-lookup"><span data-stu-id="0440e-136">As usual, the address comes from the client configuration file, which was generated by Svcutil.exe.</span></span> <span data-ttu-id="0440e-137">`Via` (Karşılık gelen `ListenUri` hizmetinin) hizmetin meta verilerinde görünmez ve bu bilgileri istemci-bant (olduğu gibi hizmetin meta veri adresi) bildirilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="0440e-137">The `Via` (which corresponds to the `ListenUri` of the service) does not appear in the service's metadata and so this information must be communicated to the client out-of-band (just like the service's metadata address).</span></span>  
  
 <span data-ttu-id="0440e-138">Bu örnekte istemci iletiler gönderen her sunucunun üç uygulama uç noktaları için BT'nin göstermek için kullanabilirsiniz ile iletişim (ve birbirinden) üç tüm uç noktalar, bunların tümü aynı olmasına rağmen `Via`.</span><span class="sxs-lookup"><span data-stu-id="0440e-138">The client in this sample sends messages to each of the server's three application endpoints, to demonstrate that it can communicate with (and differentiate) all three endpoints, even though they all have the same `Via`.</span></span>  
  
#### <a name="to-set-up-build-and-run-the-sample"></a><span data-ttu-id="0440e-139">Ayarlamak için derleme ve örneği çalıştırma</span><span class="sxs-lookup"><span data-stu-id="0440e-139">To set up, build, and run the sample</span></span>  
  
1. <span data-ttu-id="0440e-140">Gerçekleştirdiğinizden emin olmak [Windows Communication Foundation örnekleri için bir kerelik Kurulum yordamı](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md).</span><span class="sxs-lookup"><span data-stu-id="0440e-140">Ensure that you have performed the [One-Time Setup Procedure for the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/one-time-setup-procedure-for-the-wcf-samples.md).</span></span>  
  
2. <span data-ttu-id="0440e-141">Çözüm C# veya Visual Basic .NET sürümünü oluşturmak için yönergeleri izleyin. [Windows Communication Foundation örnekleri derleme](../../../../docs/framework/wcf/samples/building-the-samples.md).</span><span class="sxs-lookup"><span data-stu-id="0440e-141">To build the C# or Visual Basic .NET edition of the solution, follow the instructions in [Building the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/building-the-samples.md).</span></span>  
  
3. <span data-ttu-id="0440e-142">Tek veya çapraz makine yapılandırmasında örneği çalıştırmak için yönergeleri izleyin. [Windows Communication Foundation örneklerini çalıştırma](../../../../docs/framework/wcf/samples/running-the-samples.md).</span><span class="sxs-lookup"><span data-stu-id="0440e-142">To run the sample in a single- or cross-machine configuration, follow the instructions in [Running the Windows Communication Foundation Samples](../../../../docs/framework/wcf/samples/running-the-samples.md).</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="0440e-143">Çapraz makine için Client.cs dosyasındaki localhost hizmeti makine adını değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="0440e-143">For cross-machine, you must replace localhost in the Client.cs file with the name of the service machine.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="0440e-144">Örnekler, makinenizde zaten yüklü.</span><span class="sxs-lookup"><span data-stu-id="0440e-144">The samples may already be installed on your machine.</span></span> <span data-ttu-id="0440e-145">Devam etmeden önce şu (varsayılan) dizin denetleyin.</span><span class="sxs-lookup"><span data-stu-id="0440e-145">Check for the following (default) directory before continuing.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples`  
>   
>  <span data-ttu-id="0440e-146">Bu dizin mevcut değilse Git [Windows Communication Foundation (WCF) ve .NET Framework 4 için Windows Workflow Foundation (WF) örnekleri](https://go.microsoft.com/fwlink/?LinkId=150780) tüm Windows Communication Foundation (WCF) indirmek için ve [!INCLUDE[wf1](../../../../includes/wf1-md.md)] örnekleri.</span><span class="sxs-lookup"><span data-stu-id="0440e-146">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](https://go.microsoft.com/fwlink/?LinkId=150780) to download all Windows Communication Foundation (WCF) and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="0440e-147">Bu örnek, şu dizinde bulunur.</span><span class="sxs-lookup"><span data-stu-id="0440e-147">This sample is located in the following directory.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples\WCF\Basic\Services\MultipleEndpointsSingleUri`  
