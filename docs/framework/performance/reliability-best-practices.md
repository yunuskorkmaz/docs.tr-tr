---
title: "Güvenilirlik En İyi Yöntemleri"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- marking locks
- rebooting databases
- denial of service attacks
- back-out code
- SQL Server [.NET Framework], reliability
- synchronization, reliability
- single-threaded COM components
- slow leaks
- suspending threads
- asynchronous exception handling
- leaked resources [.NET Framework]
- unmanaged memory
- memory, reliability
- threading [.NET Framework], reliability
- process-wide domain shared states
- shared states
- SafeHandle class, reliability
- reliability contracts [.NET Framework]
- cleanup operations
- constrained execution regions
- CERs
- finalizers, reliability
- reliability [.NET Framework]
- blocks, reliability
- finally clauses
- cross-application domain shared states
- catch blocks
- identifying locks
- writing reliable code
- impersonation
- GC.KeepAlive method
- managed threading
- locks, reliability
- STA-dependent features
- fibers
ms.assetid: cf624c1f-c160-46a1-bb2b-213587688da7
caps.latest.revision: "11"
author: mairaw
ms.author: mairaw
manager: wpickett
ms.openlocfilehash: 5ed637cd5d173e12114f436b739ce3c114bb420f
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/21/2017
---
# <a name="reliability-best-practices"></a><span data-ttu-id="59bfc-102">Güvenilirlik En İyi Yöntemleri</span><span class="sxs-lookup"><span data-stu-id="59bfc-102">Reliability Best Practices</span></span>
<span data-ttu-id="59bfc-103">Aşağıdaki güvenilirlik kurallar SQL Server'a yerleştirilir; Ancak, bunlar da herhangi bir ana bilgisayar tabanlı sunucu uygulama için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-103">The following reliability rules are oriented to SQL Server; however, they also apply to any host-based server application.</span></span> <span data-ttu-id="59bfc-104">SQL Server gibi sunucu kaynakları sızıntısı değil ve duruma getirilmesi değil, son derece önemlidir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-104">It is extremely important that servers such as SQL Server not leak resources and not be brought down.</span></span>  <span data-ttu-id="59bfc-105">Ancak, bir nesnenin durumu değiştirir her yöntemi için geri çekme kodu yazarak yapılamaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-105">However, that cannot be done by writing back-out code for every method that alters an object’s state.</span></span>  <span data-ttu-id="59bfc-106">Hedef geri çekme kodu her konumdaki tüm hataları kurtarır yüzde 100 güvenilir yönetilen kodu değil yazmaktır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-106">The goal is not to write 100 percent reliable managed code that will recover from any errors in every location with back-out code.</span></span>  <span data-ttu-id="59bfc-107">Çok az fırsat başarı ile zorlu bir görev olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-107">That would be a daunting task with little chance of success.</span></span>  <span data-ttu-id="59bfc-108">Ortak dil çalışma zamanı (CLR), yönetilen kod için yeterince güçlü garanti kusursuz kod uygun yazma yapmak için kolayca sağlayamaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-108">The common language runtime (CLR) cannot easily provide strong enough guarantees to managed code to make writing perfect code feasible.</span></span>  <span data-ttu-id="59bfc-109">ASP.NET farklı olarak, SQL Server veritabanı edilemeyecek uzunlukta bir süre boyunca sürüyor olmadan geri dönüştürüldüğünde olamaz yalnızca bir işlem kullandığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-109">Note that unlike ASP.NET, SQL Server uses only one process that cannot be recycled without taking a database down for an unacceptably long time.</span></span>  
  
 <span data-ttu-id="59bfc-110">Bu zayıf garantiler ve tek bir işlemde çalışan, güvenilirlik iş parçacıklarını sonlandırma veya işletim sistemi kaynaklarını tanıtıcıları veya bellek gibi emin olmak için gereklidir ve alınması önlemleri sızdırılmaz olduğunda uygulama etki alanları geri dönüştürme temel alır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-110">With these weaker guarantees and running in a single process, reliability is based on terminating threads or recycling application domains when necessary and taking precautions to ensure operating system resources such as handles or memory are not leaked.</span></span>  <span data-ttu-id="59bfc-111">Bile bu daha basit güvenilirlik kısıtlaması ile hala bir önemli güvenilirlik gereksinim vardır:</span><span class="sxs-lookup"><span data-stu-id="59bfc-111">Even with this simpler reliability constraint, there is still a significant reliability requirement:</span></span>  
  
-   <span data-ttu-id="59bfc-112">Hiçbir zaman sızıntısı işletim sistemi kaynakları.</span><span class="sxs-lookup"><span data-stu-id="59bfc-112">Never leak operating system resources.</span></span>  
  
-   <span data-ttu-id="59bfc-113">Tüm yönetilen kilitleri CLR tüm raporlarda tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-113">Identify all managed locks in all forms to the CLR.</span></span>  
  
-   <span data-ttu-id="59bfc-114">Paylaşılan hiçbir zaman sonu çapraz uygulama etki alanı belirtin, izin verme <xref:System.AppDomain> düzgün çalışması için Geri Dönüşüm.</span><span class="sxs-lookup"><span data-stu-id="59bfc-114">Never break cross-application domain shared state, allowing <xref:System.AppDomain> recycling to function smoothly.</span></span>  
  
 <span data-ttu-id="59bfc-115">İşlemek için yönetilen kod yazmaya teorik olarak mümkün olsa <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, ve <xref:System.OutOfMemoryException> özel durumlar, geliştiricilerin yazma sağlam gibi tüm uygulama kodunuzda aşırı bekleniyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-115">Although it is theoretically possible to write managed code to handle <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, and <xref:System.OutOfMemoryException> exceptions, expecting developers to write such robust code throughout an entire application is unreasonable.</span></span>  <span data-ttu-id="59bfc-116">Bu nedenle, bant dışı özel durumlar sonlandırılıyor yürütülen iş parçacığında neden; ve iş parçacığını sonlandırdı iş parçacığı bir kilidi tutan tarafından belirlenebilir, paylaşılan durum düzenleme sonra <xref:System.AppDomain> kaldırılır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-116">For that reason, out-of-band exceptions result in the executing thread being terminated; and if the terminated thread was editing shared state, which can be determined by whether the thread holds a lock, then the <xref:System.AppDomain> is unloaded.</span></span>  <span data-ttu-id="59bfc-117">Paylaşılan durum düzenleme yöntemi sonlandırıldığında paylaşılan durum güncelleştirmeleri için güvenilir geri çekme kodu yazmak mümkün olmadığı için durumu bozuk olacaktır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-117">When a method that is editing shared state is terminated, the state will be corrupt because it is not possible to write reliable back-out code for updates to shared state.</span></span>  
  
 <span data-ttu-id="59bfc-118">.NET Framework sürüm 2. 0'da, SQL Server, güvenilirlik gerektirir yalnızca ana bilgisayardır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-118">In the .NET Framework version 2.0, the only host that requires reliability is SQL Server.</span></span>  <span data-ttu-id="59bfc-119">Derlemenizi SQL Server üzerinde çalıştırılacak varsa veritabanında çalıştırırken devre dışı belirli özellikler olsa bile bu derleme her kısmı için güvenilirlik iş yapmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-119">If your assembly will be run on SQL Server you should do the reliability work for every part of that assembly, even if there are specific features that are disabled when running in the database.</span></span>  <span data-ttu-id="59bfc-120">Kod Çözümleme altyapısı kod derleme düzeyinde inceler ve devre dışı kod ayrım olduğundan, bu gereklidir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-120">This is required because the code analysis engine examines code at the assembly level and cannot differentiate disabled code.</span></span> <span data-ttu-id="59bfc-121">Başka bir SQL Server programlama dikkat etmeniz gereken SQL Server her şey tek bir işlemde çalıştırır ve <xref:System.AppDomain> geri dönüştürme kullanılan bellek ve işletim sistemi tanıtıcıları gibi tüm kaynakları temizleniyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-121">Another SQL Server programming consideration is that SQL Server runs everything in one process, and <xref:System.AppDomain> recycling is used for cleaning up all resources such as memory and operating system handles.</span></span>  
  
 <span data-ttu-id="59bfc-122">Sonlandırıcılar veya Yıkıcılar bağımlı olamaz veya `try/finally` geri çekme kodu engeller.</span><span class="sxs-lookup"><span data-stu-id="59bfc-122">You cannot depend on finalizers or destructors or `try/finally` blocks for back-out code.</span></span> <span data-ttu-id="59bfc-123">Bunlar kesintiye veya çağrılmaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-123">They might be interrupted or not called.</span></span>  
  
 <span data-ttu-id="59bfc-124">Zaman uyumsuz özel beklenmedik konumlarda büyük olasılıkla her makine yönerge durum: <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, ve <xref:System.OutOfMemoryException>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-124">Asynchronous exceptions can be thrown in unexpected locations, possibly every machine instruction: <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, and <xref:System.OutOfMemoryException>.</span></span>  
  
 <span data-ttu-id="59bfc-125">Yönetilen iş parçacığı mutlaka SQL Win32 parçacıklarında değildir; Bunlar lifleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-125">Managed threads are not necessarily Win32 threads in SQL; they might be fibers.</span></span>  
  
 <span data-ttu-id="59bfc-126">İşlem genelinde veya çapraz uygulama etki alanı değişebilir paylaşılan durumu güvenle alter oldukça zor ve mümkün olduğunda kaçınılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-126">Process-wide or cross-application domain mutable shared state is extremely difficult to alter safely and should be avoided whenever possible.</span></span>  
  
 <span data-ttu-id="59bfc-127">Bellek koşullarını SQL Server'da nadir değildir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-127">Out-of-memory conditions are not rare in SQL Server.</span></span>  
  
 <span data-ttu-id="59bfc-128">SQL Server barındırılan kitaplıkları paylaşılan durumlarına doğru güncelleştirmezseniz kodu değil kurtarır yüksek olasılık yoktur veritabanını yeniden başlatılana kadar.</span><span class="sxs-lookup"><span data-stu-id="59bfc-128">If libraries hosted in SQL Server do not correctly update their shared state, there is a high probability that the code will not recover until the database has been restarted.</span></span>  <span data-ttu-id="59bfc-129">Ayrıca, bazı olağanüstü durumlarda, bu veritabanını yeniden başlatmak neden SQL Server işleminin başarısız olmasına neden olabilir mümkündür.</span><span class="sxs-lookup"><span data-stu-id="59bfc-129">Additionally, in some extreme cases, it is possible this might cause the SQL Server process to fail, causing the database to reboot.</span></span>  <span data-ttu-id="59bfc-130">Veritabanını yeniden başlatma bir Web sitesi alabilir ya da kullanılabilirlik mutsuz şirket işlemlerini etkiler.</span><span class="sxs-lookup"><span data-stu-id="59bfc-130">Rebooting the database can take down a Web site or affect company operations, hurting availability.</span></span>  <span data-ttu-id="59bfc-131">Yavaş sızıntısı bellek veya tanıtıcıları gibi işletim sistemi kaynak sunucunun sonunda ayırma tanıtıcıları ile olası kurtarma başarısız olmasına neden olabilir veya potansiyel olarak sunucunun yavaş performansı düşebilir ve Müşteri'nin uygulama azaltır Kullanılabilirlik.</span><span class="sxs-lookup"><span data-stu-id="59bfc-131">A slow leak of operating system resources such as memory or handles may cause the server to eventually fail allocating handles with no possibility of recovery, or potentially the server may slowly degrade in performance and reduces a customer’s application availability.</span></span>  <span data-ttu-id="59bfc-132">Açıkça bu senaryoları önlemek istiyoruz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-132">Clearly we want to avoid these scenarios.</span></span>  
  
## <a name="best-practice-rules"></a><span data-ttu-id="59bfc-133">En iyi yöntem kuralları</span><span class="sxs-lookup"><span data-stu-id="59bfc-133">Best Practice Rules</span></span>  
 <span data-ttu-id="59bfc-134">Giriş ne sunucusunda çalışan yönetilen kod için kod incelemesi kararlılık ve framework güvenilirliğini artırmak için catch gerekirdi odaklanmıştır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-134">The introduction focused on what the code review for the managed code that runs in the server would have to catch to increase the stability and reliability of the framework.</span></span> <span data-ttu-id="59bfc-135">Tüm bu denetimlerinin genel iyi bir uygulama olduğundan ve sunucuda mutlak gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-135">All these checks are good practice in general and an absolute must on the server.</span></span>  
  
 <span data-ttu-id="59bfc-136">Ölü kilit ya da kaynak kısıtlama karşısında SQL Server bir iş parçacığı iptal etmek veya kesmeden bir <xref:System.AppDomain>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-136">In the face of a dead lock or resource constraint, SQL Server will abort a thread or tear down an <xref:System.AppDomain>.</span></span>  <span data-ttu-id="59bfc-137">Bu durumda, yalnızca geri çekme kodu kısıtlı yürütme bölgede (CER) çalıştırılması sağlanır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-137">When this happens, only back-out code in a constrained execution region (CER) is guaranteed to be run.</span></span>  
  
### <a name="use-safehandle-to-avoid-resource-leaks"></a><span data-ttu-id="59bfc-138">Kaynak sızıntıları önlemek için SafeHandle kullanın</span><span class="sxs-lookup"><span data-stu-id="59bfc-138">Use SafeHandle to Avoid Resource Leaks</span></span>  
 <span data-ttu-id="59bfc-139">Durumunda bir <xref:System.AppDomain> kaldırma, size olamaz bağlı `finally` blokları veya tüm işletim sistemi kaynak erişimi aracılığıyla soyut önemlidir, yürütülen sonlandırıcılar <xref:System.Runtime.InteropServices.SafeHandle> sınıfı yerine <xref:System.IntPtr>, <xref:System.Runtime.InteropServices.HandleRef>, veya benzer sınıflar.</span><span class="sxs-lookup"><span data-stu-id="59bfc-139">In the case of an <xref:System.AppDomain> unload, you cannot depend on `finally` blocks or finalizers being executed, so it is important to abstract all operating system resource access through the <xref:System.Runtime.InteropServices.SafeHandle> class rather than <xref:System.IntPtr>, <xref:System.Runtime.InteropServices.HandleRef>, or similar classes.</span></span> <span data-ttu-id="59bfc-140">Böylece, izlemek ve kullandığınız bile tanıtıcıları kapatmak CLR <xref:System.AppDomain> kapatmayı aşağı durumu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-140">This allows the CLR to track and close the handles you use even in the <xref:System.AppDomain> tear-down case.</span></span>  <span data-ttu-id="59bfc-141"><xref:System.Runtime.InteropServices.SafeHandle>CLR her zaman çalışacak bir kritik Sonlandırıcı kullanacak.</span><span class="sxs-lookup"><span data-stu-id="59bfc-141"><xref:System.Runtime.InteropServices.SafeHandle> will be using a critical finalizer which the CLR will always run.</span></span>  
  
 <span data-ttu-id="59bfc-142">İşletim sistemi tanıtıcı yayınlanmasından şu kadar oluşturulan andan güvenli tanıtıcı depolanır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-142">The operating system handle is stored in the safe handle from the moment it is created until the moment it is released.</span></span>  <span data-ttu-id="59bfc-143">Penceresi yok sırasında bir <xref:System.Threading.ThreadAbortException> bir tanıtıcı sızıntısı için oluşabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-143">There is no window during which a <xref:System.Threading.ThreadAbortException> can occur to leak a handle.</span></span>  <span data-ttu-id="59bfc-144">Ayrıca, platform çağırma başvuru-count yakın bir yarış durumu arasında bir güvenlik sorunu önleme tanıtıcının kullanım ömrü, izleme sağlayan tanıtıcı `Dispose` ve şu anda tanıtıcısını kullanarak bir yöntem.</span><span class="sxs-lookup"><span data-stu-id="59bfc-144">Additionally, platform invoke will reference-count the handle, which allows close tracking of the lifetime of the handle, preventing a security issue with a race condition between `Dispose` and a method that is currently using the handle.</span></span>  
  
 <span data-ttu-id="59bfc-145">Şu anda yalnızca temiz bir işletim sistemini oluşturan işlemek için bir sonlandırıcı sahip çoğu sınıf sonlandırıcıyı artık ihtiyacınız olmayacaktır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-145">Most classes that currently have a finalizer to simply clean up an operating system handle will not need the finalizer anymore.</span></span> <span data-ttu-id="59bfc-146">Bunun yerine, açık sonlandırıcıyı olacak <xref:System.Runtime.InteropServices.SafeHandle> türetilmiş sınıf.</span><span class="sxs-lookup"><span data-stu-id="59bfc-146">Instead, the finalizer will be on the <xref:System.Runtime.InteropServices.SafeHandle> derived class.</span></span>  
  
 <span data-ttu-id="59bfc-147">Unutmayın <xref:System.Runtime.InteropServices.SafeHandle> için yenileme değildir <xref:System.IDisposable.Dispose%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-147">Note that <xref:System.Runtime.InteropServices.SafeHandle> is not a replacement for <xref:System.IDisposable.Dispose%2A?displayProperty=nameWithType>.</span></span>  <span data-ttu-id="59bfc-148">Açıkça işletim sistemi kaynaklarını silmek için Çekişme ve performans avantajı hala olası kaynak vardır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-148">There are still potential resource contention and performance advantages to explicitly dispose operating system resources.</span></span>  <span data-ttu-id="59bfc-149">Yalnızca fark `finally` açıkça kaynaklarını silmek blokları tamamlanıncaya kadar değil yürütme.</span><span class="sxs-lookup"><span data-stu-id="59bfc-149">Just realize that `finally` blocks that do explicitly dispose of resources may not execute to completion.</span></span>  
  
 <span data-ttu-id="59bfc-150"><xref:System.Runtime.InteropServices.SafeHandle>uygulama kendi sayesinde <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> yordamı boşaltma veya döngü tanıtıcıları kümesi boşaltma bir işletim sistemi tanıtıcısı geçirme durumuna gibi tanıtıcı boşaltmak için çalışma gerçekleştirir yöntemi.</span><span class="sxs-lookup"><span data-stu-id="59bfc-150"><xref:System.Runtime.InteropServices.SafeHandle> allows you to implement your own <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> method that performs the work to free the handle, such as passing state to an operating system handle freeing routine or freeing a set of handles in a loop.</span></span>  <span data-ttu-id="59bfc-151">CLR bu yöntem çalıştırıldığını güvence altına alır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-151">The CLR guarantees that this method is run.</span></span>  <span data-ttu-id="59bfc-152">Yazarı sorumluluğundadır <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> tanıtıcı her koşulda yayımlanan emin olmak için uygulama.</span><span class="sxs-lookup"><span data-stu-id="59bfc-152">It is the responsibility of the author of the <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> implementation to ensure that the handle is released in all circumstances.</span></span> <span data-ttu-id="59bfc-153">Bunun Sağlanamaması, genellikle tanıtıcı ile ilişkili yerel kaynakları sızıntısını sonuçlanır sızmasını tanıtıcı neden olur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-153">Failure to do so will cause the handle to be leaked, which often results in the leakage of native resources associated with the handle.</span></span> <span data-ttu-id="59bfc-154">Bu nedenle yapısına kritik <xref:System.Runtime.InteropServices.SafeHandle> türetilmiş sınıfları şekilde <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> uygulama çağırma aynı anda kullanılamayabilir herhangi bir kaynağa ayırma gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="59bfc-154">Therefore it is critical to structure <xref:System.Runtime.InteropServices.SafeHandle> derived classes such that the <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> implementation does not require the allocation of any resources that may not be available at invocation time.</span></span> <span data-ttu-id="59bfc-155">İzin verilen uygulanması içinde başlatılamayabilir çağrısı yöntemlerine olduğuna dikkat edin <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> koşuluyla kodunuzu böyle hataları işlemek ve yerel işleyici yayımlamayı sözleşme tamamlayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-155">Note that it is permissible to call methods that may fail within the implementation of <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> provided that your code can handle such failures and complete the contract to release the native handle.</span></span> <span data-ttu-id="59bfc-156">Hata ayıklama amacıyla, <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> sahip bir <xref:System.Boolean> dönmek için ayarlanabilir değeri `false` kaynak sürümü önleyen işlemi yıkıcı hatayla karşılaşılırsa.</span><span class="sxs-lookup"><span data-stu-id="59bfc-156">For debugging purposes, <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> has a <xref:System.Boolean> return value which may be set to `false` if a catastrophic error is encountered which prevents release of the resource.</span></span> <span data-ttu-id="59bfc-157">Bunun yapılması etkinleşir [releaseHandleFailed](../../../docs/framework/debug-trace-profile/releasehandlefailed-mda.md) sorun tanımlanmasına yardımcı olmak için etkinleştirilirse, MDA.</span><span class="sxs-lookup"><span data-stu-id="59bfc-157">Doing so will activate the [releaseHandleFailed](../../../docs/framework/debug-trace-profile/releasehandlefailed-mda.md) MDA, if enabled, to aid in identifying the problem.</span></span> <span data-ttu-id="59bfc-158">Çalışma zamanı başka bir şekilde etkilemez; <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> yeniden için aynı kaynak çağrılmaz ve tanıtıcı sızmasını sonuç.</span><span class="sxs-lookup"><span data-stu-id="59bfc-158">It does not affect the runtime in any other way; <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> will not be called again for the same resource and consequently the handle will be leaked.</span></span>  
  
 <span data-ttu-id="59bfc-159"><xref:System.Runtime.InteropServices.SafeHandle>Bazı bağlamlarda uygun değil.</span><span class="sxs-lookup"><span data-stu-id="59bfc-159"><xref:System.Runtime.InteropServices.SafeHandle> is not appropriate in certain contexts.</span></span>  <span data-ttu-id="59bfc-160">Bu yana <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> yöntemi, çalıştırılabilir bir <xref:System.GC> sonlandırıcıyı iş parçacığı, belirli bir iş parçacığı üzerinde boşaltılması için gerekli olan tanıtıcıları değil Sarmalanan içinde bir <xref:System.Runtime.InteropServices.SafeHandle>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-160">Since the <xref:System.Runtime.InteropServices.SafeHandle.ReleaseHandle%2A> method can be run on a <xref:System.GC> finalizer thread, any handles that are required to be freed on a particular thread should not be wrapped in a <xref:System.Runtime.InteropServices.SafeHandle>.</span></span>  
  
 <span data-ttu-id="59bfc-161">Çalışma zamanı aranabilir sarmalayıcıları (RCWs) ek kod olmadan CLR tarafından temizlenebilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-161">Runtime callable wrappers (RCWs) can be cleaned by the CLR without additional code.</span></span>  <span data-ttu-id="59bfc-162">Platform çağırma ve bir COM nesnesi değerlendirir kullanan kodu için bir `IUnknown*` veya bir <xref:System.IntPtr>, bir RCW kullanmak için kodu yazılması.</span><span class="sxs-lookup"><span data-stu-id="59bfc-162">For code that uses platform invoke and treats a COM object as an `IUnknown*` or an <xref:System.IntPtr>, the code should be rewritten to use an RCW.</span></span>  <span data-ttu-id="59bfc-163"><xref:System.Runtime.InteropServices.SafeHandle>yönetilen kod geri çağırma bir yönetilmeyen yayın yöntemi olasılığı nedeniyle bu senaryo için yeterli olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-163"><xref:System.Runtime.InteropServices.SafeHandle> may not be adequate for this scenario due to the possibility of an unmanaged release method calling back into managed code.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-164">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-164">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-165">Kullanım <xref:System.Runtime.InteropServices.SafeHandle> işletim sistemi kaynakları için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-165">Use <xref:System.Runtime.InteropServices.SafeHandle> to encapsulate operating system resources.</span></span> <span data-ttu-id="59bfc-166">Kullanmayın <xref:System.Runtime.InteropServices.HandleRef> veya türünde alanlar <xref:System.IntPtr>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-166">Do not use <xref:System.Runtime.InteropServices.HandleRef> or fields of type <xref:System.IntPtr>.</span></span>  
  
### <a name="ensure-finalizers-do-not-have-to-run-to-prevent-leaking-operating-system-resources"></a><span data-ttu-id="59bfc-167">Sonlandırıcılar işletim sistemi kaynaklarını sızmasını önlemek için Çalıştır olarak yok emin olun</span><span class="sxs-lookup"><span data-stu-id="59bfc-167">Ensure Finalizers Do Not Have to Run to Prevent Leaking Operating System Resources</span></span>  
 <span data-ttu-id="59bfc-168">Dikkatle bunlar çalıştırmayın olsa bile, kritik işletim sistemi kaynağı sızdırılmaz emin olmak için sonlandırıcılar gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-168">Review your finalizers carefully to ensure that even if they do not run, a critical operating system resource is not leaked.</span></span>  <span data-ttu-id="59bfc-169">Normal bir aksine <xref:System.AppDomain> uygulama kararlı bir duruma veya SQL Server kapatır gibi bir sunucu değil kesin bir ani sırasında nesneleri yürütülürken unload <xref:System.AppDomain> kaldırın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-169">Unlike a normal <xref:System.AppDomain> unload when the application is executing in a steady state or when a server such as SQL Server shuts down, objects are not finalized during an abrupt <xref:System.AppDomain> unload.</span></span>  <span data-ttu-id="59bfc-170">Kaynakları ani bir unload söz konusu olduğunda, bir uygulamanın doğruluğu garanti edilemez, ancak sunucu bütünlüğünü kaynakları sızmasını değil tarafından korunmalıdır beri sızdırılmaz emin olun.</span><span class="sxs-lookup"><span data-stu-id="59bfc-170">Ensure resources are not leaked in the case of an abrupt unload, since an application's correctness cannot be guaranteed, but the integrity of the server must be maintained by not leaking resources.</span></span>  <span data-ttu-id="59bfc-171">Kullanım <xref:System.Runtime.InteropServices.SafeHandle> işletim sistemi kaynakları serbest bırakma olanağı.</span><span class="sxs-lookup"><span data-stu-id="59bfc-171">Use <xref:System.Runtime.InteropServices.SafeHandle> to free any operating system resources.</span></span>  
  
### <a name="ensure-that-finally-clauses-do-not-have-to-run-to-prevent-leaking-operating-system-resources"></a><span data-ttu-id="59bfc-172">Son olarak bu yan tümceleri sızmasını işletim sistemi kaynaklarını önlemek için çalışma gerekmez emin olun</span><span class="sxs-lookup"><span data-stu-id="59bfc-172">Ensure That finally Clauses Do Not Have to Run to Prevent Leaking Operating System Resources</span></span>  
 <span data-ttu-id="59bfc-173">`finally`yan tümceleri garanti edilmez CERs dışında çalıştırma kitaplığı geliştiricilerin içindeki kod kalmamanız gerektiren bir `finally` yönetilmeyen kaynakları serbest bloğu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-173">`finally` clauses are not guaranteed to run outside of CERs, requiring library developers to not rely on code within a `finally` block to free unmanaged resources.</span></span>  <span data-ttu-id="59bfc-174">Kullanarak <xref:System.Runtime.InteropServices.SafeHandle> önerilen çözümdür.</span><span class="sxs-lookup"><span data-stu-id="59bfc-174">Using <xref:System.Runtime.InteropServices.SafeHandle> is the recommended solution.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-175">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-175">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-176">Kullanım <xref:System.Runtime.InteropServices.SafeHandle> yerine işletim sistemi kaynakları Temizleme için `Finalize`.</span><span class="sxs-lookup"><span data-stu-id="59bfc-176">Use <xref:System.Runtime.InteropServices.SafeHandle> for cleaning up operating system resources instead of `Finalize`.</span></span> <span data-ttu-id="59bfc-177">Kullanmayın <xref:System.IntPtr>; kullanın <xref:System.Runtime.InteropServices.SafeHandle> kaynakları için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-177">Do not use <xref:System.IntPtr>; use <xref:System.Runtime.InteropServices.SafeHandle> to encapsulate resources.</span></span> <span data-ttu-id="59bfc-178">Varsa yan tümcesi son gerekir, çalıştırmak bir CER yerleştirin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-178">If the finally clause must run, place it in a CER.</span></span>  
  
### <a name="all-locks-should-go-through-existing-managed-locking-code"></a><span data-ttu-id="59bfc-179">Tüm kilitleri mevcut yönetilen kilitleme kod üzerinden gitmesi gereken</span><span class="sxs-lookup"><span data-stu-id="59bfc-179">All Locks Should Go Through Existing Managed Locking Code</span></span>  
 <span data-ttu-id="59bfc-180">CLR kesmeden bilmesini kod bir kilit olduğunda bilmelisiniz <xref:System.AppDomain> yerine yalnızca iş parçacığı durduruluyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-180">The CLR must know when code is in a lock so that it will know to tear down the <xref:System.AppDomain> rather than just aborting the thread.</span></span>  <span data-ttu-id="59bfc-181">Üzerinde iş parçacığı tarafından işletilen veri tutarsız bir durumda bıraktığınız iş parçacığı durduruluyor tehlikeli olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-181">Aborting the thread could be dangerous as the data operated on by the thread could be left in an inconsistent state.</span></span> <span data-ttu-id="59bfc-182">Bu nedenle, tüm <xref:System.AppDomain> geri dönüştürülmesi gerekiyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-182">Therefore, the entire <xref:System.AppDomain> has to be recycled.</span></span>  <span data-ttu-id="59bfc-183">Kilit tanımlamak başarısız olan sonuçlarıyla kilitlenmeleri veya hatalı sonuçları olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-183">The consequences of failing to identify a lock can be either deadlocks or incorrect results.</span></span> <span data-ttu-id="59bfc-184">Yöntemleri kullanın <xref:System.Threading.Thread.BeginCriticalRegion%2A> ve <xref:System.Threading.Thread.EndCriticalRegion%2A> kilit bölgeleri tanımlamak için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-184">Use the methods <xref:System.Threading.Thread.BeginCriticalRegion%2A> and <xref:System.Threading.Thread.EndCriticalRegion%2A> to identify lock regions.</span></span>  <span data-ttu-id="59bfc-185">Üzerinde statik yöntemler olduğu <xref:System.Threading.Thread> yalnızca tek bir iş parçacığı başka bir iş parçacığının kilit sayısı düzenleme engellemeye yardımcı olacak geçerli iş parçacığının uygulamak sınıfı.</span><span class="sxs-lookup"><span data-stu-id="59bfc-185">They are static methods on the <xref:System.Threading.Thread> class that only apply to the current thread, helping to prevent one thread from editing another thread’s lock count.</span></span>  
  
 <span data-ttu-id="59bfc-186"><xref:System.Threading.Monitor.Enter%2A>ve <xref:System.Threading.Monitor.Exit%2A> kullanımları yanı sıra kullanılması önerilir, yerleşik bu CLR bildirim sahip [lock deyimi](~/docs/csharp/language-reference/keywords/lock-statement.md), bu yöntemleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-186"><xref:System.Threading.Monitor.Enter%2A> and <xref:System.Threading.Monitor.Exit%2A> have this CLR notification built in, so their usage is recommended as well as the use of the [lock Statement](~/docs/csharp/language-reference/keywords/lock-statement.md), which uses these methods.</span></span>  
  
 <span data-ttu-id="59bfc-187">Döndürme kilitleri gibi diğer kilitleme mekanizmaları ve <xref:System.Threading.AutoResetEvent> önemli bir bölümü giriliyor CLR bildirmek için bu yöntemleri çağırmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-187">Other locking mechanisms such as spin locks and <xref:System.Threading.AutoResetEvent> must call these methods to notify the CLR that a critical section is being entered.</span></span>  <span data-ttu-id="59bfc-188">Bu yöntemler kilitleri almayan; kod kritik bölümünde yürütüyor CLR bildirin ve iş parçacığı durduruluyor paylaşılan durum tutarsız bırakabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-188">These methods do not take any locks; they inform the CLR that code is executing in a critical section and aborting the thread could leave shared state inconsistent.</span></span>  <span data-ttu-id="59bfc-189">Özel bir gibi kendi kilit türü tanımlı değilse <xref:System.Threading.ReaderWriterLock> sınıfı, bu kilit sayısı yöntemlerini kullanın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-189">If you have defined your own lock type, such as a custom <xref:System.Threading.ReaderWriterLock> class, use these lock count methods.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-190">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-190">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-191">İşaretleyin ve kullanarak tüm kilitleri tanımlamak <xref:System.Threading.Thread.BeginCriticalRegion%2A> ve <xref:System.Threading.Thread.EndCriticalRegion%2A>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-191">Mark and identify all locks using <xref:System.Threading.Thread.BeginCriticalRegion%2A> and <xref:System.Threading.Thread.EndCriticalRegion%2A>.</span></span> <span data-ttu-id="59bfc-192">Kullanmayın <xref:System.Threading.Interlocked.CompareExchange%2A>, <xref:System.Threading.Interlocked.Increment%2A>, ve <xref:System.Threading.Interlocked.Decrement%2A> döngü.</span><span class="sxs-lookup"><span data-stu-id="59bfc-192">Do not use <xref:System.Threading.Interlocked.CompareExchange%2A>, <xref:System.Threading.Interlocked.Increment%2A>, and <xref:System.Threading.Interlocked.Decrement%2A> in a loop.</span></span>  <span data-ttu-id="59bfc-193">Bir platform yapmak bu yöntemleri Win32 çeşitlemelerini çağırma.</span><span class="sxs-lookup"><span data-stu-id="59bfc-193">Do not do a platform invoke of the Win32 variants of these methods.</span></span>  <span data-ttu-id="59bfc-194">Kullanmayın <xref:System.Threading.Thread.Sleep%2A> döngü.</span><span class="sxs-lookup"><span data-stu-id="59bfc-194">Do not use <xref:System.Threading.Thread.Sleep%2A> in a loop.</span></span>  <span data-ttu-id="59bfc-195">Volatile alanları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-195">Do not use volatile fields.</span></span>  
  
### <a name="cleanup-code-must-be-in-a-finally-or-a-catch-block-not-following-a-catch"></a><span data-ttu-id="59bfc-196">Temizleme kodu içinde olmalıdır bir son olarak veya bir catch bloğu bir catch değil aşağıdaki</span><span class="sxs-lookup"><span data-stu-id="59bfc-196">Cleanup Code Must Be in a finally or a catch Block, Not Following a catch</span></span>  
 <span data-ttu-id="59bfc-197">Kodu temizleme hiçbir zaman izlemelidir bir `catch` engelle; olmalıdır bir `finally` veya `catch` kendisini engelleyin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-197">Cleanup code should never follow a `catch` block; it should be in a `finally` or in the `catch` block itself.</span></span>  <span data-ttu-id="59bfc-198">Bu, normal iyi bir uygulama olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-198">This should be a normal good practice.</span></span>  <span data-ttu-id="59bfc-199">A `finally` bloğu, genellikle tercih edilen bir özel durum yakalandığında hem zaman aynı kodu çalıştırdığı için sonuna `try` blok normalde karşılaştı.</span><span class="sxs-lookup"><span data-stu-id="59bfc-199">A `finally` block is generally preferred because it runs the same code both when an exception is thrown and when the end of the `try` block is normally encountered.</span></span>  <span data-ttu-id="59bfc-200">Örneğin oluşturulan beklenmeyen bir özel durum durumunda bir <xref:System.Threading.ThreadAbortException>, temizleme kod çalışmayacak.</span><span class="sxs-lookup"><span data-stu-id="59bfc-200">In the event of an unexpected exception being thrown, for example a <xref:System.Threading.ThreadAbortException>, the cleanup code will not run.</span></span>  <span data-ttu-id="59bfc-201">İçinde temizlenmesi kaynakları yönetilmeyen herhangi bir `finally` , ideal olarak alınmalı bir <xref:System.Runtime.InteropServices.SafeHandle> sızıntıları önlemek için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-201">Any unmanaged resources that you would clean up in a `finally` should ideally be wrapped in a <xref:System.Runtime.InteropServices.SafeHandle> to prevent leaks.</span></span>  <span data-ttu-id="59bfc-202">C# Not `using` anahtar sözcüğü etkili bir şekilde işler de dahil olmak üzere nesnelerin dispose için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-202">Note the C# `using` keyword can be used effectively to dispose of objects, including handles.</span></span>  
  
 <span data-ttu-id="59bfc-203">Rağmen <xref:System.AppDomain> geri dönüştürülüyor temiz sonlandırıcıyı iş parçacığı üzerinde kaynakları, kodu temizleme doğru yerleştirdiniz hala önem taşır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-203">Although <xref:System.AppDomain> recycling can clean up resources on the finalizer thread, it is still important to put cleanup code in the correct place.</span></span>  <span data-ttu-id="59bfc-204">Bir iş parçacığı bir zaman uyumsuz özel bir kilit tutmadan alırsa, CLR geri dönüşüm gerek kalmadan iş parçacığının kendi bitiş denemesi Not <xref:System.AppDomain>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-204">Note that if a thread receives an asynchronous exception without holding a lock, the CLR attempts to end the thread itself without having to recycle the <xref:System.AppDomain>.</span></span>  <span data-ttu-id="59bfc-205">Kaynakları daha erken yerine sonraki yardımcı daha fazla kaynağı kullanılabilir hale getirme ve yaşam süresini daha iyi yönetme temizlendiğinden emin olma.</span><span class="sxs-lookup"><span data-stu-id="59bfc-205">Ensuring that resources are cleaned up sooner rather than later helps by making more resources available, and by better managing the lifetime.</span></span>  <span data-ttu-id="59bfc-206">Bazı hata kodu yolda bir dosya için bir tanıtıcı açıkça kapatmazsanız sonra bekleyin <xref:System.Runtime.InteropServices.SafeHandle> ilk kez kodunuzu çalıştığında, temizlemek için sonlandırıcıyı Sonlandırıcı olmayan zaten çalıştırıldıysa tam aynı dosyaya erişmeye başarısız olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-206">If you do not explicitly close a handle to a file in some error code path then wait for the <xref:System.Runtime.InteropServices.SafeHandle> finalizer to clean it up, the next time your code runs it may fail trying to access the exact same file if the finalizer has not already run.</span></span>  <span data-ttu-id="59bfc-207">Bu nedenle, kodu temizleme var olduğundan ve doğru bir şekilde çalıştığından emin olduktan yardımcı olacak kesinlikle gerekli olmasa da hatalarından daha düzgün bir şekilde ve hızlı kurtarma.</span><span class="sxs-lookup"><span data-stu-id="59bfc-207">For this reason, ensuring that cleanup code exists and works correctly will help recover from failures more cleanly and quickly, even though it is not strictly necessary.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-208">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-208">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-209">Temizleme koddan sonra `catch` olması gereken bir `finally` bloğu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-209">Cleanup code after `catch` needs to be in a `finally` block.</span></span> <span data-ttu-id="59bfc-210">Aramayı içinde silmek için bir son engelleyin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-210">Place calls to dispose in a finally block.</span></span>  <span data-ttu-id="59bfc-211">`catch`Bloklar içinde bir throw bitiş veya yeniden oluşturulması gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-211">`catch` blocks should end in a throw or rethrow.</span></span>  <span data-ttu-id="59bfc-212">Özel durumlar olacaktır karşın, ağ bağlantı kurulamıyor algılama kodu gibi herhangi bir sayıda özel durumlar, normal koşullar altında özel durum sayısı yakalama gerektiren herhangi bir kod burada alabilirsiniz vermelidir bir kod olmalıdır göstergesi başarılı olur, görmek için test.</span><span class="sxs-lookup"><span data-stu-id="59bfc-212">While there will be exceptions, such as code detecting whether a network connection can be established where you might get any of a large number of exceptions, any code that requires the catching of a number of exceptions under normal circumstances should give an indication that the code should be tested to see if it will succeed.</span></span>  
  
### <a name="process-wide-mutable-shared-state-between-application-domains-should-be-eliminated-or-use-a-constrained-execution-region"></a><span data-ttu-id="59bfc-213">Uygulama etki alanları arasında değişebilir paylaşılan durumu işlem genelinde ortadan veya kısıtlı yürütme bölge kullanın</span><span class="sxs-lookup"><span data-stu-id="59bfc-213">Process-Wide Mutable Shared State Between Application Domains Should Be Eliminated or Use a Constrained Execution Region</span></span>  
 <span data-ttu-id="59bfc-214">Girişte açıklandığı gibi işlem genelinde paylaşılan durum uygulama etki alanları arasında güvenilir bir şekilde izler yönetilen kod yazmak oldukça zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-214">As described in the introduction, it can be very difficult to write managed code that monitors process-wide shared state across application domains in a reliable manner.</span></span>  <span data-ttu-id="59bfc-215">İşlem genelinde paylaşılan durumu uygulama etki alanları arasında Win32 kod, CLR içinde ya da remoting kullanarak yönetilen kodda ya da paylaşılan veri yapısı herhangi bir tür değil.</span><span class="sxs-lookup"><span data-stu-id="59bfc-215">Process-wide shared state is any sort of data structure shared between application domains, either in Win32 code, inside the CLR, or in managed code using remoting.</span></span>  <span data-ttu-id="59bfc-216">Durum paylaşılmasını değişebilir doğru yönetilen kodda yazma çok zordur ve herhangi bir statik paylaşılan durum yalnızca çok dikkatli ile yapılabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-216">Any mutable shared state is very difficult to correctly write in managed code, and any static shared state might be done only with great care.</span></span>  <span data-ttu-id="59bfc-217">İşlem genelinde veya makine genelinde paylaşılan durum varsa, bunu kaldırın veya kısıtlanmış yürütme bölgesi (CER) kullanarak paylaşılan durumunu korumak için bazı yol bulun.</span><span class="sxs-lookup"><span data-stu-id="59bfc-217">If you have process-wide or machine-wide shared state, find some way to eliminate it or protect the shared state using a constrained execution region (CER).</span></span>  <span data-ttu-id="59bfc-218">Tanımlanan düzeltildi değilse ve paylaşılan durumu ile herhangi bir kitaplığı temiz gerektiren SQL Server gibi bir konak sağlayabilir Not <xref:System.AppDomain> çökmesine kaldırma.</span><span class="sxs-lookup"><span data-stu-id="59bfc-218">Note that any library with shared state that is not identified and corrected could cause a host, such as SQL Server, that requires clean <xref:System.AppDomain> unloading to crash.</span></span>  
  
 <span data-ttu-id="59bfc-219">Kod bir COM nesnesi kullanıyorsa, bu COM nesnesinin uygulama etki alanları arasında paylaşımı kaçının.</span><span class="sxs-lookup"><span data-stu-id="59bfc-219">If code uses a COM object, avoid sharing that COM object between application domains.</span></span>  
  
### <a name="locks-do-not-work-process-wide-or-between-application-domains"></a><span data-ttu-id="59bfc-220">Kilitler işlem genelinde veya uygulama etki alanları arasında çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-220">Locks Do Not Work Process-Wide or Between Application Domains.</span></span>  
 <span data-ttu-id="59bfc-221">Geçmişte, <xref:System.Threading.Monitor.Enter%2A> ve [lock deyimi](~/docs/csharp/language-reference/keywords/lock-statement.md) genel işlem kilitleri oluşturmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-221">In the past, <xref:System.Threading.Monitor.Enter%2A> and the [lock Statement](~/docs/csharp/language-reference/keywords/lock-statement.md) have been used to create global process locks.</span></span>  <span data-ttu-id="59bfc-222">Örneğin, bu üzerinde kilitleme ortaya çıkar <xref:System.AppDomain> Çevik sınıflar gibi <xref:System.Type> paylaşılmayan derlemelerden örnekleri <xref:System.Threading.Thread> nesneleri, interned dizeler ve remoting kullanarak uygulama etki alanları arasında paylaşılan herhangi bir dize.</span><span class="sxs-lookup"><span data-stu-id="59bfc-222">For example, this occurs when locking on <xref:System.AppDomain> agile classes, such as <xref:System.Type> instances from non-shared assemblies, <xref:System.Threading.Thread> objects, interned strings, and some strings shared across application domains using remoting.</span></span>  <span data-ttu-id="59bfc-223">Bu kilit artık işlem kapsamındadır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-223">These locks are no longer process-wide.</span></span>  <span data-ttu-id="59bfc-224">İşlem genelinde interapplication etki alanı kilit varlığı tanımlamak için kilit içindeki kod diske veya büyük olasılıkla bir veritabanı dosyası gibi herhangi bir dış, kalıcı kaynak kullanıp kullanmadığını belirleyin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-224">To identify the presence of a process-wide interapplication domain lock, determine if the code within the lock uses any external, persisted resource such as a file on disk or possibly a database.</span></span>  
  
 <span data-ttu-id="59bfc-225">Bu içinde bir kilit almayı unutmayın bir <xref:System.AppDomain> korunan kod, dış kaynak kullanır, bu kod, aynı anda birden çok uygulama etki alanları arasında çalıştırabilir çünkü sorunlara neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-225">Note that taking a lock within an <xref:System.AppDomain> might cause problems if the protected code uses an external resource because that code may run simultaneously across multiple application domains.</span></span>  <span data-ttu-id="59bfc-226">Bu yazma sırasında bir sorun olabilir tek bir günlük dosyası veya tüm işlem için bir yuva için bağlama.</span><span class="sxs-lookup"><span data-stu-id="59bfc-226">This can be a problem when writing to one log file or binding to a socket for the entire process.</span></span>  <span data-ttu-id="59bfc-227">Bu değişiklikler, bir adlandırılmış kullanma dışındaki bir işlem genel kilit yönetilen kodu kullanarak hiçbir kolay bir yolu olmadığını anlamına geliyor <xref:System.Threading.Mutex> veya <xref:System.Threading.Semaphore> örneği.</span><span class="sxs-lookup"><span data-stu-id="59bfc-227">These changes mean there is no easy way, using managed code, to get a process-global lock, other than using a named <xref:System.Threading.Mutex> or <xref:System.Threading.Semaphore> instance.</span></span>  <span data-ttu-id="59bfc-228">Olmayan iki uygulama etki alanında aynı anda çalıştırmak, veya kullanan kodu oluşturmak <xref:System.Threading.Mutex> veya <xref:System.Threading.Semaphore> sınıfları.</span><span class="sxs-lookup"><span data-stu-id="59bfc-228">Create code that does not run in two application domains simultaneously, or use the <xref:System.Threading.Mutex> or <xref:System.Threading.Semaphore> classes.</span></span>  <span data-ttu-id="59bfc-229">Var olan kodu değiştirilemiyorsa mutex adlı bir Win32 fiber modunda çalışan işletim sistemi iş parçacığı edinmeli ve bir mutex yayın garanti edemez gösterdiğinden bu eşitleme elde etmek için kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-229">If existing code cannot be changed, do not use a Win32 named mutex to achieve this synchronization because running in fiber mode means you cannot guarantee the same operating system thread will acquire and release a mutex.</span></span>  <span data-ttu-id="59bfc-230">Yönetilen kullanmalısınız <xref:System.Threading.Mutex> sınıfı veya bir adlandırılmış <xref:System.Threading.ManualResetEvent>, <xref:System.Threading.AutoResetEvent>, veya bir <xref:System.Threading.Semaphore> CLR yönetilmeyen kod kullanarak kilit eşitleme yerine bilmektedir bir şekilde kod kilit eşitlenecek.</span><span class="sxs-lookup"><span data-stu-id="59bfc-230">You must use the managed <xref:System.Threading.Mutex> class, or a named <xref:System.Threading.ManualResetEvent>, <xref:System.Threading.AutoResetEvent>, or a <xref:System.Threading.Semaphore> to synchronize the code lock in a manner that the CLR is aware of instead of synchronizing the lock using unmanaged code.</span></span>  
  
#### <a name="avoid-locktypeofmytype"></a><span data-ttu-id="59bfc-231">Lock(typeof(MyType)) kaçının</span><span class="sxs-lookup"><span data-stu-id="59bfc-231">Avoid lock(typeof(MyType))</span></span>  
 <span data-ttu-id="59bfc-232">Özel ve ortak <xref:System.Type> tüm uygulama etki alanları arasında paylaşılan kod yalnızca bir kopya ile paylaşılan derlemeler nesneleri sorunları de sunar.</span><span class="sxs-lookup"><span data-stu-id="59bfc-232">Private and public <xref:System.Type> objects in shared assemblies with only one copy of the code shared across all application domains also present problems.</span></span>  <span data-ttu-id="59bfc-233">Paylaşılan derlemeler için yalnızca bir örneği var. bir <xref:System.Type> her işlem, birden çok uygulama etki alanları tam aynı paylaşıma anlamına <xref:System.Type> örneği.</span><span class="sxs-lookup"><span data-stu-id="59bfc-233">For shared assemblies, there is only one instance of a <xref:System.Type> per process, meaning that multiple application domains share the exact same <xref:System.Type> instance.</span></span>  <span data-ttu-id="59bfc-234">Kilit almayı bir <xref:System.Type> örneği alır, tüm işlem etkileyen değil kilit yalnızca <xref:System.AppDomain>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-234">Taking a lock on a <xref:System.Type> instance takes a lock that affects the entire process, not just the <xref:System.AppDomain>.</span></span>  <span data-ttu-id="59bfc-235">Varsa <xref:System.AppDomain> kilit geçen bir <xref:System.Type> iş parçacığı aniden durduruldu, kilidi serbest değil nesne.</span><span class="sxs-lookup"><span data-stu-id="59bfc-235">If one <xref:System.AppDomain> takes a lock on a <xref:System.Type> object then that thread gets abruptly aborted, it will not release the lock.</span></span>  <span data-ttu-id="59bfc-236">Bu kilit sonra kilitlenme diğer uygulama etki alanları neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-236">This lock then may cause other application domains to deadlock.</span></span>  
  
 <span data-ttu-id="59bfc-237">Statik yöntemleri kilitleri olabilmesi için en iyi yolu, bir statik iç eşitleme nesnesi için kod ekleme içerir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-237">A good way to take locks in static methods involves adding a static internal synchronization object to the code.</span></span>  <span data-ttu-id="59bfc-238">Bu sınıf oluşturucusunda varsa, ancak bunu şu şekilde başlatılamıyor başlatılamadı:</span><span class="sxs-lookup"><span data-stu-id="59bfc-238">This could be initialized in the class constructor if one is present, but if not it can be initialized like this:</span></span>  
  
```  
private static Object s_InternalSyncObject;  
private static Object InternalSyncObject   
{  
    get   
    {  
        if (s_InternalSyncObject == null)   
        {  
            Object o = new Object();  
            Interlocked.CompareExchange(  
                ref s_InternalSyncObject, o, null);  
        }  
        return s_InternalSyncObject;  
    }  
}  
```  
  
 <span data-ttu-id="59bfc-239">Ardından bir kilit kullanım duruma getirirken `InternalSyncObject` kilitlemek için bir nesne almak için özellik.</span><span class="sxs-lookup"><span data-stu-id="59bfc-239">Then when taking a lock, use the `InternalSyncObject` property to obtain an object to lock on.</span></span>  <span data-ttu-id="59bfc-240">İç eşitleme nesne sınıfı oluşturucuda başlatılmış durumunda özelliğinin kullanılması gerekmez.</span><span class="sxs-lookup"><span data-stu-id="59bfc-240">You do not need to use the property if you have initialized the internal synchronization object in your class constructor.</span></span>  <span data-ttu-id="59bfc-241">Çift denetimi kilit başlatma kodu, bu örnekteki gibi görünmelidir:</span><span class="sxs-lookup"><span data-stu-id="59bfc-241">The double checking lock initialization code should look like this example:</span></span>  
  
```  
public static MyClass SingletonProperty   
{  
    get   
    {  
        if (s_SingletonProperty == null)   
        {  
            lock(InternalSyncObject)   
            {  
                // Do not use lock(typeof(MyClass))   
                if (s_SingletonProperty == null)   
                {  
                    MyClass tmp = new MyClass(…);     
                    // Do all initialization before publishing  
                    s_SingletonProperty = tmp;  
                }  
            }  
        }  
        return s_SingletonProperty;  
    }  
}  
```  
  
#### <a name="a-note-about-lockthis"></a><span data-ttu-id="59bfc-242">Not Lock(this) hakkında</span><span class="sxs-lookup"><span data-stu-id="59bfc-242">A Note About Lock(this)</span></span>  
 <span data-ttu-id="59bfc-243">Genel olarak erişilebilir olan tek tek bir nesne üzerinde bir kilit etkinleştirilir genellikle kabul edilebilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-243">It is generally acceptable to take a lock on an individual object that is publicly accessible.</span></span>  <span data-ttu-id="59bfc-244">Ancak, nesne kilitlenme için tüm bir alt neden olabilecek bir singleton nesneyse da yukarıdaki tasarım deseni kullanmayı düşünün.</span><span class="sxs-lookup"><span data-stu-id="59bfc-244">However, if the object is a singleton object that might cause an entire subsystem to deadlock, consider using the above design pattern as well.</span></span>  <span data-ttu-id="59bfc-245">Örneğin, bir kilit <xref:System.Security.SecurityManager> nesnesi içinde bir kilitlenme neden <xref:System.AppDomain> tüm yapma <xref:System.AppDomain> kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-245">For example, a lock on the one <xref:System.Security.SecurityManager> object could cause a deadlock within the <xref:System.AppDomain> making the entire <xref:System.AppDomain> unusable.</span></span> <span data-ttu-id="59bfc-246">Bu tür genel olarak erişilebilir bir nesne üzerinde bir kilit olmayacağına iyi bir uygulamadır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-246">It is good practice to not take a lock on a publicly accessible object of this type.</span></span>  <span data-ttu-id="59bfc-247">Ancak bir tek tek koleksiyonu veya dizisi üzerinde bir kilit genellikle bir sorun yoktur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-247">However a lock on an individual collection or array should generally not present a problem.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-248">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-248">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-249">Kilitleri uygulama etki alanları arasında kullanılan ya da kimlik güçlü bir fikir yok türlerinde kazanmaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-249">Do not take locks on types that might be used across application domains or do not have a strong sense of identity.</span></span> <span data-ttu-id="59bfc-250">Çağırmayın <xref:System.Threading.Monitor.Enter%2A> üzerinde bir <xref:System.Type>, <xref:System.Reflection.MethodInfo>, <xref:System.Reflection.PropertyInfo>, <xref:System.String>, <xref:System.ValueType>, <xref:System.Threading.Thread>, veya türetilen herhangi bir nesne <xref:System.MarshalByRefObject>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-250">Do not call <xref:System.Threading.Monitor.Enter%2A> on a <xref:System.Type>, <xref:System.Reflection.MethodInfo>, <xref:System.Reflection.PropertyInfo>, <xref:System.String>, <xref:System.ValueType>, <xref:System.Threading.Thread>, or any object that derives from <xref:System.MarshalByRefObject>.</span></span>  
  
### <a name="remove-gckeepalive-calls"></a><span data-ttu-id="59bfc-251">GC kaldırın. KeepAlive çağrıları</span><span class="sxs-lookup"><span data-stu-id="59bfc-251">Remove GC.KeepAlive Calls</span></span>  
 <span data-ttu-id="59bfc-252">Var olan kodu önemli miktarda kullanmayan <xref:System.GC.KeepAlive%2A> zaman gerekir veya uygun değilse, bunu kullanır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-252">A significant amount of existing code either does not use <xref:System.GC.KeepAlive%2A> when it should or uses it when it is not appropriate.</span></span>  <span data-ttu-id="59bfc-253">Dönüştürme sonra <xref:System.Runtime.InteropServices.SafeHandle>, sınıfları çağrı gerekmez <xref:System.GC.KeepAlive%2A>, bir sonlandırıcı yoksa ancak Bel varsayılarak <xref:System.Runtime.InteropServices.SafeHandle> işletim sistemi sonlandırmak için işler.</span><span class="sxs-lookup"><span data-stu-id="59bfc-253">After converting to <xref:System.Runtime.InteropServices.SafeHandle>, classes do not need to call <xref:System.GC.KeepAlive%2A>, assuming they do not have a finalizer but rely on <xref:System.Runtime.InteropServices.SafeHandle> to finalize the operating system handles.</span></span>  <span data-ttu-id="59bfc-254">While yapılan bir çağrı korunuyor performans maliyeti <xref:System.GC.KeepAlive%2A> durum, göz ardı edilebilir algısına, çağrı <xref:System.GC.KeepAlive%2A> gerekli ya da artık mevcut olmayabilir sorunu kodu korumak daha zor yapar bir ömrü çözmek yeterli.</span><span class="sxs-lookup"><span data-stu-id="59bfc-254">While the performance cost of retaining a call to <xref:System.GC.KeepAlive%2A> may be negligible, the perception that a call to <xref:System.GC.KeepAlive%2A> is either necessary or sufficient to solve a lifetime issue that may no longer exist makes the code more difficult to maintain.</span></span>  <span data-ttu-id="59bfc-255">Ancak, COM birlikte çalışma CLR aranabilir sarmalayıcılar (RCWs) kullanırken <xref:System.GC.KeepAlive%2A> kod tarafından hala gereklidir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-255">However, when using the COM interop CLR callable wrappers (RCWs), <xref:System.GC.KeepAlive%2A> is still required by code.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-256">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-256">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-257">Kaldırma <xref:System.GC.KeepAlive%2A>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-257">Remove <xref:System.GC.KeepAlive%2A>.</span></span>  
  
### <a name="use-the-host-protection-attribute"></a><span data-ttu-id="59bfc-258">Ana bilgisayar koruması özniteliğini kullanın</span><span class="sxs-lookup"><span data-stu-id="59bfc-258">Use the Host Protection Attribute</span></span>  
 <span data-ttu-id="59bfc-259"><xref:System.Security.Permissions.HostProtectionAttribute> (HPA) gibibelirlianabilgisayariçinuygunolanbazıyöntemleriçağırmabiletamgüvenilirkodunengellemekanabilgisayarizinvererekkonakkorumagereksinimlerinibelirlemekiçinbildirimtemelligüvenlikeylemlerikullanımınısağlar<xref:System.Environment.Exit%2A>veya <xref:System.Windows.Forms.MessageBox.Show%2A> SQL Server için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-259">The <xref:System.Security.Permissions.HostProtectionAttribute> (HPA) provides the use of declarative security actions to determine host protection requirements, allowing the host to prevent even fully trusted code from calling certain methods which are inappropriate for the given host, such as <xref:System.Environment.Exit%2A> or <xref:System.Windows.Forms.MessageBox.Show%2A> for SQL Server.</span></span>  
  
 <span data-ttu-id="59bfc-260">HPA barındıran ortak dil çalışma zamanı ve uygulama ana bilgisayar koruması, SQL Server gibi yalnızca yönetilmeyen uygulamaları etkiler.</span><span class="sxs-lookup"><span data-stu-id="59bfc-260">The HPA affects only unmanaged applications that host the common language runtime and implement host protection, such as SQL Server.</span></span> <span data-ttu-id="59bfc-261">Uygulandığında, bağlantı isteği oluşturulmasına güvenlik eylem sonuçlarını sınıf veya yöntemin çıkarır konak kaynaklara bağlı.</span><span class="sxs-lookup"><span data-stu-id="59bfc-261">When applied, the security action results in the creation of a link demand based on the host resources the class or method exposes.</span></span> <span data-ttu-id="59bfc-262">Kod bir istemci uygulamasında veya ana bilgisayar korumalı olmayan bir sunucuda çalıştırırsanız öznitelik "evaporates"; değil algılanır ve bu nedenle uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-262">If the code is run in a client application or on a server that is not host-protected, the attribute "evaporates"; it is not detected and therefore not applied.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="59bfc-263">Ana bilgisayara özgü programlama modeli yönergeleri, güvenliği davranışını zorlamak için bu öznitelik amacı budur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-263">The purpose of this attribute is to enforce host-specific programming model guidelines, not security behavior.</span></span>  <span data-ttu-id="59bfc-264">Model gereksinimleri programlama için uygunluk denetlemek için bir bağlantı isteği kullanılsa <xref:System.Security.Permissions.HostProtectionAttribute> bir güvenlik izni değil.</span><span class="sxs-lookup"><span data-stu-id="59bfc-264">Although a link demand is used to check for conformance to programming model requirements, the <xref:System.Security.Permissions.HostProtectionAttribute> is not a security permission.</span></span>  
  
 <span data-ttu-id="59bfc-265">Ana, model gereksinimleri programlama yoksa bağlantı talepleri gerçekleşmez.</span><span class="sxs-lookup"><span data-stu-id="59bfc-265">If the host does not have programming model requirements, the link demands do not occur.</span></span>  
  
 <span data-ttu-id="59bfc-266">Bu öznitelik aşağıdakileri tanımlar:</span><span class="sxs-lookup"><span data-stu-id="59bfc-266">This attribute identifies the following:</span></span>  
  
-   <span data-ttu-id="59bfc-267">Yöntemleri veya programlama modeli, ana bilgisayar uymayan sınıfları ancak Aksi halde zararsız.</span><span class="sxs-lookup"><span data-stu-id="59bfc-267">Methods or classes that do not fit the host programming model, but are otherwise benign.</span></span>  
  
-   <span data-ttu-id="59bfc-268">Yöntemleri veya sınıflarda konak programlama modeli sığmayan ve sunucusu yönetilen kullanıcı kodu destabilizing için neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-268">Methods or classes that do not fit the host programming model and could lead to destabilizing server-managed user code.</span></span>  
  
-   <span data-ttu-id="59bfc-269">Yöntemleri veya programlama konak uymayan sınıfları model ve bir sunucu işlemi destabilization için yol açabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-269">Methods or classes that do not fit the host programming model and could lead to a destabilization of the server process itself.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="59bfc-270">Bir korumalı konak ortamında yürütür uygulamalar tarafından çağrılacak olan bir sınıf kitaplığı oluşturuyorsanız, bu öznitelik kullanıma üyelerine uygulamalıdır <xref:System.Security.Permissions.HostProtectionResource> kaynak kategoriler.</span><span class="sxs-lookup"><span data-stu-id="59bfc-270">If you are creating a class library that is to be called by applications that may execute in a host protected environment, you should apply this attribute to members that expose <xref:System.Security.Permissions.HostProtectionResource> resource categories.</span></span> <span data-ttu-id="59bfc-271">Bu öznitelik ile .NET Framework sınıf kitaplığı üyeleri denetlenmesi yalnızca ilk çağıran neden.</span><span class="sxs-lookup"><span data-stu-id="59bfc-271">The .NET Framework class library members with this attribute cause only the immediate caller to be checked.</span></span>  <span data-ttu-id="59bfc-272">Kitaplık üyesini aynı şekilde anında arayanlar kontrol neden gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-272">Your library member must also cause a check of its immediate caller in the same manner.</span></span>  
  
 <span data-ttu-id="59bfc-273">Lütfen HPA hakkında daha fazla bilgi bulmak <xref:System.Security.Permissions.HostProtectionAttribute>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-273">Please find more information on HPA in <xref:System.Security.Permissions.HostProtectionAttribute>.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-274">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-274">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-275">SQL Server için eşitleme veya iş parçacığı oluşturma sunmak için kullanılan tüm yöntemleri gerekir tanımlanan HPA ile.</span><span class="sxs-lookup"><span data-stu-id="59bfc-275">For SQL Server, all methods used to introduce synchronization or threading must identified with the HPA.</span></span> <span data-ttu-id="59bfc-276">Bu durum paylaşmak, eşitlenen ya da dış işlemlerini yönetmek yöntemleri içerir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-276">This includes methods that share state, are synchronized, or manage external processes.</span></span> <span data-ttu-id="59bfc-277"><xref:System.Security.Permissions.HostProtectionResource> SQL Server etkisi değerler <xref:System.Security.Permissions.HostProtectionResource.SharedState>, <xref:System.Security.Permissions.HostProtectionResource.Synchronization>, ve <xref:System.Security.Permissions.HostProtectionResource.ExternalProcessMgmt>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-277">The <xref:System.Security.Permissions.HostProtectionResource> values that impact SQL Server are <xref:System.Security.Permissions.HostProtectionResource.SharedState>, <xref:System.Security.Permissions.HostProtectionResource.Synchronization>, and <xref:System.Security.Permissions.HostProtectionResource.ExternalProcessMgmt>.</span></span> <span data-ttu-id="59bfc-278">Ancak, herhangi bir kullanıma sunan herhangi bir yöntemini <xref:System.Security.Permissions.HostProtectionResource> bir HPA tarafından yalnızca bu SQL etkileyen kaynakları kullanarak tanımlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-278">However, any method that exposes any <xref:System.Security.Permissions.HostProtectionResource> should be identified by a HPA, not just those using resources affecting SQL.</span></span>  
  
### <a name="do-not-block-indefinitely-in-unmanaged-code"></a><span data-ttu-id="59bfc-279">Yönetilmeyen kod içinde süresiz olarak engellemez</span><span class="sxs-lookup"><span data-stu-id="59bfc-279">Do Not Block Indefinitely in Unmanaged Code</span></span>  
 <span data-ttu-id="59bfc-280">Yönetilen kod yerine yönetilmeyen kodunda engelleme, CLR iş parçacığı abort mümkün olmadığı için bir hizmet reddi saldırısına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-280">Blocking in unmanaged code instead of in managed code can cause a denial of service attack because the CLR is not able to abort the thread.</span></span>  <span data-ttu-id="59bfc-281">Engellenen bir iş parçacığı CLR kaldırılmasını engeller <xref:System.AppDomain>, en az son derece güvenli olmayan bazı işlemler yapmak olmadan.</span><span class="sxs-lookup"><span data-stu-id="59bfc-281">A blocked thread prevents the CLR from unloading the <xref:System.AppDomain>, at least without doing some extremely unsafe operations.</span></span>  <span data-ttu-id="59bfc-282">Win32 kullanarak engelleme eşitleme basit bir şey izin veremez Temizle bir örneği bulunmaktadır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-282">Blocking using a Win32 synchronization primitive is a clear example of something we cannot allow.</span></span>  <span data-ttu-id="59bfc-283">Çağrıda engelleme `ReadFile` bir yuvada mümkünse kaçınılmalıdır — ideal Win32 API şuna benzer bir işlem zaman aşımına için bir mekanizma sağlamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-283">Blocking in a call to `ReadFile` on a socket should be avoided if possible — ideally the Win32 API should provide a mechanism for an operation like this to time out.</span></span>  
  
 <span data-ttu-id="59bfc-284">Yerel çağırır herhangi bir yöntemini makul, sonlu zaman aşımı ile bir Win32 çağrı ideal olarak kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-284">Any method that calls into native should ideally use a Win32 call with a reasonable, finite timeout.</span></span>  <span data-ttu-id="59bfc-285">Zaman aşımını belirlemek için kullanıcı izin veriliyorsa, kullanıcı sonsuz bir zaman aşımı bazı belirli güvenlik izni olmadan belirtmek için izin verilmelidir değil.</span><span class="sxs-lookup"><span data-stu-id="59bfc-285">If the user is allowed to specify the timeout, the user should not be allowed to specify an infinite timeout without some specific security permission.</span></span>  <span data-ttu-id="59bfc-286">Bir kılavuz olarak ~ 10 saniyeden fazla için bir yöntem engeller, zaman aşımları destekleyen bir sürüm kullanıyor gerekir veya ek CLR destek gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-286">As a guideline, if a method will block for more than ~10 seconds, you need to be using a version that supports timeouts or you need additional CLR support.</span></span>  
  
 <span data-ttu-id="59bfc-287">Sorunlu API'nin bazı örnekleri aşağıda verilmiştir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-287">Here are some examples of problematic API’s.</span></span>  <span data-ttu-id="59bfc-288">Kanallar (adsız ve adlandırılmış) zaman aşımı ile oluşturulabilir; Ancak, kodu, hiçbir zaman çağrıları emin olmalısınız `CreateNamedPipe` ya da `WaitNamedPipe` NMPWAIT_WAIT_FOREVER ile.</span><span class="sxs-lookup"><span data-stu-id="59bfc-288">Pipes (both anonymous and named) can be created with a timeout; however, code must ensure it never calls `CreateNamedPipe` nor `WaitNamedPipe` with NMPWAIT_WAIT_FOREVER.</span></span>  <span data-ttu-id="59bfc-289">Ayrıca, olabilir bir zaman aşımı belirtilmiş olsa bile engelleme beklenmeyen.</span><span class="sxs-lookup"><span data-stu-id="59bfc-289">Additionally, there can be unexpected blocking even if a timeout is specified.</span></span>  <span data-ttu-id="59bfc-290">Çağırma `WriteFile` arabellek varsa anlamı tüm baytlar yazılır kadar anonim bir kanalda engeller, okunmamış verilerde `WriteFile` çağrısı okuyucu kanal 's arabellek alanı serbest kadar engeller.</span><span class="sxs-lookup"><span data-stu-id="59bfc-290">Calling `WriteFile` on an anonymous pipe will block until all bytes are written, meaning if the buffer has unread data in it, the `WriteFile` call will block until the reader has freed up space in the pipe’s buffer.</span></span>  <span data-ttu-id="59bfc-291">Yuva her zaman bir zaman aşımı mekanizması geliştirir bazı API kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-291">Sockets should always use some API that honors a timeout mechanism.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-292">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-292">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-293">Yönetilmeyen kod zaman aşımı olmadan engelleyen bir hizmet reddi saldırısına olur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-293">Blocking without a timeout in unmanaged code is a denial of service attack.</span></span> <span data-ttu-id="59bfc-294">Platform gerçekleştirmeyin çağrıları çağırma `WaitForSingleObject`, `WaitForSingleObjectEx`, `WaitForMultipleObjects`, `MsgWaitForMultipleObjects`, ve `MsgWaitForMultipleObjectsEx`.</span><span class="sxs-lookup"><span data-stu-id="59bfc-294">Do not perform platform invoke calls to `WaitForSingleObject`, `WaitForSingleObjectEx`, `WaitForMultipleObjects`, `MsgWaitForMultipleObjects`, and `MsgWaitForMultipleObjectsEx`.</span></span>  <span data-ttu-id="59bfc-295">NMPWAIT_WAIT_FOREVER kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-295">Do not use NMPWAIT_WAIT_FOREVER.</span></span>  
  
### <a name="identify-any-sta-dependent-features"></a><span data-ttu-id="59bfc-296">STA'ya bağımlı özellikler tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-296">Identify Any STA-Dependent Features.</span></span>  
 <span data-ttu-id="59bfc-297">Tek iş parçacıklı COM grupların (STAs) kullanan herhangi bir kod tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-297">Identify any code that uses COM single-threaded apartments (STAs).</span></span>  <span data-ttu-id="59bfc-298">STAs SQL Server işleminde devre dışı bırakılır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-298">STAs are disabled in the SQL Server process.</span></span>  <span data-ttu-id="59bfc-299">Bağımlı Özellikler `CoInitialize`performans sayaçlarını veya Pano devre dışı bırakılmalıdır içinde SQL Server gibi.</span><span class="sxs-lookup"><span data-stu-id="59bfc-299">Features that depend on `CoInitialize`, such as performance counters or the clipboard, must be disabled within SQL Server.</span></span>  
  
### <a name="ensure-finalizers-are-free-of-synchronization-problems"></a><span data-ttu-id="59bfc-300">Sonlandırıcılar eşitleme sorunlarını boş olduğundan emin olun</span><span class="sxs-lookup"><span data-stu-id="59bfc-300">Ensure Finalizers Are Free of Synchronization Problems</span></span>  
 <span data-ttu-id="59bfc-301">Birden çok sonlandırıcıyı iş parçacığı gelecekte sonlandırıcılar için aynı anda çalışmasına aynı türde farklı örnekleri anlamına gelir, .NET Framework sürümleri bulunuyor olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-301">Multiple finalizer threads might exist in future versions of the .NET Framework, meaning the finalizers for different instances of the same type run simultaneously.</span></span>  <span data-ttu-id="59bfc-302">Tamamen iş parçacığı içinde korumalı olması gerekmez; Çöp toplayıcı sonlandırıcıyı belirli nesne örneği için yalnızca bir iş parçacığı çalışacağını garanti eder.</span><span class="sxs-lookup"><span data-stu-id="59bfc-302">They do not have to be completely thread safe; the garbage collector guarantees that only one thread will run the finalizer for a given object instance.</span></span>  <span data-ttu-id="59bfc-303">Ancak, sonlandırıcılar yarış durumları ve kilitlenmeleri aynı anda birden çok farklı nesne örnekleri üzerinde çalışırken önlemek için kodlanmış olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-303">However, the finalizers must be coded to avoid race conditions and deadlocks when running simultaneously on multiple different object instances.</span></span>  <span data-ttu-id="59bfc-304">İş parçacığı oluşturma sorunları, bir günlük dosyasında bir sonlandırıcı yazma gibi dış durumu kullanırken ele alınması gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-304">When using any external state, such as writing to a log file, in a finalizer, threading issues must be handled.</span></span>  <span data-ttu-id="59bfc-305">İş parçacığı güvenliği sağlamak sonlandırma kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-305">Do not rely on finalization to provide thread safety.</span></span> <span data-ttu-id="59bfc-306">İş parçacığı yerel depolaması, yönetilen veya yerel, sonlandırıcıyı iş parçacığı üzerinde durumunu depolamak için kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-306">Do not use thread local storage, managed or native, to store state on the finalizer thread.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-307">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-307">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-308">Sonlandırıcılar eşitleme sorunlarını boş olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-308">Finalizers must be free of synchronization problems.</span></span> <span data-ttu-id="59bfc-309">Bir statik değişebilir bir sonlandırıcı durumda kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-309">Do not use a static mutable state in a finalizer.</span></span>  
  
### <a name="avoid-unmanaged-memory-if-possible"></a><span data-ttu-id="59bfc-310">Yönetilmeyen bellek mümkünse kaçının</span><span class="sxs-lookup"><span data-stu-id="59bfc-310">Avoid Unmanaged Memory If Possible</span></span>  
 <span data-ttu-id="59bfc-311">Yalnızca bir işletim sistemi tanıtıcısı gibi yönetilmeyen bellek sızabileceği.</span><span class="sxs-lookup"><span data-stu-id="59bfc-311">Unmanaged memory can be leaked, just like an operating system handle.</span></span>  <span data-ttu-id="59bfc-312">Mümkünse, yığın kullanarak bellek kullanmaya çalıştığınızda [stackalloc](~/docs/csharp/language-reference/keywords/stackalloc.md) veya sabitlenmiş bir yönetilen nesne gibi [deyimi sabit](~/docs/csharp/language-reference/keywords/fixed-statement.md) veya <xref:System.Runtime.InteropServices.GCHandle> kullanarak byte [].</span><span class="sxs-lookup"><span data-stu-id="59bfc-312">If possible, try to use memory on the stack using [stackalloc](~/docs/csharp/language-reference/keywords/stackalloc.md) or a pinned managed object such as the [fixed Statement](~/docs/csharp/language-reference/keywords/fixed-statement.md) or a <xref:System.Runtime.InteropServices.GCHandle> using a byte[].</span></span>  <span data-ttu-id="59bfc-313"><xref:System.GC> Sonunda bunlar temizler.</span><span class="sxs-lookup"><span data-stu-id="59bfc-313">The <xref:System.GC> eventually cleans these up.</span></span>  <span data-ttu-id="59bfc-314">Yönetilmeyen bellek gerekir, ancak türeyen bir sınıf kullanmayı <xref:System.Runtime.InteropServices.SafeHandle> bellek ayırma sarmalamak için.</span><span class="sxs-lookup"><span data-stu-id="59bfc-314">However, if you must allocate unmanaged memory, consider using a class that derives from <xref:System.Runtime.InteropServices.SafeHandle> to wrap the memory allocation.</span></span>  
  
 <span data-ttu-id="59bfc-315">En az bir harf olduğuna dikkat edin nerede <xref:System.Runtime.InteropServices.SafeHandle> yeterli değil.</span><span class="sxs-lookup"><span data-stu-id="59bfc-315">Note that there is at least one case where <xref:System.Runtime.InteropServices.SafeHandle> is not adequate.</span></span>  <span data-ttu-id="59bfc-316">Ayırma ya da boş bellek COM yöntem çağrıları için aracılığıyla bellek ayırmak bir DLL için ortak olan `CoTaskMemAlloc` bu belleğe sahip başka bir DLL boşaltır sonra `CoTaskMemFree`.</span><span class="sxs-lookup"><span data-stu-id="59bfc-316">For COM method calls that allocate or free memory, it is common for one DLL to allocate memory via `CoTaskMemAlloc` then another DLL frees that memory with `CoTaskMemFree`.</span></span>  <span data-ttu-id="59bfc-317">Kullanarak <xref:System.Runtime.InteropServices.SafeHandle> ömrü yönetilmeyen belleği ömrü bağlamanın deneyecek beri bu yerlerde uygunsuz olacaktır <xref:System.Runtime.InteropServices.SafeHandle> diğer DLL denetimi bellek ömrü izin vermek yerine.</span><span class="sxs-lookup"><span data-stu-id="59bfc-317">Using <xref:System.Runtime.InteropServices.SafeHandle> in these places would be inappropriate since it will attempt to tie the lifetime of the unmanaged memory to the lifetime of the <xref:System.Runtime.InteropServices.SafeHandle> instead of allowing the other DLL control the lifetime of the memory.</span></span>  
  
### <a name="review-all-uses-of-catchexception"></a><span data-ttu-id="59bfc-318">Catch(Exception) tüm kullanımlarını gözden geçirin</span><span class="sxs-lookup"><span data-stu-id="59bfc-318">Review All Uses of Catch(Exception)</span></span>  
 <span data-ttu-id="59bfc-319">Catch blokları catch tüm özel durumları tek bir özel durum yerine zaman uyumsuz özel durumlar da şimdi yakalar.</span><span class="sxs-lookup"><span data-stu-id="59bfc-319">Catch blocks that catch all exceptions instead of one specific exception will now catch the asynchronous exceptions as well.</span></span>  <span data-ttu-id="59bfc-320">Büyük olasılıkla yanlış davranışa catch bloğu kendisini işleme içinde yanı sıra atlanabilir hiçbir önemli kaynak serbest bırakma ya da geri çıkma kod arayan her catch(Exception) blok inceleyin bir <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, veya <xref:System.OutOfMemoryException>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-320">Examine every catch(Exception) block, looking for no important resource releasing or backout code that might be skipped, as well as potentially incorrect behavior within the catch block itself for handling a <xref:System.Threading.ThreadAbortException>, <xref:System.StackOverflowException>, or <xref:System.OutOfMemoryException>.</span></span>  <span data-ttu-id="59bfc-321">Bu kod oturum veya tek bir belirli nedenle başarısız oldu, bir özel durum olduğunda, yalnızca belirli özel durumları veya görebileceğiniz bazı varsayımlarda yapmadan mümkün olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-321">Note that it is possible this code might be logging or making some assumptions that it may only see certain exceptions, or that whenever an exception happens it failed for exactly one particular reason.</span></span>  <span data-ttu-id="59bfc-322">Bu varsayımları içerecek şekilde güncelleştirilmesi gerekebilir <xref:System.Threading.ThreadAbortException>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-322">These assumptions may need to be updated to include <xref:System.Threading.ThreadAbortException>.</span></span>  
  
 <span data-ttu-id="59bfc-323">Tüm değiştirme yerleştirir beklediğiniz özel durumu belirli bir türdeki yakalama için tüm özel durum, bu catch gibi göz önünde bulundurun bir <xref:System.FormatException> dizeden biçimlendirme yöntemlerine.</span><span class="sxs-lookup"><span data-stu-id="59bfc-323">Consider changing all places that catch all exceptions to catching a specific type of exception that you expect will be thrown, such as a <xref:System.FormatException> from string formatting methods.</span></span>  <span data-ttu-id="59bfc-324">Bu yakalama bloğunun beklenmeyen özel durumları çalışmasını engeller ve kod hataları beklenmeyen özel durumları yakalama tarafından gizlemez sağlamaya yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-324">This prevents the catch block from running on unexpected exceptions and will help ensure the code does not hide bugs by catching unexpected exceptions.</span></span>  <span data-ttu-id="59bfc-325">Genel kural olarak hiçbir zaman kitaplık kodu bir özel durum işleme (bir özel durum catch gerektiren kod çağırdığınız kodda bir tasarım kusur gösterebilir).</span><span class="sxs-lookup"><span data-stu-id="59bfc-325">As a general rule never handle an exception in library code (code that requires you to catch an exception may indicate a design flaw in the code you are calling).</span></span>  <span data-ttu-id="59bfc-326">Bazı durumlarda bir özel durum yakalamak ve daha fazla veri sağlamak için farklı bir özel durum türüne throw isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-326">In some cases you may want to catch an exception and throw a different exception type to provide more data.</span></span>  <span data-ttu-id="59bfc-327">İç içe geçmiş özel durumlar, bu durumda, gerçek başarısızlığın nedenini depolamak kullanın <xref:System.Exception.InnerException%2A> yeni özel durum özelliği.</span><span class="sxs-lookup"><span data-stu-id="59bfc-327">Use nested exceptions in this case, storing the real cause of the failure in the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-328">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-328">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-329">Yönetilen kod tüm catch blokları Bu catch tüm nesneleri veya catch tüm özel durumları gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-329">Review all catch blocks in managed code that catch all objects or catch all exceptions.</span></span>  <span data-ttu-id="59bfc-330">C# ' ta her ikisi de işaretlemesini yani `catch` {} ve `catch(Exception)` {}.</span><span class="sxs-lookup"><span data-stu-id="59bfc-330">In C#, this means flagging both `catch` {} and `catch(Exception)` {}.</span></span>  <span data-ttu-id="59bfc-331">Özel durum türü belirli yapmayı düşünün veya beklenmeyen özel durum türü yakalar varsa, hatalı bir şekilde hareket değil emin olmak için kodu gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-331">Consider making the exception type very specific, or review the code to ensure it does not act in a bad way if it catches an unexpected exception type.</span></span>  
  
### <a name="do-not-assume-a-managed-thread-is-a-win32-thread--it-is-a-fiber"></a><span data-ttu-id="59bfc-332">Varsayın değil yönetilen iş parçacığı bir Win32 iş parçacığı – bir Fiber olduğu</span><span class="sxs-lookup"><span data-stu-id="59bfc-332">Do Not Assume a Managed Thread Is a Win32 Thread – It Is a Fiber</span></span>  
 <span data-ttu-id="59bfc-333">Yönetilen iş parçacığı yerel depolama çalışır ama, düzgün yönetilmeyen iş parçacığı yerel depolama kullanın veya kodu geçerli işletim sistemi iş parçacığı üzerinde yeniden çalıştıracak varsayın kullanarak.</span><span class="sxs-lookup"><span data-stu-id="59bfc-333">Using managed thread local storage does work, but you may not use unmanaged thread local storage or assume the code will run on the current operating system thread again.</span></span>  <span data-ttu-id="59bfc-334">İş parçacığı yerel ayarları gibi ayarları değiştirmeyin.</span><span class="sxs-lookup"><span data-stu-id="59bfc-334">Do not change settings like the thread’s locale.</span></span>  <span data-ttu-id="59bfc-335">Çağırmayın `InitializeCriticalSection` veya `CreateMutex` kilit girer işletim sistemi iş parçacığına da kilidi çıkmak gerektirdiğinden platform çağırma.</span><span class="sxs-lookup"><span data-stu-id="59bfc-335">Do not call `InitializeCriticalSection` or `CreateMutex` via platform invoke because they require the operating system thread that enters a lock also exit the lock.</span></span>  <span data-ttu-id="59bfc-336">Bu durum lifleri kullanırken değil olacağından, Win32 kritik bölümler ve zaman uyumu sağlayıcılar SQL doğrudan kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-336">Since this will not be the case when using fibers, Win32 critical sections and mutexes cannot be used in SQL directly.</span></span>  <span data-ttu-id="59bfc-337">Unutmayın yönetilen <xref:System.Threading.Mutex> sınıfı, bu iş parçacığı benzeşimini sorunları işlemez.</span><span class="sxs-lookup"><span data-stu-id="59bfc-337">Note that the managed <xref:System.Threading.Mutex> class does not handle these thread affinity concerns.</span></span>  
  
 <span data-ttu-id="59bfc-338">Güvenli bir şekilde durumu çoğunu yönetilen üzerinde kullanabilirsiniz <xref:System.Threading.Thread> yönetilen iş parçacığı yerel depolaması ve iş parçacığının geçerli UI kültürü içeren nesne.</span><span class="sxs-lookup"><span data-stu-id="59bfc-338">You can safely use most of the state on a managed <xref:System.Threading.Thread> object, including managed thread local storage and the thread’s current UI culture.</span></span>  <span data-ttu-id="59bfc-339">Aynı zamanda <xref:System.ThreadStaticAttribute>, hangi yapar mevcut bir statik değişken değerini erişilebilir (Bu, yerel depolama CLR fiber yapmanın bir başka yolu) yalnızca geçerli yönetilen iş parçacığı tarafından.</span><span class="sxs-lookup"><span data-stu-id="59bfc-339">You can also use the <xref:System.ThreadStaticAttribute>, which makes the value of an existing static variable accessible only by the current managed thread (this is another way of doing fiber local storage in the CLR).</span></span>  <span data-ttu-id="59bfc-340">Programlama modeli nedenleri, geçerli bir iş parçacığı kültürünü SQL'de çalıştırırken değiştirilemiyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-340">For programming model reasons, you can not change the current culture of a thread when running in SQL.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-341">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-341">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-342">SQL Server fiber modunda çalışır; iş parçacığı yerel depolaması kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-342">SQL Server runs in fiber mode; do not use thread local storage.</span></span> <span data-ttu-id="59bfc-343">Platform kaçının çağrıları çağırma `TlsAlloc`, `TlsFree`, `TlsGetValue`, ve`TlsSetValue.`</span><span class="sxs-lookup"><span data-stu-id="59bfc-343">Avoid platform invoke calls to `TlsAlloc`, `TlsFree`, `TlsGetValue`, and `TlsSetValue.`</span></span>  
  
### <a name="let-sql-server-handle-impersonation"></a><span data-ttu-id="59bfc-344">SQL Server tanıtıcı kimliğe bürünme sağlar</span><span class="sxs-lookup"><span data-stu-id="59bfc-344">Let SQL Server Handle Impersonation</span></span>  
 <span data-ttu-id="59bfc-345">Kimliğe bürünme iş parçacığı düzeyinde çalışır ve SQL fiber modunda çalıştırmak için yönetilen kod kullanıcıların kimliğine bürüneceği değil ve değil çağırmalıdır `RevertToSelf`.</span><span class="sxs-lookup"><span data-stu-id="59bfc-345">Since impersonation operates on the thread level and SQL can run in fiber mode, managed code should not impersonate users, and should not call `RevertToSelf`.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-346">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-346">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-347">SQL Server kimliğe bürünme işlemesine olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="59bfc-347">Let SQL Server handle impersonation.</span></span> <span data-ttu-id="59bfc-348">Kullanmayın `RevertToSelf`, `ImpersonateAnonymousToken`, `DdeImpersonateClient`, `ImpersonateDdeClientWindow`, `ImpersonateLoggedOnUser`, `ImpersonateNamedPipeClient`, `ImpersonateSelf`, `RpcImpersonateClient`, `RpcRevertToSelf`, `RpcRevertToSelfEx`, veya `SetThreadToken`.</span><span class="sxs-lookup"><span data-stu-id="59bfc-348">Do not use `RevertToSelf`, `ImpersonateAnonymousToken`, `DdeImpersonateClient`, `ImpersonateDdeClientWindow`, `ImpersonateLoggedOnUser`, `ImpersonateNamedPipeClient`, `ImpersonateSelf`, `RpcImpersonateClient`, `RpcRevertToSelf`, `RpcRevertToSelfEx`, or `SetThreadToken`.</span></span>  
  
### <a name="do-not-call-threadsuspend"></a><span data-ttu-id="59bfc-349">Thread::suspend çağırmayın</span><span class="sxs-lookup"><span data-stu-id="59bfc-349">Do Not Call Thread::Suspend</span></span>  
 <span data-ttu-id="59bfc-350">Bir iş parçacığı askıya alabilme basit bir işlemle görünebilir, ancak kilitlenmeleri neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-350">The ability to suspend a thread may appear a simple operation, but it can cause deadlocks.</span></span>  <span data-ttu-id="59bfc-351">Kilit ikinci bir iş parçacığı ve ardından ikinci bir iş parçacığı tarafından askıya bulunduran bir iş parçacığı aynı kilidi alma çalışırsa, bir kilitlenme oluşur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-351">If a thread holding a lock gets suspended by a second thread and then the second thread tries taking the same lock, a deadlock occurs.</span></span>  <span data-ttu-id="59bfc-352"><xref:System.Threading.Thread.Suspend%2A>Güvenlik, sınıf yükleme, remoting ve yansıma ile şu anda etkileyebilir.</span><span class="sxs-lookup"><span data-stu-id="59bfc-352"><xref:System.Threading.Thread.Suspend%2A> can interfere with security, class loading, remoting, and reflection currently.</span></span>  
  
#### <a name="code-analysis-rule"></a><span data-ttu-id="59bfc-353">Kod Analizi kural</span><span class="sxs-lookup"><span data-stu-id="59bfc-353">Code Analysis Rule</span></span>  
 <span data-ttu-id="59bfc-354">Çağırmayın <xref:System.Threading.Thread.Suspend%2A>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-354">Do not call <xref:System.Threading.Thread.Suspend%2A>.</span></span> <span data-ttu-id="59bfc-355">Gerçek bir eşitleme kullanmayı bunun yerine, gibi basit bir <xref:System.Threading.Semaphore> veya <xref:System.Threading.ManualResetEvent> .</span><span class="sxs-lookup"><span data-stu-id="59bfc-355">Consider using a real synchronization primitive instead, such as a <xref:System.Threading.Semaphore> or <xref:System.Threading.ManualResetEvent> .</span></span>  
  
### <a name="protect-critical-operations-with-constrained-execution-regions-and-reliability-contracts"></a><span data-ttu-id="59bfc-356">Kısıtlı yürütme bölgeleri ve güvenilirlik sözleşmeleri ile ilgili kritik işlemleri koruma</span><span class="sxs-lookup"><span data-stu-id="59bfc-356">Protect Critical Operations with Constrained Execution Regions and Reliability Contracts</span></span>  
 <span data-ttu-id="59bfc-357">Karmaşık bir işlemi gerçekleştirirken paylaşılan durum güncelleştirmelerini veya belirleyici biçimde ya da tam olarak başarılı veya tamamen başarısız, kısıtlı yürütme bölge (CER) tarafından korunduğundan emin gerekiyor.</span><span class="sxs-lookup"><span data-stu-id="59bfc-357">When performing a complex operation that updates a shared status or that needs to deterministically either fully succeed or fully fail, be sure that it is protected by a constrained execution region (CER).</span></span> <span data-ttu-id="59bfc-358">Bu kodu her durumda, hatta bir ani iş parçacığı durdurma veya bir ani çalışır garanti <xref:System.AppDomain> kaldırın.</span><span class="sxs-lookup"><span data-stu-id="59bfc-358">This guarantees that the code runs in every case, even an abrupt thread abort or an abrupt <xref:System.AppDomain> unload.</span></span>  
  
 <span data-ttu-id="59bfc-359">Belirli bir CER olan `try/finally` için yapılan bir çağrı tarafından hemen öncesinde blok <xref:System.Runtime.CompilerServices.RuntimeHelpers.PrepareConstrainedRegions%2A>.</span><span class="sxs-lookup"><span data-stu-id="59bfc-359">A CER is a particular `try/finally` block immediately preceded by a call to <xref:System.Runtime.CompilerServices.RuntimeHelpers.PrepareConstrainedRegions%2A>.</span></span>  
  
 <span data-ttu-id="59bfc-360">Bunu yaptığınızda bu nedenle tüm kodda hazırlamak üzere tam zamanı derleyici bildirir çalıştırmadan önce finally bloğunda `try` bloğu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-360">Doing so instructs the just-in-time compiler to prepare all the code in the finally block before running the `try` block.</span></span> <span data-ttu-id="59bfc-361">Bu, kodda garanti finally bloğu yerleşik olarak bulunur ve tüm durumlarda çalışır.</span><span class="sxs-lookup"><span data-stu-id="59bfc-361">This guarantees that the code in the finally block is built and will run in all cases.</span></span> <span data-ttu-id="59bfc-362">Boş bir sağlamak için bir CER seyrek değil `try` bloğu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-362">It is not uncommon in a CER to have an empty `try` block.</span></span> <span data-ttu-id="59bfc-363">Bir CER kullanarak zaman uyumsuz iş parçacığı iptalleri ve bellek yetersiz özel durumları karşı korur.</span><span class="sxs-lookup"><span data-stu-id="59bfc-363">Using a CER protects against asynchronous thread aborts and out-of-memory exceptions.</span></span> <span data-ttu-id="59bfc-364">Bkz: <xref:System.Runtime.CompilerServices.RuntimeHelpers.ExecuteCodeWithGuaranteedCleanup%2A> ayrıca tanıtıcıları yığın verebilmesine derin kodunu taşar CER formu.</span><span class="sxs-lookup"><span data-stu-id="59bfc-364">See <xref:System.Runtime.CompilerServices.RuntimeHelpers.ExecuteCodeWithGuaranteedCleanup%2A> for a form of a CER that additionally handles stack overflows for exceedingly deep code.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="59bfc-365">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="59bfc-365">See Also</span></span>  
 <xref:System.Runtime.ConstrainedExecution>  
 [<span data-ttu-id="59bfc-366">SQL Server programlama ve konak koruması öznitelikleri</span><span class="sxs-lookup"><span data-stu-id="59bfc-366">SQL Server Programming and Host Protection Attributes</span></span>](../../../docs/framework/performance/sql-server-programming-and-host-protection-attributes.md)
