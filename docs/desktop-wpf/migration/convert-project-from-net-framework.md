---
title: WPF uygulamalarını .NET Core 3,0 ' e geçirme
description: Windows Presentation Foundation (WPF) uygulamasını .NET Core 3,0 ' e geçirmeyi öğrenin.
author: mjrousos
ms.date: 09/12/2019
ms.author: mikerou
ms.openlocfilehash: ccd2fc5a49d9c2d31c693e48099732614b568c7b
ms.sourcegitcommit: 1cb64b53eb1f253e6a3f53ca9510ef0be1fd06fe
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/29/2020
ms.locfileid: "82507461"
---
# <a name="migrating-wpf-apps-to-net-core"></a><span data-ttu-id="1999b-103">WPF uygulamalarını .NET Core 'a geçirme</span><span class="sxs-lookup"><span data-stu-id="1999b-103">Migrating WPF apps to .NET Core</span></span>

<span data-ttu-id="1999b-104">Bu makalede, .NET Framework bir Windows Presentation Foundation (WPF) uygulamasını .NET Core 3,0 ' e geçirmek için gereken adımlar ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="1999b-104">This article covers the steps necessary to migrate a Windows Presentation Foundation (WPF) app from .NET Framework to .NET Core 3.0.</span></span> <span data-ttu-id="1999b-105">Bağlantı noktası üzerinde bir WPF uygulamanız yoksa ancak işlemi denemek istiyorsanız, [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader)'Da bulunan **çekirdeklere Trader** örnek uygulamasını kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-105">If you don't have a WPF app on hand to port, but would like to try out the process, you can use the **Bean Trader** sample app available on [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span></span> <span data-ttu-id="1999b-106">Özgün uygulama (hedefleme .NET Framework 4.7.2) NetFx\BeanTraderClient klasöründe bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-106">The original app (targeting .NET Framework 4.7.2) is available in the NetFx\BeanTraderClient folder.</span></span> <span data-ttu-id="1999b-107">İlk olarak, genel olarak uygulama bağlantı noktası için gerekli adımları açıklayacak ve **çekirdeklere Trader** örneğine uygulanan belirli değişiklikleri adım adım inceleyeceğiz.</span><span class="sxs-lookup"><span data-stu-id="1999b-107">First we'll explain the steps necessary to port apps in general, and then we'll walk through the specific changes that apply to the **Bean Trader** sample.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

<span data-ttu-id="1999b-108">.NET Core 'a geçiş yapmak için öncelikle şunları yapmanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="1999b-108">To migrate to .NET Core, you must first:</span></span>

01. <span data-ttu-id="1999b-109">NuGet bağımlılıklarını anlayın ve güncelleştirin:</span><span class="sxs-lookup"><span data-stu-id="1999b-109">Understand and update NuGet dependencies:</span></span>

    01. <span data-ttu-id="1999b-110">`<PackageReference>` Biçimi kullanmak için NuGet bağımlılıklarını yükseltin.</span><span class="sxs-lookup"><span data-stu-id="1999b-110">Upgrade NuGet dependencies to use the `<PackageReference>` format.</span></span>
    01. <span data-ttu-id="1999b-111">.NET Core veya .NET Standard uyumluluğu için en üst düzey NuGet bağımlılıklarını gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="1999b-111">Review top-level NuGet dependencies for .NET Core or .NET Standard compatibility.</span></span>
    01. <span data-ttu-id="1999b-112">NuGet paketlerini yeni sürümlere yükseltin.</span><span class="sxs-lookup"><span data-stu-id="1999b-112">Upgrade NuGet packages to newer versions.</span></span>
    01. <span data-ttu-id="1999b-113">.Net bağımlılıklarını anlamak için [.net taşınabilirlik Çözümleyicisi](../../standard/analyzers/portability-analyzer.md) 'ni kullanın.</span><span class="sxs-lookup"><span data-stu-id="1999b-113">Use the [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) to understand .NET dependencies.</span></span>

01. <span data-ttu-id="1999b-114">Proje dosyasını yeni SDK stili biçimine geçirin:</span><span class="sxs-lookup"><span data-stu-id="1999b-114">Migrate the project file to the new SDK-style format:</span></span>

    01. <span data-ttu-id="1999b-115">Hem .NET Core hem de .NET Framework mi yoksa yalnızca .NET Core 'un mi hedefedilmeyeceğini seçin.</span><span class="sxs-lookup"><span data-stu-id="1999b-115">Choose whether to target both .NET Core and .NET Framework, or only .NET Core.</span></span>
    01. <span data-ttu-id="1999b-116">İlgili proje dosyası özelliklerini ve öğelerini yeni proje dosyasına kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="1999b-116">Copy relevant project file properties and items to the new project file.</span></span>

01. <span data-ttu-id="1999b-117">Derleme sorunlarını giderme:</span><span class="sxs-lookup"><span data-stu-id="1999b-117">Fix build issues:</span></span>

    01. <span data-ttu-id="1999b-118">[Microsoft. Windows. uyumluluk](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) paketine bir başvuru ekleyin.</span><span class="sxs-lookup"><span data-stu-id="1999b-118">Add a reference to the [Microsoft.Windows.Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) package.</span></span>
    01. <span data-ttu-id="1999b-119">API düzeyi farklılıkları bulun ve onarın.</span><span class="sxs-lookup"><span data-stu-id="1999b-119">Find and fix API-level differences.</span></span>
    01. <span data-ttu-id="1999b-120">`appSettings` Veya `connectionStrings`dışındaki *app. config* bölümlerini kaldırın.</span><span class="sxs-lookup"><span data-stu-id="1999b-120">Remove *app.config* sections other than `appSettings` or `connectionStrings`.</span></span>
    01. <span data-ttu-id="1999b-121">Gerekirse oluşturulan kodu yeniden oluşturun.</span><span class="sxs-lookup"><span data-stu-id="1999b-121">Regenerate generated code, if necessary.</span></span>

01. <span data-ttu-id="1999b-122">Çalışma zamanı testi:</span><span class="sxs-lookup"><span data-stu-id="1999b-122">Runtime testing:</span></span>

    01. <span data-ttu-id="1999b-123">Bağlantı verilen uygulamanın beklendiği gibi çalıştığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="1999b-123">Confirm the ported app works as expected.</span></span>
    01. <span data-ttu-id="1999b-124"><xref:System.NotSupportedException> Özel durumlara dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="1999b-124">Beware of <xref:System.NotSupportedException> exceptions.</span></span>

## <a name="about-the-sample"></a><span data-ttu-id="1999b-125">Örnek hakkında</span><span class="sxs-lookup"><span data-stu-id="1999b-125">About the sample</span></span>

<span data-ttu-id="1999b-126">Bu makale, gerçek dünyada WPF uygulamalarının sahip olabileceği gibi çeşitli bağımlılıklar kullandığından, [çekirdeklere Trader örnek uygulamasına](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) başvurur.</span><span class="sxs-lookup"><span data-stu-id="1999b-126">This article references the [Bean Trader sample app](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) because it uses a variety of dependencies similar to those that real-world WPF apps might have.</span></span> <span data-ttu-id="1999b-127">Uygulama büyük değildir, ancak karmaşıklık bakımından ' Merhaba Dünya ' öğesinden bir adım adım olmak üzere tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="1999b-127">The app isn't large, but is meant to be a step up from 'Hello World' in terms of complexity.</span></span> <span data-ttu-id="1999b-128">Uygulama, kullanıcıların gerçek uygulamaları taşıma sırasında karşılaşabileceği bazı sorunları gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-128">The app demonstrates some issues users may encounter while porting real apps.</span></span> <span data-ttu-id="1999b-129">Uygulama bir WCF hizmeti ile iletişim kurar, bu nedenle düzgün çalışması için BeanTraderServer projesini çalıştırmanız gerekir (aynı GitHub deposunda mevcuttur) ve BeanTraderClient yapılandırmasının doğru uç noktayı işaret ettiğini doğrulayın.</span><span class="sxs-lookup"><span data-stu-id="1999b-129">The app communicates with a WCF service, so for it to run properly, you'll also need to run the BeanTraderServer project (available in the same GitHub repository) and make sure the BeanTraderClient configuration points to the correct endpoint.</span></span> <span data-ttu-id="1999b-130">(Varsayılan olarak, örnek, sunucunun *http://localhost:8090*' de aynı makinede çalıştığını varsayar, bu, BeanTraderServer 'ı yerel olarak başlatırsanız doğru olacaktır.)</span><span class="sxs-lookup"><span data-stu-id="1999b-130">(By default, the sample assumes the server is running on the same machine at *http://localhost:8090*, which will be true if you launch BeanTraderServer locally.)</span></span>

<span data-ttu-id="1999b-131">Bu örnek uygulamanın .NET Core 'a yönelik güçlükleri ve çözümleri göstermesi gerektiğini aklınızda bulundurun.</span><span class="sxs-lookup"><span data-stu-id="1999b-131">Keep in mind that this sample app is meant to demonstrate .NET Core porting challenges and solutions.</span></span> <span data-ttu-id="1999b-132">WPF en iyi yöntemlerini göstermek için tasarlanmamıştır.</span><span class="sxs-lookup"><span data-stu-id="1999b-132">It's not meant to demonstrate WPF best practices.</span></span> <span data-ttu-id="1999b-133">Aslında, taşıma sırasında en az birkaç ilginç zorluk çekdiğinizden emin olmak için bazı çapraz düzenleri kasıtlı olarak içerir.</span><span class="sxs-lookup"><span data-stu-id="1999b-133">In fact, it deliberately includes some anti-patterns to make sure you come across at least a couple of interesting challenges while porting.</span></span>

## <a name="getting-ready"></a><span data-ttu-id="1999b-134">Hazırlanma</span><span class="sxs-lookup"><span data-stu-id="1999b-134">Getting ready</span></span>

<span data-ttu-id="1999b-135">.NET Framework uygulamayı .NET Core 'a geçirmeye yönelik birincil zorluk, bağımlılıklarının farklı şekilde çalışması veya hiç olmaması olabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-135">The primary challenge of migrating a .NET Framework app to .NET Core is that its dependencies may work differently or not at all.</span></span> <span data-ttu-id="1999b-136">Geçiş, şundan çok daha kolay; birçok NuGet paketi artık .NET Standard hedefleyin.</span><span class="sxs-lookup"><span data-stu-id="1999b-136">Migration is much easier than it used to be; many NuGet packages now target .NET Standard.</span></span> <span data-ttu-id="1999b-137">.NET Core 2,0 ile başlayarak .NET Framework ve .NET Core yüzey alanları benzer hale gelmiştir.</span><span class="sxs-lookup"><span data-stu-id="1999b-137">Starting with .NET Core 2.0, the .NET Framework and .NET Core surface areas have become similar.</span></span> <span data-ttu-id="1999b-138">Bu nedenle bile, bazı farklılıklar (hem NuGet paketlerinden hem de kullanılabilir .NET API 'Lerinde desteklenir) kalır.</span><span class="sxs-lookup"><span data-stu-id="1999b-138">Even so, some differences (both in support from NuGet packages and in available .NET APIs) remain.</span></span> <span data-ttu-id="1999b-139">Geçirilmekte olan ilk adım, uygulamanın bağımlılıklarını gözden geçirmekte ve başvuruların .NET Core 'a kolayca geçirilmiş bir biçimde olduğundan emin olmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="1999b-139">The first step in migrating is to review the app's dependencies and make sure references are in a format that's easily migrated to .NET Core.</span></span>

### <a name="upgrade-to-packagereference-nuget-references"></a><span data-ttu-id="1999b-140">`<PackageReference>` NuGet başvurularına yükselt</span><span class="sxs-lookup"><span data-stu-id="1999b-140">Upgrade to `<PackageReference>` NuGet references</span></span>

<span data-ttu-id="1999b-141">Daha eski .NET Framework projeleri, genellikle bir *Packages. config* dosyasında NuGet bağımlılıklarını listeler.</span><span class="sxs-lookup"><span data-stu-id="1999b-141">Older .NET Framework projects typically list their NuGet dependencies in a *packages.config* file.</span></span> <span data-ttu-id="1999b-142">Yeni SDK stili proje dosyası biçimi, NuGet paketlerine ayrı bir yapılandırma [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) dosyası yerine csproj dosyasındaki öğeler olarak başvurur.</span><span class="sxs-lookup"><span data-stu-id="1999b-142">The new SDK-style project file format references NuGet packages as [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elements in the csproj file itself rather than in a separate config file.</span></span>

<span data-ttu-id="1999b-143">Geçiş yaparken, stili başvuruların kullanılmasıyla `<PackageReference>`iki avantajı vardır:</span><span class="sxs-lookup"><span data-stu-id="1999b-143">When migrating, there are two advantages to using `<PackageReference>`-style references:</span></span>

- <span data-ttu-id="1999b-144">Bu, yeni .NET Core proje dosyası için gerekli olan NuGet başvurusunun stilidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-144">This is the style of NuGet reference that is required for the new .NET Core project file.</span></span> <span data-ttu-id="1999b-145">Zaten kullanıyorsanız `<PackageReference>`, bu proje dosyası öğeleri kopyalanabilir ve doğrudan yeni projeye yapıştırılabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-145">If you're already using `<PackageReference>`, those project file elements can be copied and pasted directly into the new project.</span></span>
- <span data-ttu-id="1999b-146">Packages. config dosyasından farklı olarak, `<PackageReference>` öğeler yalnızca projenizin doğrudan bağlı olduğu en üst düzey bağımlılıklara başvurur.</span><span class="sxs-lookup"><span data-stu-id="1999b-146">Unlike a packages.config file, `<PackageReference>` elements only refer to the top-level dependencies that your project depends on directly.</span></span> <span data-ttu-id="1999b-147">Diğer tüm geçişli NuGet paketleri geri yükleme sırasında belirlenir ve otomatik olarak oluşturulan obj\project.assets.JSON dosyasına kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-147">All other transitive NuGet packages will be determined at restore time and recorded in the autogenerated obj\project.assets.json file.</span></span> <span data-ttu-id="1999b-148">Bu, projenizin hangi bağımlılıklara sahip olduğunu belirlemeyi çok daha kolay hale getirir. Bu, gerekli bağımlılıkların .NET Core 'da çalışıp çalışmadığını belirlemede yararlı olur.</span><span class="sxs-lookup"><span data-stu-id="1999b-148">This makes it much easier to determine what dependencies your project has, which is useful when determining whether the necessary dependencies will work on .NET Core or not.</span></span>

<span data-ttu-id="1999b-149">.NET Framework uygulamayı .NET Core 'a geçirmeye yönelik ilk adım, NuGet başvurularını kullanacak `<PackageReference>` şekilde günceldir.</span><span class="sxs-lookup"><span data-stu-id="1999b-149">The first step to migrating a .NET Framework app to .NET Core is to update it to use `<PackageReference>` NuGet references.</span></span> <span data-ttu-id="1999b-150">Visual Studio bu basit hale getirir.</span><span class="sxs-lookup"><span data-stu-id="1999b-150">Visual Studio makes this simple.</span></span> <span data-ttu-id="1999b-151">Visual Studio 'nun **Çözüm Gezgini**projenin *Packages. config* dosyasına sağ tıklayıp, **Packages. config 'i packagereference öğesine geçir**' i seçmeniz yeterlidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-151">Just right-click the project's *packages.config* file in Visual Studio's **Solution Explorer**, and then select **Migrate packages.config to PackageReference**.</span></span>

![PackageReference 'a yükseltme](./media/convert-project-from-net-framework/package-reference-migration.png)

<span data-ttu-id="1999b-153">Hesaplanan en üst düzey NuGet bağımlılıklarını gösteren bir iletişim kutusu görünür ve diğer NuGet paketlerinin en üst düzeye yükseltilmesi gerektiğini sorar.</span><span class="sxs-lookup"><span data-stu-id="1999b-153">A dialog appears showing calculated top-level NuGet dependencies and asking which other NuGet packages should be promoted to top-level.</span></span> <span data-ttu-id="1999b-154">Bu diğer paketlerin hiçbirinin, çekirdeklere Trader örneği için en üst düzey olması gerekmez, bu nedenle bu kutuların tümünün işaretini kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-154">None of these other packages need to be top-level for the Bean Trader sample, so you can uncheck all of those boxes.</span></span> <span data-ttu-id="1999b-155">Ardından **Tamam** ' a tıklayın ve *Packages. config* dosyası kaldırılır ve `<PackageReference>` öğeler proje dosyasına eklenir.</span><span class="sxs-lookup"><span data-stu-id="1999b-155">Then, click **Ok** and the *packages.config* file is removed and `<PackageReference>` elements are added to the project file.</span></span>

<span data-ttu-id="1999b-156">`<PackageReference>`-Style başvuruları, NuGet paketlerini bir paketler klasöründe yerel olarak depolamaz.</span><span class="sxs-lookup"><span data-stu-id="1999b-156">`<PackageReference>`-style references don't store NuGet packages locally in a packages folder.</span></span> <span data-ttu-id="1999b-157">Bunun yerine, genel bir iyileştirme olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="1999b-157">Instead, they're stored globally as an optimization.</span></span> <span data-ttu-id="1999b-158">Geçiş tamamlandıktan sonra, csproj dosyasını düzenleyin ve ' den daha önce `<Analyzer>` gelen çözümleyiciler ile ilgili tüm öğeleri *kaldırın. \packages* dizini.</span><span class="sxs-lookup"><span data-stu-id="1999b-158">After the migration completes, edit the csproj file and remove any `<Analyzer>` elements referring to the analyzers that previously came from the *..\packages* directory.</span></span> <span data-ttu-id="1999b-159">Endişelenmeyin; NuGet paketi başvurularını hala içerdiğinden, çözümleyiciler projeye dahil edilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-159">Don't worry; since you still have the NuGet package references, the analyzers will be included in the project.</span></span> <span data-ttu-id="1999b-160">Yalnızca eski paketler. config stilindeki `<Analyzer>` öğeleri temizlemeniz yeterlidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-160">You just need to clean up the old packages.config-style `<Analyzer>` elements.</span></span>

### <a name="review-nuget-packages"></a><span data-ttu-id="1999b-161">NuGet paketlerini gözden geçir</span><span class="sxs-lookup"><span data-stu-id="1999b-161">Review NuGet packages</span></span>

<span data-ttu-id="1999b-162">Artık projenin bağımlı olduğu en üst düzey NuGet paketlerini görebilmeniz için, bu paketlerin .NET Core 'da kullanılabilir olup olmadığını gözden geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-162">Now that you can see the top-level NuGet packages that the project depends on, you can review whether those packages are available on .NET Core.</span></span> <span data-ttu-id="1999b-163">[NuGet.org](https://www.nuget.org/)' deki bağımlılıklara bakarak bir paketin .NET Core 'u destekleyip desteklemediğini belirleyebilirsiniz. Topluluk tarafından oluşturulan [fuget.org](https://www.fuget.org/) sitesi, bu bilgileri paket bilgileri sayfasının en üstünde belirgin şekilde gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-163">You can determine whether a package supports .NET Core by looking at its dependencies on [nuget.org](https://www.nuget.org/). The community-created [fuget.org](https://www.fuget.org/) site shows this information prominently at the top of the package information page.</span></span>

<span data-ttu-id="1999b-164">.NET Core 3,0 hedeflenirken, .NET Core veya .NET Standard hedefleyen tüm paketler çalışır (.NET Core .NET Standard Surface alanını gerçekleştirdiğinden).</span><span class="sxs-lookup"><span data-stu-id="1999b-164">When targeting .NET Core 3.0, any packages targeting .NET Core or .NET Standard should work (since .NET Core implements the .NET Standard surface area).</span></span> <span data-ttu-id="1999b-165">Bazı durumlarda, kullanılan bir paketin belirli sürümü .NET Core veya .NET Standard hedeflemiyor, ancak daha yeni sürümler olacak.</span><span class="sxs-lookup"><span data-stu-id="1999b-165">In some cases, the specific version of a package that's used won't target .NET Core or .NET Standard, but newer versions will.</span></span> <span data-ttu-id="1999b-166">Bu durumda, paketin en son sürümüne yükseltmeyi göz önünde bulundurmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-166">In this case, you should consider upgrading to the latest version of the package.</span></span>

<span data-ttu-id="1999b-167">.NET Framework hedefleyen paketleri de kullanabilirsiniz, ancak bu da bazı riskler sağlar.</span><span class="sxs-lookup"><span data-stu-id="1999b-167">You can use packages targeting .NET Framework, as well, but that introduces some risk.</span></span> <span data-ttu-id="1999b-168">.NET Core ve .NET Framework Surface alanları bu tür bağımlılıkların *genellikle* çalıştığı kadar benzer olduğundan, .net core 'a .NET Framework bağımlılıklara izin verilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-168">.NET Core to .NET Framework dependencies are allowed because .NET Core and .NET Framework surface areas are similar enough that such dependencies *often* work.</span></span> <span data-ttu-id="1999b-169">Ancak, paket .NET Core 'da mevcut olmayan bir .NET API kullanmaya çalışırsa, bir çalışma zamanı özel durumu ile karşılaşırsınız.</span><span class="sxs-lookup"><span data-stu-id="1999b-169">However, if the package tries to use a .NET API that isn't present in .NET Core, you'll encounter a runtime exception.</span></span> <span data-ttu-id="1999b-170">Bu nedenle, başka seçenek yoksa yalnızca .NET Framework paketlerine başvurmalıdır ve bu işlemin bir test yükü uyguladığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="1999b-170">Because of that, you should only reference .NET Framework packages when no other options are available and understand that doing so imposes a test burden.</span></span>

<span data-ttu-id="1999b-171">.NET Core veya .NET Standard hedefleyen paketlere başvuruluyorsa, diğer alternatifleri düşünmek zorunda kalmazsınız:</span><span class="sxs-lookup"><span data-stu-id="1999b-171">If there are packages referenced that don't target .NET Core or .NET Standard, you'll have to think about other alternatives:</span></span>

- <span data-ttu-id="1999b-172">Bunun yerine kullanılabilecek başka benzer paketler var mı?</span><span class="sxs-lookup"><span data-stu-id="1999b-172">Are there other similar packages that can be used instead?</span></span> <span data-ttu-id="1999b-173">Bazen NuGet yazarları ayrı olarak yayımlar '. .NET Core 'un özel olarak hedeflediği kitaplıkların temel sürümleri.</span><span class="sxs-lookup"><span data-stu-id="1999b-173">Sometimes NuGet authors publish separate '.Core' versions of their libraries specifically targeting .NET Core.</span></span> <span data-ttu-id="1999b-174">Kurumsal kitaplık paketleri, topluluk yayımlamanın bir örneğidir ". NetCore "alternatifler.</span><span class="sxs-lookup"><span data-stu-id="1999b-174">Enterprise Library packages are an example of the community publishing ".NetCore" alternatives.</span></span> <span data-ttu-id="1999b-175">Diğer durumlarda, belirli bir hizmet için yeni SDK 'lar (bazen farklı paket adlarıyla birlikte) .NET Standard için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-175">In other cases, newer SDKs for a particular service (sometimes with different package names) are available for .NET Standard.</span></span> <span data-ttu-id="1999b-176">Kullanılabilir alternatif yoksa, .NET Core üzerinde çalışırken bunları tamamen test etmeniz gerektiğini aklınızda bulundurarak .NET Framework hedefli paketleri kullanmaya devam edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-176">If no alternatives are available, you can proceed using the .NET Framework-targeted packages, bearing in mind that you'll need to test them thoroughly once running on .NET Core.</span></span>

<span data-ttu-id="1999b-177">Çekirdeklere Trader örneği aşağıdaki en üst düzey NuGet bağımlılıklara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="1999b-177">The Bean Trader sample has the following top-level NuGet dependencies:</span></span>

- [<span data-ttu-id="1999b-178">**Role. Wınsörü, sürüm 4.1.1**</span><span class="sxs-lookup"><span data-stu-id="1999b-178">**Castle.Windsor, version 4.1.1**</span></span>](https://www.castleproject.org/projects/windsor/)  

  <span data-ttu-id="1999b-179">Bu paket, .NET Core üzerinde çalıştığından 1,6 .NET Standard hedefler.</span><span class="sxs-lookup"><span data-stu-id="1999b-179">This package targets .NET Standard 1.6, so it works on .NET Core.</span></span>

- [<span data-ttu-id="1999b-180">**Microsoft. CodeAnalysis. Fxcopçözümleyiciler, sürüm 2.6.3**</span><span class="sxs-lookup"><span data-stu-id="1999b-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, version 2.6.3**</span></span>](https://www.nuget.org/packages/Microsoft.CodeAnalysis.FxCopAnalyzers/2.6.3)  
  <span data-ttu-id="1999b-181">Bu bir meta pakettir. bu nedenle, hangi platformların desteklediği, ancak [belge](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) en yeni sürümünün (2.9.2) hem .NET Framework hem de .NET Core için çalıştığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-181">This is a meta-package, so it's not immediately obvious which platforms it supports, but [documentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indicates that its newest version (2.9.2) will work for both .NET Framework and .NET Core.</span></span>

- [<span data-ttu-id="1999b-182">**Nito. AsyncEx, sürüm 4.0.1**</span><span class="sxs-lookup"><span data-stu-id="1999b-182">**Nito.AsyncEx, version 4.0.1**</span></span>](https://www.nuget.org/packages/Nito.AsyncEx/4.0.1)  

  <span data-ttu-id="1999b-183">Bu paket .NET Core 'u hedeflemiyor, ancak daha yeni 5,0 sürümü.</span><span class="sxs-lookup"><span data-stu-id="1999b-183">This package doesn't target .NET Core, but the newer 5.0 version does.</span></span> <span data-ttu-id="1999b-184">Bu, geçiş sırasında yaygındır çünkü çok sayıda NuGet paketi son zamanlarda .NET Standard destek ekledi, ancak eski proje sürümleri yalnızca .NET Framework hedefleyecektir.</span><span class="sxs-lookup"><span data-stu-id="1999b-184">This is common when migrating because many NuGet packages have added .NET Standard support recently, but older project versions will only target .NET Framework.</span></span> <span data-ttu-id="1999b-185">Sürüm farkı yalnızca küçük bir sürüm farklılığı ise, daha yeni sürüme yükseltmek genellikle kolaydır.</span><span class="sxs-lookup"><span data-stu-id="1999b-185">If the version difference is only a minor version difference, it's often easy to upgrade to the newer version.</span></span> <span data-ttu-id="1999b-186">Bu bir ana sürüm değişikliği olduğundan, pakette önemli değişiklikler olabileceğinden, yükseltmenin dikkatli olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-186">Because this is a major version change, you need to be cautious upgrading since there could be breaking changes in the package.</span></span> <span data-ttu-id="1999b-187">Bir yol ileri olur, ancak bu iyi bir yoldur.</span><span class="sxs-lookup"><span data-stu-id="1999b-187">There is a path forward, though, which is good.</span></span>

- [<span data-ttu-id="1999b-188">**MahApps. Metro, sürüm 1.6.5**</span><span class="sxs-lookup"><span data-stu-id="1999b-188">**MahApps.Metro, version 1.6.5**</span></span>](https://www.nuget.org/packages/MahApps.Metro/1.6.5)  

  <span data-ttu-id="1999b-189">Bu paket ayrıca .NET Core 'u hedeflemez, ancak daha yeni bir yayın öncesi sürüm (2,0-Alpha) içerir.</span><span class="sxs-lookup"><span data-stu-id="1999b-189">This package also doesn't target .NET Core, but has a newer pre-release (2.0-alpha) that does.</span></span> <span data-ttu-id="1999b-190">Yine de, son değişiklikleri gözden geçirin, ancak daha yeni paket teşvik.</span><span class="sxs-lookup"><span data-stu-id="1999b-190">Again, you have to look out for breaking changes, but the newer package is encouraging.</span></span>

<span data-ttu-id="1999b-191">Çekirdeklere Trader örneğinin NuGet bağımlılıkları her iki hedef .NET Standard/. NET Core veya daha yeni sürümlere sahiptir, bu nedenle burada herhangi bir engelleyici sorun olması düşüktür.</span><span class="sxs-lookup"><span data-stu-id="1999b-191">The Bean Trader sample's NuGet dependencies all either target .NET Standard/.NET Core or have newer versions that do, so there are unlikely to be any blocking issues here.</span></span>

### <a name="upgrade-nuget-packages"></a><span data-ttu-id="1999b-192">NuGet paketlerini yükselt</span><span class="sxs-lookup"><span data-stu-id="1999b-192">Upgrade NuGet packages</span></span>

<span data-ttu-id="1999b-193">Mümkünse, bu noktada yalnızca .NET Core veya .NET Standard hedef olan tüm paketlerin sürümlerini yükseltmek iyi olabilir (proje, hala .NET Framework hedefleyerek), herhangi bir son değişiklik bulmayı ve bu değişiklikleri daha önce bulur ve adreslemesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="1999b-193">If possible, it would be good to upgrade the versions of any packages that only target .NET Core or .NET Standard with more recent versions at this point (with the project still targeting .NET Framework) to discover and address any breaking changes early.</span></span>

<span data-ttu-id="1999b-194">Uygulamanın mevcut .NET Framework sürümünde herhangi bir malzeme değişikliği yapmayın, bu, .NET Core 'u hedefleyen yeni bir proje dosyası olana kadar bekleyebilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-194">If you would rather not make any material changes to the existing .NET Framework version of the app, this can wait until you have a new project file targeting .NET Core.</span></span> <span data-ttu-id="1999b-195">Ancak, NuGet paketlerini .NET Core ile uyumlu sürümlere yükseltmek, yeni proje dosyasını oluşturduktan sonra geçiş işlemini daha da kolay hale getirir ve uygulamanın .NET Framework ve .NET Core sürümleri arasındaki farkları sayısını azaltır.</span><span class="sxs-lookup"><span data-stu-id="1999b-195">However, upgrading the NuGet packages to .NET Core-compatible versions ahead of time makes the migration process even easier once you create the new project file and reduces the number of differences between the .NET Framework and .NET Core versions of the app.</span></span>

<span data-ttu-id="1999b-196">Çekirdeklere Trader örneğinde, tüm gerekli yükseltmeler (Visual Studio 'nun NuGet Paket Yöneticisi kullanılarak) tek bir özel durumla kolayca yapılabilir: **Mahapps 'den yükseltme. Metro 1.6.5** ile **2,0** arasında, tema ve vurgu yönetimi API 'leriyle ilgili önemli değişiklikler ortaya çıkarır.</span><span class="sxs-lookup"><span data-stu-id="1999b-196">With the Bean Trader sample, all of the necessary upgrades can be made easily (using Visual Studio's NuGet package manager) with one exception: upgrading from **MahApps.Metro 1.6.5** to **2.0** reveals breaking changes related to theme and accent management APIs.</span></span>

<span data-ttu-id="1999b-197">İdeal olarak, uygulama paketin daha yeni sürümünü kullanacak şekilde güncelleştirilir (Bu, .NET Core üzerinde çalışmaya daha olasıdır).</span><span class="sxs-lookup"><span data-stu-id="1999b-197">Ideally, the app would be updated to use the newer version of the package (since that is more likely to work on .NET Core).</span></span> <span data-ttu-id="1999b-198">Ancak bazı durumlarda, bu işlem uygulanabilir olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-198">In some cases, however, that may not be feasible.</span></span> <span data-ttu-id="1999b-199">Bu gibi durumlarda, gerekli değişiklikler önemsiz olmadığından ve bu öğretici, .NET Core 3 ' e geçirmeye odaklandığından, mahapps. Metro 2 için değil, **mahapps. Metro** 'yı yükseltmeyin **.**</span><span class="sxs-lookup"><span data-stu-id="1999b-199">In these cases, don't upgrade **MahApps.Metro** because the necessary changes are non-trivial and this tutorial focuses on migrating to .NET Core 3, not to **MahApps.Metro 2.**</span></span> <span data-ttu-id="1999b-200">Ayrıca, çekirdeklere Trader uygulaması yalnızca **Mahapps. Metro**'nın küçük bir kısmını sağladığından, bu düşük riskli .NET Framework bir bağımlılıkdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-200">Also, this is a low-risk .NET Framework dependency because the Bean Trader app only exercises a small part of **MahApps.Metro**.</span></span> <span data-ttu-id="1999b-201">Kuşkusuz, geçiş işlemi tamamlandıktan sonra her şeyin çalıştığından emin olmak için test gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-201">It will, of course, require testing to make sure everything's working once the migration is complete.</span></span> <span data-ttu-id="1999b-202">Bu gerçek dünyada bir senaryosa, daha önce bazı teknik borcunun gerisinde kalmasını sağlamak için, bu işlemi izlemek üzere **Mahapps. Metro** sürüm 2,0 ' ye geçiş yapmak için bir sorun gidermek iyi olacaktır.</span><span class="sxs-lookup"><span data-stu-id="1999b-202">If this were a real-world scenario, it would be good to file an issue to track the work to move to **MahApps.Metro** version 2.0 since not doing the migration now leaves behind some technical debt.</span></span>

<span data-ttu-id="1999b-203">NuGet paketleri son sürümlere güncelleştirildikten sonra, çekirdeklere Trader örneğinin `<PackageReference>` proje dosyasındaki öğe grubu şuna benzemelidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-203">Once the NuGet packages are updated to recent versions, the `<PackageReference>` item group in the Bean Trader sample's project file should look like this.</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Castle.Windsor">
    <Version>4.1.1</Version>
  </PackageReference>
  <PackageReference Include="MahApps.Metro">
    <Version>1.6.5</Version>
  </PackageReference>
  <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers">
    <Version>2.9.2</Version>
  </PackageReference>
  <PackageReference Include="Nito.AsyncEx">
    <Version>5.0.0</Version>
  </PackageReference>
</ItemGroup>
```

### <a name="net-framework-portability-analysis"></a><span data-ttu-id="1999b-204">.NET Framework taşınabilirlik Analizi</span><span class="sxs-lookup"><span data-stu-id="1999b-204">.NET Framework portability analysis</span></span>

<span data-ttu-id="1999b-205">Projenizin NuGet bağımlılıklarının durumunu anladığınızda, dikkate alınması gereken sonraki şey .NET Framework API bağımlılıklarıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-205">Once you understand the state of your project's NuGet dependencies, the next thing to consider is .NET Framework API dependencies.</span></span> <span data-ttu-id="1999b-206">[.Net taşınabilirlik Çözümleyicisi](../../standard/analyzers/portability-analyzer.md) Aracı, projenizin kullandığı .NET API 'lerinin hangisinin diğer .net platformlarında kullanılabildiğini anlamak için yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-206">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) tool is useful for understanding which of the .NET APIs your project uses are available on other .NET platforms.</span></span>

<span data-ttu-id="1999b-207">Araç, bir [Visual Studio eklentisi](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), bir [komut satırı aracı](https://github.com/Microsoft/dotnet-apiport/releases)veya [basit bir GUI](https://github.com/Microsoft/dotnet-apiport-ui)'de Sarmalanan, seçeneklerini kolaylaştıran bir araç olarak sunulur.</span><span class="sxs-lookup"><span data-stu-id="1999b-207">The tool comes as a [Visual Studio plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), a [command-line tool](https://github.com/Microsoft/dotnet-apiport/releases), or wrapped in a [simple GUI](https://github.com/Microsoft/dotnet-apiport-ui), which simplifies its options.</span></span> <span data-ttu-id="1999b-208">.NET taşınabilirlik Çözümleyicisi 'ni (API bağlantı noktası) kullanma hakkında daha fazla bilgiyi, [masaüstü uygulamalarında .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog gönderisine TAŞıMA bölümündeki GUI 'yi kullanarak bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-208">You can read more about using the .NET Portability Analyzer (API Port) using the GUI in the [Porting desktop apps to .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog post.</span></span> <span data-ttu-id="1999b-209">Komut satırını kullanmayı tercih ediyorsanız, gereken adımlar şunlardır:</span><span class="sxs-lookup"><span data-stu-id="1999b-209">If you prefer to use the command line, the necessary steps are:</span></span>

1. <span data-ttu-id="1999b-210">Henüz yoksa [.net taşınabilirlik Çözümleyicisi](https://github.com/Microsoft/dotnet-apiport/releases) 'ni indirin.</span><span class="sxs-lookup"><span data-stu-id="1999b-210">Download the [.NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) if you don't already have it.</span></span>
1. <span data-ttu-id="1999b-211">.NET Framework uygulamasının derlemeler için başarıyla bağlantı kurmak için olduğundan emin olun (Bu, geçişe bakılmaksızın iyi bir fikirdir).</span><span class="sxs-lookup"><span data-stu-id="1999b-211">Make sure the .NET Framework app to be ported builds successfully (this is a good idea prior to migration regardless).</span></span>
1. <span data-ttu-id="1999b-212">API bağlantı noktasını aşağıdaki gibi bir komut satırıyla çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="1999b-212">Run API Port with a command line like this.</span></span>

    ```console
    ApiPort.exe analyze -f <PathToBeanTraderBinaries> -r html -r excel -t ".NET Core"
    ```

    <span data-ttu-id="1999b-213">`-f` Bağımsız değişkeni, çözümlenecek ikilileri içeren yolu belirtir.</span><span class="sxs-lookup"><span data-stu-id="1999b-213">The `-f` argument specifies the path containing the binaries to analyze.</span></span> <span data-ttu-id="1999b-214">`-r` Bağımsız değişkeni istediğiniz çıktı dosyası biçimini belirtir.</span><span class="sxs-lookup"><span data-stu-id="1999b-214">The `-r` argument specifies which output file format you want.</span></span> <span data-ttu-id="1999b-215">Bağımsız `-t` DEĞIŞKENI, API kullanımının analiz edileceği .NET platformunu belirtir.</span><span class="sxs-lookup"><span data-stu-id="1999b-215">The `-t` argument specifies which .NET platform to analyze API usage against.</span></span> <span data-ttu-id="1999b-216">Bu durumda, .NET Core 'u istersiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-216">In this case, you want .NET Core.</span></span>

<span data-ttu-id="1999b-217">HTML raporu açtığınızda, ilk bölüm çözümlenmiş ikililerin tümünü ve kullandıkları .NET API 'lerinin yüzdesini listeler ve hedeflenen platformda bulunur.</span><span class="sxs-lookup"><span data-stu-id="1999b-217">When you open the HTML report, the first section will list all of the analyzed binaries and what percentage of the .NET APIs they use are available on the targeted platform.</span></span> <span data-ttu-id="1999b-218">Yüzde, kendi kendine anlamlı değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-218">The percentage is not meaningful by itself.</span></span> <span data-ttu-id="1999b-219">Daha fazla yararlı olan API 'Ler, eksik olan API 'Leri görşeydir.</span><span class="sxs-lookup"><span data-stu-id="1999b-219">What's more useful is to see the specific APIs that are missing.</span></span> <span data-ttu-id="1999b-220">Bunu yapmak için, bir derleme adı seçin ya da ayrı derlemeler için raporlara gidin.</span><span class="sxs-lookup"><span data-stu-id="1999b-220">To do that, either select an assembly name or scroll down to the reports for individual assemblies.</span></span>

<span data-ttu-id="1999b-221">Kaynak koduna sahip olduğunuz derlemelere odaklanın.</span><span class="sxs-lookup"><span data-stu-id="1999b-221">Focus on the assemblies that you own the source code for.</span></span> <span data-ttu-id="1999b-222">Çekirdeklere Trader ApiPort raporunda, örneğin, listelenen çok sayıda ikili dosya vardır, ancak çoğu NuGet paketlerine aittir.</span><span class="sxs-lookup"><span data-stu-id="1999b-222">In the Bean Trader ApiPort report, for example, there are many binaries listed, but most of them belong to NuGet packages.</span></span> <span data-ttu-id="1999b-223">`Castle.Windsor`.NET Core 'da eksik olan System. Web API 'Lerine bağlı olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-223">`Castle.Windsor` shows that it depends on some System.Web APIs that are missing in .NET Core.</span></span> <span data-ttu-id="1999b-224">Bu sorun, daha önce .NET Core ' u `Castle.Windsor` desteklediğini doğruladığınıza ilişkin bir sorun değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-224">This isn't a concern because you previously verified that `Castle.Windsor` supports .NET Core.</span></span> <span data-ttu-id="1999b-225">NuGet paketleri farklı .NET platformlarıyla kullanılmak üzere farklı ikililerin olması yaygındır. bu nedenle, ' nin `Castle.Windsor` .NET Framework sürümünün System. Web API 'lerini kullanması veya aynı zamanda paket .NET Standard veya .NET Core 'u (bunun yaptığı) hedeflediğinden, ilgisiz olup olmadığı.</span><span class="sxs-lookup"><span data-stu-id="1999b-225">It is common for NuGet packages to have different binaries for use with different .NET platforms, so whether the .NET Framework version of `Castle.Windsor` uses System.Web APIs or not is irrelevant as long as the package also targets .NET Standard or .NET Core (which it does).</span></span>

<span data-ttu-id="1999b-226">Çekirdeklere Trader örneğinde, dikkate almanız gereken tek ikili bir **Beantraderclient** örneğidir ve rapor yalnızca Iki .NET API 'nin eksik olduğunu gösterir: `System.ServiceModel.ClientBase<T>.Close` ve. `System.ServiceModel.ClientBase<T>.Open`</span><span class="sxs-lookup"><span data-stu-id="1999b-226">With the Bean Trader sample, the only binary that you need to consider is **BeanTraderClient** and the report shows that only two .NET APIs are missing: `System.ServiceModel.ClientBase<T>.Close` and `System.ServiceModel.ClientBase<T>.Open`.</span></span>

![BeanTraderClient taşınabilirlik raporu](./media/convert-project-from-net-framework/portability-report.png)

<span data-ttu-id="1999b-228">WCF Istemci API 'Leri (çoğunlukla) .NET Core üzerinde desteklendiğinden, bu merkezi API 'lerde kullanılabilecek alternatifler olması gerektiğinden bunlar sorunları engelliyor olabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-228">These are unlikely to be blocking issues because WCF Client APIs are (mostly) supported on .NET Core, so there must be alternatives available for these central APIs.</span></span> <span data-ttu-id="1999b-229">Aslında, .NET Core yüzey `System.ServiceModel`alanına (kullanarak <https://apisof.net>) bakarak bunun yerine .NET Core 'ta zaman uyumsuz alternatifler olduğunu görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="1999b-229">In fact, looking at `System.ServiceModel`'s .NET Core surface area (using <https://apisof.net>), you see that there are async alternatives in .NET Core instead.</span></span>

<span data-ttu-id="1999b-230">Bu rapor ve önceki NuGet bağımlılığı analizine dayalı olarak, çekirdeklere Trader örneğini .NET Core 'a geçirmede önemli bir sorun olmaması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-230">Based on this report and the previous NuGet dependency analysis, it looks like there should be no major issues migrating the Bean Trader sample to .NET Core.</span></span> <span data-ttu-id="1999b-231">Geçiş işlemine gerçekten başlayacaksınız bir sonraki adıma hazırsınız.</span><span class="sxs-lookup"><span data-stu-id="1999b-231">You're ready for the next step in which you'll actually start the migration.</span></span>

## <a name="migrating-the-project-file"></a><span data-ttu-id="1999b-232">Proje dosyası geçiriliyor</span><span class="sxs-lookup"><span data-stu-id="1999b-232">Migrating the project file</span></span>

<span data-ttu-id="1999b-233">Uygulamanız yeni [SDK stili proje dosyası biçimini](../../core/tools/csproj.md)kullanmıyor ise, .NET Core 'u hedeflemek için yeni bir proje dosyası gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-233">If your app isn't using the new [SDK-style project file format](../../core/tools/csproj.md), you'll need a new project file to target .NET Core.</span></span> <span data-ttu-id="1999b-234">Var olan csproj dosyasını değiştirebilir veya mevcut projenin geçerli durumunda kalmasını tercih ediyorsanız, .NET Core 'u hedefleyen yeni bir csproj dosyası ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-234">You can replace the existing csproj file or, if you prefer to keep the existing project untouched in its current state, you can add a new csproj file targeting .NET Core.</span></span> <span data-ttu-id="1999b-235">Çoklu [hedefleme](../../standard/library-guidance/cross-platform-targeting.md) (birden çok `<TargetFrameworks>` hedef belirterek) ile tek bir SDK stili proje dosyası Ile .NET Framework ve .NET Core için uygulamanın sürümlerini oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-235">You can build versions of the app for .NET Framework and .NET Core with a single SDK-style project file with [multi-targeting](../../standard/library-guidance/cross-platform-targeting.md) (specifying multiple `<TargetFrameworks>` targets).</span></span>

<span data-ttu-id="1999b-236">Yeni proje dosyasını oluşturmak için, Visual Studio 'da yeni bir WPF projesi oluşturabilir veya bir geçici dizinde `dotnet new wpf` komutunu kullanarak proje dosyasını oluşturabilir ve ardından doğru konuma kopyalayabilir/yeniden adlandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-236">To create the new project file, you can create a new WPF project in Visual Studio or use the `dotnet new wpf` command in a temporary directory to generate the project file and then copy/rename it to the correct location.</span></span> <span data-ttu-id="1999b-237">Ayrıca, bazı proje dosyası geçişini otomatikleştirebilen, topluluk tarafından oluşturulan bir araç olan [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017).</span><span class="sxs-lookup"><span data-stu-id="1999b-237">There is also a community-created tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), that can automate some of the project file migration.</span></span> <span data-ttu-id="1999b-238">Araç yararlı olmakla kalmaz, geçişin tüm ayrıntılarının doğru olduğundan emin olmak için sonuçları gözden geçirmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-238">The tool is helpful but still needs a human to review the results to make sure all the details of the migration are correct.</span></span> <span data-ttu-id="1999b-239">Aracın en iyi şekilde işlemediğini belirli bir alan, *Packages. config* dosyalarından NuGet paketlerini geçirmektedir.</span><span class="sxs-lookup"><span data-stu-id="1999b-239">One particular area that the tool doesn't handle optimally is migrating NuGet packages from *packages.config* files.</span></span> <span data-ttu-id="1999b-240">Araç, NuGet paketlerine başvurmak için bir *Packages. config* dosyası kullanan bir proje dosyasında çalışıyorsa, `<PackageReference>` öğeleri otomatik olarak öğelere geçirilir, ancak yalnızca en üst düzey olanlar yerine `<PackageReference>` *Tüm* paketlere yönelik öğeler ekler.</span><span class="sxs-lookup"><span data-stu-id="1999b-240">If the tool runs on a project file that still uses a *packages.config* file to reference NuGet packages, it will migrate to `<PackageReference>` elements automatically, but will add `<PackageReference>` elements for *all* of the packages instead of just top-level ones.</span></span> <span data-ttu-id="1999b-241">Visual Studio ile`<PackageReference>` öğelere zaten geçiş yaptıysanız (Bu örnekte yaptığınız gibi), araç geri kalanında dönüştürmeye yardımcı olabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-241">If you have already migrated to`<PackageReference>` elements with Visual Studio (as you've done in this sample), then the tool can help with the rest of the conversion.</span></span> <span data-ttu-id="1999b-242">Scott Hanselman, [csproj dosyalarını geçirirken blog gönderisine](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), el ile geçiş yaparak eğitim verme ve bağlantı noktası için yalnızca birkaç projeniz varsa daha iyi sonuçlar verecektir.</span><span class="sxs-lookup"><span data-stu-id="1999b-242">Like Scott Hanselman recommends in [his blog post on migrating csproj files](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), porting by hand is educational and will give better results if you only have a few projects to port.</span></span> <span data-ttu-id="1999b-243">Ancak onlarca veya yüzlerce proje dosyası aktarıyorsanız, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017) gibi bir araç bir yardım olabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-243">But if you're porting dozens or hundreds of project files, then a tool like [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017) can be a help.</span></span>

<span data-ttu-id="1999b-244">Çekirdeklere Trader örneği için yeni bir proje dosyası oluşturmak için geçici bir dizinde `dotnet new wpf` çalıştırın ve oluşturulan *. csproj* dosyasını *beantraderclient* klasörüne taşıyın ve bu dosyayı **beantraderclient. Core. csproj**olarak yeniden adlandırın.</span><span class="sxs-lookup"><span data-stu-id="1999b-244">To create a new project file for the Bean Trader sample, run `dotnet new wpf` in a temporary directory and move the generated *.csproj* file into the *BeanTraderClient* folder and rename it **BeanTraderClient.Core.csproj**.</span></span>

<span data-ttu-id="1999b-245">Yeni proje dosyası biçimi, içinde veya dizininde bulduğu C# dosyalarını, *resx* dosyalarını ve xaml dosyalarını otomatik olarak içerdiğinden, proje dosyası zaten neredeyse tamamlanmıştır!</span><span class="sxs-lookup"><span data-stu-id="1999b-245">Because the new project file format automatically includes C# files, *resx* files, and XAML files that it finds in or under its directory, the project file is already almost complete!</span></span> <span data-ttu-id="1999b-246">Geçişi sona erdirmede, eski ve yeni proje dosyalarını yan yana açın ve içerdiği bilgilerin geçirilmesi gerekip gerekmediğini görmek için eskisini gözden geçirin.</span><span class="sxs-lookup"><span data-stu-id="1999b-246">To finish the migration, open the old and new project files side-by-side and look through the old one to see if any information it contains needs to be migrated.</span></span> <span data-ttu-id="1999b-247">Çekirdeklere Trader örnek durumunda, aşağıdaki öğeler yeni projeye kopyalanmalıdır:</span><span class="sxs-lookup"><span data-stu-id="1999b-247">In the Bean Trader sample case, the following items should be copied to the new project:</span></span>

- <span data-ttu-id="1999b-248">`<RootNamespace>`, `<AssemblyName>`Ve `<ApplicationIcon>` özelliklerinin hepsi kopyalanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-248">The `<RootNamespace>`, `<AssemblyName>`, and `<ApplicationIcon>` properties should all be copied.</span></span>

- <span data-ttu-id="1999b-249">Ayrıca, çekirdeklere Trader örneği `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` bir AssemblyInfo.cs dosyasındaki derleme düzeyi öznitelikleri (gibi `[AssemblyTitle]`) içerdiğinden yeni proje dosyasına bir özellik eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-249">You also need to add a `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` property to the new project file since the Bean Trader sample includes assembly-level attributes (like `[AssemblyTitle]`) in an AssemblyInfo.cs file.</span></span> <span data-ttu-id="1999b-250">Varsayılan olarak, yeni SDK stili projeler bu öznitelikleri csproj dosyasındaki özelliklere göre otomatik olarak üretir.</span><span class="sxs-lookup"><span data-stu-id="1999b-250">By default, new SDK-style projects will autogenerate these attributes based on properties in the csproj file.</span></span> <span data-ttu-id="1999b-251">Bu durumda bunun gerçekleşmesini istemediğiniz için (otomatik olarak oluşturulan öznitelikler AssemblyInfo.cs ile çakışacak), ile `<GenerateAssemblyInfo>`otomatik olarak oluşturulan öznitelikleri devre dışı bırakabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-251">Because you don't want that to happen in this case (the autogenerated attributes would conflict with those from AssemblyInfo.cs), you disable the autogenerated attributes with `<GenerateAssemblyInfo>`.</span></span>

- <span data-ttu-id="1999b-252">*Resx* dosyaları gömülü kaynaklar olarak otomatik olarak dahil edilse de `<Resource>` , görüntüler gibi diğer öğeler değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-252">Although *resx* files are automatically included as embedded resources, other `<Resource>` items like images are not.</span></span> <span data-ttu-id="1999b-253">Bu nedenle, görüntü `<Resource>` ve simge dosyaları katıştırmak için öğeleri kopyalayın.</span><span class="sxs-lookup"><span data-stu-id="1999b-253">So, copy the `<Resource>` elements for embedding image and icon files.</span></span> <span data-ttu-id="1999b-254">Yeni proje dosyası biçiminin glob desenleri desteğini kullanarak tek bir satıra png başvurularını basitleştirebilirsiniz: `<Resource Include="**\*.png" />`.</span><span class="sxs-lookup"><span data-stu-id="1999b-254">You can simplify the png references to a single line by using the new project file format's support for globbing patterns: `<Resource Include="**\*.png" />`.</span></span>

- <span data-ttu-id="1999b-255">Benzer şekilde `<None>` , öğeler otomatik olarak eklenir, ancak varsayılan olarak çıkış dizinine kopyalanmaz.</span><span class="sxs-lookup"><span data-stu-id="1999b-255">Similarly, `<None>` items are included automatically, but they aren't copied to the output directory, by default.</span></span> <span data-ttu-id="1999b-256">Çekirdeklere Trader projesi çıkış dizinine (davranışlar `<None>` `PreserveNewest` kullanılarak) *kopyalanmış bir* öğe içerdiğinden, bu dosya için otomatik olarak doldurulmuş `<None>` öğeyi güncelleştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-256">Because the Bean Trader project includes a `<None>` item that *is* copied to the output directory (using `PreserveNewest` behaviors), you need to update the automatically populated `<None>` item for that file, like this.</span></span>

  ```xml
  <None Update="BeanTrader.pfx">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
  ```

- <span data-ttu-id="1999b-257">Çekirdeklere Trader örneği, bir XAML dosyası (varsayılan. vurgu. xaml) `Content` (as yerine `Page`) içerir, çünkü bu dosyada tanımlanan Temalar ve vurgular, uygulamanın kendine gömülmesini yerıne çalışma zamanında dosyanın xaml 'den yüklenmesidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-257">The Bean Trader sample includes a XAML file (Default.Accent.xaml) as `Content` (rather than as a `Page`) because themes and accents defined in this file are loaded from the file's XAML at runtime, rather than being embedded in the app itself.</span></span> <span data-ttu-id="1999b-258">Yeni proje sistemi, bir XAML dosyası olduğundan, bu `<Page>`dosyayı otomatik olarak bir olarak ekler.</span><span class="sxs-lookup"><span data-stu-id="1999b-258">The new project system automatically includes this file as a `<Page>`, however, since it's a XAML file.</span></span> <span data-ttu-id="1999b-259">Bu nedenle, XAML dosyasını bir sayfa (`<Page Remove="**\Default.Accent.xaml" />`) olarak kaldırmanız ve içerik olarak eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-259">So, you need to both remove the XAML file as a page (`<Page Remove="**\Default.Accent.xaml" />`) and add it as content.</span></span>

  ```xml
  <Content Include="Resources\Themes\Default.Accent.xaml">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
  ```

- <span data-ttu-id="1999b-260">Son olarak, tüm `<ItemGroup>` `<PackageReference>` öğeleri ile kopyalayarak NuGet başvuruları ekleyin.</span><span class="sxs-lookup"><span data-stu-id="1999b-260">Finally, add NuGet references by copying the `<ItemGroup>` with all the `<PackageReference>` elements.</span></span> <span data-ttu-id="1999b-261">NuGet paketlerini daha önce .NET Core ile uyumlu sürümlere yükseltmişseniz, artık paket başvurularının .NET Core 'a özgü bir projede olduğundan emin olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-261">If you hadn't previously upgraded the NuGet packages to .NET Core-compatible versions, you could do that now that the package references are in a .NET Core-specific project.</span></span>

<span data-ttu-id="1999b-262">Bu noktada, yeni projeyi BeanTrader çözümüne eklemek ve Visual Studio 'da açmak mümkün olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-262">At this point, it should be possible to add the new project to the BeanTrader solution and open it in Visual Studio.</span></span> <span data-ttu-id="1999b-263">Proje, **Çözüm Gezgini**doğru görünmelidir ve `dotnet restore BeanTraderClient.Core.csproj` paketleri başarıyla geri yüklemeniz gerekir (.NET Framework hedef olarak kullandığınız mahapps. Metro sürümü ile ilgili iki beklenen uyarıyla birlikte).</span><span class="sxs-lookup"><span data-stu-id="1999b-263">The project should look correct in **Solution Explorer**, and `dotnet restore BeanTraderClient.Core.csproj` should successfully restore packages (with two expected warnings related to the MahApps.Metro version you're using targeting .NET Framework).</span></span>

<span data-ttu-id="1999b-264">Her iki proje dosyasını yan yana tutmak mümkün olsa da (ve eski projeyi tamamen olduğu gibi oluşturmak istiyorsanız bile istenebilir), geçiş işlemini karmaşıklaştırır (iki proje aynı bin ve obj klasörlerini kullanmayı dener) ve genellikle gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-264">Although it's possible to keep both project files side-by-side (and may even be desirable if you want to keep building the old project exactly as it was), it complicates the migration process (the two projects will try to use the same bin and obj folders) and usually isn't necessary.</span></span> <span data-ttu-id="1999b-265">Hem .NET Core hem de .NET Framework hedefleri için derlemek isterseniz, yeni proje dosyasındaki `<TargetFramework>netcoreapp3.0</TargetFramework>` özelliğini bunun yerine ile `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-265">If you want to build for both .NET Core and .NET Framework targets, you can replace the `<TargetFramework>netcoreapp3.0</TargetFramework>` property in the new project file with `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` instead.</span></span> <span data-ttu-id="1999b-266">Çekirdeklere Trader örneği için artık gerekli olmadığından eski proje dosyasını (BeanTraderClient. csproj) silin.</span><span class="sxs-lookup"><span data-stu-id="1999b-266">For the Bean Trader sample, delete the old project file (BeanTraderClient.csproj) since it's no longer needed.</span></span> <span data-ttu-id="1999b-267">Her iki proje dosyasını da tutmak isterseniz, bunların farklı çıkış ve ara çıkış yolları için derlendiğinizden emin olun.</span><span class="sxs-lookup"><span data-stu-id="1999b-267">If you prefer to keep both project files, be sure to have them build to different output and intermediate output paths.</span></span>

## <a name="fix-build-issues"></a><span data-ttu-id="1999b-268">Derleme sorunlarını giderme</span><span class="sxs-lookup"><span data-stu-id="1999b-268">Fix build issues</span></span>

<span data-ttu-id="1999b-269">Taşıma işleminin üçüncü adımı, oluşturulacak projeyi alıyor.</span><span class="sxs-lookup"><span data-stu-id="1999b-269">The third step of the porting process is getting the project to build.</span></span> <span data-ttu-id="1999b-270">Proje dosyası bir SDK stili projeye dönüştürüldükten sonra bazı uygulamalar başarıyla derlencektir.</span><span class="sxs-lookup"><span data-stu-id="1999b-270">Some apps will already build successfully once the project file is converted to an SDK-style project.</span></span> <span data-ttu-id="1999b-271">Uygulamanız için bu durum, tebrikler!</span><span class="sxs-lookup"><span data-stu-id="1999b-271">If that's the case for your app, congratulations!</span></span> <span data-ttu-id="1999b-272">4. adım ' a gidebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-272">You can go on to Step 4.</span></span> <span data-ttu-id="1999b-273">Diğer uygulamalarda, .NET Core için derleme için bazı güncelleştirmeler gerekecektir.</span><span class="sxs-lookup"><span data-stu-id="1999b-273">Other apps will need some updates to get them building for .NET Core.</span></span> <span data-ttu-id="1999b-274">Çekirdeklere Trader örnek projesinde `dotnet build` , örneğin (veya Visual Studio 'da derleme) çalıştırmayı denerseniz, birçok hata olacaktır, ancak bunları hızlıca sabitleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-274">If you try to run `dotnet build` on the Bean Trader sample project now, for example, (or build it in Visual Studio), there will be many errors, but you'll get them fixed quickly.</span></span>

### <a name="systemservicemodel-references-and-microsoftwindowscompatibility"></a><span data-ttu-id="1999b-275">System. ServiceModel başvuruları ve Microsoft. Windows. Compatibility</span><span class="sxs-lookup"><span data-stu-id="1999b-275">System.ServiceModel references and Microsoft.Windows.Compatibility</span></span>

<span data-ttu-id="1999b-276">.NET Core için kullanılabilen ancak .NET Core App metapackage içinde otomatik olarak bulunmayan API 'Ler için ortak hata Kaynakları eksik.</span><span class="sxs-lookup"><span data-stu-id="1999b-276">A common source of errors is missing references for APIs that are available for .NET Core but not automatically included in the .NET Core app metapackage.</span></span> <span data-ttu-id="1999b-277">Bunu çözmek için `Microsoft.Windows.Compatibility` pakete başvurmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-277">To address this, you should reference the `Microsoft.Windows.Compatibility` package.</span></span> <span data-ttu-id="1999b-278">Uyumluluk Paketi, WCF istemcisi, Dizin Hizmetleri, kayıt defteri, yapılandırma, ACL 'Ler API 'Leri ve daha fazlası gibi Windows masaüstü uygulamalarında ortak olan geniş bir API kümesini içerir.</span><span class="sxs-lookup"><span data-stu-id="1999b-278">The compatibility package includes a broad set of APIs that are common in Windows desktop apps, such as WCF client, directory services, registry, configuration, ACLs APIs, and more.</span></span>

<span data-ttu-id="1999b-279">Çekirdeklere Trader örneğinde, derleme hatalarının çoğunluğu eksik <xref:System.ServiceModel> türler nedeniyle oluşur.</span><span class="sxs-lookup"><span data-stu-id="1999b-279">With the Bean Trader sample, the majority of the build errors are due to missing <xref:System.ServiceModel> types.</span></span> <span data-ttu-id="1999b-280">Bunlar, gerekli WCF NuGet paketlerine başvurarak çözülebilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-280">These could be addressed by referencing the necessary WCF NuGet packages.</span></span> <span data-ttu-id="1999b-281">WCF istemci API 'Leri, `Microsoft.Windows.Compatibility` pakette mevcut olanlar arasındadır, ancak bu nedenle uyumluluk paketine başvurmak daha iyi bir çözümdür (Ayrıca, API 'lerle ilgili sorunları ve ayrıca uyumluluk PAKETININ kullanılabildiği WCF sorunlarını çözümler) de ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="1999b-281">WCF client APIs are among those present in the `Microsoft.Windows.Compatibility` package, though, so referencing the compatibility package is an even better solution (since it also addresses any issues related to APIs as well as solutions to the WCF issues that the compatibility package makes available).</span></span> <span data-ttu-id="1999b-282">Bu `Microsoft.Windows.Compatibility` paket çoğu .net Core 3,0 WPF ve WinForms taşıma senaryolarında yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="1999b-282">The `Microsoft.Windows.Compatibility` package helps in most .NET Core 3.0 WPF and WinForms porting scenarios.</span></span> <span data-ttu-id="1999b-283">NuGet başvurusunu öğesine `Microsoft.Windows.Compatibility`ekledikten sonra yalnızca bir derleme hatası kalır!</span><span class="sxs-lookup"><span data-stu-id="1999b-283">After adding the NuGet reference to `Microsoft.Windows.Compatibility`, only one build error remains!</span></span>

### <a name="cleaning-up-unused-files"></a><span data-ttu-id="1999b-284">Kullanılmayan dosyaları temizleme</span><span class="sxs-lookup"><span data-stu-id="1999b-284">Cleaning up unused files</span></span>

<span data-ttu-id="1999b-285">Genellikle, *Tüm* kaynağı otomatik olarak IÇEREN yeni SDK stili projelere eklenen yapıya dahil olmayan C# ve xaml dosyalarıyla ilişkili bir tür geçiş sorunu vardır.</span><span class="sxs-lookup"><span data-stu-id="1999b-285">One type of migration issue that comes up often relates to C# and XAML files that weren't previously included in the build getting picked up by the new SDK-style projects that include *all* source automatically.</span></span>

<span data-ttu-id="1999b-286">Çekirdeklere Trader örneğinde gördüğünüz sonraki derleme hatası, *OldUnusedViewModel.cs*içindeki hatalı bir arabirim uygulamasına başvurur.</span><span class="sxs-lookup"><span data-stu-id="1999b-286">The next build error you see in the Bean Trader sample refers to a bad interface implementation in *OldUnusedViewModel.cs*.</span></span> <span data-ttu-id="1999b-287">Dosya adı bir ipucu, ancak Denetim sırasında bu kaynak dosyanın yanlış olduğunu fark edeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-287">The file name is a hint, but on inspection, you'll find that this source file is incorrect.</span></span> <span data-ttu-id="1999b-288">Bu, eski .NET Framework projesine dahil edilmediğinden, daha önce soruna neden olmadı.</span><span class="sxs-lookup"><span data-stu-id="1999b-288">It didn't cause issues previously because it wasn't included in the original .NET Framework project.</span></span> <span data-ttu-id="1999b-289">Diskte bulunan ancak eski *csproj* 'a dahil olmayan kaynak dosyaları şimdi otomatik olarak eklenir.</span><span class="sxs-lookup"><span data-stu-id="1999b-289">Source files that were present on disk but not included in the old *csproj* are included automatically now.</span></span>

<span data-ttu-id="1999b-290">Bu gibi tek bir sorun varsa, dosyanın gerekli olmadığını doğrulamak için önceki *csproj* ile karşılaştırmak kolaydır ve sonra ya da `<Compile Remove="" />` kaynak dosya bundan böyle herhangi bir yerde gerekmiyorsa, silin.</span><span class="sxs-lookup"><span data-stu-id="1999b-290">For one-off issues like this, it's easy to compare to the previous *csproj* to confirm that the file isn't needed, and then either `<Compile Remove="" />` it or, if the source file isn't needed anywhere anymore, delete it.</span></span> <span data-ttu-id="1999b-291">Bu durumda, yalnızca *OldUnusedViewModel.cs*silmek güvenlidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-291">In this case, it's safe to just delete *OldUnusedViewModel.cs*.</span></span>

<span data-ttu-id="1999b-292">Bu şekilde dışlanmanız gereken çok sayıda kaynak dosyanız varsa, proje dosyasında `<EnableDefaultCompileItems>` özelliği false olarak ayarlayarak C# dosyalarının otomatik olarak eklenmesini devre dışı bırakabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-292">If you have many source files that would need to be excluded this way, you can disable auto-inclusion of C# files by setting the `<EnableDefaultCompileItems>` property to false in the project file.</span></span> <span data-ttu-id="1999b-293">Daha sonra, yalnızca dahil `<Compile Include>` etmek istediğiniz kaynakları oluşturmak için eski proje dosyasındaki öğeleri yeni birine kopyalayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-293">Then, you can copy `<Compile Include>` items from the old project file to the new one in order to only build sources you intended to include.</span></span> <span data-ttu-id="1999b-294">Benzer şekilde `<EnableDefaultPageItems>` , XAML sayfalarının otomatik olarak eklenmesini devre dışı bırakmak ve `<EnableDefaultItems>` her ikisini de tek bir özellikle denetlemek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-294">Similarly, `<EnableDefaultPageItems>` can be used to turn off auto-inclusion of XAML pages and `<EnableDefaultItems>` can control both with a single property.</span></span>

### <a name="a-brief-aside-on-multi-pass-compilers"></a><span data-ttu-id="1999b-295">Çok geçişli derleyicilere kısa bir açıklama</span><span class="sxs-lookup"><span data-stu-id="1999b-295">A brief aside on multi-pass compilers</span></span>

<span data-ttu-id="1999b-296">Soruna neden olan dosyayı çekirdeklere Trader örneğinden kaldırdıktan sonra, yeniden derleyebilir ve dört hata alırsınız.</span><span class="sxs-lookup"><span data-stu-id="1999b-296">After removing the offending file from the Bean Trader sample, you can re-build and will get four errors.</span></span> <span data-ttu-id="1999b-297">Daha önce hiç kimse yok mu?</span><span class="sxs-lookup"><span data-stu-id="1999b-297">Didn't you have one before?</span></span> <span data-ttu-id="1999b-298">Neden hata sayısı?</span><span class="sxs-lookup"><span data-stu-id="1999b-298">Why did the number of errors go up?</span></span> <span data-ttu-id="1999b-299">C# derleyicisi, [çok taramalı bir derleyicidir](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span><span class="sxs-lookup"><span data-stu-id="1999b-299">The C# compiler is a [multi-pass compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span></span> <span data-ttu-id="1999b-300">Bu, her kaynak dosyanın iki kez gittiği anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="1999b-300">This means that it goes through each source file twice.</span></span> <span data-ttu-id="1999b-301">İlk olarak, derleyici her kaynak dosyasındaki meta verileri ve bildirimlere bakar ve bildirim düzeyindeki sorunları tanımlar.</span><span class="sxs-lookup"><span data-stu-id="1999b-301">First, the compiler just looks at metadata and declarations in each source file and identifies any declaration-level problems.</span></span> <span data-ttu-id="1999b-302">Bu hatalar düzeltildi.</span><span class="sxs-lookup"><span data-stu-id="1999b-302">Those are the errors you've fixed.</span></span> <span data-ttu-id="1999b-303">Ardından, C# kaynağını Il 'de oluşturmak için koddan yeniden gider; Bunlar, şu anda gördüğünüz ikinci hata kümesidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-303">Then it goes through the code again to build the C# source into IL; those are the second set of errors that you're seeing now.</span></span>

> [!NOTE]
> <span data-ttu-id="1999b-304">C# derleyicisi [yalnızca iki geçişte daha fazlasını](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes)yapar, ancak nihai sonuç, bu gibi büyük kod değişiklikleri için derleyici hatalarının iki dalgada elde edilmesine neden olur.</span><span class="sxs-lookup"><span data-stu-id="1999b-304">The C# compiler does [more than just two passes](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), but the end result is that compiler errors for large code changes like this tend to come in two waves.</span></span>

### <a name="third-party-dependency-fixes-castlewindsor"></a><span data-ttu-id="1999b-305">Üçüncü taraf bağımlılık düzeltmeleri (Role. Wınsma)</span><span class="sxs-lookup"><span data-stu-id="1999b-305">Third-party dependency fixes (Castle.Windsor)</span></span>

<span data-ttu-id="1999b-306">Bazı geçiş senaryolarında yer alan başka bir sorun sınıfı, bağımlılıkların .NET Framework ve .NET Core sürümleri arasındaki API farklarıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-306">Another class of issue that comes up in some migration scenarios is API differences between .NET Framework and .NET Core versions of dependencies.</span></span> <span data-ttu-id="1999b-307">Bir NuGet paketi hem .NET Framework hem de .NET Standard ya da .NET Core 'u hedefliyorsa, farklı .NET hedefleriyle kullanılmak üzere farklı kitaplıklar olabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-307">Even if a NuGet package targets both .NET Framework and .NET Standard or .NET Core, there may be different libraries for use with different .NET targets.</span></span> <span data-ttu-id="1999b-308">Bu, paketlerin farklı uygulamalar gerektirebilecek birçok farklı .NET platformunu desteklemesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="1999b-308">This allows the packages to support many different .NET platforms, which may require different implementations.</span></span> <span data-ttu-id="1999b-309">Aynı zamanda, farklı .NET platformlarını hedeflerken kitaplıklarda küçük API farklılıkları olabileceğini de gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-309">It also means that there may be small API differences in the libraries when targeting different .NET platforms.</span></span>

<span data-ttu-id="1999b-310">Çekirdeklere Trader örneğinde göreceğiniz sonraki hata kümesi, `Castle.Windsor` API 'lerle ilgilidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-310">The next set of errors you'll see in the Bean Trader sample are related to `Castle.Windsor` APIs.</span></span> <span data-ttu-id="1999b-311">.NET Core çekirdeklere Trader projesi, .NET Framework hedefli proje (4.1.1 `Castle.Windsor` ) ile aynı sürümünü kullanır, ancak bu iki platform için uygulamalar biraz farklıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-311">The .NET Core Bean Trader project uses the same version of `Castle.Windsor` as the .NET Framework-targeted project (4.1.1), but the implementations for those two platforms are slightly different.</span></span>

<span data-ttu-id="1999b-312">Bu durumda, düzeltilmesi gereken aşağıdaki sorunları görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="1999b-312">In this case, you see the following issues that need to be fixed:</span></span>

1. <span data-ttu-id="1999b-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly`.NET Core 'da kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1999b-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly` isn't available on .NET Core.</span></span> <span data-ttu-id="1999b-314">Bununla birlikte, benzer bir `Classes.FromAssemblyContaining` API mevcuttur. bu nedenle, çağrısı ile her iki kullanımını `Classes.FromThisAssembly()` `Classes.FromAssemblyContaining(t)`' a, ' `t` ın çağrısını yapan türdür.</span><span class="sxs-lookup"><span data-stu-id="1999b-314">There is, however, the similar API `Classes.FromAssemblyContaining` available, so we can replace both uses of `Classes.FromThisAssembly()` with calls to `Classes.FromAssemblyContaining(t)`, where `t` is the type making the call.</span></span>
1. <span data-ttu-id="1999b-315">Benzer şekilde, *Bootstrapper.cs*içinde `Castle.Windsor.Installer.FromAssembly`. Bu, .NET Core 'da kullanılamaz.</span><span class="sxs-lookup"><span data-stu-id="1999b-315">Similarly, in *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly`.This is unavailable on .NET Core.</span></span> <span data-ttu-id="1999b-316">Bunun yerine, bu çağrı ile `FromAssembly.Containing(typeof(Bootstrapper))`değiştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-316">Instead, that call can be replaced with `FromAssembly.Containing(typeof(Bootstrapper))`.</span></span>

### <a name="updating-wcf-client-usage"></a><span data-ttu-id="1999b-317">WCF istemci kullanımı güncelleştiriliyor</span><span class="sxs-lookup"><span data-stu-id="1999b-317">Updating WCF client usage</span></span>

<span data-ttu-id="1999b-318">`Castle.Windsor` Farklılıkları düzelttikten sonra, .NET Core çekirdeklere Trader projesinde kalan son derleme hatası, `BeanTraderServiceClient` (öğesinden `DuplexClientBase`türetilmeyen) bir `Open` yönteme sahip değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-318">Having fixed the `Castle.Windsor` differences, the last remaining build error in the .NET Core Bean Trader project is that `BeanTraderServiceClient` (which derives from `DuplexClientBase`) doesn't have an `Open` method.</span></span> <span data-ttu-id="1999b-319">Bu, bu geçiş sürecinin başlangıcında .NET taşınabilirlik Çözümleyicisi tarafından vurgulanan bir API olduğundan bu değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-319">This isn't surprising since this is an API that was highlighted by the .NET Portability Analyzer at the beginning of this migration process.</span></span> <span data-ttu-id="1999b-320">Daha büyük `BeanTraderServiceClient` bir sorunla ilgilenmenizi sağlar, ancak.</span><span class="sxs-lookup"><span data-stu-id="1999b-320">Looking at `BeanTraderServiceClient` draws our attention to a larger issue, though.</span></span> <span data-ttu-id="1999b-321">Bu WCF istemcisi [Svcutil. exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) aracı tarafından otomatik olarak oluşturuldu.</span><span class="sxs-lookup"><span data-stu-id="1999b-321">This WCF client was autogenerated by the [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) tool.</span></span>

<span data-ttu-id="1999b-322">**Svcutil tarafından oluşturulan WCF istemcileri .NET Framework kullanımı için tasarlanmıştır.**</span><span class="sxs-lookup"><span data-stu-id="1999b-322">**WCF clients generated by Svcutil are meant for use on .NET Framework.**</span></span>

<span data-ttu-id="1999b-323">Svcutil tarafından oluşturulan WCF istemcilerinin kullanıldığı çözümlerin, .NET Core ile kullanmak üzere .NET Standard uyumlu istemcileri yeniden oluşturması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-323">Solutions that use svcutil-generated WCF clients will need to regenerate .NET Standard-compatible clients for use with .NET Core.</span></span> <span data-ttu-id="1999b-324">Eski istemcilerin çalışmayabileceği başlıca nedenlerden biri, WCF bağlamaları ve uç noktaları tanımlamak için uygulama yapılandırmasına bağımlıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-324">One of the main reasons the old clients won't work is that they depend on app configuration for defining WCF bindings and endpoints.</span></span> <span data-ttu-id="1999b-325">.NET Standard WCF API 'Leri platformlar arası (System. Configuration API 'Lerinin kullanılamadığı) çalışabileceğinden, .NET Core ve .NET Standard senaryoları için WCF istemcileri, yapılandırma yerine program aracılığıyla bağlamaları ve uç noktaları tanımlamalıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-325">Because .NET Standard WCF APIs can work cross-platform (where System.Configuration APIs aren't available), WCF clients for .NET Core and .NET Standard scenarios must define bindings and endpoints programmatically instead of in configuration.</span></span>

<span data-ttu-id="1999b-326">Aslında, `<system.serviceModel>` App. config bölümüne (Svcutil veya el ile oluşturulan) bağlı olan tüm WCF istemci kullanımları .NET Core 'da çalışacak şekilde değiştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-326">In fact, any WCF client usage that depends on the `<system.serviceModel>` app.config section (whether created with Svcutil or manually) will need to be changed to work on .NET Core.</span></span>

<span data-ttu-id="1999b-327">.NET Standard uyumlu WCF istemcilerini otomatik olarak oluşturmak için iki yol vardır:</span><span class="sxs-lookup"><span data-stu-id="1999b-327">There are two ways to automatically generate .NET Standard-compatible WCF clients:</span></span>

- <span data-ttu-id="1999b-328">`dotnet-svcutil` Araç, aynı şekilde, Svcutil 'ın daha önce çalıştığı BIR şekilde WCF istemcileri üreten bir .net aracıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-328">The `dotnet-svcutil` tool is a .NET tool that generates WCF clients in a way that is similar to how Svcutil worked previously.</span></span>
- <span data-ttu-id="1999b-329">Visual Studio, bağlı hizmetler özelliğinin [WCF Web hizmeti başvurusu](../../core/additional-tools/wcf-web-service-reference-guide.md) SEÇENEĞINI kullanarak WCF istemcileri oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-329">Visual Studio can generate WCF clients using the [WCF Web Service Reference](../../core/additional-tools/wcf-web-service-reference-guide.md) option of its Connected Services feature.</span></span>

<span data-ttu-id="1999b-330">Her iki yaklaşım da geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-330">Either approach works well.</span></span> <span data-ttu-id="1999b-331">Alternatif olarak, WCF istemci kodunu kendiniz de yazabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-331">Alternatively, of course, you could write the WCF client code yourself.</span></span> <span data-ttu-id="1999b-332">Bu örnek için, Visual Studio bağlı hizmet özelliğini kullanmayı seçtim.</span><span class="sxs-lookup"><span data-stu-id="1999b-332">For this sample, I chose to use the Visual Studio Connected Service feature.</span></span> <span data-ttu-id="1999b-333">Bunu yapmak için, Visual Studio 'nun Çözüm Gezgini ' nde *beantraderclient. Core* projesine sağ tıklayın ve**bağlı hizmet** **Ekle** > ' yi seçin.</span><span class="sxs-lookup"><span data-stu-id="1999b-333">To do that, right-click on the *BeanTraderClient.Core* project in Visual Studio's solution explorer and select **Add** > **Connected Service**.</span></span> <span data-ttu-id="1999b-334">Ardından, WCF Web hizmeti başvuru sağlayıcısını seçin.</span><span class="sxs-lookup"><span data-stu-id="1999b-334">Next, choose the WCF Web Service Reference Provider.</span></span> <span data-ttu-id="1999b-335">Bu, arka uç çekirdeklere Trader Web hizmetinin adresini (`localhost:8080` sunucuyu yerel olarak çalıştırıyorsanız) ve üretilen türlerin kullanması gereken ad alanını (örneğin,**Beantrader. Service**) belirtebileceğiniz bir iletişim kutusu getirir.</span><span class="sxs-lookup"><span data-stu-id="1999b-335">This will bring up a dialog where you can specify the address of the backend Bean Trader web service (`localhost:8080` if you are running the server locally) and the namespace that generated types should use (**BeanTrader.Service**, for example).</span></span>

![WCF Web hizmeti başvurusu bağlı hizmeti Iletişim kutusu](./media/convert-project-from-net-framework/connected-service-dialog.png)

<span data-ttu-id="1999b-337">**Son** düğmesini seçtikten sonra, projeye yeni bir bağlı hizmetler düğümü eklenir ve çekirdeklere Trader hizmetine erişmek için yenı .NET Standard WCF istemcisini içeren düğüm altına bir Reference.cs dosyası eklenir.</span><span class="sxs-lookup"><span data-stu-id="1999b-337">After you select the **Finish** button, a new Connected Services node is added to the project and a Reference.cs file is added under that node containing the new .NET Standard WCF client for accessing the Bean Trader service.</span></span> <span data-ttu-id="1999b-338">Bu dosyadaki `GetEndpointAddress` veya `GetBindingForEndpoint` metotlara baktığınızda, bağlamaların ve uç noktaların artık programlı olarak oluşturulduğunu görürsünüz (uygulama yapılandırması aracılığıyla değil).</span><span class="sxs-lookup"><span data-stu-id="1999b-338">If you look at the `GetEndpointAddress` or `GetBindingForEndpoint` methods in that file, you'll see that bindings and endpoints are now generated programmatically (instead of via app config).</span></span> <span data-ttu-id="1999b-339">' Bağlı hizmetler Ekle ' özelliği aynı zamanda proje dosyasındaki bazı System. ServiceModel paketlerine başvurular ekleyebilir, bu da gerekli tüm WCF paketleri Microsoft. Windows. Compatibility aracılığıyla dahil edilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-339">The 'Add Connected Services' feature may also add references to some System.ServiceModel packages in the project file, which aren't needed since all necessary WCF packages are included via Microsoft.Windows.Compatibility.</span></span> <span data-ttu-id="1999b-340">Ek System. ServiceModel `<PackageReference>` öğelerinin eklenmiş olup olmadığını görmek için csproj 'ı denetleyin ve varsa bunları kaldırın.</span><span class="sxs-lookup"><span data-stu-id="1999b-340">Check the csproj to see if any extra System.ServiceModel `<PackageReference>` items have been added, and if so, remove them.</span></span>

<span data-ttu-id="1999b-341">Projemizin şimdi yeni WCF istemci sınıflarına sahiptir ( *Reference.cs*içinde), ancak yine de eskileri de vardır (BeanTrader.cs içinde).</span><span class="sxs-lookup"><span data-stu-id="1999b-341">Our project has new WCF client classes now (in *Reference.cs*), but it also still has the old ones (in BeanTrader.cs).</span></span> <span data-ttu-id="1999b-342">Bu noktada iki seçenek vardır:</span><span class="sxs-lookup"><span data-stu-id="1999b-342">There are two options at this point:</span></span>

- <span data-ttu-id="1999b-343">Özgün .NET Framework projesini (yeni .NET Core 'a hedeflenmiş bir şekilde) oluşturabilmek istiyorsanız, uygulamanın .NET Framework ve .NET Core sürümlerinin farklı WCF istemcileri kullanması için .NET `<Compile Remove="BeanTrader.cs" />` Core projesinin csproj dosyasındaki bir öğeyi kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-343">If you want to be able to build the original .NET Framework project (alongside the new .NET Core-targeted one), you can use a `<Compile Remove="BeanTrader.cs" />` item in the .NET Core project's csproj file so that the .NET Framework and .NET Core versions of the app use different WCF clients.</span></span> <span data-ttu-id="1999b-344">Bu, var olan .NET Framework projesini değiştirmeden bırakma avantajına sahiptir, ancak, oluşturulan WCF istemcilerinin kullanıldığı kodun, .NET Core 'da .NET Framework projesi dışında bir şekilde farklı olması gerekebilir. bu nedenle, bazı WCF istemci kullanımını koşullu olarak derlemek için (örneğin, istemci oluşturma) `#if` , .NET Core için inşa edildiğinde ve .NET Framework için inşa edildiğinde başka bir yolla çalışacak şekilde yönergeler kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-344">This has the advantage of leaving the existing .NET Framework project unchanged, but has the disadvantage that code using the generated WCF clients may need to be slightly different in the .NET Core case than it was in the .NET Framework project, so you'll likely need to use `#if` directives to conditionally compile some WCF client usage (creating clients, for example) to work one way when built for .NET Core and another way when built for .NET Framework.</span></span>

- <span data-ttu-id="1999b-345">Diğer yandan, var olan .NET Framework projesindeki bazı kod karmaşıklığı kabul edilebilir ise, *BeanTrader.cs* tümünü de kaldırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-345">If, on the other hand, some code churn in the existing .NET Framework project is acceptable, you can remove *BeanTrader.cs* all together.</span></span> <span data-ttu-id="1999b-346">Yeni WCF istemcisi .NET Standard için oluşturulduğundan, hem .NET Core hem de .NET Framework senaryolarında çalışacaktır.</span><span class="sxs-lookup"><span data-stu-id="1999b-346">Because the new WCF client is built for .NET Standard, it will work in both .NET Core and .NET Framework scenarios.</span></span> <span data-ttu-id="1999b-347">.NET Core 'a ek olarak .NET Framework oluşturuyorsanız (çoklu hedefleyerek veya iki csproj dosyası içeriyorsa), bu yeni *Reference.cs* dosyasını her iki hedef için de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-347">If you are building for .NET Framework in addition to .NET Core (either by multi-targeting or by having two csproj files), you can use this new *Reference.cs* file for both targets.</span></span> <span data-ttu-id="1999b-348">Bu yaklaşım, kodun iki farklı WCF istemcisini desteklemesi için bifurde gerek duymayabileceğinden faydalanır. her yerde aynı kod kullanılacaktır.</span><span class="sxs-lookup"><span data-stu-id="1999b-348">This approach has the advantage that the code won't need to bifurcate to support two different WCF clients; the same code will be used everywhere.</span></span> <span data-ttu-id="1999b-349">Dezavantajı, (kabul edilebilir) .NET Framework projenin değiştirilmesini içerir.</span><span class="sxs-lookup"><span data-stu-id="1999b-349">The drawback is that it involves changing the (presumably stable) .NET Framework project.</span></span>

<span data-ttu-id="1999b-350">Çekirdeklere Trader örneği söz konusu olduğunda, geçişi kolaylaştırmak için özgün projede küçük değişiklikler yapabilirsiniz, bu nedenle WCF istemci kullanımını mutabık kılmak için aşağıdaki adımları izleyin:</span><span class="sxs-lookup"><span data-stu-id="1999b-350">In the case of the Bean Trader sample, you can make small changes to the original project if it makes migration easier, so follow these steps to reconcile WCF client usage:</span></span>

01. <span data-ttu-id="1999b-351">Çözüm Gezgini 'nden ' Varolan öğe Ekle ' bağlam menüsünü kullanarak yeni Reference.cs dosyasını .NET Framework *Beantraderclient. csproj* projesine ekleyin.</span><span class="sxs-lookup"><span data-stu-id="1999b-351">Add the new Reference.cs file to the .NET Framework *BeanTraderClient.csproj* project using the 'Add existing item' context menu from the solution explorer.</span></span> <span data-ttu-id="1999b-352">Aynı dosyanın her iki proje tarafından da (C# dosyasını kopyalamaya karşı) kullanılması için ' as link ' eklediğinizden emin olun.</span><span class="sxs-lookup"><span data-stu-id="1999b-352">Be sure to add 'as link' so that the same file is used by both projects (as opposed to copying the C# file).</span></span> <span data-ttu-id="1999b-353">Hem .NET Core için oluşturuyorsanız hem de tek bir csproj ile .NET Framework (Çoklu hedefleme kullanarak), bu adım gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-353">If you are building for both .NET Core and .NET Framework with a single csproj (using multi-targeting) then this step isn't necessary.</span></span>

01. <span data-ttu-id="1999b-354">*BeanTrader.cs*silin.</span><span class="sxs-lookup"><span data-stu-id="1999b-354">Delete *BeanTrader.cs*.</span></span>

01. <span data-ttu-id="1999b-355">Yeni WCF istemcisi eskisiyle benzerdir, ancak oluşturulan koddaki birçok ad alanı farklıdır.</span><span class="sxs-lookup"><span data-stu-id="1999b-355">The new WCF client is similar to the old one, but a number of namespaces in the generated code are different.</span></span> <span data-ttu-id="1999b-356">Bu nedenle, WCF istemci türlerinin BeanTrader. model yerine (veya seçtiğiniz herhangi bir ad alanı adı) bir ad alanı olmadan kullanılması için projeyi güncelleştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-356">Because of this, it is necessary to update the project so that WCF client types are used from BeanTrader.Service (or whatever namespace name you chose) instead of BeanTrader.Model or without a namespace.</span></span> <span data-ttu-id="1999b-357">*Beantraderclient. Core. csproj* oluşturma, bu değişikliklerin nerede yapılması gerektiğini belirlemesine yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="1999b-357">Building *BeanTraderClient.Core.csproj* will help to identify where these changes need to be made.</span></span> <span data-ttu-id="1999b-358">Düzeltmelerin her ikisi de C# ve XAML kaynak dosyalarında gerekecektir.</span><span class="sxs-lookup"><span data-stu-id="1999b-358">Fixes will be needed both in C# and in XAML source files.</span></span>

01. <span data-ttu-id="1999b-359">Son olarak, *BeanTraderServiceClientFactory.cs* içinde bir hata olduğunu öğrenirsiniz çünkü bu `BeanTraderServiceClient` tür için kullanılabilir oluşturucular değişmiştir.</span><span class="sxs-lookup"><span data-stu-id="1999b-359">Finally, you'll discover that there is an error in *BeanTraderServiceClientFactory.cs* because the available constructors for the `BeanTraderServiceClient` type have changed.</span></span> <span data-ttu-id="1999b-360">Bir `InstanceContext` bağımsız değişken ( `CallbackHandler` `Castle.Windsor` ioc kapsayıcısından kullanılarak oluşturulmuş) sağlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="1999b-360">It used to be possible to supply an `InstanceContext` argument (which was created using a `CallbackHandler` from the `Castle.Windsor` IoC container).</span></span> <span data-ttu-id="1999b-361">Yeni oluşturucular yeni `CallbackHandler`s oluştur.</span><span class="sxs-lookup"><span data-stu-id="1999b-361">The new constructors create new `CallbackHandler`s.</span></span> <span data-ttu-id="1999b-362">Bununla birlikte, temel türünde, istediğiniz `BeanTraderServiceClient`şekilde eşleşen oluşturucular vardır.</span><span class="sxs-lookup"><span data-stu-id="1999b-362">There are, however, constructors in `BeanTraderServiceClient`'s base type that match what you want.</span></span> <span data-ttu-id="1999b-363">Otomatik olarak oluşturulan WCF istemci kodu kısmi sınıflarda bulunduğundan, kolayca genişletebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-363">Since the autogenerated WCF client code all exists in partial classes, you can easily extend it.</span></span> <span data-ttu-id="1999b-364">Bunu yapmak için, *BeanTraderServiceClient.cs* adlı yeni bir dosya oluşturun ve ardından aynı ada sahip kısmi bir sınıf oluşturun (BeanTrader. Service ad alanını kullanarak).</span><span class="sxs-lookup"><span data-stu-id="1999b-364">To do this, create a new file called *BeanTraderServiceClient.cs* and then create a partial class with that same name (using the BeanTrader.Service namespace).</span></span> <span data-ttu-id="1999b-365">Daha sonra, burada gösterildiği gibi kısmi türe bir Oluşturucu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="1999b-365">Then, add one constructor to the partial type as shown here.</span></span>

    ```csharp
    public BeanTraderServiceClient(System.ServiceModel.InstanceContext callbackInstance) :
        base(callbackInstance, EndpointConfiguration.NetTcpBinding_BeanTraderService)
            { }
    ```

<span data-ttu-id="1999b-366">Bu değişiklikler yapıldıktan sonra, çekirdeklere Trader örneği artık yeni .NET Standard uyumlu bir WCF istemcisi kullanacaktır ve `Open` `await OpenAsync` bunun yerine *TradingService.cs* içindeki çağrıyı değiştirmenin son düzeltmesini yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-366">With those changes made, the Bean Trader sample will now be using a new .NET Standard-compatible WCF client and you can make the final fix of changing the `Open` call in *TradingService.cs* to use `await OpenAsync` instead.</span></span>

<span data-ttu-id="1999b-367">WCF sorunları ele ılarak, çekirdeklere Trader örneğinin .NET Core sürümü artık düzgün şekilde oluşturulur!</span><span class="sxs-lookup"><span data-stu-id="1999b-367">With the WCF issues addressed, the .NET Core version of the Bean Trader sample now builds cleanly!</span></span>

## <a name="runtime-testing"></a><span data-ttu-id="1999b-368">Çalışma zamanı testi</span><span class="sxs-lookup"><span data-stu-id="1999b-368">Runtime testing</span></span>

<span data-ttu-id="1999b-369">Proje .NET Core 'a karşı düzgün bir şekilde oluşturulsa geçiş işinin yapılmadığında emin olmak kolaydır.</span><span class="sxs-lookup"><span data-stu-id="1999b-369">It's easy to forget that migration work isn't done as soon as the project builds cleanly against .NET Core.</span></span> <span data-ttu-id="1999b-370">Ayrıca, bağlantı verilen uygulamayı test etmek için zaman bırakmanız önemlidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-370">It's important to leave time for testing the ported app, too.</span></span> <span data-ttu-id="1999b-371">İşlemler başarıyla kurulduktan sonra, özellikle de .NET Framework hedefleyen paketler kullanıyorsanız, uygulamanın çalıştığından ve beklendiği gibi çalıştığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="1999b-371">Once things build successfully, make sure the app runs and works as expected, especially if you are using any packages targeting .NET Framework.</span></span>

<span data-ttu-id="1999b-372">Ayrıca, bağlantı noktası çekirdeklere Trader uygulamasını başlatmayı deneyelim ve neler olduğunu görelim.</span><span class="sxs-lookup"><span data-stu-id="1999b-372">Let's try launching the ported Bean Trader app and see what happens.</span></span> <span data-ttu-id="1999b-373">Uygulama aşağıdaki özel durumla başarısız olmadan önce değil.</span><span class="sxs-lookup"><span data-stu-id="1999b-373">The app doesn't get far before failing with the following exception.</span></span>

```output
System.Configuration.ConfigurationErrorsException: 'Configuration system failed to initialize'

Inner Exception
ConfigurationErrorsException: Unrecognized configuration section system.serviceModel.
```

<span data-ttu-id="1999b-374">Bu, kuşkusuz bir fikir sunar.</span><span class="sxs-lookup"><span data-stu-id="1999b-374">This makes sense, of course.</span></span> <span data-ttu-id="1999b-375">WCF 'nin artık uygulama yapılandırması kullanmadığını unutmayın. bu nedenle, App. config dosyasının eski System. serviceModel bölümünün kaldırılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="1999b-375">Remember that WCF no longer uses app configuration, so the old system.serviceModel section of the app.config file needs to be removed.</span></span> <span data-ttu-id="1999b-376">Güncelleştirilmiş WCF istemcisi, kodunda aynı bilgilerin tümünü içerir, bu nedenle yapılandırma bölümü artık gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="1999b-376">The updated WCF client includes all of the same information in its code, so the config section isn't needed anymore.</span></span> <span data-ttu-id="1999b-377">WCF uç noktasının App. config içinde yapılandırılabilmesine isterseniz, bunu bir uygulama ayarı olarak ekleyebilir ve WCF hizmet uç noktasını yapılandırmadan almak için WCF istemci kodunu güncelleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1999b-377">If you wanted the WCF endpoint to be configurable in app.config, you could add it as an app setting and update the WCF client code to retrieve the WCF service endpoint from configuration.</span></span>

<span data-ttu-id="1999b-378">*App. config*dosyasının System. ServiceModel bölümünü kaldırdıktan sonra, uygulama başlatılır ancak kullanıcı oturum açtığında başka bir özel durumla başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="1999b-378">After removing the system.serviceModel section of *app.config*, the app launches but fails with another exception when a user signs in.</span></span>

```output
System.PlatformNotSupportedException: 'Operation is not supported on this platform.'
```

<span data-ttu-id="1999b-379">Desteklenmeyen API `Func<T>.BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="1999b-379">The unsupported API is `Func<T>.BeginInvoke`.</span></span> <span data-ttu-id="1999b-380">[DotNet/corefx # 5940](https://github.com/dotnet/corefx/issues/5940)' de açıklandığı gibi, .NET Core, `BeginInvoke` temel uzaktan `EndInvoke` iletişim bağımlılıkları nedeniyle temsilci türlerindeki ve yöntemlerini desteklemez.</span><span class="sxs-lookup"><span data-stu-id="1999b-380">As explained in [dotnet/corefx#5940](https://github.com/dotnet/corefx/issues/5940), .NET Core doesn't support the `BeginInvoke` and `EndInvoke` methods on delegate types due to underlying remoting dependencies.</span></span> <span data-ttu-id="1999b-381">Bu sorun ve bu sorunun düzeltilmesi, .NET Core blog gönderisine [Ilişkin geçiş temsilcisi. BeginInvoke çağrılarında](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) daha ayrıntılı bir şekilde açıklanmıştır, ancak bu `BeginInvoke` , `EndInvoke` çağrılarınız (veya mümkünse zaman uyumsuz alternatifler `Task.Run` ) ile değiştirilmelidir.</span><span class="sxs-lookup"><span data-stu-id="1999b-381">This issue and its fix are explained in more detail in the [Migrating Delegate.BeginInvoke Calls for .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) blog post, but the gist is that `BeginInvoke` and `EndInvoke` calls should be replaced with `Task.Run` (or async alternatives, if possible).</span></span> <span data-ttu-id="1999b-382">Genel çözümü buraya uygulayarak, `BeginInvoke` çağrı tarafından `Invoke` `Task.Run`başlatılan bir çağrı ile değiştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="1999b-382">Applying the general solution here, the `BeginInvoke` call can be replaced with an `Invoke` call launched by `Task.Run`.</span></span>

```csharp
Task.Run(() =>
{
    return userInfoRetriever.Invoke();
}).ContinueWith(result =>
{
    // BeginInvoke's callback is replaced with ContinueWith
    var task = result.ConfigureAwait(false);
    CurrentTrader = task.GetAwaiter().GetResult();
}, TaskScheduler.Default);
```

<span data-ttu-id="1999b-383">`BeginInvoke` Kullanım kaldırıldıktan sonra, çekirdeklere Trader uygulaması .NET Core 'da başarıyla çalışır!</span><span class="sxs-lookup"><span data-stu-id="1999b-383">After removing the `BeginInvoke` usage, the Bean Trader app runs successfully on .NET Core!</span></span>

![.NET Core üzerinde çalışan çekirdeklere Trader](./media/convert-project-from-net-framework/running-on-core.png)

<span data-ttu-id="1999b-385">Tüm uygulamalar farklıdır, bu nedenle kendi uygulamalarınızı .NET Core 'a geçirmek için gereken belirli adımlar farklılık gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-385">All apps are different, so the specific steps needed to migrate your own apps to .NET Core will vary.</span></span> <span data-ttu-id="1999b-386">Ancak çekirdeklere Trader örneği, genel iş akışını ve beklenilen sorun türlerini gösterir.</span><span class="sxs-lookup"><span data-stu-id="1999b-386">But hopefully the Bean Trader sample demonstrates the general workflow and the types of issues that can be expected.</span></span> <span data-ttu-id="1999b-387">Bu makalenin uzunluğuna karşın, .NET Core üzerinde çalışmasını sağlamak için çekirdeklere Trader örneğinde gerekli olan gerçek değişiklikler oldukça sınırlı.</span><span class="sxs-lookup"><span data-stu-id="1999b-387">And, despite this article's length, the actual changes needed in the Bean Trader sample to make it work on .NET Core were fairly limited.</span></span> <span data-ttu-id="1999b-388">Birçok uygulama aynı şekilde .NET Core 'a geçirilir; sınırlı ve hatta kod değişikliğine gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="1999b-388">Many apps migrate to .NET Core in this same way; with limited or even no code changes needed.</span></span>
