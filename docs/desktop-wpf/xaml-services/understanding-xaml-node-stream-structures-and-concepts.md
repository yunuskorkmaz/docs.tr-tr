---
title: XAML Düğüm Akış Yapılarını ve Kavramlarını Anlama
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: b3de3dca029c5e676fc7cdebc7735cfdade0228a
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/27/2020
ms.locfileid: "82071621"
---
# <a name="xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="1e3bb-102">XAML düğüm akışı yapıları ve kavramları</span><span class="sxs-lookup"><span data-stu-id="1e3bb-102">XAML node stream structures and concepts</span></span>

<span data-ttu-id="1e3bb-103">.NET XAML Services'da uygulanan XAML okuyucuları ve XAML yazarları, XAML düğüm akışının tasarım konseptine dayanır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-103">XAML readers and XAML writers as implemented in .NET XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="1e3bb-104">XAML düğümü akışı, XAML düğüm kümesinin kavramsallaştırılmasıdır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="1e3bb-105">Bu kavramsallaştırmada, bir XAML işlemci sis ilişkileri yapısı nda teker teker yürür.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="1e3bb-106">Herhangi bir zamanda, açık bir XAML düğüm akışında yalnızca bir geçerli kayıt veya geçerli konum bulunur ve API'nin birçok yönü yalnızca bu konumdan kullanılabilen bilgileri raporlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="1e3bb-107">XAML düğümü akışındaki geçerli düğüm bir nesne, üye veya değer olarak tanımlanabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="1e3bb-108">XAML'yi XAML düğümü akışı olarak ele alarak, XAML okuyucuları XAML yazarlarıyla iletişim kurabilir ve bir programın bir yükleme yolu veya XAML içeren bir kaydet yolu işlemi sırasında XAML düğüm akışının içeriğini görüntülemesini, etkileşimde bulunmasını veya değiştirmesini sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="1e3bb-109">XAML okuyucu ve yazar API tasarımı ve XAML düğüm akışı kavramı önceki ilgili okuyucu ve yazar tasarımları ve kavramları, <xref:System.Xml.XmlReader> <xref:System.Xml.XmlWriter> XML Belge Nesne Modeli (DOM) ve ve sınıflar gibi benzer.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the XML Document Object Model (DOM) and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="1e3bb-110">Bu konu XAML düğüm akışı kavramlarını açıklar ve XAML düğüm düzeyinde XAML gösterimleri ile etkileşim yordamları nasıl yazabileceğinizi açıklar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="1e3bb-111">XAML Okuyucuya XAML Yükleme</span><span class="sxs-lookup"><span data-stu-id="1e3bb-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="1e3bb-112">Taban <xref:System.Xaml.XamlReader> sınıf, ilk XAML'yi XAML okuyucuya yüklemek için belirli bir teknik bildirmez.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="1e3bb-113">Bunun yerine, türemiş bir sınıf XAML için giriş kaynağının genel özellikleri ve kısıtlamaları da dahil olmak üzere yükleme tekniğini bildirir ve uygular.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="1e3bb-114">Örneğin, bir <xref:System.Xaml.XamlObjectReader> nesne grafiği, kök veya tabanı temsil eden tek bir nesnenin giriş kaynağından başlayarak okur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="1e3bb-115">Daha <xref:System.Xaml.XamlObjectReader> sonra nesne grafiğinden bir XAML düğüm akışı üretir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="1e3bb-116">En belirgin .NET XAML <xref:System.Xaml.XamlReader> Hizmetleri tanımlı alt sınıfıdır. <xref:System.Xaml.XamlXmlReader></span><span class="sxs-lookup"><span data-stu-id="1e3bb-116">The most prominent .NET XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="1e3bb-117"><xref:System.Xaml.XamlXmlReader>bir metin dosyasını doğrudan bir akış veya dosya yolu üzerinden yükleyerek veya dolaylı olarak <xref:System.IO.TextReader>ilgili bir okuyucu sınıfı aracılığıyla ilk XAML yükler.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="1e3bb-118">XAML <xref:System.Xaml.XamlReader> giriş kaynağının tamamını içeren olarak düşünülebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="1e3bb-119"><xref:System.Xaml.XamlReader> Ancak, temel API okuyucu XAML tek bir düğüm ile etkileşim olacak şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="1e3bb-120">İlk yüklendiğinizde, karşılaştığınız ilk tek düğüm XAML'nin kökü ve başlangıç nesnesidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="1e3bb-121">XAML Düğüm Akışı Kavramı</span><span class="sxs-lookup"><span data-stu-id="1e3bb-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="1e3bb-122">Bir DOM, ağaç metaforu veya XML tabanlı teknolojilere erişime yönelik sorgu tabanlı bir yaklaşıma daha aşinaysanız, XAML düğüm akışını kavramsallaştırmanın yararlı bir yolu aşağıdaki gibidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-122">If you are more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="1e3bb-123">Yüklü XAML'nin bir DOM veya olası düğümün tamamen genişletildiği ve doğrusal olarak sunulduğu bir ağaç olduğunu düşünün.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="1e3bb-124">Düğümler arasında ilerledikçe, bir DOM ile ilgili olacak düzeylerin "içinde" veya "dışında" geçiş yapıyor olabilirsiniz, ancak xaml düğüm akışı açıkça izlemiyor, çünkü bu düzey kavramları bir düğüm akışıyla ilgili değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="1e3bb-125">Düğüm akışı bir "geçerli" konuma sahiptir, ancak akışın diğer bölümlerini başvuru olarak kendiniz saklamadığınız sürece, düğüm akışının geçerli düğüm konumu dışındaki her yönü görüntülenmez.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="1e3bb-126">XAML düğüm akışı kavramı, tüm düğüm akışı üzerinden giderseniz, tüm XAML gösterimi işlenmiş emin olduğunu önemli bir avantaja sahiptir; bir sorgu, bir DOM işlemi veya bilgileri işlemeye yönelik diğer doğrusal olmayan bir yaklaşımın xaml gösteriminin bir bölümünü kaçırdığından endişelenmenize gerek yoktur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="1e3bb-127">Bu nedenle, XAML düğüm akışı gösterimi hem XAML okuyucuları ve XAML yazarlarını bağlamak hem de bir XAML işleme işleminin okuma ve yazma aşamaları arasında hareket eden kendi işleminizi ekebileceğiniz bir sistem sağlamak için idealdir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="1e3bb-128">Çoğu durumda, XAML düğüm akışındaki düğümlerin sıralanması, xaml okuyucuları tarafından, siparişin kaynak metin, ikili veya nesne grafiğinde nasıl görünebileceğine göre kasıtlı olarak optimize edilir veya yeniden sıralanır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="1e3bb-129">Bu davranış, XAML yazarlarıdüğüm akışı "geri" gitmek zorunda bir konumda asla bir XAML işleme mimarisi zorlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="1e3bb-130">İdeal olarak, tüm XAML yazma işlemleri şema bağlamına ve düğüm akışının geçerli konumuna göre hareket edebilmelidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="1e3bb-131">Temel Okuma Düğümü Döngüsü</span><span class="sxs-lookup"><span data-stu-id="1e3bb-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="1e3bb-132">XAML düğüm akışını incelemek için temel bir okuma düğümü döngüsü aşağıdaki kavramlardan oluşur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="1e3bb-133">Düğüm döngüleri amacıyla bu konuda tartışılan, bir metin tabanlı, insan tarafından okunabilir XAML dosyasını <xref:System.Xaml.XamlXmlReader>kullanarak okuyorsanız varsayalım.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="1e3bb-134">Bu bölümdeki bağlantılar, '. tarafından uygulanan belirli XAML <xref:System.Xaml.XamlXmlReader>düğüm döngüsü API'sine bakın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="1e3bb-135">XAML düğüm akışının sonunda olmadığınızı unutmayın <xref:System.Xaml.XamlXmlReader.IsEof%2A> <xref:System.Xaml.XamlXmlReader.Read%2A> (iade değerini denetleyin veya kullanın).</span><span class="sxs-lookup"><span data-stu-id="1e3bb-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="1e3bb-136">Akışın sonundaysanız, akım düğümü yoktur ve çıkmalısınız.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="1e3bb-137">XAML düğümü akışının şu anda ne tür <xref:System.Xaml.XamlXmlReader.NodeType%2A>bir düğüm ortaya çıkardığını kontrol edin.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="1e3bb-138">Doğrudan bağlı ilişkili bir XAML nesne yazar varsa, <xref:System.Xaml.XamlWriter.WriteNode%2A> genellikle bu noktada arama.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="1e3bb-139"><xref:System.Xaml.XamlNodeType> Geçerli düğüm veya geçerli kayıt olarak bildirilen bağlı olarak, düğüm içeriği hakkında bilgi edinmek için aşağıdakilerden birini arayın:</span><span class="sxs-lookup"><span data-stu-id="1e3bb-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

  - <span data-ttu-id="1e3bb-140"><xref:System.Xaml.XamlXmlReader.NodeType%2A> Bir <xref:System.Xaml.XamlNodeType.StartMember> <xref:System.Xaml.XamlNodeType.EndMember>veya , <xref:System.Xaml.XamlXmlReader.Member%2A> bir <xref:System.Xaml.XamlMember> üye hakkında bilgi almak için arayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="1e3bb-141">Üye bir <xref:System.Xaml.XamlDirective>, ve bu nedenle mutlaka önceki nesnenin geleneksel bir tür tanımlı üyesi olmayabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-141">The member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="1e3bb-142">`x:Name` Örneğin, bir nesneye uygulanan bir XAML <xref:System.Xaml.XamlMember.IsDirective%2A> üyesi olarak <xref:System.Xaml.XamlMember.Name%2A> görünür nerede `Name`doğrudur ve üyenin , bu yönerge XAML dili XAML ad alanı altında olduğunu gösteren diğer özellikleri ile.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

  - <span data-ttu-id="1e3bb-143"><xref:System.Xaml.XamlXmlReader.NodeType%2A> Bir <xref:System.Xaml.XamlNodeType.StartObject> <xref:System.Xaml.XamlNodeType.EndObject>veya , <xref:System.Xaml.XamlXmlReader.Type%2A> bir <xref:System.Xaml.XamlType> nesne hakkında bilgi almak için arayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

  - <span data-ttu-id="1e3bb-144">Bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>için <xref:System.Xaml.XamlXmlReader.Value%2A>, çağrı .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="1e3bb-145">Düğüm, yalnızca bir üye için bir değerin en basit ifadesi veya bir nesnenin başlatma metniyse bir değerdir (ancak, bu konunun gelecek bölümünde belgelenen tür dönüştürme davranışının farkında olmalısınız).</span><span class="sxs-lookup"><span data-stu-id="1e3bb-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

  - <span data-ttu-id="1e3bb-146">Bir <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, ad <xref:System.Xaml.XamlXmlReader.Namespace%2A> alanı düğümü için ad alanı bilgilerini almak için arayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="1e3bb-147">XAML okuyucuyu XAML düğüm akışındaki bir sonraki düğüme ilerletmek için arayın <xref:System.Xaml.XamlXmlReader.Read%2A> ve adımları tekrarlayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="1e3bb-148">.NET XAML Services XAML okuyucuları tarafından sağlanan XAML düğüm akışı her zaman tüm olası düğümlerin tam ve derin bir geçişsağlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-148">The XAML node stream provided by .NET XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="1e3bb-149">XAML düğüm döngüsü için tipik akış kontrol teknikleri içinde `while (reader.Read())`bir gövde <xref:System.Xaml.XamlXmlReader.NodeType%2A> tanımlama ve düğüm döngüsündeki her düğüm noktasında açma içerir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="1e3bb-150">Düğüm akışı dosyanın sonundaysa, geçerli düğüm null'dur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="1e3bb-151">Bir okuyucu ve yazar kullanan en basit döngü aşağıdaki örneğe benzer.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="1e3bb-152">Bir yük yolu XAML düğüm döngü bu temel örnek şeffaf XAML okuyucu ve XAML yazar <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>bağlanır, eğer kullanmış olsaydı farklı bir şey yapıyor.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="1e3bb-153">Ancak bu temel yapı daha sonra okuma veya yazma senaryonuza uygulanacak şekilde genişletilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="1e3bb-154">Bazı olası senaryolar aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="1e3bb-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="1e3bb-155">Aç. <xref:System.Xaml.XamlXmlReader.NodeType%2A></span><span class="sxs-lookup"><span data-stu-id="1e3bb-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="1e3bb-156">Hangi düğüm türüokunduğuna bağlı olarak farklı eylemler gerçekleştirin.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="1e3bb-157">Her durumda <xref:System.Xaml.XamlWriter.WriteNode%2A> aramayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="1e3bb-158">Sadece <xref:System.Xaml.XamlWriter.WriteNode%2A> bazı <xref:System.Xaml.XamlXmlReader.NodeType%2A> durumlarda ararsın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="1e3bb-159">Belirli bir düğüm türü için mantık içinde, bu düğümün özelliklerini analiz ve onlara hareket.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="1e3bb-160">Örneğin, yalnızca belirli bir XAML ad alanından gelen nesneleri yazabilir ve ardından xaml ad alanından olmayan nesneleri bırakabilir veya erteleyebilirdiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-160">For example, you could only write objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="1e3bb-161">Veya XAML sisteminizin üye işlemenizin bir parçası olarak desteklemediği XAML yönergelerini bırakabilir veya başka bir şekilde yeniden işleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="1e3bb-162">XAML <xref:System.Xaml.XamlObjectWriter> şema `Write*` bağlamı atlar tür eşleme gerçekleştirerek, yöntemleri geçersiz kılan bir özel tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="1e3bb-163">Varsayılan <xref:System.Xaml.XamlXmlReader> olmayan bir XAML şeması bağlamı kullanmak için, Böylece XAML davranışında özelleştirilmiş farklılıklar hem okuyucu hem de yazar tarafından kullanılır oluştur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="1e3bb-164">Düğüm Döngüsü Kavramının Ötesinde XAML'ye Erişim</span><span class="sxs-lookup"><span data-stu-id="1e3bb-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="1e3bb-165">XAML düğüm döngüsü dışında bir XAML gösterimi ile çalışmak için potansiyel olarak başka yollar vardır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="1e3bb-166">Örneğin, dizine eklenmiş bir düğümü okuyabilen veya özellikle düğümlere doğrudan `x:Name`, tarafından `x:Uid`veya diğer tanımlayıcılar aracılığıyla erişen bir XAML okuyucusu olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="1e3bb-167">.NET XAML Hizmetleri tam bir uygulama sağlamaz, ancak hizmetler ve destek türleri aracılığıyla önerilen bir desen sağlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-167">.NET XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="1e3bb-168">Daha fazla bilgi için <xref:System.Xaml.IXamlIndexingReader> ve <xref:System.Xaml.XamlNodeList> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

## <a name="working-with-the-current-node"></a><span data-ttu-id="1e3bb-169">Geçerli Düğümle Çalışma</span><span class="sxs-lookup"><span data-stu-id="1e3bb-169">Working with the Current Node</span></span>

<span data-ttu-id="1e3bb-170">XAML düğüm döngüsü kullanan çoğu senaryo yalnızca düğümleri okumaz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-170">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="1e3bb-171">Çoğu senaryo geçerli düğümleri işler ve her düğümü bir er <xref:System.Xaml.XamlWriter>teker bir uygulamaya geçirir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-171">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="1e3bb-172">Tipik yük yolu senaryosunda, bir <xref:System.Xaml.XamlXmlReader> XAML düğüm akışı üretir; XAML düğümleri mantığınıza ve XAML şema bağlamınıza göre işlenir; ve düğümler bir <xref:System.Xaml.XamlObjectWriter>.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-172">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="1e3bb-173">Daha sonra ortaya çıkan nesne grafiğini uygulamanıza veya çerçevenize tümleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-173">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="1e3bb-174">Tipik bir kaydet yolu <xref:System.Xaml.XamlObjectReader> senaryosunda, nesne grafiği ni okur, tek tek <xref:System.Xaml.XamlXmlWriter> XAML düğümleri işlenir ve XAML metin dosyası olarak serileştirilmiş sonucu çıktıları.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-174">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="1e3bb-175">Anahtar, hem yolların hem de senaryoların aynı anda tam olarak bir XAML düğümüyle çalışmayı içermesi ve XAML düğümlerinin XAML tipi sistemi ve the.NET XAML Hizmetleri API'leri tarafından tanımlanan standart bir şekilde tedavi edilebilmektedir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-175">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="1e3bb-176">Çerçeveler ve Kapsam</span><span class="sxs-lookup"><span data-stu-id="1e3bb-176">Frames and Scope</span></span>

<span data-ttu-id="1e3bb-177">XAML düğüm döngüsü, Bir XAML düğüm akışında doğrusal bir şekilde yürür.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-177">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="1e3bb-178">Düğüm akışı nesnelere, diğer nesneleri içeren üyelere ve benzeri geçişler.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-178">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="1e3bb-179">Genellikle bir çerçeve ve yığın kavramı uygulayarak XAML düğüm akışı içinde kapsamı izlemek için yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-179">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="1e3bb-180">Bu, özellikle düğüm akışını içindeyken etkin olarak ayarlıyorsanız geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-180">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="1e3bb-181">Düğüm döngü mantığınız bir parçası olarak uyguladığınız çerçeve `StartObject` ve `GetObject`yığın `EndObject` desteği, yapı bir DOM perspektifinden düşünülürse XAML düğüm yapısına inerken sayılabilir (veya) ve kapsamlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-181">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="1e3bb-182">Nesne Düğümlerini Geçiş ve Girme</span><span class="sxs-lookup"><span data-stu-id="1e3bb-182">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="1e3bb-183">XAML okuyucusu tarafından açıldığında düğüm akışındaki ilk düğüm, kök nesnenin başlangıç nesnesi düğümüdür.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-183">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="1e3bb-184">Tanım olarak, bu nesne her zaman tek bir nesne düğümüdür ve eşleri yoktur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-184">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="1e3bb-185">Herhangi bir gerçek dünya XAML örneğinde, kök nesne daha fazla nesne tutan bir veya daha fazla özellise sahip olarak tanımlanır ve bu özelliklerin üye düğümleri vardır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-185">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="1e3bb-186">Üye düğümleri sonra bir veya daha fazla nesne düğümleri var, ya da yerine bir değer düğümü sona erdirebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-186">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="1e3bb-187">Kök nesne genellikle XAML metin biçimlendirmesinde öznitelik olarak atanan ancak XAML düğüm akışı gösterimindeki bir `Namescope` düğüm türüyle eşleyen XAML adskoplarını tanımlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-187">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="1e3bb-188">Aşağıdaki XAML örneğini göz önünde bulundurun (bu rasgele XAML'dir, .NET'teki varolan türler tarafından desteklenmez).</span><span class="sxs-lookup"><span data-stu-id="1e3bb-188">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in .NET).</span></span> <span data-ttu-id="1e3bb-189">Bu nesne modelinde, `FavorCollection` `List<T>` wpf bilinen renk adları olarak `Balloon.Color` renkleri tanımlar ve `Color` atnitelik sözdizimi için bir tür dönüştürücü `Color` destekler benzer bir nesne tarafından desteklenen, `Favor`ve `Balloon` `NoiseMaker` atayabiliyor, ve atayabiliyor `Favor`varsayalım.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-189">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="1e3bb-190">XAML biçimlendirmesi</span><span class="sxs-lookup"><span data-stu-id="1e3bb-190">XAML markup</span></span>|<span data-ttu-id="1e3bb-191">Ortaya çıkan XAML düğüm akışı</span><span class="sxs-lookup"><span data-stu-id="1e3bb-191">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="1e3bb-192">`Namespace`için düğüm`Party`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-192">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="1e3bb-193">`StartObject`için düğüm`Party`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-193">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="1e3bb-194">`StartMember`için düğüm`Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-194">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="1e3bb-195">`StartObject`örtük düğüm`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-195">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="1e3bb-196">`StartMember`örtük `FavorCollection` öğeler özelliği için düğüm.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-196">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="1e3bb-197">`StartObject`için düğüm`Balloon`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-197">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="1e3bb-198">`StartMember`için düğüm`Color`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-198">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="1e3bb-199">`Value`öznitelik değer dizesi için düğüm`"Red"`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-199">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="1e3bb-200">`EndMember`Için`Color`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-200">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="1e3bb-201">`StartMember`için düğüm`HasHelium`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-201">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="1e3bb-202">`Value`öznitelik değer dizesi için düğüm`"True"`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-202">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="1e3bb-203">`EndMember`Için`HasHelium`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-203">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="1e3bb-204">`EndObject`Için`Balloon`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-204">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="1e3bb-205">`StartObject`için düğüm`NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-205">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="1e3bb-206">`StartMember`için düğüm`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-206">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="1e3bb-207">`Value`başlatma değeri dizesi için düğüm`"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-207">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="1e3bb-208">`EndMember`için düğüm`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-208">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="1e3bb-209">`EndObject`Için`NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-209">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="1e3bb-210">`EndMember`örtük `FavorCollection` öğeler özelliği için düğüm.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-210">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="1e3bb-211">`EndObject`örtük düğüm`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-211">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="1e3bb-212">`EndMember`Için`Favors`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-212">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="1e3bb-213">`EndObject`Için`Party`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-213">`EndObject` for `Party`</span></span>|

<span data-ttu-id="1e3bb-214">XAML düğüm akışında, aşağıdaki davranışa güvenebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="1e3bb-214">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="1e3bb-215">Bir `Namespace` düğüm varsa, xaml ad alanını `StartObject` ' ile bildirenden hemen `xmlns`önce akışa eklenir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-215">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="1e3bb-216">XAML ve örnek düğüm akışı ile önceki tabloya tekrar bakın.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-216">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="1e3bb-217">`StartObject` Düğümlerin `Namespace` metin biçimlendirmedeki bildirim konumlarına göre nasıl aktarılana dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-217">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="1e3bb-218">Bu, ad alanı düğümlerinin her zaman düğüm akışında uyguladıkları düğümden önce göründüğü davranışı temsil eder.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-218">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="1e3bb-219">Bu tasarımın amacı, ad alanı bilgilerinin nesne yazarları için hayati önem taşıdığını ve nesne yazarının tür eşleme gerçekleştirmeye veya nesneyi işlemeye çalışmadan önce bilinmesi gerektiğidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-219">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="1e3bb-220">XAML ad alanı bilgilerini akışta uygulama kapsamının önüne yerleştirmek, düğüm akışını her zaman sunulan sırada işlemeyi kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-220">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="1e3bb-221">Yukarıdaki dikkate nedeniyle, en gerçek `Namespace` dünya biçimlendirme durumlarda ilk okuduğunuz bir veya daha fazla düğümleri başından itibaren `StartObject` düğümleri değil, kök geçiş.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-221">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="1e3bb-222">Bir `StartObject` düğüm tarafından `StartMember`takip `Value`edilebilir , `EndObject`, veya hemen .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-222">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="1e3bb-223">Hemen başka bir `StartObject`takip asla .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-223">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="1e3bb-224">A, `StartMember` bir `StartObject`, `Value`veya hemen `EndMember`bir tarafından takip edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-224">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="1e3bb-225">Bu, değerin `GetObject`yeni bir değeri anında anlayacak bir `StartObject` değer yerine ana nesnenin varolan değerinden gelmesi gereken üyeler tarafından izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-225">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="1e3bb-226">Ayrıca yaklaşan `StartObject`bir düğüm `Namespace` için geçerlidir bir düğüm tarafından takip edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-226">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="1e3bb-227">Hemen başka bir `StartMember`takip asla .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-227">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="1e3bb-228">Düğüm `Value` değerin kendisini temsil eder; "EndValue" diye bir şey yoktur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-228">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="1e3bb-229">Bu sadece bir `EndMember`tarafından takip edilebilir .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-229">It can be followed only by an `EndMember`.</span></span>

  - <span data-ttu-id="1e3bb-230">Yapı tarafından kullanılabilecek nesnenin XAML başlatma metni nesne-değer yapısıyla sonuçlanmaz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-230">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="1e3bb-231">Bunun yerine, adlı `_Initialization` bir üye için özel bir üye düğüm oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-231">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="1e3bb-232">ve bu üye düğüm başlatma değeri dizesini içerir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-232">and that member node contains the initialization value string.</span></span> <span data-ttu-id="1e3bb-233">Varsa, `_Initialization` her zaman ilk `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-233">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="1e3bb-234">`_Initialization`bazı XAML hizmetleri gösterimlerinde XAML dili XAML adskopu `_Initialization` ile nitelikli olabilir, bu destek türleri tanımlanmış bir özellik olmadığını açıklamak için.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-234">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

  - <span data-ttu-id="1e3bb-235">Üye-Değer birleşimi, değerin öznitelik ayarını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-235">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="1e3bb-236">Sonunda bu değeri işleme dahil bir değer dönüştürücü olabilir ve değer düz bir dize.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-236">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="1e3bb-237">Ancak, bir XAML nesne yazar bu düğüm akışı işleyene kadar değerlendirilmemiştir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-237">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="1e3bb-238">XAML nesne yazarı gerekli XAML şema bağlamına, tür sistem eşleneme ve değer dönüşümleri için gerekli diğer desteğe sahip.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-238">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="1e3bb-239">Bir `EndMember` düğüm sonraki bir `StartMember` üye için bir düğüm veya `EndObject` üye sahibi için bir düğüm tarafından izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-239">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="1e3bb-240">Bir `EndObject` düğüm bir `EndMember` düğüm tarafından takip edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-240">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="1e3bb-241">Ayrıca, bir koleksiyonun `StartObject` öğelerinde nesnelerin eş olduğu durumlar için bir düğüm tarafından da izlenebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-241">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="1e3bb-242">Ya da yaklaşan `Namespace` `StartObject`bir düğüm için geçerlidir bir düğüm tarafından takip edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-242">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

  - <span data-ttu-id="1e3bb-243">Tüm düğüm akışının kapatılması benzersiz durumda `EndObject` için, kök bir şey tarafından takip edilmez; okuyucu artık dosya sonu ve <xref:System.Xaml.XamlReader.Read%2A> döndürür. `false`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-243">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="1e3bb-244">Değer Dönüştürücüler ve XAML Düğüm Akışı</span><span class="sxs-lookup"><span data-stu-id="1e3bb-244">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="1e3bb-245">Değer dönüştürücü, biçimlendirme uzantısı, tür dönüştürücü (değer serileştiricileri dahil) veya XAML türü sistemi aracılığıyla değer dönüştürücüolarak bildirilen başka bir özel sınıf için genel bir terimdir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-245">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="1e3bb-246">XAML düğüm akışında, tür dönüştürücü kullanımı ve biçimlendirme uzantısı kullanımı çok farklı gösterimlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-246">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="1e3bb-247">XAML Düğüm Akışında Tür Dönüştürücüler</span><span class="sxs-lookup"><span data-stu-id="1e3bb-247">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="1e3bb-248">Sonunda bir tür dönüştürücü kullanımı yla sonuçlanan bir öznitelik kümesi, XAML düğümü akışında bir üyenin değeri olarak bildirilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-248">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="1e3bb-249">XAML düğüm akışı bir tür dönüştürücü örnek nesnesi üretmek ve değeri ona geçmek için denemez.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-249">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="1e3bb-250">Bir tür dönüştürücünün dönüştürme uygulamasını kullanmak, XAML şema bağlamını çağırmak ve yazı eşleme için kullanmak gerektirir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-250">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="1e3bb-251">Değeri işlemek için hangi tür dönüştürücü sınıfının kullanılması gerektiğini belirlemek bile XAML şema bağlamını dolaylı olarak gerektirir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-251">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="1e3bb-252">Varsayılan XAML şema bağlamını kullandığınızda, bu bilgiler XAML türü sisteminden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-252">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="1e3bb-253">Bir XAML yazıcısına bağlanmadan önce XAML düğüm akışı düzeyindetür dönüştürücü sınıf bilgilerine <xref:System.Xaml.XamlMember> ihtiyacınız varsa, bunu ayarlanan üyenin bilgisinden elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-253">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="1e3bb-254">Ancak aksi takdirde, tür eşleç girişi, örneğin xaml nesne yazarı tarafından nesne oluşturma işlemi gerektiren işlemlerin geri kalanı gerçekleştirile kadar düz bir değer olarak XAML düğüm akışında korunmalıdır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-254">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type-mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="1e3bb-255">Örneğin, bunun için aşağıdaki sınıf tanımı anahat ve XAML kullanımı düşünün:</span><span class="sxs-lookup"><span data-stu-id="1e3bb-255">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="1e3bb-256">Bu kullanım için XAML düğüm akışının metin gösterimi aşağıdaki gibi ifade edilebilir:</span><span class="sxs-lookup"><span data-stu-id="1e3bb-256">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="1e3bb-257">`StartObject`temsil <xref:System.Xaml.XamlType> ile`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-257">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="1e3bb-258">`StartMember`temsil <xref:System.Xaml.XamlMember> ile`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-258">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="1e3bb-259">`Value`düğüm, metin dizeli " "`8x8`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-259">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="1e3bb-260">`EndMember`Eşleşen`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-260">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="1e3bb-261">`EndObject`Eşleşen`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="1e3bb-261">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="1e3bb-262">Bu düğüm akışında tür dönüştürücü örneği olmadığına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-262">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="1e3bb-263">Ama <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> <xref:System.Xaml.XamlMember> for'u `BoardSize`arayarak tür dönüştürücü bilgilerini alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-263">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="1e3bb-264">Geçerli bir XAML şema bağlamınız varsa, 'den <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>bir örnek alarak dönüştürücü yöntemlerini de çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-264">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="1e3bb-265">XAML Düğümü Akışında Biçimlendirme Uzantıları</span><span class="sxs-lookup"><span data-stu-id="1e3bb-265">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="1e3bb-266">Biçimlendirme uzantısı kullanımı XAML düğümü akışında, nesnenin biçimlendirme uzantısı örneğini temsil ettiği bir üye içindeki nesne düğümü olarak bildirilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-266">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="1e3bb-267">Böylece bir biçimlendirme uzantısı kullanımı, düğüm akışı gösteriminde bir tür dönüştürücü kullanımından daha açık bir şekilde sunulur ve daha fazla bilgi taşır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-267">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="1e3bb-268"><xref:System.Xaml.XamlMember>bilgiler biçimlendirme uzantısı hakkında size bir şey söylemiş olabilirsiniz, çünkü kullanım durumsaldır ve olası her biçimlendirme durumunda değişir; tür dönüştürücüler ile olduğu gibi türü veya üyesi başına adanmış ve örtülü değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-268"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="1e3bb-269">Biçimlendirme uzantılarının nesne düğümleri olarak düğüm akışı gösterimi, biçimlendirme uzantısı kullanımı XAML metin biçimlendirmesinde öznitelik biçiminde yapılmış olsa bile (genellikle böyledir) durumdur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-269">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="1e3bb-270">Açık nesne öğesi formu kullanılan Biçimlendirme uzantısı kullanımları aynı şekilde işlenir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-270">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="1e3bb-271">Biçimlendirme uzantısı nesne düğümü içinde, bu biçimlendirme uzantısı üyeleri olabilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-271">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="1e3bb-272">XAML düğüm akışı gösterimi, ister konumsal parametre kullanımı ister açık adlandırılmış parametrelere sahip bir kullanım olsun, bu biçimlendirme uzantısının kullanımını korur.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-272">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="1e3bb-273">Konumsal parametre kullanımı için XAML düğümü akışı, kullanımı kaydeden XAML dili tanımlı bir özellik `_PositionalParameters` içerir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-273">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="1e3bb-274">Bu özellik, <xref:System.Collections.Generic.List%601> kısıtlaması olan <xref:System.Object> genel bir özelliktir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-274">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="1e3bb-275">Kısıtlama, bir konumsal parametre kullanımı iç içe geçmiş biçimlendirme uzantısı kullanımları içerebilir, çünkü kısıtlama ve dize değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-275">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="1e3bb-276">Kullanımdan konumparametrelerine erişmek için, liste boyunca yineleyebilir ve tek tek liste değerleri için dizinleyicileri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-276">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="1e3bb-277">Adlandırılmış parametre kullanımı için, adlandırılmış her parametre düğüm akışında bu adın bir üye düğümü olarak temsil edilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-277">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="1e3bb-278">İç içe biçimlendirilmiş biçimlendirme uzantısı kullanımı olabileceğinden, üye değerler mutlaka dizeleri değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-278">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="1e3bb-279">`ProvideValue`biçimlendirme uzantısından henüz çağrılmadı.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-279">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="1e3bb-280">Ancak, düğüm akışında incelediğinizde biçimlendirme uzantısı düğümünde çağrılan `WriteEndObject` bir XAML okuyucusu ve XAML yazarı bağlanırsa çağrılır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-280">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="1e3bb-281">Bu nedenle, genellikle yük yolunda nesne grafiği oluşturmak için kullanılacak aynı XAML şema bağlamı gerekir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-281">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="1e3bb-282">Aksi `ProvideValue` takdirde, herhangi bir biçimlendirme uzantısı burada özel durumlar atabilir, çünkü beklenen hizmetler kullanılabilir değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-282">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="1e3bb-283">XAML Düğümü Akışında XAML ve XML Dil Tanımlı Üyeler</span><span class="sxs-lookup"><span data-stu-id="1e3bb-283">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="1e3bb-284">Bazı üyeler, açık <xref:System.Xaml.XamlMember> bir arama veya inşaat yerine, bir XAML okuyucunun yorumları ve kuralları nedeniyle XAML düğüm akışına tanıtılır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-284">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="1e3bb-285">Genellikle, bu üyeler XAML yönergeleridir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-285">Often, these members are XAML directives.</span></span> <span data-ttu-id="1e3bb-286">Bazı durumlarda, XAML düğüm akışına yönergeyi tanıtan XAML okuma eylemidir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-286">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="1e3bb-287">Başka bir deyişle, özgün giriş XAML metni üye yönergesini açıkça belirtmedi, ancak XAML okuyucusu, yapısal bir XAML kuralını karşılamak ve bu bilgiler kaybolmadan önce XAML düğüm akışındaki bilgileri bildirmek için yönergeyi ekler.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-287">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="1e3bb-288">Aşağıdaki liste, XAML okuyucusunun bir yönerge XAML üye düğümü getirmesinin beklendiği tüm durumları ve üye düğümün .NET XAML Hizmetleri uygulamalarında nasıl tanımlandığını not eder.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-288">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in .NET XAML Services implementations.</span></span>

- <span data-ttu-id="1e3bb-289">**Nesne düğümü için başlatma metni:** Bu üye düğümün `_Initialization`adı, bir XAML yönergesini temsil eder ve XAML dilinde xaml ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-289">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="1e3bb-290">Bunun için statik bir varlık <xref:System.Xaml.XamlLanguage.Initialization%2A>alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-290">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="1e3bb-291">**Biçimlendirme uzantısı için konumsal parametreler:** Bu üye düğümün adı `_PositionalParameters`, ve XAML dil XAML ad alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-291">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="1e3bb-292">Her zaman, xaml girişinde verilen `,` delimiter karakterine bölünerek önceden ayrılmış konumsal bir parametre olan genel bir nesne listesi içerir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-292">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="1e3bb-293">Konumsal parametreler yönergesi için statik <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>bir varlık elde edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-293">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="1e3bb-294">**Bilinmeyen içerik:** Bu üye düğümün `_UnknownContent`adı.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-294">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="1e3bb-295">Açıkçası, bir <xref:System.Xaml.XamlDirective>, ve XAML dil XAML isim alanında tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-295">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="1e3bb-296">Bu yönerge, xaml nesne öğesinin kaynak XAML'de içerik içerdiği, ancak şu anda kullanılabilir XAML şeması bağlamında hiçbir içerik özelliğinin belirlenemeden bulunduğu durumlar için sentinel olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-296">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="1e3bb-297">Bu servis talebinin bir XAML düğüm akışında, `_UnknownContent`adı geçen üyeleri denetleyerek algılayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-297">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="1e3bb-298">Bir yük yolu XAML düğüm akışında başka bir <xref:System.Xaml.XamlObjectWriter> eylem yapılmazsa, varsayılan `WriteEndObject` `_UnknownContent` değer, herhangi bir nesnede üyeyle karşılaştığında denenmeye başlar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-298">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="1e3bb-299">Varsayılan <xref:System.Xaml.XamlXmlWriter> atmaz ve üyeyi örtülü olarak ele alır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-299">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="1e3bb-300">Statik bir varlık `_UnknownContent` için <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-300">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="1e3bb-301">**Koleksiyonun toplama özelliği:** XAML için kullanılan bir koleksiyon sınıfının destek CLR türü genellikle toplama öğelerini tutan özel bir adlandırılmış özelliğe sahip olsa da, bu özellik tür çözümlemesi desteklemeden önce bir XAML türü sistemi tarafından bilinmemektedir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-301">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="1e3bb-302">Bunun yerine, XAML düğüm akışı, bir `Items` yer tutucuyu xaml koleksiyonunun bir üyesi olarak tanıtır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-302">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="1e3bb-303">.NET XAML Hizmetleri uygulamasında, bu yönergenin adı veya `_Items`düğüm akışındaki üye .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-303">In .NET XAML Services implementation, the name of this directive or member in the node stream is `_Items`.</span></span> <span data-ttu-id="1e3bb-304">Bu yönerge için bir <xref:System.Xaml.XamlLanguage.Items%2A>sabit elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-304">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="1e3bb-305">Bir XAML düğüm akışı, destek türü çözünürlüğü ve XAML şema bağlamına bağlı olarak ayrıştırılamaz öğeleri olan bir Öğeler özelliği içerebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-305">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="1e3bb-306">Örneğin,</span><span class="sxs-lookup"><span data-stu-id="1e3bb-306">For example,</span></span>

- <span data-ttu-id="1e3bb-307">**XML tanımlı üyeler:** XML tanımlı `xml:base` `xml:lang` ve `xml:space` üyeler XAML yönergeleri olarak `base` `lang`, `space` ve .NET XAML Hizmetleri uygulamaları olarak bildirilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-307">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in .NET XAML Services implementations.</span></span> <span data-ttu-id="1e3bb-308">Bunların ad alanı XML ad `http://www.w3.org/XML/1998/namespace`alanıdır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-308">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="1e3bb-309">Bunların her biri için sabitler <xref:System.Xaml.XamlLanguage>elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-309">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="1e3bb-310">Düğüm Sırası</span><span class="sxs-lookup"><span data-stu-id="1e3bb-310">Node Order</span></span>

<span data-ttu-id="1e3bb-311">Bazı durumlarda, <xref:System.Xaml.XamlXmlReader> XAML düğümü akışındaki XAML düğümlerinin sırasını değiştirir, biçimlendirmede görüntülenince veya XML olarak işlenmişse düğümlerin görünme sırasına karşılık.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-311">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="1e3bb-312">Bu, düğüm akışını yalnızca ileriye doğru <xref:System.Xaml.XamlObjectWriter> işleyebilir şekilde sıralamak için yapılır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-312">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="1e3bb-313">.NET XAML Services'da, XAML okuyucusu düğüm akışının XAML nesne yazarı tüketicileri için bir performans optimizasyonu olarak bu görevi XAML yazarına bırakmak yerine düğümleri yeniden sıralar.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-313">In .NET XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="1e3bb-314">Belirli yönergeler özellikle bir nesne öğesinden bir nesnenin oluşturulması için daha fazla bilgi sağlamak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-314">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="1e3bb-315">Bu direktifler `Initialization`şunlardır: `FactoryMethod` `Arguments`, `PositionalParameters`, `TypeArguments`, , .</span><span class="sxs-lookup"><span data-stu-id="1e3bb-315">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="1e3bb-316">.NET XAML Services XAML okuyucuları, bir nesnenin `StartObject`bir sonraki bölümünde açıklanan nedenlerle bu yönergeleri düğüm akışında ilk üye olarak yerleştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-316">.NET XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="1e3bb-317">XamlObjectWriter Davranış ve Düğüm Sırası</span><span class="sxs-lookup"><span data-stu-id="1e3bb-317">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="1e3bb-318">`StartObject`a <xref:System.Xaml.XamlObjectWriter> için mutlaka xaml nesne yazar için hemen nesne örneği oluşturmak için bir sinyal değildir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-318">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="1e3bb-319">XAML, bir nesneyi ek girdiyle başlatmayı ve ilk nesneyi oluşturmak için parametresiz bir oluşturucuya ve ancak daha sonra özellikleri ayarlamaya tamamen güvenmemelerini mümkün kınlayan çeşitli dil özellikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-319">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a parameterless constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="1e3bb-320">Bu özellikler <xref:System.Windows.Markup.XamlDeferLoadAttribute>şunlardır: ; başlatma metni; [x:TypeArguments](xtypearguments-directive.md); biçimlendirme uzantısının konumsal parametreleri; fabrika yöntemleri ve ilişkili [x:Bağımsız değişken](xarguments-directive.md) düğümleri (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="1e3bb-320">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](xtypearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](xarguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="1e3bb-321">Bu durumların her biri gerçek nesne yapısını geciktirir ve düğüm akışı yeniden sıralandığından, XAML nesne swriter, özellikle bu nesne türü için bir yapı yönergesi olmayan bir başlangıç üyesi karşılaşıldığında örneği gerçekten oluşturma davranışına güvenebilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-321">Each of these cases delays the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="1e3bb-322">GetObject</span><span class="sxs-lookup"><span data-stu-id="1e3bb-322">GetObject</span></span>

<span data-ttu-id="1e3bb-323">`GetObject`yeni bir nesne oluşturmak yerine, bir XAML nesne yazarı yerine nesnenin içeren özelliğinin değerini almalıdır bir XAML düğümü temsil eder.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-323">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="1e3bb-324">XAML düğümü `GetObject` akışında bir düğümle karşılaşılan tipik bir durum, içeren özelliğin destek türünün nesne modelinde kasıtlı olarak yalnızca okunduğu bir koleksiyon nesnesi veya sözlük nesnesi içindir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-324">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="1e3bb-325">Bu senaryoda, koleksiyon veya sözlük genellikle oluşturulur ve sahip bir tür başlatma mantığı tarafından (genellikle boş) başharfe çevrilir.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-325">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="1e3bb-326">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="1e3bb-326">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="1e3bb-327">XAML Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="1e3bb-327">XAML Services</span></span>](index.md)
- [<span data-ttu-id="1e3bb-328">XAML Ad Uzayları</span><span class="sxs-lookup"><span data-stu-id="1e3bb-328">XAML Namespaces</span></span>](namespaces.md)
