---
title: XAMLServices Sınıfı ve Temel XAML Okuma veya Yazma
ms.date: 03/30/2017
helpviewer_keywords:
- XAML [XAML Services], XamlServices class
- XamlServices class [XAML Services], how to use
ms.assetid: 6ac27fad-3687-4d7a-add1-3e90675fdfde
ms.openlocfilehash: 264a8c4abcf9a7efceb7c7accd786495d35476e5
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/12/2020
ms.locfileid: "82071880"
---
# <a name="xamlservices-class-and-basic-xaml-reading-or-writing"></a><span data-ttu-id="714f8-102">XAMLServices sınıf ve temel XAML okuma veya yazma</span><span class="sxs-lookup"><span data-stu-id="714f8-102">XAMLServices class and basic XAML reading or writing</span></span>

<span data-ttu-id="714f8-103"><xref:System.Xaml.XamlServices>.NET tarafından sağlanan ve XAML düğüm akışına veya bu düğümlerden elde edilen XAML türü sistem bilgilerine özel erişim gerektirmeyen XAML senaryolarını ele almak için kullanılabilen bir sınıftır.</span><span class="sxs-lookup"><span data-stu-id="714f8-103"><xref:System.Xaml.XamlServices> is a class provided by .NET that can be used to address XAML scenarios that don't require specific access to the XAML node stream or to XAML type system information obtained from those nodes.</span></span> <span data-ttu-id="714f8-104"><xref:System.Xaml.XamlServices>API aşağıdaki gibi `Load` özetlenebilir: `Parse` veya xaml yük `Save` yolunu desteklemek, bir XAML `Transform` kaydetme yolunu desteklemek ve bir yük yolunu birleştiren ve yolu kaydeden bir teknik sağlamak için.</span><span class="sxs-lookup"><span data-stu-id="714f8-104"><xref:System.Xaml.XamlServices> API can be summarized as follows: `Load` or `Parse` to support a XAML load path, `Save` to support a XAML save path, and `Transform` to provide a technique that joins a load path and save path.</span></span> <span data-ttu-id="714f8-105">`Transform`bir XAML şemasından diğerine geçiş için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-105">`Transform` can be used to change from one XAML schema to another.</span></span> <span data-ttu-id="714f8-106">Bu konu, bu API sınıflandırmalarının her birini özetler ve belirli yöntem aşırı yüklemeleri arasındaki farkları açıklar.</span><span class="sxs-lookup"><span data-stu-id="714f8-106">This topic summarizes each of these API classifications and describes the differences between particular method overloads.</span></span>

## <a name="load"></a><span data-ttu-id="714f8-107">Yükleme</span><span class="sxs-lookup"><span data-stu-id="714f8-107">Load</span></span>

<span data-ttu-id="714f8-108">Çeşitli aşırı <xref:System.Xaml.XamlServices.Load%2A> yükler, bir yük yolu için tam mantığı uygular.</span><span class="sxs-lookup"><span data-stu-id="714f8-108">Various overloads of <xref:System.Xaml.XamlServices.Load%2A> implement the complete logic for a load path.</span></span> <span data-ttu-id="714f8-109">Yük yolu bazı biçimlerde XAML kullanır ve bir XAML düğüm akışı çıkar.</span><span class="sxs-lookup"><span data-stu-id="714f8-109">The load path uses XAML in some form and outputs a XAML node stream.</span></span> <span data-ttu-id="714f8-110">Bu yükleme yollarının çoğu kodlanmış bir XML metin dosyası formunda XAML kullanır.</span><span class="sxs-lookup"><span data-stu-id="714f8-110">Most of these load paths use XAML in an encoded XML text-file form.</span></span> <span data-ttu-id="714f8-111">Ancak, genel bir akış da yükleyebilir veya zaten farklı <xref:System.Xaml.XamlReader> bir uygulamada bulunan önceden yüklenmiş bir XAML kaynağı yükleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="714f8-111">However, you can also load a general stream, or you can load a preloaded XAML source that is already contained in a different <xref:System.Xaml.XamlReader> implementation.</span></span>

<span data-ttu-id="714f8-112">Çoğu senaryo için en basit <xref:System.Xaml.XamlServices.Load%28System.String%29>aşırı yük .</span><span class="sxs-lookup"><span data-stu-id="714f8-112">The simplest overload for most scenarios is <xref:System.Xaml.XamlServices.Load%28System.String%29>.</span></span> <span data-ttu-id="714f8-113">Bu aşırı yükleme, yalnızca yüklenmesi gereken XAML'yi içeren bir metin dosyasının adı olan bir `fileName` parametreye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="714f8-113">This overload has a `fileName` parameter that is simply the name of a text file that contains the XAML to load.</span></span> <span data-ttu-id="714f8-114">Bu, daha önce durumu veya verileri yerel bilgisayara serileştirmiş tam güven uygulamaları gibi uygulama senaryoları için uygundur.</span><span class="sxs-lookup"><span data-stu-id="714f8-114">This is appropriate for application scenarios such as full trust applications that have previously serialized state or data to the local computer.</span></span> <span data-ttu-id="714f8-115">Bu, uygulama modelini tanımladığınız ve uygulama davranışını tanımlayan, Kullanıcı Arasını başlatan veya XAML kullanan diğer çerçeve tanımlı özelliklerden birini yüklemek istediğiniz çerçeveler için de yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="714f8-115">This is also useful for frameworks where you are defining the application model and want to load one of the standard files that defines application behavior, starting UI, or other framework-defined capabilities that use XAML.</span></span>

<span data-ttu-id="714f8-116"><xref:System.Xaml.XamlServices.Load%28System.IO.Stream%29>benzer senaryolara sahiptir.</span><span class="sxs-lookup"><span data-stu-id="714f8-116"><xref:System.Xaml.XamlServices.Load%28System.IO.Stream%29> has similar scenarios.</span></span> <span data-ttu-id="714f8-117">Bir dosya sistemine erişebilen diğer <xref:System.IO.Stream> <xref:System.IO> API'lerin sık sık çıktısı olduğundan, kullanıcının yüklemek için dosyaları seçmesi gerekiyorsa, bu aşırı yükleme yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-117">This overload might be useful if you have the user choose files to load, because a <xref:System.IO.Stream> is a frequent output of other <xref:System.IO> APIs that can access a file system.</span></span> <span data-ttu-id="714f8-118">Veya xaml kaynaklarına eşzamanlı indirmeler veya akış sağlayan diğer ağ teknikleri aracılığıyla erişiyor olabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="714f8-118">Or you could be accessing XAML sources through asynchronous downloads or other network techniques that also provide a stream.</span></span> <span data-ttu-id="714f8-119">(Bir akıştan veya kullanıcı tarafından seçilen bir kaynaktan yüklemenin güvenlik etkileri olabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-119">(Loading from a stream or user-selected source may have security implications.</span></span> <span data-ttu-id="714f8-120">Daha fazla bilgi için Bkz. [XAML Güvenlik Hususları](security-considerations.md).)</span><span class="sxs-lookup"><span data-stu-id="714f8-120">For more information, see [XAML Security Considerations](security-considerations.md).)</span></span>

<span data-ttu-id="714f8-121"><xref:System.Xaml.XamlServices.Load%28System.IO.TextReader%29>ve <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29> .NET'in önceki sürümlerindeki biçimlerin okuyucularına dayanan aşırı yüklerdir.</span><span class="sxs-lookup"><span data-stu-id="714f8-121"><xref:System.Xaml.XamlServices.Load%28System.IO.TextReader%29> and <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29> are overloads that rely on readers of formats from previous versions of .NET.</span></span> <span data-ttu-id="714f8-122">Bu aşırı yüklemeleri kullanmak için, zaten bir okuyucu `Create` örneği oluşturmuş ve XAML'yi ilgili forma (metin veya XML) yüklemek için API'sini kullanmış olmalısınız.</span><span class="sxs-lookup"><span data-stu-id="714f8-122">To use these overloads, you should have already created a reader instance and used its `Create` API to load the XAML in the relevant form (text or XML).</span></span> <span data-ttu-id="714f8-123">Kayıt işaretçilerini zaten diğer okuyuculara taşıdıysanız veya onlarla birlikte başka işlemler gerçekleştirdiyseniz, bu önemli değildir.</span><span class="sxs-lookup"><span data-stu-id="714f8-123">If you have already moved record pointers in the other readers or performed other operations with them, this is not important.</span></span> <span data-ttu-id="714f8-124">Yük yolu mantığı <xref:System.Xaml.XamlServices.Load%2A> her zaman kökten aşağı tüm XAML girişi işler.</span><span class="sxs-lookup"><span data-stu-id="714f8-124">The load path logic from <xref:System.Xaml.XamlServices.Load%2A> always processes the entire XAML input from the root down.</span></span> <span data-ttu-id="714f8-125">Aşağıdaki senaryolar, bu aşırı yüklerin kullanımını garanti edebilir:</span><span class="sxs-lookup"><span data-stu-id="714f8-125">The following scenarios might warrant the use of these overloads:</span></span>

- <span data-ttu-id="714f8-126">Varolan XML'e özgü metin düzenleyicisinden basit XAML düzenleme özelliği sağladığınız tasarım yüzeyleri.</span><span class="sxs-lookup"><span data-stu-id="714f8-126">Design surfaces where you provide simple XAML editing capability from an existing XML-specific text editor.</span></span>

- <span data-ttu-id="714f8-127">Dosyaları veya akışları <xref:System.IO> açmak için özel okuyucuları kullandığınız temel senaryoların türevleri.</span><span class="sxs-lookup"><span data-stu-id="714f8-127">Variants of the core <xref:System.IO> scenarios, where you use the dedicated readers to open files or streams.</span></span> <span data-ttu-id="714f8-128">Mantığınız, XAML olarak yüklemeye başlamadan önce içeriği temel olarak denetlemeyi veya işlemeyi gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="714f8-128">Your logic performs rudimentary checking or processing of the contents before it tries to load as XAML.</span></span>

<span data-ttu-id="714f8-129">Bir dosyayı veya akışı yükleyebilirsiniz veya <xref:System.Xml.XmlReader>okuyucunun API'leri ile yükleyerek XAML girişinizi sarar bir , <xref:System.IO.TextReader>yükleyebilirsiniz. <xref:System.Xaml.XamlReader></span><span class="sxs-lookup"><span data-stu-id="714f8-129">You can either load a file or stream, or you can load an <xref:System.Xml.XmlReader>, <xref:System.IO.TextReader>, or <xref:System.Xaml.XamlReader> that wraps your XAML input by loading with the reader's APIs.</span></span>

<span data-ttu-id="714f8-130">Dahili olarak, önceki aşırı yüklerin <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29>her biri <xref:System.Xml.XmlReader> sonuçta , ve <xref:System.Xaml.XamlXmlReader>geçen yeni bir oluşturmak için kullanılır .</span><span class="sxs-lookup"><span data-stu-id="714f8-130">Internally, each of the preceding overloads is ultimately <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29>, and the passed <xref:System.Xml.XmlReader> is used to create a new <xref:System.Xaml.XamlXmlReader>.</span></span>

<span data-ttu-id="714f8-131">Daha `Load` gelişmiş senaryolar sağlayan imza. <xref:System.Xaml.XamlServices.Load%28System.Xaml.XamlReader%29></span><span class="sxs-lookup"><span data-stu-id="714f8-131">The `Load` signature that provides for more advanced scenarios is <xref:System.Xaml.XamlServices.Load%28System.Xaml.XamlReader%29>.</span></span> <span data-ttu-id="714f8-132">Bu imzayı aşağıdaki durumlardan biri için kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="714f8-132">You can use this signature for one of the following cases:</span></span>

- <span data-ttu-id="714f8-133">Kendi uygulamanızı <xref:System.Xaml.XamlReader>tanımladınız.</span><span class="sxs-lookup"><span data-stu-id="714f8-133">You've defined your own implementation of a <xref:System.Xaml.XamlReader>.</span></span>

- <span data-ttu-id="714f8-134">Varsayılan ayarlardan farklı <xref:System.Xaml.XamlReader> ayarlar belirtmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="714f8-134">You need to specify settings for <xref:System.Xaml.XamlReader> that vary from the default settings.</span></span>

<span data-ttu-id="714f8-135">Varsayılan olmayan ayarlara örnekler:</span><span class="sxs-lookup"><span data-stu-id="714f8-135">Examples of non-default settings:</span></span>

<xref:System.Xaml.XamlReaderSettings.AllowProtectedMembersOnRoot%2A>\
<xref:System.Xaml.XamlReaderSettings.BaseUri%2A>\
<xref:System.Xaml.XamlReaderSettings.IgnoreUidsOnPropertyElements%2A>\
<xref:System.Xaml.XamlReaderSettings.LocalAssembly%2A>\
<span data-ttu-id="714f8-136"><xref:System.Xaml.XamlReaderSettings.ValuesMustBeString%2A>.</span><span class="sxs-lookup"><span data-stu-id="714f8-136"><xref:System.Xaml.XamlReaderSettings.ValuesMustBeString%2A>.</span></span>

<span data-ttu-id="714f8-137">Varsayılan okuyucu <xref:System.Xaml.XamlServices> için. <xref:System.Xaml.XamlXmlReader></span><span class="sxs-lookup"><span data-stu-id="714f8-137">The default reader for <xref:System.Xaml.XamlServices> is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="714f8-138">Kendi <xref:System.Xaml.XamlXmlReader> ayarlarınızı sağlarsanız, varsayılan olmayan ı ayarlamak <xref:System.Xaml.XamlXmlReaderSettings>için aşağıdaki özellikler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="714f8-138">If you provide your own <xref:System.Xaml.XamlXmlReader> with settings, the following are properties to set non-default <xref:System.Xaml.XamlXmlReaderSettings>:</span></span>

<xref:System.Xaml.XamlXmlReaderSettings.CloseInput%2A>\
<xref:System.Xaml.XamlXmlReaderSettings.SkipXmlCompatibilityProcessing%2A>\
<xref:System.Xaml.XamlXmlReaderSettings.XmlLang%2A>\
<xref:System.Xaml.XamlXmlReaderSettings.XmlSpacePreserve%2A>

## <a name="parse"></a><span data-ttu-id="714f8-139">Ayrıştır</span><span class="sxs-lookup"><span data-stu-id="714f8-139">Parse</span></span>

<span data-ttu-id="714f8-140"><xref:System.Xaml.XamlServices.Parse%2A>xaml girişi bir XAML düğüm akışı oluşturan bir yük yolu API `Load` olduğu gibi.</span><span class="sxs-lookup"><span data-stu-id="714f8-140"><xref:System.Xaml.XamlServices.Parse%2A> is like `Load` because it is a load path API that creates a XAML node stream from XAML input.</span></span> <span data-ttu-id="714f8-141">Ancak, bu durumda, XAML girişi doğrudan yüklemek için tüm XAML içeren bir dize olarak sağlanır.</span><span class="sxs-lookup"><span data-stu-id="714f8-141">However, in this case, the XAML input is provided directly as a string that contains all the XAML to load.</span></span> <span data-ttu-id="714f8-142"><xref:System.Xaml.XamlServices.Parse%2A>çerçeve senaryolarından çok uygulama senaryoları için daha uygun olan hafif bir yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="714f8-142"><xref:System.Xaml.XamlServices.Parse%2A> is a lightweight approach that is more appropriate for application scenarios than framework scenarios.</span></span> <span data-ttu-id="714f8-143">Daha fazla bilgi için bkz. <xref:System.Xaml.XamlServices.Parse%2A>.</span><span class="sxs-lookup"><span data-stu-id="714f8-143">For more information, see <xref:System.Xaml.XamlServices.Parse%2A>.</span></span> <span data-ttu-id="714f8-144"><xref:System.Xaml.XamlServices.Parse%2A>sadece bir iç <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29> içeren sarılmış <xref:System.IO.StringReader> bir çağrıdır.</span><span class="sxs-lookup"><span data-stu-id="714f8-144"><xref:System.Xaml.XamlServices.Parse%2A> is just a wrapped <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29> call that involves a <xref:System.IO.StringReader> internally.</span></span>

## <a name="save"></a><span data-ttu-id="714f8-145">Kaydet</span><span class="sxs-lookup"><span data-stu-id="714f8-145">Save</span></span>

<span data-ttu-id="714f8-146">Kaydet yolunu <xref:System.Xaml.XamlServices.Save%2A> n çeşitli aşırı yüklemeler uygulayın.</span><span class="sxs-lookup"><span data-stu-id="714f8-146">Various overloads of <xref:System.Xaml.XamlServices.Save%2A> implement the save path.</span></span> <span data-ttu-id="714f8-147">Tüm <xref:System.Xaml.XamlServices.Save%2A> yöntemlerin tümü bir nesne grafiğini giriş olarak alır ve <xref:System.Xml.XmlWriter> / <xref:System.IO.TextWriter> akış, dosya veya örnek olarak çıktı üretir.</span><span class="sxs-lookup"><span data-stu-id="714f8-147">All of the <xref:System.Xaml.XamlServices.Save%2A> methods all take an object graph as input and produce output as a stream, file, or <xref:System.Xml.XmlWriter>/<xref:System.IO.TextWriter> instance.</span></span>

<span data-ttu-id="714f8-148">Giriş nesnesinin bazı nesne gösteriminin kök nesnesi olması beklenir.</span><span class="sxs-lookup"><span data-stu-id="714f8-148">The input object is expected to be the root object of some object representation.</span></span> <span data-ttu-id="714f8-149">Bu, bir iş nesnesinin tek kökü, bir Web-oI senaryosundaki bir sayfa için nesne ağacının kökü, tasarım aracından çalışan düzenleme yüzeyi veya senaryolar için uygun olan diğer kök nesne kavramları olabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-149">This might be the single root of a business object, the root of an object tree for a page in a UI scenario, the working editing surface from a design tool, or other root object concepts that are appropriate for scenarios.</span></span>

<span data-ttu-id="714f8-150">Birçok senaryoda, kaydettiğiniz nesne ağacı, bir çerçeve/uygulama modeli tarafından <xref:System.Xaml.XamlServices.Load%2A> uygulanan diğer API'lerle veya XAML yüklenen özgün bir işlemle ilişkilidir.</span><span class="sxs-lookup"><span data-stu-id="714f8-150">In many scenarios, the object tree that you save is related to an original operation that loaded XAML either with <xref:System.Xaml.XamlServices.Load%2A> or with other API implemented by a framework/application model.</span></span> <span data-ttu-id="714f8-151">Nesne ağacında yakalanan ve durum değişiklikleri, uygulamanızın çalışma zamanı ayarlarını kullanıcıdan yakaladığı değişiklikler, xaml'ı değiştirmiş olabilir, çünkü uygulamanız XAML tasarım yüzeyi, vb. Değişiklikli veya değişikliksiz, önce biçimlendirmeden XAML yükleme ve sonra tekrar kaydetme ve iki XAML biçimlendirme formunu karşılaştırma kavramı bazen XAML'nin gidiş-dönüş gösterimi olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="714f8-151">There might be differences captured in the object tree that are due to state changes, changes where your application captured runtime settings from a user, changed XAML because your application is a XAML design surface, etc. With or without changes, the concept of first loading XAML from markup and then saving it again and comparing the two XAML markup forms is sometimes referred as a round-trip representation of the XAML.</span></span>

<span data-ttu-id="714f8-152">Biçimlendirme biçiminde ayarlanan karmaşık bir nesneyi kaydetme ve seri hale getirmenin zorluğu, xaml'ı daha az okunabilir kılan tam temsil arasında bir denge sağlamaktır.</span><span class="sxs-lookup"><span data-stu-id="714f8-152">The challenge with saving and serializing a complex object that is set in a markup form is in achieving a balance between full representation without information loss, versus verbosity that makes the XAML less human-readable.</span></span> <span data-ttu-id="714f8-153">Ayrıca, XAML için farklı müşteriler, bu bakiyenin nasıl ayarlanması gerektiğine ilişkin farklı tanımlara veya beklentilere sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-153">Moreover, different customers for XAML might have different definitions or expectations for how that balance should be set.</span></span> <span data-ttu-id="714f8-154">API'ler <xref:System.Xaml.XamlServices.Save%2A> bu dengenin bir tanımını temsil ediyor.</span><span class="sxs-lookup"><span data-stu-id="714f8-154">The <xref:System.Xaml.XamlServices.Save%2A> APIs represent one definition of that balance.</span></span> <span data-ttu-id="714f8-155">API'ler, <xref:System.Xaml.XamlServices.Save%2A> belirli XAML düğüm akışı yapılarının biçimlendirmeye geri kaydedildiğinde nerede optimize edilebileceğini belirlemek için kullanılabilir XAML şeması bağlamını ve varsayılan CLR tabanlı , <xref:System.Xaml.XamlType>ve <xref:System.Xaml.XamlMember>diğer XAML içsel ve XAML türü sistem kavramlarıözelliklerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="714f8-155">The <xref:System.Xaml.XamlServices.Save%2A> APIs use available XAML schema context and the default CLR-based characteristics of <xref:System.Xaml.XamlType>, <xref:System.Xaml.XamlMember>, and other XAML intrinsic and XAML type system concepts to determine where certain XAML node stream constructs can be optimized when they are saved back into markup.</span></span> <span data-ttu-id="714f8-156">Örneğin, <xref:System.Xaml.XamlServices> save yolları nesneler için çözmek <xref:System.Xaml.XamlType> için CLR tabanlı varsayılan XAML şema <xref:System.Xaml.XamlType.ContentProperty%2A?displayProperty=nameWithType>bağlamını kullanabilir, bir , belirleyebilir ve özelliği nesnenin XAML içeriğine yazdıklarında özellik öğesi etiketlerini atlayabilir.</span><span class="sxs-lookup"><span data-stu-id="714f8-156">For example, <xref:System.Xaml.XamlServices> save paths can use CLR-based default XAML schema context to resolve <xref:System.Xaml.XamlType> for objects, can determine a <xref:System.Xaml.XamlType.ContentProperty%2A?displayProperty=nameWithType>, and then can omit property element tags when they write the property to the XAML content of the object.</span></span>

<a name="transform"></a>
## <a name="transform"></a><span data-ttu-id="714f8-157">Dönüşüm</span><span class="sxs-lookup"><span data-stu-id="714f8-157">Transform</span></span>

<span data-ttu-id="714f8-158"><xref:System.Xaml.XamlServices.Transform%2A>bir yük yolunu ve kaydetme yolunu tek bir işlem olarak bağlayarak XAML'yi dönüştürür veya dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="714f8-158"><xref:System.Xaml.XamlServices.Transform%2A> converts or transforms XAML by linking a load path and a save path as a single operation.</span></span> <span data-ttu-id="714f8-159">Farklı bir şema bağlamı veya farklı bir <xref:System.Xaml.XamlReader> <xref:System.Xaml.XamlWriter>destek türü sistemi için kullanılabilir ve , hangi neden ortaya çıkan XAML dönüştürülür etkiler.</span><span class="sxs-lookup"><span data-stu-id="714f8-159">A different schema context or different backing type system can be used for <xref:System.Xaml.XamlReader> and <xref:System.Xaml.XamlWriter>, which is what influences how the resulting XAML is transformed.</span></span> <span data-ttu-id="714f8-160">Bu, geniş dönüştürme işlemleri için iyi çalışır.</span><span class="sxs-lookup"><span data-stu-id="714f8-160">This works well for broad transform operations.</span></span>

<span data-ttu-id="714f8-161">XAML düğüm akışındaki her düğümün incelenmesine dayanan işlemler için genellikle <xref:System.Xaml.XamlServices.Transform%2A>.</span><span class="sxs-lookup"><span data-stu-id="714f8-161">For operations that rely on examining each node in a XAML node stream, you typically do not use <xref:System.Xaml.XamlServices.Transform%2A>.</span></span> <span data-ttu-id="714f8-162">Bunun yerine kendi yük yolu kaydetme yol işlem serisini tanımlamanız ve kendi mantığınızı müdahale etmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="714f8-162">Instead you need to define your own load path-save path operation series and interject your own logic.</span></span> <span data-ttu-id="714f8-163">Yollardan birinde, kendi düğüm döngünüzün etrafında bir XAML okuyucu/XAML yazar çifti kullanın.</span><span class="sxs-lookup"><span data-stu-id="714f8-163">In one of the paths, use a XAML reader/XAML writer pair around your own node loop.</span></span> <span data-ttu-id="714f8-164">Örneğin, ilk XAML'yi <xref:System.Xaml.XamlXmlReader> kullanarak yükleyin ve ardışık <xref:System.Xaml.XamlXmlReader.Read%2A> çağrılarla düğümlere adım atın.</span><span class="sxs-lookup"><span data-stu-id="714f8-164">For example, load the initial XAML using <xref:System.Xaml.XamlXmlReader> and step into the nodes with successive <xref:System.Xaml.XamlXmlReader.Read%2A> calls.</span></span> <span data-ttu-id="714f8-165">XAML düğüm akışı düzeyinde çalışan artık bir dönüşüm uygulamak için tek tek düğümleri (türleri, üyeler, diğer düğümler) ayarlayabilir veya düğümü olduğu gibi bırakabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="714f8-165">Operating at the XAML node stream level you can now adjust individual nodes (types, members, other nodes) to apply a transformation, or leave the node as-is.</span></span> <span data-ttu-id="714f8-166">Ardından düğümü a'nın `Write` <xref:System.Xaml.XamlObjectWriter> ilgili API'sine gönderir ve nesneyi yazarsınız.</span><span class="sxs-lookup"><span data-stu-id="714f8-166">Then you send the node onwards to the relevant `Write` API of a <xref:System.Xaml.XamlObjectWriter> and write out the object.</span></span> <span data-ttu-id="714f8-167">Daha fazla bilgi için Bkz. [XAML Düğüm Akışı Yapılarını ve Kavramlarını Anlama.](understanding-xaml-node-stream-structures-and-concepts.md)</span><span class="sxs-lookup"><span data-stu-id="714f8-167">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="714f8-168">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="714f8-168">See also</span></span>

- <xref:System.Xaml.XamlObjectWriter>
- <xref:System.Xaml.XamlServices>
- [<span data-ttu-id="714f8-169">XAML Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="714f8-169">XAML Services</span></span>](../../../api/index.md)
