---
title: Visual Basic'te diziler
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: d76c14aa83fcf47faea41946614e83cfbc8ad57b
ms.sourcegitcommit: c93fd5139f9efcf6db514e3474301738a6d1d649
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/27/2018
ms.locfileid: "50195508"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="bf4a4-102">Diziler (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="bf4a4-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="bf4a4-103">Visual Basic 2017'den itibaren Visual Basic dili yapan diziler için yerleşik destek sunan tanımlama grupları oluşturma ve tanımlama grubu daha kolay öğelere erişme.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="bf4a4-104">Bir demet belirli sayıda ve sıralamadaki değerleri olan bir basit veri yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="bf4a4-105">Tanımlama grubu başlattığınızda, sayı ve her değeri (veya öğenin) veri türünü tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="bf4a4-106">Örneğin, iki öğe 2 bölütlü (veya çift) vardır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="bf4a4-107">İlk olabilir bir `Boolean` ikinci olmakla birlikte, değeri bir `String`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="bf4a4-108">Tanımlama grubu birden çok değeri tek bir nesnede depolamak kolaylaştırmak için bunlar genellikle bir yöntemden birden çok değer döndürmek için basit bir yolu olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="bf4a4-109">Tanımlama grubu desteği gerektiren <xref:System.ValueTuple> türü.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="bf4a4-110">.NET Framework 4.7 yüklü değilse, NuGet paketini eklemeniz gerekir `System.ValueTuple`, kullanılabildiği NuGet galerisinde.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="bf4a4-111">Bu paket olmadan bir derleme hatası benzer şekilde, "'ValueTuple(Of,,,)' tanımlı veya içeri aktarılmamış önceden tanımlanmış türü." alabilirsiniz</span><span class="sxs-lookup"><span data-stu-id="bf4a4-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="bf4a4-112">Örnekleme ve bir tanımlama grubu kullanımı</span><span class="sxs-lookup"><span data-stu-id="bf4a4-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="bf4a4-113">Bir kayıt düzeni, virgülle ayrılmış değerler IM parantezleri kapsayan tarafından örneği oluşturur.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="bf4a4-114">Bu değerlerin her birinin ardından düzeninin bir alan haline gelir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="bf4a4-115">Örneğin, aşağıdaki kod bir Üçlü (veya 3'lü demet) ile tanımlar bir `Date` ilk değer olarak bir `String` olarak kendi ikinci ve `Boolean` kendi üçüncü olarak.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="bf4a4-116">Varsayılan olarak, her bir alan tanımlama grubu adı dizesi oluşur `Item` alanın bir tabanlı konumu dizideki yanı sıra.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="bf4a4-117">Bu 3'lü demet için `Date` alandır `Item1`, `String` alandır `Item2`ve `Boolean` alandır `Item3`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="bf4a4-118">Aşağıdaki örnek kod önceki satırda örneği düzeninin alanların değerlerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="bf4a4-119">Visual Basic tanımlama grubu salt okunur alanları; bir demet kullanmanızın sonra değerlerini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="bf4a4-120">Aşağıdaki örnek önceki örnekte oluşturulan demet üç alanı iki değiştirir ve sonucu görüntüler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="bf4a4-121">Örnekleme ve adlandırılmış bir tanımlama grubu kullanımı</span><span class="sxs-lookup"><span data-stu-id="bf4a4-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="bf4a4-122">Bir Demetin alanlar için varsayılan adlar kullanmak yerine örneği oluşturabilir bir *demet adlı* düzeninin öğelerine kendi adlarınızı atayarak.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="bf4a4-123">Kayıt düzeninin alanları sonra atanan adlarıyla erişilebilir *veya* varsayılan adlarına göre.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="bf4a4-124">Açıkça ilk alan adları dışında aynı 3 demet olarak daha önce aşağıdaki örnekte başlatır `EventDate`, ikinci `Name`ve üçüncü `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="bf4a4-125">Ardından alan değerlerini görüntüler, bunları değiştirir ve yeniden alan değerlerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="bf4a4-126">Çıkarsanan demet öğesi adları</span><span class="sxs-lookup"><span data-stu-id="bf4a4-126">Inferred tuple element names</span></span>

<span data-ttu-id="bf4a4-127">Visual Basic, Visual Basic 15.3 ile başlayarak, tanımlama grubu öğelerinin adlarını çıkarımını; açıkça atamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="bf4a4-128">Çıkarsanan demet adları, değişkenlerin bir kümeden bir tanımlama grubu başlatmak ve demet öğesi adı değişken adı ile aynı olmasını istediğinizde yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="bf4a4-129">Aşağıdaki örnek, oluşturur bir `stateInfo` açıkça içeren üç dizi öğeleri, adlı `state`, `stateName`, ve `capital`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="bf4a4-130">Öğeleri adlandırmada unutmayın, dizi başlatma ifadesi, yalnızca adlandırılmış öğeleri aynı adlı değişkenlerin değerleri atar.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="bf4a4-131">Öğeleri ve değişkenler aynı ada sahip olduğundan, Visual Basic derleyicisi aşağıdaki örnekte gösterildiği gibi alanların adlarını çıkarabilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="bf4a4-132">İnterred demet öğesi adları etkinleştirmek için Visual Basic projesinde kullanmak için Visual Basic derleyici tanımlayın (\*.vbproj) dosya:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-132">To enable interred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="bf4a4-133">Herhangi bir sürümü 15.3 ile başlayarak Visual Basic derleyicinin sürüm numarası olabilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="bf4a4-134">Belirli bir derleyici sürümü kodlamak yerine, "Son" değeri olarak belirtebilirsiniz `LangVersion` sisteminizde yüklü Visual Basic derleyici en son sürümünü derlemek için.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="bf4a4-135">Daha fazla bilgi için [Visual Basic dil sürümü ayarını](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="bf4a4-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="bf4a4-136">Bazı durumlarda, Visual Basic Derleyicisi demet öğesi adı aday adından çıkarsanamıyor ve demet alanı yalnızca kendi varsayılan adı gibi kullanarak başvurulabilir `Item1`, `Item2`vb. Bu güncelleştirmeler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="bf4a4-137">Aday adı bir tanımlama grubu üyesinin adıyla aynı olduğu gibi `Item3`, `Rest`, veya `ToString`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="bf4a4-138">Tanımlama grubunda bulunan aday adı yineleniyor.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-138">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="bf4a4-139">Alan adı anlam çıkarma başarısız olduğunda, Visual Basic derleyici hatası oluşturmaz veya çalışma zamanında bir durum değil.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="bf4a4-140">Bunun yerine, tanımlama grubu alanları önceden tanımlanmış adlarıyla gibi başvurulmalıdır `Item1` ve `Item2`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="bf4a4-141">Diziler ve yapılar</span><span class="sxs-lookup"><span data-stu-id="bf4a4-141">Tuples versus structures</span></span>

<span data-ttu-id="bf4a4-142">Visual Basic tanımlama grubu örneği biri olan bir değer türü olan bir **System.ValueTuple** genel türler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="bf4a4-143">Örneğin, `holiday` önceki örnekte tanımlanan dizi öğesinin bir örneğiyse <xref:System.ValueTuple%603> yapısı.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="bf4a4-144">Veriler için basit bir kapsayıcı olacak şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="bf4a4-145">Birden çok veri öğeleri ile bir nesne oluşturmak kolay hale getirmek için demet amaçlar olduğundan, özel bir yapı olabilecek özelliklerden bazıları eksik olabilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="bf4a4-146">Bu güncelleştirmeler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-146">These include:</span></span>

- <span data-ttu-id="bf4a4-147">Özel üyeler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-147">Custom members.</span></span> <span data-ttu-id="bf4a4-148">Kendi özellikleri, yöntemleri ve olayları bir demet için tanımlayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="bf4a4-149">Doğrulama.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-149">Validation.</span></span> <span data-ttu-id="bf4a4-150">Alanlara atanan veri doğrulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="bf4a4-151">Değiştirilemezlik.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-151">Immutability.</span></span> <span data-ttu-id="bf4a4-152">Visual Basic diziler değişebilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="bf4a4-153">Bir örneği değişebilir veya sabit olup olmadığını buna karşılık, özel bir yapı denetlemenize izin verir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="bf4a4-154">Özel üyeler, özellik ve alan doğrulama veya değiştirilemezlik önemliyse, Visual Basic kullanması gereken [yapısı](../../../language-reference/statements/structure-statement.md) deyimi bir özel değer türü tanımlamak için.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="bf4a4-155">Visual Basic tanımlama grubu üyelerini devralır, **ValueTuple** türü.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="bf4a4-156">Alanlara ek olarak, bunlar aşağıdaki yöntemleri içerir:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="bf4a4-157">Üye</span><span class="sxs-lookup"><span data-stu-id="bf4a4-157">Member</span></span> | <span data-ttu-id="bf4a4-158">Açıklama</span><span class="sxs-lookup"><span data-stu-id="bf4a4-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="bf4a4-159">compareTo</span><span class="sxs-lookup"><span data-stu-id="bf4a4-159">CompareTo</span></span> | <span data-ttu-id="bf4a4-160">Geçerli tanımlama grubu için başka bir tanımlama grubu aynı sayıda öğe ile karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="bf4a4-161">Eşittir</span><span class="sxs-lookup"><span data-stu-id="bf4a4-161">Equals</span></span> | <span data-ttu-id="bf4a4-162">Geçerli tanımlama grubu başka bir tanımlama grubu veya nesneye eşit olup olmadığını belirler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="bf4a4-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="bf4a4-163">GetHashCode</span></span> | <span data-ttu-id="bf4a4-164">Geçerli örneğin karma kodunu hesaplar.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="bf4a4-165">ToString</span><span class="sxs-lookup"><span data-stu-id="bf4a4-165">ToString</span></span> | <span data-ttu-id="bf4a4-166">Bu dizi biçimi alır dize gösterimini döndürür `(Item1, Item2...)`burada `Item1` ve `Item2` düzeninin alanların değerlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="bf4a4-167">Ayrıca, **ValueTuple** türleri uygulayan <xref:System.Collections.IStructuralComparable> ve <xref:System.Collections.IStructuralEquatable> form veya arabirimlerini müşteri karşılaştırıcılarla tanımlamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="bf4a4-168">Atama ve diziler</span><span class="sxs-lookup"><span data-stu-id="bf4a4-168">Assignment and tuples</span></span>

<span data-ttu-id="bf4a4-169">Visual Basic, aynı sayıda alan tanımlama grubu türleri arasında atama destekler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="bf4a4-170">Aşağıdakilerden biri true ise alan türleri dönüştürülebilir:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="bf4a4-171">Kaynak ve hedef alan aynı türde olan.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="bf4a4-172">Kaynak türü hedef türü için bir genişletme (ya da örtük) dönüştürme tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="bf4a4-173">`Option Strict` olan `On`, ve bir kaynak türü daraltma (veya açık) dönüştürme için hedef türü tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="bf4a4-174">Bu dönüştürme, kaynak değeri hedef türün aralığı dışında ise bir özel durum.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="bf4a4-175">Diğer dönüştürme atamalarını kabul edilmez.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="bf4a4-176">Tanımlama grubu türleri arasında izin atamaları türlerini bakalım.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="bf4a4-177">Aşağıdaki örneklerde kullanılan bu değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="bf4a4-178">İlk iki değişken `unnamed` ve `anonymous`, alanlar için sağlanan anlam adları yok.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="bf4a4-179">Varsayılan alan adları olan `Item1` ve `Item2`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="bf4a4-180">Son iki değişken `named` ve `differentName` anlam alan adlarına sahip.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="bf4a4-181">Bu iki diziler alanları için farklı adlar olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="bf4a4-182">Dört bu tanımlama grubu ('kutup' adlandırılır) alanları aynı sayıda sahiptir ve bu alan türlerini aynıdır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="bf4a4-183">Bu nedenle, tüm bu atamaları çalışır:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="bf4a4-184">Tanımlama grubu adlarını atanmamış dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="bf4a4-185">Kayıt düzeni, alanların sırasını aşağıdaki alanların değerlerini atanır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="bf4a4-186">Son olarak, biz atayabilirsiniz dikkat edin `named` kullanılacak kayıt düzeni `conversion` başlığın olsa bile ilk alanı, `named` olduğu bir `Integer`ve ilk alanı, `conversion` olduğu bir `Long`.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="bf4a4-187">Dönüştürme çünkü bu atama başarılı bir `Integer` için bir `Long` genişleyen bir dönüştürmedir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="bf4a4-188">Diziler farklı sayıda alanlar atanabilir değil:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="bf4a4-189">Yöntemin dönüş değeri olarak diziler</span><span class="sxs-lookup"><span data-stu-id="bf4a4-189">Tuples as method return values</span></span>

<span data-ttu-id="bf4a4-190">Bir yöntem, yalnızca tek bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-190">A method can return only a single value.</span></span> <span data-ttu-id="bf4a4-191">Sık, ancak birden çok değer döndürmek için bir yöntem çağrısının istediğiniz.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="bf4a4-192">Bu sınırlara yakın çalışmak için birkaç yol vardır:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="bf4a4-193">Özel bir sınıf veya yapı özelliklerini oluşturabilir veya alanları yöntem tarafından döndürülen değerleri temsil eder.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="bf4a4-194">Bu nedenle ağır bir çözümdür; tek amacı, bir yöntem çağrısından değerleri almak için olan özel bir tür tanımlamak gerektiriyor.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="bf4a4-195">Yönteminden tek bir değer döndürmesi ve yönteme başvuru tarafından geçirerek kalan değerler döndürür.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="bf4a4-196">Bu, bir değişken ve başvuruya göre geçiren değişkenin değerini yanlışlıkla üzerine riskleri örnekleme ek yükünü içerir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="bf4a4-197">Birden çok değer almak için basit bir çözüm sağlayan bir tanımlama grubu kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="bf4a4-198">Örneğin, **TryParse** .NET dönüş yöntemleri bir `Boolean` ayrıştırma işleminin başarılı olup olmadığını gösteren değer.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="bf4a4-199">Ayrıştırma işleminin sonucu bir değişkene bir yönteme başvuru tarafından geçirilen döndürülür.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="bf4a4-200">Normalde, bir çağrı gibi bir ayrıştırma yönteminin <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> aşağıdaki gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="bf4a4-201">Biz çağrısını sarmalamak, biz bir demet ayrıştırma işlemi döndürebilir <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> kendi yöntemi yöntemi.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="bf4a4-202">Aşağıdaki örnekte, `NumericLibrary.ParseInteger` çağrıları <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> yöntemi ve iki öğe ile adlandırılmış bir tanımlama grubu döndürür.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="bf4a4-203">Ardından, aşağıdaki gibi kod ile yöntemi çağırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="bf4a4-204">Visual Basic diziler ve .NET Framework içindeki diziler</span><span class="sxs-lookup"><span data-stu-id="bf4a4-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="bf4a4-205">Visual Basic tanımlama grubu birinin örneğidir **System.ValueTuple** içinde .NET Framework 4.7 tanıtılan genel türler.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="bf4a4-206">.NET Framework ayrıca genel bir kümesini içerir **System.Tuple** sınıfları.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="bf4a4-207">Bu sınıfların ancak Visual Basic dizilerini ' farklı ve **System.ValueTuple** çeşitli yollarla genel türleri:</span><span class="sxs-lookup"><span data-stu-id="bf4a4-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="bf4a4-208">Öğeleri **demet** sınıflardır adlı özellikleri `Item1`, `Item2`ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="bf4a4-209">Visual Basic demetlerde ve **ValueTuple** türleri, tanımlama grubu öğelerinin alanlardır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="bf4a4-210">Öğeleri için anlamlı adlar atanamaz bir **demet** örneği veya bir **ValueTuple** örneği.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="bf4a4-211">Visual Basic, anlamı alanları, iletişim kuran adları atamanıza olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="bf4a4-212">Özellikleri bir **demet** örnek salt okunur; diziler sabittir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="bf4a4-213">Visual Basic demetlerde ve **ValueTuple** türleri, dizi alanları salt okunur; diziler değişebilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="bf4a4-214">Genel **demet** türleri başvuru türleridir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="bf4a4-215">Bunları kullanarak **demet** türleri nesneleri ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="bf4a4-216">Etkin yolları üzerinde bu, uygulamanızın performans üzerinde ölçülebilir bir etkisi olabilir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="bf4a4-217">Visual Basic diziler ve **ValueTuple** türler değer türleridir.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="bf4a4-218">Uzantı yöntemleri <xref:System.TupleExtensions> sınıfı kolaylaştırır .NET ve Visual Basic diziler arasında dönüştürme **demet** nesneleri.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="bf4a4-219">**ToTuple** yöntemi, bir Visual Basic tanımlama grubu için bir .NET dönüştürür **demet** nesnesi ve **ToValueTuple** yöntemi, bir .NET dönüştürür **demet** Visual Basic tanımlama grubu için nesne.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="bf4a4-220">Aşağıdaki örnek bir tanımlama grubu oluşturur, bir .NET ile dönüştürür **demet** nesnesi ve bir Visual Basic demet için geri dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="bf4a4-221">Örnek daha sonra bu demet eşit olmasını sağlamak için özgün bir karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="bf4a4-222">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="bf4a4-222">See also</span></span>

[<span data-ttu-id="bf4a4-223">Visual Basic Dili Başvurusu</span><span class="sxs-lookup"><span data-stu-id="bf4a4-223">Visual Basic Language Reference</span></span>](index.md)  
