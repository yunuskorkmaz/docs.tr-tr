---
description: Daha fazla bilgi için bkz. tanımlama bilgileri (Visual Basic)
title: Demetler
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: f598facb446b7d50864c0cf9151195cfcde158bb
ms.sourcegitcommit: 10e719780594efc781b15295e499c66f316068b8
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/14/2021
ms.locfileid: "100454462"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="0d555-103">Tanımlama grupları (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="0d555-103">Tuples (Visual Basic)</span></span>

<span data-ttu-id="0d555-104">Visual Basic 2017 ' den itibaren Visual Basic dili, tanımlama gruplarını oluşturmayı ve tanımlama gruplarının öğelerine erişimi kolaylaştıran tanımlama grupları için yerleşik destek sunar.</span><span class="sxs-lookup"><span data-stu-id="0d555-104">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="0d555-105">Kayıt düzeni, belirli bir sayı ve değer dizisi olan basit bir veri yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="0d555-105">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="0d555-106">Kayıt kümesini örneklediğinizde, her bir değerin (veya öğesinin) numarasını ve veri türünü tanımlarsınız.</span><span class="sxs-lookup"><span data-stu-id="0d555-106">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="0d555-107">Örneğin, 2 demet (veya çift) iki öğeye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="0d555-107">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="0d555-108">İlki `Boolean` bir değer, ikincisi ise bir değer olabilir `String` .</span><span class="sxs-lookup"><span data-stu-id="0d555-108">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="0d555-109">Tanımlama grupları birden çok değeri tek bir nesnede depolamayı kolaylaştırdığından, genellikle bir yöntemden birden çok değer döndürmenin hafif bir yolu olarak kullanılırlar.</span><span class="sxs-lookup"><span data-stu-id="0d555-109">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="0d555-110">Tanımlama grubu desteği <xref:System.ValueTuple> türü gerektirir.</span><span class="sxs-lookup"><span data-stu-id="0d555-110">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="0d555-111">.NET Framework 4,7 yüklü değilse, NuGet galerisinde bulunan NuGet paketini eklemeniz gerekir `System.ValueTuple` .</span><span class="sxs-lookup"><span data-stu-id="0d555-111">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="0d555-112">Bu paket olmadan şuna benzer bir derleme hatası alabilirsiniz, "önceden tanımlanmış tür ' ValueTuple (Of,,,) ' tanımlanmamış veya içeri aktarılmaz."</span><span class="sxs-lookup"><span data-stu-id="0d555-112">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="0d555-113">Tanımlama grubu örneği oluşturma ve kullanma</span><span class="sxs-lookup"><span data-stu-id="0d555-113">Instantiating and using a tuple</span></span>

<span data-ttu-id="0d555-114">Virgülle ayrılmış değerleri parantez içine alarak bir tanımlama grubu örnekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-114">You instantiate a tuple by enclosing its comma-delimited values in parentheses.</span></span> <span data-ttu-id="0d555-115">Bu değerlerin her biri, kayıt düzeni alanı haline gelir.</span><span class="sxs-lookup"><span data-stu-id="0d555-115">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="0d555-116">Örneğin, aşağıdaki kod, `Date` birinci değeri,, `String` ikincisinin ve üçüncü olarak bir ile bir üçlü (veya 3-kayıt) tanımlar `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="0d555-116">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="0d555-117">Varsayılan olarak, bir tanımlama grubu içindeki her alanın adı, dizenin `Item` kayıt düzeninde tek tabanlı konumuyla birlikte dizeden oluşur.</span><span class="sxs-lookup"><span data-stu-id="0d555-117">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="0d555-118">Bu 3 tanımlama grubu için alan, alanı `Date` `Item1` `String` `Item2` ve `Boolean` alanıdır `Item3` .</span><span class="sxs-lookup"><span data-stu-id="0d555-118">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="0d555-119">Aşağıdaki örnek, önceki kod satırında oluşturulan kayıt kümesi alanlarının değerlerini görüntüler</span><span class="sxs-lookup"><span data-stu-id="0d555-119">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="0d555-120">Visual Basic bir tanımlama grubu alanları okuma-yazma; bir tanımlama grubu örnekledikten sonra, değerlerini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-120">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="0d555-121">Aşağıdaki örnek, önceki örnekte oluşturulan kayıt düzeninin üç alanının ikisini değiştirir ve sonucu görüntüler.</span><span class="sxs-lookup"><span data-stu-id="0d555-121">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="0d555-122">Adlandırılmış bir tanımlama grubu örneği oluşturma ve kullanma</span><span class="sxs-lookup"><span data-stu-id="0d555-122">Instantiating and using a named tuple</span></span>

<span data-ttu-id="0d555-123">Bir tanımlama grubu alanları için varsayılan adları kullanmak yerine, kendi adlarınızı kayıt düzeni öğelerine atayarak *adlandırılmış bir tanımlama grubu* örneğini oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-123">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="0d555-124">Kayıt düzeni alanlarına, kendilerine atanan adlarıyla *veya* varsayılan adlarıyla erişilebilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-124">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="0d555-125">Aşağıdaki örnek, ilk alanı `EventDate` , ikincisini ve üçüncü kez açıkça isimlendiren şekilde, daha önceden aynı 3 kayıt hattı oluşturur `Name` `IsHoliday` .</span><span class="sxs-lookup"><span data-stu-id="0d555-125">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="0d555-126">Ardından alan değerlerini görüntüler, değiştirir ve alan değerlerini yeniden görüntüler.</span><span class="sxs-lookup"><span data-stu-id="0d555-126">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="0d555-127">Gösterilen demet öğesi adları</span><span class="sxs-lookup"><span data-stu-id="0d555-127">Inferred tuple element names</span></span>

<span data-ttu-id="0d555-128">Visual Basic 15,3 ' den başlayarak, Visual Basic demet öğelerinin adlarını çıkarabilir; Bunları açıkça atamanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="0d555-128">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="0d555-129">Çıkarsanan tanımlama grubu adları, bir dizi değişkenden bir tanımlama grubu başlattığınızda ve demet öğesi adının değişken adıyla aynı olmasını istediğinizde faydalıdır.</span><span class="sxs-lookup"><span data-stu-id="0d555-129">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="0d555-130">Aşağıdaki örnek, `stateInfo` açıkça adlandırılmış üç öğe,, `state` , ve içeren bir tanımlama grubu oluşturur `stateName` `capital` .</span><span class="sxs-lookup"><span data-stu-id="0d555-130">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="0d555-131">Öğeleri adlandırırken, demet başlatma bildiriminin adlandırılmış öğeleri, aynı adlı değişkenlerin değerlerini atadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="0d555-131">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="0d555-132">Öğeler ve değişkenler aynı ada sahip olduğundan, aşağıdaki örnekte gösterildiği gibi Visual Basic derleyici alanların adlarını çıkarsalabilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-132">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="0d555-133">Gösterilen demet öğesi adlarını etkinleştirmek için, Visual Basic projesi ( \* . vbproj) dosyanızda kullanmak üzere Visual Basic derleyicisinin sürümünü tanımlamanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="0d555-133">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="0d555-134">Sürüm numarası, 15,3 ile başlayan Visual Basic derleyicisinin herhangi bir sürümü olabilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-134">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="0d555-135">Belirli bir derleyici sürümünü sabit kodlamak yerine, `LangVersion` sisteminizde yüklü Visual Basic derleyicisinin en son sürümüyle derlemek için "en son" değerini de belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-135">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="0d555-136">Daha fazla bilgi için, [Visual Basic dil sürümünü ayarlama](../../../language-reference/configure-language-version.md)bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="0d555-136">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="0d555-137">Bazı durumlarda Visual Basic derleyici, aday adından demet öğesi adını çıkarsamaz ve demet alanına yalnızca varsayılan adı kullanılarak başvurulabilir, örneğin, `Item1` `Item2` vb. Bunlar şunları içerir:</span><span class="sxs-lookup"><span data-stu-id="0d555-137">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="0d555-138">Aday adı,, veya gibi bir demet üyesinin adı ile aynıdır `Item3` `Rest` `ToString` .</span><span class="sxs-lookup"><span data-stu-id="0d555-138">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="0d555-139">Aday adı kayıt düzeninde yinelenir.</span><span class="sxs-lookup"><span data-stu-id="0d555-139">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="0d555-140">Alan adının çıkarımı başarısız olduğunda, Visual Basic bir derleyici hatası oluşturmaz veya çalışma zamanında oluşturulan bir özel durumdur.</span><span class="sxs-lookup"><span data-stu-id="0d555-140">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="0d555-141">Bunun yerine, demet alanlarına, ve gibi önceden tanımlanmış adlarıyla başvurulmalıdır `Item1` `Item2` .</span><span class="sxs-lookup"><span data-stu-id="0d555-141">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="0d555-142">Tanımlama grupları ve yapılar</span><span class="sxs-lookup"><span data-stu-id="0d555-142">Tuples versus structures</span></span>

<span data-ttu-id="0d555-143">Visual Basic tanımlama grubu, **System. ValueTuple** genel türlerinden birinin bir örneği olan bir değer türüdür.</span><span class="sxs-lookup"><span data-stu-id="0d555-143">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="0d555-144">Örneğin, `holiday` Önceki örnekte tanımlanan kayıt düzeni yapının bir örneğidir <xref:System.ValueTuple%603> .</span><span class="sxs-lookup"><span data-stu-id="0d555-144">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="0d555-145">Veriler için hafif bir kapsayıcı olacak şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="0d555-145">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="0d555-146">Kayıt düzeni, birden fazla veri öğesiyle bir nesne oluşturmayı kolaylaştıran bir özel yapının sahip olabileceği bazı özelliklerden oluşur.</span><span class="sxs-lookup"><span data-stu-id="0d555-146">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="0d555-147">Bu modüller şunlardır:</span><span class="sxs-lookup"><span data-stu-id="0d555-147">These include:</span></span>

- <span data-ttu-id="0d555-148">Özel Üyeler.</span><span class="sxs-lookup"><span data-stu-id="0d555-148">Custom members.</span></span> <span data-ttu-id="0d555-149">Tanımlama grubu için kendi özelliklerinizi, yöntemlerinizi veya olaylarını tanımlayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="0d555-149">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="0d555-150">Doğrulamasına.</span><span class="sxs-lookup"><span data-stu-id="0d555-150">Validation.</span></span> <span data-ttu-id="0d555-151">Alanlara atanan verileri doğrulayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="0d555-151">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="0d555-152">Değiştirilemezlik.</span><span class="sxs-lookup"><span data-stu-id="0d555-152">Immutability.</span></span> <span data-ttu-id="0d555-153">Visual Basic tanımlama grupları değişebilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-153">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="0d555-154">Buna karşılık, özel bir yapı, bir örneğin değişebilir mi yoksa sabit mi olduğunu denetlemenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="0d555-154">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="0d555-155">Özel Üyeler, özellik ve alan doğrulama veya imlik kullanılabilirliği önemliyse, bir özel değer türü tanımlamak için Visual Basic [yapısı](../../../language-reference/statements/structure-statement.md) ifadesini kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="0d555-155">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="0d555-156">Visual Basic bir tanımlama grubu, değer **Etuple** türünün üyelerini devralınır.</span><span class="sxs-lookup"><span data-stu-id="0d555-156">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="0d555-157">Alanlarına ek olarak, bunlar aşağıdaki yöntemleri içerir:</span><span class="sxs-lookup"><span data-stu-id="0d555-157">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="0d555-158">Yöntem</span><span class="sxs-lookup"><span data-stu-id="0d555-158">Method</span></span> | <span data-ttu-id="0d555-159">Açıklama</span><span class="sxs-lookup"><span data-stu-id="0d555-159">Description</span></span> |
| ---|---|
| <span data-ttu-id="0d555-160">CompareTo</span><span class="sxs-lookup"><span data-stu-id="0d555-160">CompareTo</span></span> | <span data-ttu-id="0d555-161">Geçerli tanımlama grubunu aynı sayıda öğeye sahip başka bir tanımlama grubu ile karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="0d555-161">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="0d555-162">Eşittir</span><span class="sxs-lookup"><span data-stu-id="0d555-162">Equals</span></span> | <span data-ttu-id="0d555-163">Geçerli tanımlama grubunun başka bir demet veya nesneye eşit olup olmadığını belirler.</span><span class="sxs-lookup"><span data-stu-id="0d555-163">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="0d555-164">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="0d555-164">GetHashCode</span></span> | <span data-ttu-id="0d555-165">Geçerli örnek için karma kodu hesaplar.</span><span class="sxs-lookup"><span data-stu-id="0d555-165">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="0d555-166">ToString</span><span class="sxs-lookup"><span data-stu-id="0d555-166">ToString</span></span> | <span data-ttu-id="0d555-167">Bu tanımlama grubunun dize gösterimini döndürür. Bu, formunu alır `(Item1, Item2...)` `Item1` ve `Item2` kayıt düzeni alanlarının değerlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="0d555-167">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="0d555-168">Ayrıca, **Valuetuple** türleri <xref:System.Collections.IStructuralComparable> <xref:System.Collections.IStructuralEquatable> , ve arabirimlerini özel Karşılaştırıcılar tanımlamanızı sağlayacak şekilde uygular.</span><span class="sxs-lookup"><span data-stu-id="0d555-168">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define custom comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="0d555-169">Atama ve tanımlama grupları</span><span class="sxs-lookup"><span data-stu-id="0d555-169">Assignment and tuples</span></span>

<span data-ttu-id="0d555-170">Visual Basic, aynı sayıda alana sahip demet türleri arasında atamayı destekler.</span><span class="sxs-lookup"><span data-stu-id="0d555-170">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="0d555-171">Aşağıdakilerden biri doğruysa alan türleri dönüştürülebilir:</span><span class="sxs-lookup"><span data-stu-id="0d555-171">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="0d555-172">Kaynak ve hedef alanı aynı türde.</span><span class="sxs-lookup"><span data-stu-id="0d555-172">The source and target field are of the same type.</span></span>

- <span data-ttu-id="0d555-173">Kaynak türü için bir genişletme (veya örtük) hedef türüne dönüştürme tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="0d555-173">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="0d555-174">`Option Strict``On`, ve kaynak türün hedef türüne bir daraltma (veya açık) dönüştürmesi tanımlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="0d555-174">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="0d555-175">Bu dönüştürme, kaynak değer hedef türü aralığının dışındaysa bir özel durum oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-175">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="0d555-176">Diğer dönüşümler atamalar için değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="0d555-176">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="0d555-177">Demet türleri arasında izin verilen atama türlerine bakalım.</span><span class="sxs-lookup"><span data-stu-id="0d555-177">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="0d555-178">Aşağıdaki örneklerde kullanılan değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="0d555-178">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="0d555-179">İlk iki değişken `unnamed` ve `anonymous` , alanlar için belirtilen semantik adlara sahip değildir.</span><span class="sxs-lookup"><span data-stu-id="0d555-179">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="0d555-180">Alan adları varsayılan `Item1` ve ' dir `Item2` .</span><span class="sxs-lookup"><span data-stu-id="0d555-180">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="0d555-181">Son iki değişken `named` ve `differentName` anlamsal alan adlarına sahip.</span><span class="sxs-lookup"><span data-stu-id="0d555-181">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="0d555-182">Bu iki tanımlama alanının alanlar için farklı adlara sahip olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="0d555-182">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="0d555-183">Bu başlıkların dördü, aynı sayıda alana sahiptir (' parametre sayısı ' olarak adlandırılır) ve bu alanların türleri aynıdır.</span><span class="sxs-lookup"><span data-stu-id="0d555-183">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="0d555-184">Bu nedenle, bu atamaların hepsi çalışır:</span><span class="sxs-lookup"><span data-stu-id="0d555-184">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="0d555-185">Başlıkların adlarının atanmadığından emin olun.</span><span class="sxs-lookup"><span data-stu-id="0d555-185">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="0d555-186">Alanların değerleri, kayıt grubundaki alanların sırasını izleyerek atanır.</span><span class="sxs-lookup"><span data-stu-id="0d555-186">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="0d555-187">Son olarak, `named` `conversion` ilk alanı `named` bir, `Integer` ve ' nin ilk alanı bir olan olan ' a sahip olsa da, kayıt kümesini tanımlama grubu `conversion` atayabiliriz `Long` .</span><span class="sxs-lookup"><span data-stu-id="0d555-187">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="0d555-188">Bu atama başarılı oldu çünkü bir `Integer` öğesine dönüştürme `Long` , genişleyen bir dönüştürme.</span><span class="sxs-lookup"><span data-stu-id="0d555-188">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="0d555-189">Farklı sayıda alan içeren diziler atanamaz:</span><span class="sxs-lookup"><span data-stu-id="0d555-189">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="0d555-190">Yöntem dönüş değerleri olarak tanımlama grubu</span><span class="sxs-lookup"><span data-stu-id="0d555-190">Tuples as method return values</span></span>

<span data-ttu-id="0d555-191">Bir yöntem yalnızca tek bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-191">A method can return only a single value.</span></span> <span data-ttu-id="0d555-192">Genellikle, bir yöntem çağrısını birden çok değer döndürecek şekilde istersiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-192">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="0d555-193">Bu kısıtlamayı geçici olarak çözmek için birkaç yol vardır:</span><span class="sxs-lookup"><span data-stu-id="0d555-193">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="0d555-194">Özellikleri veya alanları yöntemi tarafından döndürülen değerleri temsil eden özel bir sınıf veya yapı oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-194">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="0d555-195">Bu nedenle ağır bir çözümdür; yalnızca amacı bir yöntem çağrısından değerleri almak olan özel bir tür tanımlamanızı gerektirir.</span><span class="sxs-lookup"><span data-stu-id="0d555-195">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="0d555-196">Yönteminden tek bir değer döndürebilir ve diğer değerleri yöntemine başvuruya göre geçirerek döndürebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-196">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="0d555-197">Bu, başvuruya göre geçirdiğiniz değişkenin değerinin yanlışlıkla üzerine yazılması için bir değişken ve riskleri örnekleyerek oluşan ek yükünü içerir.</span><span class="sxs-lookup"><span data-stu-id="0d555-197">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="0d555-198">Birden çok dönüş değerini almak için basit bir çözüm sağlayan bir tanımlama grubu kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="0d555-198">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="0d555-199">Örneğin, .NET 'teki **Tryparo** yöntemleri `Boolean` ayrıştırma işleminin başarılı olup olmadığını gösteren bir değer döndürür.</span><span class="sxs-lookup"><span data-stu-id="0d555-199">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="0d555-200">Ayrıştırma işleminin sonucu, metoduna verilen başvuruya göre geçirilen bir değişkende döndürülür.</span><span class="sxs-lookup"><span data-stu-id="0d555-200">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="0d555-201">Normalde, gibi bir ayrıştırma yöntemine yapılan bir çağrı aşağıdaki gibi <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> görünür:</span><span class="sxs-lookup"><span data-stu-id="0d555-201">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="0d555-202">Yöntemini kendi yöntemdeki yöntemine sarımızda ayrıştırma işleminden bir tanımlama grubu döndürüyoruz <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="0d555-202">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="0d555-203">Aşağıdaki örnekte, `NumericLibrary.ParseInteger` <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> yöntemini çağırır ve iki öğesi olan adlandırılmış bir tanımlama grubu döndürür.</span><span class="sxs-lookup"><span data-stu-id="0d555-203">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="0d555-204">Daha sonra aşağıdaki gibi kodla yöntemi çağırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="0d555-204">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="0d555-205">.NET Framework Visual Basic tanımlama grupları ve tanımlama grupları</span><span class="sxs-lookup"><span data-stu-id="0d555-205">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="0d555-206">Visual Basic tanımlama grubu, .NET Framework 4,7 ' de tanıtılan **System. ValueTuple** genel türlerinden birinin bir örneğidir.</span><span class="sxs-lookup"><span data-stu-id="0d555-206">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="0d555-207">.NET Framework Ayrıca bir dizi genel **System. Tuple** sınıfı içerir.</span><span class="sxs-lookup"><span data-stu-id="0d555-207">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="0d555-208">Ancak, bu sınıflar, Visual Basic tanımlama tiplerinden ve **System. ValueTuple** genel türlerinden farklı çeşitli yollarla farklılık gösterir:</span><span class="sxs-lookup"><span data-stu-id="0d555-208">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="0d555-209">**Demet** sınıflarının öğeleri, vb. adlı özelliklerdir `Item1` `Item2` .</span><span class="sxs-lookup"><span data-stu-id="0d555-209">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="0d555-210">Visual Basic tanımlama tiplerinde ve **Valuetuple** türlerinde, demet öğeleri alanlardır.</span><span class="sxs-lookup"><span data-stu-id="0d555-210">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="0d555-211">Bir **demet** örneğinin veya bir **Valuetuple** örneğinin öğelerine anlamlı adlar atayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="0d555-211">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="0d555-212">Visual Basic alanların anlamını karşılayan adlar atamanıza izin verir.</span><span class="sxs-lookup"><span data-stu-id="0d555-212">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="0d555-213">Bir **tanımlama grubu** örneğinin özellikleri salt okunurdur; tanımlama grupları sabittir.</span><span class="sxs-lookup"><span data-stu-id="0d555-213">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="0d555-214">Visual Basic tanımlama tiplerinde ve **Valuetuple** türlerinde, demet alanları okuma-yazma ' dır; tanımlama grupları değişebilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-214">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="0d555-215">Genel **demet** türleri başvuru türleridir.</span><span class="sxs-lookup"><span data-stu-id="0d555-215">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="0d555-216">Bu **demet** türlerinin kullanılması, nesneleri ayırmayı gösterir.</span><span class="sxs-lookup"><span data-stu-id="0d555-216">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="0d555-217">Etkin yollarda bu, uygulamanızın performansı üzerinde ölçülebilir bir etkiye sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="0d555-217">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="0d555-218">Visual Basic tanımlama grupları ve **Valuetuple** türleri değer türlerdir.</span><span class="sxs-lookup"><span data-stu-id="0d555-218">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="0d555-219">Sınıfındaki genişletme yöntemleri <xref:System.TupleExtensions> Visual Basic tanımlama grupları ve .net **demet** nesneleri arasında dönüştürme yapmayı kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="0d555-219">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="0d555-220">**ToTuple** yöntemi bir Visual Basic tanımlama grubunu bir .net **demet** nesnesine dönüştürür ve **tovaluetuple** yöntemi bir .net **demet** nesnesini bir Visual Basic tanımlama grubu öğesine dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="0d555-220">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="0d555-221">Aşağıdaki örnek bir tanımlama grubu oluşturur, bunu bir .NET **demet** nesnesine dönüştürür ve Visual Basic bir tanımlama grubu geri dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="0d555-221">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="0d555-222">Daha sonra örnek, eşit olduklarından emin olmak için bu tanımlama grubunu orijinal bir ile karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="0d555-222">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="0d555-223">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="0d555-223">See also</span></span>

- [<span data-ttu-id="0d555-224">Visual Basic dil başvurusu</span><span class="sxs-lookup"><span data-stu-id="0d555-224">Visual Basic Language Reference</span></span>](index.md)
