---
title: Visual Basic'de diziler
ms.custom: ''
ms.date: 04/23/2017
ms.prod: .net
ms.reviewer: ''
ms.suite: ''
ms.technology:
- devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 68c82e75ce4a438381bc9c60ce8c992565eb31cb
ms.sourcegitcommit: 9a4fe1a1c37b26532654b4bbe22d702237950009
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/16/2018
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="67391-102">Diziler (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="67391-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="67391-103">Visual Basic 2017 ile başlayarak, Visual Basic Dil yapar diziler için yerleşik destek sunar tanımlama grupları oluşturma ve diziler daha kolay öğelerini erişme.</span><span class="sxs-lookup"><span data-stu-id="67391-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="67391-104">Bir tanımlama grubu belirli sayıda ve değerleri dizisi olan bir hafif veri yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="67391-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="67391-105">Tuple örneği olduğunda sayısını ve her değeri (veya bir öğenin) veri türünü tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="67391-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="67391-106">Örneğin, bir 2 bölütlü (veya çifti) iki öğe sahiptir.</span><span class="sxs-lookup"><span data-stu-id="67391-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="67391-107">İlk olabilir bir `Boolean` değer ikinci olsa da bir `String`.</span><span class="sxs-lookup"><span data-stu-id="67391-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="67391-108">Tanımlama grupları birden çok değeri tek bir nesnede depolama kolaylaştırmak için bunlar genellikle birden çok değer döndürme için basit bir yol olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="67391-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="67391-109">Tuple destek gerektiren <xref:System.ValueTuple> türü.</span><span class="sxs-lookup"><span data-stu-id="67391-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="67391-110">.NET Framework 4.7 yüklü değilse, NuGet paketini eklemeniz gerekir `System.ValueTuple`, hangi kullanılabilir NuGet galerisinde.</span><span class="sxs-lookup"><span data-stu-id="67391-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="67391-111">Bu paketi bir derleme hatası benzer şekilde, "'ValueTuple(Of,,,)' tanımlı veya alınmamış önceden tanımlanmış türü." alabilirsiniz</span><span class="sxs-lookup"><span data-stu-id="67391-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="67391-112">Örnek oluşturma ve bir tanımlama grubu kullanma</span><span class="sxs-lookup"><span data-stu-id="67391-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="67391-113">Bir tanımlama grubu, virgülle ayrılmış değerler IM parantez kapsayan tarafından örneği oluşturur.</span><span class="sxs-lookup"><span data-stu-id="67391-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="67391-114">Bu değerlerin her birini daha sonra kayıt bir alan olur.</span><span class="sxs-lookup"><span data-stu-id="67391-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="67391-115">Örneğin, aşağıdaki kodu Üçlü (veya 3-tanımlama grubu) ile tanımlayan bir `Date` ilk değeri olarak bir `String` , ikinci olarak ve bir `Boolean` kendi üçüncü olarak.</span><span class="sxs-lookup"><span data-stu-id="67391-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="67391-116">Varsayılan olarak, bir tanımlama grubu içindeki her alan adını dizesi oluşur `Item` birlikte alanın tabanlı konumda yer alan tanımlama grubu.</span><span class="sxs-lookup"><span data-stu-id="67391-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="67391-117">Bu 3-başlığı için `Date` alandır `Item1`, `String` alandır `Item2`ve `Boolean` alanı `Item3`.</span><span class="sxs-lookup"><span data-stu-id="67391-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="67391-118">Aşağıdaki örnek kod önceki satırında örneği kayıt alanların değerlerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="67391-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="67391-119">Visual Basic tanımlama grubu okuma-yazma alanlardır; bir tanımlama grubu örneği sonra değerlerini değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="67391-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="67391-120">Aşağıdaki örnek önceki örnekte oluşturulan tanımlama grubu üç alanlarını ikilisi değiştirir ve sonucu görüntüler.</span><span class="sxs-lookup"><span data-stu-id="67391-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="67391-121">Örnek oluşturma ve adlandırılmış bir tanımlama grubu kullanma</span><span class="sxs-lookup"><span data-stu-id="67391-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="67391-122">Demete ait alanları için varsayılan adlar kullanmak yerine, bir örneğini oluşturabilirsiniz bir *tanımlama grubu adlı* demete ait öğelerine kendi adlarınızı atayarak.</span><span class="sxs-lookup"><span data-stu-id="67391-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="67391-123">Demete ait alanlara sonra atanan adlarıyla erişilebilir *veya* varsayılan adlarına göre.</span><span class="sxs-lookup"><span data-stu-id="67391-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="67391-124">Açıkça ilk alan adları dışında aynı 3-tanımlama grubu olarak daha önce aşağıdaki örnek başlatır `EventDate`, ikinci `Name`ve üçüncü `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="67391-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="67391-125">Bunu ardından alan değerlerini, bunları değiştirir ve alan değerlerini yeniden görüntüler.</span><span class="sxs-lookup"><span data-stu-id="67391-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="67391-126">Çıkarsanan tanımlama grubu öğe adları</span><span class="sxs-lookup"><span data-stu-id="67391-126">Inferred tuple element names</span></span>

<span data-ttu-id="67391-127">Visual Basic, Visual Basic 15.3 ile başlayarak, başlığın öğeleri adlarının çıkarımını; açıkça atamak gerekmez.</span><span class="sxs-lookup"><span data-stu-id="67391-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="67391-128">Çıkarsanan tanımlama grubu adları değişkenleri kümesinden bir tanımlama grubu başlatmak ve tanımlama grubu öğe adı değişken adı ile aynı olacak şekilde istediğinizde faydalıdır.</span><span class="sxs-lookup"><span data-stu-id="67391-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="67391-129">Aşağıdaki örnekte bir `stateInfo` üç açıkça içeren tanımlama grubu adlı öğeleri `state`, `stateName`, ve `capital`.</span><span class="sxs-lookup"><span data-stu-id="67391-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="67391-130">Öğeleri adlandırmada tanımlama grubu başlatma deyimi yalnızca adlandırılmış öğeleri aynı adlı değişkenlerin değerleri atar, unutmayın.</span><span class="sxs-lookup"><span data-stu-id="67391-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="67391-131">Visual Basic derleyici öğeleri ve değişkenleri aynı ada sahip olduğundan, aşağıdaki örnekte gösterildiği gibi alanların adlarını çıkarımını.</span><span class="sxs-lookup"><span data-stu-id="67391-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="67391-132">İnterred tanımlama grubu öğe adları etkinleştirmek için Visual Basic projesinde kullanmak için Visual Basic derleyici sürümünü tanımlama (\*.vbproj) dosyası:</span><span class="sxs-lookup"><span data-stu-id="67391-132">To enable interred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="67391-133">Sürüm numarası 15.3 ile başlayan Visual Basic derleyici herhangi bir sürümü olabilir.</span><span class="sxs-lookup"><span data-stu-id="67391-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="67391-134">Belirli derleyici sürümü kodlamak yerine, ayrıca "Son" değeri olarak belirtebilirsiniz `LangVersion` sisteminizde yüklü Visual Basic derleyici en son sürümü ile derlemek için.</span><span class="sxs-lookup"><span data-stu-id="67391-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="67391-135">Bazı durumlarda, Visual Basic derleyici adayı adından tanımlama grubu öğe adı gösterilemez ve dizi alanını yalnızca varsayılan adını kullanarak başvurulabilir `Item1`, `Item2`vb. Bu güncelleştirmeler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="67391-135">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="67391-136">Aday adı bir tanımlama grubu üye adı ile aynı olduğu gibi `Item3`, `Rest`, veya `ToString`.</span><span class="sxs-lookup"><span data-stu-id="67391-136">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="67391-137">Aday adı düzeninde yineleniyor.</span><span class="sxs-lookup"><span data-stu-id="67391-137">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="67391-138">Alan adı çıkarım başarısız olduğunda, Visual Basic derleyici hatası oluşturmaz veya çalışma zamanında bir durum değil.</span><span class="sxs-lookup"><span data-stu-id="67391-138">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="67391-139">Bunun yerine, kayıt alanları önceden tanımlanmış adlarıyla gibi başvurulmalıdır `Item1` ve `Item2`.</span><span class="sxs-lookup"><span data-stu-id="67391-139">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="67391-140">Yapılar ve diziler</span><span class="sxs-lookup"><span data-stu-id="67391-140">Tuples versus structures</span></span>

<span data-ttu-id="67391-141">Visual Basic tanımlama grubu birini örneği olan bir değer türü olan bir **System.ValueTuple** genel türler.</span><span class="sxs-lookup"><span data-stu-id="67391-141">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="67391-142">Örneğin, `holiday` tanımlama grubu önceki örnekte tanımlanan bir örneğidir <xref:System.ValueTuple%603> yapısı.</span><span class="sxs-lookup"><span data-stu-id="67391-142">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="67391-143">Veriler için basit bir kapsayıcı olacak şekilde tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="67391-143">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="67391-144">Birden çok veri öğeleri ile bir nesne oluşturmayı kolaylaştırmak için tanımlama grubu amaçlar olduğundan, özel bir yapıya sahip özelliklerden bazıları eksik.</span><span class="sxs-lookup"><span data-stu-id="67391-144">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="67391-145">Bu güncelleştirmeler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="67391-145">These include:</span></span>

- <span data-ttu-id="67391-146">Özel üye.</span><span class="sxs-lookup"><span data-stu-id="67391-146">Custom members.</span></span> <span data-ttu-id="67391-147">Kendi özellikleri, yöntemleri ya da bir tanımlama grubu için olayları tanımlayamazsınız.</span><span class="sxs-lookup"><span data-stu-id="67391-147">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="67391-148">Doğrulama.</span><span class="sxs-lookup"><span data-stu-id="67391-148">Validation.</span></span> <span data-ttu-id="67391-149">Alanlarına atanan veri doğrulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="67391-149">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="67391-150">Girişi.</span><span class="sxs-lookup"><span data-stu-id="67391-150">Immutability.</span></span> <span data-ttu-id="67391-151">Visual Basic diziler değişebilir.</span><span class="sxs-lookup"><span data-stu-id="67391-151">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="67391-152">Değişebilir veya sabit bir örneği olup olmadığını buna karşılık, özel bir yapı denetlemenize izin verir.</span><span class="sxs-lookup"><span data-stu-id="67391-152">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="67391-153">Özel üye, özellik ve alan doğrulama veya girişi önemliyse, Visual Basic kullanması gereken [yapısı](../../../language-reference/statements/structure-statement.md) deyimi bir özel değer türünü tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="67391-153">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="67391-154">Visual Basic tanımlama grubu üyelerinin devralmıyor kendi **ValueTuple** türü.</span><span class="sxs-lookup"><span data-stu-id="67391-154">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="67391-155">Kendi alanlara ek olarak, bunlar aşağıdaki yöntemler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="67391-155">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="67391-156">Üye</span><span class="sxs-lookup"><span data-stu-id="67391-156">Member</span></span> | <span data-ttu-id="67391-157">Açıklama</span><span class="sxs-lookup"><span data-stu-id="67391-157">Description</span></span> |
| ---|---|
| <span data-ttu-id="67391-158">CompareTo</span><span class="sxs-lookup"><span data-stu-id="67391-158">CompareTo</span></span> | <span data-ttu-id="67391-159">Geçerli tanımlama grubu için başka bir tanımlama grubu aynı sayıda öğe ile karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="67391-159">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="67391-160">Eşittir</span><span class="sxs-lookup"><span data-stu-id="67391-160">Equals</span></span> | <span data-ttu-id="67391-161">Geçerli tanımlama grubu başka bir tanımlama grubu veya nesneye eşit olup olmadığını belirler.</span><span class="sxs-lookup"><span data-stu-id="67391-161">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="67391-162">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="67391-162">GetHashCode</span></span> | <span data-ttu-id="67391-163">Geçerli örneğe ilişkin karma kodu hesaplar.</span><span class="sxs-lookup"><span data-stu-id="67391-163">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="67391-164">ToString</span><span class="sxs-lookup"><span data-stu-id="67391-164">ToString</span></span> | <span data-ttu-id="67391-165">Formundadır bu tanımlama grubu dize gösterimini döndürür `(Item1, Item2...)`, burada `Item1` ve `Item2` demete ait alanların değerlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="67391-165">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="67391-166">Ayrıca, **ValueTuple** türleri uygulayan <xref:System.Collections.IStructuralComparable> ve <xref:System.Collections.IStructuralEquatable> müşteri comparers tanımlamanıza izin arabirimleri.</span><span class="sxs-lookup"><span data-stu-id="67391-166">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="67391-167">Atama ve diziler</span><span class="sxs-lookup"><span data-stu-id="67391-167">Assignment and tuples</span></span>

<span data-ttu-id="67391-168">Visual Basic atama alanları aynı sayıda tanımlama grubu türleri arasındaki destekler.</span><span class="sxs-lookup"><span data-stu-id="67391-168">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="67391-169">Aşağıdakilerden biri doğruysa alan türleri dönüştürülebilir:</span><span class="sxs-lookup"><span data-stu-id="67391-169">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="67391-170">Kaynak ve hedef alan aynı türde olan.</span><span class="sxs-lookup"><span data-stu-id="67391-170">The source and target field are of the same type.</span></span>

- <span data-ttu-id="67391-171">Kaynak türü hedef türü için bir genişletme (ya da örtük) dönüştürme tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="67391-171">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="67391-172">`Option Strict` olan `On`, ve bir kaynak türü daraltma (veya açık) dönüştürülmesi hedef türü için tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="67391-172">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="67391-173">Kaynak değerin hedef türü aralık dışında ise bu dönüştürme bir özel durum.</span><span class="sxs-lookup"><span data-stu-id="67391-173">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="67391-174">Diğer dönüştürme atamaları dikkate alınmaz.</span><span class="sxs-lookup"><span data-stu-id="67391-174">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="67391-175">Dizi türleri arasında izin atamalarını türlerini bakalım.</span><span class="sxs-lookup"><span data-stu-id="67391-175">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="67391-176">Aşağıdaki örneklerde kullanılan bu değişkenleri göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="67391-176">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="67391-177">İlk iki değişkenleri `unnamed` ve `anonymous`, alanlar için sağlanan anlamsal adları yok.</span><span class="sxs-lookup"><span data-stu-id="67391-177">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="67391-178">Varsayılan alan adları olan `Item1` ve `Item2`.</span><span class="sxs-lookup"><span data-stu-id="67391-178">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="67391-179">Son iki değişken `named` ve `differentName` anlamsal alan adları içeriyor.</span><span class="sxs-lookup"><span data-stu-id="67391-179">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="67391-180">Bu iki başlığın alanları için farklı adlar gerektiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="67391-180">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="67391-181">Dört Bu başlık aynı sayıda alanlar ('parametre' adlandırılır) sahip ve bu alan türlerini aynıdır.</span><span class="sxs-lookup"><span data-stu-id="67391-181">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="67391-182">Bu nedenle, tüm bu atamaları çalışır:</span><span class="sxs-lookup"><span data-stu-id="67391-182">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="67391-183">Diziler adları atanmamıştır dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="67391-183">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="67391-184">Tanımlama grubundaki alanların sırasını aşağıdaki alanların değerlerini atanır.</span><span class="sxs-lookup"><span data-stu-id="67391-184">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="67391-185">Son olarak, biz atayabilirsiniz dikkat edin `named` tanımlama grubu için `conversion` başlığın olsa bile ilk alanını `named` olan bir `Integer`ve ilk alanını `conversion` olan bir `Long`.</span><span class="sxs-lookup"><span data-stu-id="67391-185">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="67391-186">Dönüştürme çünkü bu ataması başarılı bir `Integer` için bir `Long` genişletme dönüştürme değildir.</span><span class="sxs-lookup"><span data-stu-id="67391-186">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="67391-187">Diziler farklı sayıda alana sahip atanabilir değil:</span><span class="sxs-lookup"><span data-stu-id="67391-187">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="67391-188">Diziler yöntemi olarak dönüş değerleri</span><span class="sxs-lookup"><span data-stu-id="67391-188">Tuples as method return values</span></span>

<span data-ttu-id="67391-189">Bir yöntem yalnızca tek bir değer döndürebilir.</span><span class="sxs-lookup"><span data-stu-id="67391-189">A method can return only a single value.</span></span> <span data-ttu-id="67391-190">Sık sık rağmen birden çok değer döndürmek için bir yöntem çağrısı istersiniz.</span><span class="sxs-lookup"><span data-stu-id="67391-190">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="67391-191">Bu sınırlamaya geçici bir çözüm için birkaç yol vardır:</span><span class="sxs-lookup"><span data-stu-id="67391-191">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="67391-192">Özel sınıf veya yapı özellikleri oluşturabilir veya alanlar yöntemi tarafından döndürülen değerleri temsil eder.</span><span class="sxs-lookup"><span data-stu-id="67391-192">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="67391-193">Bu nedenle bir ağır çözümdür; Bu yöntem çağrısından değerleri almak için tek amacı olan bir özel tür tanımlamanızı gerektirir.</span><span class="sxs-lookup"><span data-stu-id="67391-193">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="67391-194">Tek bir değer döndürme ve yöntemine başvuruya geçirerek kalan değerler döndürür.</span><span class="sxs-lookup"><span data-stu-id="67391-194">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="67391-195">Bu değişken ve başvuruya göre geçirme değişkenin değerini yanlışlıkla üzerine riskleri başlatmasını yükünü içerir.</span><span class="sxs-lookup"><span data-stu-id="67391-195">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="67391-196">Birden çok dönüş değerleri alma için basit bir çözüm sağlar bir tanımlama grubu kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="67391-196">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="67391-197">Örneğin, **TryParseyöntemini** .NET return yöntemleri bir `Boolean` ayrıştırma işlemi başarılı olup olmadığını belirten değer.</span><span class="sxs-lookup"><span data-stu-id="67391-197">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="67391-198">Ayrıştırma işleminin sonucu başvuruya yöntemine geçirilen bir değişken döndürülür.</span><span class="sxs-lookup"><span data-stu-id="67391-198">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="67391-199">Normal olarak yapılan bir çağrı gibi ayrıştırma yönteminin <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> aşağıdaki gibi görünür:</span><span class="sxs-lookup"><span data-stu-id="67391-199">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="67391-200">Biz çağrısı sarmalama, biz bir tanımlama grubu ayrıştırma işlemi döndürebilir <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> kendi yöntemi yöntemi.</span><span class="sxs-lookup"><span data-stu-id="67391-200">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="67391-201">Aşağıdaki örnekte, `NumericLibrary.ParseInteger` çağrıları <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> yöntemi ve iki öğe ile adlandırılmış bir tanımlama grubu döndürür.</span><span class="sxs-lookup"><span data-stu-id="67391-201">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="67391-202">Ardından aşağıdaki gibi kod ile yöntemi çağırabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="67391-202">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="67391-203">Visual Basic başlıkları ve .NET Framework diziler</span><span class="sxs-lookup"><span data-stu-id="67391-203">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="67391-204">Visual Basic tanımlama grubu birinin örneğidir **System.ValueTuple** .NET Framework 4.7 içinde sunulan genel türler.</span><span class="sxs-lookup"><span data-stu-id="67391-204">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="67391-205">.NET Framework genel bir dizi de içerir **System.Tuple** sınıfları.</span><span class="sxs-lookup"><span data-stu-id="67391-205">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="67391-206">Bu sınıfları, ancak Visual Basic diziler farklı ve **System.ValueTuple** çeşitli yollarla genel türleri:</span><span class="sxs-lookup"><span data-stu-id="67391-206">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="67391-207">Öğelerini **tanımlama grubu** sınıflardır adlı özellikleri `Item1`, `Item2`ve benzeri.</span><span class="sxs-lookup"><span data-stu-id="67391-207">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="67391-208">Visual Basic dizilerde ve **ValueTuple** türleri, başlığın öğeleri alanlardır.</span><span class="sxs-lookup"><span data-stu-id="67391-208">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="67391-209">Anlamlı adlar öğelerine atayamazsınız bir **tanımlama grubu** örneği veya bir **ValueTuple** örneği.</span><span class="sxs-lookup"><span data-stu-id="67391-209">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="67391-210">Visual Basic, alanları anlamını iletişim adları atamanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="67391-210">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="67391-211">Özelliklerini bir **tanımlama grubu** örnek salt okunur; başlıklar değişmez.</span><span class="sxs-lookup"><span data-stu-id="67391-211">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="67391-212">Visual Basic dizilerde ve **ValueTuple** türleri tanımlama grubu alanlardır okuma-yazma; başlıklar değişebilir.</span><span class="sxs-lookup"><span data-stu-id="67391-212">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="67391-213">Genel **tanımlama grubu** türleri başvuru türleridir.</span><span class="sxs-lookup"><span data-stu-id="67391-213">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="67391-214">Bunlar kullanarak **tanımlama grubu** türleri nesneleri ayırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="67391-214">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="67391-215">Sık kullanılan yollarına göre bu, uygulamanızın performans üzerinde ölçülebilir bir etkisi olabilir.</span><span class="sxs-lookup"><span data-stu-id="67391-215">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="67391-216">Visual Basic başlıkları ve **ValueTuple** değer türleri türleridir.</span><span class="sxs-lookup"><span data-stu-id="67391-216">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="67391-217">Genişletme yöntemleri <xref:System.TupleExtensions> sınıfı kolaylaştırır Visual Basic başlıkları ve .NET arasında dönüştürme **tanımlama grubu** nesneleri.</span><span class="sxs-lookup"><span data-stu-id="67391-217">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="67391-218">**ToTuple** yöntemi dönüştürür Visual Basic tanımlama grubu için bir .NET **tanımlama grubu** nesnesi ve **ToValueTuple** yöntemi bir .NET dönüştürür **tanımlama grubu** Visual Basic tanımlama grubu nesne.</span><span class="sxs-lookup"><span data-stu-id="67391-218">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="67391-219">Aşağıdaki örnek bir tanımlama grubu oluşturur, bir .NET dönüştürür **tanımlama grubu** nesne ve bir Visual Basic tanımlama grubu geri dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="67391-219">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="67391-220">Örnek daha sonra bu tanımlama grubu eşit olduğundan emin olmak için özgün bir karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="67391-220">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="67391-221">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="67391-221">See also</span></span>

[<span data-ttu-id="67391-222">Visual Basic Dil Başvurusu</span><span class="sxs-lookup"><span data-stu-id="67391-222">Visual Basic Language Reference</span></span>](index.md)  
