---
title: TümevarLanabilir Uygulama
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266917"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="d2c09-102">İzlenecek yol: Visual Basic'de IEnumerable(Of T) Uygulama</span><span class="sxs-lookup"><span data-stu-id="d2c09-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="d2c09-103">Arabirim, <xref:System.Collections.Generic.IEnumerable%601> bir defada bir öğe olan değerler dizisini döndürebilen sınıflar tarafından uygulanır.</span><span class="sxs-lookup"><span data-stu-id="d2c09-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="d2c09-104">Verileri bir seferde bir öğe döndürmenin avantajı, onunla çalışmak için tüm veri kümesini belleğe yüklemeniz gerekmemesidir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="d2c09-105">Verilerden tek bir öğeyi yüklemek için yalnızca yeterli bellek kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="d2c09-106">`IEnumerable(T)` Arabirimi uygulayan sınıflar döngüler `For Each` veya LINQ sorguları ile kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="d2c09-107">Örneğin, büyük bir metin dosyasını okuması ve belirli arama ölçütleri ile eşleşen dosyadan her satırı döndürmesi gereken bir uygulama düşünün.</span><span class="sxs-lookup"><span data-stu-id="d2c09-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="d2c09-108">Uygulama, belirtilen ölçütlere uyan dosyadan satırları döndürmek için bir LINQ sorgusu kullanır.</span><span class="sxs-lookup"><span data-stu-id="d2c09-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="d2c09-109">Linq sorgusu kullanarak dosyanın içeriğini sorgulamak için, uygulama dosyanın içeriğini bir diziye veya koleksiyona yükleyebilir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="d2c09-110">Ancak, tüm dosyayı bir diziye veya koleksiyona yüklemek gerekenden çok daha fazla bellek tüketir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="d2c09-111">LINQ sorgusu bunun yerine, yalnızca arama ölçütleri ile eşleşen değerleri döndürerek, dosya içeriğini yeniden kullanılabilir bir sınıf kullanarak sorgulayabilir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="d2c09-112">Yalnızca birkaç eşleşen değer döndüren sorgular çok daha az bellek tüketir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="d2c09-113">Kaynak verileri sayısal veriler olarak <xref:System.Collections.Generic.IEnumerable%601> ortaya çıkarmak için arabirimi uygulayan bir sınıf oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d2c09-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="d2c09-114">`IEnumerable(T)` Arabirimi uygulayan sınıfınız, kaynak veriler aracılığıyla <xref:System.Collections.Generic.IEnumerator%601> yinelenecek arabirimi uygulayan başka bir sınıf gerektirir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="d2c09-115">Bu iki sınıf, belirli bir tür olarak sırayla veri öğeleri döndürmenize olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="d2c09-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="d2c09-116">Bu gözden geçirme de, `IEnumerable(Of String)` arabirimi uygulayan bir sınıf ve bir `IEnumerator(Of String)` metin dosyasını teker tek satır okumak için arabirimi uygulayan bir sınıf oluşturursunuz.</span><span class="sxs-lookup"><span data-stu-id="d2c09-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="d2c09-117">Sayısal Sınıf Oluşturma</span><span class="sxs-lookup"><span data-stu-id="d2c09-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="d2c09-118">**Sayısal sınıf projesini oluşturma**</span><span class="sxs-lookup"><span data-stu-id="d2c09-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="d2c09-119">Visual Basic'te **Dosya** menüsünde **Yeni'yi** işaret edin ve **Project'i**tıklatın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="d2c09-120">Yeni **Proje** iletişim kutusunda, **Proje Türleri** bölmesinde **Windows'un** seçildiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="d2c09-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="d2c09-121">**Şablonlar** bölmesinde **Sınıf Kitaplığı'nı** seçin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="d2c09-122">**Ad** kutusuna, `StreamReaderEnumerable`''yı yazın ve sonra **Tamam'ı**tıklatın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="d2c09-123">Yeni proje görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-123">The new project is displayed.</span></span>

1. <span data-ttu-id="d2c09-124">**Çözüm Gezgini'nde**Class1.vb dosyasına sağ tıklayın ve **Yeniden Adlandır'ı**tıklatın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="d2c09-125">Dosyayı enter `StreamReaderEnumerable.vb` tuşuna yeniden adlandırın ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="d2c09-126">Dosyayı yeniden adlandırma da sınıfı `StreamReaderEnumerable`' nun adını .'a yeniden adla taslar</span><span class="sxs-lookup"><span data-stu-id="d2c09-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="d2c09-127">Bu sınıf `IEnumerable(Of String)` arabirimi uygular.</span><span class="sxs-lookup"><span data-stu-id="d2c09-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="d2c09-128">StreamReaderEnumerable projesini sağ tıklatın, **Ekle'ye**işaret edin ve ardından **Yeni Öğe'yi**tıklatın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="d2c09-129">**Sınıf** şablonu'nu seçin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-129">Select the **Class** template.</span></span> <span data-ttu-id="d2c09-130">**Ad** kutusuna Tamam `StreamReaderEnumerator.vb` yazın ve **tıklatın.**</span><span class="sxs-lookup"><span data-stu-id="d2c09-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="d2c09-131">Bu projede birinci sınıf sayısal sınıf ve `IEnumerable(Of String)` arayüzü uygulayacak.</span><span class="sxs-lookup"><span data-stu-id="d2c09-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d2c09-132">Bu genel arabirim <xref:System.Collections.IEnumerable> arabirimi uygular ve bu sınıfın tüketicilerinin `String`.</span><span class="sxs-lookup"><span data-stu-id="d2c09-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="d2c09-133">**Tümesiyatize uygulamak için kodu ekleyin**</span><span class="sxs-lookup"><span data-stu-id="d2c09-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="d2c09-134">StreamReaderEnumerable.vb dosyasını açın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="d2c09-135">Satırda sonra, `Public Class StreamReaderEnumerable`aşağıdaki leri yazın ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="d2c09-136">Visual Basic, sınıfı `IEnumerable(Of String)` arabirim tarafından gerekli olan üyelerle otomatik olarak doldurur.</span><span class="sxs-lookup"><span data-stu-id="d2c09-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="d2c09-137">Bu sayısal sınıf, bir metin dosyasındaki satırları bir defada bir satır okur.</span><span class="sxs-lookup"><span data-stu-id="d2c09-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="d2c09-138">Dosya yolunu giriş parametresi olarak alan bir ortak oluşturucuyu ortaya çıkarmak için sınıfa aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="d2c09-139">Arabirim <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> yöntemini uygulamanız sınıfın yeni bir `StreamReaderEnumerator` örneğini döndürecektir. `IEnumerable(Of String)`</span><span class="sxs-lookup"><span data-stu-id="d2c09-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="d2c09-140">Arabirimin `GetEnumerator` yönteminin uygulanması `Private`yapılabilir, çünkü `IEnumerable(Of String)` yalnızca arabirimin üyelerini ortaya çıkarmanız gerekir. `IEnumerable`</span><span class="sxs-lookup"><span data-stu-id="d2c09-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="d2c09-141">Visual Basic'in `GetEnumerator` yöntemler için oluşturduğu kodu aşağıdaki kodla değiştirin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="d2c09-142">**IEnumerator uygulamak için kodu ekleyin**</span><span class="sxs-lookup"><span data-stu-id="d2c09-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="d2c09-143">StreamReaderEnumerator.vb dosyasını açın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="d2c09-144">Satırda sonra, `Public Class StreamReaderEnumerator`aşağıdaki leri yazın ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="d2c09-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="d2c09-145">Visual Basic, sınıfı `IEnumerator(Of String)` arabirim tarafından gerekli olan üyelerle otomatik olarak doldurur.</span><span class="sxs-lookup"><span data-stu-id="d2c09-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="d2c09-146">Enumerator sınıfı metin dosyasını açar ve dosyadaki satırları okumak için G/Ç dosyasını gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="d2c09-147">Bir dosya yolunu giriş parametresi olarak alan bir ortak oluşturucuyu ortaya çıkarmak ve metin dosyasını okumak için açmak için sınıfa aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="d2c09-148">Hem `Current` arabirimlerin `IEnumerator(Of String)` hem `IEnumerator` de arabirimlerin özellikleri, metin dosyasından geçerli öğeyi bir `String`.</span><span class="sxs-lookup"><span data-stu-id="d2c09-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="d2c09-149">Arabirimin `Current` özelliğinin uygulanması `Private`yapılabilir, çünkü `IEnumerator(Of String)` yalnızca arabirimin üyelerini ortaya çıkarmanız gerekir. `IEnumerator`</span><span class="sxs-lookup"><span data-stu-id="d2c09-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="d2c09-150">Visual Basic'in `Current` özellikler için oluşturduğu kodu aşağıdaki kodla değiştirin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="d2c09-151">Arabirimin `MoveNext` yöntemi metin dosyasındaki bir sonraki öğeye doğru ilerler ve `Current` özellik tarafından döndürülen değeri güncelleştirir. `IEnumerator`</span><span class="sxs-lookup"><span data-stu-id="d2c09-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="d2c09-152">Okunacak başka öğe yoksa, `MoveNext` yöntem `False`döndürür; aksi `MoveNext` takdirde `True`yöntem döndürür.</span><span class="sxs-lookup"><span data-stu-id="d2c09-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="d2c09-153">`MoveNext` yöntemine aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="d2c09-154">`IEnumerator` Arabirimin `Reset` yöntemi, yinelemeyi metin dosyasının başlangıcına işaret etmeye yönlendirir ve geçerli madde değerini temizler.</span><span class="sxs-lookup"><span data-stu-id="d2c09-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="d2c09-155">`Reset` yöntemine aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="d2c09-156">`IEnumerator` Arabirimin yöntemi, `Dispose` yineleyici yok edilmeden önce tüm yönetilmeyen kaynakların serbest bırakılmasını garanti eder.</span><span class="sxs-lookup"><span data-stu-id="d2c09-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="d2c09-157">`StreamReader` Nesne tarafından kullanılan dosya işleyicisi yönetilmeyen bir kaynaktır ve yineleyici örneği yok edilmeden önce kapatılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="d2c09-158">Visual Basic'in `Dispose` yöntem için oluşturduğu kodu aşağıdaki kodla değiştirin.</span><span class="sxs-lookup"><span data-stu-id="d2c09-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="d2c09-159">Örnek Yinelemeyi Kullanma</span><span class="sxs-lookup"><span data-stu-id="d2c09-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="d2c09-160">Döngü veya LINQ sorgusu gibi `IEnumerable` `For Next` bir nesne gerektiren denetim yapıları ile birlikte kodunuzda sayısalsınıf kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d2c09-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="d2c09-161">Aşağıdaki `StreamReaderEnumerable` örnekte BIR LINQ sorgusunda gösterir.</span><span class="sxs-lookup"><span data-stu-id="d2c09-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="d2c09-162">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="d2c09-162">See also</span></span>

- [<span data-ttu-id="d2c09-163">Visual Basic'de LINQ'e Giriş</span><span class="sxs-lookup"><span data-stu-id="d2c09-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="d2c09-164">Kontrol Akışı</span><span class="sxs-lookup"><span data-stu-id="d2c09-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="d2c09-165">Döngü Yapıları</span><span class="sxs-lookup"><span data-stu-id="d2c09-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="d2c09-166">For Each...Next Deyimi</span><span class="sxs-lookup"><span data-stu-id="d2c09-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
