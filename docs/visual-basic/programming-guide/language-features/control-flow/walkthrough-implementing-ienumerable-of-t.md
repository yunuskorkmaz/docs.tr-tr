---
title: Visual Basic'de IEnumerable uygulama
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: be2eefdc52d38df3071d457b7a71dbac6eaa2657
ms.sourcegitcommit: 76a304c79a32aa13889ebcf4b9789a4542b48e3e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/13/2018
ms.locfileid: "45526856"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="b9dd2-102">İzlenecek yol: Visual Basic'de IEnumerable(Of T) Uygulama</span><span class="sxs-lookup"><span data-stu-id="b9dd2-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="b9dd2-103"><xref:System.Collections.Generic.IEnumerable%601> Arabirimi, aynı anda bir öğe değerlerini bir dizi döndüren sınıflar tarafından uygulanır.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="b9dd2-104">Aynı anda bir öğe ile çalışmak için eksiksiz veri Seti yüklemek gerekmez veri döndüren avantajı.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="b9dd2-105">Yalnızca tek bir öğe verileri yüklemek için yeterli bellek kullanmak zorunda.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="b9dd2-106">Uygulayan sınıflar `IEnumerable(T)` arabirimi ile kullanılabilir `For Each` döngüler veya LINQ sorguları.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="b9dd2-107">Örneğin, büyük bir metin dosyası okuma ve her satırın belirli arama ölçütleri ile eşleşen dosyasından dönüş bir uygulamayı düşünün.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="b9dd2-108">Uygulama, belirtilen ölçütlerle eşleşen dosyanın satırlar döndürülecek LINQ sorgusu kullanır.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="b9dd2-109">LINQ sorgusu kullanarak dosya içeriğini sorgulamak için uygulama dosyasının içeriğini bir dizi veya koleksiyon yükleyebilir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="b9dd2-110">Ancak, bir dizi veya koleksiyon tüm dosya yüklenirken gerekenden çok daha fazla bellek kullanılmasına neden olur.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="b9dd2-111">LINQ sorgusu, bunun yerine arama ölçütleriyle eşleşen değerler döndüren bir numaralandırılabilir sınıfını kullanarak dosya içeriklerini sorgulayabilir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="b9dd2-112">Yalnızca birkaç döndüren sorgular eşleşen değerler çok daha az bellek tüketen.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="b9dd2-113">Uygulayan bir sınıf oluşturabilirsiniz <xref:System.Collections.Generic.IEnumerable%601> kaynak verileri numaralandırılabilir veri olarak kullanıma sunmak için arabirim.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="b9dd2-114">Sınıfınızın uyguladığı `IEnumerable(T)` arabirimi uygulayan başka bir sınıf gerektiren <xref:System.Collections.Generic.IEnumerator%601> kaynak veri içerisinde yineleme yapmak için arabirim.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="b9dd2-115">Bu iki sınıf veri öğelerini belirli bir tür olarak sıralı olarak döndürülecek etkinleştirin.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="b9dd2-116">Bu kılavuzda, uygulayan bir sınıf oluşturur `IEnumerable(Of String)` arabirimi ve uygulayan bir sınıf `IEnumerator(Of String)` bir metin dosyası bir satırı okumak için arabirim.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="b9dd2-117">Numaralandırılabilir sınıfı oluşturma</span><span class="sxs-lookup"><span data-stu-id="b9dd2-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="b9dd2-118">**Numaralandırılabilir sınıf projesi oluşturma**</span><span class="sxs-lookup"><span data-stu-id="b9dd2-118">**Create the enumerable class project**</span></span>

1.  <span data-ttu-id="b9dd2-119">Visual Basic'te üzerinde **dosya** menüsünde **yeni** ve ardından **proje**.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1.  <span data-ttu-id="b9dd2-120">İçinde **yeni proje** iletişim kutusundaki **proje türleri** bölmesinde emin olun **Windows** seçilir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="b9dd2-121">Seçin **sınıf kitaplığı** içinde **şablonları** bölmesi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="b9dd2-122">İçinde **adı** kutusuna `StreamReaderEnumerable`ve ardından **Tamam**.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="b9dd2-123">Yeni Proje görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-123">The new project is displayed.</span></span>

1.  <span data-ttu-id="b9dd2-124">İçinde **Çözüm Gezgini**Class1.vb dosyaya sağ tıklayın ve tıklayın **Yeniden Adlandır**.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="b9dd2-125">Dosyayı Yeniden Adlandır `StreamReaderEnumerable.vb` ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="b9dd2-126">Dosya yeniden adlandırılırken da yeniden adlandırmak sınıfa `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="b9dd2-127">Bu sınıf uygulayacak `IEnumerable(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1.  <span data-ttu-id="b9dd2-128">StreamReaderEnumerable projeye sağ tıklayın, fareyle **Ekle**ve ardından **yeni öğe**.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="b9dd2-129">Seçin **sınıfı** şablonu.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-129">Select the **Class** template.</span></span> <span data-ttu-id="b9dd2-130">İçinde **adı** kutusuna `StreamReaderEnumerator.vb` tıklatıp **Tamam**.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="b9dd2-131">Bu projedeki ilk sınıf numaralandırılabilir sınıftır ve uygulayacak `IEnumerable(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="b9dd2-132">Bu genel arabirimi uygulayan <xref:System.Collections.IEnumerable> arabirimi ve bu sınıf tüketicilerinin olarak yazılan değerleri erişebilirsiniz garantiler `String`.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="b9dd2-133">**IEnumerable uygulamak için kod ekleyin**</span><span class="sxs-lookup"><span data-stu-id="b9dd2-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="b9dd2-134">StreamReaderEnumerable.vb dosyasını açın.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="b9dd2-135">Sonra satırında `Public Class StreamReaderEnumerable`, aşağıdaki komutu yazın ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]

   <span data-ttu-id="b9dd2-136">Visual Basic sınıf tarafından gerekli üyeleri ile otomatik olarak doldurur `IEnumerable(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="b9dd2-137">Numaralandırılabilir bu sınıfı aynı anda bir metin dosyası bir satırından satırları oku.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="b9dd2-138">Bir dosya yolu giriş parametresi olarak alan bir Genel oluşturucu kullanıma sunmak için sınıfa aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]

4. <span data-ttu-id="b9dd2-139">Uygulamanıza <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> yöntemi `IEnumerable(Of String)` arabirimi yeni bir örneğini döndürür `StreamReaderEnumerator` sınıfı.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="b9dd2-140">Uygulamasını `GetEnumerator` yöntemi `IEnumerable` arabirimi yapılabilir `Private`yalnızca üyelerinin kullanıma sunmak sahip olduğunuz için `IEnumerable(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="b9dd2-141">Visual Basic için oluşturulan kodu değiştirme `GetEnumerator` aşağıdaki kodla yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]  
  
<span data-ttu-id="b9dd2-142">**IEnumerator uygulamak için kod ekleyin**</span><span class="sxs-lookup"><span data-stu-id="b9dd2-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="b9dd2-143">StreamReaderEnumerator.vb dosyasını açın.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="b9dd2-144">Sonra satırında `Public Class StreamReaderEnumerator`, aşağıdaki komutu yazın ve ENTER tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]

   <span data-ttu-id="b9dd2-145">Visual Basic sınıf tarafından gerekli üyeleri ile otomatik olarak doldurur `IEnumerator(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="b9dd2-146">Numaralandırıcı sınıfı metin dosyasını açıp ' % s'dosya g/ç satırları dosyasından okumak için gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="b9dd2-147">Bir dosya yolu giriş parametresi olarak alan bir Genel oluşturucu kullanımına sunun ve metin dosyası okuma için sınıfa aşağıdaki kodu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]

4. <span data-ttu-id="b9dd2-148">`Current` Özellikleri hem de `IEnumerator(Of String)` ve `IEnumerator` arabirimleri metin dosyasından geçerli öğeyi iade bir `String`.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="b9dd2-149">Uygulamasını `Current` özelliği `IEnumerator` arabirimi yapılabilir `Private`yalnızca üyelerinin kullanıma sunmak sahip olduğunuz için `IEnumerator(Of String)` arabirimi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="b9dd2-150">Visual Basic için oluşturulan kodu değiştirme `Current` aşağıdaki kodla özellikleri.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]

5. <span data-ttu-id="b9dd2-151">`MoveNext` Yöntemi `IEnumerator` arabirimi metin dosyasındaki bir sonraki öğeye gider ve tarafından döndürülen değerin güncelleştirmeleri `Current` özelliği.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="b9dd2-152">Okumak için daha fazla öğe varsa `MoveNext` yöntemi döndürür `False`; Aksi takdirde `MoveNext` yöntemi döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="b9dd2-153">Aşağıdaki kodu ekleyin `MoveNext` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-153">Add the following code to the `MoveNext` method.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]

6. <span data-ttu-id="b9dd2-154">`Reset` Yöntemi `IEnumerator` arabirimi metin dosyası başlangıcına işaret edecek şekilde yineleyici yönlendirir ve geçerli öğe değerini temizler.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="b9dd2-155">Aşağıdaki kodu ekleyin `Reset` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-155">Add the following code to the `Reset` method.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]

7. <span data-ttu-id="b9dd2-156">`Dispose` Yöntemi `IEnumerator` arabirimi garanti yineleyici edildiğinde önce tüm yönetilmeyen kaynaklar serbest bırakılır.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="b9dd2-157">Tarafından kullanılan dosya tanıtıcısı `StreamReader` nesne yönetilmeyen bir kaynağı ve yineleyici örneği yok önce kapatılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="b9dd2-158">Visual Basic için oluşturulan kodu değiştirme `Dispose` yöntemini aşağıdaki kod ile.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

   [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="b9dd2-159">Örnek yineleyici kullanma</span><span class="sxs-lookup"><span data-stu-id="b9dd2-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="b9dd2-160">Numaralandırılabilir bir sınıf uygulayan bir nesne gerektiren denetim yapıları birlikte kodunuzda kullanabileceğiniz `IEnumerable`, gibi bir `For Next` döngü veya LINQ sorgusu.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="b9dd2-161">Aşağıdaki örnekte gösterildiği `StreamReaderEnumerable` bir LINQ Sorgu.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="b9dd2-162">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="b9dd2-162">See Also</span></span>  
 [<span data-ttu-id="b9dd2-163">Visual Basic'de LINQ'e giriş</span><span class="sxs-lookup"><span data-stu-id="b9dd2-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="b9dd2-164">Denetim Akışı</span><span class="sxs-lookup"><span data-stu-id="b9dd2-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="b9dd2-165">Döngü Yapıları</span><span class="sxs-lookup"><span data-stu-id="b9dd2-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="b9dd2-166">For Each...Next Deyimi</span><span class="sxs-lookup"><span data-stu-id="b9dd2-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
