---
title: Olayları İşleme
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: cb42f2e41e366cf8765cb9395d1a5641434bab40
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345078"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="a55b1-102">İzlenecek yol: Olayları İşleme (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="a55b1-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="a55b1-103">Bu, etkinliklerle nasıl çalışabileceğini gösteren iki konunun ikinci konudır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="a55b1-104">[Izlenecek yol: Izlenecek yol: olayları bildirme ve](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)oluşturma, olayların nasıl bildirilemeyeceğini ve tetikleyeceğinizi gösterir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="a55b1-105">Bu bölüm, bu kılavuzda yer alan formu ve sınıfı kullanır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="a55b1-106">`Widget` sınıfı örneği geleneksel olay işleme deyimlerini kullanır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="a55b1-107">Visual Basic olaylarla çalışmaya yönelik başka teknikler de sağlar.</span><span class="sxs-lookup"><span data-stu-id="a55b1-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="a55b1-108">Bir alıştırma olarak, `AddHandler` ve `Handles` deyimlerini kullanmak için bu örneği değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="a55b1-109">Pencere öğesi sınıfının PercentDone olayını işlemek için</span><span class="sxs-lookup"><span data-stu-id="a55b1-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="a55b1-110">Aşağıdaki kodu `Form1`yerleştirin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="a55b1-111">`WithEvents` anahtar sözcüğü, `mWidget` değişkeninin bir nesnenin olaylarını işlemek için kullanıldığını belirtir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="a55b1-112">Nesnenin türünü, nesnenin oluşturulacağı sınıfın adını sağlayarak belirtirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="a55b1-113">`WithEvents` değişkenlerinin sınıf düzeyi olması gerektiğinden `mWidget` değişkeni `Form1` olarak bildirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="a55b1-114">Bu, yerleştirdiğiniz sınıf türünden bağımsız olarak geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="a55b1-115">`mblnCancel` değişkeni `LongTask` metodunu iptal etmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="a55b1-116">Bir olayı Işlemek için kod yazma</span><span class="sxs-lookup"><span data-stu-id="a55b1-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="a55b1-117">`WithEvents`kullanarak bir değişken bildirdikten hemen sonra, sınıfın **kod düzenleyicisinin**sol açılır listesinde değişken adı belirir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="a55b1-118">`mWidget`' yi seçtiğinizde, `Widget` sınıfının olayları sağ açılan listede görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="a55b1-119">Bir olay seçildiğinde ilgili olay yordamı, önek `mWidget` ve alt çizgiyle birlikte görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="a55b1-120">Bir `WithEvents` değişkeniyle ilişkili tüm olay yordamlarına önek olarak değişken adı verilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="a55b1-121">Bir olayı işlemek için</span><span class="sxs-lookup"><span data-stu-id="a55b1-121">To handle an event</span></span>  
  
1. <span data-ttu-id="a55b1-122">**Kod düzenleyicisinde**sol aşağı açılan listeden `mWidget` ' yi seçin.</span><span class="sxs-lookup"><span data-stu-id="a55b1-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="a55b1-123">Sağ açılan listeden `PercentDone` olayı ' nı seçin.</span><span class="sxs-lookup"><span data-stu-id="a55b1-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="a55b1-124">**Kod düzenleyicisi** `mWidget_PercentDone` olay yordamını açar.</span><span class="sxs-lookup"><span data-stu-id="a55b1-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="a55b1-125">**Kod Düzenleyicisi** , yeni olay işleyicileri eklemek için yararlıdır, ancak gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="a55b1-126">Bu kılavuzda, yalnızca olay işleyicilerini doğrudan kodunuza kopyalamak daha doğrudan bir örnek olur.</span><span class="sxs-lookup"><span data-stu-id="a55b1-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="a55b1-127">`mWidget_PercentDone` olay işleyicisine aşağıdaki kodu ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="a55b1-128">`PercentDone` olayı her oluşturulduğunda, olay yordamı bir `Label` denetimindeki tamamlanma yüzdesini gösterir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="a55b1-129">`DoEvents` yöntemi etiketin yeniden çizileceğinin yanı sıra kullanıcıya **iptal** düğmesine tıklama fırsatı verir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="a55b1-130">`Button2_Click` olay işleyicisi için aşağıdaki kodu ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="a55b1-131">`LongTask` çalışırken Kullanıcı **iptal** düğmesine tıkladığında, `DoEvents` deyimin olay işlemenin oluşmasına izin verdiği anda `Button2_Click` olay yürütülür.</span><span class="sxs-lookup"><span data-stu-id="a55b1-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="a55b1-132">Sınıf düzeyi değişken `mblnCancel` `True`olarak ayarlanır ve `mWidget_PercentDone` olayı bunu sınar ve `ByRef Cancel` bağımsız değişkenini `True`olarak ayarlar.</span><span class="sxs-lookup"><span data-stu-id="a55b1-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="a55b1-133">Bir WithEvents değişkenini bir nesneye bağlama</span><span class="sxs-lookup"><span data-stu-id="a55b1-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="a55b1-134">`Form1` artık `Widget` nesnenin olaylarını işleyecek şekilde ayarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="a55b1-135">Her şey bir yerde `Widget` bulmaktadır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="a55b1-136">Tasarım zamanında `WithEvents` bir değişken bildirdiğinizde, onunla ilişkili bir nesne yoktur.</span><span class="sxs-lookup"><span data-stu-id="a55b1-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="a55b1-137">`WithEvents` değişken diğer herhangi bir nesne değişkeni gibidir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="a55b1-138">Bir nesne oluşturmanız ve `WithEvents` değişkeniyle buna bir başvuru atamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="a55b1-139">Bir nesne oluşturmak ve buna bir başvuru atamak için</span><span class="sxs-lookup"><span data-stu-id="a55b1-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="a55b1-140">**Kod düzenleyicisinde**sol aşağı açılan listeden **(Form1 olayları)** öğesini seçin.</span><span class="sxs-lookup"><span data-stu-id="a55b1-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="a55b1-141">Sağ açılan listeden `Load` olayı ' nı seçin.</span><span class="sxs-lookup"><span data-stu-id="a55b1-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="a55b1-142">**Kod düzenleyicisi** `Form1_Load` olay yordamını açar.</span><span class="sxs-lookup"><span data-stu-id="a55b1-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="a55b1-143">`Widget`oluşturmak için `Form1_Load` olay yordamı için aşağıdaki kodu ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="a55b1-144">Bu kod yürütüldüğünde, Visual Basic bir `Widget` nesnesi oluşturur ve olaylarını `mWidget`ilişkili olay yordamlarına bağlar.</span><span class="sxs-lookup"><span data-stu-id="a55b1-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="a55b1-145">Bu noktadan itibaren, `Widget` `PercentDone` olayını her yükseldiğinde `mWidget_PercentDone` olay yordamı yürütülür.</span><span class="sxs-lookup"><span data-stu-id="a55b1-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="a55b1-146">LongTask yöntemini çağırmak için</span><span class="sxs-lookup"><span data-stu-id="a55b1-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="a55b1-147">`Button1_Click` olay işleyicisine aşağıdaki kodu ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="a55b1-148">`LongTask` yöntemi çağrılmadan önce, tamamlanma yüzdesini gösteren etiketin başlatılması ve yöntemin iptal edilmesi için sınıf düzeyi `Boolean` bayrağının `False`olarak ayarlanması gerekir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="a55b1-149">`LongTask`, 12,2 saniyelik bir görev süresiyle çağrılır.</span><span class="sxs-lookup"><span data-stu-id="a55b1-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="a55b1-150">`PercentDone` olay, saniyenin her biri üçte bir kez tetiklenir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="a55b1-151">Olay her oluşturulduğunda `mWidget_PercentDone` olay yordamı yürütülür.</span><span class="sxs-lookup"><span data-stu-id="a55b1-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="a55b1-152">`LongTask` tamamlandığında, `LongTask` normal şekilde sonlandırıldığı veya `mblnCancel` `True`olarak ayarlandığı için `mblnCancel` test edilmiştir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="a55b1-153">Tamamlanma yüzdesi yalnızca önceki durumda güncelleştirilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="a55b1-154">Programı çalıştırmak için</span><span class="sxs-lookup"><span data-stu-id="a55b1-154">To run the program</span></span>  
  
1. <span data-ttu-id="a55b1-155">Projeyi çalışma moduna almak için F5 tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="a55b1-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="a55b1-156">**Görevi Başlat** düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="a55b1-156">Click the **Start Task** button.</span></span> <span data-ttu-id="a55b1-157">`PercentDone` olayı her oluşturulduğunda etiket, tamamlanan görevin yüzdesine göre güncelleştirilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="a55b1-158">Görevi durdurmak için **iptal** düğmesine tıklayın.</span><span class="sxs-lookup"><span data-stu-id="a55b1-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="a55b1-159">**İptal** düğmesinin görünümü tıkladığınızda hemen değişmediğine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="a55b1-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="a55b1-160">`My.Application.DoEvents` deyimin olay işlemeye izin verdiğinden `Click` olayı gerçekleşmeyecek.</span><span class="sxs-lookup"><span data-stu-id="a55b1-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="a55b1-161">`My.Application.DoEvents` yöntemi, olayları yalnızca formla aynı şekilde işlemez.</span><span class="sxs-lookup"><span data-stu-id="a55b1-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="a55b1-162">Örneğin, bu kılavuzda, **iptal** düğmesine iki kez tıklamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="a55b1-163">Formun olayları doğrudan işlemesine izin vermek için çoklu iş parçacığı kullanımı ' nı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="a55b1-164">Daha fazla bilgi için bkz. [yönetilen Iş parçacığı](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="a55b1-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="a55b1-165">Programı F11 ile çalıştırmaya ve bir kerede bir satırda bir satır adım adım ilerlebileceğinizi fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="a55b1-166">Yürütmenin `LongTask`nasıl girdiğini açıkça görebilir ve `PercentDone` olayı her oluşturulduğunda `Form1` kısa bir süre sonra yeniden girebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="a55b1-167">Yürütme `Form1`koduna geri alındığı sürece ne olur? `LongTask` yöntemi yeniden çağrıldı mı?</span><span class="sxs-lookup"><span data-stu-id="a55b1-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="a55b1-168">En kötü, olay her başlatıldığında `LongTask` çağrılırsa bir yığın taşması meydana gelebilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="a55b1-169">Yeni `Widget` `mWidget`için bir başvuru atayarak, değişken `mWidget` farklı bir `Widget` nesnesinin olaylarını işlemesini sağlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="a55b1-170">Aslında, düğmesine her tıkladığınızda kodu `Button1_Click` yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="a55b1-171">Farklı bir pencere öğesinin olaylarını işlemek için</span><span class="sxs-lookup"><span data-stu-id="a55b1-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="a55b1-172">Aşağıdaki kod satırını, `mWidget.LongTask(12.2, 0.33)`okuyan satırdan hemen önce `Button1_Click` yordama ekleyin:</span><span class="sxs-lookup"><span data-stu-id="a55b1-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="a55b1-173">Yukarıdaki kod, düğme tıklandığında yeni bir `Widget` oluşturur.</span><span class="sxs-lookup"><span data-stu-id="a55b1-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="a55b1-174">`LongTask` yöntemi tamamlandıktan hemen sonra, `Widget` başvurusu serbest bırakılır ve `Widget` yok edilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="a55b1-175">Bir `WithEvents` değişken aynı anda yalnızca bir nesne başvurusu içerebilir, bu nedenle `mWidget`farklı bir `Widget` nesnesi atarsanız, önceki `Widget` nesnesinin olayları artık işlenmeyecek.</span><span class="sxs-lookup"><span data-stu-id="a55b1-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="a55b1-176">`mWidget` eski `Widget`bir başvuru içeren tek nesne değişkense, nesne yok edilir.</span><span class="sxs-lookup"><span data-stu-id="a55b1-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="a55b1-177">Birkaç `Widget` nesnesinden olayları işlemek istiyorsanız, her bir nesneden olayları ayrı olarak işlemek için `AddHandler` ifadesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="a55b1-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="a55b1-178">İhtiyaç duyduğunuz sayıda `WithEvents` değişken bildirebilirsiniz, ancak `WithEvents` değişkenlerin dizileri desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="a55b1-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a55b1-179">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="a55b1-179">See also</span></span>

- [<span data-ttu-id="a55b1-180">İzlenecek yol: Olay Bildirme ve Oluşturma</span><span class="sxs-lookup"><span data-stu-id="a55b1-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="a55b1-181">Olaylar</span><span class="sxs-lookup"><span data-stu-id="a55b1-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
