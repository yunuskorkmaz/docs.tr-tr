---
title: Olaylarını işleme (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: aa3c1f00922cda46d3ce5a788b606a6f5da52636
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/28/2019
ms.locfileid: "64604036"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="9d4d4-102">İzlenecek yol: Olaylarını işleme (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="9d4d4-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="9d4d4-103">Olaylar ile çalışmaya nasıl gösteren iki konuları saniyedir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="9d4d4-104">İlk konu [izlenecek yol: Olayları bildirmek ve yükseltmeyi](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), bildirmek ve olaylarını gösterir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="9d4d4-105">Bu bölümde, bu izlenecek yol sınıf ve form bunlar gerçekleştiğinde olayların nasıl işleneceğini göstermek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="9d4d4-106">`Widget` Geleneksel olay işleme deyimleri sınıfı örneğini kullanır.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="9d4d4-107">Visual Basic, olayları ile çalışmaya yönelik diğer teknikleri sağlar.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="9d4d4-108">Bu örneği kullanmak için değiştirebileceğiniz bir alıştırma olarak `AddHandler` ve `Handles` deyimleri.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="9d4d4-109">Pencere öğesi sınıfının PercentDone olayı işlemek için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="9d4d4-110">Aşağıdaki kodda yerleştirin `Form1`:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="9d4d4-111">`WithEvents` Anahtar sözcüğü belirtir değişkeni `mWidget` nesnenin olayları işlemek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="9d4d4-112">Nesne türünün, nesnenin oluşturulacağı sınıfının adı sağlayarak belirtin.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="9d4d4-113">Değişken `mWidget` bölümünde bildirilen `Form1` çünkü `WithEvents` sınıf düzeyi değişkenleri olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="9d4d4-114">Bu, yerleştirebilirsiniz, sınıf türünden bağımsız olarak geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="9d4d4-115">Değişken `mblnCancel` iptal etmek için kullanılan `LongTask` yöntemi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="9d4d4-116">Bir olayı işlemek için kod yazma</span><span class="sxs-lookup"><span data-stu-id="9d4d4-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="9d4d4-117">Bir değişken kullanarak bildirdiğiniz hemen sonra `WithEvents`, değişken adı sınıfın sol aşağı açılan listede görünür **Kod Düzenleyicisi**.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="9d4d4-118">Seçtiğinizde, `mWidget`, `Widget` sınıfın olayları, sağda açılan listede görünür.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="9d4d4-119">Bir olay seçerek görüntüler önek ile ilgili olay yordamı `mWidget` ve alt çizgi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="9d4d4-120">İle ilişkili tüm olay yordamları bir `WithEvents` değişkeni, değişken adı bir önek olarak verilmiştir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="9d4d4-121">Bir olayı işlemek için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-121">To handle an event</span></span>  
  
1. <span data-ttu-id="9d4d4-122">Seçin `mWidget` sol aşağı açılan listeden **Kod Düzenleyicisi**.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="9d4d4-123">Seçin `PercentDone` doğru aşağı açılan listeden olay.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="9d4d4-124">**Kod Düzenleyicisi** açılır `mWidget_PercentDone` olay yordamı.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="9d4d4-125">**Kod Düzenleyicisi** kullanışlı, ancak gerekli değil, yeni olay işleyicileri ekleme.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="9d4d4-126">Bu kılavuzda, olay işleyicileri doğrudan kodunuza hemen kopyalayıp daha doğrudan.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="9d4d4-127">Aşağıdaki kodu ekleyin `mWidget_PercentDone` olay işleyicisi:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="9d4d4-128">Her `PercentDone` olayı, olay yordamı, tamamlanma yüzdesini görüntüler bir `Label` denetimi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="9d4d4-129">`DoEvents` Yöntemi çizilecek, etiket sağlar ve ayrıca kullanıcı tıklayın fırsatı sunar **iptal** düğmesi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="9d4d4-130">İçin aşağıdaki kodu ekleyin `Button2_Click` olay işleyicisi:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="9d4d4-131">Kullanıcı tıklarsa **iptal** düğmeye `LongTask` çalıştırıldığı `Button2_Click` olay yürütülür hemen sonra `DoEvents` deyimi gerçekleşmesi olay işleme sağlar.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="9d4d4-132">Sınıf düzeyi değişkenleri `mblnCancel` ayarlanır `True`ve `mWidget_PercentDone` olay ardından bunu test eder ve ayarlar `ByRef Cancel` bağımsız değişkeni `True`.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="9d4d4-133">Bir nesneye bir WithEvents değişkeni bağlanma</span><span class="sxs-lookup"><span data-stu-id="9d4d4-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="9d4d4-134">`Form1` artık işlemek üzere ayarlanmış bir `Widget` nesnenin olayları.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="9d4d4-135">Kalan tek şey bulmak için bir `Widget` yerde.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="9d4d4-136">Bir değişken bildirdiğinizde `WithEvents` tasarım zamanında, kendisiyle ilişkilendirilmiş nesne yok.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="9d4d4-137">A `WithEvents` başka herhangi nesne bir değişken gibi bir değişkendir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="9d4d4-138">Bir nesne oluşturun ve bunu bir başvuru atamak sahip olduğunuz `WithEvents` değişkeni.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="9d4d4-139">Bir nesne oluşturun ve ona bir başvuru atamak için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="9d4d4-140">Seçin **(Form1 olayları)** sol aşağı açılan listeden **Kod Düzenleyicisi**.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="9d4d4-141">Seçin `Load` doğru aşağı açılan listeden olay.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="9d4d4-142">**Kod Düzenleyicisi** açılır `Form1_Load` olay yordamı.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="9d4d4-143">İçin aşağıdaki kodu ekleyin `Form1_Load` oluşturmak için olay yordamı `Widget`:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="9d4d4-144">Bu kod yürütüldüğünde, Visual Basic oluşturur bir `Widget` nesnesini ve ilişkili olay yordamları olaylarına bağlanır `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="9d4d4-145">Buradan işaret, zaman `Widget` başlatır, `PercentDone` olay `mWidget_PercentDone` olay yordamı yürütülür.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="9d4d4-146">LongTask yöntemini çağırmak için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="9d4d4-147">Aşağıdaki kodu ekleyin `Button1_Click` olay işleyicisi:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="9d4d4-148">Önce `LongTask` yöntemi çağrıldığında, tamamlanma görüntüler başlatılmalıdır etiket ve sınıf düzeyi `Boolean` yöntemi iptal ayarlanmalıdır için bayrak `False`.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="9d4d4-149">`LongTask` görev süresi 12.2 saniye ile adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="9d4d4-150">`PercentDone` Olayı kez her üçte birinin bir saniye.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="9d4d4-151">Olayı oluşturulur, her zaman `mWidget_PercentDone` olay yordamı yürütülür.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="9d4d4-152">Zaman `LongTask` yapıldığında `mblnCancel` olmadığını görmek için test `LongTask` normalde, sona erdi veya nedeniyle durursa `mblnCancel` ayarlandı `True`.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="9d4d4-153">Tamamlanma yüzdesi, yalnızca önceki durumda güncelleştirilir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="9d4d4-154">Programı çalıştırmak için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-154">To run the program</span></span>  
  
1. <span data-ttu-id="9d4d4-155">Proje çalışma moduna için F5 tuşuna basın.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="9d4d4-156">Tıklayın **görevi Başlat** düğmesi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-156">Click the **Start Task** button.</span></span> <span data-ttu-id="9d4d4-157">Her zaman `PercentDone` olayı, etiketi tamamlandığında görev yüzdesi ile güncelleştirilir.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="9d4d4-158">Tıklayın **iptal** görevi Durdur düğmesini.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="9d4d4-159">Dikkat görünümünü **iptal** tıkladığınız zaman hemen düğmesi değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="9d4d4-160">`Click` Olamaz, olay ortaya kadar `My.Application.DoEvents` deyimi olay işleme sağlar.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="9d4d4-161">`My.Application.DoEvents` Yöntemi form gibi tam olarak aynı şekilde olayları işlemez.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="9d4d4-162">Örneğin, bu kılavuzda, tıklatmalısınız **iptal** düğmesini iki kez.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="9d4d4-163">Olayları doğrudan işlemeye izin vermek için kullanabileceğiniz çoklu iş parçacığı kullanımı.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="9d4d4-164">Daha fazla bilgi için [yönetilen iş parçacığı](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="9d4d4-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="9d4d4-165">F11 ile programı çalıştırın ve kodda bir satır aynı anda adım öğretici bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="9d4d4-166">NET bir şekilde nasıl yürütme aşamasına girer gördüğünüz `LongTask`ve sonra kısa bir süreliğine yeniden girer `Form1` her zaman `PercentDone` olayı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="9d4d4-167">Ne olacağını yürütme kod içinde geri ederken ise `Form1`, `LongTask` yöntemi yeniden çağırılırsa?</span><span class="sxs-lookup"><span data-stu-id="9d4d4-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="9d4d4-168">En kötü senaryoda, bir yığın taşması oluşabilir `LongTask` her olayın tetiklendiği zaman adı veriliyordu.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="9d4d4-169">Değişken neden olabilir `mWidget` için farklı bir olayları işlemek için `Widget` yeni başvuru atayarak nesne `Widget` için `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="9d4d4-170">Aslında, kodda yapabileceğiniz `Button1_Click` düğmesine her seferinde bunu.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="9d4d4-171">Farklı bir pencere öğesi için olayları işlemek için</span><span class="sxs-lookup"><span data-stu-id="9d4d4-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="9d4d4-172">Aşağıdaki kod satırını ekleyin `Button1_Click` yazan satırı hemen önceki yordamı `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="9d4d4-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="9d4d4-173">Yukarıdaki kod yeni bir oluşturur `Widget` düğmesine tıklandığında her zaman.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="9d4d4-174">Hemen sonra `LongTask` yöntemi tamamlandığında, başvuru `Widget` yayımlandığı ve `Widget` yok.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="9d4d4-175">A `WithEvents` değişkeni, bir kerede yalnızca bir nesne başvurusu içerebilir bunu farklı bir atarsanız `Widget` nesnesini `mWidget`, önceki `Widget` nesnenin olayları artık işleneceğini.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="9d4d4-176">Varsa `mWidget` içeren eski bir başvuru yalnızca nesne değişkeni `Widget`, nesne yok.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="9d4d4-177">Birkaç olayları işlemek isterseniz `Widget` nesneleri kullanan `AddHandler` her bir nesneden olayları ayrı ayrı işlemek için deyimi.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="9d4d4-178">Çok bildirebilirsiniz `WithEvents` yazarken değişkenleri gerekir, dizileri `WithEvents` değişkenleri desteklenmiyor.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9d4d4-179">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="9d4d4-179">See also</span></span>

- [<span data-ttu-id="9d4d4-180">İzlenecek yol: Olay bildirme ve oluşturma</span><span class="sxs-lookup"><span data-stu-id="9d4d4-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="9d4d4-181">Olaylar</span><span class="sxs-lookup"><span data-stu-id="9d4d4-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
