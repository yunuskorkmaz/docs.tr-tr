---
title: Diziler
ms.date: 12/06/2017
f1_keywords:
- vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
ms.openlocfilehash: 9dfe7814b00b4d060fa4ab9aa594faa948217d8d
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74351859"
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="d89f5-102">Visual Basic'de Diziler</span><span class="sxs-lookup"><span data-stu-id="d89f5-102">Arrays in Visual Basic</span></span>

<span data-ttu-id="d89f5-103">Dizi, mantıksal olarak birbirleriyle ilgili olan, adlandırılmış *öğeler*olan bir değerler kümesidir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="d89f5-104">Örneğin, bir dizi, bir dilbilgisi okulundaki her bir sınıfta bulunan öğrencilerin sayısından oluşabilir; dizideki her öğe, tek bir sınıfta bulunan öğrencilerin sayısıdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="d89f5-105">Benzer şekilde, bir dizi bir sınıfın bir öğrenciye ait olan bir sınıftan oluşabilir; dizideki her öğe tek bir sınıf olur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>

<span data-ttu-id="d89f5-106">Her bir veri öğesini depolamak için bağımsız değişkenler mümkündür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="d89f5-107">Örneğin, uygulamamız öğrenci 'yi çözümlerimizde, her öğrencinin derecesi için `englishGrade1`, `englishGrade2`vb. gibi ayrı bir değişken kullanabiliriz. Bu yaklaşım üç önemli sınırlamalara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="d89f5-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>

- <span data-ttu-id="d89f5-108">Tasarım zamanında, işlemek zorunda olduğumuz çok sayıda Not olduğunu bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="d89f5-109">Büyük sayıda denetimi hızla işlemek, çok daha hızlı hale gelir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="d89f5-110">Bu işlem, bir uygulamanın önemli hatalara karşı çok daha büyük bir hale gelmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="d89f5-111">Sürdürmek zordur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-111">It is difficult to maintain.</span></span> <span data-ttu-id="d89f5-112">Eklediğimiz her yeni sınıf, uygulamanın değiştirilmesini, yeniden derlendiğini ve yeniden dağıtılmasını gerektirir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>

<span data-ttu-id="d89f5-113">Bir dizi kullanarak, aynı ada sahip bu ilgili değerlere başvurabilir ve dizideki konumuna göre tek bir öğeyi tanımlamak için *Dizin* veya *alt simge* olarak adlandırılan bir sayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="d89f5-114">Bir dizinin dizinlerinden biri, dizideki öğelerin toplam sayısından küçük bir olan dizin aralığıdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="d89f5-115">Bir dizinin boyutunu tanımlamak için Visual Basic sözdizimini kullandığınızda, dizideki toplam öğe sayısını değil, en yüksek dizinini belirtirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="d89f5-116">Dizi ile bir birim olarak çalışabilirsiniz ve öğelerini tekrarlayabilme özelliği, tasarım zamanında kaç öğe içerdiğini tam olarak öğrenmenize gerek kalmadan sizi serbest bırakabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>

<span data-ttu-id="d89f5-117">Açıklama öncesi bazı hızlı örnekler:</span><span class="sxs-lookup"><span data-stu-id="d89f5-117">Some quick examples before explanation:</span></span>

```vb
' Declare a single-dimension array of 5 numbers.
Dim numbers(4) As Integer

' Declare a single-dimension array and set its 4 values.
Dim numbers = New Integer() {1, 2, 4, 8}

' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)

' Redefine the size of an existing array and reset the values.
ReDim numbers(15)

' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double

' Declare a 4 x 3 multidimensional array and set array element values.
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}

' Declare a jagged array
Dim sales()() As Double = New Double(11)() {}
```

## <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="d89f5-118">Basit bir dizideki dizi öğeleri</span><span class="sxs-lookup"><span data-stu-id="d89f5-118">Array elements in a simple array</span></span>

<span data-ttu-id="d89f5-119">Bir dilbilgisi okulundaki her bir sınıfta öğrenci sayısını depolamak için `students` adlı bir dizi oluşturalım.</span><span class="sxs-lookup"><span data-stu-id="d89f5-119">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="d89f5-120">Öğelerin dizinleri 0 ile 6 arasında değişir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-120">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="d89f5-121">Bu diziyi kullanmak yedi değişken bildirenden daha basittir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-121">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="d89f5-122">Aşağıdaki çizimde `students` dizisi gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-122">The following illustration shows the `students` array.</span></span> <span data-ttu-id="d89f5-123">Dizideki her öğe için:</span><span class="sxs-lookup"><span data-stu-id="d89f5-123">For each element of the array:</span></span>

- <span data-ttu-id="d89f5-124">Öğenin dizini, sınıfı temsil eder (Dizin 0, kindergaron öğesini temsil eder).</span><span class="sxs-lookup"><span data-stu-id="d89f5-124">The index of the element represents the grade (index 0 represents kindergarten).</span></span>

- <span data-ttu-id="d89f5-125">Öğesinde yer alan değer, bu sınıfta bulunan öğrencilerin sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-125">The value that’s contained in the element represents the number of students in that grade.</span></span>

![Öğrencilerin sayı dizisini gösteren diyagram](./media/index/students-array-elements.gif)

<span data-ttu-id="d89f5-127">Aşağıdaki örnek, diziyi oluşturan ve kullanan Visual Basic kodu içerir:</span><span class="sxs-lookup"><span data-stu-id="d89f5-127">The following example contains the Visual Basic code that creates and uses the array:</span></span>

[!code-vb[simple-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]

<span data-ttu-id="d89f5-128">Örnek üç şeyi yapar:</span><span class="sxs-lookup"><span data-stu-id="d89f5-128">The example does three things:</span></span>

- <span data-ttu-id="d89f5-129">Yedi öğe içeren bir `students` dizisi bildirir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-129">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="d89f5-130">Dizi bildiriminde `6` Sayı dizideki son dizini gösterir; dizideki öğe sayısından bir küçüktür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-130">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="d89f5-131">Dizideki her öğeye değerler atar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-131">It assigns values to each element in the array.</span></span> <span data-ttu-id="d89f5-132">Dizi öğelerine, dizi adı kullanılarak erişilir ve parantez içinde tek bir öğenin dizini de eklenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-132">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="d89f5-133">Dizinin her bir değerini listeler.</span><span class="sxs-lookup"><span data-stu-id="d89f5-133">It lists each value of the array.</span></span> <span data-ttu-id="d89f5-134">Örnek, dizinin her öğesine Dizin numarası ile erişmek için bir [`For`](../../../language-reference/statements/for-next-statement.md) ifadesini kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-134">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>

<span data-ttu-id="d89f5-135">Yukarıdaki örnekteki `students` dizisi tek boyutlu bir dizidir çünkü bir dizin kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-135">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="d89f5-136">Birden fazla dizin veya alt simge kullanan bir dizi *çok boyutlu*olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-136">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="d89f5-137">Daha fazla bilgi için bu makalenin geri kalanına ve [dizi boyutlarına Visual Basic](../../language-features/arrays/array-dimensions.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-137">For more information, see the rest of this article and [Array Dimensions in Visual Basic](../../language-features/arrays/array-dimensions.md).</span></span>

## <a name="creating-an-array"></a><span data-ttu-id="d89f5-138">Dizi oluşturma</span><span class="sxs-lookup"><span data-stu-id="d89f5-138">Creating an array</span></span>

<span data-ttu-id="d89f5-139">Bir dizinin boyutunu çeşitli yollarla tanımlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="d89f5-139">You can define the size of an array in several ways:</span></span>

- <span data-ttu-id="d89f5-140">Dizinin bildirildiği zaman boyutunu belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="d89f5-140">You can specify the size when the array is declared:</span></span>

  [!code-vb[creating1](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]

- <span data-ttu-id="d89f5-141">Bir dizinin boyutunu oluşturulduğu sırada sağlamak için bir `New` yan tümcesi kullanabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="d89f5-141">You can use a `New` clause to supply the size of an array when it’s created:</span></span>

  [!code-vb[creating2](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]

<span data-ttu-id="d89f5-142">Mevcut bir diziniz varsa, [`ReDim`](../../../language-reference/statements/redim-statement.md) ifadesini kullanarak boyutunu yeniden tanımlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-142">If you have an existing array, you can redefine its size by using the [`ReDim`](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="d89f5-143">`ReDim` deyimin dizide bulunan değerleri tutabilmeniz veya boş bir dizi oluşturmasını belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-143">You can specify that the `ReDim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="d89f5-144">Aşağıdaki örnek, var olan bir dizinin boyutunu değiştirmek için `ReDim` deyimin farklı kullanımlarını gösterir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-144">The following example shows different uses of the `ReDim` statement to modify the size of an existing array.</span></span>

[!code-vb[redimensioning](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]

<span data-ttu-id="d89f5-145">Daha fazla bilgi için [ReDim bildirimine](../../../language-reference/statements/redim-statement.md)bakın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-145">For more information, see the [ReDim Statement](../../../language-reference/statements/redim-statement.md).</span></span>

## <a name="storing-values-in-an-array"></a><span data-ttu-id="d89f5-146">Değerleri bir dizide depolama</span><span class="sxs-lookup"><span data-stu-id="d89f5-146">Storing values in an array</span></span>

<span data-ttu-id="d89f5-147">`Integer`türünde bir dizin kullanarak dizideki her konuma erişebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-147">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="d89f5-148">Parantez içine alınmış dizinini kullanarak her dizi konumuna başvurarak bir dizideki değerleri depolayıp alabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-148">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="d89f5-149">Çok boyutlu diziler için dizinler virgüllerle (,) ayrılır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-149">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="d89f5-150">Her dizi boyutu için bir dizine ihtiyacınız vardır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-150">You need one index for each array dimension.</span></span>

<span data-ttu-id="d89f5-151">Aşağıdaki örnek, dizilere değerler depolayan ve alan bazı deyimleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-151">The following example shows some statements that store and retrieve values in arrays.</span></span>

[!code-vb[store-and-retrieve](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]

## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="d89f5-152">Dizi değişmez değerleri ile bir diziyi doldurma</span><span class="sxs-lookup"><span data-stu-id="d89f5-152">Populating an array with array literals</span></span>

<span data-ttu-id="d89f5-153">Bir dizi değişmez değeri kullanarak, bir diziyi, oluşturduğunuz aynı anda bir ilk değer kümesiyle doldurabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-153">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="d89f5-154">Dizi sabit değeri, küme ayraçları (`{}`) içine alınmış bir virgülle ayrılmış değerler listesinden oluşur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-154">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>

<span data-ttu-id="d89f5-155">Dizi değişmez değeri kullanarak bir dizi oluşturduğunuzda, dizi türünü ve dizi türünü belirleyebilmek için tür çıkarımı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-155">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="d89f5-156">Aşağıdaki örnekte her iki seçenek de gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-156">The following example shows both options.</span></span>

[!code-vb[create-with-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]

<span data-ttu-id="d89f5-157">Tür çıkarımı kullandığınızda, dizi türü, değişmez değerler listesindeki *baskın tür* tarafından belirlenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-157">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="d89f5-158">Baskın tür, dizideki diğer tüm türlerin genişlebileceği türdür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-158">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="d89f5-159">Bu benzersiz tür belirlenemiyorsa, baskın tür, dizideki diğer tüm türlerin daraltabileceği benzersiz türdür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-159">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="d89f5-160">Bu benzersiz türlerden hiçbiri belirlenemiyorsa, baskın tür `Object`.</span><span class="sxs-lookup"><span data-stu-id="d89f5-160">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="d89f5-161">Örneğin, dizi değişmez değeri için sağlanan değer listesi `Integer`, `Long`ve `Double`türünde değerler içeriyorsa, sonuçta elde edilen dizi `Double`türündedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-161">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="d89f5-162">`Integer` ve `Long` yalnızca `Double`için genişletiğinden, baskın tür `Double`.</span><span class="sxs-lookup"><span data-stu-id="d89f5-162">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="d89f5-163">Daha fazla bilgi için bkz. [genişletme ve daraltma dönüştürmeleri](../../language-features/data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="d89f5-163">For more information, see [Widening and Narrowing Conversions](../../language-features/data-types/widening-and-narrowing-conversions.md).</span></span>

> [!NOTE]
> <span data-ttu-id="d89f5-164">Tür çıkarımı yalnızca bir tür üyesinde yerel değişkenler olarak tanımlanmış diziler için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-164">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="d89f5-165">Açık bir tür tanımı yoksa, sınıf düzeyinde dizi değişmez değerleri ile tanımlanan diziler `Object[]`türündedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-165">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="d89f5-166">Daha fazla bilgi için bkz. [Yerel tür çıkarımı](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="d89f5-166">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span>

<span data-ttu-id="d89f5-167">Önceki örnekte, dizi değişmez değerlerinin `Integer`türünde olmasına rağmen `Double` türünde bir dizi olarak `values` tanımladığına unutmayın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-167">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="d89f5-168">Bu diziyi, dizi sabit değerindeki değerler `Double` değerlere genişlebildiğinden oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-168">You can create this array because the values in the array literal can widen to `Double` values.</span></span>

<span data-ttu-id="d89f5-169">Ayrıca, *iç içe dizi değişmez*değerlerini kullanarak çok boyutlu bir dizi oluşturup doldurabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-169">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="d89f5-170">İç içe geçmiş dizi değişmez değerleri, sonuçta elde edilen dizi ile tutarlı olan sayıda boyutlara sahip olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-170">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="d89f5-171">Aşağıdaki örnek, iç içe dizi değişmez değerlerini kullanarak iki boyutlu tamsayılar dizisi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-171">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>

[!code-vb[nested-array-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]

<span data-ttu-id="d89f5-172">Bir diziyi oluşturmak ve doldurmak için iç içe geçmiş dizi değişmez değerleri kullanılırken, iç içe dizi değişmez değerlerinde öğe sayısı eşleşmezse bir hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-172">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="d89f5-173">Dizi değişkenini açıkça dizi değişmezinden farklı sayıda boyuta sahip olacak şekilde bildirirseniz bir hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-173">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span>

<span data-ttu-id="d89f5-174">Tek boyutlu diziler için de olduğu gibi, iç içe geçmiş dizi değişmez değerleri içeren çok boyutlu bir dizi oluştururken tür çıkarımı ' nı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-174">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="d89f5-175">Çıkarsanan tür, tüm iç içe geçmiş düzeyi için tüm dizi değişmez değerlerinde bulunan tüm değerlerin baskın türüdür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-175">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="d89f5-176">Aşağıdaki örnek, `Integer` ve `Double`türündeki değerlerden `Double[,]` türünde iki boyutlu bir dizi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-176">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>

[!code-vb[nested-type-inference](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]

<span data-ttu-id="d89f5-177">Daha fazla örnek için bkz. [nasıl yapılır: Visual Basic dizi değişkenini başlatma](../../language-features/arrays/how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="d89f5-177">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](../../language-features/arrays/how-to-initialize-an-array-variable.md).</span></span>

## <a name="iterating-through-an-array"></a><span data-ttu-id="d89f5-178">Bir dizi boyunca yineleme yapma</span><span class="sxs-lookup"><span data-stu-id="d89f5-178">Iterating through an array</span></span>

<span data-ttu-id="d89f5-179">Bir dizi boyunca yineleme yaparken, dizideki her öğeye en düşük dizinden en yüksek veya en düşük değerden en düşüğe erişirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-179">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="d89f5-180">Genellikle, [için kullanın... Sonraki bildiri](../../../language-reference/statements/for-next-statement.md) veya [for each... ](../../../language-reference/statements/for-each-next-statement.md)Bir dizinin öğeleri boyunca yinelemek için sonraki ifade.</span><span class="sxs-lookup"><span data-stu-id="d89f5-180">Typically, use either the [For...Next Statement](../../../language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="d89f5-181">Dizinin üst sınırlarını bilmiyorsanız, dizinin en yüksek değerini almak için <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> yöntemini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-181">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="d89f5-182">En düşük dizin değeri neredeyse her zaman 0 olsa da, dizinin en düşük değerini almak için <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> yöntemini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-182">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>

<span data-ttu-id="d89f5-183">Aşağıdaki örnek, [`For...Next`](../../../language-reference/statements/for-next-statement.md) ifadesini kullanarak tek boyutlu bir dizi aracılığıyla yinelenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-183">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span>

[!code-vb[iterate-one-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]

<span data-ttu-id="d89f5-184">Aşağıdaki örnek, bir [`For...Next`](../../../language-reference/statements/for-next-statement.md) deyimleri kullanarak çok boyutlu bir dizi boyunca yinelenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-184">The following example iterates through a multidimensional array by using a [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="d89f5-185"><xref:System.Array.GetUpperBound%2A> yöntemi, boyutu belirten bir parametreye sahiptir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-185">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="d89f5-186">`GetUpperBound(0)` ilk boyutun en yüksek dizinini döndürür ve `GetUpperBound(1)` ikinci boyutun en yüksek dizinini döndürür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-186">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>

[!code-vb[iterate-two-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]

<span data-ttu-id="d89f5-187">Aşağıdaki örnek [her biri için bir kullanır... Bir sonraki Ifade](../../../language-reference/statements/for-each-next-statement.md), tek boyutlu bir dizi ve iki boyutlu bir dizi aracılığıyla yinelenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-187">The following example uses a [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>

[!code-vb[iterate-for-each-next](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]

## <a name="array-size"></a><span data-ttu-id="d89f5-188">Dizi boyutu</span><span class="sxs-lookup"><span data-stu-id="d89f5-188">Array size</span></span>

<span data-ttu-id="d89f5-189">Bir dizinin boyutu, tüm boyutlarının uzunluklarının ürünüdür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-189">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="d89f5-190">Dizide bulunan toplam öğe sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-190">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="d89f5-191">Örneğin, aşağıdaki örnek, her boyutta dört öğe içeren 2 boyutlu bir dizi bildirir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-191">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="d89f5-192">Örneğin çıkışının gösterdiği gibi, dizinin boyutu 16 ' dır (veya (3 + 1) \* (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="d89f5-192">As the output from the example shows, the array's size is 16 (or (3 + 1) \* (3 + 1).</span></span>

[!code-vb[array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]

> [!NOTE]
> <span data-ttu-id="d89f5-193">Dizi boyutu tartışması, pürüzlü Diziler için uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-193">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="d89f5-194">Sivri diziler hakkında bilgi edinmek ve pürüzlü bir dizinin boyutunu belirlemek için, [pürüzlü Diziler](#jagged-arrays) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-194">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>

<span data-ttu-id="d89f5-195">Bir dizinin boyutunu <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliğini kullanarak bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-195">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="d89f5-196"><xref:System.Array.GetLength%2A?displayProperty=nameWithType> yöntemini kullanarak çok boyutlu bir dizinin her boyutunun uzunluğunu bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-196">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d89f5-197">Bir dizi değişkenini, kendisine yeni bir dizi nesnesi atayarak veya [`ReDim` ifade](../../../language-reference/statements/redim-statement.md) ifadesini kullanarak yeniden boyutlandırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-197">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="d89f5-198">Aşağıdaki örnek, 100 öğeli bir diziyi 51 öğeli bir dizi olarak değiştirmek için `ReDim` ifadesini kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-198">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

[!code-vb[resize-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]

<span data-ttu-id="d89f5-199">Bir dizinin boyutuyla ilgilenirken göz önünde bulundurmanız gereken birkaç nokta vardır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-199">There are several things to keep in mind when dealing with the size of an array.</span></span>

|||
|---|---|
|<span data-ttu-id="d89f5-200">Boyut uzunluğu</span><span class="sxs-lookup"><span data-stu-id="d89f5-200">Dimension Length</span></span>|<span data-ttu-id="d89f5-201">Her boyutun dizini 0 tabanlıdır, bu da 0 ' dan büyük ' a kadar değişen anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-201">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="d89f5-202">Bu nedenle, belirli bir boyutun uzunluğu, bu boyutun belirtilen üst sınırından bir büyük.</span><span class="sxs-lookup"><span data-stu-id="d89f5-202">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|
|<span data-ttu-id="d89f5-203">Uzunluk sınırları</span><span class="sxs-lookup"><span data-stu-id="d89f5-203">Length Limits</span></span>|<span data-ttu-id="d89f5-204">Bir dizinin her boyutunun uzunluğu, <xref:System.Int32.MaxValue?displayProperty=nameWithType> veya (2 ^ 31)-1 olan `Integer` veri türünün en büyük değeriyle sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-204">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="d89f5-205">Ancak, bir dizinin toplam boyutu, sisteminizdeki kullanılabilir bellek ile de sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-205">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="d89f5-206">Kullanılabilir bellek miktarını aşan bir diziyi başlatmaya çalışırsanız, çalışma zamanı bir <xref:System.OutOfMemoryException>oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-206">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|
|<span data-ttu-id="d89f5-207">Boyut ve öğe boyutu</span><span class="sxs-lookup"><span data-stu-id="d89f5-207">Size and Element Size</span></span>|<span data-ttu-id="d89f5-208">Bir dizinin boyutu, öğelerinin veri türünden bağımsızdır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-208">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="d89f5-209">Boyut her zaman, bellekte kullandıkları bayt sayısını değil, toplam öğe sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-209">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|
|<span data-ttu-id="d89f5-210">Bellek tüketimi</span><span class="sxs-lookup"><span data-stu-id="d89f5-210">Memory Consumption</span></span>|<span data-ttu-id="d89f5-211">Bir dizinin bellekte nasıl depolandığına ilişkin varsayımlar yapmak güvenli değildir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-211">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="d89f5-212">Depolama, farklı veri genişliklerinin platformları üzerinde farklılık gösterdiği için aynı dizi, 32 bitlik 64 bir sistemde daha fazla bellek kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-212">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="d89f5-213">Bir diziyi başlattığınızda sistem yapılandırmasına bağlı olarak, ortak dil çalışma zamanı (CLR) depolama alanını paket öğelerine mümkün olduğunca yakın bir şekilde veya tüm doğal donanım sınırları üzerinde hizalamak için atayabilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-213">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="d89f5-214">Ayrıca, bir dizi, denetim bilgileri için bir depolama ek yükü gerektirir ve bu ek yük, eklenen her boyutla birlikte artar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-214">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|

## <a name="the-array-type"></a><span data-ttu-id="d89f5-215">Dizi türü</span><span class="sxs-lookup"><span data-stu-id="d89f5-215">The array type</span></span>

<span data-ttu-id="d89f5-216">Her dizide, öğelerinin veri türünden farklı bir veri türü vardır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-216">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="d89f5-217">Tüm diziler için tek bir veri türü yoktur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-217">There is no single data type for all arrays.</span></span> <span data-ttu-id="d89f5-218">Bunun yerine, bir dizinin veri türü, dizinin boyut sayısı veya *sıralaması*ile dizideki öğelerin veri türü tarafından belirlenir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-218">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="d89f5-219">İki dizi değişkeni yalnızca aynı dereceye sahip olduklarında ve öğeleri aynı veri türüne sahip olduğunda aynı veri türündedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-219">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="d89f5-220">Bir dizinin boyutlarının uzunlukları dizi veri türünü etkilemez.</span><span class="sxs-lookup"><span data-stu-id="d89f5-220">The lengths of the dimensions of an array do not influence the array data type.</span></span>

<span data-ttu-id="d89f5-221">Her dizi <xref:System.Array?displayProperty=nameWithType> sınıfından devralır ve bir değişkeni `Array`türünde olacak şekilde bildirebilirsiniz, ancak `Array`türünde bir dizi oluşturamazsınız.</span><span class="sxs-lookup"><span data-stu-id="d89f5-221">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="d89f5-222">Örneğin, aşağıdaki kod `arr` değişkenini `Array` türü olarak bildirir ve diziyi başlatmak için <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> yöntemini çağırır, dizinin türü Object [] olarak kanıtlar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-222">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

[!code-vb[array-class](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)]

<span data-ttu-id="d89f5-223">Ayrıca, [ReDim deyimleri](../../../language-reference/statements/redim-statement.md) tür `Array`olarak belirtilen bir değişkende çalışamaz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-223">Also, the [ReDim Statement](../../../language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="d89f5-224">Bu nedenlerle ve tür güvenliği için her dizinin belirli bir tür olarak bildirilmesini önerilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-224">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>

<span data-ttu-id="d89f5-225">Bir dizinin veya öğelerinin veri türünü çeşitli yollarla bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-225">You can find out the data type of either an array or its elements in several ways.</span></span>

- <span data-ttu-id="d89f5-226">Değişkenin çalışma zamanı türünü temsil eden bir <xref:System.Type> nesnesi almak için değişkende <xref:System.Object.GetType%2A> yöntemini çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-226">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="d89f5-227"><xref:System.Type> nesnesi, özellikleri ve yöntemlerinde kapsamlı bilgiler içerir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-227">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>
- <span data-ttu-id="d89f5-228">Çalışma zamanı türünün adı ile bir `String` almak için değişkeni <xref:Microsoft.VisualBasic.Information.TypeName%2A> işleve geçirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-228">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>

<span data-ttu-id="d89f5-229">Aşağıdaki örnek, bir dizi türünü belirleyebilmek için hem `GetType` yöntemini hem de `TypeName` işlevini çağırır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-229">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="d89f5-230">Dizi türü `Byte(,)`.</span><span class="sxs-lookup"><span data-stu-id="d89f5-230">The array type is `Byte(,)`.</span></span> <span data-ttu-id="d89f5-231"><xref:System.Type.BaseType%2A?displayProperty=nameWithType> özelliğinin aynı zamanda Byte dizisinin temel türünün <xref:System.Array> sınıfı olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-231">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>

[!code-vb[array-type](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]

## <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="d89f5-232">Dönüş değerleri ve parametreleri olarak diziler</span><span class="sxs-lookup"><span data-stu-id="d89f5-232">Arrays as return values and parameters</span></span>

<span data-ttu-id="d89f5-233">Bir `Function` yordamından bir dizi döndürmek için, dizi veri türü ve boyut sayısını [Function ifadesinin](../../../language-reference/statements/function-statement.md)dönüş türü olarak belirtin.</span><span class="sxs-lookup"><span data-stu-id="d89f5-233">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="d89f5-234">İşlevi içinde, aynı veri türüne ve boyut sayısına sahip bir yerel dizi değişkeni bildirin.</span><span class="sxs-lookup"><span data-stu-id="d89f5-234">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="d89f5-235">[Return ifadesinde](../../../language-reference/statements/return-statement.md), yerel dizi değişkenini parantez olmadan dahil edin.</span><span class="sxs-lookup"><span data-stu-id="d89f5-235">In the [Return Statement](../../../language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>

<span data-ttu-id="d89f5-236">Bir diziyi bir `Sub` veya `Function` yordamına parametre olarak belirtmek için, parametreyi belirtilen veri türüne ve boyut sayısına sahip bir dizi olarak tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-236">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="d89f5-237">Yordamın çağrısında, aynı veri türüne ve boyut sayısına sahip bir dizi değişkeni geçirin.</span><span class="sxs-lookup"><span data-stu-id="d89f5-237">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>

<span data-ttu-id="d89f5-238">Aşağıdaki örnekte `GetNumbers` işlevi, `Integer`türünde tek boyutlu bir dizi `Integer()`döndürür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-238">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="d89f5-239">`ShowNumbers` yordam bir `Integer()` bağımsız değişkenini kabul eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-239">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span>

[!code-vb[return-value-and-params](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]

<span data-ttu-id="d89f5-240">Aşağıdaki örnekte `GetNumbersMultiDim` işlevi, `Integer`türünde iki boyutlu bir dizi `Integer(,)`döndürür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-240">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="d89f5-241">`ShowNumbersMultiDim` yordam bir `Integer(,)` bağımsız değişkenini kabul eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-241">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>

[!code-vb[multidimensional-return-value](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]

## <a name="jagged-arrays"></a><span data-ttu-id="d89f5-242">Sivri diziler</span><span class="sxs-lookup"><span data-stu-id="d89f5-242">Jagged arrays</span></span>

<span data-ttu-id="d89f5-243">Bazen uygulamanızdaki veri yapısı iki boyutlu ancak dikdörtgen değildir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-243">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="d89f5-244">Örneğin, ayın her gününün yüksek sıcaklığına ilişkin verileri depolamak için bir dizi kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-244">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="d89f5-245">Dizinin ilk boyutu ayı temsil eder, ancak ikinci boyut gün sayısını ve ay içindeki gün sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="d89f5-245">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="d89f5-246">Dizi *dizileri*olarak da adlandırılan *pürüzlü bir dizi*, bu tür senaryolar için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-246">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="d89f5-247">Pürüzlü dizi öğeleri de diziler olan bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-247">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="d89f5-248">Sivri dizi ve pürüzlü dizideki her öğe bir veya daha fazla boyuta sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-248">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>

<span data-ttu-id="d89f5-249">Aşağıdaki örnek, her bir bir gün dizisi olan her öğesi bir ay dizisini kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-249">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="d89f5-250">Farklı aylar farklı gün sayılarına sahip olduğundan, örnek pürüzlü bir diziyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-250">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="d89f5-251">Örnek, pürüzlü bir dizi oluşturmayı, bu değere değer atamayı ve değerlerini almayı ve görüntülemeyi gösterir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-251">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>

[!code-vb[jagged-arrays](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]

<span data-ttu-id="d89f5-252">Önceki örnek, `For...Next` döngüsünü kullanarak öğeleri öğe temelinde pürüzlü diziye değerler atar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-252">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="d89f5-253">Ayrıca, iç içe dizi değişmez değerlerini kullanarak pürüzlü bir dizinin öğelerine değerler atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-253">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="d89f5-254">Ancak, iç içe dizi değişmez değerlerini kullanma girişimi (örneğin, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}`) derleyici hatası oluşturur [BC30568](../../../,,/../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="d89f5-254">However, the attempt to use nested array literals (for example, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}`) generates compiler error [BC30568](../../../,,/../misc/bc30568.md).</span></span> <span data-ttu-id="d89f5-255">Hatayı düzeltmek için, iç dizi değişmez değerlerini parantez içine alın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-255">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="d89f5-256">Parantez, dizi değişmez ifadesinin değerlendirilmesini zorlar ve elde edilen değerler, aşağıdaki örnekte gösterildiği gibi dış dizi sabiti ile birlikte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-256">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>

[!code-vb[jagged-array-initialization](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)]

<span data-ttu-id="d89f5-257">Sivri dizi öğeleri diziler içeren tek boyutlu bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-257">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="d89f5-258">Bu nedenle, <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliği ve `Array.GetLength(0)` yöntemi tek boyutlu dizideki öğelerin sayısını döndürür ve pürüzlü bir dizi çok boyutlu olmadığından `Array.GetLength(1)` bir <xref:System.IndexOutOfRangeException> oluşturur.</span><span class="sxs-lookup"><span data-stu-id="d89f5-258">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="d89f5-259">Her bir alt dizinin <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliğinin değerini alarak her bir alt dizideki öğelerin sayısını belirlersiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-259">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="d89f5-260">Aşağıdaki örnek, pürüzlü bir dizideki öğelerin sayısının nasıl belirleneceğini göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-260">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)]

## <a name="zero-length-arrays"></a><span data-ttu-id="d89f5-261">Sıfır uzunluklu diziler</span><span class="sxs-lookup"><span data-stu-id="d89f5-261">Zero-length arrays</span></span>

<span data-ttu-id="d89f5-262">Visual Basic, başlatılmamış bir dizi (değeri `Nothing`olan bir dizi) ve *sıfır uzunluklu bir dizi* ya da boş dizi (hiçbir öğesi olmayan bir dizi) arasında farklılaştırır. Başlatılmamış bir dizi, boyutlandırılmış olmayan veya kendisine atanmış değer içeren bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-262">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="d89f5-263">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="d89f5-263">For example:</span></span>

```vb
Dim arr() As String
```

<span data-ttu-id="d89f5-264">Sıfır uzunlukta bir dizi,-1 boyutuyla birlikte bildirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-264">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="d89f5-265">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="d89f5-265">For example:</span></span>

```vb
Dim arrZ(-1) As String
```

<span data-ttu-id="d89f5-266">Aşağıdaki koşullarda sıfır uzunluklu bir dizi oluşturmanız gerekebilir:</span><span class="sxs-lookup"><span data-stu-id="d89f5-266">You might need to create a zero-length array under the following circumstances:</span></span>

- <span data-ttu-id="d89f5-267"><xref:System.NullReferenceException> bir özel durumu risksiz, kodunuzun <xref:System.Array.Length%2A> veya <xref:System.Array.Rank%2A>gibi <xref:System.Array> sınıfının üyelerine erişmesi veya Visual Basic gibi bir <xref:Microsoft.VisualBasic.Information.UBound%2A>işlevi çağırması gerekir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-267">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>

- <span data-ttu-id="d89f5-268">Özel bir durum olarak `Nothing` denetlemek zorunda kalmadan kodunuzu basit tutmak istersiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-268">You want to keep your code simple by not having to check for `Nothing` as a special case.</span></span>

- <span data-ttu-id="d89f5-269">Kodunuz bir veya daha fazla yordamdan sıfır uzunluklu bir diziyi geçirmenize ya da bir veya daha fazla yordamdan sıfır uzunluklu bir dizi döndürmeniz gereken bir uygulama programlama arabirimi (API) ile etkileşime girer.</span><span class="sxs-lookup"><span data-stu-id="d89f5-269">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="d89f5-270">Diziyi bölme</span><span class="sxs-lookup"><span data-stu-id="d89f5-270">Splitting an array</span></span>

<span data-ttu-id="d89f5-271">Bazı durumlarda, tek bir diziyi birden çok diziye bölmeniz gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-271">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="d89f5-272">Bu, dizinin bölüneceği nokta veya noktaları tanımlamayı ve sonra diziyi iki veya daha fazla ayrı dizilere Spitting.</span><span class="sxs-lookup"><span data-stu-id="d89f5-272">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span>

> [!NOTE]
> <span data-ttu-id="d89f5-273">Bu bölüm, tek bir dizeyi bir dize dizisine bölme bölümünü bir sınırlayıcı temelinde ele almaz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-273">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="d89f5-274">Bir dizeyi bölme hakkında daha fazla bilgi için <xref:System.String.Split%2A?displayProperty=nameWithType> yöntemine bakın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-274">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d89f5-275">Bir diziyi bölmek için en yaygın ölçütler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="d89f5-275">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="d89f5-276">Dizideki öğelerin sayısı</span><span class="sxs-lookup"><span data-stu-id="d89f5-276">The number of elements in the array.</span></span> <span data-ttu-id="d89f5-277">Örneğin, belirtilen sayıda öğeden fazla bir diziyi, yaklaşık olarak eşit sayıda parçaya bölmek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-277">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="d89f5-278">Bu amaçla, <xref:System.Array.Length%2A?displayProperty=nameWithType> ya da <xref:System.Array.GetLength%2A?displayProperty=nameWithType> yöntemi tarafından döndürülen değeri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-278">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="d89f5-279">Dizinin nerede bölüneceği belirten bir sınırlayıcı görevi gören bir öğenin değeri.</span><span class="sxs-lookup"><span data-stu-id="d89f5-279">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="d89f5-280"><xref:System.Array.FindIndex%2A?displayProperty=nameWithType> ve <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> yöntemlerini çağırarak belirli bir değeri arayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-280">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>

<span data-ttu-id="d89f5-281">Dizinin bölünmesi gereken dizin veya dizinleri belirledikten sonra, <xref:System.Array.Copy%2A?displayProperty=nameWithType> yöntemini çağırarak ayrı dizileri oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-281">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d89f5-282">Aşağıdaki örnek, bir diziyi yaklaşık olarak eşit boyuttaki iki diziye böler.</span><span class="sxs-lookup"><span data-stu-id="d89f5-282">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="d89f5-283">(Dizi öğelerinin toplam sayısı tek ise, ilk dizide ikinciden daha fazla bir öğe vardır.)</span><span class="sxs-lookup"><span data-stu-id="d89f5-283">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span>

[!code-vb[splitting-an-array-by-length](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)]

<span data-ttu-id="d89f5-284">Aşağıdaki örnek, bir dize dizisini değeri "zzz" olan ve dizi sınırlayıcısı görevi gören bir öğe varlığını temel alarak iki diziye ayırır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-284">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="d89f5-285">Yeni diziler, sınırlayıcıyı içeren öğeyi içermez.</span><span class="sxs-lookup"><span data-stu-id="d89f5-285">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)]

## <a name="joining-arrays"></a><span data-ttu-id="d89f5-286">Dizileri birleştirme</span><span class="sxs-lookup"><span data-stu-id="d89f5-286">Joining arrays</span></span>

<span data-ttu-id="d89f5-287">Ayrıca, bir dizi diziyi tek bir daha büyük dizi içinde birleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-287">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="d89f5-288">Bunu yapmak için <xref:System.Array.Copy%2A?displayProperty=nameWithType> yöntemini de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-288">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

> [!NOTE]
> <span data-ttu-id="d89f5-289">Bu bölüm, dize dizisinin tek bir dizeye katılmasını tartışır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-289">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="d89f5-290">Bir dize dizisini birleştirme hakkında daha fazla bilgi için <xref:System.String.Join%2A?displayProperty=nameWithType> yöntemine bakın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-290">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="d89f5-291">Her bir dizinin öğelerini yeni diziye kopyalamadan önce, diziyi yeni diziye uyum sağlayacak kadar büyük olacak şekilde oluşturduğunuzdan emin olmalısınız.</span><span class="sxs-lookup"><span data-stu-id="d89f5-291">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accommodate the new array.</span></span> <span data-ttu-id="d89f5-292">Bunu iki yoldan biriyle yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="d89f5-292">You can do this in one of two ways:</span></span>

- <span data-ttu-id="d89f5-293">Yeni öğe eklemeden önce diziyi dinamik olarak genişletmek için [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) ifadesini kullanın.</span><span class="sxs-lookup"><span data-stu-id="d89f5-293">Use the [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="d89f5-294">Bu en kolay tekniktir, ancak büyük dizileri kopyalarken performans düşüşüne ve aşırı bellek kullanımına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-294">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="d89f5-295">Yeni büyük dizi için gereken toplam öğe sayısını hesaplayın, ardından her kaynak dizisinin öğelerini buna ekleyin.</span><span class="sxs-lookup"><span data-stu-id="d89f5-295">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="d89f5-296">Aşağıdaki örnek, her biri tek bir diziye on öğe içeren dört dizi eklemek için ikinci yaklaşımı kullanır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-296">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>

[!code-vb[joining-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)]

<span data-ttu-id="d89f5-297">Bu durumda, kaynak dizileri tümüyle küçük olduğundan, her yeni dizinin öğelerini eklediğimiz için diziyi de dinamik olarak genişletebiliriz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-297">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="d89f5-298">Aşağıdaki örnek bunu yapar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-298">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)]

## <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="d89f5-299">Dizilere Alternatif olarak Koleksiyonlar</span><span class="sxs-lookup"><span data-stu-id="d89f5-299">Collections as an alternative to arrays</span></span>

<span data-ttu-id="d89f5-300">Diziler, kesin olarak belirlenmiş sabit sayıda nesne oluşturmak ve bunlarla çalışmak için en yararlı seçenektir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-300">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="d89f5-301">Koleksiyonlar, nesne gruplarıyla çalışmak için daha esnek bir yol sağlar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-301">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="d89f5-302">Bir dizinin boyutunu açıkça [`ReDim` ifadesiyle](../../../language-reference/statements/redim-statement.md)değiştirmenize gerek olan dizilerin aksine koleksiyonlar, bir uygulama değişikliğinin ihtiyaçlarına göre dinamik olarak büyür ve küçülür.</span><span class="sxs-lookup"><span data-stu-id="d89f5-302">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>

<span data-ttu-id="d89f5-303">Bir diziyi yeniden Dimension yapmak için `ReDim` kullandığınızda, Visual Basic yeni bir dizi oluşturur ve öncekini yayınlar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-303">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="d89f5-304">Bu, yürütme süresini alır.</span><span class="sxs-lookup"><span data-stu-id="d89f5-304">This takes execution time.</span></span> <span data-ttu-id="d89f5-305">Bu nedenle, çalıştığınız öğe sayısı sıklıkla değişirse veya ihtiyacınız olan en fazla öğe sayısını tahmin edemezsiniz, genellikle bir koleksiyon kullanarak daha iyi performans elde edersiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-305">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>

<span data-ttu-id="d89f5-306">Bazı koleksiyonlar için, anahtarı kullanarak nesneyi hızlı bir şekilde alabilmeniz için koleksiyona yerleştirdiğiniz herhangi bir nesneye bir anahtar atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-306">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>

<span data-ttu-id="d89f5-307">Koleksiyonunuz yalnızca bir veri türünün öğelerini içeriyorsa, <xref:System.Collections.Generic?displayProperty=nameWithType> ad alanındaki sınıflardan birini kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-307">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="d89f5-308">Genel bir koleksiyon, tür güvenliğini, başka bir veri türü eklenememesi için uygular.</span><span class="sxs-lookup"><span data-stu-id="d89f5-308">A generic collection enforces type safety so that no other data type can be added to it.</span></span>

<span data-ttu-id="d89f5-309">Koleksiyonlar hakkında daha fazla bilgi için bkz. [koleksiyonlar](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="d89f5-309">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="d89f5-310">İlgili konular</span><span class="sxs-lookup"><span data-stu-id="d89f5-310">Related topics</span></span>

|<span data-ttu-id="d89f5-311">Terim</span><span class="sxs-lookup"><span data-stu-id="d89f5-311">Term</span></span>|<span data-ttu-id="d89f5-312">Tanım</span><span class="sxs-lookup"><span data-stu-id="d89f5-312">Definition</span></span>|
|----------|----------------|
|[<span data-ttu-id="d89f5-313">Visual Basic dizi boyutları</span><span class="sxs-lookup"><span data-stu-id="d89f5-313">Array Dimensions in Visual Basic</span></span>](../../language-features/arrays/array-dimensions.md)|<span data-ttu-id="d89f5-314">Dizilerde derecelendirme ve boyutları açıklar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-314">Explains rank and dimensions in arrays.</span></span>|
|[<span data-ttu-id="d89f5-315">Nasıl yapılır: Visual Basic dizi değişkenini başlatma</span><span class="sxs-lookup"><span data-stu-id="d89f5-315">How to: Initialize an Array Variable in Visual Basic</span></span>](../../language-features/arrays/how-to-initialize-an-array-variable.md)|<span data-ttu-id="d89f5-316">İlk değerlerle dizilerin nasıl doldurulacağını açıklar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-316">Describes how to populate arrays with initial values.</span></span>|
|[<span data-ttu-id="d89f5-317">Nasıl yapılır: Visual Basic bir diziyi sıralama</span><span class="sxs-lookup"><span data-stu-id="d89f5-317">How to: Sort An Array in Visual Basic</span></span>](../../language-features/arrays/how-to-sort-an-array.md)|<span data-ttu-id="d89f5-318">Bir dizinin öğelerinin alfabetik olarak nasıl sıralanacağını gösterir.</span><span class="sxs-lookup"><span data-stu-id="d89f5-318">Shows how to sort the elements of an array alphabetically.</span></span>|
|[<span data-ttu-id="d89f5-319">Nasıl yapılır: Bir Diziyi Başka Diziye Atama</span><span class="sxs-lookup"><span data-stu-id="d89f5-319">How to: Assign One Array to Another Array</span></span>](../../language-features/arrays/how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="d89f5-320">Bir diziyi başka bir dizi değişkenine atamaya yönelik kuralları ve adımları açıklar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-320">Describes the rules and steps for assigning an array to another array variable.</span></span>|
|[<span data-ttu-id="d89f5-321">Dizilerle İlgili Sorun Giderme</span><span class="sxs-lookup"><span data-stu-id="d89f5-321">Troubleshooting Arrays</span></span>](../../language-features/arrays/troubleshooting-arrays.md)|<span data-ttu-id="d89f5-322">Dizilerle çalışırken ortaya çıkan bazı yaygın sorunları açıklar.</span><span class="sxs-lookup"><span data-stu-id="d89f5-322">Discusses some common problems that arise when working with arrays.</span></span>|

## <a name="see-also"></a><span data-ttu-id="d89f5-323">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="d89f5-323">See also</span></span>

- <xref:System.Array?displayProperty=nameWithType>
- [<span data-ttu-id="d89f5-324">Dim Deyimi</span><span class="sxs-lookup"><span data-stu-id="d89f5-324">Dim Statement</span></span>](../../../language-reference/statements/dim-statement.md)
- [<span data-ttu-id="d89f5-325">ReDim Deyimi</span><span class="sxs-lookup"><span data-stu-id="d89f5-325">ReDim Statement</span></span>](../../../language-reference/statements/redim-statement.md)
