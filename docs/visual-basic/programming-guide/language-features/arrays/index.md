---
title: Visual Basic'de Diziler
ms.custom: ''
ms.date: 12/06/2017
ms.prod: .net
ms.technology:
- devlang-visual-basic
ms.topic: article
f1_keywords:
- vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
author: rpetrusha
ms.author: ronpet
ms.manager: wpickett
ms.openlocfilehash: d223ca8b0ff59a13c31fa777e5cb6a97918421c6
ms.sourcegitcommit: 01ea3686e74ff05e4f6de3d8d46dc603d051ec00
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 12/13/2017
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="c4b42-102">Visual Basic'de Diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-102">Arrays in Visual Basic</span></span>
<span data-ttu-id="c4b42-103">Bir dizi olarak ifade edilmektedir değerler kümesidir *öğeleri*, mantıksal olarak ilgili diğer için.</span><span class="sxs-lookup"><span data-stu-id="c4b42-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="c4b42-104">Örneğin, bir dizi dilbilgisi Okul içinde her düzeyde öğrencinin sayısının oluşabilir; Dizideki her öğe tek bir düzeyde öğrencinin sayısıdır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="c4b42-105">Benzer şekilde, bir dizi bir sınıf için öğrencinin dereceleri oluşur; her dizinin tek bir düzeyde öğedir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>    

<span data-ttu-id="c4b42-106">Bizim veri öğelerin her birini depolamak için olası bağımsız değişkenler var.</span><span class="sxs-lookup"><span data-stu-id="c4b42-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="c4b42-107">Uygulamamız Öğrenci dereceleri analiz eder, örneğin, biz ayrı bir değişken her Öğrencinin notu için gibi kullanabilir `englishGrade1`, `englishGrade2`vb. Bu yaklaşım, üç ana sınırlamalara sahiptir:</span><span class="sxs-lookup"><span data-stu-id="c4b42-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>
- <span data-ttu-id="c4b42-108">Biz işlemek sahibiz tam olarak kaç dereceleri tasarım zamanında bilmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="c4b42-109">Çok sayıda derece hızlı bir şekilde işleme yönetilmeleri haline gelir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="c4b42-110">Bu sırayla ciddi hatalar sahip çok daha büyük olasılıkla bir uygulama sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="c4b42-111">Etmek zordur.</span><span class="sxs-lookup"><span data-stu-id="c4b42-111">It is difficult to maintain.</span></span> <span data-ttu-id="c4b42-112">Eklediğimiz her yeni bir sınıf uygulama değiştiren, yeniden derlenmesi, imzalanması ve dağıtılması gerektiğini gerektirir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>  
 
 <span data-ttu-id="c4b42-113">Bir dizi kullanarak tarafından aynı ad ile ilgili bu değerleri başvurmak ve çağrılan bir sayı kullanın bir *dizin* veya *alt simge* dizideki konumuna bağlı tek bir öğe tanımlamak için.</span><span class="sxs-lookup"><span data-stu-id="c4b42-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="c4b42-114">Bir dizi aralığı 0'dan bir dizide öğe toplam sayısından daha az dizinleri.</span><span class="sxs-lookup"><span data-stu-id="c4b42-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="c4b42-115">Bir dizinin boyutu tanımlamak için Visual Basic sözdizimini kullandığınızda, en yüksek dizinini, dizideki öğeler değil toplam sayısını belirtin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="c4b42-116">Bir birim olarak dizi birlikte çalışabilir ve öğeleri yineleme özelliğini tam olarak tasarım zamanında içerdiği kaç öğeleri bilmenize gerek gelen boşaltır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>
  
 <span data-ttu-id="c4b42-117">Açıklama önce hızlı bazı örnekler:</span><span class="sxs-lookup"><span data-stu-id="c4b42-117">Some quick examples before explanation:</span></span>  
  
```vb  
' Declare a single-dimension array of 5 numbers.  
Dim numbers(4) As Integer   
  
'Declare a single-dimension array and set its 4 values.  
Dim numbers = New Integer() {1, 2, 4, 8}  
  
' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)
  
' Redefine the size of an existing array and reset the values.
ReDim numbers(15)  
  
' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double  
  
' Declare a 4 x 3 multidimensional array and set array element values.  
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}  
  
' Declare a jagged array  
Dim sales()() As Double = New Double(11)() {}  
```  
  
 ## <a name="in-this-article"></a><span data-ttu-id="c4b42-118">Bu makalede</span><span class="sxs-lookup"><span data-stu-id="c4b42-118">In this article</span></span>
  
- [<span data-ttu-id="c4b42-119">Basit dizi dizi öğeleri</span><span class="sxs-lookup"><span data-stu-id="c4b42-119">Array elements in a simple array</span></span>](#array-elements-in-a-simple-array)  
  
- [<span data-ttu-id="c4b42-120">Bir dizi oluşturma</span><span class="sxs-lookup"><span data-stu-id="c4b42-120">Creating an array</span></span>](#creating-an-array)  
  
- [<span data-ttu-id="c4b42-121">Bir dizi değerlerini depolama</span><span class="sxs-lookup"><span data-stu-id="c4b42-121">Storing values in an array</span></span>](#storing-values-in-an-array)  
  
- [<span data-ttu-id="c4b42-122">Dizi değişmez değerleri olan bir dizi doldurma</span><span class="sxs-lookup"><span data-stu-id="c4b42-122">Populating an array with array literals</span></span>](#populating-an-array-with-array-literals)  
  
- [<span data-ttu-id="c4b42-123">Bir dizi yineleme yapma</span><span class="sxs-lookup"><span data-stu-id="c4b42-123">Iterating through an array</span></span>](#iterating-through-an-array)  
  
- [<span data-ttu-id="c4b42-124">Dizi boyutu</span><span class="sxs-lookup"><span data-stu-id="c4b42-124">Array size</span></span>](#BKMK_ArraySize)  

- [<span data-ttu-id="c4b42-125">Dizi türü</span><span class="sxs-lookup"><span data-stu-id="c4b42-125">The array type</span></span>](#the-array-type)  
  
- [<span data-ttu-id="c4b42-126">Dönüş değerleri ve parametre olarak diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-126">Arrays as return values and parameters</span></span>](#arrays-as-return-values-and-parameters)  
- [<span data-ttu-id="c4b42-127">Basit diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-127">Jagged arrays</span></span>](#jagged-arrays)  
  
- [<span data-ttu-id="c4b42-128">Sıfır uzunlukta diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-128">Zero-length arrays</span></span>](#zero-length-arrays)  

- [<span data-ttu-id="c4b42-129">Bir dizi bölme</span><span class="sxs-lookup"><span data-stu-id="c4b42-129">Splitting an array</span></span>](#splitting-an-array)
  
- [<span data-ttu-id="c4b42-130">Diziler için bir alternatif olarak koleksiyonları</span><span class="sxs-lookup"><span data-stu-id="c4b42-130">Collections as an alternative to arrays</span></span>](#collections-as-an-alternative-to-arrays)  
  
##  <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="c4b42-131">Basit dizi dizi öğeleri</span><span class="sxs-lookup"><span data-stu-id="c4b42-131">Array elements in a simple array</span></span>  

<span data-ttu-id="c4b42-132">Adlı bir dizi oluşturalım `students` Öğrenciler sayısı her düzeyde dilbilgisi Okul içinde depolamak.</span><span class="sxs-lookup"><span data-stu-id="c4b42-132">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="c4b42-133">0 ile 6 öğeleri aralığından dizinleri.</span><span class="sxs-lookup"><span data-stu-id="c4b42-133">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="c4b42-134">Bu dizi kullanarak yedi değişkenleri bildirme daha basittir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-134">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="c4b42-135">Aşağıdaki çizimde gösterildiği `students` dizi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-135">The following illustration shows the `students` array.</span></span> <span data-ttu-id="c4b42-136">Dizideki her öğe için:</span><span class="sxs-lookup"><span data-stu-id="c4b42-136">For each element of the array:</span></span>  
  
-   <span data-ttu-id="c4b42-137">Düzey öğenin dizinini temsil eder (dizin 0 kindergarten temsil eder).</span><span class="sxs-lookup"><span data-stu-id="c4b42-137">The index of the element represents the grade (index 0 represents kindergarten).</span></span>
  
-   <span data-ttu-id="c4b42-138">Öğesinde bulunan değer o düzeyde öğrencinin sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c4b42-138">The value that’s contained in the element represents the number of students in that grade.</span></span>
  
 <span data-ttu-id="c4b42-139">![Öğrenciler sayıda gösteren dizinin resmi](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span><span class="sxs-lookup"><span data-stu-id="c4b42-139">![Picture of array showing numbers of students](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span></span>  
<span data-ttu-id="c4b42-140">"Öğrenciler" dizideki öğeler</span><span class="sxs-lookup"><span data-stu-id="c4b42-140">Elements of the "students" array</span></span>  
 
<span data-ttu-id="c4b42-141">Aşağıdaki örnek, oluşturur ve dizi kullanan Visual Basic kodu içerir:</span><span class="sxs-lookup"><span data-stu-id="c4b42-141">The following example contains the Visual Basic code that creates and uses the array:</span></span>

 [!code-vb[simple-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]  

<span data-ttu-id="c4b42-142">Örneğin, üç şey yapar:</span><span class="sxs-lookup"><span data-stu-id="c4b42-142">The example does three things:</span></span>

- <span data-ttu-id="c4b42-143">Bunu bildiren bir `students` yedi öğeleri dizisi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-143">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="c4b42-144">Sayı `6` dizideki bildirimi dizideki son dizin gösterir; bir değer dizideki öğe sayısından daha az.</span><span class="sxs-lookup"><span data-stu-id="c4b42-144">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="c4b42-145">Dizideki her öğe için değerleri atar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-145">It assigns values to each element in the array.</span></span> <span data-ttu-id="c4b42-146">Dizi öğeleri dizi adı kullanarak ve tek tek öğenin dizini parantez içine dahil olmak üzere erişilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-146">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="c4b42-147">Dizinin her bir değeri listeler.</span><span class="sxs-lookup"><span data-stu-id="c4b42-147">It lists each value of the array.</span></span> <span data-ttu-id="c4b42-148">Örnek kullanan bir [ `For` ](../../../language-reference/statements/for-next-statement.md) dizin numarası ile dizinin her öğesine erişmek için bildirimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-148">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>
  
 <span data-ttu-id="c4b42-149">`students` Önceki örnekte dizidir tek boyutlu dizi çünkü bir dizin kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-149">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="c4b42-150">Birden çok dizini veya alt simge kullanan bir dizi adlı *çok boyutlu*.</span><span class="sxs-lookup"><span data-stu-id="c4b42-150">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="c4b42-151">Daha fazla bilgi için bu makalenin geri kalanını görmek ve [Visual Basic'de dizi boyutları](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-151">For more information, see the rest of this article and [Array Dimensions in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span></span>  
  
##  <a name="creating-an-array"></a><span data-ttu-id="c4b42-152">Bir dizi oluşturma</span><span class="sxs-lookup"><span data-stu-id="c4b42-152">Creating an Array</span></span>  
 
<span data-ttu-id="c4b42-153">Bir dizinin boyutu çeşitli yollarla tanımlayabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c4b42-153">You can define the size of an array in several ways:</span></span> 

- <span data-ttu-id="c4b42-154">Dizi bildirilmişse boyutu belirtebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c4b42-154">You can specify the size when the array is declared:</span></span>
  
    [!code-vb[creating1](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]  
  
 - <span data-ttu-id="c4b42-155">Kullanabileceğiniz bir `New` oluşturulduğunda bir dizi boyutu sağlamak için yan tümcesi:</span><span class="sxs-lookup"><span data-stu-id="c4b42-155">You can use a `New` clause to supply the size of an array when it’s created:</span></span>  
  
    [!code-vb[creating2](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]  
  
 <span data-ttu-id="c4b42-156">Var olan bir dizi varsa, kullanarak boyutunu tanımlayabilirsiniz [ `Redim` ](../../../../visual-basic/language-reference/statements/redim-statement.md) deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-156">If you have an existing array, you can redefine its size by using the [`Redim`](../../../../visual-basic/language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="c4b42-157">Belirtebilirsiniz `Redim` deyimi tutmak dizideki değerleri ya da boş bir dizi oluşturmak belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-157">You can specify that the `Redim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="c4b42-158">Aşağıdaki örnek, farklı kullanımlarını gösterir `Redim` var olan bir dizi boyutunu değiştirmek için deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-158">The following example shows different uses of the `Redim` statement to modify the size of an existing array.</span></span>  
  
 [!code-vb[redimensioning](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]  
  
 <span data-ttu-id="c4b42-159">Daha fazla bilgi için bkz: [ReDim deyimi](../../../../visual-basic/language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-159">For more information, see the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md).</span></span>  
  
##  <a name="storing-values-in-an-array"></a><span data-ttu-id="c4b42-160">Bir dizi değerlerini depolama</span><span class="sxs-lookup"><span data-stu-id="c4b42-160">Storing Values in an Array</span></span>
 
 <span data-ttu-id="c4b42-161">Her iki yerde de bir dizi türü bir dizin kullanarak erişebilirsiniz `Integer`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-161">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="c4b42-162">Depolamak ve bir dizi değerlerini parantez içine alınmış kendi dizini kullanılarak her bir dizi konum başvurarak alın.</span><span class="sxs-lookup"><span data-stu-id="c4b42-162">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="c4b42-163">Çok boyutlu diziler için dizin (,) virgülle ayrılır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-163">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="c4b42-164">Her bir dizi boyut için bir dizin gerekir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-164">You need one index for each array dimension.</span></span> 

<span data-ttu-id="c4b42-165">Aşağıdaki örnek, depolama ve diziler değerleri alma bazı deyimleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-165">The following example shows some statements that store and retrieve values in arrays.</span></span>
  
 [!code-vb[store-and-retrieve](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]  
  
## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="c4b42-166">Dizi değişmez değerleri olan bir dizi doldurma</span><span class="sxs-lookup"><span data-stu-id="c4b42-166">Populating an array with array literals</span></span>
 <span data-ttu-id="c4b42-167">Değişmez değer bir dizi kullanarak oluşturduğunuz aynı anda bir dizi bir başlangıç değerleri kümesi ile doldurabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-167">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="c4b42-168">Değişmez değer bir dizi ayraç içine virgülle ayrılmış değerler listesini oluşur (`{}`).</span><span class="sxs-lookup"><span data-stu-id="c4b42-168">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>  
  
 <span data-ttu-id="c4b42-169">Değişmez değer bir dizi kullanarak bir dizi oluşturduğunuzda, dizi türü girmeyin veya dizi türü belirlemek için tür çıkarımı kullanın.</span><span class="sxs-lookup"><span data-stu-id="c4b42-169">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="c4b42-170">Aşağıdaki örnek, her iki seçenek gösterir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-170">The following example shows both options.</span></span>  
  
 [!code-vb[create-with-literals](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]  
  
 <span data-ttu-id="c4b42-171">Tür çıkarımı kullandığınızda, dizi türü tarafından belirlenir *baskın türü* değişmez değerler listesinde.</span><span class="sxs-lookup"><span data-stu-id="c4b42-171">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="c4b42-172">Baskın türü dizisindeki tüm diğer türleri genişletmek türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4b42-172">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="c4b42-173">Bu benzersiz türü belirlenemiyorsa baskın türü dizisindeki tüm diğer türleri daraltmak benzersiz türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4b42-173">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="c4b42-174">Bu benzersiz türlerinden hiçbiri belirlenebilir baskın türü varsa, `Object`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-174">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="c4b42-175">Değişmez değer dizisine sağlanan değerler listesi türündeki değerler içeriyorsa, örneğin, `Integer`, `Long`, ve `Double`, sonuç dizisi türünde `Double`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-175">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="c4b42-176">Çünkü `Integer` ve `Long` yalnızca genişletmek `Double`, `Double` baskın türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4b42-176">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="c4b42-177">Daha fazla bilgi için bkz: [Widening ve daraltma dönüşümleri](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-177">For more information, see [Widening and Narrowing Conversions](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span></span> 
 
> [!NOTE] 
> <span data-ttu-id="c4b42-178">Yalnızca bir tür üyesi yerel değişkenleri olarak tanımlanan diziler için tür çıkarımı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-178">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="c4b42-179">Bir açık tür tanımı olmazsa, dizi değişmez değerleri sınıf düzeyinde tanımlanan dizilere türlerinin `Object[]`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-179">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="c4b42-180">Daha fazla bilgi için bkz: [yerel türü çıkarımı](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-180">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span> 

<span data-ttu-id="c4b42-181">Önceki örnekte tanımlar Not `values` türünde bir dizi olarak `Double` tüm dizi değişmez değerleri türü olsa bile `Integer`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-181">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="c4b42-182">Dizi değişmez değerleri için genişletebilirsiniz olduğundan bu diziye oluşturabilirsiniz `Double` değerleri.</span><span class="sxs-lookup"><span data-stu-id="c4b42-182">You can create this array because the values in the array literal can widen to `Double` values.</span></span> 
  
 <span data-ttu-id="c4b42-183">Ayrıca oluşturabilir ve çok boyutlu bir diziye kullanarak doldurmak *dizi değişmez değerleri iç içe geçmiş*.</span><span class="sxs-lookup"><span data-stu-id="c4b42-183">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="c4b42-184">İç içe geçmiş dizi değişmez değerleri elde edilen dizi ile tutarlı olan bir dizi boyutları olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-184">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="c4b42-185">Aşağıdaki örnek, iç içe geçmiş dizi değişmez değerleri kullanarak iki boyutlu dizisi oluşturur.</span><span class="sxs-lookup"><span data-stu-id="c4b42-185">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>  
  
 [!code-vb[nested-array-literals](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]  
  
<span data-ttu-id="c4b42-186">İç içe geçmiş dizi değişmez değerleri öğe sayısı eşleşmiyorsa iç içe geçmiş dizi değişmez değerleri oluşturma ve bir dizi doldurmak için kullanılırken bir hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="c4b42-186">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="c4b42-187">Dizi değişmez değerleri boyutlardan farklı sayıda dizi değişkeni açıkça bildirirseniz da bir hata oluşur.</span><span class="sxs-lookup"><span data-stu-id="c4b42-187">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span> 
  
<span data-ttu-id="c4b42-188">Tek boyutlu diziler için yapabildiğiniz gibi çok boyutlu bir diziye iç içe geçmiş dizi değişmez değerleri oluştururken tür çıkarımı güvenebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-188">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="c4b42-189">Çıkarılan türü, tüm dizi değişmez değerleri tüm iç içe geçirme düzeyi tüm değerleri için baskın türüdür.</span><span class="sxs-lookup"><span data-stu-id="c4b42-189">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="c4b42-190">Aşağıdaki örnek türünde iki boyutlu bir dizi oluşturur `Double[,]` türü değerlerinden `Integer` ve `Double`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-190">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>  
  
 [!code-vb[nested-type-inference](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]  
  
 <span data-ttu-id="c4b42-191">Ek örnekler için bkz: [nasıl yapılır: bir dizi değişken Visual Basic'te başlatma](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-191">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span></span>  
  
##  <a name="iterating-through-an-array"></a><span data-ttu-id="c4b42-192">Bir dizi yineleme yapma</span><span class="sxs-lookup"><span data-stu-id="c4b42-192">Iterating through an array</span></span>  
 <span data-ttu-id="c4b42-193">Bir dizi yineleme yaparken, dizideki her öğe için en düşük dizin en yüksek veya en yüksek erişim için en düşük.</span><span class="sxs-lookup"><span data-stu-id="c4b42-193">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="c4b42-194">Genellikle, ya da kullanma [için... Sonraki deyim](../../../../visual-basic/language-reference/statements/for-next-statement.md) veya [For Each... Sonraki deyim](../../../../visual-basic/language-reference/statements/for-each-next-statement.md) bir dizi öğelerinde yineleme yapmak için.</span><span class="sxs-lookup"><span data-stu-id="c4b42-194">Typically, use use either the [For...Next Statement](../../../../visual-basic/language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="c4b42-195">Dizinin üst sınırları bilmiyorsanız, çağırabilirsiniz <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> dizin için en yüksek değeri almak için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-195">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="c4b42-196">En düşük dizin değeri neredeyse olsa da her zaman 0 çağırabilirsiniz <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> dizini en düşük değerini almak için yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-196">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>   
  
 <span data-ttu-id="c4b42-197">Aşağıdaki örnek bir tek boyutlu dizi kullanarak tekrarlanan [ `For...Next` ](../../../../visual-basic/language-reference/statements/for-next-statement.md) deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-197">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../../visual-basic/language-reference/statements/for-next-statement.md) statement.</span></span> 
  
 [!code-vb[iterate-one-dimensional-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]  
  
 <span data-ttu-id="c4b42-198">Aşağıdaki örnek kullanarak çok boyutlu bir diziye tekrarlanan bir [ `For...Next` ](../../../../visual-basic/language-reference/statements/for-next-statement.md) deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-198">The following example iterates through a multidimensional array by using a [`For...Next`](../../../../visual-basic/language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="c4b42-199"><xref:System.Array.GetUpperBound%2A> Yöntemi boyut belirten bir parametre vardır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-199">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="c4b42-200">`GetUpperBound(0)`ilk boyut, en yüksek dizinini döndürür ve `GetUpperBound(1)` ikinci boyutu en yüksek dizinini döndürür.</span><span class="sxs-lookup"><span data-stu-id="c4b42-200">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>
  
 [!code-vb[iterate-two-dimensional-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]  
  
 <span data-ttu-id="c4b42-201">Aşağıdaki örnek kullanan bir [For Each... Sonraki deyim](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)tek boyutlu dizi ve iki boyutlu bir dizi aracılığıyla yinelemek için.</span><span class="sxs-lookup"><span data-stu-id="c4b42-201">The following example uses a [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>  
  
 [!code-vb[iterate-for-each-next](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]  
  
## <a name="array-size"></a><span data-ttu-id="c4b42-202">Dizi boyutu</span><span class="sxs-lookup"><span data-stu-id="c4b42-202">Array Size</span></span>  

 <span data-ttu-id="c4b42-203">Bir dizi tüm boyutlar uzunluklarının ürün boyutudur.</span><span class="sxs-lookup"><span data-stu-id="c4b42-203">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="c4b42-204">Şu anda dizisinde bulunan öğeleri toplam sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c4b42-204">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="c4b42-205">Örneğin, aşağıdaki örnekte, her boyut dört öğelerle 2 boyutlu bir dizi bildirir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-205">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="c4b42-206">Örneğin çıktısını gösterildiği gibi dizinin boyutu 16'dır (veya (3 + 1) \* (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="c4b42-206">As the output from the example shows, the array's size is 16 (or (3 + 1) \* (3 + 1).</span></span>

 [!code-vb[array-size](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]  

> [!NOTE] 
> <span data-ttu-id="c4b42-207">Bu tartışma dizi boyutunun basit diziler için geçerli değildir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-207">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="c4b42-208">Basit diziler ve basit bir dizi boyutunu belirleme hakkında daha fazla bilgi için bkz: [basit dizileri](#jagged-arrays) bölümü.</span><span class="sxs-lookup"><span data-stu-id="c4b42-208">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>
  
  <span data-ttu-id="c4b42-209">Kullanarak bir dizinin boyutu bulabilirsiniz <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliği.</span><span class="sxs-lookup"><span data-stu-id="c4b42-209">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="c4b42-210">Kullanarak her boyut bir çok boyutlu dizi uzunluğu bulabilirsiniz <xref:System.Array.GetLength%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-210">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>  
  
 <span data-ttu-id="c4b42-211">Bir dizi değişkeni kullanarak veya ona yeni bir dizi nesnesi atayarak boyutlandırabilirsiniz [ `ReDim` deyimi](../../../../visual-basic/language-reference/statements/redim-statement.md) deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-211">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../../visual-basic/language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="c4b42-212">Aşağıdaki örnek kullanır `ReDim` 100 öğeli bir dizi 51 öğeli bir dizi değiştirmek için deyimi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-212">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

 [!code-vb[resize-an-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]  
  
 <span data-ttu-id="c4b42-213">Bir dizi boyutu ile ilgilenirken göz önünde bulundurmanız gereken birkaç nokta vardır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-213">There are several things to keep in mind when dealing with the size of an array.</span></span>  
  
|||  
|---|---|  
|<span data-ttu-id="c4b42-214">Boyut uzunluğu</span><span class="sxs-lookup"><span data-stu-id="c4b42-214">Dimension Length</span></span>|<span data-ttu-id="c4b42-215">Her boyut dizin 0 tabanlı kendi üst sınırı 0'dan aralıkları anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-215">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="c4b42-216">Bu nedenle, belirli bir boyutun uzunluğu bu boyut bildirilen üst sınırdan büyük biridir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-216">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|  
|<span data-ttu-id="c4b42-217">Uzunluğu sınırları</span><span class="sxs-lookup"><span data-stu-id="c4b42-217">Length Limits</span></span>|<span data-ttu-id="c4b42-218">Bir dizinin her boyutun uzunluğu için en büyük değerini sınırlıdır `Integer` olan veri türü <xref:System.Int32.MaxValue?displayProperty=nameWithType> veya (2 ^ 31) - 1.</span><span class="sxs-lookup"><span data-stu-id="c4b42-218">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="c4b42-219">Ancak, bir dizi toplam boyutu da sisteminizdeki kullanılabilir bellek sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-219">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="c4b42-220">Bir dizi başlatmaya çalışırsanız, kullanılabilir bellek miktarını aşıyor, çalışma zamanı oluşturur bir <xref:System.OutOfMemoryException>.</span><span class="sxs-lookup"><span data-stu-id="c4b42-220">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|  
|<span data-ttu-id="c4b42-221">Boyutu ve öğesi boyutu</span><span class="sxs-lookup"><span data-stu-id="c4b42-221">Size and Element Size</span></span>|<span data-ttu-id="c4b42-222">Bir dizinin boyut öğelerinden veri türünü bağımsızdır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-222">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="c4b42-223">Boyut her zaman öğeleri, değil bellekte tüketen bayt sayısı toplam sayısını temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c4b42-223">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|  
|<span data-ttu-id="c4b42-224">Bellek tüketimi</span><span class="sxs-lookup"><span data-stu-id="c4b42-224">Memory Consumption</span></span>|<span data-ttu-id="c4b42-225">Bir dizi bellekte nasıl depolandığını ilgili tüm varsayımlarda güvenli değildir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-225">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="c4b42-226">Aynı dizi bir 32 bit sistemde 64-bit sistemde daha fazla bellek tüketebileceği şekilde depolama farklı veri genişliği platformlarda değişir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-226">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="c4b42-227">Bir dizi başlattığınızda sistem yapılandırmasına bağlı olarak ortak dil çalışma zamanı (CLR) depolama öğeleri olarak birbirine yakın mümkün olduğunca paketi veya tüm doğal donanım sınırları hizalamak için atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-227">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="c4b42-228">Ayrıca, bir dizi bir depolama ek yükü için denetim bilgilerini gerektirir ve bu ek yükü ile eklenen her boyut artırır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-228">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|  

## <a name="the-array-type"></a><span data-ttu-id="c4b42-229">Dizi türü</span><span class="sxs-lookup"><span data-stu-id="c4b42-229">The array type</span></span> 
 <span data-ttu-id="c4b42-230">Her dizi öğeleri veri türünden farklı bir veri türü var.</span><span class="sxs-lookup"><span data-stu-id="c4b42-230">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="c4b42-231">Tüm dizileri için tek bir veri türü yok.</span><span class="sxs-lookup"><span data-stu-id="c4b42-231">There is no single data type for all arrays.</span></span> <span data-ttu-id="c4b42-232">Bunun yerine, bir dizi veri türü boyutları sayısı tarafından belirlenir veya *derece*, dizi ve dizideki öğeler veri türü.</span><span class="sxs-lookup"><span data-stu-id="c4b42-232">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="c4b42-233">İki dizi değişkenleri yalnızca zaman aynı derece sahip oldukları yazın ve öğeleri aynı veri türüne sahip aynı verilerin kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-233">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="c4b42-234">Bir dizi boyutları uzunlukları dizi veri türü etkilemek değil.</span><span class="sxs-lookup"><span data-stu-id="c4b42-234">The lengths of the dimensions of an array do not influence the array data type.</span></span>  
  
 <span data-ttu-id="c4b42-235">Her dizi devraldığı <xref:System.Array?displayProperty=nameWithType> sınıfı ve türünde olması için bir değişken bildirebilirsiniz `Array`, ancak türünde bir dizi oluşturamazsınız `Array`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-235">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="c4b42-236">Örneğin, aşağıdaki kod bildirir rağmen `arr` türünde olması için değişken `Array` ve çağırır <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> dizi, dizinin türü örneği için yöntemi kanıtlar olmasını Object [].</span><span class="sxs-lookup"><span data-stu-id="c4b42-236">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

 [!code-vb[array-class](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)] 

<span data-ttu-id="c4b42-237">Ayrıca, [ReDim deyimi](../../../../visual-basic/language-reference/statements/redim-statement.md) türü olarak bildirilen bir değişken çalışamaz `Array`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-237">Also, the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="c4b42-238">Bu nedenlerle ve tür güvenliği için belirli bir tür olarak her diziyi bildirmek için önerilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-238">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>  
  
 <span data-ttu-id="c4b42-239">Bir dizi veya öğeleri çeşitli şekillerde veri türünü bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-239">You can find out the data type of either an array or its elements in several ways.</span></span> 
  
-   <span data-ttu-id="c4b42-240">Çağırabilirsiniz <xref:System.Object.GetType%2A> yöntemi almak için değişken bir <xref:System.Type> değişkeni çalışma zamanı türünü temsil eden nesne.</span><span class="sxs-lookup"><span data-stu-id="c4b42-240">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="c4b42-241"><xref:System.Type> Nesnesi özelliklerini ve yöntemlerini kapsamlı bilgileri tutar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-241">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>  
  
-   <span data-ttu-id="c4b42-242">Değişkene geçirebilirsiniz <xref:Microsoft.VisualBasic.Information.TypeName%2A> almak için işlevi bir `String` çalışma zamanı tür adı.</span><span class="sxs-lookup"><span data-stu-id="c4b42-242">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>  
  
 <span data-ttu-id="c4b42-243">Aşağıdaki örnek hem çağırır `GetType` yöntemi ve `TypeName` işlevi bir dizi türü belirlenemedi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-243">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="c4b42-244">Dizi türü `Byte(,)`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-244">The array type is `Byte(,)`.</span></span> <span data-ttu-id="c4b42-245">Unutmayın <xref:System.Type.BaseType%2A?displayProperty=nameWithType> özelliği de bayt dizisi temel türü olduğunu gösterir <xref:System.Array> sınıfı.</span><span class="sxs-lookup"><span data-stu-id="c4b42-245">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>  
  
 [!code-vb[array-type](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]  
  
##  <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="c4b42-246">Dönüş değerleri ve parametre olarak diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-246">Arrays as return values and parameters</span></span>  
 <span data-ttu-id="c4b42-247">Dizisinden döndürmek için bir `Function` yordamı, dizi veri türü ve dimensions sayısı dönüş türünü belirtmeniz [Function deyimi](../../../../visual-basic/language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-247">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../../visual-basic/language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="c4b42-248">İşlev içinde aynı veri türüne ve dimensions sayısı ile bir yerel dizi değişken bildirin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-248">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="c4b42-249">İçinde [dönüş deyimi](../../../../visual-basic/language-reference/statements/return-statement.md), parantezler olmadan yerel dizi değişken içerir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-249">In the [Return Statement](../../../../visual-basic/language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>  
  
 <span data-ttu-id="c4b42-250">Bir parametre olarak bir dizi belirtmek için bir `Sub` veya `Function` yordamı, belirtilen veri türü ve boyut sayısını içeren bir dizi olarak parametre tanımlayın.</span><span class="sxs-lookup"><span data-stu-id="c4b42-250">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="c4b42-251">Yordam çağrısında dimensions sayısı ve aynı veri türüne sahip bir dizi değişken geçirin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-251">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>  
  
 <span data-ttu-id="c4b42-252">Aşağıdaki örnekte, `GetNumbers` işlev döndürür bir `Integer()`, tek boyutlu dizi türü `Integer`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-252">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="c4b42-253">`ShowNumbers` Yordamı kabul eden bir `Integer()` bağımsız değişkeni.</span><span class="sxs-lookup"><span data-stu-id="c4b42-253">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span> 
  
 [!code-vb[return-value-and-params](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]  
  
 <span data-ttu-id="c4b42-254">Aşağıdaki örnekte, `GetNumbersMultiDim` işlev döndürür bir `Integer(,)`, iki boyutlu bir dizi türü `Integer`.</span><span class="sxs-lookup"><span data-stu-id="c4b42-254">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="c4b42-255">`ShowNumbersMultiDim` Yordamı kabul eden bir `Integer(,)` bağımsız değişkeni.</span><span class="sxs-lookup"><span data-stu-id="c4b42-255">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>  
  
 [!code-vb[multidimensional-return-value](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]  
  
## <a name="jagged-arrays"></a><span data-ttu-id="c4b42-256">Basit Diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-256">Jagged Arrays</span></span>  
 
<span data-ttu-id="c4b42-257">Bazen veri uygulamanızda iki boyutlu ancak dikdörtgen yapısıdır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-257">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="c4b42-258">Örneğin, her ayın yüksek sıcaklık hakkındaki verileri depolamak için bir dizi kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-258">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="c4b42-259">Dizinin ilk boyutu ay temsil eder, ancak ikinci boyut gün sayısını temsil eder ve bir ay içindeki gün sayısını Tekdüzen değil.</span><span class="sxs-lookup"><span data-stu-id="c4b42-259">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="c4b42-260">A *Basit dizi*, ayrıca adlandırılan bir *dizi dizisi*, bu tür senaryoları için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-260">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="c4b42-261">Basit bir dizi öğeleri de diziler olan bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-261">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="c4b42-262">Basit dizi ve basit bir dizideki her öğe bir veya daha fazla boyutları sahip olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-262">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>  
  
 <span data-ttu-id="c4b42-263">Aşağıdaki örnek, ay, her öğeye gün dizisidir dizisi kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-263">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="c4b42-264">Farklı ay gün sayıları farklı olduğundan bu örnek basit bir dizi kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-264">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="c4b42-265">Örneğin, basit bir dizi oluşturmak, değerler, atayın ve alabilir ve değerlerini görüntülemek gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-265">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>
  
 [!code-vb[jagged-arrays](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]  

<span data-ttu-id="c4b42-266">Önceki örnek değerler Basit dizi öğesi öğeli düzenli olarak kullanarak atayan bir `For...Next` döngü.</span><span class="sxs-lookup"><span data-stu-id="c4b42-266">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="c4b42-267">İç içe geçmiş dizi değişmez değerleri kullanarak basit bir dizi öğelerine değerler de atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-267">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="c4b42-268">Ancak, kullanma girişimi iç içe geçmiş dizi değişmez değerleri (örneğin, ```Dim valuesjagged = {{1, 2}, {2, 3, 4}}```) derleyici hatası oluşturur [BC30568](../../../,,/../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-268">However, the attempt to use nested array literals (for example, ```Dim valuesjagged = {{1, 2}, {2, 3, 4}}```) generates compiler error [BC30568](../../../,,/../misc/bc30568.md).</span></span> <span data-ttu-id="c4b42-269">Hatayı düzeltmek için iç dizi değişmez değerleri parantez içine alın.</span><span class="sxs-lookup"><span data-stu-id="c4b42-269">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="c4b42-270">Değerlendirilecek dizi değişmez değer ifadesi parantez zorlamak ve aşağıdaki örnekte gösterildiği gibi sonuçta elde edilen değerleri dış dizi ile kullanılır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-270">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>  
  
 [!code-vb[jagged-array-initialization](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)] 

<span data-ttu-id="c4b42-271">Basit bir dizi diziler öğeleri içeren tek boyutlu bir dizidir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-271">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="c4b42-272">Bu nedenle, <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliği ve `Array.GetLength(0)` yöntemi tek boyutlu diziye öğe sayısını döndürür ve `Array.GetLength(1)` oluşturur bir <xref:System.IndexOutOfRangeException> basit bir dizi çok boyutlu olmadığından.</span><span class="sxs-lookup"><span data-stu-id="c4b42-272">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="c4b42-273">Her subarray's değerini alarak her subarray öğelerin sayısını belirlemek <xref:System.Array.Length%2A?displayProperty=nameWithType> özelliği.</span><span class="sxs-lookup"><span data-stu-id="c4b42-273">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="c4b42-274">Aşağıdaki örnekte basit bir dizide öğe sayısını belirlemek nasıl gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-274">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)] 

## <a name="zero-length-arrays"></a><span data-ttu-id="c4b42-275">Sıfır uzunlukta diziler</span><span class="sxs-lookup"><span data-stu-id="c4b42-275">Zero-length arrays</span></span>  
<span data-ttu-id="c4b42-276">Visual Basic ayıran arasında başlatılmamış bir dizi (değeri olan bir dizi `Nothing`) ve bir *sıfır uzunluk dizisi* veya boş dizi (herhangi bir öğesi olan bir dizi.) Başlatılmamış bir dizi değil dimensioned bir ya da herhangi bir değeri atanmış vardı.</span><span class="sxs-lookup"><span data-stu-id="c4b42-276">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="c4b42-277">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="c4b42-277">For example:</span></span>

```vb
Dim arr() As String
```
<span data-ttu-id="c4b42-278">Sıfır uzunluklu dizi -1 olan bir boyut ile bildirilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-278">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="c4b42-279">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="c4b42-279">For example:</span></span>

```vb
Dim arrZ(-1) As String
```
<span data-ttu-id="c4b42-280">Sıfır uzunluklu dizi aşağıdaki koşullarda oluşturmanız gerekebilir:</span><span class="sxs-lookup"><span data-stu-id="c4b42-280">You might need to create a zero-length array under the following circumstances:</span></span>  
  
-   <span data-ttu-id="c4b42-281">Riski olmadan bir <xref:System.NullReferenceException> özel durum, kodunuzu gerekir üyelerine erişme <xref:System.Array> gibi sınıfı <xref:System.Array.Length%2A> veya <xref:System.Array.Rank%2A>, veya Visual Basic işlevi gibi arama <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span><span class="sxs-lookup"><span data-stu-id="c4b42-281">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>  
  
-   <span data-ttu-id="c4b42-282">Korumak istediğiniz kodunuzu denetlemek zorunluluğunu ortadan kaldırarak basit `Nothing` bir özel durum olarak.</span><span class="sxs-lookup"><span data-stu-id="c4b42-282">You want to keep the your code simple by not having to check for `Nothing` as a special case.</span></span>  
  
-   <span data-ttu-id="c4b42-283">Kodunuzu sıfır uzunluk dizisi için bir veya daha fazla yordamları geçirmeniz gerekir ya da bir veya daha fazla yordamlardan sıfır uzunlukta bir dizi döndürür bir uygulama programlama arabirimi (API) ile etkileşim kurar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-283">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="c4b42-284">Bir dizi bölme</span><span class="sxs-lookup"><span data-stu-id="c4b42-284">Splitting an array</span></span>

<span data-ttu-id="c4b42-285">Bazı durumlarda, tek bir dizi birden çok diziye bölme gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-285">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="c4b42-286">Bu, noktası veya noktaları Bölünecek dizi olan tanımlama ve sonra iki veya daha fazla ayrı diziye dizi yollanmasından içerir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-286">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span> 

> [!NOTE] 
> <span data-ttu-id="c4b42-287">Bu bölümde bazı sınırlayıcıyı kullanarak bir dize dizisi içine tek bir dize bölme açıklanmamıştır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-287">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="c4b42-288">Bir dize bölme hakkında daha fazla bilgi için bkz: <xref:System.String.Split%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-288">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="c4b42-289">Bir dizi bölme en yaygın ölçütlerini şunlardır:</span><span class="sxs-lookup"><span data-stu-id="c4b42-289">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="c4b42-290">Dizideki öğelerin sayısı</span><span class="sxs-lookup"><span data-stu-id="c4b42-290">The number of elements in the array.</span></span> <span data-ttu-id="c4b42-291">Örneğin, birden çok belirtilen sayıda öğeyi bir dizi yaklaşık eşit parçalar sayıya bölme isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-291">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="c4b42-292">Bu amaçla tarafından döndürülen değer kullanabilirsiniz <xref:System.Array.Length%2A?displayProperty=nameWithType> veya <xref:System.Array.GetLength%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-292">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="c4b42-293">Dizi bölme yeri gösteren ayırıcı olarak hizmet veren bir öğenin değeri.</span><span class="sxs-lookup"><span data-stu-id="c4b42-293">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="c4b42-294">Belirli bir değeri çağırarak arayabilirsiniz <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> ve <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> yöntemleri.</span><span class="sxs-lookup"><span data-stu-id="c4b42-294">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>
 
<span data-ttu-id="c4b42-295">Dizin veya dizinleri, dizi bölme saptadıktan sonra daha sonra ayrı diziler çağırarak oluşturabilirsiniz <xref:System.Array.Copy%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-295">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span> 

<span data-ttu-id="c4b42-296">Aşağıdaki örnek, bir dizi yaklaşık eşit boyutu iki diziye ayırır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-296">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="c4b42-297">(Dizi öğelerinin toplam sayı tek ise ilk dizi ikinciden daha fazla öğe var.)</span><span class="sxs-lookup"><span data-stu-id="c4b42-297">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span> 

[!code-vb[splitting-an-array-by-length](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)] 

<span data-ttu-id="c4b42-298">Aşağıdaki örnek dizilerinin dizi ayırıcı olarak hizmet veren "zzz" değeri olan bir öğeyi varlığına dayalı bir dize dizisi ayıran.</span><span class="sxs-lookup"><span data-stu-id="c4b42-298">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="c4b42-299">Yeni diziler sınırlayıcısı içeriyor öğesi dahil etmeyin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-299">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)] 

## <a name="joining-arrays"></a><span data-ttu-id="c4b42-300">Diziler birleştirme</span><span class="sxs-lookup"><span data-stu-id="c4b42-300">Joining arrays</span></span>

<span data-ttu-id="c4b42-301">Ayrıca, tek bir büyük dizi dizileri sayısı birleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-301">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="c4b42-302">Bunu yapmak için ayrıca kullandığınız <xref:System.Array.Copy%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-302">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span> 

> [!NOTE] 
> <span data-ttu-id="c4b42-303">Bu bölümde, tek bir dize halinde bir dize dizisi birleştirme açıklanmamıştır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-303">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="c4b42-304">Bir dize dizisi birleştirme hakkında daha fazla bilgi için bkz: <xref:System.String.Join%2A?displayProperty=nameWithType> yöntemi.</span><span class="sxs-lookup"><span data-stu-id="c4b42-304">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="c4b42-305">Her dizinin öğeleri yeni diziye kopyalamadan önce böylece yeni bir dizi accompodate için yeterince büyük dizi ayarladıktan ilk sağlamalısınız.</span><span class="sxs-lookup"><span data-stu-id="c4b42-305">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accompodate the new array.</span></span> <span data-ttu-id="c4b42-306">Bunu iki yoldan biriyle yapabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="c4b42-306">You can do this in one of two ways:</span></span>

- <span data-ttu-id="c4b42-307">Kullanım [ `ReDim Preserve` ](../../../../visual-basic/language-reference/statements/redim-statement.md) deyimi dinamik olarak yeni öğeler eklemeden önce dizi genişletin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-307">Use the [`ReDim Preserve`](../../../../visual-basic/language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="c4b42-308">Bu kolay tekniği olan, ancak büyük diziler kopyalarken, performans düşüşünü ve aşırı bellek kullanımına neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-308">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="c4b42-309">Yeni bir büyük dizi için gereken öğeleri toplam sayısını hesaplayın sonra her kaynak dizinin öğeleri ekleyin.</span><span class="sxs-lookup"><span data-stu-id="c4b42-309">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="c4b42-310">Aşağıdaki örnek, tek bir dizi on öğeleri dört dizilerle eklemek için ikinci yaklaşımı kullanır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-310">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>  

[!code-vb[joining-an-array](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)] 

<span data-ttu-id="c4b42-311">Bu durumda kaynak diziler tüm küçük olduğundan, biz her yeni bir dizi öğelerini eklemek gibi biz de dinamik olarak dizi genişletebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-311">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="c4b42-312">Aşağıdaki örnek, yapar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-312">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](../../../../../samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)] 

##  <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="c4b42-313">Diziler için bir alternatif olarak koleksiyonları</span><span class="sxs-lookup"><span data-stu-id="c4b42-313">Collections as an alternative to arrays</span></span>  
 <span data-ttu-id="c4b42-314">Diziler oluşturmak ve güçlü şekilde yazılan nesnelerin sabit sayıda ile çalışmak için çok kullanışlıdır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-314">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="c4b42-315">Koleksiyonları nesneleri grupları ile çalışmak için daha esnek bir şekilde sağlar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-315">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="c4b42-316">Diziler gerektiren açıkça bir dizi boyutunu değiştirme [ `ReDim` deyimi](../../../../visual-basic/language-reference/statements/redim-statement.md), koleksiyonları büyür ve dinamik olarak bir uygulama değişikliği ihtiyaçları geliştikçe daraltma.</span><span class="sxs-lookup"><span data-stu-id="c4b42-316">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../../visual-basic/language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>  
  
 <span data-ttu-id="c4b42-317">Kullandığınızda `ReDim` bir dizi redimension için Visual Basic yeni bir dizi oluşturur ve önceki serbest bırakır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-317">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="c4b42-318">Bu, yürütme sürüyor.</span><span class="sxs-lookup"><span data-stu-id="c4b42-318">This takes execution time.</span></span> <span data-ttu-id="c4b42-319">Sık değişiklikler veya çalıştığınız öğe sayısı ihtiyacınız öğe maksimum sayısı tahmin edilemez, bu nedenle, genellikle daha iyi performans koleksiyonu kullanarak elde.</span><span class="sxs-lookup"><span data-stu-id="c4b42-319">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>  
  
 <span data-ttu-id="c4b42-320">Bazı koleksiyonlar için nesne anahtarı kullanarak hızlı bir şekilde alabilmeleri koleksiyona koymak nesnesine bir anahtar atayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-320">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>  
  
 <span data-ttu-id="c4b42-321">Koleksiyonunuz yalnızca bir veri türündeki öğeler içeriyorsa, sınıflarda birini kullanabilirsiniz <xref:System.Collections.Generic?displayProperty=nameWithType> ad alanı.</span><span class="sxs-lookup"><span data-stu-id="c4b42-321">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="c4b42-322">Böylece başka bir veri türü eklenebilir bir genel koleksiyon tür güvenliği zorlar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-322">A generic collection enforces type safety so that no other data type can be added to it.</span></span>  
  
 <span data-ttu-id="c4b42-323">Koleksiyonlar hakkında daha fazla bilgi için bkz: [koleksiyonları](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="c4b42-323">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>
  
## <a name="related-topics"></a><span data-ttu-id="c4b42-324">İlgili Konular</span><span class="sxs-lookup"><span data-stu-id="c4b42-324">Related Topics</span></span>  
  
|<span data-ttu-id="c4b42-325">Terim</span><span class="sxs-lookup"><span data-stu-id="c4b42-325">Term</span></span>|<span data-ttu-id="c4b42-326">Tanım</span><span class="sxs-lookup"><span data-stu-id="c4b42-326">Definition</span></span>|  
|----------|----------------|  
|[<span data-ttu-id="c4b42-327">Visual Basic'de dizi boyutları</span><span class="sxs-lookup"><span data-stu-id="c4b42-327">Array Dimensions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md)|<span data-ttu-id="c4b42-328">Rank ve boyutları dizilerde açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-328">Explains rank and dimensions in arrays.</span></span>|  
|[<span data-ttu-id="c4b42-329">Nasıl yapılır: Visual Basic'te dizi değişkeni başlatma</span><span class="sxs-lookup"><span data-stu-id="c4b42-329">How to: Initialize an Array Variable in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md)|<span data-ttu-id="c4b42-330">Dizilerle ilk değerleri doldurmak açıklar.</span><span class="sxs-lookup"><span data-stu-id="c4b42-330">Describes how to populate arrays with initial values.</span></span>|  
|[<span data-ttu-id="c4b42-331">Nasıl yapılır: Visual Basic'te bir dizi sıralama</span><span class="sxs-lookup"><span data-stu-id="c4b42-331">How to: Sort An Array in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-sort-an-array.md)|<span data-ttu-id="c4b42-332">Dizi öğelerini alfabetik olarak sıralamak gösterilmiştir.</span><span class="sxs-lookup"><span data-stu-id="c4b42-332">Shows how to sort the elements of an array alphabetically.</span></span>|  
|[<span data-ttu-id="c4b42-333">Nasıl yapılır: bir diziyi başka diziye atama</span><span class="sxs-lookup"><span data-stu-id="c4b42-333">How to: Assign One Array to Another Array</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="c4b42-334">Kuralları ve bir dizi başka bir dizi değişkenine atamak için adımlar açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-334">Describes the rules and steps for assigning an array to another array variable.</span></span>|  
|[<span data-ttu-id="c4b42-335">Dizilerle ilgili sorun giderme</span><span class="sxs-lookup"><span data-stu-id="c4b42-335">Troubleshooting Arrays</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/troubleshooting-arrays.md)|<span data-ttu-id="c4b42-336">Dizilerle çalışırken ortaya bazı yaygın sorunlar ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="c4b42-336">Discusses some common problems that arise when working with arrays.</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="c4b42-337">Ayrıca Bkz.</span><span class="sxs-lookup"><span data-stu-id="c4b42-337">See Also</span></span>  
 <xref:System.Array?displayProperty=nameWithType>  
 [<span data-ttu-id="c4b42-338">Dim deyimi</span><span class="sxs-lookup"><span data-stu-id="c4b42-338">Dim Statement</span></span>](../../../../visual-basic/language-reference/statements/dim-statement.md)  
 [<span data-ttu-id="c4b42-339">ReDim deyimi</span><span class="sxs-lookup"><span data-stu-id="c4b42-339">ReDim Statement</span></span>](../../../../visual-basic/language-reference/statements/redim-statement.md)
