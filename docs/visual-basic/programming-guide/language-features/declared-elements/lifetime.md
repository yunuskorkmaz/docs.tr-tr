---
title: Visual Basic'de Ömür
ms.date: 07/20/2015
helpviewer_keywords:
- static variables [Visual Basic], lifetime
- static variables [Visual Basic], Visual Basic
- declared elements [Visual Basic], lifetime
- Shared variable lifetime [Visual Basic]
- lifetime [Visual Basic], declared elements
- lifetime [Visual Basic], Visual Basic
- lifetime [Visual Basic]
ms.assetid: bd91e390-690a-469a-9946-8dca70bc14e7
ms.openlocfilehash: 15fb6895836de95d37081c44973f455622da178e
ms.sourcegitcommit: 40364ded04fa6cdcb2b6beca7f68412e2e12f633
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/28/2019
ms.locfileid: "56976050"
---
# <a name="lifetime-in-visual-basic"></a><span data-ttu-id="c3d2e-102">Visual Basic'de Ömür</span><span class="sxs-lookup"><span data-stu-id="c3d2e-102">Lifetime in Visual Basic</span></span>
<span data-ttu-id="c3d2e-103">*Ömrü* bir bildirilen süre sırasında hangi BT kullanıma açık öğesidir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-103">The *lifetime* of a declared element is the period of time during which it is available for use.</span></span> <span data-ttu-id="c3d2e-104">Değişkenleri ömürlü yalnızca öğelerdir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-104">Variables are the only elements that have lifetime.</span></span> <span data-ttu-id="c3d2e-105">Bu amaçla, derleyici parametreler değerlendirir ve değişkenlerin özel durumlar işlevi döndürür.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-105">For this purpose, the compiler treats procedure parameters and function returns as special cases of variables.</span></span> <span data-ttu-id="c3d2e-106">Bir değişkenin ömrü değeri biriken içerebileceği süreyi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-106">The lifetime of a variable represents the period of time during which it can hold a value.</span></span> <span data-ttu-id="c3d2e-107">Yaşam süresi boyunca değerini değiştirebilirsiniz, ancak her zaman belirli bir değeri tutar.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-107">Its value can change over its lifetime, but it always holds some value.</span></span>  
  
## <a name="different-lifetimes"></a><span data-ttu-id="c3d2e-108">Farklı bir yaşam süresi yok</span><span class="sxs-lookup"><span data-stu-id="c3d2e-108">Different Lifetimes</span></span>  
 <span data-ttu-id="c3d2e-109">A *üye değişkeni* (dışında her türlü yordam, Modül düzeyinde bildirilmiş) genellikle bunu bildirilen öğe olarak aynı kullanım ömrü vardır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-109">A *member variable* (declared at module level, outside any procedure) typically has the same lifetime as the element in which it is declared.</span></span> <span data-ttu-id="c3d2e-110">Bir sınıf veya yapı içinde bildirilen bir paylaşılmayan değişken sınıf veya yapı içinde bildirildiği her örneği için ayrı bir kopya olarak mevcut.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-110">A nonshared variable declared in a class or structure exists as a separate copy for each instance of the class or structure in which it is declared.</span></span> <span data-ttu-id="c3d2e-111">Her bir değişken, örnek olarak aynı kullanım ömrü vardır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-111">Each such variable has the same lifetime as its instance.</span></span> <span data-ttu-id="c3d2e-112">Ancak, bir `Shared` değişkeni, uygulamanızı çalıştıran tüm zamanı sürer yalnızca bir tek ömür, sahip.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-112">However, a `Shared` variable has only a single lifetime, which lasts for the entire time your application is running.</span></span>  
  
 <span data-ttu-id="c3d2e-113">A *yerel değişken* (bir yordam içinde bildirilen) yalnızca onu bildirilen yordamı çalışırken mevcut.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-113">A *local variable* (declared inside a procedure) exists only while the procedure in which it is declared is running.</span></span> <span data-ttu-id="c3d2e-114">Bu da bu yordamın parametreleri ve dönüş herhangi bir işlev için de geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-114">This applies also to that procedure's parameters and to any function return.</span></span> <span data-ttu-id="c3d2e-115">Ancak, bu yordamı diğer yordamlar çağırırsa, çağrılan yordamları çalışırken yerel değişkenlerin değerlerini korur.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-115">However, if that procedure calls other procedures, the local variables retain their values while the called procedures are running.</span></span>  
  
## <a name="beginning-of-lifetime"></a><span data-ttu-id="c3d2e-116">Etkin kalma süresi başlangıcı</span><span class="sxs-lookup"><span data-stu-id="c3d2e-116">Beginning of Lifetime</span></span>  
 <span data-ttu-id="c3d2e-117">Yerel değişkenin ömrü içinde bildirildiği yordamı denetime girdiğinde başlar.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-117">A local variable's lifetime begins when control enters the procedure in which it is declared.</span></span> <span data-ttu-id="c3d2e-118">Yordamı başlar başlamaz her yerel değişken veri türü için varsayılan değer başlatılır çalışıyor.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-118">Every local variable is initialized to the default value for its data type as soon as the procedure begins running.</span></span> <span data-ttu-id="c3d2e-119">Yordamı karşılaştığında bir `Dim` başlangıç değerleri belirten bir deyimi ayarlar değişkenlere bu değerleri, kodunuzu zaten diğer değerleri atanmış bile.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-119">When the procedure encounters a `Dim` statement that specifies initial values, it sets those variables to those values, even if your code had already assigned other values to them.</span></span>  
  
 <span data-ttu-id="c3d2e-120">Ayrı bir değişken gibi bir yapı değişken her üyesi başlatıldı.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-120">Each member of a structure variable is initialized as if it were a separate variable.</span></span> <span data-ttu-id="c3d2e-121">Benzer şekilde, bir dizi değişkeni her öğe ayrı ayrı başlatılır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-121">Similarly, each element of an array variable is initialized individually.</span></span>  
  
 <span data-ttu-id="c3d2e-122">Bir blok içinde bir yordam içinde tanımlanan değişkenleri (gibi bir `For` döngü) girişi yordama başlatılır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-122">Variables declared within a block inside a procedure (such as a `For` loop) are initialized on entry to the procedure.</span></span> <span data-ttu-id="c3d2e-123">Kodunuzu şimdiye kadar bloğun olup olmadığına bakılmaksızın bu başlatmalar geçerli olur.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-123">These initializations take effect whether or not your code ever executes the block.</span></span>  
  
## <a name="end-of-lifetime"></a><span data-ttu-id="c3d2e-124">Yaşam süresi sonu</span><span class="sxs-lookup"><span data-stu-id="c3d2e-124">End of Lifetime</span></span>  
 <span data-ttu-id="c3d2e-125">Bir yordam sonlandığında, kendi yerel değişkenlerin değerleri korunmaz ve Visual Basic kullandığı belleği geri kazanır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-125">When a procedure terminates, the values of its local variables are not preserved, and Visual Basic reclaims their memory.</span></span> <span data-ttu-id="c3d2e-126">Yordamı, bir sonraki çağırışınızda tüm yerel değişkenlerin parçalarından oluşturulur ve yeniden başlatıldı.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-126">The next time you call the procedure, all its local variables are created afresh and reinitialized.</span></span>  
  
 <span data-ttu-id="c3d2e-127">Bir sınıfın veya yapının örneği sonlandığında, kendi paylaşılmayan değişkenler, bellek ve bunların değerlerini kaybedersiniz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-127">When an instance of a class or structure terminates, its nonshared variables lose their memory and their values.</span></span> <span data-ttu-id="c3d2e-128">Her yeni bir sınıf veya yapının örneğini oluşturur ve kendi paylaşılmayan değişkenler yeniden başlatır.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-128">Each new instance of the class or structure creates and reinitializes its nonshared variables.</span></span> <span data-ttu-id="c3d2e-129">Ancak, `Shared` değişkenleri uygulamanızın çalışmayı kadar korunur.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-129">However, `Shared` variables are preserved until your application stops running.</span></span>  
  
## <a name="extension-of-lifetime"></a><span data-ttu-id="c3d2e-130">Yaşam süresi uzantısı</span><span class="sxs-lookup"><span data-stu-id="c3d2e-130">Extension of Lifetime</span></span>  
 <span data-ttu-id="c3d2e-131">Yerel bir değişken bildirirseniz `Static` anahtar sözcüğü, yaşam süresi, kendi yordamı yürütme süreden daha uzun.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-131">If you declare a local variable with the `Static` keyword, its lifetime is longer than the execution time of its procedure.</span></span> <span data-ttu-id="c3d2e-132">Aşağıdaki tabloda yordam bildirimi ne kadar süreyle nasıl belirlediğini gösterir bir `Static` değişkeni yok.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-132">The following table shows how the procedure declaration determines how long a `Static` variable exists.</span></span>  
  
|<span data-ttu-id="c3d2e-133">Yordam konumu ve paylaşma</span><span class="sxs-lookup"><span data-stu-id="c3d2e-133">Procedure location and sharing</span></span>|<span data-ttu-id="c3d2e-134">Statik değişken ömrü başlar</span><span class="sxs-lookup"><span data-stu-id="c3d2e-134">Static variable lifetime begins</span></span>|<span data-ttu-id="c3d2e-135">Statik değişken ömrü sona erer</span><span class="sxs-lookup"><span data-stu-id="c3d2e-135">Static variable lifetime ends</span></span>|  
|------------------------------------|-------------------------------------|-----------------------------------|  
|<span data-ttu-id="c3d2e-136">Bir modüle (varsayılan olarak paylaşılan)</span><span class="sxs-lookup"><span data-stu-id="c3d2e-136">In a module (shared by default)</span></span>|<span data-ttu-id="c3d2e-137">İlk kez yordamı çağrılır</span><span class="sxs-lookup"><span data-stu-id="c3d2e-137">The first time the procedure is called</span></span>|<span data-ttu-id="c3d2e-138">Uygulamanızın çalışmayı durdurduğunda</span><span class="sxs-lookup"><span data-stu-id="c3d2e-138">When your application stops running</span></span>|  
|<span data-ttu-id="c3d2e-139">Bir sınıftaki `Shared` (yordamı bir örnek üyesi değildir)</span><span class="sxs-lookup"><span data-stu-id="c3d2e-139">In a class, `Shared` (procedure is not an instance member)</span></span>|<span data-ttu-id="c3d2e-140">İlk kez yordamı belirli bir örneğine veya sınıf veya yapı adı kendisi üzerinde çağrılır</span><span class="sxs-lookup"><span data-stu-id="c3d2e-140">The first time the procedure is called either on a specific instance or on the class or structure name itself</span></span>|<span data-ttu-id="c3d2e-141">Uygulamanızın çalışmayı durdurduğunda</span><span class="sxs-lookup"><span data-stu-id="c3d2e-141">When your application stops running</span></span>|  
|<span data-ttu-id="c3d2e-142">İçinde bir sınıfın bir örneği değil `Shared` (yordamı, bir örnek üyesi olduğu)</span><span class="sxs-lookup"><span data-stu-id="c3d2e-142">In an instance of a class, not `Shared` (procedure is an instance member)</span></span>|<span data-ttu-id="c3d2e-143">İlk kez yordamı belirli bir örneği üzerinde çağrılır</span><span class="sxs-lookup"><span data-stu-id="c3d2e-143">The first time the procedure is called on the specific instance</span></span>|<span data-ttu-id="c3d2e-144">Çöp toplama (GC) örneği yayınlandığında</span><span class="sxs-lookup"><span data-stu-id="c3d2e-144">When the instance is released for garbage collection (GC)</span></span>|  
  
## <a name="static-variables-of-the-same-name"></a><span data-ttu-id="c3d2e-145">Statik değişkenler aynı ada sahip</span><span class="sxs-lookup"><span data-stu-id="c3d2e-145">Static Variables of the Same Name</span></span>  
 <span data-ttu-id="c3d2e-146">Statik değişkenler aynı adla birden fazla yordam bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-146">You can declare static variables with the same name in more than one procedure.</span></span> <span data-ttu-id="c3d2e-147">Bunu yaparsanız, Visual Basic Derleyicisi ayrı bir öğe gibi her bir değişken olarak değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-147">If you do this, the Visual Basic compiler considers each such variable to be a separate element.</span></span> <span data-ttu-id="c3d2e-148">Bu değişkenlerin birinin başlatma diğer değerlerini etkilemez.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-148">The initialization of one of these variables does not affect the values of the others.</span></span> <span data-ttu-id="c3d2e-149">Bir yordamı aşırı sayıda tanımlayın ve her aşırı yükleme aynı ada sahip bir statik değişken bildirimini durumunda da aynısı geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-149">The same applies if you define a procedure with a set of overloads and declare a static variable with the same name in each overload.</span></span>  
  
## <a name="containing-elements-for-static-variables"></a><span data-ttu-id="c3d2e-150">Statik değişkenler için öğeleri içeren</span><span class="sxs-lookup"><span data-stu-id="c3d2e-150">Containing Elements for Static Variables</span></span>  
 <span data-ttu-id="c3d2e-151">Bu sınıfta bir yordam içinde statik bir yerel değişken bir sınıf içindeki diğer bir deyişle, bildirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-151">You can declare a static local variable within a class, that is, inside a procedure in that class.</span></span> <span data-ttu-id="c3d2e-152">Ancak, yapı üyesi veya bir yordam, yapı içinde yerel bir değişken olarak bir yapı içinde statik bir yerel değişken bildiremezsiniz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-152">However, you cannot declare a static local variable within a structure, either as a structure member or as a local variable of a procedure within that structure.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c3d2e-153">Örnek</span><span class="sxs-lookup"><span data-stu-id="c3d2e-153">Example</span></span>  
  
### <a name="description"></a><span data-ttu-id="c3d2e-154">Açıklama</span><span class="sxs-lookup"><span data-stu-id="c3d2e-154">Description</span></span>  
 <span data-ttu-id="c3d2e-155">Aşağıdaki örnek, bir değişken bildirir [statik](../../../../visual-basic/language-reference/modifiers/static.md) anahtar sözcüğü.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-155">The following example declares a variable with the [Static](../../../../visual-basic/language-reference/modifiers/static.md) keyword.</span></span> <span data-ttu-id="c3d2e-156">(Gerekmeyen Not `Dim` anahtar sözcüğü, [Dim deyimi](../../../../visual-basic/language-reference/statements/dim-statement.md) gibi bir değiştirici kullanan `Static`.)</span><span class="sxs-lookup"><span data-stu-id="c3d2e-156">(Note that you do not need the `Dim` keyword when the [Dim Statement](../../../../visual-basic/language-reference/statements/dim-statement.md) uses a modifier such as `Static`.)</span></span>  
  
### <a name="code"></a><span data-ttu-id="c3d2e-157">Kod</span><span class="sxs-lookup"><span data-stu-id="c3d2e-157">Code</span></span>  
 [!code-vb[VbVbalrKeywords#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrKeywords/VB/class7.vb#13)]  
  
### <a name="comments"></a><span data-ttu-id="c3d2e-158">Açıklamalar</span><span class="sxs-lookup"><span data-stu-id="c3d2e-158">Comments</span></span>  
 <span data-ttu-id="c3d2e-159">Yukarıdaki örnekte, değişken `applesSold` yordam sonra varolmaya devam eder `runningTotal` çağrıldığı koda döndürür.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-159">In the preceding example, the variable `applesSold` continues to exist after the procedure `runningTotal` returns to the calling code.</span></span> <span data-ttu-id="c3d2e-160">Sonraki `runningTotal` çağrıldığında `applesSold` önceden hesaplanmış değerini korur.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-160">The next time `runningTotal` is called, `applesSold` retains its previously calculated value.</span></span>  
  
 <span data-ttu-id="c3d2e-161">Varsa `applesSold` olmadan bildirilmiş `Static`, önceki birikmiş değerleri çağrılar üzerinden korunamadı `runningTotal`.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-161">If `applesSold` had been declared without using `Static`, the previous accumulated values would not be preserved across calls to `runningTotal`.</span></span> <span data-ttu-id="c3d2e-162">Sonraki `runningTotal` çağrıldı, `applesSold` alınan yeniden oluşturulması ve 0 olarak başlatılır ve `runningTotal` yalnızca aynı değeri ile bunu çağrıldı döndüğünüzü.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-162">The next time `runningTotal` was called, `applesSold` would have been recreated and initialized to 0, and `runningTotal` would have simply returned the same value with which it was called.</span></span>  
  
### <a name="compiling-the-code"></a><span data-ttu-id="c3d2e-163">Kod Derleniyor</span><span class="sxs-lookup"><span data-stu-id="c3d2e-163">Compiling the Code</span></span>  
 <span data-ttu-id="c3d2e-164">Bildiriminin bir parçası olarak bir statik yerel değişkenin değerini yeniden başlatabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-164">You can initialize the value of a static local variable as part of its declaration.</span></span> <span data-ttu-id="c3d2e-165">Bir dizi olarak bildirirseniz `Static`, başlatabilir, rank (boyut sayısı), tek tek öğelerin değerlerinin yanı sıra her boyutun uzunluğu.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-165">If you declare an array to be `Static`, you can initialize its rank (number of dimensions), the length of each dimension, and the values of the individual elements.</span></span>  
  
### <a name="security"></a><span data-ttu-id="c3d2e-166">Güvenlik</span><span class="sxs-lookup"><span data-stu-id="c3d2e-166">Security</span></span>  
 <span data-ttu-id="c3d2e-167">Önceki örnekte, aynı yaşam bildirerek üretebilir `applesSold` Modül düzeyinde.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-167">In the preceding example, you can produce the same lifetime by declaring `applesSold` at module level.</span></span> <span data-ttu-id="c3d2e-168">Ancak, bu şekilde bir değişkenin kapsamını değiştirdiyseniz, yordam artık bu özel kullanım erişimi yoktur.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-168">If you changed the scope of a variable this way, however, the procedure would no longer have exclusive access to it.</span></span> <span data-ttu-id="c3d2e-169">Diğer yordamlar erişemiyor çünkü `applesSold` ve değerini değiştirmek, değişen toplamı güvenilir olabilir ve kod korumak daha zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-169">Because other procedures could access `applesSold` and change its value, the running total could be unreliable and the code could be more difficult to maintain.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c3d2e-170">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="c3d2e-170">See also</span></span>
- [<span data-ttu-id="c3d2e-171">Shared</span><span class="sxs-lookup"><span data-stu-id="c3d2e-171">Shared</span></span>](../../../../visual-basic/language-reference/modifiers/shared.md)
- [<span data-ttu-id="c3d2e-172">Nothing</span><span class="sxs-lookup"><span data-stu-id="c3d2e-172">Nothing</span></span>](../../../../visual-basic/language-reference/nothing.md)
- [<span data-ttu-id="c3d2e-173">Bildirilen Öğe Adları</span><span class="sxs-lookup"><span data-stu-id="c3d2e-173">Declared Element Names</span></span>](../../../../visual-basic/programming-guide/language-features/declared-elements/declared-element-names.md)
- [<span data-ttu-id="c3d2e-174">Bildirilmiş Öğelere Başvurular</span><span class="sxs-lookup"><span data-stu-id="c3d2e-174">References to Declared Elements</span></span>](../../../../visual-basic/programming-guide/language-features/declared-elements/references-to-declared-elements.md)
- [<span data-ttu-id="c3d2e-175">Visual Basic'de kapsam</span><span class="sxs-lookup"><span data-stu-id="c3d2e-175">Scope in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/declared-elements/scope.md)
- [<span data-ttu-id="c3d2e-176">Visual Basic'de erişim düzeyleri</span><span class="sxs-lookup"><span data-stu-id="c3d2e-176">Access levels in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/declared-elements/access-levels.md)
- [<span data-ttu-id="c3d2e-177">Değişkenler</span><span class="sxs-lookup"><span data-stu-id="c3d2e-177">Variables</span></span>](../../../../visual-basic/programming-guide/language-features/variables/index.md)
- [<span data-ttu-id="c3d2e-178">Değişken Bildirimi</span><span class="sxs-lookup"><span data-stu-id="c3d2e-178">Variable Declaration</span></span>](../../../../visual-basic/programming-guide/language-features/variables/variable-declaration.md)
- [<span data-ttu-id="c3d2e-179">Veri Türü Sorunlarını Giderme</span><span class="sxs-lookup"><span data-stu-id="c3d2e-179">Troubleshooting Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/troubleshooting-data-types.md)
- [<span data-ttu-id="c3d2e-180">Static</span><span class="sxs-lookup"><span data-stu-id="c3d2e-180">Static</span></span>](../../../../visual-basic/language-reference/modifiers/static.md)
