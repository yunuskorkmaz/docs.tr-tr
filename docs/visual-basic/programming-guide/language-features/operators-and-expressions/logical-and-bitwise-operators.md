---
title: Visual Basic'de Mantıksal ve Bit Düzeyinde İşleçler
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 40076b2ad6606b4c565bcd39dbeea9e55da47211
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69963311"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="75998-102">Visual Basic'de Mantıksal ve Bit Düzeyinde İşleçler</span><span class="sxs-lookup"><span data-stu-id="75998-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="75998-103">Mantıksal işleçler ifadeleri `Boolean` karşılaştırır ve bir `Boolean` sonuç döndürür.</span><span class="sxs-lookup"><span data-stu-id="75998-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="75998-104">`And` ,`OrElse` ,,`Not` Ve işleçleri iki işlenen kullandığından, tek bir işlenen aldığı için işleç tekil olduğu için ikili bir dosyaysa. `Xor` `Or` `AndAlso`</span><span class="sxs-lookup"><span data-stu-id="75998-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="75998-105">Bu işleçlerden bazıları, tam sayı değerlerinde bit düzeyinde mantıksal işlemler de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="75998-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="75998-106">Birli mantıksal Işleç</span><span class="sxs-lookup"><span data-stu-id="75998-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="75998-107">[Not işleci](../../../../visual-basic/language-reference/operators/not-operator.md) bir `Boolean` ifadede mantıksal *değilleme* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="75998-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="75998-108">İşleneni, işleneninin mantıksal tersini verir.</span><span class="sxs-lookup"><span data-stu-id="75998-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="75998-109">İfade `True`olarak değerlendirilirse `Not` `Not` , `False`, ifadesi olarak `False`değerlendirilirse, öğesinidöndürür.`True`</span><span class="sxs-lookup"><span data-stu-id="75998-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="75998-110">Aşağıdaki örnek bunu göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="75998-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="75998-111">İkili mantıksal Işleçler</span><span class="sxs-lookup"><span data-stu-id="75998-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="75998-112">[And işleci](../../../../visual-basic/language-reference/operators/and-operator.md) iki `Boolean` ifade üzerinde mantıksal bir *birlikte* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="75998-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="75998-113">Her iki ifade olarak `True`değerlendirilir `And` , öğesini döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="75998-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="75998-114">Deyimlerden en az biri olarak `False`değerlendirilirse `And` , öğesini döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="75998-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="75998-115">[OR işleci](../../../../visual-basic/language-reference/operators/or-operator.md) , mantıksal *ayırıcı* veya iki `Boolean` ifadeye *dahil* etme işlemini gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="75998-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="75998-116">İki ifade olarak `True`değerlendirilirse veya her ikisi de `Or` olarak `True`değerlendirilirse, öğesini döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="75998-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="75998-117">Hiçbir ifadenin olarak `True`değerlendiriliyorsa, `Or` döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="75998-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="75998-118">[Xor işleci](../../../../visual-basic/language-reference/operators/xor-operator.md) iki `Boolean` ifadeye mantıksal *dışlama* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="75998-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="75998-119">Tam olarak bir ifade olarak değerlendirilir `True`, ancak `Xor` her ikisini de değil `True`, döndürür.</span><span class="sxs-lookup"><span data-stu-id="75998-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="75998-120">Her iki ifade de olarak `True` değerlendirilir veya her ikisini `False`de `Xor` olarak `False`değerlendirir, döndürür.</span><span class="sxs-lookup"><span data-stu-id="75998-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="75998-121">Aşağıdaki örnekte `And`, `Or`, ve `Xor` işleçleri gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="75998-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="75998-122">Kısa devre mantıksal Işlemler yürütülüyor</span><span class="sxs-lookup"><span data-stu-id="75998-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="75998-123">[AndAlso işleci](../../../../visual-basic/language-reference/operators/andalso-operator.md) , `And` işleçle çok benzerdir, bu da iki `Boolean` ifadeye de mantıksal bir işlem yapar.</span><span class="sxs-lookup"><span data-stu-id="75998-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="75998-124">İkisi arasındaki temel fark, *kısa* devre uygulayan `AndAlso` davranışı gösteriyor.</span><span class="sxs-lookup"><span data-stu-id="75998-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="75998-125">Bir `AndAlso` ifadedeki ilk ifade olarak değerlendirilirse, ikinci ifade `False`nihai sonucu değiştirmediği ve `AndAlso` döndürdüğü `False`için değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="75998-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="75998-126">Benzer şekilde, [OrElse işleci](../../../../visual-basic/language-reference/operators/orelse-operator.md) iki `Boolean` ifadeye kısa devre uygulayan mantıksal bir birleşim uygular.</span><span class="sxs-lookup"><span data-stu-id="75998-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="75998-127">Bir `OrElse` ifadedeki ilk ifade olarak değerlendirilirse, ikinci ifade `True`nihai sonucu değiştirmediği ve `OrElse` döndürdüğü `True`için değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="75998-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="75998-128">Kısa süreli bir denge</span><span class="sxs-lookup"><span data-stu-id="75998-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="75998-129">Kısa devre uygulayan, mantıksal işlemin sonucunu değiştirebilecek bir ifadeyi değerlendirmeden performansı iyileştirebilir.</span><span class="sxs-lookup"><span data-stu-id="75998-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="75998-130">Ancak, bu ifade ek eylemler gerçekleştirdiğinde, kısa devre uygulayan bu eylemleri atlar.</span><span class="sxs-lookup"><span data-stu-id="75998-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="75998-131">Örneğin, ifade bir `Function` yordama çağrı içeriyorsa, ifade kısa devre dışı ise ve `Function` içinde yer alan ek kodlar çalıştırılsa, bu yordam çağrılmaz.</span><span class="sxs-lookup"><span data-stu-id="75998-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="75998-132">Bu nedenle, işlev yalnızca belirli bir zaman çalışabilir ve doğru şekilde sınanmayabilir.</span><span class="sxs-lookup"><span data-stu-id="75998-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="75998-133">Ya da program mantığı içindeki `Function`koda bağlı olabilirler.</span><span class="sxs-lookup"><span data-stu-id="75998-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="75998-134">Aşağıdaki örnek, `Or`, ve bunların kısa `And`devre dışı karşılıkları arasındaki farkı gösterir.</span><span class="sxs-lookup"><span data-stu-id="75998-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="75998-135">Yukarıdaki örnekte, çağrı kısa devre dışı olduğunda, içindeki `checkIfValid()` bazı önemli kodların çalıştırılmadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="75998-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="75998-136">`If` İlk ifade çağrı `checkIfValid()` yapmaz `12 > 45` ,`And`ancak kısa devre dışı değildir. `False`</span><span class="sxs-lookup"><span data-stu-id="75998-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="75998-137">İkinci `If` deyim çağırmaz `checkIfValid()` ,çünkü`12 > 45` döndürür `False`, kısadevrelerikinciifadedir.`AndAlso`</span><span class="sxs-lookup"><span data-stu-id="75998-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="75998-138">Kısa devre `If` olmadığından, `checkIfValid()` `12 < 45` Üçüncüifade`True`çağrıları döndürür. `Or`</span><span class="sxs-lookup"><span data-stu-id="75998-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="75998-139">Dördüncü `If` deyim çağrı `checkIfValid()`yapmaz `12 < 45` ,çünkü`True`döndürür, kısadevrelerikinciifadedir.`OrElse`</span><span class="sxs-lookup"><span data-stu-id="75998-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="75998-140">Bit düzeyinde Işlemler</span><span class="sxs-lookup"><span data-stu-id="75998-140">Bitwise Operations</span></span>  
 <span data-ttu-id="75998-141">Bit düzeyinde işlemler ikili (taban 2) formda iki integral değeri değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="75998-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="75998-142">Bunlara karşılık gelen konumlarda bitleri karşılaştırırlar ve sonra karşılaştırmaya göre değerler atar.</span><span class="sxs-lookup"><span data-stu-id="75998-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="75998-143">Aşağıdaki örnekte `And` işleç gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="75998-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="75998-144">Önceki örnek değerini `x` 1 olarak ayarlar.</span><span class="sxs-lookup"><span data-stu-id="75998-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="75998-145">Bu durum aşağıdaki nedenlerden dolayı gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="75998-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="75998-146">Değerler ikili olarak değerlendirilir:</span><span class="sxs-lookup"><span data-stu-id="75998-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="75998-147">3 ikili biçiminde 3 = 011</span><span class="sxs-lookup"><span data-stu-id="75998-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="75998-148">5 ikili biçimi = 101</span><span class="sxs-lookup"><span data-stu-id="75998-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="75998-149">`And` İşleci, tek seferde bir ikili konum (bit) ikili gösterimlerini karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="75998-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="75998-150">Belirli bir konumdaki bitlerin her ikisi de 1 ise, sonuçta bu konuma bir 1 konur.</span><span class="sxs-lookup"><span data-stu-id="75998-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="75998-151">Her iki bit de 0 ise, sonuçta bu konuma 0 yerleştirilir.</span><span class="sxs-lookup"><span data-stu-id="75998-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="75998-152">Yukarıdaki örnekte bu, aşağıdaki gibi çalışmaktadır:</span><span class="sxs-lookup"><span data-stu-id="75998-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="75998-153">011 (ikili biçimde 3)</span><span class="sxs-lookup"><span data-stu-id="75998-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="75998-154">101 (ikili biçimde 5)</span><span class="sxs-lookup"><span data-stu-id="75998-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="75998-155">001 (sonuç, ikili biçimde)</span><span class="sxs-lookup"><span data-stu-id="75998-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="75998-156">Sonuç ondalık olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="75998-156">The result is treated as decimal.</span></span> <span data-ttu-id="75998-157">001 değeri 1, bu nedenle `x` = 1 ' in ikili gösterimidir.</span><span class="sxs-lookup"><span data-stu-id="75998-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="75998-158">Bit düzeyinde `Or` işlem benzerdir, ancak karşılaştırılan bitlerin biri veya her ikisi 1 ise sonuç bitine 1 atanır.</span><span class="sxs-lookup"><span data-stu-id="75998-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="75998-159">`Xor`karşılaştırılan bitlerin (her ikisi de değil) tam olarak biri 1 ise, sonuç bitini 1 atar.</span><span class="sxs-lookup"><span data-stu-id="75998-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="75998-160">`Not`tek bir işlenen alır ve işaret biti dahil olmak üzere tüm bitleri ters çevirir ve bu değeri sonuca atar.</span><span class="sxs-lookup"><span data-stu-id="75998-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="75998-161">Bu, `Not` işaretli pozitif sayıların her zaman negatif bir değer döndüğü ve negatif `Not` sayıların her zaman pozitif veya sıfır değer döndürdüğü anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="75998-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="75998-162">`AndAlso` Ve`OrElse` işleçleri bit düzeyinde işlemleri desteklemez.</span><span class="sxs-lookup"><span data-stu-id="75998-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="75998-163">Bit düzeyinde işlemler yalnızca integral türlerinde gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="75998-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="75998-164">Bit düzeyinde işlemin devam edebilmesi için kayan nokta değerlerinin tamsayı türlerine dönüştürülmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="75998-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="75998-165">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="75998-165">See also</span></span>

- [<span data-ttu-id="75998-166">Mantıksal/bit düzeyinde Işleçler (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="75998-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="75998-167">Boole İfadeleri</span><span class="sxs-lookup"><span data-stu-id="75998-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="75998-168">Visual Basic aritmetik Işleçler</span><span class="sxs-lookup"><span data-stu-id="75998-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="75998-169">Visual Basic karşılaştırma Işleçleri</span><span class="sxs-lookup"><span data-stu-id="75998-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="75998-170">Visual Basic birleştirme Işleçleri</span><span class="sxs-lookup"><span data-stu-id="75998-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="75998-171">İşleçlerin Etkili Bileşimi</span><span class="sxs-lookup"><span data-stu-id="75998-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
