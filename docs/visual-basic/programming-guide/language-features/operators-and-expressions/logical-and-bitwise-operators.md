---
title: Mantıksal ve Bit Düzeyinde İşleçler
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 55a246c0d56501a409ebbc7d0d0aa39ae9fa1770
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74343600"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="df9f2-102">Visual Basic'de Mantıksal ve Bit Düzeyinde İşleçler</span><span class="sxs-lookup"><span data-stu-id="df9f2-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="df9f2-103">Mantıksal işleçler `Boolean` ifadelerini karşılaştırır ve `Boolean` sonucunu döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="df9f2-104">`And`, `Or`, `AndAlso`, `OrElse`ve `Xor` işleçleri *ikili bir dosya* olsa da, tek bir işlenen aldığı için `Not` işleci *birli* olur.</span><span class="sxs-lookup"><span data-stu-id="df9f2-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="df9f2-105">Bu işleçlerden bazıları, tam sayı değerlerinde bit düzeyinde mantıksal işlemler de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="df9f2-106">Birli mantıksal Işleç</span><span class="sxs-lookup"><span data-stu-id="df9f2-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="df9f2-107">[Not işleci](../../../../visual-basic/language-reference/operators/not-operator.md) `Boolean` ifadesinde mantıksal *değilleme* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="df9f2-108">İşleneni, işleneninin mantıksal tersini verir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="df9f2-109">İfade `True`olarak değerlendirilirse `Not` `False`döndürür; ifade `False`olarak değerlendirilirse `Not` `True`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="df9f2-110">Aşağıdaki örnek bunu göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="df9f2-111">İkili mantıksal Işleçler</span><span class="sxs-lookup"><span data-stu-id="df9f2-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="df9f2-112">[And işleci](../../../../visual-basic/language-reference/operators/and-operator.md) iki `Boolean` ifadelerinde mantıksal bir *birlikte* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="df9f2-113">Her iki ifade de `True`olarak değerlendirilir `And` `True`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="df9f2-114">Deyimlerden en az biri `False`değerlendirilirse `And` `False`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="df9f2-115">[OR işleci](../../../../visual-basic/language-reference/operators/or-operator.md) , mantıksal *ayırıcı* veya iki `Boolean` ifadeye *dahil* etme işlemini gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="df9f2-116">Her iki ifade de `True`olarak değerlendirilir veya `True`her ikisi de `Or` `True`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="df9f2-117">Hiçbir ifadenin `True`olarak değerlendiriliyorsa `Or` `False`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="df9f2-118">[Xor işleci](../../../../visual-basic/language-reference/operators/xor-operator.md) iki `Boolean` ifadelerinde mantıksal *dışlama* gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="df9f2-119">Tam olarak bir ifade `True`olarak değerlendirilir, ancak her ikisini birden değil `Xor` `True`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="df9f2-120">Her iki ifade de `True` veya her ikisi de `False`olarak değerlendirilir `Xor` `False`döndürür.</span><span class="sxs-lookup"><span data-stu-id="df9f2-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="df9f2-121">Aşağıdaki örnekte `And`, `Or`ve `Xor` işleçleri gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="df9f2-122">Kısa devre mantıksal Işlemler yürütülüyor</span><span class="sxs-lookup"><span data-stu-id="df9f2-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="df9f2-123">[AndAlso işleci](../../../../visual-basic/language-reference/operators/andalso-operator.md) `And` işlecine çok benzer ve ayrıca iki `Boolean` ifadesi üzerinde mantıksal bir işlem gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="df9f2-124">İkisi arasındaki temel fark, `AndAlso` *kısa* devre davranýþý davranışını gösteriyor.</span><span class="sxs-lookup"><span data-stu-id="df9f2-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="df9f2-125">`AndAlso` ifadesindeki ilk ifade `False`olarak değerlendirilirse, ikinci ifade, nihai sonucu değiştirmediği ve `AndAlso` `False`döndürdüğü için değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="df9f2-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="df9f2-126">Benzer şekilde, [OrElse işleci](../../../../visual-basic/language-reference/operators/orelse-operator.md) iki `Boolean` ifadeye kısa devre uygulayan mantıksal bir birleşim gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="df9f2-127">`OrElse` ifadesindeki ilk ifade `True`olarak değerlendirilirse, ikinci ifade, nihai sonucu değiştirmediği ve `OrElse` `True`döndürdüğü için değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="df9f2-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="df9f2-128">Kısa süreli bir denge</span><span class="sxs-lookup"><span data-stu-id="df9f2-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="df9f2-129">Kısa devre uygulayan, mantıksal işlemin sonucunu değiştirebilecek bir ifadeyi değerlendirmeden performansı iyileştirebilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="df9f2-130">Ancak, bu ifade ek eylemler gerçekleştirdiğinde, kısa devre uygulayan bu eylemleri atlar.</span><span class="sxs-lookup"><span data-stu-id="df9f2-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="df9f2-131">Örneğin, ifade bir `Function` yordamına çağrı içeriyorsa, ifade kısa devre dışı ise ve `Function` dahil olan ek kodlar çalıştırılmadığından Bu yordam çağrılmaz.</span><span class="sxs-lookup"><span data-stu-id="df9f2-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="df9f2-132">Bu nedenle, işlev yalnızca belirli bir zaman çalışabilir ve doğru şekilde sınanmayabilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="df9f2-133">Ya da program mantığı `Function`koda bağlı olarak değişebilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="df9f2-134">Aşağıdaki örnek, `And`, `Or`ve bunların kısa devre dışı karşılıkları arasındaki farkı gösterir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="df9f2-135">Yukarıdaki örnekte, çağrı kısa devre dışı olduğunda `checkIfValid()` içindeki bazı önemli kodun çalıştırılmadığını unutmayın.</span><span class="sxs-lookup"><span data-stu-id="df9f2-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="df9f2-136">İlk `If` ifade, `And` kısa devre olmadığından `12 > 45` `False`döndürse bile `checkIfValid()` çağırır.</span><span class="sxs-lookup"><span data-stu-id="df9f2-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="df9f2-137">İkinci `If` deyimi `checkIfValid()`çağırmaz; çünkü `12 > 45` `False`döndürdüğünde, `AndAlso` kısa devreler ikinci ifadedir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="df9f2-138">`Or` kısa devre olmadığından, üçüncü `If` ifade `12 < 45` `True`döndürse bile `checkIfValid()` çağırır.</span><span class="sxs-lookup"><span data-stu-id="df9f2-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="df9f2-139">Dördüncü `If` deyimi, `12 < 45` `True`döndürdüğünde, `OrElse` kısa devreden ikinci ifadeye `checkIfValid()`çağırmaz.</span><span class="sxs-lookup"><span data-stu-id="df9f2-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="df9f2-140">Bit düzeyinde Işlemler</span><span class="sxs-lookup"><span data-stu-id="df9f2-140">Bitwise Operations</span></span>  
 <span data-ttu-id="df9f2-141">Bit düzeyinde işlemler ikili (taban 2) formda iki integral değeri değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="df9f2-142">Bunlara karşılık gelen konumlarda bitleri karşılaştırırlar ve sonra karşılaştırmaya göre değerler atar.</span><span class="sxs-lookup"><span data-stu-id="df9f2-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="df9f2-143">Aşağıdaki örnekte `And` işleci gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="df9f2-144">Yukarıdaki örnek `x` değerini 1 olarak ayarlar.</span><span class="sxs-lookup"><span data-stu-id="df9f2-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="df9f2-145">Bu durum aşağıdaki nedenlerden dolayı gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="df9f2-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="df9f2-146">Değerler ikili olarak değerlendirilir:</span><span class="sxs-lookup"><span data-stu-id="df9f2-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="df9f2-147">3 ikili biçiminde 3 = 011</span><span class="sxs-lookup"><span data-stu-id="df9f2-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="df9f2-148">5 ikili biçimi = 101</span><span class="sxs-lookup"><span data-stu-id="df9f2-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="df9f2-149">`And` işleci, tek seferde bir ikili konum (bit) ikili gösterimlerini karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="df9f2-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="df9f2-150">Belirli bir konumdaki bitlerin her ikisi de 1 ise, sonuçta bu konuma bir 1 konur.</span><span class="sxs-lookup"><span data-stu-id="df9f2-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="df9f2-151">Her iki bit de 0 ise, sonuçta bu konuma 0 yerleştirilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="df9f2-152">Yukarıdaki örnekte bu, aşağıdaki gibi çalışmaktadır:</span><span class="sxs-lookup"><span data-stu-id="df9f2-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="df9f2-153">011 (ikili biçimde 3)</span><span class="sxs-lookup"><span data-stu-id="df9f2-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="df9f2-154">101 (ikili biçimde 5)</span><span class="sxs-lookup"><span data-stu-id="df9f2-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="df9f2-155">001 (sonuç, ikili biçimde)</span><span class="sxs-lookup"><span data-stu-id="df9f2-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="df9f2-156">Sonuç ondalık olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-156">The result is treated as decimal.</span></span> <span data-ttu-id="df9f2-157">001 değeri 1 ' in ikili gösterimidir, bu nedenle `x` = 1 ' dir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="df9f2-158">Bit düzeyinde `Or` işlem benzerdir, ancak karşılaştırılan bitlerin biri veya her ikisi 1 ise sonuç bitine 1 atanır.</span><span class="sxs-lookup"><span data-stu-id="df9f2-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="df9f2-159">`Xor`, karşılaştırılan bitlerin (her ikisi de değil) tam olarak biri 1 olduğunda sonuç bitini 1 atar.</span><span class="sxs-lookup"><span data-stu-id="df9f2-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="df9f2-160">`Not` tek bir işlenen alır ve işaret biti dahil olmak üzere tüm bitleri ters çevirir ve bu değeri sonuca atar.</span><span class="sxs-lookup"><span data-stu-id="df9f2-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="df9f2-161">Bu, `Not` her zaman negatif bir değer döndüren ve negatif sayılar için `Not` her zaman pozitif veya sıfır değeri döndüren anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="df9f2-162">`AndAlso` ve `OrElse` işleçleri bit düzeyinde işlemleri desteklemez.</span><span class="sxs-lookup"><span data-stu-id="df9f2-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="df9f2-163">Bit düzeyinde işlemler yalnızca integral türlerinde gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="df9f2-164">Bit düzeyinde işlemin devam edebilmesi için kayan nokta değerlerinin tamsayı türlerine dönüştürülmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="df9f2-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="df9f2-165">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="df9f2-165">See also</span></span>

- [<span data-ttu-id="df9f2-166">Mantıksal/bit düzeyinde Işleçler (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="df9f2-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="df9f2-167">Boole İfadeleri</span><span class="sxs-lookup"><span data-stu-id="df9f2-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="df9f2-168">Visual Basic aritmetik Işleçler</span><span class="sxs-lookup"><span data-stu-id="df9f2-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="df9f2-169">Visual Basic karşılaştırma Işleçleri</span><span class="sxs-lookup"><span data-stu-id="df9f2-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="df9f2-170">Visual Basic birleştirme Işleçleri</span><span class="sxs-lookup"><span data-stu-id="df9f2-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="df9f2-171">İşleçlerin Etkili Bileşimi</span><span class="sxs-lookup"><span data-stu-id="df9f2-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
