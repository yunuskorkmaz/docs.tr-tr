---
title: Visual Basic'de Mantıksal ve Bit Düzeyinde İşleçler
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 23f3758527b787551ad83cbd4e19076b788c9dd8
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/28/2019
ms.locfileid: "64649688"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="6de9a-102">Visual Basic'de Mantıksal ve Bit Düzeyinde İşleçler</span><span class="sxs-lookup"><span data-stu-id="6de9a-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="6de9a-103">Mantıksal işleçler karşılaştırma `Boolean` ifadeleri ve dönüş bir `Boolean` sonucu.</span><span class="sxs-lookup"><span data-stu-id="6de9a-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="6de9a-104">`And`, `Or`, `AndAlso`, `OrElse`, Ve `Xor` işleçleri *ikili* bunlar iki işlenenden tuttuğundan, while `Not` işleci *birli* tek bir işlenen aldığından.</span><span class="sxs-lookup"><span data-stu-id="6de9a-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="6de9a-105">Bu işleçlerden bazıları, tam sayı değerleri üzerinde bit düzeyinde mantıksal işlemleri de yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="6de9a-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="6de9a-106">Birli mantıksal işleç</span><span class="sxs-lookup"><span data-stu-id="6de9a-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="6de9a-107">[Not işleci](../../../../visual-basic/language-reference/operators/not-operator.md) mantıksal gerçekleştirir *olumsuzlama* üzerinde bir `Boolean` ifade.</span><span class="sxs-lookup"><span data-stu-id="6de9a-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="6de9a-108">Bu mantıksal tersidir işlenenin verir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="6de9a-109">İfade değerlendirme sonucu `True`, ardından `Not` döndürür `False`; ifadenin değerlendirdiği `False`, ardından `Not` döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="6de9a-110">Aşağıdaki örnek bunu göstermektedir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="6de9a-111">İkili mantıksal işleçler</span><span class="sxs-lookup"><span data-stu-id="6de9a-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="6de9a-112">[And işlecini](../../../../visual-basic/language-reference/operators/and-operator.md) mantıksal gerçekleştirir *birlikte* iki `Boolean` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="6de9a-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="6de9a-113">Her iki ifade sonucunu verirse `True`, ardından `And` döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="6de9a-114">En az bir ifade değerlendirme sonucu `False`, ardından `And` döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="6de9a-115">[Veya işleci](../../../../visual-basic/language-reference/operators/or-operator.md) mantıksal gerçekleştirir *ayrım* veya *ekleme* iki `Boolean` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="6de9a-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="6de9a-116">Her iki ifade değerlendirilirse `True`, veya her ikisi de olarak değerlendirilmesi `True`, ardından `Or` döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="6de9a-117">Her iki ifade değerlendirilirse `True`, `Or` döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="6de9a-118">[Xor işleci](../../../../visual-basic/language-reference/operators/xor-operator.md) mantıksal gerçekleştirir *dışlama* iki `Boolean` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="6de9a-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="6de9a-119">Tam olarak bir ifade değerlendirme sonucu `True`, iki değil, `Xor` döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="6de9a-120">Her iki ifade sonucunu verirse `True` veya her ikisi de olarak değerlendirilmesi `False`, `Xor` döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="6de9a-121">Aşağıdaki örnekte gösterilmiştir `And`, `Or`, ve `Xor` işleçleri.</span><span class="sxs-lookup"><span data-stu-id="6de9a-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="6de9a-122">Kısa devre mantıksal işlemleri</span><span class="sxs-lookup"><span data-stu-id="6de9a-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="6de9a-123">[AndAlso işleci](../../../../visual-basic/language-reference/operators/andalso-operator.md) çok benzer `And` işleci, ayrıca mantıksal ve işlecini iki gerçekleştirdiği, `Boolean` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="6de9a-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="6de9a-124">İkisi arasındaki temel fark `AndAlso` sergiler *kısa devre* davranışı.</span><span class="sxs-lookup"><span data-stu-id="6de9a-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="6de9a-125">İlk ifadesinde bir `AndAlso` ifadeyi hesaplar için `False`, sonra da nihai sonucu değiştirilemiyor çünkü ikinci ifade değerlendirilmez ve `AndAlso` döndürür `False`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="6de9a-126">Benzer şekilde, [OrElse işleci](../../../../visual-basic/language-reference/operators/orelse-operator.md) kısa devre mantıksal veya işlecini iki gerçekleştirir `Boolean` ifadeler.</span><span class="sxs-lookup"><span data-stu-id="6de9a-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="6de9a-127">İlk ifadesinde bir `OrElse` ifadeyi hesaplar için `True`, sonra da nihai sonucu değiştirilemiyor çünkü ikinci ifade değerlendirilmez ve `OrElse` döndürür `True`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="6de9a-128">Denge kısa devre</span><span class="sxs-lookup"><span data-stu-id="6de9a-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="6de9a-129">Kısa devre mantıksal işleminin sonucu değiştirilemez bir ifade değerlendirerek değil performansını iyileştirebilir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="6de9a-130">Ancak, bu ifade ek eylemler gerçekleştiriyorsa bu eylemlerin bir kısa devre atlar.</span><span class="sxs-lookup"><span data-stu-id="6de9a-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="6de9a-131">Örneğin, ifade için bir çağrı içeriyorsa bir `Function` yordamı, yordam varsa ifade kısa devre yapılma ve herhangi ek bir kod içindeki çağrılmaz `Function` çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="6de9a-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="6de9a-132">Bu nedenle, işlev nadiren çalışabilir ve doğru bir şekilde test edilecek değil.</span><span class="sxs-lookup"><span data-stu-id="6de9a-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="6de9a-133">Veya bir programın mantığı kodda bağımlı olabileceği `Function`.</span><span class="sxs-lookup"><span data-stu-id="6de9a-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="6de9a-134">Arasındaki fark aşağıdaki örnekte `And`, `Or`ve short-circuiting karşılıkları.</span><span class="sxs-lookup"><span data-stu-id="6de9a-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="6de9a-135">Unutmayın önceki örnekte, bazı önemli kod içinde `checkIfValid()` çağrı kısa devre yapılma olduğunda çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="6de9a-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="6de9a-136">İlk `If` deyim aramaları `checkIfValid()` olsa bile `12 > 45` döndürür `False`, çünkü `And` değil kısa devre oluşturur.</span><span class="sxs-lookup"><span data-stu-id="6de9a-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="6de9a-137">İkinci `If` deyimi çağırmaz `checkIfValid()`, çünkü zaman `12 > 45` döndürür `False`, `AndAlso` short-circuits ikinci ifade.</span><span class="sxs-lookup"><span data-stu-id="6de9a-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="6de9a-138">Üçüncü `If` deyim aramaları `checkIfValid()` olsa bile `12 < 45` döndürür `True`, çünkü `Or` değil kısa devre oluşturur.</span><span class="sxs-lookup"><span data-stu-id="6de9a-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="6de9a-139">Dördüncü `If` deyimi çağırmaz `checkIfValid()`, çünkü zaman `12 < 45` döndürür `True`, `OrElse` short-circuits ikinci ifade.</span><span class="sxs-lookup"><span data-stu-id="6de9a-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="6de9a-140">Bit düzeyinde işlemler</span><span class="sxs-lookup"><span data-stu-id="6de9a-140">Bitwise Operations</span></span>  
 <span data-ttu-id="6de9a-141">Bit düzeyinde işlemler (taban 2) ikili biçimde iki tamsayı değerlerini değerlendirin.</span><span class="sxs-lookup"><span data-stu-id="6de9a-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="6de9a-142">Bunlar, karşılık gelen konumlarda BITS karşılaştırın ve karşılaştırma üzerine dayalı değerler atayın.</span><span class="sxs-lookup"><span data-stu-id="6de9a-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="6de9a-143">Aşağıdaki örnekte gösterilmiştir `And` işleci.</span><span class="sxs-lookup"><span data-stu-id="6de9a-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="6de9a-144">Yukarıdaki örnekte ayarlar `x` 1.</span><span class="sxs-lookup"><span data-stu-id="6de9a-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="6de9a-145">Bu, aşağıdaki nedenlerden dolayı gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="6de9a-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="6de9a-146">Değerleri, ikili olarak kabul edilir:</span><span class="sxs-lookup"><span data-stu-id="6de9a-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="6de9a-147">3. ikili biçimini 011 =</span><span class="sxs-lookup"><span data-stu-id="6de9a-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="6de9a-148">5'te ikili biçimini 101 =</span><span class="sxs-lookup"><span data-stu-id="6de9a-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="6de9a-149">`And` İşleci, ikili temsili bir ikili konum (bit) aynı anda karşılaştırır.</span><span class="sxs-lookup"><span data-stu-id="6de9a-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="6de9a-150">Verilen konumda bitlerin her ikisi de 1, 1 sonuç o konuma yerleştirilir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="6de9a-151">Her iki bit 0 ise, 0 sonuç o konuma yerleştirilir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="6de9a-152">Önceki örnekte bu gibi işe:</span><span class="sxs-lookup"><span data-stu-id="6de9a-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="6de9a-153">011 (ikili biçimde 3)</span><span class="sxs-lookup"><span data-stu-id="6de9a-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="6de9a-154">101 (ikili biçimde 5)</span><span class="sxs-lookup"><span data-stu-id="6de9a-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="6de9a-155">001 (ikili biçimde sonuç)</span><span class="sxs-lookup"><span data-stu-id="6de9a-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="6de9a-156">Sonucun ondalık olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-156">The result is treated as decimal.</span></span> <span data-ttu-id="6de9a-157">001 değeri ikili 1, bu nedenle gösterimidir `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="6de9a-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="6de9a-158">Bit düzeyinde `Or` veya her ikisini karşılaştırılan bitleri 1 ise 1 sonuç bite atanır dışında işlemi benzer.</span><span class="sxs-lookup"><span data-stu-id="6de9a-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="6de9a-159">`Xor` Karşılaştırılan BITS (hem değil) tam olarak birine 1 ise 1 sonuç bite atar.</span><span class="sxs-lookup"><span data-stu-id="6de9a-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="6de9a-160">`Not` tek bir işlenen alan, imza biti dahil olmak üzere tüm bitleri tersine çevirir ve sonucu bu değeri atar.</span><span class="sxs-lookup"><span data-stu-id="6de9a-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="6de9a-161">Yani için pozitif bir sayı, imzalı `Not` her zaman negatif bir değer döndürür ve negatif sayılar için `Not` her zaman bir pozitif veya sıfır değeri döndürür.</span><span class="sxs-lookup"><span data-stu-id="6de9a-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="6de9a-162">`AndAlso` Ve `OrElse` işleçler bit düzeyinde işlemler desteklemez.</span><span class="sxs-lookup"><span data-stu-id="6de9a-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6de9a-163">Yalnızca integral türleri üzerinde bit düzeyinde işlemler gerçekleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="6de9a-164">Bit düzeyinde işlem devam etmeden önce kayan nokta değerleri integral türlerine dönüştürülmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="6de9a-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6de9a-165">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="6de9a-165">See also</span></span>

- [<span data-ttu-id="6de9a-166">Mantıksal/bit düzeyinde işleçler (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="6de9a-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="6de9a-167">Boole İfadeleri</span><span class="sxs-lookup"><span data-stu-id="6de9a-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="6de9a-168">Visual Basic'de aritmetik işleçler</span><span class="sxs-lookup"><span data-stu-id="6de9a-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="6de9a-169">Visual Basic'de Karşılaştırma işleçleri</span><span class="sxs-lookup"><span data-stu-id="6de9a-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="6de9a-170">Visual Basic'de birleştirme işleçleri</span><span class="sxs-lookup"><span data-stu-id="6de9a-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="6de9a-171">İşleçlerin Etkili Bileşimi</span><span class="sxs-lookup"><span data-stu-id="6de9a-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
