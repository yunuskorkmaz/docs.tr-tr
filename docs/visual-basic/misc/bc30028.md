---
title: "' #Else ' öncesinde eşleşen bir ' #If ' veya ' #ElseIf ' olmalıdır"
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: fee4a48a95aeb020a0f7e3c42a29812b9a0848e9
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84404089"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="bf23a-102">' #Else ' öncesinde eşleşen bir ' #If ' veya ' #ElseIf ' olmalıdır</span><span class="sxs-lookup"><span data-stu-id="bf23a-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>
<span data-ttu-id="bf23a-103">`#Else`, koşullu bir derleme yönergedir.</span><span class="sxs-lookup"><span data-stu-id="bf23a-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="bf23a-104">Bir `#Else` yönergeden önce karşılık gelen `#If` or yönergesi yoktur `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="bf23a-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="bf23a-105">**Hata kimliği:** BC30028</span><span class="sxs-lookup"><span data-stu-id="bf23a-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="bf23a-106">Bu hatayı düzeltmek için</span><span class="sxs-lookup"><span data-stu-id="bf23a-106">To correct this error</span></span>  
  
1. <span data-ttu-id="bf23a-107">Önünde `#If` veya `#ElseIf` Hatalı bir `#Else` koşullu derleme bloğunun veya yanlış yerleştirilmiş bir şekilde, bu veya ondan ayrılmadığından emin olun `#End If` .</span><span class="sxs-lookup"><span data-stu-id="bf23a-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2. <span data-ttu-id="bf23a-108">Daha `#Else` önce başka bir yönergenin olduğunu denetleyin `#Else` .</span><span class="sxs-lookup"><span data-stu-id="bf23a-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="bf23a-109">Varsa, kaldırın veya ' a `#Else` değiştirin `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="bf23a-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3. <span data-ttu-id="bf23a-110">Başka her şey sıralamayla, `#If` koşullu derleme bloğunun başlangıcına bir yönerge ekleyin.</span><span class="sxs-lookup"><span data-stu-id="bf23a-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bf23a-111">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="bf23a-111">See also</span></span>

- [<span data-ttu-id="bf23a-112">#If... Sonra... #Else yönergeler</span><span class="sxs-lookup"><span data-stu-id="bf23a-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
