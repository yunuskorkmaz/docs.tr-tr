---
title: <variablename>Döngü sınırları ve Step değişkeni aynı türe genişlemediğinden ' ' türü belirsiz
ms.date: 07/20/2015
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords:
- BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
ms.openlocfilehash: 2df86d167903fe6d1d0d38f68735f4e8b74b862f
ms.sourcegitcommit: bf5c5850654187705bc94cc40ebfb62fe346ab02
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 09/23/2020
ms.locfileid: "91101250"
---
# <a name="type-of-variablename-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="f7b2e-102">\<variablename>Döngü sınırları ve Step değişkeni aynı türe genişlemediğinden ' ' türü belirsiz</span><span class="sxs-lookup"><span data-stu-id="f7b2e-102">Type of '\<variablename>' is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>

<span data-ttu-id="f7b2e-103">Kodunuz, `For...Next` aşağıdaki koşullar doğru olduğu için derleyicinin döngü denetim değişkeni için bir veri türünü çıkarmadığı bir döngü içerir:</span><span class="sxs-lookup"><span data-stu-id="f7b2e-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
- <span data-ttu-id="f7b2e-104">Döngü denetim değişkeninin veri türü bir `As` yan tümcesiyle belirtilmemiş.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
- <span data-ttu-id="f7b2e-105">Döngü sınırları ve Step değişkeni en az iki veri türü içeriyor.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
- <span data-ttu-id="f7b2e-106">Veri türleri arasında birden fazla olası dönüştürme var.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-106">More than one possible conversion exists between the data types.</span></span>  
  
- <span data-ttu-id="f7b2e-107">Adaylar arasında en iyi tür yoktur, böylece döngü denetim değişkeni türü seçimi belirsiz olur.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="f7b2e-108">Örneğin, aşağıdaki döngüde, türünde bir döngü `Integer` ve bir döngü sınırı vardır `UInteger` :</span><span class="sxs-lookup"><span data-stu-id="f7b2e-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="f7b2e-109">Ve arasında olası bir dönüştürme ve arasında olası bir dönüştürme vardır `Integer` `UInteger` `UInteger` `Integer` ancak her ikisi de daraltma dönüştürmelerinden hiçbiri en iyi seçenektir.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="f7b2e-110">Sonraki örnekte, türünde üçüncü bir değişken `Double` eklenir.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="f7b2e-111">Her ikisi `Integer` ve `UInteger` için standart genişletme dönüştürmeleri vardır ve `Double` Bu, `Double` en iyi seçime dönüştürme yapar.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="f7b2e-112">Tür çıkarımı `i` , veri türü döngü denetim değişkenine atar `Double` .</span><span class="sxs-lookup"><span data-stu-id="f7b2e-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="f7b2e-113">**Hata kimliği:** BC30983</span><span class="sxs-lookup"><span data-stu-id="f7b2e-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="f7b2e-114">Bu hatayı düzeltmek için</span><span class="sxs-lookup"><span data-stu-id="f7b2e-114">To correct this error</span></span>  
  
- <span data-ttu-id="f7b2e-115">Değişkenlerden birini, diğer kişilerin Genişletme dönüştürmesi olan bir türe açıkça dönüştürün, örneğin:</span><span class="sxs-lookup"><span data-stu-id="f7b2e-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```vb  
    For i = m To CLng(n)  
    ```  
  
- <span data-ttu-id="f7b2e-116">`As`Denetim değişkeninin türünü belirtmek için bir yan tümce kullanın:</span><span class="sxs-lookup"><span data-stu-id="f7b2e-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```vb  
    For i As Double = m To n
    ```  
  
## <a name="see-also"></a><span data-ttu-id="f7b2e-117">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="f7b2e-117">See also</span></span>

- [<span data-ttu-id="f7b2e-118">For...Next Deyimi</span><span class="sxs-lookup"><span data-stu-id="f7b2e-118">For...Next Statement</span></span>](../language-reference/statements/for-next-statement.md)
- [<span data-ttu-id="f7b2e-119">Örtük ve Açık Dönüştürmeler</span><span class="sxs-lookup"><span data-stu-id="f7b2e-119">Implicit and Explicit Conversions</span></span>](../programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)
- [<span data-ttu-id="f7b2e-120">Yerel Tür Arabirimi</span><span class="sxs-lookup"><span data-stu-id="f7b2e-120">Local Type Inference</span></span>](../programming-guide/language-features/variables/local-type-inference.md)
- [<span data-ttu-id="f7b2e-121">Option Infer Deyimi</span><span class="sxs-lookup"><span data-stu-id="f7b2e-121">Option Infer Statement</span></span>](../language-reference/statements/option-infer-statement.md)
- [<span data-ttu-id="f7b2e-122">Genişletme ve Daraltma Dönüşümleri</span><span class="sxs-lookup"><span data-stu-id="f7b2e-122">Widening and Narrowing Conversions</span></span>](../programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
