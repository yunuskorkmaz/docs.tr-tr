---
description: "Daha fazla bilgi edinin: ' #ElseIf ', ' #Else ' veya ' #End If ' öncesinde eşleşen bir ' #If ' olmalıdır"
title: "' #ElseIf ', ' #Else ' veya ' #End If ' öncesinde eşleşen bir ' #If ' olmalıdır"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: f954fecf592783a6323bf2b9bce9392f6625a781
ms.sourcegitcommit: 10e719780594efc781b15295e499c66f316068b8
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/14/2021
ms.locfileid: "100463812"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="24f54-103">' #ElseIf ', ' #Else ' veya ' #End If ' öncesinde eşleşen bir ' #If ' olmalıdır</span><span class="sxs-lookup"><span data-stu-id="24f54-103">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>

<span data-ttu-id="24f54-104">`#ElseIf`, `#Else` ve, `#End If` koşullu derleme yönergelerinden.</span><span class="sxs-lookup"><span data-stu-id="24f54-104">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="24f54-105">`#ElseIf`, `#Else` , Veya `#End If` öncesinde karşılık gelen bir yönerge değildir `#If` .</span><span class="sxs-lookup"><span data-stu-id="24f54-105">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="24f54-106">**Hata kimliği:** BC30013</span><span class="sxs-lookup"><span data-stu-id="24f54-106">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="24f54-107">Bu hatayı düzeltmek için</span><span class="sxs-lookup"><span data-stu-id="24f54-107">To correct this error</span></span>  
  
1. <span data-ttu-id="24f54-108">Amaçlanan `#If` koşullu derleme bloğu veya yanlış yerleştirilmiş bir şekilde söz konusu yan tümce tarafından, hedeflenen öğenin ayrılmadığından emin olun `#End If` .</span><span class="sxs-lookup"><span data-stu-id="24f54-108">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="24f54-109">`#Else`Her blokta yalnızca birine izin verilir `#If` , bu nedenle Art arda iki `#Else` yönergeler bu hataya neden olur.</span><span class="sxs-lookup"><span data-stu-id="24f54-109">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="24f54-110">Önceki bir yönergede önde gelen olmadığından emin olun `#` `#If` .</span><span class="sxs-lookup"><span data-stu-id="24f54-110">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="24f54-111">Başka her şey sıralamayla, `#If` koşullu derleme bloğunun başlangıcına bir yönerge ekleyin.</span><span class="sxs-lookup"><span data-stu-id="24f54-111">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="24f54-112">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="24f54-112">See also</span></span>

- [<span data-ttu-id="24f54-113">#If... Sonra... #Else yönergeler</span><span class="sxs-lookup"><span data-stu-id="24f54-113">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
