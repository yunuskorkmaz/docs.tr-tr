---
title: Kod Tırnak İşaretleri
description: Programlama yoluyla F# kod ifadeleri oluşturmanıza ve bunlarla F# çalışmanıza olanak tanıyan bir dil özelliği olan kod teklifleri hakkında bilgi edinin.
ms.date: 05/16/2016
ms.openlocfilehash: c6ec0078c685a6452f49edd289b01491dd62e3db
ms.sourcegitcommit: f20dd18dbcf2275513281f5d9ad7ece6a62644b4
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 07/30/2019
ms.locfileid: "68630421"
---
# <a name="code-quotations"></a><span data-ttu-id="7ee8a-103">Kod Tırnak İşaretleri</span><span class="sxs-lookup"><span data-stu-id="7ee8a-103">Code Quotations</span></span>

> [!NOTE]
> <span data-ttu-id="7ee8a-104">API başvuru bağlantısı sizi MSDN 'ye götürür.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-104">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="7ee8a-105">Docs.microsoft.com API başvurusu tamamlanmadı.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-105">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="7ee8a-106">Bu konu, kod ifadelerini programlama yoluyla oluşturmanıza ve bunlarla F# çalışmanıza olanak tanıyan bir dil özelliği olan *kod tekliflerini*açıklar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-106">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="7ee8a-107">Bu özellik, kodu temsil F# eden bir soyut sözdizimi ağacı oluşturmanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-107">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="7ee8a-108">Soyut sözdizimi ağacı, uygulamanızın gereksinimlerine göre çapraz ve işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-108">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="7ee8a-109">Örneğin, ağacı kod oluşturmak F# veya başka bir dilde kod oluşturmak için kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-109">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="7ee8a-110">Tırnak işareti Ifadesi</span><span class="sxs-lookup"><span data-stu-id="7ee8a-110">Quoted Expressions</span></span>

<span data-ttu-id="7ee8a-111">*Tırnak içine alınmış* bir ifade F# , kodunuzda, programınızın bir parçası olarak derlenmediği, ancak bunun yerine bir F# ifadeyi temsil eden bir nesneye derlenmiş şekilde sınırlanmış bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-111">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="7ee8a-112">Tırnak içine alınmış bir ifadeyi iki şekilde işaretleyebilirsiniz: tür bilgisi veya tür bilgisi olmadan.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-112">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="7ee8a-113">Tür bilgilerini eklemek istiyorsanız, sembolleri `<@` kullanır ve `@>` tırnak içine alınmış ifadeyi sınırlandırın.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-113">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="7ee8a-114">Tür bilgilerine ihtiyacınız yoksa, ve `<@@` `@@>`simgelerini kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-114">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="7ee8a-115">Aşağıdaki kod, yazılan ve türsüz teklifleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-115">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="7ee8a-116">Tür bilgilerini eklemezseniz büyük bir ifade ağacına geçiş yapmak daha hızlıdır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-116">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="7ee8a-117">Türü belirlenmiş simgelerle tırnak içine alınmış bir ifadenin sonuç türü, tür `Expr<'T>`parametresi F# derleyicinin tür çıkarımı algoritması tarafından belirlendiği şekilde ifadenin türüne sahip olur.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-117">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="7ee8a-118">Kod tekliflerini tür bilgisi olmadan kullandığınızda, tırnak işareti türü genel olmayan [ifadedir.](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65)</span><span class="sxs-lookup"><span data-stu-id="7ee8a-118">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="7ee8a-119">Türsüz [](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) `Expr` nesneyieldeetmekiçin,yazılansınıftahamözelliğini`Expr` çağırabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-119">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="7ee8a-120">Tırnak içine alınmış ifadeler kullanmadan F# `Expr` sınıfında program aracılığıyla ifade nesneleri oluşturmanıza imkan tanıyan çeşitli statik yöntemler vardır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-120">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="7ee8a-121">Bir kod teklifinin bir bütün ifadeyi içermesi gerektiğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-121">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="7ee8a-122">Örneğin, `let` bir bağlama için, hem ilişkili ad tanımının hem de bağlamayı kullanan ek bir ifadenin olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-122">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="7ee8a-123">Ayrıntılı söz diziminde, bu `in` anahtar sözcüğünü izleyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-123">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="7ee8a-124">Modüldeki en üst düzey bu, modüldeki yalnızca bir sonraki ifadedir, ancak bir teklifte, açıkça gereklidir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-124">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="7ee8a-125">Bu nedenle, aşağıdaki ifade geçerli değildir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-125">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="7ee8a-126">Ancak aşağıdaki ifadeler geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-126">But the following expressions are valid.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="7ee8a-127">Teklifleri değerlendirmek F# için, [ F# teklif değerlendirici](https://github.com/fsprojects/FSharp.Quotations.Evaluator)' ni kullanmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-127">To evaluate F# quotations, you must use the [F# Quotation Evaluator](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span></span> <span data-ttu-id="7ee8a-128">İfade nesnelerini değerlendirmek ve yürütmek F# için destek sağlar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-128">It provides support for evaluating and executing F# expression objects.</span></span>

## <a name="expr-type"></a><span data-ttu-id="7ee8a-129">Expr türü</span><span class="sxs-lookup"><span data-stu-id="7ee8a-129">Expr Type</span></span>

<span data-ttu-id="7ee8a-130">`Expr` Türün bir örneği bir F# ifadeyi temsil eder.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-130">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="7ee8a-131">Hem genel hem de genel `Expr` olmayan türler F# kitaplık belgelerinde belgelenmiştir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-131">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="7ee8a-132">Daha fazla bilgi için bkz. [Microsoft. FSharp. Quotations ad alanı](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) ve [Quotations. Expr sınıfı](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="7ee8a-132">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="7ee8a-133">Splicing Işleçleri</span><span class="sxs-lookup"><span data-stu-id="7ee8a-133">Splicing Operators</span></span>

<span data-ttu-id="7ee8a-134">Splicing, sabit kod tekliflerini programlı bir şekilde veya başka bir kod teklifinden oluşturduğunuz ifadelerle birleştirmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-134">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="7ee8a-135">Ve `%` işleçleri`%%` , bir kod teklifine bir F# ifade nesnesi eklemenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-135">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="7ee8a-136">Türü belirlenmiş bir `%` teklife yazılmış bir ifade nesnesi eklemek için işlecini kullanırsınız; türsüz bir ifade nesnesini `%%` türsüz bir teklife eklemek için işlecini kullanırsınız.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-136">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="7ee8a-137">Her iki işleç de birli önek işleçleridir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-137">Both operators are unary prefix operators.</span></span> <span data-ttu-id="7ee8a-138">Bu nedenle `expr` , türünde `Expr`türsüz bir ifade varsa, aşağıdaki kod geçerli olur.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-138">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="7ee8a-139">`expr` Türü`Expr<int>`belirlenmiş bir tırnak ise, aşağıdaki kod geçerli olur.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-139">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="7ee8a-140">Örnek</span><span class="sxs-lookup"><span data-stu-id="7ee8a-140">Example</span></span>

### <a name="description"></a><span data-ttu-id="7ee8a-141">Açıklama</span><span class="sxs-lookup"><span data-stu-id="7ee8a-141">Description</span></span>

<span data-ttu-id="7ee8a-142">Aşağıdaki örnek, kod tekliflerinin bir ifade nesnesine kod koymak F# ve sonra ifadeyi temsil eden F# kodu yazdırmak için kullanımını gösterir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-142">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="7ee8a-143">Bir işlev `println` , F# bir ifade nesnesini (türünde `Expr`) kolay bir biçimde görüntüleyen özyinelemeli bir işlev `print` içeren tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-143">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="7ee8a-144">[Microsoft. FSharp. Quotations. Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) ve [Microsoft. FSharp. Quotations. DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modüllerinde ifade nesnelerini çözümlemek için kullanılabilen çeşitli etkin desenler vardır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-144">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="7ee8a-145">Bu örnek, bir F# ifadede görünebilen tüm olası desenleri içermez.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-145">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="7ee8a-146">Tüm tanınmayan desenler joker karakter düzeniyle (`_`) bir eşleşme tetikleyip, `Expr` türü `ToString` kullanılarak işlenir ve bu da tür üzerinde, eşleştirme ifadenizde eklemek üzere etkin olan kalıbı bilmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-146">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="7ee8a-147">Kod</span><span class="sxs-lookup"><span data-stu-id="7ee8a-147">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="7ee8a-148">Çıkış</span><span class="sxs-lookup"><span data-stu-id="7ee8a-148">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="7ee8a-149">Örnek</span><span class="sxs-lookup"><span data-stu-id="7ee8a-149">Example</span></span>

### <a name="description"></a><span data-ttu-id="7ee8a-150">Açıklama</span><span class="sxs-lookup"><span data-stu-id="7ee8a-150">Description</span></span>

<span data-ttu-id="7ee8a-151">Ayrıca, daha az etkin desenle ifade ağaçlarına çapraz geçiş yapmak için [ExprShape modülünde](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) üç etkin deseni de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-151">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="7ee8a-152">Bu etkin desenler, bir ağaca geçiş yapmak istediğinizde yararlı olabilir ancak düğümlerin çoğunda tüm bilgilere ihtiyacınız yoktur.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-152">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="7ee8a-153">Bu desenleri F# kullandığınızda, herhangi bir ifade aşağıdaki üç desenden biriyle eşleşir: `ShapeVar` ifade bir değişkense, `ShapeLambda` ifade bir lambda ifadesiyse veya `ShapeCombination` ifade başka bir şeydir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-153">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="7ee8a-154">Önceki kod örneğinde olduğu gibi etkin desenleri kullanarak bir ifade ağacında çapraz geçiş yaparsanız, olası F# tüm ifade türlerini işlemek için çok daha fazla desen kullanmanız gerekir ve kodunuz daha karmaşık olacaktır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-154">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="7ee8a-155">Daha fazla bilgi için bkz. [ExprShape. ShapeVar&#124;ShapeLambda&#124;shapebirleşimi etkin model](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="7ee8a-155">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="7ee8a-156">Aşağıdaki kod örneği, daha karmaşık traversals için temel olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-156">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="7ee8a-157">Bu kodda, bir işlev çağrısını `add`içeren bir ifade için bir ifade ağacı oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-157">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="7ee8a-158">İfade ağacında yapılan `add` herhangi bir çağrıyı algılamak için [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) etkin deseninin kullanılması.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-158">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="7ee8a-159">Bu etkin model, çağrının `exprList` bağımsız değişkenlerini değerine atar.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-159">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="7ee8a-160">Bu durumda, yalnızca ikisi vardır, bu nedenle bunlar kullanıma alınır ve işlev bağımsız değişkenlerde yinelemeli olarak çağırılır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-160">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="7ee8a-161">Sonuçlar, splice işleci ( `mul` `%%`) kullanılarak yapılan çağrıyı temsil eden bir kod teklifine eklenir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-161">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="7ee8a-162">Önceki `println` örnekteki işlev, sonuçları göstermek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-162">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="7ee8a-163">Diğer etkin model dallarındaki kod yalnızca aynı ifade ağacını yeniden oluşturur, bu nedenle sonuç ifadesindeki tek değişiklik, ' dan `add` `mul`' a değişir.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-163">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="7ee8a-164">Kod</span><span class="sxs-lookup"><span data-stu-id="7ee8a-164">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="7ee8a-165">Çıkış</span><span class="sxs-lookup"><span data-stu-id="7ee8a-165">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="7ee8a-166">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="7ee8a-166">See also</span></span>

- [<span data-ttu-id="7ee8a-167">F# Dili Başvurusu</span><span class="sxs-lookup"><span data-stu-id="7ee8a-167">F# Language Reference</span></span>](index.md)
