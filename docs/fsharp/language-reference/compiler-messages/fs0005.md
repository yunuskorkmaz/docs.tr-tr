---
description: 'Hakkında daha fazla bilgi edinin: FS0005: Bu alan değişebilir değil'
title: Derleyici hatası FS0005
ms.date: 01/27/2020
f1_keywords:
- FS0005
helpviewer_keywords:
- FS0005
ms.openlocfilehash: f6691deb90a7da5f721cff14b1aa09bdd873e04f
ms.sourcegitcommit: ddf7edb67715a5b9a45e3dd44536dabc153c1de0
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/06/2021
ms.locfileid: "99631026"
---
# <a name="fs0005-this-field-is-not-mutable"></a><span data-ttu-id="2fa55-103">FS0005: Bu alan değişebilir değil</span><span class="sxs-lookup"><span data-stu-id="2fa55-103">FS0005: This field is not mutable</span></span>

<span data-ttu-id="2fa55-104">Bu ileti, bir kayıt alanı zaman içinde atanmadığında, ancak kayıt alanı değiştirilebilir olarak işaretlenmediğinde verilir.</span><span class="sxs-lookup"><span data-stu-id="2fa55-104">This message is given when a record field is mutably assigned, but the record field is not marked mutable.</span></span>

<span data-ttu-id="2fa55-105">Aşağıdaki kodda, sabit alanlara sahip bir kayıt bildirilmiştir, bu kaydın bir örneği oluşturulur ve bu kaydın bir alanı, hatayı gösteren bir alan için zaman zaman atanabilir olarak atanmamıştır:</span><span class="sxs-lookup"><span data-stu-id="2fa55-105">In the following code, a record with immutable fields is declared, an instance of that record is created, and a field of that record is mutably assigned, showing the error:</span></span>

[!code-fsharp[FS0005-simple](~/samples/snippets/fsharp/compiler-messages/fs0005.fsx#L2-L8)]

<span data-ttu-id="2fa55-106">Bu kod aşağıdaki çıkışa neden olur:</span><span class="sxs-lookup"><span data-stu-id="2fa55-106">This code results in the following output:</span></span>

```text
error FS0005: This field is not mutable
```

<span data-ttu-id="2fa55-107">Burada, sabit kayıt alanına `Kind` "turuncu" dize değeri atandı, ancak alan bu şekilde açıkça işaretlenmedikçe F # değişebilir atamayı desteklemez.</span><span class="sxs-lookup"><span data-stu-id="2fa55-107">Here, the immutable record field `Kind` was assigned the string value "orange", but F# doesn't support mutable assignment unless the field is explicitly marked that way.</span></span>

<span data-ttu-id="2fa55-108">Bu iletiyi çözmek için, alanın tanımlandığı şekilde veya yeni değeri nasıl ayardığınıza göre değiştirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="2fa55-108">To solve this message you have to change either the way the field is defined, or how you're setting the new value.</span></span>

<span data-ttu-id="2fa55-109">Gerçekten kaydın kesilebilir bir alana sahip olmasını istiyorsanız (oldukça nadir bir uygulamadır) `mutable` , burada gösterildiği gibi, alanı tür tanımında olarak işaretleyebilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="2fa55-109">If you actually intended for the record to have a mutable field (which is a fairly rare practice) you can mark the field as `mutable` in the type definition, as shown here:</span></span>

[!code-fsharp[FS0005-mutable](~/samples/snippets/fsharp/compiler-messages/fs0005.fsx#L11-L17)]

<span data-ttu-id="2fa55-110">Ancak, `with` burada gösterildiği gibi, belirttiğiniz alanlarla orijinal kaydın bir kopyasını oluşturan ifadesi aracılığıyla bir kaydın sabit bir güncelleştirmesini gerçekleştirmek Için F # ' ta daha fazla deyim vardır:</span><span class="sxs-lookup"><span data-stu-id="2fa55-110">However, it is more idiomatic in F# to perform an immutable update of a record via the `with` expression, which creates a copy of the original record with the fields you specified set to new values as shown here:</span></span>

[!code-fsharp[FS0005-record-update](~/samples/snippets/fsharp/compiler-messages/fs0005.fsx#L20-L26)]
