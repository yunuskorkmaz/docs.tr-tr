---
title: .NET Core CLI ile uygulama yayımlama
description: .NET Core CLI komutlarını kullanarak bir .NET Core uygulaması yayımlamayı öğrenin.
author: adegeo
ms.author: adegeo
ms.date: 12/12/2019
dev_langs:
- csharp
- vb
ms.openlocfilehash: a592b397d1ffee5b224638a8d17ce6fa9e44eea2
ms.sourcegitcommit: dc2feef0794cf41dbac1451a13b8183258566c0e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 06/24/2020
ms.locfileid: "85325028"
---
# <a name="publish-net-core-apps-with-the-net-core-cli"></a><span data-ttu-id="43d0d-103">.NET Core CLI .NET Core uygulamaları yayımlayın</span><span class="sxs-lookup"><span data-stu-id="43d0d-103">Publish .NET Core apps with the .NET Core CLI</span></span>

<span data-ttu-id="43d0d-104">Bu makalede, .NET Core uygulamanızı komut satırından nasıl yayımlayacağınız gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-104">This article demonstrates how you can publish your .NET Core application from the command line.</span></span> <span data-ttu-id="43d0d-105">.NET Core, uygulamalarınızı yayımlamanın üç yolunu sağlar.</span><span class="sxs-lookup"><span data-stu-id="43d0d-105">.NET Core provides three ways to publish your applications.</span></span> <span data-ttu-id="43d0d-106">Çerçeveye bağımlı dağıtım, yerel olarak yüklenmiş .NET Core çalışma zamanını kullanan platformlar arası bir. dll dosyası üretir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-106">Framework-dependent deployment produces a cross-platform .dll file that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="43d0d-107">Çerçeveye bağımlı yürütülebilir, yerel olarak yüklenmiş .NET Core çalışma zamanını kullanan platforma özgü bir yürütülebilir dosya oluşturur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-107">Framework-dependent executable produces a platform-specific executable that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="43d0d-108">Kendi içinde bulunan yürütülebilir bir platforma özgü yürütülebilir dosya oluşturur ve .NET Core çalışma zamanının yerel bir kopyasını içerir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-108">Self-contained executable produces a platform-specific executable and includes a local copy of the .NET Core runtime.</span></span>

<span data-ttu-id="43d0d-109">Bu yayımlama modlarına genel bakış için bkz. [.NET Core uygulama dağıtımı](index.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-109">For an overview of these publishing modes, see [.NET Core Application Deployment](index.md).</span></span>

<span data-ttu-id="43d0d-110">CLı kullanımıyla ilgili bazı hızlı yardım mı arıyorsunuz?</span><span class="sxs-lookup"><span data-stu-id="43d0d-110">Looking for some quick help on using the CLI?</span></span> <span data-ttu-id="43d0d-111">Aşağıdaki tabloda, uygulamanızı nasıl yayımlayacağınız hakkında bazı örnekler gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-111">The following table shows some examples of how to publish your app.</span></span> <span data-ttu-id="43d0d-112">Hedef çerçeveyi `-f <TFM>` parametresiyle veya proje dosyasını düzenleyerek belirtebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-112">You can specify the target framework with the `-f <TFM>` parameter or by editing the project file.</span></span> <span data-ttu-id="43d0d-113">Daha fazla bilgi için bkz. [Yayımlama temelleri](#publishing-basics).</span><span class="sxs-lookup"><span data-stu-id="43d0d-113">For more information, see [Publishing basics](#publishing-basics).</span></span>

| <span data-ttu-id="43d0d-114">Yayımlama modu</span><span class="sxs-lookup"><span data-stu-id="43d0d-114">Publish Mode</span></span> | <span data-ttu-id="43d0d-115">SDK Sürümü</span><span class="sxs-lookup"><span data-stu-id="43d0d-115">SDK Version</span></span> | <span data-ttu-id="43d0d-116">Komut</span><span class="sxs-lookup"><span data-stu-id="43d0d-116">Command</span></span> |
| ------------ | ----------- | ------- |
| <span data-ttu-id="43d0d-117">Çerçeveye bağımlı dağıtım</span><span class="sxs-lookup"><span data-stu-id="43d0d-117">Framework-dependent deployment</span></span> | <span data-ttu-id="43d0d-118">2.x</span><span class="sxs-lookup"><span data-stu-id="43d0d-118">2.x</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="43d0d-119">Çerçeveye bağımlı yürütülebilir dosya</span><span class="sxs-lookup"><span data-stu-id="43d0d-119">Framework-dependent executable</span></span> | <span data-ttu-id="43d0d-120">2,2</span><span class="sxs-lookup"><span data-stu-id="43d0d-120">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="43d0d-121">3.0</span><span class="sxs-lookup"><span data-stu-id="43d0d-121">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="43d0d-122">3,0 \*</span><span class="sxs-lookup"><span data-stu-id="43d0d-122">3.0\*</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="43d0d-123">Kendi kendine kapsanan dağıtım</span><span class="sxs-lookup"><span data-stu-id="43d0d-123">Self-contained deployment</span></span>      | <span data-ttu-id="43d0d-124">2.1</span><span class="sxs-lookup"><span data-stu-id="43d0d-124">2.1</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="43d0d-125">2,2</span><span class="sxs-lookup"><span data-stu-id="43d0d-125">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="43d0d-126">3.0</span><span class="sxs-lookup"><span data-stu-id="43d0d-126">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |

<span data-ttu-id="43d0d-127">\*SDK sürüm 3,0 kullanılırken, temel komut çalıştırılırken çerçeveye bağlı yürütülebilir dosya varsayılan yayımlama modudur `dotnet publish` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-127">\* When using SDK version 3.0, framework-dependent executable is the default publishing mode when running the basic `dotnet publish` command.</span></span> <span data-ttu-id="43d0d-128">Bu, yalnızca proje **.net core 2,1** veya **.NET Core 3,0**' i hedefliyorsa geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-128">This only applies when the project targets either **.NET Core 2.1** or **.NET Core 3.0**.</span></span>

## <a name="publishing-basics"></a><span data-ttu-id="43d0d-129">Yayımlama temelleri</span><span class="sxs-lookup"><span data-stu-id="43d0d-129">Publishing basics</span></span>

<span data-ttu-id="43d0d-130">`<TargetFramework>`Uygulamanızı yayımladığınızda proje dosyasının ayarı varsayılan hedef çerçevesini belirtir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-130">The `<TargetFramework>` setting of the project file specifies the default target framework when you publish your app.</span></span> <span data-ttu-id="43d0d-131">Hedef Framework 'ü geçerli bir [hedef çerçeve bilinen adı (tfd)](../../standard/frameworks.md)olarak değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-131">You can change the target framework to any valid [Target Framework Moniker (TFM)](../../standard/frameworks.md).</span></span> <span data-ttu-id="43d0d-132">Örneğin, projeniz kullanıyorsa `<TargetFramework>netcoreapp2.2</TargetFramework>` , .NET Core 2,2 ' i hedefleyen bir ikili oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-132">For example, if your project uses `<TargetFramework>netcoreapp2.2</TargetFramework>`, a binary that targets .NET Core 2.2 is created.</span></span> <span data-ttu-id="43d0d-133">Bu ayarda belirtilen tfd, komut tarafından kullanılan varsayılan hedeftir [`dotnet publish`](../tools/dotnet-publish.md) .</span><span class="sxs-lookup"><span data-stu-id="43d0d-133">The TFM specified in this setting is the default target used by the [`dotnet publish`](../tools/dotnet-publish.md) command.</span></span>

<span data-ttu-id="43d0d-134">Birden fazla çerçeveyi hedeflemek istiyorsanız, `<TargetFrameworks>` ayarı noktalı virgülle ayırarak birden fazla TFI değeri olarak ayarlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-134">If you want to target more than one framework, you can set the `<TargetFrameworks>` setting to more than one TFM value separated by a semicolon.</span></span> <span data-ttu-id="43d0d-135">Komutuyla bir çerçeve yayımlayabilirsiniz `dotnet publish -f <TFM>` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-135">You can publish one of the frameworks with the `dotnet publish -f <TFM>` command.</span></span> <span data-ttu-id="43d0d-136">Örneğin, `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` ve çalıştırırsanız `dotnet publish -f netcoreapp2.1` , .net Core 2,1 ' i hedefleyen bir ikili oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-136">For example, if you have `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` and run `dotnet publish -f netcoreapp2.1`, a binary that targets .NET Core 2.1 is created.</span></span>

<span data-ttu-id="43d0d-137">Aksi belirtilmedikçe, komutun çıkış dizini [`dotnet publish`](../tools/dotnet-publish.md) olur `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-137">Unless otherwise set, the output directory of the [`dotnet publish`](../tools/dotnet-publish.md) command is `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`.</span></span> <span data-ttu-id="43d0d-138">Varsayılan **derleme yapılandırma** modu, parametresiyle değiştirilmediği takdirde **hata ayıklaması** olur `-c` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-138">The default **BUILD-CONFIGURATION** mode is **Debug** unless changed with the `-c` parameter.</span></span> <span data-ttu-id="43d0d-139">Örneğin, ' a `dotnet publish -c Release -f netcoreapp2.1` yayınlar `myfolder/bin/Release/netcoreapp2.1/publish/` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-139">For example, `dotnet publish -c Release -f netcoreapp2.1` publishes to `myfolder/bin/Release/netcoreapp2.1/publish/`.</span></span>

<span data-ttu-id="43d0d-140">.NET Core SDK 3,0 veya sonraki bir sürümü kullanırsanız, .NET Core sürümleri 2,1, 2,2, 3,0 veya sonraki bir sürümünü hedefleyen uygulamalar için varsayılan yayımlama modu, çerçeveye bağlı yürütülebilir dosyadır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-140">If you use .NET Core SDK 3.0 or later, the default publish mode for apps that target .NET Core versions 2.1, 2.2, 3.0, or a later version, is framework-dependent executable.</span></span>

<span data-ttu-id="43d0d-141">.NET Core SDK 2,1 kullanırsanız, .NET Core sürümleri 2,1 ve 2,2 ' i hedefleyen uygulamalar için varsayılan yayımlama modu, çerçeveye bağımlı dağıtımdır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-141">If you use .NET Core SDK 2.1, the default publish mode for apps that target .NET Core versions 2.1 and 2.2 is framework-dependent deployment.</span></span>

### <a name="native-dependencies"></a><span data-ttu-id="43d0d-142">Yerel bağımlılıklar</span><span class="sxs-lookup"><span data-stu-id="43d0d-142">Native dependencies</span></span>

<span data-ttu-id="43d0d-143">Uygulamanızda Yerel bağımlılıklar varsa, farklı bir işletim sisteminde çalışmayabilir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-143">If your app has native dependencies, it may not run on a different operating system.</span></span> <span data-ttu-id="43d0d-144">Örneğin, uygulamanız yerel Windows API 'sini kullanıyorsa macOS veya Linux üzerinde çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-144">For example, if your app uses the native Windows API, it won't run on macOS or Linux.</span></span> <span data-ttu-id="43d0d-145">Platforma özgü kod sağlamanız ve her platform için bir yürütülebilir dosya derlemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-145">You would need to provide platform-specific code and compile an executable for each platform.</span></span>

<span data-ttu-id="43d0d-146">Ayrıca, başvurduğunuz bir kitaplığın yerel bağımlılığı varsa, uygulamanız her platformda çalışmayabilir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-146">Consider also, if a library you referenced has a native dependency, your app may not run on every platform.</span></span> <span data-ttu-id="43d0d-147">Ancak, başvurduğunuz bir NuGet paketi, sizin için gerekli yerel bağımlılıkları işlemek üzere platforma özgü sürümlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-147">However, it's possible a NuGet package you're referencing has included platform-specific versions to handle the required native dependencies for you.</span></span>

<span data-ttu-id="43d0d-148">Bir uygulamayı yerel bağımlılıklarla dağıtırken, `dotnet publish -r <RID>` yayımlamak istediğiniz hedef platformu belirtmek için anahtarını kullanmanız gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-148">When distributing an app with native dependencies, you may need to use the `dotnet publish -r <RID>` switch to specify the target platform you want to publish for.</span></span> <span data-ttu-id="43d0d-149">Çalışma zamanı tanımlayıcılarının listesi için bkz. [Runtime Identifier (RID) Catalog](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-149">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

<span data-ttu-id="43d0d-150">Platforma özgü ikililer hakkında daha fazla bilgi, [çerçeveye bağımlı yürütülebilir](#framework-dependent-executable) ve [kendi kendine dahil edilen dağıtım](#self-contained-deployment) bölümlerinde ele alınmıştır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-150">More information about platform-specific binaries is covered in the [Framework-dependent executable](#framework-dependent-executable) and [Self-contained deployment](#self-contained-deployment) sections.</span></span>

## <a name="sample-app"></a><span data-ttu-id="43d0d-151">Örnek uygulama</span><span class="sxs-lookup"><span data-stu-id="43d0d-151">Sample app</span></span>

<span data-ttu-id="43d0d-152">Yayımlama komutlarını araştırmak için aşağıdaki uygulamayı kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-152">You can use the following app to explore the publishing commands.</span></span> <span data-ttu-id="43d0d-153">Uygulama, terminalinizde aşağıdaki komutlar çalıştırılarak oluşturulur:</span><span class="sxs-lookup"><span data-stu-id="43d0d-153">The app is created by running the following commands in your terminal:</span></span>

```dotnetcli
mkdir apptest1
cd apptest1
dotnet new console
dotnet add package Figgle
```

<span data-ttu-id="43d0d-154">`Program.cs` `Program.vb` Konsol şablonu tarafından oluşturulan veya dosyanın aşağıdaki şekilde değiştirilmesi gerekir:</span><span class="sxs-lookup"><span data-stu-id="43d0d-154">The `Program.cs` or `Program.vb` file that is generated by the console template needs to be changed to the following:</span></span>

```csharp
using System;

namespace apptest1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"));
        }
    }
}
```

```vb
Module Program
    Sub Main(args As String())
        Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"))
    End Sub
End Module
```

<span data-ttu-id="43d0d-155">Uygulamayı çalıştırdığınızda [`dotnet run`](../tools/dotnet-run.md) , () aşağıdaki çıktı görüntülenir:</span><span class="sxs-lookup"><span data-stu-id="43d0d-155">When you run the app ([`dotnet run`](../tools/dotnet-run.md)), the following output is displayed:</span></span>

```terminal
  _   _      _ _         __        __         _     _ _
 | | | | ___| | | ___    \ \      / /__  _ __| | __| | |
 | |_| |/ _ \ | |/ _ \    \ \ /\ / / _ \| '__| |/ _` | |
 |  _  |  __/ | | (_) |    \ V  V / (_) | |  | | (_| |_|
 |_| |_|\___|_|_|\___( )    \_/\_/ \___/|_|  |_|\__,_(_)
                     |/
```

## <a name="framework-dependent-deployment"></a><span data-ttu-id="43d0d-156">Çerçeveye bağımlı dağıtım</span><span class="sxs-lookup"><span data-stu-id="43d0d-156">Framework-dependent deployment</span></span>

<span data-ttu-id="43d0d-157">.NET Core SDK 2. x CLı için, çerçeveye bağımlı dağıtım (FDD), temel komut için varsayılan moddur `dotnet publish` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-157">For the .NET Core SDK 2.x CLI, framework-dependent deployment (FDD) is the default mode for the basic `dotnet publish` command.</span></span>

<span data-ttu-id="43d0d-158">Uygulamanızı FDD olarak yayımladığınızda, `<PROJECT-NAME>.dll` klasörde bir dosya oluşturulur `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-158">When you publish your app as an FDD, a `<PROJECT-NAME>.dll` file is created in the `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` folder.</span></span> <span data-ttu-id="43d0d-159">Uygulamanızı çalıştırmak için çıkış klasörüne gidin ve `dotnet <PROJECT-NAME>.dll` komutunu kullanın.</span><span class="sxs-lookup"><span data-stu-id="43d0d-159">To run your app, navigate to the output folder and use the `dotnet <PROJECT-NAME>.dll` command.</span></span>

<span data-ttu-id="43d0d-160">Uygulamanız .NET Core 'un belirli bir sürümünü hedefleyecek şekilde yapılandırılmıştır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-160">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="43d0d-161">Bu hedeflenen .NET Core çalışma zamanının, uygulamanızın çalıştırıldığı herhangi bir makinede olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-161">That targeted .NET Core runtime is required to be on any machine where your app runs.</span></span> <span data-ttu-id="43d0d-162">Örneğin, uygulamanız .NET Core 2,2 hedefliyorsa, uygulamanızın üzerinde çalıştığı tüm makineler .NET Core 2,2 çalışma zamanının yüklü olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-162">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="43d0d-163">[Yayımlama temelleri](#publishing-basics) bölümünde belirtildiği gibi, varsayılan hedef Framework 'ü değiştirmek veya birden fazla çerçeveyi hedeflemek için proje dosyanızı düzenleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-163">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="43d0d-164">FDD yayımlama, uygulamayı çalıştıran sistemde kullanılabilir olan en son .NET Core güvenlik düzeltme ekine otomatik olarak bağlanan bir uygulama oluşturur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-164">Publishing an FDD creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="43d0d-165">Derleme zamanında sürüm bağlama hakkında daha fazla bilgi için bkz. [kullanılacak .NET Core sürümünü seçme](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="43d0d-165">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

## <a name="framework-dependent-executable"></a><span data-ttu-id="43d0d-166">Çerçeveye bağımlı yürütülebilir dosya</span><span class="sxs-lookup"><span data-stu-id="43d0d-166">Framework-dependent executable</span></span>

<span data-ttu-id="43d0d-167">.NET Core SDK 3. x CLı için, çerçeveye bağımlı yürütülebilir dosya (FDE), temel komut için varsayılan moddur `dotnet publish` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-167">For the .NET Core SDK 3.x CLI, framework-dependent executable (FDE) is the default mode for the basic `dotnet publish` command.</span></span> <span data-ttu-id="43d0d-168">Geçerli işletim sistemini hedeflemek istediğiniz sürece herhangi bir parametreyi belirtmeniz gerekmez.</span><span class="sxs-lookup"><span data-stu-id="43d0d-168">You don't need to specify any other parameters, as long as you want to target the current operating system.</span></span>

<span data-ttu-id="43d0d-169">Bu modda, platformlar arası uygulamanızı barındırmak için platforma özgü bir yürütülebilir ana bilgisayar oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-169">In this mode, a platform-specific executable host is created to host your cross-platform app.</span></span> <span data-ttu-id="43d0d-170">FDD, komut biçiminde bir konak gerektirdiğinden, bu mod FDD ile benzerdir `dotnet` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-170">This mode is similar to FDD, as FDD requires a host in the form of the `dotnet` command.</span></span> <span data-ttu-id="43d0d-171">Ana bilgisayar yürütülebilir dosya adı, platforma göre farklılık gösterir ve şuna benzer bir şey olarak adlandırılır `<PROJECT-FILE>.exe` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-171">The host executable filename varies per platform and is named something similar to `<PROJECT-FILE>.exe`.</span></span> <span data-ttu-id="43d0d-172">Bu yürütülebilir dosyayı çağırmak yerine doğrudan çalıştırabilirsiniz, bu da `dotnet <PROJECT-FILE>.dll` uygulamayı çalıştırmak için yine de kabul edilebilir bir yoldur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-172">You can run this executable directly instead of calling `dotnet <PROJECT-FILE>.dll`, which is still an acceptable way to run the app.</span></span>

<span data-ttu-id="43d0d-173">Uygulamanız .NET Core 'un belirli bir sürümünü hedefleyecek şekilde yapılandırılmıştır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-173">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="43d0d-174">Bu hedeflenen .NET Core çalışma zamanının, uygulamanızın çalıştırıldığı herhangi bir makinede olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-174">That targeted .NET Core runtime is required to be on any machine where your app runs.</span></span> <span data-ttu-id="43d0d-175">Örneğin, uygulamanız .NET Core 2,2 hedefliyorsa, uygulamanızın üzerinde çalıştığı tüm makineler .NET Core 2,2 çalışma zamanının yüklü olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-175">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="43d0d-176">[Yayımlama temelleri](#publishing-basics) bölümünde belirtildiği gibi, varsayılan hedef Framework 'ü değiştirmek veya birden fazla çerçeveyi hedeflemek için proje dosyanızı düzenleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-176">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="43d0d-177">Bir FDE yayımlamak, uygulamayı çalıştıran sistemde kullanılabilir en son .NET Core güvenlik düzeltme ekine otomatik olarak bağlanan bir uygulama oluşturur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-177">Publishing an FDE creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="43d0d-178">Derleme zamanında sürüm bağlama hakkında daha fazla bilgi için bkz. [kullanılacak .NET Core sürümünü seçme](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="43d0d-178">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

<span data-ttu-id="43d0d-179">.NET Core 2,2 ve önceki sürümlerde, `dotnet publish` FDE yayımlamak için komutuyla aşağıdaki anahtarları kullanmanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="43d0d-179">For .NET Core 2.2 and earlier, you must use the following switches with the `dotnet publish` command to publish an FDE:</span></span>

- <span data-ttu-id="43d0d-180">`-r <RID>`Bu anahtar, hedef platformu belirtmek için bir tanımlayıcı (RID) kullanır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-180">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="43d0d-181">Çalışma zamanı tanımlayıcılarının listesi için bkz. [Runtime Identifier (RID) Catalog](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-181">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="43d0d-182">`--self-contained false`Bu anahtar, .NET Core SDK çalıştırılabilir bir FDE oluşturmasını söyler.</span><span class="sxs-lookup"><span data-stu-id="43d0d-182">`--self-contained false` This switch tells the .NET Core SDK to create an executable as an FDE.</span></span>

<span data-ttu-id="43d0d-183">Anahtarı her kullandığınızda `-r` , çıkış klasörü yolu şu şekilde değişir:`./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span><span class="sxs-lookup"><span data-stu-id="43d0d-183">Whenever you use the `-r` switch, the output folder path changes to: `./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span></span>

<span data-ttu-id="43d0d-184">[Örnek uygulamayı](#sample-app)kullanıyorsanız, öğesini çalıştırın `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false` .</span><span class="sxs-lookup"><span data-stu-id="43d0d-184">If you use the [example app](#sample-app), run `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false`.</span></span> <span data-ttu-id="43d0d-185">Bu komut aşağıdaki yürütülebiliri oluşturur:`./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span><span class="sxs-lookup"><span data-stu-id="43d0d-185">This command creates the following executable: `./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span></span>

> [!NOTE]
> <span data-ttu-id="43d0d-186">**Genelleştirme sabit modunu**etkinleştirerek, dağıtımınızın toplam boyutunu azaltabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-186">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="43d0d-187">Bu mod, genel olarak kullanmayan ve [sabit kültürün](xref:System.Globalization.CultureInfo.InvariantCulture)biçimlendirme kurallarını, büyük/küçük harf kurallarını ve dize karşılaştırma ve sıralama düzenini kullanabilen uygulamalar için yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-187">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="43d0d-188">**Genelleştirme sabit modu** ve nasıl etkinleştirileceği hakkında daha fazla bilgi için bkz. [.NET Core Genelleştirme sabit modu](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-188">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span></span>

## <a name="self-contained-deployment"></a><span data-ttu-id="43d0d-189">Kendi kendine kapsanan dağıtım</span><span class="sxs-lookup"><span data-stu-id="43d0d-189">Self-contained deployment</span></span>

<span data-ttu-id="43d0d-190">Kendi içinde bir dağıtımı yayımladığınızda (SCD), .NET Core SDK platforma özgü bir yürütülebilir dosya oluşturur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-190">When you publish a self-contained deployment (SCD), the .NET Core SDK creates a platform-specific executable.</span></span> <span data-ttu-id="43d0d-191">Bir SCD yayımlamak, uygulamanızı çalıştırmak için gerekli tüm .NET Core dosyalarını içerir [, ancak .NET Core 'un yerel bağımlılıklarını](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md)içermez.</span><span class="sxs-lookup"><span data-stu-id="43d0d-191">Publishing an SCD includes all required .NET Core files to run your app but it doesn't include the [native dependencies of .NET Core](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md).</span></span> <span data-ttu-id="43d0d-192">Uygulamanın çalışması için önce bu bağımlılıkların sistemde mevcut olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="43d0d-192">These dependencies must be present on the system before the app runs.</span></span>

<span data-ttu-id="43d0d-193">Bir SCD yayımlamak, en son kullanılabilir .NET Core güvenlik düzeltme ekine geri dönerek bir uygulama oluşturur.</span><span class="sxs-lookup"><span data-stu-id="43d0d-193">Publishing an SCD creates an app that doesn't roll forward to the latest available .NET Core security patch.</span></span> <span data-ttu-id="43d0d-194">Derleme zamanında sürüm bağlama hakkında daha fazla bilgi için bkz. [kullanılacak .NET Core sürümünü seçme](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span><span class="sxs-lookup"><span data-stu-id="43d0d-194">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span></span>

<span data-ttu-id="43d0d-195">`dotnet publish`BIR SCD yayımlamak için komutuyla aşağıdaki anahtarları kullanmanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="43d0d-195">You must use the following switches with the `dotnet publish` command to publish an SCD:</span></span>

- <span data-ttu-id="43d0d-196">`-r <RID>`Bu anahtar, hedef platformu belirtmek için bir tanımlayıcı (RID) kullanır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-196">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="43d0d-197">Çalışma zamanı tanımlayıcılarının listesi için bkz. [Runtime Identifier (RID) Catalog](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-197">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="43d0d-198">`--self-contained true`Bu anahtar, .NET Core SDK çalıştırılabilir bir SCD oluşturmasını söyler.</span><span class="sxs-lookup"><span data-stu-id="43d0d-198">`--self-contained true` This switch tells the .NET Core SDK to create an executable as an SCD.</span></span>

> [!NOTE]
> <span data-ttu-id="43d0d-199">**Genelleştirme sabit modunu**etkinleştirerek, dağıtımınızın toplam boyutunu azaltabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-199">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="43d0d-200">Bu mod, genel olarak kullanmayan ve [sabit kültürün](xref:System.Globalization.CultureInfo.InvariantCulture)biçimlendirme kurallarını, büyük/küçük harf kurallarını ve dize karşılaştırma ve sıralama düzenini kullanabilen uygulamalar için yararlıdır.</span><span class="sxs-lookup"><span data-stu-id="43d0d-200">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="43d0d-201">**Genelleştirme sabit modu** ve nasıl etkinleştirileceği hakkında daha fazla bilgi için bkz. [.NET Core Genelleştirme sabit modu](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span><span class="sxs-lookup"><span data-stu-id="43d0d-201">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/runtime/blob/master/docs/design/features/globalization-invariant-mode.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="43d0d-202">Ayrıca bkz.</span><span class="sxs-lookup"><span data-stu-id="43d0d-202">See also</span></span>

- [<span data-ttu-id="43d0d-203">.NET Core uygulama dağıtımına genel bakış</span><span class="sxs-lookup"><span data-stu-id="43d0d-203">.NET Core Application Deployment Overview</span></span>](index.md)
- [<span data-ttu-id="43d0d-204">.NET Core çalışma zamanı tanımlayıcısı (RID) kataloğu</span><span class="sxs-lookup"><span data-stu-id="43d0d-204">.NET Core Runtime IDentifier (RID) catalog</span></span>](../rid-catalog.md)
