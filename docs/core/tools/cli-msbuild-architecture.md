---
title: .NET Core komut satırı araçları mimarisi
description: .NET Core araç katmanları ve son sürümlerde nelerin değiştiğini öğrenin.
ms.date: 03/06/2017
ms.openlocfilehash: 05183a9edc26615e00d6383043fd10d8bec06f2b
ms.sourcegitcommit: 4f4a32a5c16a75724920fa9627c59985c41e173c
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/17/2019
ms.locfileid: "72521303"
---
# <a name="high-level-overview-of-changes-in-the-net-core-tools"></a><span data-ttu-id="d10d5-103">.NET Core araçlarındaki değişikliklere yüksek düzeyde genel bakış</span><span class="sxs-lookup"><span data-stu-id="d10d5-103">High-level overview of changes in the .NET Core tools</span></span>

<span data-ttu-id="d10d5-104">Bu belge, .NET Core araçları 'nın katmanlanmasında ve CLı komutlarının uygulanması üzerindeki değişikliklerle ilgili bilgilerle, *Project. JSON* 'dan MSBuild 'e ve *csproj* proje sistemine geçiş ile ilişkili değişiklikleri açıklamaktadır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-104">This document describes the changes associated with moving from *project.json* to MSBuild and the *csproj* project system with information on the changes to the layering of the .NET Core tooling and the implementation of the CLI commands.</span></span> <span data-ttu-id="d10d5-105">Bu değişiklikler, 7 Mart 2017 ' de .NET Core SDK 1,0 ve Visual Studio 2017 sürümü ile oluştu, ancak başlangıçta .NET Core SDK [](https://devblogs.microsoft.com/dotnet/announcing-net-core-tools-1-0/)Preview 3 sürümü ile uygulandı.</span><span class="sxs-lookup"><span data-stu-id="d10d5-105">These changes occurred with the release of .NET Core SDK 1.0 and Visual Studio 2017 on March 7, 2017 (see the [announcement](https://devblogs.microsoft.com/dotnet/announcing-net-core-tools-1-0/)) but were initially implemented with the release of the .NET Core SDK Preview 3.</span></span>

## <a name="moving-away-from-projectjson"></a><span data-ttu-id="d10d5-106">Project. JSON öğesinden uzağa taşıma</span><span class="sxs-lookup"><span data-stu-id="d10d5-106">Moving away from project.json</span></span>

<span data-ttu-id="d10d5-107">.NET Core için araçdaki en büyük değişiklik, Project [. JSON 'dan csproj 'a](https://devblogs.microsoft.com/dotnet/changes-to-project-json/) proje sistemi olarak geçilendir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-107">The biggest change in the tooling for .NET Core is certainly the [move away from project.json to csproj](https://devblogs.microsoft.com/dotnet/changes-to-project-json/) as the project system.</span></span> <span data-ttu-id="d10d5-108">Komut satırı araçlarının en son sürümleri *Project. JSON* dosyalarını desteklemez.</span><span class="sxs-lookup"><span data-stu-id="d10d5-108">The latest versions of the command-line tools don't support *project.json* files.</span></span> <span data-ttu-id="d10d5-109">Bu, Project. JSON tabanlı uygulamaları ve kitaplıkları derlemek, çalıştırmak veya yayımlamak için kullanılamayan anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-109">That means that it cannot be used to build, run or publish project.json based applications and libraries.</span></span> <span data-ttu-id="d10d5-110">Araçların bu sürümünü kullanabilmeniz için mevcut projelerinizi geçirmeniz veya yenilerini başlatmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-110">In order to use this version of the tools, you will need to migrate your existing projects or start new ones.</span></span> 

<span data-ttu-id="d10d5-111">Bu taşımanın bir parçası olarak, Project. JSON projelerini derlemek için geliştirilmiş olan özel derleme altyapısı, aynı şekilde [MSBuild](https://github.com/Microsoft/msbuild)adlı ve tam özellikli bir yapı altyapısı ile değiştirilmiştir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-111">As part of this move, the custom build engine that was developed to build project.json projects was replaced with a mature and fully capable build engine called [MSBuild](https://github.com/Microsoft/msbuild).</span></span> <span data-ttu-id="d10d5-112">MSBuild, platformun ilk sürümünden bu yana önemli bir teknoloji olduğundan, .NET Community 'de iyi bilinen bir altyapıdır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-112">MSBuild is a well-known engine in the .NET community, since it has been a key technology since the platform's first release.</span></span> <span data-ttu-id="d10d5-113">Kuşkusuz, .NET Core uygulamaları oluşturması gerektiğinden, MSBuild .NET Core 'a eklenmiştir ve .NET Core 'un üzerinde çalıştığı tüm platformlarda kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-113">Of course, because it needs to build .NET Core applications, MSBuild has been ported to .NET Core and can be used on any platform that .NET Core runs on.</span></span> <span data-ttu-id="d10d5-114">.NET Core 'un asıl adedlerinden biri platformlar arası bir geliştirme yığınıdır ve bu taşımanın bu taahhüdünü bozmadığından emin yaptık.</span><span class="sxs-lookup"><span data-stu-id="d10d5-114">One of the main promises of .NET Core is that of a cross-platform development stack, and we have made sure that this move does not break that promise.</span></span>

> [!NOTE]
> <span data-ttu-id="d10d5-115">MSBuild 'e yeni başladıysanız ve bu konuda daha fazla bilgi edinmek istiyorsanız, [MSBuild kavramlarını](/visualstudio/msbuild/msbuild-concepts) okuyun makalesini okuyun.</span><span class="sxs-lookup"><span data-stu-id="d10d5-115">If you are new to MSBuild and would like to learn more about it, you can start by reading the [MSBuild Concepts](/visualstudio/msbuild/msbuild-concepts) article.</span></span> 

## <a name="the-tooling-layers"></a><span data-ttu-id="d10d5-116">Araç katmanları</span><span class="sxs-lookup"><span data-stu-id="d10d5-116">The tooling layers</span></span>

<span data-ttu-id="d10d5-117">Var olan proje sisteminin yanı sıra altyapı anahtarları oluşturma ile, doğal olarak takip eden soru, bu değişikliklerden herhangi biri, tüm .NET Core araçları ekosisteminin genel "katmanlama" düzeyini değiştirir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-117">With the move away from the existing project system as well as with building engine switches, the question that naturally follows is do any of these changes change the overall "layering" of the whole .NET Core tooling ecosystem?</span></span> <span data-ttu-id="d10d5-118">Yeni BITS ve bileşenler var mı?</span><span class="sxs-lookup"><span data-stu-id="d10d5-118">Are there new bits and components?</span></span>

<span data-ttu-id="d10d5-119">Aşağıdaki resimde gösterildiği gibi Önizleme 2 katmanlarındaki hızlı yenileyici ile başlayalım:</span><span class="sxs-lookup"><span data-stu-id="d10d5-119">Let's start with a quick refresher on Preview 2 layering as shown in the following picture:</span></span>

![Preview 2 araçları üst düzey mimari](media/cli-msbuild-architecture/p2-arch.png)

<span data-ttu-id="d10d5-121">Araçların katmanlama işlemi oldukça basittir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-121">The layering of the tools is quite simple.</span></span> <span data-ttu-id="d10d5-122">En altta, temel olarak .NET Core komut satırı araçları vardır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-122">At the bottom we have the .NET Core Command Line tools as a foundation.</span></span> <span data-ttu-id="d10d5-123">Visual Studio veya Visual Studio Code gibi diğer tüm diğer, daha üst düzey araçlar, projeleri oluşturmak, bağımlılıkları geri yüklemek ve daha fazlasını yapmak için CLı 'ye bağımlıdır ve kullanır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-123">All other, higher-level tools such as Visual Studio or Visual Studio Code, depend and rely on the CLI to build projects, restore dependencies and so on.</span></span> <span data-ttu-id="d10d5-124">Bu, örneğin, Visual Studio 'Nun geri yükleme işlemi gerçekleştirmesini istediği anlamına gelir. Bu, CLı 'daki `dotnet restore` (bkz.[Note](#dotnet-restore-note)) komutuna çağrı yapar.</span><span class="sxs-lookup"><span data-stu-id="d10d5-124">This meant that, for example, if Visual Studio wanted to perform a restore operation, it would call into `dotnet restore` ([see note](#dotnet-restore-note)) command in the CLI.</span></span> 

<span data-ttu-id="d10d5-125">Yeni proje sistemine taşıma ile önceki diyagramda değişiklik yapılır:</span><span class="sxs-lookup"><span data-stu-id="d10d5-125">With the move to the new project system, the previous diagram changes:</span></span> 

![.NET Core SDK 1.0.0 üst düzey mimari](media/cli-msbuild-architecture/p3-arch.png)

<span data-ttu-id="d10d5-127">Temel fark, CLı 'nin temel katman olmadığı bir şekilde değildir; Bu rol artık "paylaşılan SDK bileşeni" tarafından doldurulmuştur.</span><span class="sxs-lookup"><span data-stu-id="d10d5-127">The main difference is that the CLI is not the foundational layer anymore; this role is now filled by the "shared SDK component".</span></span> <span data-ttu-id="d10d5-128">Bu paylaşılan SDK bileşeni, kodunuzun derlenmesi, yayımlanması, NuGet paketleri ve paketlemesinden sorumlu bir hedef ve ilişkili görev kümesidir. SDK 'nın kendisi açık kaynaktır ve [SDK](https://github.com/dotnet/sdk)deposunda GitHub 'da kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-128">This shared SDK component is a set of targets and associated tasks that are responsible for compiling your code, publishing it, packing NuGet packages etc. The SDK itself is open-source and is available on GitHub on the [SDK repo](https://github.com/dotnet/sdk).</span></span> 

> [!NOTE]
> <span data-ttu-id="d10d5-129">"Target", MSBuild 'in çağırabileceği adlandırılmış bir işlemi gösteren bir MSBuild terimidir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-129">A "target" is a MSBuild term that indicates a named operation that MSBuild can invoke.</span></span> <span data-ttu-id="d10d5-130">Genellikle hedefin yapması gereken bazı mantığı çalıştıran bir veya daha fazla görevle birlikte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-130">It is usually coupled with one or more tasks that execute some logic that the target is supposed to do.</span></span> <span data-ttu-id="d10d5-131">MSBuild, `Copy` veya `Execute` gibi çok sayıda kullanıma yönelik hedefi destekler; Ayrıca, kullanıcıların yönetilen kod kullanarak kendi görevlerini yazmasına ve bu görevleri yürütmek için hedefleri tanımlamasına olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-131">MSBuild supports many ready-made targets such as `Copy` or `Execute`; it also allows users to write their own tasks using managed code and define targets to execute those tasks.</span></span> <span data-ttu-id="d10d5-132">Daha fazla bilgi için bkz. [MSBuild görevleri](/visualstudio/msbuild/msbuild-tasks).</span><span class="sxs-lookup"><span data-stu-id="d10d5-132">For more information, see [MSBuild tasks](/visualstudio/msbuild/msbuild-tasks).</span></span> 

<span data-ttu-id="d10d5-133">Tüm araç takımları 'ler artık paylaşılan SDK bileşenini ve bunların hedeflerini kullanır ve CLI dahildir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-133">All the toolsets now consume the shared SDK component and its targets, CLI included.</span></span> <span data-ttu-id="d10d5-134">Örneğin, Visual Studio 'nun bir sonraki sürümü, .NET Core projelerinin bağımlılıklarını geri yüklemek için `dotnet restore` ([bkz. Not](#dotnet-restore-note)) komutuna çağırmayacak, "Restore" hedefini doğrudan kullanacaktır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-134">For example, the next version of Visual Studio will not call into `dotnet restore` ([see note](#dotnet-restore-note)) command to restore dependencies for .NET Core projects, it will use the "Restore" target directly.</span></span> <span data-ttu-id="d10d5-135">Bunlar MSBuild hedefleri olduğundan, [DotNet MSBuild](dotnet-msbuild.md) komutunu kullanarak bunları yürütmek Için ham MSBuild 'i de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d10d5-135">Since these are MSBuild targets, you can also use raw MSBuild to execute them using the [dotnet msbuild](dotnet-msbuild.md) command.</span></span> 

### <a name="cli-commands"></a><span data-ttu-id="d10d5-136">CLı komutları</span><span class="sxs-lookup"><span data-stu-id="d10d5-136">CLI commands</span></span>
<span data-ttu-id="d10d5-137">Paylaşılan SDK bileşeni, var olan CLı komutlarının büyük çoğunluğunun MSBuild görevi ve hedefleri olarak yeniden uygulandığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="d10d5-137">The shared SDK component means that the majority of existing CLI commands have been re-implemented as MSBuild tasks and targets.</span></span> <span data-ttu-id="d10d5-138">Bu, CLı komutları ve araç takımını kullanımınız için ne anlama geliyor?</span><span class="sxs-lookup"><span data-stu-id="d10d5-138">What does this mean for the CLI commands and your usage of the toolset?</span></span> 

<span data-ttu-id="d10d5-139">Kullanım perspektifinden, CLı 'yı kullanma yöntemini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="d10d5-139">From an usage perspective, it doesn't change the way you use the CLI.</span></span> <span data-ttu-id="d10d5-140">CLı, Preview 2 sürümünde var olan temel komutlara hala sahiptir:</span><span class="sxs-lookup"><span data-stu-id="d10d5-140">The CLI still has the core commands that exist in Preview 2 release:</span></span>

- `new`
- `restore`
- `run` 
- `build`
- `publish`
- `test`
- `pack` 

<span data-ttu-id="d10d5-141">Bu komutlar, bunların ne olduğunu beklediğinizi (bir proje oluşturun, oluşturun, yayımlayın, bu şekilde paketleyebilir).</span><span class="sxs-lookup"><span data-stu-id="d10d5-141">These commands still do what you expect them to do (new up a project, build it, publish it, pack it and so on).</span></span> <span data-ttu-id="d10d5-142">Seçeneklerin büyük bölümü değiştirilmez ve hâlâ orada bulunur ve herhangi bir değişikliği öğrenmek için bu sitedeki komutlara (`dotnet <command> --help` kullanarak) ya da belgelere başvurabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="d10d5-142">Majority of the options are not changed, and are still there, and you can consult either the commands' help screens (using `dotnet <command> --help`) or documentation on this site to get familiar with any changes.</span></span> 

<span data-ttu-id="d10d5-143">Bir yürütme perspektifinden, CLı komutları parametrelerini alır ve gerekli özellikleri ayarlanacak ve istenen hedefi çalıştıran "RAW" MSBuild çağrısı oluşturacak.</span><span class="sxs-lookup"><span data-stu-id="d10d5-143">From an execution perspective, the CLI commands will take their parameters and construct a call to "raw" MSBuild that will set the needed properties and run the desired target.</span></span> <span data-ttu-id="d10d5-144">Bunu daha iyi anlamak için aşağıdaki komutu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="d10d5-144">To better illustrate this, consider the following command:</span></span> 

   ```dotnetcli
   dotnet publish -o pub -c Release
   ```
    
<span data-ttu-id="d10d5-145">Bu komut, bir uygulamayı bir `pub` klasöre yayımlarken "yayın" yapılandırması kullanılarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-145">This command is publishing an application into a `pub` folder using the "Release" configuration.</span></span> <span data-ttu-id="d10d5-146">Dahili olarak, bu komut aşağıdaki MSBuild çağrısına çevrilir:</span><span class="sxs-lookup"><span data-stu-id="d10d5-146">Internally, this command gets translated into the following MSBuild invocation:</span></span> 

   ```dotnetcli
   dotnet msbuild -t:Publish -p:OutputPath=pub -p:Configuration=Release
   ```

<span data-ttu-id="d10d5-147">Bu kuralın önemli özel durumu, MSBuild hedefleri olarak uygulanmadığından `new` ve `run` komutlardır.</span><span class="sxs-lookup"><span data-stu-id="d10d5-147">The notable exception to this rule are `new` and `run` commands, as they have not been implemented as MSBuild targets.</span></span>

<a name="dotnet-restore-note"></a>  
[!INCLUDE[DotNet Restore Note](~/includes/dotnet-restore-note.md)]
